# makanankucing_my — Category Context

> Custom (non-NIQ) source: `sincere-hearth-273704.makanankucing.9_makanankucing_my_daily`
> (daily grain summed to monthly). Not a `master_clean_niq` table — different
> schema, no `model_id`/`month`/`merchant_badge`-driven official-store data in the form other
> categories rely on. Taxonomy state is keyed under `master_table = 'makanankucing_my'`, not the
> source table name.
>
> **Platform scope (updated 2026-07-28):** originally Shopee Malaysia only. As of the 2026-07-28
> top-up session, `ecommerce_platform='Tiktok'` rows are also IN SCOPE — this is the first category
> to mix platforms under one `master_table`. `product_taxonomy_map` is keyed on `(product_id,
> platform, country)` precisely so Shopee and Tiktok products never collide. **Known, accepted data
> issue:** Tiktok rows' `price`/`gmv_daily` figures in this source table are corrupted by an
> inconsistent scale factor and mixed daily/monthly grain — the 95%-cumulative-GMV worklist ordering
> and `gmv_monthly` figures are unreliable for Tiktok rows specifically (values run ~1000x too high).
> This is a separate upstream data-pipeline problem, out of scope for taxonomy sessions to fix —
> proceed with extraction using the worklist as given.

---

## Status

| Field | Value |
|-------|-------|
| LLM Pass | ✅ Complete (Full Rebuild, first run) + 🔶 Top-up coverage pass (2026-07-23) + 🔶 second top-up coverage pass (2026-07-23) + 🔶 third top-up coverage pass (2026-07-28, Tiktok scope added) + 🔶 fourth top-up coverage pass (2026-07-28) + 🔶 fifth top-up coverage pass (2026-07-28) + 🔶 sixth top-up coverage pass (2026-07-28) + 🔶 seventh top-up coverage pass (2026-07-28) + 🔶 eighth top-up coverage pass (2026-07-28) + 🔶 ninth top-up coverage pass (2026-07-28, this session, partial — 244 products still gap) |
| GMV Coverage | 86.4% as of Full Rebuild; see prior QA History rows for the 2026-07-23 and third-top-up (2026-07-28) sessions. Fourth top-up's live worklist was 398 products (all Tiktok), resolved 101. Fifth top-up's live worklist was 297, resolved 11. Sixth top-up's live worklist was 286, resolved 14. Seventh top-up's live worklist was 272, resolved 9. Eighth top-up's live worklist was 263, resolved 1. This session's live worklist was 261 (all Tiktok), resolved 17 |
| Last run | 2026-07-28 (ninth top-up coverage session, this session) |
| Current MAX taxonomy_id (this category) | SKU-194586 |
| SKU blocks this category has claimed | SKU-145586–147585 (Full Rebuild, 1,554 used) · SKU-155023–155717 (top-up 1, 77 used) · SKU-157403–157956 (top-up 2, 53 used) · SKU-191043–192543 (top-up 3, 151 used) · SKU-192544–192941 (top-up 4, 63 used) · SKU-193342–193361 (top-up 5, 5 used) · SKU-193362–193647 (top-up 6, 9 used) · SKU-193848–194119 (top-up 7, 4 used) · SKU-194320–194582 (top-up 8, 1 used) · SKU-194583–194843 (top-up 9, this session, 4 used) |

---

## SKU Blocks Assigned

| Block | Usage |
|-------|-------|
| SKU-145586–SKU-147585 | Claimed block (2,000 slots, `sku_block_registry`, scenario `custom_full_rebuild`) |
| SKU-145586–SKU-147139 | Actually used (1,554 taxonomy entries) — 435 slots left unused in the block |
| SKU-155023–SKU-155717 | Claimed block (695 slots, `sku_block_registry`, scenario `custom_topup`, 2026-07-23) |
| SKU-155023–SKU-155099 | Actually used (77 new taxonomy entries) — 618 slots left unused in the block |
| SKU-157403–SKU-157956 | Claimed block (554 slots, `sku_block_registry`, scenario `custom_topup`, 2026-07-23, second top-up) |
| SKU-157403–SKU-157455 | Actually used (53 new taxonomy entries) — 501 slots left unused in the block |
| SKU-191043–SKU-192543 | Claimed block (1,501 slots, `sku_block_registry`, scenario `custom_topup`, 2026-07-28, third top-up — Tiktok scope added) |
| SKU-191043–SKU-191193 | Actually used (151 new taxonomy entries) — 1,350 slots left unused in the block |
| SKU-192544–SKU-192941 | Claimed block (398 slots, `sku_block_registry`, scenario `custom_topup`, 2026-07-28, fourth top-up) |
| SKU-192544–SKU-192606 | Actually used (63 new taxonomy entries) — 335 slots left unused in the block |
| SKU-193342–SKU-193361 | Claimed block (20 slots, `sku_block_registry`, scenario `custom_topup`, 2026-07-28, fifth top-up — see QA History for why this block isn't a round 297/200/2000; the registry's own missing-row bug meant the naive 297-slot claim collided with fourth top-up's already-written range) |
| SKU-193342–SKU-193346 | Actually used (5 new taxonomy entries) — 15 slots left unused in the block |
| SKU-193362–SKU-193647 | Claimed block (286 slots, `sku_block_registry`, scenario `custom_topup`, 2026-07-28, sixth top-up — registry verified clean before claim, no repeat of the fifth top-up's missing-row bug) |
| SKU-193362–SKU-193370 | Actually used (9 new taxonomy entries) — 277 slots left unused in the block |
| SKU-193848–SKU-194119 | Claimed block (272 slots, `sku_block_registry`, scenario `custom_topup`, 2026-07-28, seventh top-up — registry cross-checked clean before claim: `MAX(block_end)`=193847 vs global `product_taxonomy` `MAX(taxonomy_id)`=SKU-193652, no missing-row gap) |
| SKU-193848–SKU-193851 | Actually used (4 new taxonomy entries) — 268 slots left unused in the block |
| SKU-194320–SKU-194582 | Claimed block (263 slots, `sku_block_registry`, scenario `custom_topup`, 2026-07-28, eighth top-up, this session — registry cross-checked clean before claim: `MAX(block_end)`=194319 vs global `product_taxonomy` `MAX(taxonomy_id)`=SKU-194121, no missing-row gap; claimed range independently re-verified to contain 0 existing `product_taxonomy` rows immediately before insert) |
| SKU-194320 | Actually used (1 new taxonomy entry) — 262 slots left unused in the block |

## Brand IDs Assigned

| Range | Usage |
|-------|-------|
| BRD-MY-01070–BRD-MY-01127 | 58 new local/house brands minted in the Full Rebuild session |
| BRD-MY-01169–BRD-MY-01184 | 16 new local/house brands minted in the 2026-07-23 top-up session, after confirming the other 21 initially-assumed-new brands already existed in the *global* `brand_dict` under other markets/categories (see top-up QA History rows for the full reuse map, incl. routing "Advance" products to the existing `BRD-MY-01072` "Advance Veterinary") |
| BRD-MY-01187–BRD-MY-01214 | 28 new local/house brands minted in the second 2026-07-23 top-up session, after a global `brand_dict` check confirmed several other candidates (Hi-Cubs/喜崽, PETSEE, Carry's, Chun Fu, Prochoice, Partner, Kucinta, Rich.Co, Proud Holistic, NurturePro, Gewuan, Legendsandy, Felix, Meow, BOSSKU, Wono) already existed globally and were reused instead of re-minted. `Hegen` (the pet-treat listings, distinct from the pre-existing SG baby-bottle brand `BRD-SG-00702 Hegen`) was evaluated but left unmapped this session — its products' per-unit size could not be confirmed even from the product image, so no taxonomy entry was created pending a future size-resolution pass |
| BRD-MY-01230–BRD-MY-01247 | 18 new local/house brands minted in the 2026-07-28 top-up session (third top-up, Tiktok scope), after a global `brand_dict` REGEXP_CONTAINS search confirmed several other candidates already existed globally and were reused instead: Kucinta (BRD-MY-01032), Deeplove (BRD-SG-00669), Midalac (BRD-MY-01219), Omega Plus (BRD-SG-05090), PUAINTA (BRD-TH-02109), Pet Record (BRD-GLOBAL-00348), my meow (BRD-SG-04971). Also reused this category's own existing entries where the Tiktok worklist's text form differed from `brand_dict`'s stored spelling: `SPACECAT` (worklist: "Space Cat"), `Si Comot` (worklist: "Comot"), `Carry's` (worklist: "CARRYS"), `SmartHeart` (worklist: "Smart Heart"), `Smartz Choice` (worklist: "SMARTCHOICE PREFECTKAT" — read as a typo'd "Smart Choice Perfect Cat"), and `A-Tier` (worklist: "Sarar" — reusing this category's own pre-existing brand_id/canonical_name mismatch from an earlier session rather than minting a second "Sarar" brand; the mismatch itself is a precision-pass fix, not resolved this session). New mints: Mivita, Public Pet, Regal Whiskers, COMEOW, GoodMew, Dr.Pikac, PAWROO, Neco, FURREVER, CAPTAIN CAT, Pets Science, WormShield, LUMeO, YESFAVOR, Petfos, Purr Lab, Aplus (MY-scoped; a same-named `BRD-TH-02250 "A Plus"` exists but wasn't reused — no verified real-world link between the TH and MY listings, same caution as the Hegen precedent above), MeowShop |
| BRD-MY-01248–BRD-MY-01283 | 36 new local/house brands minted in the 2026-07-28 fourth top-up session, against the STILL-Tiktok live worklist gap left by the third top-up session (the 6 brands `GoodMew`/`FURREVER`/`Pets Science`/`WormShield`/`LUMeO`/`Petfos` minted in that prior session — `BRD-MY-01234/01238/01240/01241/01242/01244` — had zero `product_taxonomy` entries yet; this session finally wrote taxonomy for some of them, see QA History). New mints: Ali Cat, Booster King, Bungener, Carnise, Cat and Friend, Catsome, Chahaya, DrRidz, EKO, Ezuma, Felice, Guser, Halopaws, KpsPro, Love Cat, Lusi, Meowxology, Mesti, Miko, MoeMagic, More More Please Cat, Mossy, Nobility, OHM, OMI Petshop, POTOPET, Paws & Tails, Petgo, Royal Felinn, SK Signature, Sanabelle, Simple, VetCoro, Vetmomo, WaggyWaggy, YOUMAN. Deliberately kept distinct from similarly-named existing brands rather than merged (same caution as the Hegen/Aplus precedent — no verified real-world link confirmed): `Ali Cat` vs existing `Alley Cat` (BRD-MY-01073), `Miko` vs existing `Misko` (BRD-MY-01103), `Mesti` vs existing `MASTI`/`Masti` (BRD-SG-05517), `VetCoro` vs existing `VetCo` (BRD-MY-01125). Reused existing brands via spelling-variant aliases: `FAMILY PET`→Family Pets (BRD-MY-01094), `KennyWood`→Kenny Wood (BRD-MY-01179), `RICH CO`→Rich.Co (BRD-MY-01015), `Dr.Pika`→Dr.Pikac (BRD-MY-01235), `SMAK FULLPLUS`→SMAKFULL (BRD-MY-01115), `Aristo Cat`→Aristo Cats (BRD-MY-01076), `Button And Bows`→Buttons & Bows (BRD-MY-01081), `RC`→Royal Canin (BRD-GLOBAL-00001, boundary-safe short-token match only) |
| BRD-MY-01285–BRD-MY-01287 | 3 new brands minted in the 2026-07-28 sixth top-up session (this session), all identified from product images since none of the three appeared as literal text in their sku_names: `Catto` (Premium All Life Stages Complete Food, packaging logo "Catto Premium"), `GOOD Pet Store` (Booster Freeze Dried treats — deliberately MY-scoped and not merged with the existing global `BRD-SG-01400 "Good"`, which itself has zero taxonomy entries anywhere and no verifiable real-world link, per the Hegen/Aplus caution), `Yummy Mix` (10kg dry kibble, Turkey-formulated, packaging brand "Yummy Mix" — not merged with generic `BRD-SG-08706 "Yummy"` for the same reason). Also reused two brands whose worklist sku_name text didn't literally name them but whose packaging images confirmed the brand unambiguously: `Uncle Tails` (BRD-MY-01124, existing) for a 400g×6 canned-food bundle, and `Aplus` (BRD-MY-01246, existing) for a 1kg Ocean Fish bag |

**Collision note:** `brand_dict` has no atomic claim registry equivalent to `sku_block_registry`.
A parallel session inserted 47 new `BRD-MY-*` rows (`BRD-MY-01012`–`BRD-MY-01058`) ~4 minutes before
this session's insert at the *same* IDs (both sessions independently computed `MAX+1` from a stale
read). Detected via `COUNT(*) > COUNT(DISTINCT brand_id)` in the claimed range post-insert. Fix:
this session's 58 rows were deleted and re-inserted at a freshly-verified-safe range
(`BRD-MY-01070`–`BRD-MY-01127`); the 1,554 `product_taxonomy` rows referencing the old range were
updated to the new IDs. One casualty during cleanup: both sessions had independently minted a brand
row named exactly `Josera` at the same colliding ID (`BRD-MY-01041`) — the delete-by-`(brand_id,
canonical_name)` match caught both identical rows, silently removing the other session's `Josera`
row too. Restored immediately after detection (re-inserted `BRD-MY-01041 = Josera`) to avoid leaving
a dangling FK for that other session's data. **Recommendation for future sessions on this or other
new-market categories: `brand_dict` needs the same atomic-claim mechanism `sku_block_registry`
provides for `taxonomy_id` — this collision class will recur otherwise.**

---

## Brand Scope

134 distinct brands mapped (76 reused from existing `brand_dict`, 58 newly minted as `BRD-MY-*`
local/house brands not previously seen in SG/TH data). Top 15 by GMV:

1. **Royal Canin** — `BRD-GLOBAL-00001` — 10.07M
2. **Purina ONE** — `BRD-SG-00003` — 1.90M
3. **LGD** — `BRD-MY-*` (new) — 1.82M
4. **Whiskas** — `BRD-SG-00004` — 1.75M
5. **Cindy's Recipe** — `BRD-MY-*` (new) — 1.54M
6. **ProDiet** — `BRD-MY-*` (new) — 1.31M
7. **SmartHeart** — `BRD-GLOBAL-00039` — 1.27M
8. **Reflex** (incl. "Reflex Plus" listings) — `BRD-SG-02025` — 1.24M
9. **Molly** — `BRD-MY-*` (new) — 1.06M
10. **Snappy Tom** — `BRD-MY-*` (new) — 1.04M
11. **NOTTI** — `BRD-MY-*` (new) — 0.92M
12. **SIMBA** — `BRD-MY-*` (new) — 0.91M
13. **Sheba** — `BRD-SG-00097` — 0.90M
14. **Family Pets** — `BRD-MY-*` (new) — 0.65M
15. **Brit** (incl. "Brit Care") — `BRD-SG-02158` — 0.60M

**Brand consolidation applied during extraction** (spelling/casing variants merged to one brand
before ID assignment — see `docs/product-lifecycle.md` §4.1, same real-world entity must not get two
IDs): `Dr Bites`/`Dr. Bites`→`Dr.Bites`; `Cindy's`/`Cindy's Recipe` (curly apostrophe)→`Cindy's
Recipe`; `Cindy & Friend`→`Cindy & Friends`; `Buttons and Bows`→`Buttons & Bows`; `Hill's
Prescription/Science Diet`→`Hill's`; `Maxi Cat`→`Maxi`; `PawmisedLand`→`Pawmised`; `MrVET`→`MR.VET`;
`Vital Plus`→`VitalPlus`; `TOTW`→`Taste Of The Wild` (existing brand, shorthand in listings);
`Reflex Plus`→`Reflex` (existing brand — "Plus" is a product line, not a separate brand); `Brit
Care`→`Brit` (existing brand); `Ciao Churu`/`Churu`→`Ciao` (existing brand — Churu is Ciao's
treats/paste line).

No official-store allowlist — this source table has no `merchant_badge`/official-store signal
usable the way `master_clean_niq` tables have it (see Scope note below); Pass 1 (official-store)
was skipped entirely per the session brief, going straight to bulk keyword-matched routing.

---

## Scope — What's In vs Out

**In scope:** cat dry food, cat wet food (pouch/can/tray), cat treats and freeze-dried snacks/boosters.

**Out of scope (left NULL/unmapped, 237 products, 2.99M MYR GMV):**
- Dog food of any kind (dry, wet, treats) — `dog food`, `anjing`, `puppy`, brand-specific dog lines
  (`Advance Dog`, `Nature's Protection White Dog`, `ALPS NATURAL Pureness Dog`, `Back2Nature B2N ...
  Dry Dog`, `Bravery`/`Wilderness Legend`/`Natural Core` when explicitly dog-labelled)
- Cat litter (`cat litter`, `pasir kucing`, `tofu litter`, `clumping litter`)
- Flea/tick treatment (`Frontline`, `flea`, `ubat kutu`, `earmite`) — pet care, not pet food
- Dewormer supplements (`WormShield`, "Suplemen ... Untuk Penjagaan Dalaman Daripada Cacing") — pet care/medication, not pet food, same exclusion class as flea/tick treatment (found 2026-07-28, ninth top-up session)
- Human baby formula miscategorized into the feed (`Dumex Dugro`)
- Non-pet items miscategorized into the feed (rolling papers `Gajah Duduk Pelikat`, a children's
  balance bike, building-block toys, A4 paper, a "PURINA Felix" branded shopping bag)

**Edge cases:**
- Freeze-dried "booster"/chicken-cube treats sold by many near-identical generic listings with no
  discernible brand text (`Pet Booster Freeze Dried Chicken Cube ...`) — genuinely unbranded
  commodity treats from many small resellers, not a single missed brand. Left unmapped rather than
  forced into a fabricated brand.
- `[GWP]`/`[NOT FOR SALE]` flagged rows (6 in the worklist) were still extracted normally where a
  brand/size was identifiable (e.g. `WHISKAS Pouch TastyMix ... 70g`); a "PURINA Felix Wide Shopping
  Bag" and one ambiguous "Classic 500g" listing were left unmapped (merch item / unidentifiable).

---

## Taxonomy Design Notes

**Extraction method:** bulk SQL/regex text-matching on `sku_name` (per session brief — no
merchant/official-store data to seed Pass 1). Brand identified via a ~180-entry curated keyword
dictionary (longest-match-first) built iteratively against the worklist's GMV-ranked unmatched
tail. Size and pack-count extracted via regex over `sku_name` (kg/g/ml/L units, `x{N}` and `{N}
{pouches|cans|packs|...}` multiplier patterns, `{N}unit / {M}unit` alternation → `is_multi_size`).
A small number (~10) of highest-GMV size-ambiguous listings were verified against the product image
directly (`curl` + `Read`) — confirmed sizes for Cindy's Recipe Original 24-can cartons (80g/can),
Cindy's Recipe Naturelle Holistic (7kg), Sheba pouch cartons (70g/pouch), and BEAST U36 (1.5kg, whose
`sku_name` is truncated mid-title in the source data itself, not a query artifact).

**product_line vs variant split:** where `sku_name` yields a real on-label formula/line name (e.g.
Royal Canin's `Hair & Skin Care`, `Fit 32`, `Indoor 27`; SmartHeart's `Refine`; Fancy Feast's
`Royale`), that text is written to `product_line`. Where the only differentiating text is a
flavor/recipe description (e.g. `Chicken Tuna With Milk`, `Ocean Fish Salmon Chicken Goat Milk`) —
common for house/Turkish mass-market brands (LGD, Family Pets, MISHA, Timi, Molly) that carry no
named sub-line beyond the flavor — that text is written to `variant` instead, per the food-keyword
heuristic (chicken/tuna/salmon/fish/beef/duck/lamb/milk/etc.), and `product_line` is left NULL rather
than forcing a fabricated line name. 25 of 1,554 entries (1.6%) have neither field populated — plain
`{Brand} {Size}` for brands with no distinguishing text at all in this catalog (e.g. `Reflex 15kg`,
`Snappy Tom 400g`).

**Known coverage gaps (flagged for `targeted_qa_fix.sh` / a future NULL-coverage pass, not fixed this
session):**
- 334 products (2.22M MYR GMV) — brand not identifiable from `sku_name` text alone; mostly
  single-occurrence generic/no-name listings. Left `taxonomy_id IS NULL`.
- 124 products (1.32M MYR GMV) — brand matched but size not confidently extractable from text and
  not genuinely multi-size (e.g. bulk-carton listings stating pouch *count* but not per-pouch
  *size*, like `"1 kotak Cindy's Recipe ... Wet Food"` with no gram figure anywhere in the title).
  Left `taxonomy_id IS NULL` rather than guess or force a `NULL` size onto a non-multi-size entry.
  These are good candidates for image-based resolution in a follow-up session.

**Pack-count/size regex gotchas found this session** (fixed in the extraction script, noted here in
case a future session hand-rolls similar regex against this source table):
- `"24 x 80g"` (count-before-size) vs `"80g x24"` (size-before-count) both occur; must extract the
  count from whichever side is *not* attached to a unit, not always the token immediately after `x`.
- Unit regex must handle plural forms (`70grams`, `7kgs`) and no-space runs (`80gx24pouch`) — a bare
  `\b` word-boundary after `kg`/`g` fails on both (`s`/`x` are word characters, no boundary).
- `"6.6kg / 10kg"` style listings (buyer picks one of two sizes) are multi-size seller listings, not
  a single extractable size — must be detected and routed to `is_multi_size=TRUE, size=NULL` rather
  than silently taking the first size mentioned.

---

## QA History

| Date | Pass | Finding | Resolution |
|------|------|---------|------------|
| 2026-07-23 | Full Rebuild (first run) | Confirmed 0 pre-existing `product_taxonomy_map` rows for `master_table='makanankucing_my'` — genuine first run | Proceeded per Full Rebuild scenario |
| 2026-07-23 | Full Rebuild | `brand_dict` new-ID collision with a concurrent parallel session at `BRD-MY-01012`–`01058` (no atomic claim mechanism for brand IDs, unlike `sku_block_registry` for `taxonomy_id`) | Relocated this session's 58 new brand rows to `BRD-MY-01070`–`01127` (re-verified safe immediately before re-insert); updated the 1,554 affected `product_taxonomy.brand_id` references; restored one row (`BRD-MY-01041 = Josera`) accidentally deleted because both sessions had independently minted an identically-named row at the same colliding ID |
| 2026-07-23 | Full Rebuild | Self-check QA gates (G1, G2, G5, placeholder-leak, structured-fields-NULL%) — see session findings | All passed: 0 dual-mapped, 0 HUMAN+LLM coexistence (no HUMAN rows exist for this category), 0 provenance gaps, 0 placeholder leaks, 34% NULL `product_line` (well under the 50% fail threshold, and expected — many entries correctly carry the flavor in `variant` instead) |
| 2026-07-23 | Top-up coverage (custom_topup) | Re-ran the live 95%-cumulative-GMV (GWP-zeroed) worklist query rather than trusting the wrapper's pre-check number — confirmed 695 products with `taxonomy_id IS NULL`, matching the wrapper's figure exactly | Proceeded with bulk-first reuse-before-mint against this live worklist |
| 2026-07-23 | Top-up coverage | Scope classification: 252 of 695 correctly OOS (138 pure dog food, 59 non-pet merch/accessories, 24 cat litter, 17 dog-only brands e.g. Pedigree/Cesar/Greenies, 6 flea/tick treatment, 3 merch/GWP-only, 1 human baby formula); 443 in-scope. Two Korean "ISKHAN...Dog Food...Makanan Anjing" listings with a stray "Kitten" keyword (template/copy-paste artifact) were manually overridden to OOS despite matching the cat-word regex — the explicit "Dog Food"/"Makanan Anjing" phrasing outweighed the single stray word | Scope filter + 2 manual overrides |
| 2026-07-23 | Top-up coverage | **Caught before writing (self-review, prompted by an advisor consult):** a CJK-only brand alias (`蓝氏`→Legendsandy, `纯福`→Chun Fu) normalized to whitespace-only text after CJK-stripping, which then matched as a substring of *any* string containing a space — silently mislabeling 195 of 389 brand-matched rows as "Legendsandy". Found via a full manual read of all (brand, sku_name) pairs sorted by brand, not by the row-count summary, which looked plausible. Fixed by removing CJK-only alias entries (their ASCII equivalents already covered the same brands) and hardening the matcher to reject any alias that normalizes to empty/whitespace | Full re-run of brand matching from the corrected matcher before any SQL was generated — no bad rows were ever written |
| 2026-07-23 | Top-up coverage | A bare `"tom"` alias (added for a `TOM🐾`-branded product) collided with `"Snappy Tom"` (existing brand) and the substring `"sTOMach"` in unrelated sku_names | Removed the alias; the 3 genuine `TOM🐾` products (~44K MYR GMV) were left brand-unmatched rather than risk another short-token collision |
| 2026-07-23 | Top-up coverage | Brand-dedup check against the *global* `brand_dict` (not just this category's existing 1,554 entries) found 21 of an initially-assumed "37 new brands" already registered under other markets/categories: Legendsandy, Masti (MASTI), Tommy's Kitchen, Agogo, Leonardo, Schesir, Empire, Regalos, Iskhan, Urbanwolf, La Philanth, Weruva, Vet-Pro, Instinct, Chun Fu, Mumm, Petlusci, Neku, Hukii, Furrytail. "Advance Cat - Urinary/Renal/Gastro sensitive/Hypoallergenic" products were routed to the existing `BRD-MY-01072` **"Advance Veterinary"** (not a new plain "Advance" brand, and not the unrelated `BRD-ID-06375` Indonesian "Advance") since the products are explicitly the veterinary/therapeutic diet line | Reused existing `brand_id`s for all 21 — avoided minting 21 duplicate brand entities for real-world brands that already exist elsewhere in `brand_dict` (ADR-1) |
| 2026-07-23 | Top-up coverage | 16 brands confirmed genuinely new after the global check (Allando, Bonacibo, Cator, Cattylov, DogsPlus, FURPET, Felicia, Fregate, Furvit, Gewuan, Kenny Wood, LILIEN, Nutritail, SND, Smartz Choice, Ultimates) | Minted `BRD-MY-01169`–`01184` (re-verified `MAX(BRD-MY-*)` immediately before insert per the category's own documented collision risk; re-checked post-insert for duplicates — none found) |
| 2026-07-23 | Top-up coverage | Bulk-first reuse-before-mint resolved 141 of 695 live-worklist products (15 via exact `(brand,size,pack)` match to an existing taxonomy entry, 126 via 77 newly-minted entries) — 5.62M MYR of the ~9.5M in-scope worklist GMV remains unmapped: 115 brand-matched-but-size-not-extractable, 187 brand-unidentifiable-from-text (mostly the already-documented "genuinely unbranded freeze-dried booster/treat" pattern), plus the 252 correctly-OOS. Claimed SKU block `SKU-155023`–`SKU-155717` (695 slots); used `SKU-155023`–`SKU-155099` (77), 618 unused | Session ran out of reasonable turn budget for a full image-verification pass on the remaining 302 brand-or-size-ambiguous products — flagged below for a future session |
| 2026-07-23 | Top-up coverage | **Known simplification:** new entries were grouped/deduped by `(brand, size, pack_count)` only, not by product line — e.g. all "Advance Cat - Urinary" and "Advance Cat - Renal" 1.5kg listings would collapse to one entry if they shared that exact size/pack (in this run they didn't collide, but the risk exists for future brand/size overlaps). This is the coverage-over-precision tradeoff the Full Rebuild/top-up scenario calls for; a future `targeted_qa_fix.sh` precision pass should re-check each of the 77 new entries' `product_line`/`variant` split rather than assume the bulk-derived descriptor is exact | Not fixed this session — flagged for precision pass |
| 2026-07-23 | Top-up coverage | Self-check QA gates (G1, G2, G4, G5, placeholder-leak, structured-fields-NULL%) run without `--skip-coexistence` (per this being a post-initial-ship category) | All passed: 0 dual-mapped (LLM-scoped), 0 HUMAN+LLM coexistence, 0 cross-category `taxonomy_id` leakage, 0 provenance gaps, 0 placeholder-leak canonical names, 32% NULL `product_line` (existing 1,554 + 77 new combined, under the 50% threshold) |
| 2026-07-23 | Top-up coverage | **Anomaly found during post-write verification, not resolved:** re-running the exact STEP-0 live-worklist SQL (grouped by `url, image, product_id, sku_name, flag_GWP`) after the writes intermittently still listed some of this session's own newly-mapped `product_id`s as `canonical_name IS NULL`, and returned inconsistent total counts (1,085) across repeated cache-busted re-runs of the identical query. Direct verification (`product_taxonomy_map JOIN product_taxonomy` for all 141 target `product_id`s, and a simplified product_id-grain rewrite of the worklist query) both confirm all 141 writes are correct and stable, and the true remaining gap is **554 products / 5.62M MYR GMV** — matching 695−141 exactly. Root cause not identified in this session (suspect BigQuery `QUALIFY`/window-function tie-breaking instability in the finer (url,image,sku_name)-grained grouping, though no fan-out was found for the specific products checked) | Do not trust the STEP-0 query's raw row count for this table without cross-checking against a simple product_id-grain join — flagged for investigation in a future session |
| 2026-07-23 | Second top-up coverage (this session) | Re-ran the live STEP-0 worklist query directly rather than trusting the wrapper's pre-check number (per prior session's own anomaly finding) — confirmed exactly 554 products with `taxonomy_id IS NULL`, matching the wrapper's figure. Also found `/tmp/worklist.csv` and `/tmp/existing_taxonomy.csv` (generic shared filenames) got silently overwritten mid-session by other concurrent sessions running against `makanananjing_my` and `shopee_sg_pet_food` (visible live in `sku_block_registry`) | Moved all working files to a session-unique `/tmp` directory and re-generated every BQ export from scratch there before using it — a real, live collision risk this environment doesn't otherwise guard against for ad hoc file exports (only `sku_block_registry` and BigQuery itself are collision-safe) |
| 2026-07-23 | Second top-up coverage | Scope classification of the 554 live-worklist rows found and fixed several real classifier bugs before any SQL was generated: (1) `re.VERBOSE` silently stripped literal spaces from non-food phrase patterns ("condo house" → "condohouse"), so pet-bed/training-pad/toy/shampoo listings were never actually excluded; (2) a blanket dog-brand-name regex (TOTW, Addiction, Sniffly, Brit, Hill's, IAMS, Royal Canin, SmartHeart, Carnilove — all dual cat+dog brands) wrongly excluded real cat products just for containing the brand name, e.g. a genuine "Taste Of The Wild Rocky Mountain Cat Kibble" and a "Cindy's Recipe ... Sniffly ... Cat Wet Food"; (3) `\bdog\b`/`\bcat\b` word-boundary checks missed plural "Dogs"/"Cats" (e.g. "Goat Milk Powder For Dogs" was never excluded); (4) `Gajah Duduk Pelikat` (rolling papers), explicitly documented as OOS in this file's own Scope section, was never actually filtered by any pattern. Final split: 301 in-scope / 253 OOS (158 dog, 63 non-food, 24 litter, 6 flea, 1 baby formula) | Fixed all four bugs before generating any SQL; verified against this file's own documented OOS examples |
| 2026-07-23 | Second top-up coverage | Brand matching against the 301 in-scope rows found the same class of bug as scope classification: a naive substring match on normalized text (adopted to fix CJK brand names like 纯福/Chun Fu fusing with no space against adjacent Chinese text) let the alias `"n d"` (from brand `N&D`) match inside any "...protein **n** **d**og/duck..." text, since Latin words aren't separated by spaces the way tokens are padded. Fixed by requiring CJK aliases to match as plain substrings (correct — Chinese has no space delimiters) but Latin aliases to match only as a padded, space-bounded whole token | 175 of 301 in-scope rows brand-matched (127 to one of this category's 163 existing brands or a documented alias, e.g. `Reflex Plus`→Reflex, `D Zahara`→D'Zahara, `TOTW`→Taste Of The Wild; 57 to a brand new to this category — see below) |
| 2026-07-23 | Second top-up coverage | **Global `brand_dict` reuse-before-mint check** (per this file's own documented collision precedent) on all "new" brand candidates found 17 already existed elsewhere in the global dictionary: PETSEE (BRD-MY-01024), Carry's (BRD-MY-01133), Prochoice (BRD-SG-11543), Partner (BRD-MY-01157), Kucinta (BRD-MY-01032), Rich.Co (BRD-MY-01015), Proud Holistic (BRD-MY-01160), NurturePro (BRD-MY-01029), Gewuan (BRD-MY-01178, already known from this category), Legendsandy (BRD-MY-01147, already known), Hi-Cubs/喜崽 (BRD-SG-06569), Felix (BRD-GLOBAL-00230), Meow (BRD-SG-01596), BOSSKU (BRD-MY-01044), Wono (BRD-ID-07771), Chun Fu (BRD-MY-01051, already known). `Hegen` (BRD-SG-00702, SG) was deliberately **not** reused — that existing entity is a Singapore baby-bottle company, and this session's "Hegen Pet" cat-treat listings show a distinct "hegen pet" logo in-image, making them very likely a different real-world business coincidentally sharing the name; conflating them would misattribute cat treats to an unrelated baby-products brand | Reused 16 existing global `brand_id`s; did not mint or map `Hegen` this session (products left unmapped — see below) |
| 2026-07-23 | Second top-up coverage | 28 brands confirmed genuinely new after the global check (AliCat, CICI, Cat's Eye, Docile, Fish In, Food Chain, GINO, Greens, Healthy Catz, Hell's Kitchen, I Have Shanhai, MICAT, MORTITI, Manroupaipai, Masha Cat, Matisse, Mera, Mito Lamito, Muezza, Naisi, Nice Bite, PETSUP, Pet Runner, Si Comot, The Cat's Travel, Trawler, Yamagold, ZUVA). Minted `BRD-MY-01187`–`01214` (re-verified `MAX(BRD-MY-*)` immediately before the atomic-computed insert per this category's own documented collision risk; re-checked post-insert via `COUNT(*) = COUNT(DISTINCT brand_id)` — no collision, 28/28 unique) | Minted 28 new brand rows |
| 2026-07-23 | Second top-up coverage | Size/pack extraction via `sku_name` regex resolved only 57 of 175 brand-matched rows (much of this category's remaining gap is genuinely size-silent listings, consistent with the first top-up session's finding). Given the resulting ~1.47M MYR of brand-matched-but-size-unresolved GMV, image-verified the 15 highest-GMV such listings directly (`curl` + `Read`, per this file's and the runbook's documented pattern) — resolved 8 more (Nanovet Freeze Dried Combo 200g, Rosy Fresh Roast Meat 2kg, MORTITI Chicken Breast 40g in both a 50-pack and single-pack SKU, Nanovet Topper 70g×6, Cassiel Urinary 1kg, MASTI Chicken Breast 40g, Whiskas Tasty Mix 70g×24), confirmed 1 as genuine multi-brand mix unattributable to any single brand_id (Royal Canin + Purina ONE + Taste Of The Wild kibble blended into one reseller SKU — left unmapped rather than misattribute to one of the three), confirmed 1 as a non-food merch item ("PURINA Felix Wide Shopping Bag", `[NOT FOR SALE]` — matches this file's own documented merch-item precedent), and left 5 genuinely size-unresolved even from the image (per-sachet/per-stick gram weight not legible on 3 Hegen listings, 1 FURVIT Nutri Lick, 1 Amelisa Rawly Stick, 1 Pawmised Catty Flossy topper) | 65 total mappable products (6 reused an existing taxonomy entry exactly on `(brand_id, size, pack_count)`, 59 needed a new entry) |
| 2026-07-23 | Second top-up coverage | Claimed SKU block `SKU-157403`–`SKU-157956` (554 slots, atomic `DECLARE`/`BEGIN TRANSACTION` claim per the runbook). Grouped the 65 mappable products by `(brand_id, size, pack_count, is_multi_size)`: 6 exact-matched an existing taxonomy entry (reused directly, no new row), 53 groups had no existing match and got one new taxonomy entry each (covering the other 59 products) — used `SKU-157403`–`SKU-157455` (53 of 554 claimed slots), 501 unused. Two groups (Food Chain, Hell's Kitchen, both genuine "80g/170g"-style buyer-choice multi-size listings) were correctly written `is_multi_size=TRUE, size=NULL` with a real brand+descriptor name, never "Multiple Sizes" text (checked against this file's own banned-phrase precedent before writing) | 53 new `product_taxonomy` rows + 65 new `product_taxonomy_map` rows written via `bq query` DML, `meta_agent='CLAUDE_CODE'`, `source='LLM'` on every row |
| 2026-07-23 | Second top-up coverage | Self-check QA gates (G1 dual-mapped, G2 HUMAN+LLM coexistence, G4 cross-category `taxonomy_id` leakage, G5 provenance, placeholder-leak) run without `--skip-coexistence`, scoped to `master_table='makanankucing_my'` | All passed: 0 dual-mapped (LLM-scoped), 0 HUMAN+LLM coexistence, 0 out-of-range/cross-category `taxonomy_id`s (all map rows resolve to this category's own 3 claimed blocks), 0 provenance gaps, 0 placeholder-leak canonical names |
| 2026-07-23 | Second top-up coverage | **Remaining gap after this session: 489 products (554 − 65)**, breakdown: ~237 in-scope but brand-unidentifiable-from-text or size-unresolved-even-from-image (mostly the already-documented "genuinely unbranded freeze-dried booster/treat" commodity pattern, plus a handful of newly-identified-but-size-silent branded listings like Hegen/FURVIT/Amelisa/Pawmised toppers), ~253 correctly-OOS this pass (158 dog, 63 non-food/accessory, 24 litter, 6 flea/dewormer, 1 baby formula) | Not fixed this session — flagged for a future top-up or `targeted_qa_fix.sh` pass; per this session's own turn budget, did not attempt a full image-verification sweep of all ~118 remaining brand-matched-but-size-unresolved rows, only the top 15 by GMV |
| 2026-07-28 | Third top-up coverage (Tiktok scope added) | Re-ran the live STEP-0 worklist query directly rather than trusting the wrapper's 1,501 pre-check number — confirmed exactly 1,501 products with `taxonomy_id IS NULL`, matching the wrapper's figure. All 1,501 were `ecommerce_platform='Tiktok'` — Tiktok entered scope for the first time this session (prior sessions' 489-product Shopee gap dropped out of the live top-95%-cumulative-GMV window because Tiktok's inflated GMV figures now dominate the ranking; this is a side-effect of the documented Tiktok GMV data issue, not a coverage regression — those Shopee products still have `taxonomy_id IS NULL`, just outside this session's 95% window) | Proceeded with the live 1,501-product Tiktok worklist per the session brief's explicit scope decision |
| 2026-07-28 | Third top-up coverage | Scope classifier's first pass wrongly excluded ~112 genuine cat-food listings (`no_food_kw` rule required an explicit food/kibble/makanan keyword, but many listings are brand+descriptor+size only, e.g. `"Reflex Plus Adult Chicken 15kg"`, `"ROYAL CANIN HAIR & SKIN 10KG"`) — caught by manually reading the rejected bucket before generating any SQL. Also found 2 false-positive OOS matches: `"Rich Choice Holistic ... Dry Cat Food (Coat & Flea Control...)"` (flea-repelling *feature* of food, not a standalone flea treatment) and `"Meow Litter Fat Premium Cat Treat..."` (bare `litter` substring-matched a typo'd "Little Fat" booster-treat listing, not actual cat litter) | Dropped the food-keyword requirement (source table is already the cat-food category feed — default in-scope, exclude only on explicit OOS signal); tightened `flea`/`litter` patterns to require the real product bigram or absence of a food-framing keyword. Final split: 1,499 in-scope / 2 OOS (1 litter, 1 grooming/supplement product) |
| 2026-07-28 | Third top-up coverage | Brand matching: built an alias table from this category's 198 existing `brand_dict` entries + documented consolidation aliases, then a `REGEXP_CONTAINS` sweep of the *global* `brand_dict` for repeated unmatched-candidate tokens found 7 reuse hits (Kucinta, Deeplove, Midalac, Omega Plus, PUAINTA, Pet Record, my meow) plus several worklist-text-vs-brand_dict-spelling mismatches within this category's own existing brands (`SPACECAT`, `Si Comot`, `Carry's`, `SmartHeart`, `Smartz Choice`, and `A-Tier`/"Sarar" — the last reusing this category's own pre-existing brand_id/canonical_name mismatch from an earlier session for consistency rather than minting a duplicate). 1,233 of 1,499 in-scope rows (82%, 90% of in-scope GMV) brand-matched; 266 left unmatched (genuinely brand-unidentifiable from text, consistent with prior sessions' commodity-treat pattern) | Minted 18 confirmed-new brands (`BRD-MY-01230`–`01247`, re-verified `MAX(BRD-MY-*)` immediately before insert; re-checked post-insert via `COUNT(*) = COUNT(DISTINCT brand_id)` — no collision, 18/18 unique) — see Brand IDs Assigned above for the full list and reuse map |
| 2026-07-28 | Third top-up coverage | Size/pack regex (built fresh this session, not reused from a prior session's script since none is committed) had two bugs caught before writing: (1) the unit-alternation regex required a trailing "m" for gram-unit matches (`gr?a?ms?`), so bare `"g"` and `"gr"` never matched at all — silently missed slash-separated multi-size listings like `"60g/550g"`; (2) `\b` word-boundary checks after a unit fail on no-space runs like `"85gx12"` (letter→letter, no boundary) and on multi-digit numbers following a single-digit pack check (`"x 16"` — boundary check landed between "1" and "6"), both matching this file's own previously-documented gotcha class for this source table. Also caught a false-positive: `"ProDiet 8kg... (8kg/500g x 16 Packs)"` initially flagged as multi-size-alternation by the slash-pattern gate even though `500g×16=8kg` is a consistent pack breakdown, not two alternative sizes | Fixed unit alternation to include bare `g`/`gr`, replaced boundary check with a lookahead permitting no-space `x<digit>` runs, and added a pack-qualifier guard + same-unit ratio-consistency check so a "total/breakdown" pattern isn't misread as buyer-choice multi-size. Verified against multiple sample rows post-fix before generating SQL |
| 2026-07-28 | Third top-up coverage | Grouped the 1,233 brand-matched rows by `(brand_id, size, pack_count)` (concrete) or `(brand_id)` with `is_multi_size=TRUE` (40 rows, buyer-choice multi-size listings like `"1.5KG & 2KG"`, `"500GM/1KG"`) — 428 groups total, 277 exact-matched an existing taxonomy entry (reused), 151 needed a new entry. 130 rows had no extractable size (not genuinely multi-size either) and were left unmapped rather than guessed, per this file's own established precedent | Claimed SKU block `SKU-191043`–`SKU-192543` (1,501 slots, atomic `DECLARE`/`BEGIN TRANSACTION` claim). Wrote 151 new `product_taxonomy` rows (`SKU-191043`–`SKU-191193`, 1,350 slots unused) + 1,103 new `product_taxonomy_map` rows via `bq query` DML, `meta_agent='CLAUDE_CODE'`, `source='LLM'`, `platform='Tiktok'`, `country='MY'` on every row. First DML attempt for the map rows failed cleanly with 0 rows written (`confidence` column is `STRING` in this table, not `FLOAT64` as `ARCHITECTURE.md` documents, and the `country` column — part of the ADR-006 composite key — was initially omitted) — fixed and re-ran before any partial write occurred |
| 2026-07-28 | Third top-up coverage | Self-check QA gates (G1 dual-mapped, G2 HUMAN+LLM coexistence, G4 cross-category `taxonomy_id` leakage, G5 provenance, placeholder-leak), run without `--skip-coexistence`, scoped to `master_table='makanankucing_my'` | All passed: 0 dual-mapped (LLM-scoped, keyed on product_id+platform+country), 0 HUMAN+LLM coexistence, 0 out-of-range/cross-category `taxonomy_id`s (all map rows resolve to this category's own 4 claimed blocks), 0 provenance gaps, 0 placeholder-leak canonical names, 30% NULL `product_line` among LLM entries excluding multi-size catch-alls (well under the 50% threshold) |
| 2026-07-28 | Third top-up coverage | **Remaining gap after this session: 396 products** (266 brand-unidentifiable-from-text + 130 brand-matched-but-size-unresolved), all in-scope. This is a bulk-first regex-only pass (per the session brief's coverage-over-precision mandate) — no product images were read this session; `product_line`/`variant` text is regex-stripped `sku_name` remainder and retains noise on some long-tail entries (consistent with the "known simplification" already accepted for this category's prior sessions) | Not fixed this session — flagged for a future top-up (image-based resolution of the size-unresolved tail) or `targeted_qa_fix.sh` precision pass on the 151 newly-minted entries' `product_line`/`variant` split |
| 2026-07-28 | Fourth top-up coverage (this session) | Re-ran the live STEP-0 worklist query directly rather than trusting the wrapper's 398 pre-check number — confirmed exactly 398 products with `taxonomy_id IS NULL` (all `platform='Tiktok'`), matching the wrapper's figure exactly. Row count sanity-checked against `--max_rows=100000` output (399 lines incl. header) per the documented `bq query` 100-row truncation pitfall | Proceeded with the live 398-product worklist |
| 2026-07-28 | Fourth top-up coverage | Scope classifier bug caught before writing: an initial `dog_only` OOS rule matched any sku_name containing the substring `"dog food"`, which wrongly excluded ~8 genuine cat-and-dog-usable freeze-dried booster/treat listings (e.g. `"...Cat Dog Food Cat Snack Dada Ayam Makanan Kucing 40G"`) that this category's own prior sessions have consistently kept in-scope. Fixed to the same default-in-scope rule as the third top-up: exclude on `dog`/`anjing`/`puppy` signal only when **no** cat/kucing/kitten/meow/feline signal is present anywhere in the sku_name. Final split: 396 in-scope / 2 OOS (1 `[5KG] MOLASSES ... HAIWAN TERNAKAN` livestock/plant feed, 1 `Best In Show Cat Sand ... (Cat Litter) 8L`) | Fixed the dog-only rule before generating any SQL; re-verified against this file's own documented OOS examples |
| 2026-07-28 | Fourth top-up coverage | Brand matching: built an alias table from this category's 216 existing `brand_dict` entries (loaded via `existing_taxonomy` JOIN, not just the STATUS table) plus documented consolidation aliases, then added ~35 spelling-variant aliases for existing brands whose worklist text differs from `brand_dict`'s stored spelling (e.g. `KennyWood`→Kenny Wood, `Dr.Pika`→Dr.Pikac, `SMAK FULLPLUS`→SMAKFULL, `RC`→Royal Canin — see Brand IDs Assigned above for the full list). Switched all alias matching (not just short 1–3 char tokens) to a padded word/phrase-boundary regex after confirming the category's own documented `"meow"` substring-collision risk (e.g. `"Meowxology"`/`"MeowShop"` must not match bare brand `"Meow"`) applied more broadly than previously guarded. 270 of 396 in-scope rows (68%) brand-matched; 126 left unmatched, consistent with prior sessions' "genuinely unbranded freeze-dried booster/treat" commodity pattern | Minted 36 confirmed-new brands (`BRD-MY-01248`–`01283`, re-verified `MAX(BRD-MY-*)` immediately before insert; re-checked post-insert via `COUNT(*) = COUNT(DISTINCT brand_id)` — no collision, 36/36 unique) — see Brand IDs Assigned above for the full list and reuse map |
| 2026-07-28 | Fourth top-up coverage | Size/pack extraction found and fixed three bugs before writing: (1) `extract_pack`'s `x\s*(\d+)` regex captured the wrong number out of a count-before-size pattern (`"26 x 85g"` read as pack_count=85 instead of 26 — the exact `llm-extraction-rules.md` §1 gotcha this category has hit before); fixed by requiring the captured multiplier not be immediately followed by a unit. (2) A `"500g/1kg"`-style buyer-choice size alternation (product `1730162435595273582`, "RC Urinary SO Batu Karang Kucing 500g/1kg Repack Original") was silently resolved to the first value instead of being flagged unresolved-multi-size, per this file's own `"6.6kg / 10kg"` precedent; fixed by detecting 2+ distinct size values joined by `/` and returning unresolved rather than guessing. (3) A `"PICKY MEOW PremiumFit ... 1kg / 10kg (10x1kg)"` listing compounded both bugs (ambiguous size **and** a questionable brand match — "Picky Meow" read as bare brand "Meow") — excluded that one product from mapping rather than risk a bad row. Reused four established per-unit sizes from this category's own existing taxonomy for pouch/stick listings that state a unit count but not the per-unit weight (Royal Canin pouch=85g, Snappy Tom pouch=85g, Sheba pouch=70g, SmartHeart pouch=85g, Whiskas pouch=80g, JJANGX3 pouch=70g, Friskies pouch=70g, Ishtar stick=16g) — recovered 13 additional brand-matched rows this way | 105 of 270 brand-matched rows had (or were resolved to) an extractable size; 1 further excluded per the Picky Meow finding above, leaving 104 mappable |
| 2026-07-28 | Fourth top-up coverage | Reuse-before-mint grouping by `(brand_id, size, pack_count)` against this category's existing 1,835 taxonomy entries found one coincidental key collision before writing: product `1730162435595273582` (Royal Canin, 500g, pack=1) exact-matched `SKU-191136`, an existing entry whose `canonical_name` is actually a reseller-created cross-brand custom mix (`"Royal Canin DENO MIX ... + TOTW ROCKY + ... + BOOSTER CHICKEN 500g"`) stored under Royal Canin's `brand_id` — a real but unrelated product that happened to share the same `(brand,size,pack)` key. (This same product was separately dropped by the multi-size fix above, so the exclusion was double-covered, but the underlying reuse-matching gap was real and would recur for other products.) Fixed by excluding any existing-taxonomy candidate whose `canonical_name` contains `DENO`, `MIX`, or `+` from the reuse-candidate pool — those are reseller-assembled bundles, not reusable single-brand/single-line entries | 81 groups from the 104 mappable rows: 29 reused an existing taxonomy entry exactly, 63 new entries needed (covering 72 products) — see Brand IDs Assigned / SKU Blocks Assigned above |
| 2026-07-28 | Fourth top-up coverage | Claimed SKU block `SKU-192544`–`SKU-192941` (398 slots, atomic `DECLARE`/`BEGIN TRANSACTION` claim per the runbook, `MAX(block_end)` re-verified immediately before claim). Wrote 63 new `product_taxonomy` rows (`SKU-192544`–`SKU-192606`, 335 slots unused) + 101 new `product_taxonomy_map` rows via `bq query` DML, `meta_agent='CLAUDE_CODE'`, `source='LLM'`, `confidence='0.75'`, `platform='Tiktok'`, `country='MY'` on every row. Pre-insert duplicate-`product_id` check on this session's own map rows found 0 dupes before writing | 63 new `product_taxonomy` rows + 101 new `product_taxonomy_map` rows written |
| 2026-07-28 | Fourth top-up coverage | Self-check QA gates (G1 dual-mapped, G2 HUMAN+LLM coexistence, G4/G4-ish cross-category `taxonomy_id` leakage against this category's 5 claimed blocks, G5 provenance, placeholder-leak, structured-fields-NULL%), run without `--skip-coexistence`, scoped to `master_table='makanankucing_my'` | All passed: 0 dual-mapped (LLM-scoped, keyed on product_id+platform+country), 0 HUMAN+LLM coexistence, 0 out-of-range/cross-category `taxonomy_id`s, 0 provenance gaps, 0 placeholder-leak canonical names, 33% NULL `product_line` among LLM entries excluding multi-size catch-alls (well under the 50% threshold). Live worklist re-run post-write confirms remaining gap = 297 (398 − 101), matching exactly |
| 2026-07-28 | Fourth top-up coverage | **Remaining gap after this session: 297 products** (126 brand-unidentifiable-from-text + 165 brand-matched-but-size-unresolved, minus the 1 excluded ambiguous "Picky Meow" row now folded into one of those buckets), all in-scope. Bulk-first regex-only pass — no product images were read this session, consistent with this category's established coverage-over-precision approach. Six of the third-top-up session's minted-but-unused brands (`GoodMew`, `FURREVER`, `Pets Science`, `WormShield`, `LUMeO`, `Petfos`) still have zero `product_taxonomy` entries after this session too — their worklist products fell into the size-unresolved bucket, not the brand-unidentifiable one | Not fixed this session — flagged for a future top-up (image-based resolution of the size-unresolved tail, particularly for treat/stick products sold by piece-count with no stated per-unit weight) or `targeted_qa_fix.sh` precision pass on this session's 63 newly-minted entries' `product_line`/`variant` split (33% NULL, mostly plain `{Brand} {Size}` entries per this category's accepted "no distinguishing text" pattern) |
| 2026-07-28 | Fifth top-up coverage (this session) | Re-ran the live STEP-0 worklist query directly rather than trusting the wrapper's 297 pre-check number — confirmed exactly 297 products with `taxonomy_id IS NULL` (all `platform='Tiktok'`), cross-checked with an independent product_id-grain rewrite of the same query per this category's own documented anomaly caution (both agreed at 297) | Proceeded with the live 297-product worklist |
| 2026-07-28 | Fifth top-up coverage | Given the prior session's own finding that the remaining 297 is the exhausted residue of four rounds of text-matching (126 brand-unidentifiable, 165 brand-matched-but-size-unresolved), pivoted to image-led resolution per `docs/headless-runbook.md`'s documented `curl -sL -o ... then Read` pattern rather than re-running a fifth text-only sweep, which would resolve near-zero. Caught one bulk-matcher bug before writing: the single highest-GMV row (`【PAWS&TAILS】...bulu cantik mortiti - A`) matched brand "MORTITI" via a keyword-stuffed lowercase mention buried at the end of the title, when the real brand is the bracketed leading tag `【PAWS&TAILS】` — fixed the matcher to check bracketed/leading tags first, before falling through to a full-text scan (a stronger form of the routing-order rule already established for text matching in this category and `llm-extraction-rules.md` §3) | Fixed the bracket-priority bug before generating any SQL; re-verified against the corrected match |
| 2026-07-28 | Fifth top-up coverage | Image-read 17 of the highest-GMV brand-matched-but-size-unresolved candidates (all already-established brands in this category — no new brand needed this session). Resolved 6: Dr.Pikac cat stick = 15g/stick (confirmed on-package "Net Contents:15G", pack=120 from "[100 PERCUMA 20]" buy-get text), Snappy Tom pouch = 85g (already-established weight; pack=48 from "BORONG 1 KOTAK - 48 PCS" on-image text), JJANGX3 dry kibble = 1kg/bag (on-package "NET WEIGHT 1 kg"), Rich Choice dry kibble = 2kg/bag (on-package "NET WEIGHT: 2KG/4.4LBS"; the sku_name's "*FREE PREMIUM BOOSTER LICK*" is a different-product GWP freebie per `llm-extraction-rules.md` §1, not a multipack, so pack_count=1), Ciao Churu = 14g×50 sticks (sku_name explicitly states "50 Sticks", confirmed on-jar), and a `Halopaws` 80g/85g weight-tolerance range on a canned-food listing resolved to 85g (larger value per this file's own `"6.6kg / 10kg"`-range precedent, applied here to a genuine ±5g labeling-tolerance case rather than a real buyer-choice alternative — distinguished by ratio: same-unit values within 25% of each other are a tolerance range, not a real size choice) | 11 of 297 products newly resolvable (Ishtar's already-established 16g stick weight matched 2 of these 11 via existing regex, no new image read needed for those) |
| 2026-07-28 | Fifth top-up coverage | 11 image/text-verified candidates left deliberately unresolved rather than guessed: `【PAWS&TAILS】` 30/60pcs listing (co-branded "HALOPAWS X PAWS & TAILS" with a third on-package name "PAWSOME" — 3 plausible brand names, multiple pack sizes and formats in one listing, genuinely ambiguous), "Meow Little Fat" and "PetGo Chicken Breast" (both stated per-unit weight but an explicit "50 PCS / 100 PCS" buyer-selectable pack-count range with no way to tell which the buyer received — same `"[5/15 packs]"` SKIP precedent already documented for `th_drinking_water` in `llm-extraction-rules.md`), OMI Petshop kibble topper and Furvit Nutri Lick (no legible weight anywhere on-package or in text; Furvit specifically matches this category's own second-top-up-session finding on the same brand), MORTITI chicken cube (label text too small/cropped to confirm a specific gram figure, not risking a fabricated number), Ishtar "[LIVE PROMO]" stick listing, Booster King freeze-dried chicken, Dr.Pika "5 Perisa" stick listing, and 2 Pawmised Barf+ listings (all four: per-unit weight or format confirmed but no pack-count signal anywhere in text or image — a treat pack sold with zero legible quantity is not safely defaulted to `pack_count=1`) | Left `taxonomy_id IS NULL` — candidates for a future session with page-level (not just cover-image) access |
| 2026-07-28 | Fifth top-up coverage | Reuse-before-mint check against this category's 1,898 existing taxonomy entries found exact `(brand_id, size, pack_count)` matches for 4 of the 11 resolvable products (Snappy Tom 85g×48 → `SKU-146310`, Rich Choice 2kg → `SKU-146474`, Ciao 14g×50 → `SKU-145891`, Ishtar 16g×1 → `SKU-146059`, the last covering both Ishtar rows) — reused directly, no new entry. All 4 keys had multiple duplicate-looking existing entries already in this category (a pre-existing messiness not introduced or fixed this session); picked the closest-matching existing `canonical_name` in each case rather than adding a 4th/5th near-duplicate | 5 products mapped via reuse, 0 new taxonomy rows for these |
| 2026-07-28 | Fifth top-up coverage | **Registry bug found and fixed before any taxonomy write**: the first atomic block claim (`DECLARE`/`BEGIN TRANSACTION` per the runbook) computed `next_start` from `COALESCE(MAX(block_end),...)` and landed at `192544`–`192941` — identical to the fourth top-up session's already-used range, which has 63 real `product_taxonomy` rows (`SKU-192544`–`SKU-192606`) already in it. What was actually observed: the registry had no row at all for that range, so `MAX(block_end)` for this category was stale at `192543` — the inference that the fourth top-up session simply never wrote its claim row is the most likely explanation but was not independently confirmed (the row could equally have been deleted by something else; no evidence either way was checked). Caught by cross-checking the freshly-claimed range against `product_taxonomy` row counts before writing anything — found 63 existing rows in the "empty" claimed range. Fix: deleted the bad claim row, backfilled a corrective registry row for the fourth top-up's actual `192544`–`192941` range **using a fabricated `claimed_at` timestamp (`2026-07-28 06:00:00`, chosen only to sort before this session's own claim)** — this is a synthetic reconstruction, not a recovered original value; anyone auditing `sku_block_registry` later should not treat that row's `claimed_at` as real provenance. Then re-claimed fresh — landed at `193342`–`193361` (the gap between `192941` and `193342` reflects a *different* category's parallel claim in between, confirmed via the registry's other rows, not a further bug) | Registry corrected (with one synthetic-timestamp caveat above); claimed a verified-clean 20-slot block (`SKU-193342`–`193361`) sized to this session's actual 5 new entries, not the prompt's literal 297, since 297 was never going to be the count of *new mints* — see `docs/headless-runbook.md`'s Shared Mechanics section, which itself flags `sku_block_registry` has no equivalent atomic-claim protection issue for `brand_dict` but doesn't call out that a *missing* registry row for a real claim is possible; flagging that gap for a future runbook update |
| 2026-07-28 | Fifth top-up coverage | Wrote 5 new `product_taxonomy` rows (`SKU-193342`–`SKU-193346`) + 11 new `product_taxonomy_map` rows via `bq query` DML, `meta_agent='CLAUDE_CODE'`, `source='LLM'`, `confidence='0.75'`–`'0.8'` (0.8 for exact reuse-matches, 0.75 for new mints), `platform='Tiktok'`, `country='MY'` on every row. Direct join verification against all 11 target `product_id`s confirmed correct `taxonomy_id`/`canonical_name` before proceeding to QA gates | 5 new `product_taxonomy` rows + 11 new `product_taxonomy_map` rows written |
| 2026-07-28 | Fifth top-up coverage | Self-check QA gates (G1 dual-mapped LLM-scoped, G2 HUMAN+LLM coexistence, G4-ish cross-category/out-of-range `taxonomy_id` leakage against this category's 6 claimed blocks, G5 provenance, placeholder-leak, structured-fields-NULL%), run without `--skip-coexistence`, scoped to `master_table='makanankucing_my'` | All passed: 0 dual-mapped, 0 HUMAN+LLM coexistence, 0 out-of-range/cross-category `taxonomy_id`s, 0 provenance gaps, 0 placeholder-leak canonical names, 33% NULL `product_line` among LLM entries excluding multi-size catch-alls (unchanged from fourth top-up, well under the 50% threshold). Live worklist re-run post-write confirms remaining gap = 286 (297 − 11), matching exactly |
| 2026-07-28 | Fifth top-up coverage | **Remaining gap after this session: 286 products.** Image-led resolution recovered 11 of 297 (3.7%) — much lower yield per-row than the bulk text-matching passes (101/65/141), as expected once a category is down to its text-exhausted residue; most of the remaining 286 need either full product-page access (variant/quantity selectors aren't visible in a single cover image) or are genuinely unbranded commodity treats consistent with this category's long-established pattern | Not fixed this session — flagged: a future session with page-level (not cover-image-only) access would likely resolve most of the 40 bundle/combo/DENO-MIX-excluded and pack-count-ambiguous listings identified this session; the remaining brand-unidentifiable tail (126 products) is the same genuinely-unbranded-commodity-treat population documented across every prior session |
| 2026-07-28 | Sixth top-up coverage (this session) | STEP-0's literal query as given in the session prompt failed outright: `SELECT ... flag_GWP ... GROUP BY 1,2,3,4,5` groups by only 5 columns but `flag_GWP` (a derived boolean, not an aggregate) is SELECT column 6 — BigQuery correctly rejected it (`flag_GWP ... neither grouped nor aggregated`). Not a data issue, a bug in the prompt's own copy of the query | Fixed to `GROUP BY 1,2,3,4,5,6` before running anything; re-ran and got exactly 286 products with `taxonomy_id IS NULL`, matching the wrapper's pre-check figure |
| 2026-07-28 | Sixth top-up coverage | Addressed the gap explicitly flagged at the end of the fifth top-up session ("this session ... never ran an in-scope/OOS classification pass ... a future session needs to run the scope classifier before trusting any in-scope gap figure"): built a Python scope classifier from this file's own documented dog/litter/flea/baby-formula/non-pet/livestock rules and ran it over all 286. Result: **284 in-scope, 2 OOS** (1 cat-litter, 1 livestock/molasses feed) — same 2 OOS products the third/fourth top-up sessions already found at this same live-worklist boundary, confirming the classifier against this file's own documented precedent rather than trusting it blind | Scope classified before any brand/size work; corrects the "unclassified mix" caveat left by the fifth top-up session |
| 2026-07-28 | Sixth top-up coverage | Built a bulk brand matcher from this category's 254 existing `brand_dict` entries (via existing taxonomy) plus documented consolidation aliases. Caught and fixed two collision bugs **before generating any SQL**, both instances of failure classes this file has hit before: (1) a bracket/leading-tag priority check used loose substring containment (`"meo" in "lumeo"`, `"meo" in "limitedtimeoffer"`) and silently mis-tagged two unrelated listings as brand `"ME-O"` — tightened to exact compact-string equality, which also correctly re-confirmed the already-documented `【PAWS&TAILS】`→`Paws & Tails` bracket-priority fix from the fifth top-up session; (2) a naive "any brand token matches → ambiguous multi-brand" rule wrongly flagged 4 genuine single-brand `PURINA ONE ...` bundle listings as ambiguous just because the substring `PURINA` (a separate, shorter existing brand entry) also matched — fixed by collapsing any matched brand whose compact form is a substring of another matched brand's compact form (Decision 12: specific over generic) before deciding ambiguity. After both fixes: 95 of 284 in-scope rows brand-matched cleanly, 10 correctly left ambiguous (mostly genuine reseller `DENO MIX`/`+`-joined cross-brand blends, matching this file's own established DENO-MIX exclusion precedent from the fourth top-up session), 179 brand-unidentifiable from text alone | Fixed both bugs before writing; re-verified the `PAWS&TAILS` and `PURINA ONE` groups by hand after the fix |
| 2026-07-28 | Sixth top-up coverage | Cross-checked this session's 95 brand-matched rows against this category's full `BRD-MY-01070`–`01283` minted-brand range (not just brands with existing `product_taxonomy` rows) and found `Omega Plus` (`BRD-SG-05090`, reused as an alias in the third top-up session) plus the six brands the fourth/fifth top-up sessions already flagged as still having **zero `product_taxonomy` entries** (`GoodMew`, `FURREVER`, `Pets Science`, `WormShield`, `LUMeO`, `Petfos`) were entirely missing from a brand list built only from existing taxonomy joins — the same class of gap the fourth top-up session's own note described. Added all 7 to the matcher, recovering 10 more brand-matched rows (105 total) | Brand list built from `product_taxonomy` joins alone is provably incomplete for this category — a future session should build it from the full documented `BRD-MY-*` range instead |
| 2026-07-28 | Sixth top-up coverage | Bulk regex size/pack extraction against the 105 brand-matched rows resolved only 3 directly from text (`Kucinta 400g`, two multi-size slash patterns) — confirms this category's long-established finding that the text-exhausted residue is overwhelmingly size-silent in the title. One additional row (`Ciao Churu 50pcs`) matched an established per-unit weight (Ciao stick = 14g, from the fifth top-up session's own image-verified finding) and exact-matched existing entry `SKU-145891` ("Ciao Treats 14g x50") — reused directly, no new taxonomy row | 4 of 105 resolved from text/established-weight alone before any image work |
| 2026-07-28 | Sixth top-up coverage | Image-verified 25 of the highest-GMV remaining candidates (9 for the 6 gap-brands + Omega Plus, 4 unresolved Royal Canin dry-food bags, 1 JJANGX3 nested-pack listing, 11 brand-unmatched high-GMV rows), all via the documented `curl -sL` + `Read` pattern, downloaded in parallel. Resolved 10 more: **Royal Canin** Hair & Skin 2kg (exact-matched existing `SKU-146865`), Adult British Shorthair 10kg (`SKU-146408`), Urinary Care 10kg (`SKU-145695`) — all three sizes were printed cleanly on a `pets.twentyone` reseller card layered onto the product photo, no ambiguity; **JJANGX3** "5+1 ... x8 packs" confirmed on-box as 6 pouches/box (5+1) × 8 boxes = 48 × 70g (JJANGX3's established pouch weight), exact-matched existing `SKU-147032` ("Pouch Packs 70g x48"); **GoodMew** Freeze Dried Chicken Cube confirmed "Nett Weight 500g" on-bag, pack=1 (new entry, GoodMew's first-ever `product_taxonomy` row, closing the gap the fourth/fifth top-up sessions both flagged); **Friskies** "Wet Bundle of 18" resolved via Friskies' own established 70g pouch weight (fourth top-up session), pack=18 explicit in title (new entry) | 5 exact-matched existing entries (no new taxonomy row), 2 new entries |
| 2026-07-28 | Sixth top-up coverage | Of the 6 previously-orphaned gap-brands (`GoodMew` resolved above) and `Omega Plus`, the other 5 (`FURREVER` ×3, `WormShield`, `Pets Science`, `LUMeO` ×2, `Omega Plus`) had legible brand/product identity on-image but **no legible weight or volume number anywhere on the packaging or in text** (small dropper-bottle labels, kibble-bag nutrition labels with no gram figure, or a genuinely buyer-choice pack-count bracket like `[100+XTRA 20 PCS] / [30 PCS]`) — left `taxonomy_id IS NULL` rather than guess, per this file's own repeated "don't guess" precedent. Also left unresolved: a `[RAYA BUNDLE]` Royal Canin listing spanning "12 Selected Variants Across 3 Ranges" with no way to tell which variant the buyer receives, and a genuinely unbranded "Booster Gemuk (Freeze Dried) Pet Food" listing (no brand text/logo anywhere on-package, consistent with this category's long-documented unbranded-commodity-treat pattern) | Left `taxonomy_id IS NULL` — same "no legible quantity" caution already established for this category's treat/drop products |
| 2026-07-28 | Sixth top-up coverage | Image-read 4 of the 11 brand-unmatched high-GMV candidates and found real brands invisible to text matching (none of the four brand names appeared literally in their sku_name): **Catto** ("( 2kg x 4 ) 85% Protein... 8kg...") — packaging logo "Catto Premium", confirmed "NET 2KG" per bag × 4 = 8kg matching the title, genuinely new brand, minted `BRD-MY-01285`; **Yummy Mix** ("Makanan Kucing Murah 10kg Yummy Mix Chicken & Tuna") — packaging brand "Yummy Mix" (Turkey-formulated), confirmed 10kg on-bag, minted `BRD-MY-01287` rather than reuse the unrelated generic `BRD-SG-08706 "Yummy"` (Hegen/Aplus caution); **Uncle Tails** ("...Makanan Kucing Dalam Tin 400g 6 Tin - Pilihan 3 Rasa") — packaging brand "Uncle Tails" (secondary mark "UP"), confirmed 400g × 6 cans, reused existing `BRD-MY-01124`; **Aplus** ("【MURAH-MURAH】1 KG Dry Cat Food Ocean Fish...") — packaging brand "APLUS", confirmed 1kg, reused existing `BRD-MY-01246`. Also resolved **Cattysland** (already text-matched correctly this session, ambiguous-brand flag from an earlier draft of the matcher had already been superseded by the substring-collapse fix) — image confirmed product line "Cutes & Cat", sub-line "Mother & Baby", flavor "Salmon & Milk", 10kg, distinct enough from this category's existing "Najis Kurang Berbau" Cattysland entries (no flavor specified) to warrant a new entry rather than reuse | 4 new taxonomy entries (Catto, Yummy Mix, Uncle Tails, Cattysland variant) |
| 2026-07-28 | Sixth top-up coverage | Two remaining unmatched-brand candidates: **GOOD Pet Store** (booster treats — logo "GOOD PET STORE", 50g Chicken Breast bag and a 100g "Booster Gemuk" Chicken+Liver powder bag, both legible) minted as MY-scoped `BRD-MY-01286`, deliberately not merged with the unrelated generic global `BRD-SG-01400 "Good"` (also a zero-taxonomy-entry orphan, no verifiable link, same Hegen/Aplus caution); one row (`1729632873183283995`, "Kombo Jajan Combo Snack Set: 50g Chicken Breast + 1pack Glove Wipes + 2pcs Mainan Ball + 2pcs Random Jajan") is a mixed bundle where the food core (50g Chicken Breast) is clearly this same GOOD Pet Store product and the wipes/toy/random-jajan items are non-food GWP-style add-ons — mapped using only the 50g food core, consistent with GWP handling in `llm-extraction-rules.md` §1 (different-product freebies don't change pack_count); **DAI GOR** ("COMBO 2 Pack Booster Kucing Cepat Gemuk 1000g...") — brand clearly legible on-bag but the bag's own weight label was not legible in the image and "1000g" in the title is ambiguously either the combo total or the per-bag weight; **Mossy** ("【Booster Kucing Balang-Besar】...") — brand already established in this category, but the only weight reference in the image was an illustrative example calculation ("250G + 20% EXTRA = 300G", explicitly captioned "Photo is for illustration purposes only"), not this listing's actual weight | DAI GOR and the Mossy Balang-Besar listing left `taxonomy_id IS NULL` rather than guess a size from an illustrative example or an ambiguous combo total |
| 2026-07-28 | Sixth top-up coverage | Claimed SKU block `SKU-193362`–`SKU-193647` (286 slots, atomic `DECLARE`/`BEGIN TRANSACTION` claim; registry cross-checked clean first — `MAX(block_end)` across all categories matched the global `product_taxonomy` `MAX(taxonomy_id)`'s containing block exactly, no repeat of the fifth top-up's missing-row bug). Minted 3 new brands (`BRD-MY-01285`–`01287`; re-verified `MAX(BRD-MY-*)` immediately before insert — found `BRD-MY-01284 "Meals by StreetPaw"` already existed from a session not previously logged in this file, re-verified again post-insert via `COUNT(*) = COUNT(DISTINCT brand_id)`, no collision). Wrote 9 new `product_taxonomy` rows (`SKU-193362`–`SKU-193370`, 277 of 286 claimed slots unused) + 14 new `product_taxonomy_map` rows (5 exact-reuse of existing entries + 9 against the new entries) via `bq query` DML, `meta_agent='CLAUDE_CODE'`, `source='LLM'`, `confidence='0.75'`–`'0.85'`, `platform='Tiktok'`, `country='MY'` on every row | 3 new brands + 9 new `product_taxonomy` rows + 14 new `product_taxonomy_map` rows written |
| 2026-07-28 | Sixth top-up coverage | Self-check QA gates (G1 dual-mapped LLM-scoped, G2 HUMAN+LLM coexistence, out-of-range/cross-category `taxonomy_id` leakage against all 7 of this category's claimed blocks, G5 provenance, placeholder-leak, structured-fields-NULL%), run without `--skip-coexistence`, scoped to `master_table='makanankucing_my'` | All passed: 0 dual-mapped, 0 HUMAN+LLM coexistence, 0 out-of-range/cross-category `taxonomy_id`s, 0 provenance gaps, 0 placeholder-leak canonical names, 33% NULL `product_line` among LLM entries excluding multi-size catch-alls (unchanged from fourth/fifth top-up, well under the 50% threshold). Live worklist re-run post-write confirms remaining gap = 272 (286 − 14), matching exactly |
| 2026-07-28 | Sixth top-up coverage | **Remaining gap after this session: 272 products** (2 OOS excluded from the 286; of the 284 in-scope, 105 brand-matched-but-size-unresolved minus the 14 resolved this session = ~91 still size-unresolved, plus 179 brand-unidentifiable-from-text minus the handful resolved via image = ~175 still unidentified). Image-led yield this session (10 of 25 read, 40%) was notably higher than the fifth top-up's 11/297 (3.7%) — likely because this session specifically targeted rows where text signals were completely absent (new/unindexed brands, no sku_name brand token) rather than rows already brand-matched-but-size-only-ambiguous, which this category's prior sessions had already picked over | Not fixed this session — flagged: `DAI GOR` and the Mossy Balang-Besar listing are good candidates for a future session with product-description or option-list access (neither's weight was legible from the cover image alone); the ~175 remaining brand-unidentifiable rows are consistent with this category's long-documented unbranded-commodity-treat population |
| 2026-07-28 | Seventh top-up coverage (this session) | Ran the STEP-0 query exactly as given in the session prompt (with the `GROUP BY 1,2,3,4,5,6` fix the sixth top-up session already documented for this same query's `flag_GWP` bug) via direct BigQuery execution (the `bigquery-data-analytics` skill's `execute_sql` script, not `bq` CLI — this Linux environment has no `bq`/macOS paths from `CLAUDE.md`, which are for a different host). Confirmed exactly 272 products with `taxonomy_id IS NULL`, matching the wrapper's pre-check figure, and cross-checked with an independent product_id-grain rewrite of the same query (per this category's own documented `(url,image,sku_name)`-grain instability finding) — both agreed at 272 | Proceeded with the live 272-product worklist |
| 2026-07-28 | Seventh top-up coverage | Scope classification found 2 OOS (1 cat-litter "Best In Show Cat Sand", 1 livestock/molasses feed) — the same two products this exact live-worklist boundary has surfaced in the third/fourth/sixth top-up sessions. One classifier false-positive caught before finalizing: `"Meow Litter Fat Premium Cat Treat"` regex-matched the litter keyword but is the same documented `"Litter"`/`"Little"` typo this category's third top-up session already found — corrected to in-scope. Final: 270 in-scope, 2 OOS | Scope classified before any brand/size work |
| 2026-07-28 | Seventh top-up coverage | Built the brand matcher from the full documented `BRD-MY-01070`–`01287` range plus this category's existing-taxonomy brand joins plus `Omega Plus` (`BRD-SG-05090`, missing from a taxonomy-join-only list per the sixth top-up session's own finding) — 318 distinct brands total. Bracket/leading-tag priority + padded word-boundary matching + Decision-12 substring-collapse (per this category's established matcher fixes) resolved 114 of 270 in-scope rows to a single brand; 9 rows were genuine multi-brand `DENO MIX`/`TRIPLE MIX` reseller blends (left ambiguous, matching the fourth top-up's established DENO-MIX exclusion precedent) plus one false-positive (`"Bright"` substring-matched inside `"SHINE BRIGHT FUR-EVER"`, a Rich Choice product-line name, not the separate `Bright` brand — corrected to single-brand Rich Choice); 147 rows remained brand-unidentifiable from text alone, consistent with this category's long-documented unbranded-commodity-treat tail | 114 brand-matched rows carried forward |
| 2026-07-28 | Seventh top-up coverage | Bulk regex size/pack extraction against the 114 brand-matched rows (established per-unit weights: Ciao 14g/stick, Friskies 70g/pouch, JJANGX3 70g/pouch, Royal Canin 85g/pouch, Snappy Tom 85g/pouch, Sheba 70g/pouch, SmartHeart 85g/pouch, Whiskas 80g/pouch, Ishtar 16g/stick, Dr.Pikac 15g/stick) resolved only 5 directly from text (2 `is_multi_size` buyer-choice listings: Royal Canin "500g/1kg Repack", Picky Meow "1kg/10kg"; 3 concrete sizes: 2 Yummy Mix 10kg listings, 1 Uncle Tail 50g) — confirms this category's long-established finding that the text-exhausted residue is overwhelmingly size-silent in the title | 5 of 114 resolved from text alone |
| 2026-07-28 | Seventh top-up coverage | Image-verified 20 of the highest-GMV remaining brand-matched-but-size-unresolved candidates via the documented `curl -sL` + `Read` pattern (downloaded in parallel to a session-unique `/tmp/mkc_imgs/` directory per this category's own documented shared-filename collision caution). Resolved 6 more: **Cassiel** Immune Booster liquid — "150ml" printed on-label (new product line for this brand in this category, distinct from its existing Urinary Tract Health Puffed dry-food line); **Cassiel** Urinary Tract Health Puffed — "Net Weight 1kg" printed on-bag, exact-matched an already-existing entry (`SKU-157407`, minted in the second top-up session); **Bite of Wild** "FARM" Tuna — "1.5KG x1" badge on-image, a size/line combination none of this brand's ~35 existing entries cover; **Neco** — "10KG" badge on-bag, exact-matched existing `SKU-191074`; **Omega Plus** Fish Oil — "30ml" printed on the bottle label, "2 Bottles" per sku_name text (pack=2); **Molly** — "15kg" badge on-bag, exact-matched existing `SKU-145673` ("Molly Hypoallergenic & Low Grain 15kg"). One image-selection miss noted: the largest-dimension image for `FURREVER`'s candidate was a customer-review screenshot rather than the product photo (this environment's image list isn't always photo-first) — left that row unresolved rather than guess from a non-product image, consistent with the `llm-extraction-rules.md` §6 cover-image caution | 6 of 20 image-verified candidates resolved |
| 2026-07-28 | Seventh top-up coverage | 14 of the 20 image-verified candidates left deliberately unresolved — no legible weight/volume anywhere on-package or the pack-count is genuinely buyer-selectable with no way to tell what shipped: `LUMeO` catstick (buyer picks 100+20 or 30 pcs, no per-stick gram), `【PAWS&TAILS】` HALOPAWS co-brand stick set (30/60pcs buyer choice, matches this category's own already-documented ambiguous finding for this exact co-branded listing), `Pet Record` pouch bundle (7-flavour 30/24/12pcs buyer choice, no per-pouch gram), `OMI Petshop` kibble topper jar (no gram figure legible), `Petfos` kibble bag ("Petfcs" logo confirmed but bag's own weight text too small to read), `Furvit` Nutri Lick box (40 sachets stated, no per-sachet weight — same brand/product this category's second top-up session already found unresolvable), `WormShield` dewormer bottle (no ml figure legible on-label, matches the sixth top-up's own finding for this same brand), and the `Meow` "Litter/Little Fat" listing (per-stick weight confirmed 15g on-image, but the 50/100pcs pack count is buyer-selectable — same unresolvable-pack-count class as `Meow Little Fat`/`PetGo` already documented in the fifth top-up session) | Left `taxonomy_id IS NULL` — consistent with this category's repeated "don't guess" precedent |
| 2026-07-28 | Seventh top-up coverage | Two products excluded from mapping despite a resolved/partial signal, both matching this category's own established exclusion precedents rather than being re-litigated fresh: `RC Urinary SO Batu Karang Kucing 500g/1kg Repack Original` (product `1730162435595273582`) — the exact same product_id the fourth top-up session already excluded for this exact buyer-choice-size reason; `[FREE KRILL] PICKY MEOW PremiumFit ... 1kg / 10kg (10x1kg)` — the same "Picky Meow" bare-brand-match-plus-multi-size-ambiguity product the fourth top-up session already excluded | Left `taxonomy_id IS NULL`, consistent with prior-session precedent for these exact two products |
| 2026-07-28 | Seventh top-up coverage | Reuse-before-mint check against this category's existing taxonomy (44,963 rows across the whole reference table) found 5 of the 9 resolvable products exact-matched an existing entry: `Cassiel` Urinary 1kg → `SKU-157407`, `Neco` 10kg → `SKU-191074`, `Molly` Hypoallergenic & Low Grain 15kg → `SKU-145673`, `Uncle Tails` 50g wet pouch → `SKU-146769`, and one `Yummy Mix` 10kg listing with no stated flavor (`"...Yummy Mix (Kucing Dewasa)"`) reused the existing generic `Yummy Mix Chicken & Tuna 10kg` (`SKU-193367`, minted in the sixth top-up session) rather than mint a near-duplicate on an unconfirmed flavor match — confidence lowered to `0.7` on that one row to reflect the flavor uncertainty. The other `Yummy Mix` listing explicitly stated a different flavor (`"Chicken With Milk"`) and got a new entry per this category's established product-line-vs-variant flavor-split rule (`variant` field, not folded into a shared bucket) | 4 groups had no existing match and got one new taxonomy entry each |
| 2026-07-28 | Seventh top-up coverage | Claimed SKU block `SKU-193848`–`SKU-194119` (272 slots, atomic `DECLARE`/`BEGIN TRANSACTION` claim per the runbook, sized to the prompt's literal worklist count per instruction). Registry cross-checked clean first: `MAX(block_end)`=193847 vs global `product_taxonomy` `MAX(taxonomy_id)`=SKU-193652 (registry ahead of usage, no repeat of the fifth top-up's missing-row bug) and re-verified 0 existing `product_taxonomy` rows in the claimed range immediately before insert. Wrote 4 new `product_taxonomy` rows (`SKU-193848`–`SKU-193851`, 268 of 272 claimed slots unused) + 9 new `product_taxonomy_map` rows (5 reusing existing entries + 4 against the new entries) via `bq query`-equivalent DML (BigQuery `INSERT ... VALUES` through the `bigquery-data-analytics` skill), `meta_agent='CLAUDE_CODE'`, `source='LLM'`, `confidence='0.7'`–`'0.85'`, `platform='Tiktok'`, `country='MY'` on every row. Direct join verification against all 9 target `product_id`s confirmed correct `taxonomy_id`/`canonical_name` before proceeding to QA gates | 4 new `product_taxonomy` rows + 9 new `product_taxonomy_map` rows written |
| 2026-07-28 | Seventh top-up coverage | Self-check QA gates (G1 dual-mapped LLM-scoped, G2 HUMAN+LLM coexistence, out-of-range/cross-category `taxonomy_id` leakage against all 8 of this category's claimed blocks, G5 provenance, placeholder-leak, structured-fields-NULL%), run without `--skip-coexistence`, scoped to `master_table='makanankucing_my'` | All passed: 0 dual-mapped, 0 HUMAN+LLM coexistence, 0 out-of-range/cross-category `taxonomy_id`s, 0 provenance gaps, 0 placeholder-leak canonical names, 33% NULL `product_line` among LLM entries excluding multi-size catch-alls (unchanged from prior sessions, well under the 50% threshold). Gap re-verified via the product_id-grain join (not the raw STEP-0 row count, per this category's own documented query-instability caution) confirms remaining gap = 263 (272 − 9), matching exactly |
| 2026-07-28 | Seventh top-up coverage | **Remaining gap after this session: 263 products.** Image-led yield this session (6 of 20, 30%) continued the pattern of diminishing per-row returns on this category's text-exhausted residue (101→65→151→63→11→14→9 new-taxonomy-adjacent resolutions across the seven sessions). No new brands needed minting this session — all resolvable rows matched an already-documented brand. The remaining ~147 brand-unidentifiable-from-text rows are consistent with the long-documented unbranded freeze-dried-booster/treat commodity population; the remaining brand-matched-but-size-unresolved rows are overwhelmingly buyer-selectable-pack-count or no-legible-weight cases already characterized across sessions two through six | Not fixed this session — flagged: a future session with product-page/option-list access (not just a single cover image) would likely resolve the buyer-choice-pack-count listings (`LUMeO`, `Pet Record`, `Meow Little Fat`, `【PAWS&TAILS】`) that this and prior sessions could only confirm a per-unit weight for, not an actual shipped quantity |
| 2026-07-28 | Eighth top-up coverage (this session) | Re-ran the live STEP-0 worklist query directly (with the sixth top-up's documented `GROUP BY 1,2,3,4,5,6` fix already applied) rather than trusting the wrapper's 263 pre-check number — confirmed exactly 263 products with `taxonomy_id IS NULL` (all `platform='Tiktok'`, all `flag_GWP=FALSE`), cross-checked with an independent product_id-grain rewrite of the same query per this category's own documented anomaly caution (both agreed at 263). This Linux environment has both `bq`/`gcloud` on `PATH` and working `gcloud auth` (unlike the seventh top-up session's environment, which reported neither available) — used `bq query` directly per `CLAUDE.md`/the runbook, not the `bigquery-data-analytics` skill | Proceeded with the live 263-product worklist |
| 2026-07-28 | Eighth top-up coverage | Registry cross-checked clean before claiming: `MAX(block_end)`=194319 vs global `product_taxonomy` `MAX(taxonomy_id)`=SKU-194121 (registry ahead of usage, no repeat of the fifth top-up's missing-row bug) | Proceeded to scope/brand/size work before claiming |
| 2026-07-28 | Eighth top-up coverage | Scope classification found 2 OOS (1 cat-litter "Best In Show Cat Sand", 1 livestock/molasses feed) — the same two products this exact live-worklist boundary has surfaced in the third/fourth/sixth/seventh top-up sessions, confirming the classifier against established precedent. 261 in-scope | Scope classified before any brand/size work |
| 2026-07-28 | Eighth top-up coverage | Built a brand matcher directly from BigQuery (`product_taxonomy` × `product_taxonomy_map` × `brand_dict` joined for this category, 259 brands, plus the full documented `BRD-MY-01070`–`01287` range and `Omega Plus` for brands with zero taxonomy rows — 320 distinct brands total) rather than hand-transcribing this file's brand lists, to avoid missing brands added since the last time this file's own brand tables were updated. Caught one implementation bug before generating any SQL: an alias-matching pass was searching sku_name text for the *target* canonical brand name instead of the *alias text itself* (e.g. checking for "Pawmised" instead of "PawmisedLand"), silently failing every alias-based match; fixed by building (alias_text, target_name) search pairs correctly. 106 of 261 in-scope rows brand-matched (105 via the matcher, 1 more — "RICH CHOICE CANNED FOOD ... / SHINE BRIGHT FUR-EVER ..." — manually resolved to single-brand Rich Choice after confirming "SHINE BRIGHT FUR-EVER" is a Rich Choice product-line name, not the separate brand `Bright`, matching the exact false-positive class the seventh top-up session already documented for this brand pair). 8 genuine `DENO MIX`/`TRIPLE MIX` multi-brand reseller blends left ambiguous, matching the established DENO-MIX exclusion precedent. 147 rows remained brand-unidentifiable from text alone, consistent with this category's long-documented unbranded-commodity-treat tail | 106 brand-matched rows carried forward |
| 2026-07-28 | Eighth top-up coverage | Bulk regex size/pack extraction against the 106 brand-matched rows (established per-unit weights: Ciao 14g/stick, Friskies/JJANGX3/Royal Canin/Snappy Tom pouch weights, Sheba/Smart Heart/Whiskas pouch weights, Ishtar 16g/stick, Dr.Pikac 15g/stick) initially resolved 0 directly — confirmed by inspection that the top-GMV matched rows are literally the same exhausted residue prior sessions already picked over (LUMeO, PAWS&TAILS, Pet Record, Meow Litter Fat, OMI Petshop, Petfos, Furvit, FURREVER, WormShield, MORTITI, Pets Science all reappear verbatim). Found and fixed two unit-regex gaps scanning the full matched set: (1) `"grm"`/`"gm"` gram-unit variants (e.g. `"400grm"`, `"5gm"`) weren't in the unit alternation at all, so `\b` boundary checks after `"gr"`/`"g"` failed on the trailing consonant — added `grms?`/`gms?` to the alternation; (2) count-before-size patterns with an intervening unit word (`"12can x 400grm"`) weren't captured by either `x\s*N` direction — added a `{N}{unit_word} x {size}` pattern (can/cans/pcs/pouch/pouches/sachets/sticks/packs/bags) and an `N * M sachets` pattern. After both fixes, resolved exactly 2 from text: `Kucinta Wet Canned Food ... 12can x 400grm` → 400g×12 (exact-matched existing `SKU-191096` "Kucinta Borong Sniflly Cat 400g x12", reused directly), and `WaggyWaggy Pet Goat Milk Powder Probiotic Prebiotic Cat Dog (5gm * 15sachets)` → 5g×15 (no existing WaggyWaggy entry at this size — the brand's other entries are `WaggyWaggy Chicken & Salmon 1.5kg` kibble and several dog-labelled "Masti WaggyWaggy" products under a different brand_id entirely — new entry needed) | 2 of 106 resolved from text alone |
| 2026-07-28 | Eighth top-up coverage | Image-verified 9 of the highest-GMV remaining brand-matched-but-size-unresolved candidates via the documented `curl -sL` + `Read` pattern, downloaded in parallel to a session-unique `/tmp` directory. **Zero new resolutions** — every candidate reproduced an already-documented unresolvable pattern: `Dr.Bites` "Jumbo Set" pack image shows ~20 countable pouches but no confidently-legible per-pouch gram text (existing catalog has both 22g and 85g Dr.Bites pouch entries, so guessing which applies was rejected per this category's "don't guess" precedent); `Booster King` KOMBO and plain listings are pure marketing images with no weight/count text anywhere; `SIMBA` "3 in 1 Booster" is a marketing collage, no weight text; `Petgo` "50pcs" listing's largest-dimension image turned out to be a certification/FDA document, not the product photo — the same image-selection-miss class the seventh top-up session already flagged for this environment; `REFLEX` "plus chicken, plus salmon, plus kitten" is a tightly-cropped kibble macro shot with no visible label; `PURINA ONE Indoor Advantage Bundle` image resolved a real content list ("6.6KG Dry + 12 Pack of Purina ONE 70g") but this is a fixed dry+wet cross-format bundle that doesn't fit this schema's single size+pack_count pair without fabricating a value — left unresolved rather than force a bad structure; `PawmisedLand SUPER CATTY FLOSSY BUNDLE` resolved to "Chicken Catgrass + Omega Krill" (two distinct established-125g formulas) but with no legible count of bags per formula — same buyer-choice-bundle ambiguity class already documented for this brand; `[Paws and Tail] 3 boxes 180PCS + 4 packs of nutritional 120PCS` — its cover image shows an unrelated smaller "15g x 30 sachets" single-bag product, contradicting the title's much larger bundle math (3×180+4×120=1,020 pieces), an instance of `llm-extraction-rules.md` §6's documented cover-image caveat (the cover/hero image can show a different product than the one being sold) — also caught that this product had been brand-matched to "MORTITI" by mistake (a keyword-stuffed mention buried at the end of the title), when the real brand is Paws & Tails; left unmapped either way given the image/title mismatch | Left `taxonomy_id IS NULL` — consistent with this category's repeated "don't guess" precedent; candidates for a future session with product-page/option-list access |
| 2026-07-28 | Eighth top-up coverage | Reuse-before-mint check: of the 2 text-resolved products, `Kucinta` 400g×12 exact-matched existing `SKU-191096`; `WaggyWaggy` 5g×15 goat milk powder had no existing match, needed a new entry (`brand_id=BRD-MY-01282`, `product_line="Goat Milk Powder"`, `variant="Probiotic Prebiotic"`) | 1 group had no existing match and got one new taxonomy entry |
| 2026-07-28 | Eighth top-up coverage | Claimed SKU block `SKU-194320`–`SKU-194582` (263 slots, atomic `DECLARE`/`BEGIN TRANSACTION` claim per the runbook, sized to the prompt's literal worklist count per instruction). Registry cross-checked clean first (see above) and re-verified 0 existing `product_taxonomy` rows in the claimed range immediately before insert. Pre-insert duplicate check confirmed neither target `product_id` already had a `makanankucing_my` map row. Wrote 1 new `product_taxonomy` row (`SKU-194320`, 262 of 263 claimed slots unused) + 2 new `product_taxonomy_map` rows (1 reusing an existing entry + 1 against the new entry) via `bq query` DML, `meta_agent='CLAUDE_CODE'`, `source='LLM'`, `confidence='0.8'`–`'0.85'`, `platform='Tiktok'`, `country='MY'` on every row. Direct join verification against both target `product_id`s confirmed correct `taxonomy_id`/`canonical_name` before proceeding to QA gates | 1 new `product_taxonomy` row + 2 new `product_taxonomy_map` rows written |
| 2026-07-28 | Eighth top-up coverage | Self-check QA gates (G1 dual-mapped LLM-scoped, G2 HUMAN+LLM coexistence, out-of-range/cross-category `taxonomy_id` leakage against all 9 of this category's claimed blocks, G5 provenance, placeholder-leak, structured-fields-NULL%), run without `--skip-coexistence`, scoped to `master_table='makanankucing_my'` | All passed: 0 dual-mapped, 0 HUMAN+LLM coexistence, 0 out-of-range/cross-category `taxonomy_id`s, 0 provenance gaps, 0 placeholder-leak canonical names, 33% NULL `product_line` among LLM entries excluding multi-size catch-alls (unchanged from prior sessions, well under the 50% threshold). Gap re-verified via the product_id-grain join (not the raw STEP-0 row count, per this category's own documented query-instability caution) confirms remaining gap = 261 (263 − 2), matching exactly |
| 2026-07-28 | Eighth top-up coverage | **Remaining gap after this session: 261 products.** Image-led yield this session was 0/9 (0%) — the first session in this category's history to resolve zero candidates from a dedicated image-verification batch, a further data point (after the seventh top-up's 30%, down from earlier sessions' 40%+) that this residue's easily-resolvable population is exhausted under cover-image-only access. All 9 read this session reproduced an already-documented unresolvable pattern (no legible weight, marketing-only imagery, buyer-choice bundle ambiguity, or a cover-image/title mismatch) rather than surfacing a new failure mode. The 8 genuine `DENO MIX`/`TRIPLE MIX` rows and the `PURINA ONE`/`Pawmised` cross-format or multi-formula bundles are structurally a poor fit for this schema's single size+pack_count pair regardless of image access | Not fixed this session — flagged: this category's remaining gap is very likely now dominated by genuinely unresolvable-from-cover-image cases (buyer-choice pack counts, no legible weight anywhere on-package, multi-item bundles) rather than an image-access gap; a future session would get more value from product-page/option-list access (per the seventh top-up's same flag) or from accepting a documented `UNRESOLVED`/long-tail floor for this category rather than continuing single-digit-yield cover-image passes |
| 2026-07-28 | Ninth top-up coverage (this session) | Re-ran the live STEP-0 worklist query directly rather than trusting the prompt's stated 261 figure — confirmed exactly 261 products with `taxonomy_id IS NULL`, all `platform='Tiktok'`, all `flag_GWP=FALSE`, matching the prompt's number exactly. Registry cross-checked clean before claiming: `MAX(block_end)`=194582 vs global `product_taxonomy` `MAX(taxonomy_id)`=SKU-194320 (both consistent with the eighth top-up's documented end-state, no missing-row gap) | Proceeded with the live 261-product worklist |
| 2026-07-28 | Ninth top-up coverage | Built a brand matcher against this category's existing-taxonomy brand join (259 brands) plus the full documented `BRD-MY-01070`–`01288` `brand_dict` range (to catch brands already minted with zero `product_taxonomy` rows yet, e.g. `LUMeO`, `FURREVER`, `Petfos`, `WormShield`, `Pets Science`, `Pro Performance`, `BOCO`) using bracket/leading-token normalized matching. 119 of 261 rows brand-matched; one fallback-substring false positive caught and discarded before use (`"KRILL PET"` inside a generic booster listing spuriously matched brand `LLPET` via naive substring — same collision class as this category's documented `"tom"`/`"sTOMach"` precedent) | 119 brand-matched rows carried forward, 1 false positive discarded |
| 2026-07-28 | Ninth top-up coverage | Identified 8 genuine `DENO MIX`/`TRIPLE MIX` multi-brand reseller-blend listings (e.g. `"DENO MIX KITTEN BOOSTER (ROYAL CANIN KITTE + PURINA ONE INDOOR + TOTW ROCKY MOUNTAIN + BOOSTER FREEZE CHICKEN)"`) that token-overlap-scored as false high-confidence matches to single existing entries (`Royal Canin Kitten`, `Purina ONE Indoor`, `Royal Canin Hair & Skin`) purely because those short canonical names' tokens are common words also present inside the multi-brand blend text. Excluded all 8 from single-brand routing — matches the established DENO-MIX exclusion precedent from prior sessions, extended here to confirm the token-overlap scorer itself needs this exclusion applied before trusting its output, not just brand-substring matching | Left `taxonomy_id IS NULL`, consistent with prior-session precedent |
| 2026-07-28 | Ninth top-up coverage | Reuse-before-mint via token-overlap scoring against each matched brand's existing entries resolved 12 products directly to already-existing taxonomy: `Royal Canin Indoor 7+` → `SKU-146907` (existing `is_multi_size` Indoor catch-all), `Royal Canin Aroma Exigent`/`Protein Exigent` → `SKU-146595`/`SKU-146584`, `Reflex` (mixed formula description, no clear single line) → `SKU-191189` (existing `is_multi_size` bare-brand catch-all), `Purina ONE Indoor Advantage Bundle` ×2 listings → `SKU-145598` (existing `is_multi_size` Indoor catch-all), `Furvit Cat Candy's Nutri Lick (40 Sachets)` ×3 listings → `SKU-191170` (exact `5g x40` match), `Pawmised Catty Munchies Chicken Catgrass Functional Series...` → `SKU-191097` (near-verbatim text match to an existing 375g entry), `Nanovet Freeze Dried Chicken/Salmon Cube Booster Gemuk` ×2 listings → `SKU-157403` (existing `200g` Freeze Dried Cube Series) | 12 products mapped to existing taxonomy, 0 new entries needed |
| 2026-07-28 | Ninth top-up coverage | Image-verified a batch of brand-known-zero-taxonomy candidates (`LUMeO`, `Petfos`, `FURREVER`, `WormShield`, `Pets Science`, `Pro Performance`, `BOCO`, `Catty Bistro Bites`) via `curl -sL` + `Read` (webp renders correctly through this pipeline, confirmed before batching). Findings: **`WormShield`** is a liquid dewormer bottle, not pet food — reclassified OOS, same exclusion class as this category's documented flea/tick-treatment precedent (pet-care product, not pet food); **`FURREVER`** is a dropper-bottle liquid product ("Cat Treats, Not Pet Medication" disclaimer) with no legible volume and ambiguous fit against this category's food/treat scope — left unresolved rather than force a classification; **`LUMeO`** catstick is an explicit buyer-choice pack-count listing ("SET 100+XTRA 20 PCS" vs "SET 30 PCS"), same unresolvable-pack-count class as `Pet Record`/`Meow Little Fat` in prior sessions; **`Petfos`** and **`Pets Science`** kibble bags both show a real product photo but no legible gram/kg figure on the label at available resolution; **`Catty Bistro Bites`** turned out to be a *product line of the already-known `Pawmised`/`PawmisedLand` brand* (logo visible on-pack), not a separate new brand — 3 flavor sub-lines identified (Sunny Side Up Egg, Shrimpy Cracker, Meowt-cha Cracker) across 8 listings, but no gram weight legible on any of the pack images checked (including a 3334px high-res version) — left unresolved rather than guess, flagged for a future session with option-list/product-page access. **`Pro Performance`** (15kg stated in `sku_name` itself, image confirmed) and **`BOCO`** (image revealed a genuine 100g/200g/400g buyer-choice size range not stated in `sku_name`) were the only two of this batch with enough signal to mint | 2 of 8 image-checked candidates resolved; `WormShield` reclassified OOS; `Catty Bistro Bites`/`Pawmised` (8 products) and 5 others left unresolved and documented |
| 2026-07-28 | Ninth top-up coverage | Minted 4 new taxonomy entries for products with a confirmed brand+line+size but no matching existing entry: `Masti Chicken Breast Snack Food 40g x50` and `...x100` (per-unit 40g reused from this brand's own existing single-unit entry `SKU-146030`; an existing `SKU-145710` "...40g x200" confirms 40g as this brand's established per-unit weight for this treat) covering 3 products (1×50pcs, 2×100pcs listings); `Pro Performance Ultra Premium 15kg` (`is_multi_variant=TRUE` — the product image shows 5 distinct formula bags — Adult Sterilised Chicken & Rice, Adult Lamb, Kitten Lamb & Rice, Adult Sportfit Chicken & Fish, Adult Sterilised Lamb & Cranberry — with no way to tell which the buyer received) covering 1 product; `BOCO Antarctic Shrimp` (`is_multi_size=TRUE`, confirmed genuine buyer-selectable 100g/200g/400g range from the product image, matching the sanctioned is_multi_size pattern rather than the separate buyer-choice-pack-count exclusion class) covering 1 product. All 4 new entries used already-existing `brand_id`s (`BRD-SG-05517` Masti, `BRD-MY-01047` Pro Performance, `BRD-MY-01216` BOCO) — no new `brand_dict` rows needed this session, avoiding the brand_id-collision class documented earlier in this file | 4 new `product_taxonomy` entries covering 5 products |
| 2026-07-28 | Ninth top-up coverage | Claimed SKU block `SKU-194583`–`SKU-194843` (261 slots, atomic `DECLARE`/`BEGIN TRANSACTION` claim per the runbook, sized to the prompt's literal worklist count per instruction). Registry cross-checked clean first (see above) and re-verified 0 existing `product_taxonomy` rows in the claimed range immediately before insert. Pre-insert duplicate check confirmed none of the 17 target `product_id`s already had a `makanankucing_my` map row. Wrote 4 new `product_taxonomy` rows (`SKU-194583`–`SKU-194586`, 257 of 261 claimed slots unused) + 17 new `product_taxonomy_map` rows (12 reusing existing entries + 5 against the 4 new entries) via `bq query` DML (note: this table's `confidence` column is `STRING`, not `FLOAT64` as `ARCHITECTURE.md` documents — quoted values used), `meta_agent='CLAUDE_CODE'`, `source='LLM'`, `confidence='0.7'`–`'0.85'`, `platform='Tiktok'`, `country='MY'` on every row | 4 new `product_taxonomy` rows + 17 new `product_taxonomy_map` rows written |
| 2026-07-28 | Ninth top-up coverage | Self-check QA gates (G1 dual-mapped, G2 HUMAN+LLM coexistence, placeholder-leak, G5 provenance — both category-wide and scoped specifically to this session's 4 new `taxonomy_id`s), run without `--skip-coexistence`, scoped to `master_table='makanankucing_my'` | All passed: 0 dual-mapped, 0 HUMAN+LLM coexistence, 0 placeholder-leak canonical names, 0 provenance gaps (category-wide and session-scoped) |
| 2026-07-28 | Ninth top-up coverage | **Remaining gap after this session: 244 products** (261 − 17). Resolved 17 this session (12 reuse + 5 via 4 new mints), the best single-session yield since the fourth top-up (101) — driven mainly by widening the brand-match dictionary to include zero-taxonomy `brand_dict` entries from prior sessions' un-actioned mints, not by new image discoveries (image batch yield was 2/8, continuing the low-yield pattern from sessions seven and eight). One brand reclassified OOS this session (`WormShield` — dewormer, not food): add to this file's Scope exclusion list. `Catty Bistro Bites` confirmed as a `Pawmised` sub-line worth 8 products once a size signal becomes available — flagged as the single highest-value target for a future session with option-list/product-page access | Not fixed this session — flagged: `Catty Bistro Bites`/`Pawmised` (8 products, brand+line known, size the only blocker), `FURREVER`/`Petfos`/`Pets Science`/`LUMeO` (brand known, size/scope the only blocker), the 8 `DENO MIX`/`TRIPLE MIX` multi-brand blends, and the Pet-Record/cat-stick buyer-choice-pack-count listings remain the concrete, named residue for the next session |

| 2026-07-28 15:32 UTC | Automated review session (auto-discovery) | Auto-discovery review of the never-reviewed worklist (1,921 taxonomy entries; category had no prior _meta review history at all) found the Tier 1 sweep's usual mechanical defects (11 duplicate_brand, 22 excess_content, 0 stub_leak/canonical_field_mismatch/null_size/garbage_brand) plus a much larger structural problem the sweep's brand_casing_mismatch flag stumbled into: several brand_id assignments were flat wrong, not miscased. Two were cross-domain identity collisions — 83 'Molly' cat-food entries (brand_id shared with an unrelated Thai hair-treatment brand 'molly') and 42 'Belif' cat-food entries (shared with the Korean skincare brand 'belif') — confirmed via sku_name text ('Made in Turkey' cat food vs 'The True Cream Aqua Bomb'). Six more groups (Sarar/A-Tier, PETSEE/CATQUEEN, Urbanwolf/EGOCAT, Kucinta/Dr.Bites, Blackwood/Cattus, Ezuma/Bento, VitalPlus/Anyson) were internal Full-Rebuild-era brand_dict mismatches where a correctly-named brand_id already existed elsewhere in this category's own brand_dict range. The largest single cluster: brand_id BRD-MY-01082 ('CATQUEEN', a reseller storefront name baked into sku_name as if it were the product's own brand — the same failure class as llm-extraction-rules.md §11's watermark rule, generalized to title text) had absorbed 46 entries spanning 16+ real, unrelated brands (Kenko, Sheba, Whiskas, Blackwood, Josera, Kucinta, Midalac, Misha, Purina/Felix, Sarar, Sniffly, Wono, Pet Nourish, Bossku, plus the Molly/Belif cases above), with several individual TikTok products cross-routed onto the wrong brand's taxonomy entry entirely (e.g. a Josera 10kg product mapped onto a Wono 10kg entry; a Primaluxe product mapped onto a Purina Felix entry). Also found and fixed: a duplicate-brand-token naming bug on 11 Ciao/Reflex/Buttons & Bows entries, and a leaked mid-string per-unit-size/count token on 22 entries whose canonical_name already carried a correct trailing 'size xN' suffix. STEP 2c's shared-bucket sweep found 29 flagged taxonomy entries; whole-bucket aggregation confirmed 12 were genuinely single-value (write-in-place candidates) and roughly 9 are real multi-value catch-alls needing a proper per-value reroute pass. STEP 2b's promo-language sweep returned ~2,183 hits (mostly GWP/legitimate pack_count=1, per the established false-positive pattern) — too large to review row-by-row this session; folded into remaining Tier 2 budget rather than treated as an automatic fix list. A high-GMV Tier 2 spot-check also found a Royal Canin variant-collapse defect: distinct Maine Coon Kitten and Persian Kitten 4kg formulas (674,945 combined GMV) were merged into a generic 'Royal Canin Kitten 4kg' entry alongside two ambiguous multi-formula reseller listings. | Repointed brand_id (with meta_agent + _meta reset) for all 9 mismatched-brand groups plus the CATQUEEN cluster: minted 4 new brand_dict rows for genuinely distinct real-world brands (Molly, Belif, Pet Nourish, VitalPlus — all MY-scoped, re-verified against live MAX(BRD-MY-*) immediately before insert, no collision) and reused 8 already-existing correct brand_ids for the rest. Stripped the 'CATQUEEN ' reseller prefix from 13 canonical_names. Rerouted 10 individually-mis-routed product_taxonomy_map rows to their correct existing taxonomy entries (verified via matching sku_name text), and minted 4 new taxonomy entries (claimed SKU block SKU-194849-195048, used 194849-194852) for products whose correct brand+size combination had no existing entry, rerouting their map rows accordingly. Fixed all 11 duplicate_brand rows via a strip-and-rebuild regex (keep first brand occurrence, remove repeats) and all 22 excess_content rows via a 3-step regex chain (BigQuery RE2 has no lookahead, so the trailing 'size xN' is merged into one token first to shield it, then bare size tokens are stripped, then the space is restored). Applied STEP 2c write-in-place fixes to the 12 confirmed single-value buckets (10 pack_count + xN suffix, 2 size + is_multi_size=FALSE corrections). Split Royal Canin Maine Coon Kitten and Persian Kitten out of the generic 4kg bucket into 2 new dedicated entries. Immediately Tier-1-rechecked every row touched this session and found + fixed 14 residual casing/duplicate/field-order defects the initial fixes had introduced or exposed (mostly all-caps brand text inherited from the CATQUEEN-prefixed source titles). Promoted 177 taxonomy entries to review_confidence='confident' (174 via Path 1(b) same-session gate-verify, 3 via Path 1(a) never-reviewed+Tier-2-correct+Tier-1-clean). Left 3 entries at 'fixed pending recheck, unresolved': one deliberately (the residual Royal Canin base 'Kitten 4kg' bucket still holds 2 ambiguous multi-formula reseller listings), one due to a known accepted false-positive (a genuinely global brand_dict casing defect on 'MASTI' shared across categories, out of this session's single-category scope to fix), and 21 taxonomy entries (9 STEP 2c genuine multi-multiplier catch-alls, plus the still-unresolved Primaluxe/PRIMALUXE product wrongly sharing the Felix taxonomy entry) are flagged findings, not fixed, for a future targeted session with the aggregation data already computed. |
| 2026-07-28 16:05 UTC | Automated review session (auto-discovery) | Auto-discovery review of the 1,745-row never-reviewed/unconfident worklist for makanankucing_my (Shopee+Tiktok). Pre-fix qa_report.sh showed one entry-level gate failing: 'canonical_name fields' (16 rows) — traced to the actual qa_report.sh query (not the count alone) since it's scoped to ALL rows for the table, not just the review worklist, and found each was a per-unit weight (e.g. '15g', '70g', '380g') or stray reseller/promo text ('CARTON', '!!', 'BIG PACK!', 'Purina' parent-brand mention) embedded inside product_line/variant that didn't match the entry's own aggregate size field. Tier 1 SQL sweep over the worklist found 9 wrong_field_order rows (brand_id correct but canonical_name used an abbreviated/misspelled brand form — 'Advance' instead of 'Advance Veterinary', 'Furry Tail' instead of 'Furrytail'), 1 taxonomy_id (SKU-155068) whose brand_id resolved to the wrong existing brand ('MASTI' BRD-SG-05517) when the product's own packaging image (verified via curl+Read) showed brand 'Cattie' — a brand-new, unrelated brand not yet in brand_dict, since sku_name's 'MESTI' token was actually the Malay word for 'must' (marketing text), not a brand at all. Also found 259 brand_casing_mismatch flags (e.g. brand_dict 'ROSY FRESH'/'PowerCat'/'Prodiet' vs canonical_name's differently-cased 'Rosy Fresh'/'Powercat'/'ProDiet') that are structurally unresolvable within a category-scoped session since the only clean fix would edit globally-shared brand_dict — left un-promoted, not excepted (needs a second confirming session per the exception process). STEP 2b/2c sweeps and Tier-2 GMV-prioritized sampling surfaced two classes of pack-count/size misses (Furlove 80g listing genuinely x12 not x1; SmartHeart/Aatas/Mossy/LILIEN single-value buckets mislabeled pack_count=1) plus several duplicated-brand-in-product_line and garbled-text defects (SIMBA 'Urinary Urinary', SmartHeart 'Smart Heart Kibble', Buttons & Bows 'Button Bow', Bento 'Booster...Booster...For', Family Pets dangling '&', Aristo Cats 'G Plus' truncation, Sarar entries mixing contradictory 'Basah'(wet)+'Kibbles'(dry) text on 15kg/2kg bags that are unambiguously dry kibble). The single largest finding, out of scope for this session's fix budget: Royal Canin (the #1 brand by GMV) has pervasive cross-entry duplication across essentially its whole catalog at every size (e.g. at 4kg alone: four separate 'Hair & Skin' variants, two 'Royal Canin 4kg' NULL-product_line entries, two 'Digestive Care 4kg' entries), plus a generic-umbrella-as-product_line defect where SKU-145604 'Feline Health Nutrition' (RC's marketing tagline, not a real formula name) silently collapses at least 6 distinct real formulas (Kitten, Sensible 33, Mother & Babycat, Aroma Exigent, and others). The same two defect classes (cross-entry duplication + generic-umbrella-collapse) recur at smaller scale in Mr.Vet (BRD-GLOBAL-00167, ~26 entries, SKU-146092 alone silently merges 7 distinct T1-T4/E1-E3 formulas under one 'T4' label), ProDiet, and Bite of Wild. These require a dedicated dedup/rebuild session, not a routine QA sweep, given the risk of a half-finished merge leaving worse state than the existing duplication. | Fixed and confirmed-confident this session (62 taxonomy entries): 15 rows for the 'canonical_name fields' gate (stripped embedded per-unit sizes and stray promo/reseller text from product_line/variant) — gate now reads 0/0 confirmed via qa_report.sh directly, not just the Tier 1 count; 9 wrong_field_order rows (canonical_name rewritten to the full/correct brand form, brand_id untouched since it was already correct); 1 brand misassignment (SKU-155068 moved off BRD-SG-05517 'MASTI' onto newly-minted BRD-MY-01299 'Cattie', confirmed via product image, canonical_name rebuilt); 4 pack-count/size single-value-bucket misses (Furlove x12, SmartHeart-pouch x55, Aatas x24, Mossy x2, LILIEN x50); 6 duplicated-brand/garbled-text cleanups (SIMBA, SmartHeart, Buttons & Bows, Bento, Family Pets, Aristo Cats); 2 wet/dry-contradictory-text cleanups (Sarar 15kg/2kg, resolved as dry kibble per bag size, matching the G3 type gate's intent); 25 rows Tier-2-judged correct with no fix needed. Every fixed row was re-verified clean against the full 8-flag Tier 1 sweep in the same session before promotion (no waiting for a future pass). Left un-promoted and un-excepted, pending a second confirming session: 259 brand_casing_mismatch rows (structural brand_dict-vs-canonical_name casing difference, cross-category blast radius if 'fixed' via brand_dict edit). Left entirely untouched, flagged for a dedicated remediation session: Royal Canin's catalog-wide entry duplication and generic-umbrella product_line collapse (~50+ entries just at 4kg), plus the same defect pattern in Mr.Vet (~26 entries), ProDiet, and Bite of Wild. Post-session qa_report.sh: all 9 hard gates pass (0 dual-mapped, 0 HUMAN+LLM coexistence, 0 placeholder-leak, 33% structured-fields NULL — under threshold, 0 duplicate product_id/product+taxon, 0 'all variant/size' name, 0 canonical_name fields, 0 garbled brand text). qa_coverage_report.sh: 1,923 total taxonomy entries for this category, 223 confident, 1 unconfident (SKU-145588 — second review didn't agree with a prior non-'correct' verdict, correctly needs a third confirming review per the two-consecutive-agreement rule), 0 fixed-pending-recheck, 1,699 never-reviewed (unchanged from before minus this session's fixes — the bulk of the category, including nearly all of Royal Canin/Mr.Vet/ProDiet/Bite of Wild, remains for future sessions). |
| 2026-07-28 17:30 UTC | Automated review session (auto-discovery) | Auto-discovery targeted QA fix on makanankucing_my's ~1,700-entry never-reviewed/unconfident worklist. Tier 1 SQL sweep found only brand_casing_mismatch active (258 entries; all other Tier-1 flags — stub_leak, duplicate_brand, wrong_field_order, excess_content, canonical_field_mismatch, null_size, garbage_brand — were 0). STEP 2c's shared-bucket size/multiplier sweep flagged 15 taxonomy_ids; per-bucket aggregation showed all 15 were false positives on the size axis (multi-size/multi-brand listings my regex under-counted, e.g. '500GM' not matching the unit regex, or two-brand bundled listings), but surfaced a genuine, separate defect: 11 of those buckets are real pack-count-multiplier catch-alls (~360 mostly-Tiktok products merged under pack_count=1 despite sku_name stating 2/3/5/12+ packs). STEP 2b's promo-language sweep (1,945 raw hits, 79 distinct taxonomy_ids after filtering 'grain free'/'freeze-dried'/'human grade' false positives) was mostly correct GWP pack_count=1, but spot-checking the top-GMV hits surfaced 3 genuine mismaps. Deepest finding: reviewing the top-80-by-GMV Tier 2 sample uncovered a systemic, category-wide defect — bulk text-matching extraction (documented in this category's own Taxonomy Design Notes) created generic brand+size catch-all taxonomy entries that silently absorbed multiple genuinely distinct product lines/formulas, confirmed in Royal Canin (SKU-145604/145638/145643/145615/145717 merge Kitten, Mother & Babycat, Aroma Exigent, Protein Exigent, and multiple cat-breed Kitten variants under generic 'Feline Health Nutrition' names), Purina ONE (SKU-145721 merges Healthy Adult, Healthy Kitten, Indoor Advantage, Urinary Care, and Hairball Control under one 'Indoor 1.2kg' entry), and Bite of Wild (SKU-146031 still mixes P42/F42 formula products under a 'K42' name after this session's partial cleanup). | Bulk-fixed brand_casing_mismatch across 258 entries (canonical_name brand prefix rewritten to match brand_dict.canonical_name exactly). Applied 10 precision fixes to Royal Canin/Taste Of The Wild/Sheba/misc entries, replacing generic marketing-tagline or reseller-noise text in product_line/canonical_name with the real distinguishing line name read from sku_name (verified each bucket was genuinely single-line before renaming, not a hidden multi-line collapse). Rerouted 3 individually-verified mismapped products to existing correct taxonomy entries via product_taxonomy_map UPDATEs (no new entries minted, no rows deleted). Promoted 272 taxonomy_ids to review_confidence='confident' (258 casing-fixed + 10 precision-fixed, all clean on same-session Tier-1 recheck, plus 4 never-reviewed entries directly verified as unambiguous single-product matches). Did NOT attempt the systemic multi-line-collapse remediation (Royal Canin/Purina ONE/Bite of Wild) or the 11-bucket pack-count-catch-all reroute — both require minting multiple new taxonomy entries and rerouting hundreds of products by real per-product line/pack-count, sized beyond this session's turn and 200-slot SKU budget; left _meta untouched on all discovered-defective-but-unfixed entries so they remain in a future session's worklist. All hard gates re-verified after fixes: G1 dual-mapped=0, G2 HUMAN+LLM coexistence=0, placeholder-leak=0, structured-fields NULL%=33% (within 50% threshold), duplicate product_id=0, garbled-brand-text=0, canonical-field-mismatch=0, G5 provenance=0 — matches the pre-fix STEP 1B report exactly, no regressions introduced. |
| 2026-07-28 17:43 UTC | Automated review session (auto-discovery) | Tier 1 SQL sweep: 0/1,428 worklist rows flagged (all 8 flag classes clean) — this category's Tier 1 defect classes appear exhausted by 9 prior sessions' cumulative fixes. STEP 2c's shared-bucket sweep flagged 9 taxonomy_ids; whole-bucket aggregation confirmed SKU-146907 (Royal Canin Indoor) is a false positive (genuinely multi-size 500g/1kg, regex undercounted), and 6 more are real pack-count-multiplier catch-alls: COUCOU Snack 85g (SKU-146421) and Furlove Bento Pouch 80g (SKU-146017) were clean single-multiplier text with no existing catalog duplication risk; Sheba Pouch 70g (SKU-146164), Whiskas Pouch 80g (SKU-146577), and Royal Canin Kitten 85g (SKU-146069) were left untouched because their brand catalogs already carry dozens of near-duplicate entries per pack size (the same catalog-wide-duplication class documented for Royal Canin/Mr.Vet/ProDiet/Bite of Wild in prior sessions) and/or their sku_names use ambiguous nested multiplier text ("[8 x Value Pack (6 Packs)]", "(5+1)") that risks a wrong pack_count without image confirmation. Separately found a product-line collapse at PAWROO (SKU-191186): a catnip/grass powder topper and an unrelated freeze-dried chicken breast product were merged under one generic entry. GMV-prioritized Tier 2 spot-check (caveat: Tiktok GMV figures are ~1000x corrupted per this category's documented data issue, so ordering is rough) surfaced a major, previously-undiscovered scope defect: 53 taxonomy_ids / 70 product_taxonomy_map rows under master_table='makanankucing_my' are genuine dog-food products (canonical_name explicitly states "Dog", e.g. "Royal Canin Dog Adult Large Breed 13kg", "Orijen Fish Dog 2kg", "Hill's Z D Dog 7.98kg", "Wanpy Dog 375g") despite this category's own documented Scope section excluding all dog food — none of the 9 prior sessions caught this since no prior QA pass checked scope against canonical_name text. Also found 2 individual OOS health-supplement products misclassified as food: SKU-193848 "Cassiel Immune Booster 150ml" (liquid wellness formula "For Dogs & Cats", not food) and a PETSUP Fish Oil supplement inside SKU-191146's bucket — same exclusion class as the WormShield dewormer precedent from the ninth top-up session. STEP 2b's promo-language sweep returned 227 distinct taxonomy_ids (1,372 raw hits), consistent with the established finding that this sweep is dominated by legitimate GWP pack_count=1 false positives; not exhaustively reviewed, folded into Tier 2 budget. | Rerouted 7 products out of the COUCOU Snack 85g and Furlove Bento Pouch 80g catch-alls to their correct pack-count-specific entries (3 existing: SKU-146179, SKU-191056, SKU-146379; 1 newly minted: SKU-195249 Furlove Bento Pouch 80g x48, 2 products). Split SKU-191186's product-line collapse: renamed the retained entry to 'PAWROO Grass Flavour Powder Topper' (3 products) and minted SKU-195250 'PAWROO Freeze Dried Chicken Breast' (1 product, is_multi_size=TRUE for its stated 75g/500g). Promoted all 3 touched/created taxonomy_ids to review_confidence='confident' after an immediate same-session Tier-1 recheck came back fully clean. Left the dog-food scope contamination (53 taxonomy_ids/70 products) and the 2 supplement-miscategorization products unfixed and undeleted — correcting them requires deleting product_taxonomy_map rows, which this session's STEP 7 forbids; flagged for a deletion-authorized session. Left the Sheba/Whiskas/Royal Canin pack-count catch-alls (SKU-146164, SKU-146577, SKU-146069) untouched for the same catalog-duplication-risk reason documented in prior sessions. Post-fix hard gates: 0 dual-mapped(LLM), 0 HUMAN+LLM coexistence, 0 placeholder-leak, 0 G5 provenance gaps — no regressions. Confidence distribution left behind: 1,925 total taxonomy entries mapped to this category, 498 confident (495 prior + 3 this session), 1 unconfident (SKU-145588, pre-existing, untouched, needs a third confirming review), 0 fixed-pending-recheck, 1,426 never-reviewed. |
| 2026-07-28 18:08 UTC | Automated review session (auto-discovery) | Auto-discovery pass over the 1,427-entry never-reviewed/unconfident worklist: Tier 1 SQL sweep (stub_leak, duplicate_brand, wrong_field_order, brand_casing_mismatch, excess_content, canonical_field_mismatch, null_size, garbage_brand) came back 100% clean — this category's prior sessions already drove those defect classes to zero. STEP 2c's shared-bucket size/multiplier sweep found 9 buckets with a genuine single-value size or pack-count silently dropped (e.g. 'Royal Canin Kitten 85g' catch-all actually hiding x12/x24/x36 multipacks; 'Snappy Tom Tin Tin 400g' hiding an explicit x24 case; 'SmartHeart Refine 85g x48' whose real size was 70g per every product's own sku_name). A GMV-prioritized Tier 2 sample of 40 un-flagged rows surfaced a second, more systemic defect class not caught by any SQL sweep: several high-GMV Royal Canin buckets (85g x12, 4kg, 2kg, and especially a 10kg bucket carrying 40 products) are generic-range catch-alls ('Feline Health Nutrition'/'Hair And Skin') silently absorbing genuinely distinct on-label product lines (Kitten, Mother & Babycat, Sensible 33, Protein/Aroma Exigent, Sterilised, Instinctive, British Shorthair, Persian, Indoor 27, Fit 32, Urinary Care) — a D2/D3 variant-collapse defect this category's own QA History had already flagged as an accepted risk from the reuse-before-mint-by-(brand,size,pack)-only simplification. A same-shaped Purina ONE 1.2kg catch-all (Urinary Care/Indoor Advantage/Hairball Control/Healthy Kitten all merged) was found but not fixed — flagged below. Also hit a live SKU block collision: an unrelated concurrent session inserted 'Coffee Product' rows into this session's atomically-claimed SKU-195649-195658 block About 9 minutes after this session's own insert, duplicating 5 taxonomy_ids' primary keys. | Fixed all 9 shared-bucket size/pack-count defects: reused 5 already-existing exact-match taxonomy entries and minted 3 new ones (later relocated, see below) for the remainder, rerouting 12 products. Split the Royal Canin 10kg/4kg/2kg generic-range catch-alls: reused 12 existing line-specific entries and minted 2 new ones (Sensible 33 4kg; Sterilised Adult 85g x12), rerouting 29 unambiguous products (left ~20 keyword-stuffed/genuinely-ambiguous reseller titles on their original bucket rather than guess). Collision resolved per this category's own documented brand_dict-collision precedent: deleted this session's 5 rows by exact (taxonomy_id, canonical_name, brand_id) match — never a blind delete-by-taxonomy_id — claimed a freshly-verified-clean block (SKU-197649-197658), re-inserted the same 5 rows there, and repointed all affected product_taxonomy_map rows. Every changed/reused/orphaned taxonomy entry got its _meta reset and an immediate same-session Tier 1 recheck; all 31 fixed-this-session entries plus 29 never-reviewed rows sampled and Tier-2-judged correct were promoted straight to review_confidence='confident' (60 total promotions this session). All hard gates pass post-fix: 0 dual-mapped, 0 HUMAN+LLM coexistence, 0 duplicate product_id/product+taxon, 0 placeholder-leak, 33% NULL product_line (under the 50% threshold), 0 provenance gaps. |
| 2026-07-29 02:43 UTC | Automated review session (auto-discovery) | STEP 1C fast-lane: 4 fixed-pending-recheck rows (SKU-145690, 145771, 146796, 146379) came back Tier-1-clean; promoted per the literal given SQL (landed on 'unconfident', not 'confident' — prior _meta had no last_verdict='correct' to agree with, so this counts as their first clean confirmation, not their second; converges next session). STEP 2 Tier-1 SQL sweep across the full 1,378-row worklist returned zero flags on every check (stub_leak, duplicate_brand, wrong_field_order, brand_casing_mismatch, excess_content, canonical_field_mismatch, null_size, garbage_brand) — verified this wasn't a broken sweep via an independent direct-count query before trusting it (this category's bulk-regex build produces uniformly brand-prefixed, x{N}-suffixed names, so a genuine zero is plausible). STEP 2c flagged 4 taxonomy_ids from single-readable-size/multiplier text not reflected in canonical_name; 3 were false positives of the heuristic (already-correct is_multi_size/pack_count=1 entries where the sku_name's '1pcs'/single-size tag just confirmed the existing value). The 4th, SKU-146577 'Whiskas Pouch ... 80g' (pack_count=1), was a genuine shared-bucket defect: 6 Tiktok listings with real multipack counts (6/12/24/28/28/36) had been silently routed into a bucket meant for a single Shopee variety-pouch listing. Verified the ambiguous '(N x) Value Pack (M Packs)' phrasing via per-pouch price-ratio cross-validation (~2.1-2.3 MYR/pouch consistent across all 6 listings) since TikTok GMV/price data for this category is known-corrupted at scale but the relative per-listing ratios still held up as a secondary signal in lieu of image confirmation. GMV-prioritized Tier-2 sample (top ~45 of 1,378 by Shopee GMV) surfaced two more shared-bucket / variant-collapse defects: SKU-145801 'Pet Factory ... 10kg' merged two genuinely different formulas (Ocean Fish & Tuna vs. Gemuk Digest & Immune Pro) under one entry; SKU-145721 'Purina ONE Indoor 1.2kg' (the single highest-GMV entry in the worklist, 1.27M MYR) was a D3 variant-collapse: 17 products spanning 6+ distinct real flavors (Indoor Advantage, Hairball Control, Urinary Care, Healthy Adult, Healthy Adult w/ Salmon & Tuna, Healthy Kitten) plus genuinely ambiguous multi-flavor-selector listings were all merged under the 'Indoor' flavor name alone. Also found SKU-145794 'Orijen ... ' had lost a digit from its own sku_name during original extraction ('Guardian 8' -> 'Guardian') and needed is_multi_variant=TRUE (3 real flavors, not one). Largest single finding, NOT fixed (fixing requires deleting/unmapping a product_taxonomy_map row, which this session never does): 53 of this category's taxonomy entries (spanning the whole category, not just the 1,378-row worklist) are dog-only products (Royal Canin Dog Gastrointestinal, Orijen Fish Dog, Hill's Z/D Dog, etc.) incorrectly mapped under master_table='makanankucing_my' -- ~1.05M MYR in Shopee GMV (TikTok GMV excluded as unreliable per this category's known data issue). Verified this is distinct from 4 other entries (Cattus, Nanovet Topper, Petlusci, PETSUP) that are genuinely 'for cat and dog' products and correctly in scope -- do not conflate the two groups in a future cleanup. | Fixed and promoted to confident: SKU-145650, SKU-145666 (genuine Tier-2 'correct' verdicts, Tier-1-clean), SKU-145794 (restored dropped digit + is_multi_variant fix, clean post-fix recheck), SKU-145801 (split off the Gemuk Digest & Immune Pro formula into new SKU-197660, clean post-fix recheck on both halves). Rerouted without new entries: 5 Whiskas Tiktok products to existing correct-pack-count 80g entries (SKU-145975 x6, SKU-145670 x12, SKU-145702 x24, SKU-146006 x28 x2). Minted 8 new entries and rerouted 13 products: SKU-197659 (Whiskas 80g x36, price-ratio-confirmed), SKU-197660 (Pet Factory Gemuk Digest & Immune Pro 10kg), SKU-197661-197666 (6 Purina ONE flavor-specific 1.2kg entries: Indoor Advantage w/ Chicken, Hairball Control w/ Chicken, Adult Urinary Care w/ Chicken, Healthy Adult, Healthy Adult w/ Salmon & Tuna, Healthy Kitten w/ Chicken), with SKU-145721 renamed to 'Purina ONE Selection 1.2kg' (is_multi_variant=TRUE) as the residual catch-all for the 7 remaining genuinely-ambiguous flavor-selector listings. New/fixed entries left at _meta={'is_reviewed': false} (unreviewed) pending a future pass except the 4 promoted above. Confidence distribution after this session: 4 entries confident (newly promoted), 4 entries unconfident (STEP 1C fast-lane, one clean confirmation down, needs one more agreeing review), 9 entries unreviewed-pending (this session's new/renamed entries), 1,382 total taxonomy entries remain in the incremental worklist (1,378 - 4 promoted + 8 newly created). Dog-scope contamination (53 entries, ~1.05M MYR) flagged for a future deletion-authorized session -- do not attempt to fix by rerouting within this category, these products don't belong here at all. |
| 2026-07-29 03:04 UTC | Automated review session (auto-discovery) | Auto-discovery review of the 1,382-row never-reviewed/unconfident worklist for makanankucing_my (Shopee+Tiktok). STEP 1C fast-lane: 9 fixed-pending-recheck rows (SKU-145721/197659-197666, Purina ONE flavor-split entries from the prior session) came back fully Tier-1-clean; promoted (landed on 'unconfident' — first clean confirmation, not second). STEP 2's Tier 1 SQL sweep across the full worklist returned zero flags on all 8 checks — this category's mechanical defect classes remain exhausted, consistent with the last several sessions. STEP 2c's shared-bucket sweep returned 3 hits, all already-documented false positives (SKU-146907 Royal Canin Indoor genuinely multi-size; 2 trivial '1pcs'=pack_count=1 matches). GMV-prioritized Tier 2 sampling (top-60 by Shopee GMV plus a 25-row slice of STEP 2b's promo-language hits) surfaced a systemic defect class Tier 1's exact-string checks structurally cannot catch: bulk-regex extraction artifacts — stray 'G' tokens leaked immediately before size units (e.g. 'In Jelly G 85g x24'), '!!'/'!!!'/'<!!!>' promotional-banner punctuation carried into canonical_name, 'TPF'/merchant-SKU-code suffixes, and duplicated brand-name tokens differing only in casing/spacing/apostrophe from brand_dict's exact string (e.g. 'SmartHeart Smart Heart Pouch', 'iCats ICat's Icat', Cindy's Recipe apostrophe form) — none trip duplicate_brand's exact-REPLACE check. A bulk scan of the whole worklist for this pattern class found 71 affected entries (68 auto-detected + 3 needing individual handling: a genuine 4-formula buyer-choice Happy Cat listing wrongly flattened into one stub name, a redundant '..........M&B' abbreviation tail, and a badly garbled SmartHeart entry with cross-brand 'MEO ME-O' SEO-stuffing noise and a dropped x4 multiplier). Also found 2 genuine pack-count misses via GMV sampling where sku_name stated an explicit, unambiguous multiplier never captured at extraction time: Snappy Tom canned food ('12can x 400g', was pack_count=1) and Amelisa Rawly Stick ('1Box = 16g x 25Sticks', was pack_count=1). One entry (SKU-145764, A-Tier Freeze Dried Chicken) had a genuine size-range defect matching this category's own established '500g/1kg buyer-choice' exclusion precedent — sku_name stated '500/100 Grams' with a 5x ratio (not a labeling-tolerance range) but had been confidently mapped to a single guessed 100g. Two more size-ambiguous entries (SKU-145668 Farmina N&D stating both 5kg and 10kg, SKU-145730 Farmina N&D stating both 1kg Grain-Free and 700g Quinoa) were found but left unresolved pending deeper investigation, not promoted. | Fixed and Tier-1-rechecked-clean 74 taxonomy entries: bulk REGEXP_REPLACE UPDATE stripped stray 'G'-before-size/'!!'-punctuation/'TPF' artifacts across 58 rows in one statement (per CLAUDE.md's preferred bulk-SQL-per-defect-class pattern), plus 13 individually-fixed rows (3 needing custom handling beyond the bulk pattern — SKU-146208 converted to is_multi_variant=TRUE 'Happy Cat Dry Food 10kg', SKU-145980 stripped the '..........M&B' tail, SKU-146542 rebuilt as 'SmartHeart Creamy Treat Snack Stick 15g x4' with pack_count corrected to 4 and the ME-O SEO-stuffing noise removed; plus 10 other stray-fragment/duplicate-brand rows found via manual GMV-list inspection). Applied the 2 genuine pack-count fixes (Snappy Tom pack_count 1→12, canonical 'Snappy Tom Tin 400g x12'; Amelisa pack_count 1→25, canonical '...16g x25'). Fixed SKU-145764 by converting to is_multi_size=TRUE/size=NULL rather than guessing between the two stated values, matching this category's own established don't-guess precedent for genuine (not tolerance-range) size ambiguity. Every fixed row had meta_agent='CLAUDE_CODE' and _meta reset in the same UPDATE, then was immediately Tier-1-rechecked in this session (batch-verified 0/74 flags) before promotion — no waiting for a future pass. Promoted 129 taxonomy_ids total this session: 74 via Path 1(b) (fixed + clean same-session recheck), 45 via Path 1(a) (never-reviewed, Tier-2-judged correct, Tier-1-clean — the rest of the GMV-60 sample with no defect found), 9 via STEP 1C fast-lane, 1 via Path 2 (SKU-145690, second agreeing 'correct' verdict → confident). Left unresolved and un-promoted: SKU-145668 and SKU-145730 (genuine residual size ambiguity, need image/bucket investigation a future session should prioritize), the pre-existing SmartHeart/Royal Canin catalog-duplication buckets my fixes ran alongside but did not merge (e.g. SKU-146024's cleaned text is now near-identical to pre-existing SKU-146520 — same already-documented systemic dedup issue from prior sessions, out of scope for a routine sweep), and the 53-taxonomy-id dog-food scope contamination + 2 supplement-miscategorization products flagged by the immediately preceding session (both require deleting/unmapping product_taxonomy_map rows, which this session's STEP 7 forbids). Post-fix hard gates: 0 dual-mapped, 0 HUMAN+LLM coexistence, 0 placeholder-leak, 33% structured-fields NULL (unchanged, under the 50% threshold), 0 G5 provenance gaps, 0 duplicate product_id — no regressions. Confidence distribution left behind: 1,382 confident, 29 unconfident, 1,884 never-reviewed. |
| 2026-07-29 03:21 UTC | Automated review session (auto-discovery) | Auto-discovery review of the 1,262-row never-reviewed/unconfident worklist for makanankucing_my (Shopee+Tiktok). STEP 1C fast-lane bucket was empty (0 rows). STEP 2's Tier 1 SQL sweep across the full worklist returned zero flags on all 8 checks. STEP 1B's pre-run qa_report.sh 'canonical_name fields' gate FAIL (count=1) traced to SKU-145841 (Amelisa Rawly Stick), an already-'confident' row from the immediately preceding session whose product_line field retained a stray '1Box = Sticks' pack-descriptor tail never reflected in canonical_name. STEP 2c's shared-bucket sweep returned 3 hits (SKU-146907, SKU-146421 x2), all re-confirmed (2nd confirmation) as pre-existing false positives already documented by the prior session (SKU-146907 genuinely multi-size; the others trivial '1pcs'=pack_count=1 matches) — no fix needed, no qa_gate_exceptions entry warranted since these aren't STEP-1B gate names. GMV-prioritized Tier 2 sampling (top-60 by Shopee-only GMV, since Tiktok GMV is documented-corrupted) surfaced: (1) the two previously-flagged-but-unresolved genuine size-ambiguity entries SKU-145668 (Farmina N&D, sku_name states both 10kg and 5kg buyer-choice) and SKU-145730 (Farmina N&D PRIME, states two distinct formula+size combos 'Grain Free 1KG / QUINOA 700G') — both matched this category's own established multi-size buyer-choice precedent; (2) 5 more instances of the systemic 'duplicate brand token differing in casing/spacing' defect class the prior session identified as escaping Tier 1's exact-REPLACE check (SKU-146089 'Dormeo...Dormeo', SKU-145926/146322 'Pawcipes...Pawcipes', SKU-146045 'ICat's Icat', SKU-146111 'Ciao CIAO'); (3) assorted extraction-artifact defects not caught by any Tier 1 regex: an empty CJK-stripped bracket '【 】' (SKU-145763), promotional discount-banner text '【Buy Off RM6】' leaked into canonical_name (SKU-145807), a truncated/duplicated 'THE THE FOOD' fragment alongside a brand_dict name correction 'Boss Cat'→'The Boss Cat' (SKU-145785, verified against both of its sku_names), and 5 more repeated-word artifacts (SKU-145762 SmartHeart/'Smart Heart' triplicated 'Pouch', SKU-145775 'Treats Treats', SKU-155027 'Freeze Dried Booster' doubled, SKU-145821 'Tin...Tin', SKU-145828 'Tuna With Milk' doubled); (4) a genuine D3-class variant-collapse/bucket-contamination defect at SKU-145755 ('Happy Cat Minkas Urinary Care...'), which held 4 products with materially different formulas (a generic 'Minkas 10kg' listing, a 'Perfect Mix Urinary Kitten Hairball Lamb Salmon' listing, a bare 'Happy Cat Dry Cat Food 10kg' listing with no line name, and the one Shopee product genuinely matching the stored name) collapsed under one taxonomy entry. Also noted but not actioned (out of this session's authority): two Tier-2-reviewed rows (SKU-145775 Tommy Kitchen Salmon Fillet, SKU-145820 Dr.Bites Booster) are dual dog/cat-labeled treats whose in-scope status is arguably borderline per this category's dog-food exclusion rule — names were corrected but the scope question itself would require unmapping, which this session's STEP 7 forbids. | Fixed the 1 qa_report.sh gate failure (SKU-145841 product_line cleared of stray artifact text). Fixed both known size-ambiguity entries via the established multi-size/multi-variant conversion (SKU-145668: is_multi_size=TRUE, size=NULL; SKU-145730: is_multi_size=TRUE + is_multi_variant=TRUE, size/variant=NULL). Fixed 5 duplicate-brand-token defects and 8 other stray-artifact/repeated-word defects via individual UPDATEs (18 rows total, all bulk-pattern-ineligible since each needed distinct cleaned text), including one brand_dict.canonical_name correction ('Boss Cat'→'The Boss Cat', single-product brand, safe). Claimed SKU block SKU-203329–SKU-203528 (200 slots) and split SKU-145755's contaminated bucket into 3 new taxonomy entries (SKU-203329 'Happy Cat Minkas 10kg', SKU-203330 'Happy Cat Perfect Mix Urinary Kitten Hairball Lamb Salmon 10kg', SKU-203331 'Happy Cat 10kg'), rerouting the 3 non-matching product_taxonomy_map rows via bq query DML; SKU-145755 itself was left correctly matching its one remaining (Shopee) product. Used 3 of 200 claimed slots (197 unused). Every fixed/newly-minted row had meta_agent='CLAUDE_CODE' and _meta reset to {"is_reviewed": false}, then was immediately Tier-1-rechecked in this same session (one row, SKU-145768, needed a second corrective UPDATE after the recheck caught that the cleanup had been written to the wrong field — product_line instead of variant — then re-passed clean) before promotion. Promoted 27 taxonomy_ids total: 20 via Path 1(b) (fixed/newly-minted + clean same-session recheck), 6 via Path 1(a) (never-reviewed, Tier-2-judged correct, Tier-1-clean: SKU-146907, SKU-145755, SKU-145792, SKU-145820, SKU-146017, SKU-146421), 1 via Path 2 (SKU-145721, second agreeing 'correct' verdict → confident). A final full-worklist Tier 1 re-sweep after all writes confirmed 0 flags on all 8 checks, no regressions introduced. Post-fix hard gates: 0 dual-mapped (LLM-scoped), 0 HUMAN+LLM coexistence, 0 placeholder-leak, 0 G5 provenance gaps, 0 duplicate product_id, canonical_name-fields gate now 0 (was 1, fixed), 34% structured-fields NULL (unchanged, under the 50% threshold). Confidence distribution left behind: 1,425 confident, 22 unconfident, 1,848 never-reviewed. |
| 2026-07-29 03:38 UTC | Automated review session (auto-discovery) | Auto-discovery review of the 1,239-row never-reviewed/unconfident worklist. Tier 1's literal exact-substring duplicate_brand regex (>=2x length ratio) came back 0/1239 flagged, but it misses spacing/punctuation variants of the brand name embedded in product_line/variant — found via manual Tier-2 read of the top-60-GMV sample (SmartHeart: 'SmartHeart Smart Heart Chicken & Tuna 7kg', brand duplicated with a space Tier 1's exact-match couldn't see). A normalized (space/period-insensitive) sweep across the full worklist found this same defect class on 8 more brands (PowerCat, Dr.Bites, Mr.Vet, Happy Cat, Hicubs, Chun Fu, Fish In, Dr.Pikac, Primecut) — 46 taxonomy entries total. STEP 2b's literal promo/multiplier regex over sku_name was dominated by false positives specific to pet food ('free' matching 'grain-free'/'freeze-dried' product descriptors, not promo language); a tightened filter found 3 genuine pack-count misses, including a Buy-1-Get-1 and a 'BELI 10 PERCUMA 2' (buy-10-free-2) promo whose raw Malay text had leaked straight into canonical_name uncleaned. A Snappy Tom 85g pouch entry (SKU-146285) was a genuine shared multi-pack-count bucket (single pouches + a 12pkt + a 48-pouch box all mapped to one pack_count=1 entry) — resolved by rerouting the two multi-unit products to their correct pre-existing x12/x48 entries rather than overwriting the shared bucket. Also found, not fixed (requires deleting/unmapping product_taxonomy_map rows, out of scope for this session per CLAUDE.md/STEP 7): 53 taxonomy entries (69 products) whose canonical_name is unambiguously dog food (e.g. 'Royal Canin Maxi Adult Dog 10kg', 'Hill's Z D Dog 7.98kg', 'Orijen Fish Dog 2kg') mapped under master_table='makanankucing_my' (cat food only per this category's own documented scope) — a cross-category contamination class the existing named QA gates don't check for. | Fixed 46 taxonomy entries via bulk brand-duplication cleanup (canonical_name/product_line/variant), 3 via write-in-place pack_count/canonical_name correction (Bite of Wild BOGO -> x2, JJANGX3 buy-10-free-2 -> x12 with cleaned name, Pro Mill garbled apostrophe text), and rerouted 2 product_taxonomy_map rows to correct existing pack-size entries (no new SKUs minted). All 49 fixed rows re-checked clean against the same-session Tier 1 gate-verify. Promoted 105 taxonomy_ids to review_confidence='confident' this session: 49 fixed rows (Path 1b) + 56 rows given a genuine Tier-2 'correct' verdict during the GMV-prioritized top-60 sample review, all Tier-1-clean (Path 1a/2, including one prior-unconfident row now agreeing 'correct' twice). Worklist reduced from 1,239 to 1,134 taxonomy_ids. Hard gates self-checked clean post-fix (dual-mapped 0, HUMAN+LLM coexistence 0, placeholder-leak 0, duplicate product_id 0, structured-fields NULL% 34%, within threshold). |
| 2026-07-29 04:03 UTC | Automated review session (auto-discovery) | Auto-discovery review of the 1,134-row never-reviewed/unconfident worklist for makanankucing_my (Shopee+Tiktok). STEP 1C fast-lane bucket was empty. STEP 2's Tier 1 SQL sweep (8 checks) returned 0 flags across the full worklist, consistent with recent sessions. STEP 2c's shared-bucket sweep returned 0 hits. STEP 2b's promo/multiplier sweep, tightened to exclude pet-food-specific false positives ('grain-free'/'freeze-dried'/'GMO free'/shipping banners), found 44 distinct candidates; manual review confirmed 8 genuine defects: 4 real pack-count misses (Snappy Tom 70gx12, Amelisa Rawly Stick x22, Sniffly Stick Treats x5, Mossy 'Percuma 1 Beli 1'=x2), 1 unusual promo requiring image verification (Bite of Wild FARM 'Buy 3 Get 7 Free' — confirmed via product image as 3x1.5kg bags + GWP treats, not x10), 2 bulk-regex artifact cleanups (Reflex 'Refle Kg' duplicate fragment, TOTW 'Free Shipping‼️' leaked promotional text, the latter also revealing a genuine multi-variant listing for two named TOTW recipes), and 1 cross-brand bundle (Friskies Party Mix 60g + Felix Play Tubes 50g) whose canonical was garbled by bulk extraction. Also found and reverted a genuine shared-bucket size miss: Whiskas SKU-146717 served both a single-70g product and a 24-pack-carton product under one pack_count=1 entry; rerouted the 24-pack product to a pre-existing exact-match entry (SKU-157408) rather than overwriting the shared bucket. GMV-prioritized Tier 2 sampling (top-60 by Shopee GMV) surfaced a major, previously-undocumented systemic defect class: Royal Canin variant-collapse buckets, where multiple genuinely distinct recipes (Kitten, Sterilised, Mother & Babycat, Urinary S/O, Diabetic, Hair & Skin, etc.) are silently folded under one generic taxonomy entry per pack size. Found across 6 different RC size buckets (400g, 10kg, 3.5kg, 85g-pouch, and two separate 4kg dry-food buckets) — fixed the first 4 (SKU-146002, SKU-146032, SKU-146137, SKU-146232), covering 12 distinct recipes plus 2 genuine multi-variant buyer-choice listings; left the remaining 2 (SKU-146968 with ~14 distinct recipes, SKU-146126 with ~5) unfixed as too large for this session's remaining SKU-block budget. Also found smaller-scale defects: Snappy Tom (SKU-146227) and Family Pets (SKU-145895) duplicate-token garbling not caught by Tier 1's exact-substring check (spacing/repetition variants), a TOTW 'Mountian' typo (SKU-145951), a Josera multi-variant listing missing its is_multi_variant flag (SKU-145929), SPACECAT punctuation-garbled canonical (SKU-146000), a Wanpy stray-'G' artifact (SKU-146345), and a smaller 2-product Tommy's Kitchen bucket (SKU-155037, 'Catnip Biscuit' vs 'Premium Crunchy Bites' collapsed together) left unfixed pending a dedicated pass. | Fixed 8 STEP-2b-sourced defects (pack-count corrections verified against source text/image, 2 garble cleanups, 1 bundle restructure with is_bundle=TRUE) plus 1 shared-bucket reroute (Whiskas), all individually UPDATEd since none were bulk-pattern-eligible. Claimed SKU block SKU-208029–SKU-208048 (20 slots) and split 4 Royal Canin variant-collapse buckets into 16 correctly-scoped entries: SKU-146002/208029/208030 (400g: Sterilised Adult/Mother & Babycat/Kitten), SKU-146032/208031 (10kg: Sterilised 37/Kitten), SKU-146137/208032-208037 (3.5kg: Satiety Support/Urinary S/O/Diabetic/Neutered Satiety Balance/Indoor 7+ Senior/Skin & Coat/Vet Care Senior Stage 1), SKU-146232/208038-208043 (85g pouch x12: multi-variant Hair&Skin-Instinctive-Kitten-Sterilised catch-all/Sensitivity Control/Urinary S/O/Wet Range Kitten/Instinctive Adult/Hair&Skin Gravy/Sterilised Adult), rerouting 20 product_taxonomy_map rows to the correct new/existing entries (used 15 of 20 claimed slots). Fixed 6 more artifact/typo/metadata defects from the GMV-60 Tier 2 sample (SKU-145895, SKU-146227, SKU-145951, SKU-145929, SKU-146000, SKU-146345). Every fixed/newly-minted row had meta_agent='CLAUDE_CODE' and _meta reset, then immediately Tier-1-rechecked in this same session before promotion. One fixed row (SKU-146733, the Friskies/Felix bundle) still trips Tier 1's excess_content/null_size flags on recheck — structural false positives for a legitimate 2-component bundle with no is_bundle carve-out in the current Tier 1 sweep; left unpromoted ({"is_reviewed": false}) for a future session to either add the carve-out or accept via qa_gate_exceptions. Promoted 79 taxonomy_ids to review_confidence='confident': 47 via Path 1(a) (never-reviewed, Tier-2-judged correct, Tier-1-clean) and 32 via Path 1(b) (fixed/newly-minted this session, clean on immediate recheck — includes all 15 new RC split entries and 17 other fixed entries, excluding SKU-146733). Post-fix hard gates: 0 dual-mapped (LLM-scoped), 0 HUMAN+LLM coexistence, 0 placeholder-leak, 33% structured-fields NULL (unchanged, under 50% threshold), 0 G5 provenance gaps, 0 duplicate product_id — no regressions. Confidence distribution left behind: 881 confident, 10 unconfident, 1,059 never-reviewed, 1 fixed-pending-recheck (SKU-146733). |
| 2026-07-29 04:21 UTC | Automated review session (auto-discovery) | Auto-discovery review of the 1,070-row never-reviewed/unconfident worklist for makanankucing_my (Shopee+Tiktok). STEP 1C fast-lane: 1 fixed-pending-recheck row (SKU-146733, Friskies/Felix bundle) re-confirmed as the same structural false positive already documented in the prior session (excess_content/null_size trip on a legitimate 2-component bundle with no is_bundle carve-out in the Tier 1 sweep) — left unpromoted, not eligible for qa_gate_exceptions since neither flag is a named STEP-1B gate. STEP 2's Tier 1 SQL sweep across the full worklist returned only that same 1 flagged row (all 8 checks otherwise 0/1070) — this category's mechanical defect classes remain exhausted after 11 prior sessions. STEP 2c's shared-bucket sweep returned 0 hits. STEP 2b's promo/multiplier sweep (911 raw hits) was, after filtering established pet-food false positives (grain-free/freeze-dried/GMO-free/free-shipping/'(1 x Nkg)' single-unit notation), almost entirely GWP/single-unit false positives — only 1 genuine miss found (SKU-146832, see below). GMV-prioritized Tier 2 sampling (top-90 Shopee-GMV entries, Tiktok GMV excluded as unreliable per this category's documented data issue) surfaced 13 genuine defects Tier 1's exact-string checks structurally cannot catch: bulk-regex garbling artifacts (stray '=' character on a Purina Felix entry, garbled 'BO G!'/'T S C' fragments on a Misko entry, truncated mid-string text on a Snappy Tom flavor-selector entry and a Cindy's Recipe entry, a stray full-width bracket '）' orphaned after an ASCII '(' mismatch on a Cindy's Recipe Delight entry, repeated-word duplication on a Tommy Kitchen and a Wanpy entry), leaked reseller/promo banner text (a 'READY STOCK MALAYSIA' banner on a Whiskas entry, 'FREE SHIPPING' on a Fussie Cat entry, 'Buy Free Gift'/'Buy 3 Free Gift' promo banners on an Uncle Tails entry, a '10/20 PCS BUNDLE' buyer-choice fragment on an EZPET entry), and 2 genuine multi-size/multi-variant misses incorrectly written as fixed single values (Cindy's Recipe Original Can, stating both 70G/80G plus a 3-flavor selector; Chun Fu, a Chinese-language listing stating both 1kg/1.5KG plus kitten/adult formula choice). Also found one genuine shared-bucket dedup opportunity outside the GMV-90 sample: SKU-146832 ('Sheba Pouch Pkt Pouch 70g', garbled product_line, pack_count=1) held a single Shopee product whose own sku_name explicitly states 'Value Pack 70g x 6pkt' (6-pouch bundle), and an exact-match existing entry (SKU-145696 'Sheba Wet Pouch Food 70g x6') already existed for this same brand+size+pack_count combination. The rest of the GMV-90 sample (15 rows individually cross-referenced against their own sku_name text) matched their canonical_name correctly with no defect found. Known, unfixed, and out of scope for this session (documented in every recent QA History entry, requires deleting/unmapping product_taxonomy_map rows which STEP 7 forbids): ~53 taxonomy_ids / ~70 products that are unambiguously dog food (Royal Canin Dog, Orijen Fish Dog, Hill's Z/D Dog, etc.) incorrectly mapped under master_table='makanankucing_my'; the Royal Canin/Mr.Vet/ProDiet/Bite of Wild catalog-wide variant-collapse and duplication pattern documented across many prior sessions; and the named residue list from the ninth top-up session (Catty Bistro Bites/Pawmised, FURREVER/Petfos/Pets Science/LUMeO) still blocked on a size signal. | Fixed and Tier-1-rechecked-clean 12 taxonomy entries via individual UPDATEs (garbling/artifact cleanup on 10: SKU-145992, SKU-146120, SKU-146239, SKU-146316, SKU-145864, SKU-146285, SKU-146115, SKU-146124, SKU-145927, SKU-146021; genuine multi-size+multi-variant conversion on 2: SKU-146049 Cindy's Recipe Original Can, SKU-155040 Chun Fu). Deduplicated SKU-146832's single mapped product by rerouting its product_taxonomy_map row to the pre-existing exact-match entry SKU-145696 ('Sheba Wet Pouch Food 70g x6') rather than fixing the garbled/wrong-pack-count SKU-146832 in place, since a correct duplicate already existed (SKU-146832 is now orphaned, harmless). Every fixed row had meta_agent='CLAUDE_CODE' and _meta reset to {"is_reviewed": false}, then was immediately Tier-1-rechecked in this same session (0/12 flags on recheck) before promotion — no waiting for a future pass. Promoted 27 taxonomy_ids to review_confidence='confident': 12 via Path 1(b) (fixed this session, clean on immediate recheck) + 15 via Path 1(a) (never-reviewed, genuinely Tier-2-judged correct via direct sku_name cross-reference, Tier-1-clean: SKU-146194, SKU-146191, SKU-145889, SKU-146238, SKU-146161, SKU-146385, SKU-146074, SKU-146121, SKU-146133, SKU-146071, SKU-146016, SKU-146027, SKU-146254, SKU-155037, SKU-146350). SKU-146733 (Friskies/Felix bundle) left unpromoted per STEP 1C — same structural false positive as the prior session, still not eligible for a qa_gate_exceptions carve-out since excess_content/null_size aren't named STEP-1B gates. Post-fix hard gates: 0 dual-mapped (LLM-scoped), 0 HUMAN+LLM coexistence, 0 placeholder-leak, 0 G5 provenance gaps, 0 duplicate product_id/platform — no regressions. Confidence distribution left behind: 908 confident (881 prior + 27 this session), 10 unconfident, 1 fixed-pending-recheck (SKU-146733), 1,031 never-reviewed, 1,950 total taxonomy entries mapped to this category. |
| 2026-07-29 04:43 UTC | Automated review session (auto-discovery) | Tier 1 SQL sweep across the full 1,042-row worklist came back almost entirely clean (1 flag: SKU-146733, a legitimate cross-brand bundle tripping null_size/excess_content — those checks lack an is_bundle exemption analogous to is_multi_size). STEP 2b's promo-language regex was extremely noisy for this category (773/868 raw hits were 'Grain Free'/'Freeze Dried' false-matching on 'free', not promos). Tier 2 GMV-prioritized sampling (73 products, ~40 Shopee + ~33 Tiktok, Tiktok GMV known-unreliable) found a major D3 contamination: SKU-146968 ('Royal Canin Sterilised 4kg') had 33 distinct Royal Canin 4kg product lines (Fit 32, Indoor 27, Digestive Care, Persian, British Shorthair, Hairball Care, Maine Coon, Mother & Babycat, Urinary Care, Renal, Exigent variants, Hair & Skin, Gastrointestinal, plus ambiguous multi-line reseller listings) misrouted into it — found only after switching an inner-join membership query to a LEFT JOIN, which surfaced 33 rows the inner join had silently dropped. Also found: 2 genuine pack-count misses (100+20 promo, Buy-1-Free-1), 1 entry whose size/pack fields had captured a GWP booster's stats instead of the main product's, 1 wrong-variant-text entry, and a near-duplicate Pet Factory entry pair left unresolved for a future pass. | Rerouted 32 of the 33 Royal Canin products to their correct pre-existing taxonomy entries (1 genuinely belonged to Sterilised, kept). Fixed 2 pack-count entries in place (Paws & Tails 15g x100→x120, Mossy Chicken 500g→500g x2). Rerouted 2 more single-product misassignments to correct pre-existing sibling entries (JJANGX3 wet pouch, LGD 10kg main bag). Corrected 1 wrong variant/canonical text (Rich Choice Cheek Booster). All fixed rows re-verified clean on immediate Tier-1 recheck and promoted to confident this session. Promoted 75 entries total to confident (69 never-reviewed-and-clean, 4 freshly-fixed-and-verified, 2 re-reviewed agreements); 8 remain unconfident, 956 never-reviewed, 1 fixed-pending-recheck. All hard gates pass (dual-mapped 0, HUMAN+LLM coexistence 0, placeholder-leak 0, structured-fields-NULL 33%, duplicate product_id 0, provenance 0). Not fixed, flagged for a future session: near-duplicate Pet Factory entries (SKU-145875/SKU-146177, same line split by cosmetic text differences), and lower-severity multi-line-ambiguous-listing routing scattered across a few other Royal Canin buckets outside this session's sampled scope — Royal Canin is the #1 GMV brand in this category and likely merits a dedicated brand-wide audit given the scale of contamination found in just one bucket this session. |
| 2026-07-29 05:30 UTC | Automated review session (auto-discovery) | Auto-discovery review of the 965-row never-reviewed/unconfident worklist for makanankucing_my (Shopee+Tiktok), plus 2 STEP 1C fast-lane rows. STEP 1C: SKU-146733 (Friskies/Felix bundle) re-confirmed as the same documented structural false positive from the prior 2 sessions (excess_content/null_size trip on a legitimate cross-brand bundle, no is_bundle carve-out in the Tier 1 sweep) — third consecutive confirmation, still not eligible for qa_gate_exceptions since neither flag is a named STEP-1B gate. STEP 2's Tier 1 sweep returned 0 flags across the full worklist (all 8 checks) except that same known SKU-146733 false positive — this category's mechanical defect classes remain exhausted after 14 prior sessions. STEP 2c returned 0 hits. STEP 2b's promo/multiplier sweep (45 raw hits, 11 distinct taxonomy_ids after filtering) found zero genuine misses this session — all were GWP free-gift language, or a JJANGX3-brand-name false match ('JJANGX3' itself contains 'X3', spuriously read as a x3 multiplier). GMV-prioritized Tier 2 sampling (top-90 by Shopee GMV) surfaced two defect classes: (1) a previously-undiscovered systemic bug specific to brand JJANGX3 — its own brand string 'JJANGX3' was misread as a 'x3' pack-count multiplier during original extraction on at least 6 entries, 2 of which (SKU-145790, SKU-146932) had already been wrongly promoted to review_confidence='confident' in earlier sessions without this being caught; one of the 6 (SKU-146131, a 70g pouch bucket) was a genuine shared-bucket defect hiding two distinct real multiplier values (45pcs and 90pcs) under the fabricated pack_count=3. (2) the same duplicate-token/stray-artifact class documented in sessions 289-292 as escaping Tier 1's exact-substring checks: 'Cindy' / 'Cindy Recipe' repeated 3x within Cindy's Recipe entries (SKU-146061, SKU-146068, SKU-146156), a trailing duplicate 'Felix' on a PURINA Felix entry (SKU-146109), a duplicated 'TOTW' brand abbreviation on a Taste Of The Wild entry (SKU-146083), 'Kibbles Kibble' duplication on a Snappy Tom entry (SKU-146041), a leaked parent-company 'Purina' mention inside a Fancy Feast product_line (SKU-146064), and a stray full-width bracket/duplicated size fragment '（85' on another Snappy Tom entry (SKU-146096). Also found a genuine D2/D3 defect at SKU-146091 ('Royal Canin Indoor 2kg'): both of its 2 mapped products are ambiguous multi-formula buyer-choice reseller listings (none state 'Indoor' alone), mislabeled as a single specific formula. A related, larger instance at SKU-146126 ('Royal Canin Hair & Skin 4kg', 5 products, ~20.6K GMV) was found but NOT fixed — 1 of its 5 products is genuinely single-formula Hair & Skin, the other 4 are ambiguous multi-formula listings; splitting it safely needs more care than this session's remaining budget allowed, and it's another confirmed instance of the already-documented systemic Royal Canin catalog-wide variant-collapse issue (Royal Canin/Mr.Vet/ProDiet/Bite of Wild) that multiple prior sessions have flagged as needing a dedicated dedup/rebuild session rather than routine-sweep fixes. Known, unfixed, out of this session's authority (STEP 7 forbids deleting/unmapping product_taxonomy_map rows): the ~53 taxonomy_ids / ~70 products of dog-food scope contamination under master_table='makanankucing_my', flagged again in every recent session. | Fixed 14 existing taxonomy entries in place and minted 2 new ones (claimed SKU block SKU-215654–SKU-215853, used 2 of 200 slots): SKU-145790 and SKU-146374 had their fabricated pack_count (3) corrected to 1 (no real multiplier in source text); SKU-146393 corrected from pack_count=3 to the real 15; SKU-146932 corrected from pack_count=3 to the real 12 (also stripped a stray 'G' artifact from product_line); SKU-146131's shared bucket (45pcs vs 90pcs, both mismapped as 'x3') was split — its 2 products rerouted to newly-minted SKU-215654 (70g x45) and SKU-215655 (70g x90), leaving SKU-146131 orphaned (0 products, harmless, matching prior sessions' established orphaning pattern); SKU-191181's duplicated 'Eco Choice Eco Choice' text was collapsed to one occurrence. Fixed the 6 duplicate-token/stray-artifact entries (SKU-146061, SKU-146068, SKU-146156, SKU-146109, SKU-146083, SKU-146041, SKU-146096) by stripping the repeated/stray text from product_line/variant/canonical_name. Converted SKU-146091 to an honest is_multi_variant=TRUE entry naming its actual constituent formulas ('Royal Canin (Indoor/Fit 32/Kitten/Hairball/Mother & Babycat) 2kg') instead of the incorrect single-formula 'Indoor' label. Every fixed/minted row had meta_agent='CLAUDE_CODE' and _meta reset, then was immediately Tier-1-rechecked in this same session (0/16 flags on recheck) before promotion — no waiting for a future pass. Promoted 96 taxonomy_ids to review_confidence='confident': 16 via Path 1(b) (fixed/minted this session, clean on immediate recheck) + 80 via Path 1(a) (never-reviewed, genuinely Tier-2-judged correct via the GMV-90 sample review, Tier-1-clean). SKU-146733 and SKU-146126 left un-promoted and un-excepted, same as prior sessions. Post-fix hard gates (full-table, not just worklist-scoped): 0 dual-mapped (LLM-scoped), 0 HUMAN+LLM coexistence, 0 placeholder-leak, 0 G5 provenance gaps, 0 duplicate product_id/platform, 0 duplicate product+taxon, 0 'all variant/size' name, 0 canonical_name-fields mismatch, 0 garbled-brand-text, 33% structured-fields NULL (unchanged, under the 50% threshold) — no regressions. Confidence distribution left behind: 1,076 confident, 8 unconfident, 1 fixed-pending-recheck (SKU-146733), 864 never-reviewed, 1,949 total taxonomy entries mapped to this category. Remaining incremental worklist: 873. |
---

## Targeted QA Fix Brief

> Scope for a future `targeted_qa_fix.sh` / NULL-coverage session — not executed this session.
> **Note (2026-07-28):** the figures below predate the Tiktok-scope top-up sessions and are stale —
> see "Map Row Counts" above for the current gap (396 products, all Tiktok, as of 2026-07-28) and this
> session's QA History rows for what's specifically unresolved. Kept here as historical record of the
> original Shopee-only gap.

**Verdict:** D6 in-scope NULL coverage gap + D1/D2 precision pass needed.

- **Coverage gap A** (458 products, ~3.54M MYR GMV): 334 brand-unidentifiable + 124 size-ambiguous
  products left `taxonomy_id IS NULL`. See "Known coverage gaps" above for the exact lists —
  candidates for direct image-based resolution (text signals were exhausted; image wasn't checked
  per-product given this session's bulk-first budget, only spot-checked on ~10 top-GMV examples).
- **Precision pass B**: this was a coverage-first bulk regex/text-matching pass (Full Rebuild
  scenario explicitly deprioritizes per-row precision — see `docs/headless-runbook.md`). Product-line
  text for long-tail brands is regex-stripped `sku_name` remainder and occasionally retains noise
  (e.g. `Aristo Cats G Plus Tuna Wet Chunk Meat Snack TPF` — stray `G`/`TPF` fragments). A pass
  re-reading product images for the long tail (~130 brands outside the top 15) would sharpen D2
  (product line accuracy) without needing full re-extraction.

---

## Scripts

No committed pipeline scripts for this category — extraction was done directly (multimodal reading +
ad hoc SQL/Python, not a `pipeline/05_product_taxonomy/llm_{table}/` script) per the session brief for
this custom, non-NIQ source table.

---

## Map Row Counts (as of the seventh top-up session, 2026-07-28, this session)

| Source | Count | Notes |
|--------|-------|-------|
| LLM | 3,276 | 1,832 Full Rebuild + 141 first top-up + 65 second top-up + 1,103 third top-up + 101 fourth top-up + 11 fifth top-up + 14 sixth top-up + 9 this session (all Tiktok — scope classification + bulk brand matching + image-led resolution, see QA History) |
| HUMAN | 0 | No prior keyword-seed pass for this category |
| NULL (unmapped, in-scope, live-worklist gap remaining) | 263 | See this session's QA History rows for the resolved/excluded breakdown — gap re-verified via product_id-grain join (not the raw STEP-0 count, per this category's own documented query-instability caution) confirms 263 (272 − 9) exactly |
| NULL (out-of-scope, correctly excluded, this session's live worklist) | 2 | 1 cat litter, 1 livestock/plant feed (molasses) — same two products this exact live-worklist boundary has surfaced since the third top-up session |

Prior sessions' snapshots (kept for history):

Snapshot before this session's top-up (after the sixth 2026-07-28 top-up):

| Source | Count | Notes |
|--------|-------|-------|
| LLM | 3,267 | 1,832 Full Rebuild + 141 first top-up + 65 second top-up + 1,103 third top-up + 101 fourth top-up + 11 fifth top-up + 14 sixth top-up (all Tiktok — scope classification + bulk brand matching + image-led resolution) |
| NULL (unmapped, in-scope, live-worklist gap remaining) | 272 | 284 in-scope minus 21 resolved across the sixth top-up (9 new-entry + 5 exact-reuse), consistent with the sixth top-up's own QA History breakdown |

Snapshot before this session's top-up (after the fifth 2026-07-28 top-up):

| Source | Count | Notes |
|--------|-------|-------|
| LLM | 3,253 | 1,832 Full Rebuild + 141 first top-up + 65 second top-up + 1,103 third top-up + 101 fourth top-up + 11 fifth top-up (all Tiktok — image-led resolution of the text-exhausted residue) |
| NULL (unmapped, in-scope, live-worklist gap remaining) | 286 | Unclassified mix of in-scope and possibly-OOS rows as of the fifth top-up session — see this session's scope-classification finding above (284 in-scope / 2 OOS) |

Snapshot before this session's top-up (after the fourth 2026-07-28 top-up):

| Source | Count | Notes |
|--------|-------|-------|
| LLM | 3,242 | 1,832 Full Rebuild + 141 first top-up + 65 second top-up + 1,103 third top-up + 101 fourth top-up (all Tiktok — bulk regex text-matching against sku_name, no image reads that session) |
| NULL (unmapped, in-scope, live-worklist gap remaining) | 297 | Roughly 126 brand-unidentifiable-from-text + 165 brand-matched-but-size-unresolved + a handful excluded on ambiguity grounds |
| NULL (out-of-scope, correctly excluded) | 2 | 1 livestock/plant feed (molasses), 1 cat litter |

Snapshot before this session's top-up (after the third 2026-07-28 top-up, Tiktok scope added):

| Source | Count | Notes |
|--------|-------|-------|
| LLM | 3,141 | 1,832 Full Rebuild + 141 first top-up + 65 second top-up + 1,103 third top-up (all Tiktok — bulk regex text-matching against sku_name, no image reads that session) |
| NULL (unmapped, in-scope, live-worklist gap remaining) | 396 | 266 brand-unidentifiable-from-text + 130 brand-matched-but-size-unresolved |
| NULL (out-of-scope, correctly excluded) | 2 | 1 cat litter, 1 grooming/supplement product mixed with shampoo mention |

Snapshot before the third 2026-07-28 top-up (after the second 2026-07-23 top-up):

| Source | Count | Notes |
|--------|-------|-------|
| LLM | 2,038 | 1,832 Full Rebuild + 141 first top-up + 65 second top-up |
| NULL (unmapped, in-scope, live-worklist gap remaining) | 489 (~3.35M MYR) | Mostly genuinely brand-unidentifiable or size-unresolved-even-from-image commodity treats |
| NULL (out-of-scope, correctly excluded) | 253 | 158 dog, 63 non-food/accessory, 24 cat litter, 6 flea/dewormer, 1 baby formula |

Snapshot before the second top-up (after the first 2026-07-23 top-up):

| Source | Count | Notes |
|--------|-------|-------|
| LLM | 1,973 | 1,832 from Full Rebuild + 141 from the first 2026-07-23 top-up |
| NULL (unmapped, in-scope, live-worklist gap remaining) | 554 (~5.62M MYR) | 187 brand-unidentifiable-from-text + 115 brand-matched-but-size-unresolved + the original 458/237 breakdown below |
| NULL (out-of-scope, correctly excluded) | 252 | 138 pure dog food, 59 non-pet merch/accessories, 24 cat litter, 17 dog-only brands, 6 flea/tick, 3 merch/GWP-only, 1 baby formula, 2 manually-overridden mislabeled dog-food listings |

Snapshot before the first 2026-07-23 top-up (Full Rebuild only):

| Source | Count | Notes |
|--------|-------|-------|
| LLM | 1,832 | Full Rebuild first run |
| NULL (unmapped, in-scope) | 458 | 334 brand-unresolved + 124 size-ambiguous — see Targeted QA Fix Brief |
| NULL (out-of-scope, correctly excluded) | 237 | Dog food / litter / flea treatment / non-pet items |
