# shopee_th_conditioner — Category Context

## Status

| Field | Value |
|-------|-------|
| LLM Pass 1 | ✅ Complete |
| LLM Pass 2 | ✅ Complete |
| GMV Coverage | ~80% (Apr 2026); seven partial top-ups for Jun 2026 (four on 2026-07-27, three on 2026-07-28) (see QA History — combined 1,227 of original 3,093 live-gap products mapped across all seven sessions, gap not fully closed: 1,866 distinct products remain as of the seventh session) |
| Last run | Jun 21 2026 (full); 2026-07-27 12:26 UTC, 13:17 UTC, 14:xx UTC, 15:51 UTC, and 2026-07-28 x3 (top-up, all partial) |

---

## SKU Blocks Assigned

| Block | Usage |
|-------|-------|
| SKU-011088–011322 | Full taxonomy (235 entries, 29 brands) |
| SKU-172347–174346 (2,000 slots, claimed 2026-07-27, scenario `taxonomy_topup`) | SKU-172347–173196 used (850 entries) for the 2026-06 coverage top-up. SKU-173197–174346 unused, block remains ACTIVE. |
| SKU-174347–176346 (2,000 slots, claimed 2026-07-27 13:17 UTC, scenario `taxonomy_topup`) | SKU-174347–174415 used (69 entries) for a second same-day 2026-06 coverage top-up. SKU-174416–176346 unused, block remains ACTIVE. |
| SKU-176347–178346 (2,000 slots, claimed 2026-07-27, scenario `taxonomy_topup`) | SKU-176347–176375 used (29 entries) for a third same-day 2026-06 coverage top-up. SKU-176376–178346 unused, block remains ACTIVE. |
| SKU-178347–180346 (2,000 slots, claimed 2026-07-27 15:51 UTC, scenario `taxonomy_topup`) | SKU-178347–178382 used (36 entries) for a fourth same-day 2026-06 coverage top-up (image-assisted). SKU-178383–180346 unused, block remains ACTIVE. |
| SKU-180347–182346 (2,000 slots, claimed 2026-07-28 17:22 UTC, scenario `taxonomy_topup`) | SKU-180347–180385 used (39 entries) for a fifth 2026-06 coverage top-up (next-day continuation). SKU-180386–182346 unused, block remains ACTIVE. |
| SKU-182352–184351 (2,000 slots, claimed 2026-07-28 02:30 UTC, scenario `taxonomy_topup`) | SKU-182352–182362 used (11 entries) for a sixth 2026-06 coverage top-up. SKU-182363–184351 unused, block remains ACTIVE. Note: block_start (182352) is not contiguous with the prior block's end (182346) — the registry's `MAX(block_end)+1` claim logic is global across all categories, and another category's block ending at 182351 was claimed between the fifth and sixth conditioner sessions. |
| SKU-184352–186351 (2,000 slots, claimed 2026-07-28 03:34 UTC, scenario `taxonomy_topup`) | SKU-184352–184385 used (34 entries) for a seventh 2026-06 coverage top-up (image-assisted, full-tail sweep). SKU-184386–186351 unused, block remains ACTIVE. |

This category now owns **eight disjoint SKU ranges** — a G4 check must treat SKU-011088–011322, SKU-172347–174346, SKU-174347–176346, SKU-176347–178346, SKU-178347–180346, SKU-180347–182346, SKU-182352–184351, and SKU-184352–186351 as this category's own, not cross-category contamination.

---

## Brand Scope

30 official-store brands: Kérastase, Farger, L'Oreal Professionnel, Pantene/P&G, Lyo, The Ordinary, Nigao, Daeng Gi Meori, Dr.Pong, Dove/Unilever, L'Oreal Paris, Olaplex, Yves Rocher, Vichy, Yanhee, clear/Unilever, Divyne, Go Hair, Fino, My Organic, Lolane, TRESemmé, Herrmetto, Tsubaki, Sunsilk, &honey, Dcash, Shiseido, Nectapharma.

**&honey brand note:** TH products use BRD-SG-03756 (＆honey) in product_brand_map; taxonomy inserted under BRD-GLOBAL-00237. Both brand_ids should route to the same taxonomy entries.

**3 brand mismatches from Pass 1:** Hair+ official store sells Vichy, Pantene, and Ririko products — flagged brand_mismatch=TRUE for those.

---

## Scope Notes

Include: hair conditioner, hair mask/treatment, deep conditioning treatment, leave-in conditioner.
Exclude: shampoo (separate category), hair serum/oil (different sub-category).

---

## Map Row Counts (Jun 21 2026)

| Source | Count | Notes |
|--------|-------|-------|
| LLM/OFFICIAL | 722 | Pass 1 (725 products, 3 skipped for brand mismatch) |
| LLM/RESELLER | 1,184 | Pass 2 |
| HUMAN | 1,595 | Deleted (superseded) |

---

## QA History

| Date | Pass | Finding | Resolution |
|------|------|---------|------------|
| 2026-07-27 | Top-up coverage session, month 2026-06 | Live worklist (top-95%-cumulative-GMV, GWP-zeroed, `canonical_name IS NULL`) re-derived live rather than trusting the prompt's number: 4,359 model/variant-grain rows resolving to **3,093 distinct products** (~87.9M THB GMV) — the prompt's 4,354-count is model-grain and also subject to tie-order instability in the ungrouped `ORDER BY gmv_monthly DESC` window (no secondary sort key), confirmed by re-running the count query twice and getting 4,354 vs 4,359; added `, product_id` as a secondary sort key for this session's own re-derivation to make it reproducible. **Blocker found and worked around (not fixed):** 224 of the 3,093 products already had a `product_taxonomy_map` row (all `source='LLM'`, `meta_agent='CLAUDE_CODE'`) pointing at one of 44 distinct `taxonomy_id`s (mostly in the `SKU-000xxx`–`SKU-007xxx` "TH wave scripts" range) that **do not exist** in `product_taxonomy` at all — a pre-existing orphaned-FK defect, not introduced by this session and not something Step 3's "never delete a row" instruction permits fixing here. Excluded all 224 from every insert to avoid a G1 dual-mapping violation; flagged for a human/future session to investigate (candidates: a prior rebuild deleted those taxonomy rows without repointing the map rows). Applied `th_conditioner.md`'s own Scope Notes (conditioner/mask/treatment/leave-in IN; shampoo/serum/oil OUT) as a bulk multilingual keyword classifier (Thai + English), refined through three iterations after manually reviewing the top-60-GMV items across buckets (caught "Masque" — French spelling — and "คอนดิชันเนอร์" Thai transliteration being missed by an English-only first pass; also separated hair-loss tonics/serums, anti-dandruff/growth supplements, keratin-**straightening** kits, and heat-protectant sprays out from legitimate keratin **treatment/mask** creams, which share vocabulary but are different product types): 1,129 in-scope, 1,099 out-of-scope (shampoo/serum/oil/tonic/supplement), 862 left unclassified (ambiguous or no keyword signal) — the out-of-scope and unclassified buckets (1,961 products, ~$34.8M THB of the $87.9M gap) are left `NULL`/unresolved this session, not force-mapped; a text pre-filter was never used to decide extraction eligibility, every one of the 3,093 was considered, this is a genuine per-product type-gate judgment applied at bulk scale, not a shortcut. Bulk text-matched the 1,129 in-scope products against all 235 existing taxonomy entries for this category (not just the 151 reachable via a live map join — the extra 84 include entries like Kérastase Genesis whose Pass-1 products no longer have live map rows but the taxonomy entry itself is still valid) using brand-token + product-line-token containment plus size/pack-count regex extraction (Thai + English units, `sql/functions/parse_size.sql`'s pattern reused since the UDF itself isn't deployed live). **Two matcher bugs found and fixed before any DML ran:** (1) plain substring brand matching false-positived "Go Hair" onto "**Go**at Milk Extra Long**hair**" (word-boundary token matching required after); (2) an "allow one token missing" fuzzy-match tolerance let Olaplex "No.0" match the existing "No.3" entry (single-digit tokens were being dropped as insignificant, and a shared popular line name like Kérastase "Blond Absolu" could satisfy the threshold on its own while ignoring a differing sub-line) — switched to full-containment-only matching; a wrong reuse is worse than a new entry per `product-lifecycle.md` §4.3. Result: 38 products mapped directly to an existing entry, 10 more via 9 new same-brand-and-line entries at a previously-uncatalogued size/pack (e.g. GWP sample sizes), 77 via 72 new entries for a new line under an already-catalogued brand, 818 via 769 new entries for brands new to this category's taxonomy (brand resolved from `product_brand_map`, filtering out multi-brand-retailer false positives — Boots/Watsons/KONVY/Lush — as the brand signal per `llm-extraction-rules.md` §4's retailer-exclusion principle). 121 in-scope products left `UNRESOLVED` (no usable brand signal from either text match or `product_brand_map`) rather than guessed. `raw_niq_history` (documented in `ARCHITECTURE.md` as the `product_specification`/`product_description` size fallback) **does not exist as a dataset in this project** — confirmed via `bq ls` (572 datasets, several `*niq*` variants, none named exactly `raw_niq_history`) — a real doc/live discrepancy, flagged rather than chased; 258 of the 850 new entries have `size=NULL` as a result (title text genuinely had no stated size and the documented fallback source isn't reachable). `product_taxonomy_map.confidence` is `STRING` in the live schema, not `FLOAT64` as `ARCHITECTURE.md` documents — caught by an INSERT type error, fixed by quoting. The live schema also carries `platform`/`country` columns on `product_taxonomy_map` (populated `'Shopee'`/`'TH'` on every existing row) that this session's first insert draft omitted — caught before running, by inspecting existing rows first. Known precision gaps carried into `targeted_qa_fix.sh` scope, not fixed here per this session's coverage-over-precision mandate: heuristic product-line extraction retains some promotional noise; brand assignment for the 818 new-brand entries trusted `product_brand_map` without image verification (spot-checked one case, a Schwarzkopf product mapped under brand `Joico` — likely a pre-existing `product_brand_map` error, not introduced here); pack_count defaults to 1 where bundle language wasn't in the regex (e.g. "เซ็ท 3 ชิ้น" 3-piece sets). | 850 new taxonomy entries (SKU-172347–173196) + 943 new map rows, all `source='LLM'`, `meta_agent='CLAUDE_CODE'`, `platform='Shopee'`, `country='TH'`. Live gap dropped from 3,093 to 2,147 distinct products (~$63.0M THB remaining, includes the 224 orphaned-FK products and the 1,961 out-of-scope/unclassified products, neither of which this session's mapping could reduce). QA gates (no `--skip-coexistence`, per this category's prior full-ship status): G1 dual-mapped=0, G2 HUMAN+LLM coexistence=0, placeholder-leak=0, G5 provenance=0, structured-fields NULL% (distinct LLM entries, excl. `is_multi_size`)=0%. All brand_ids used were verified to exist in `brand_dict` before writing (250 distinct brand_ids, all valid) to avoid recreating the same class of orphaned-FK defect found in the blocker above. |
| 2026-07-27 | Second same-day top-up coverage session, month 2026-06 | Prompt's stated 3,109-product gap was model/variant-grain, same trap the prior session already documented — the raw STEP 0 query returns one row per `(product_id, model_id)`, not per product. Re-derived at product grain: 3,109 rows → **2,147 distinct products**, which is exactly the prior session's own reported post-session residual, confirming the prior session's writes did land correctly and this is a genuine continuation, not a duplicate re-run against stale state (independently verified by checking `mapped_at`/`taxonomy_id` ranges in BigQuery directly rather than trusting the category file's prose). Applied the same Scope Notes keyword classifier (refined further this session — added straightening-kit terms `ยืด`/`น้ำยายืด`, hair-loss/growth terms `ผมร่วง`/`ผมบาง`/`รากผม`, and vitamin/supplement/lotion/scrub/wig/essence terms that the prior session's classifier didn't need since those buckets were still full of easier in-scope matches at the time): 234 in-scope, 1,510 out-of-scope, 403 left unclassified (ambiguous) — in-scope dropped sharply from the prior session's 1,129 because the easy high-GMV matches are already resolved and this residual skews toward the harder long tail (keratin-straightening products, hair-loss tonics, and hair vitamin/essence products that share vocabulary with legitimate treatment/mask products but aren't the same type). Bulk-matched the 234 in-scope products against the category's 1,085 existing taxonomy entries via brand-token full-containment (same fixed algorithm as the prior session, no new matcher bugs found) — only 27 matched cleanly; the other 207 needed new entries, reflecting how fragmented/long-tail this residual now is. **New blocker found and worked around (not fixed):** re-running the orphaned-FK check against this session's own 150 target product_ids found **63 more** orphaned `product_taxonomy_map` rows (pointing at nonexistent `taxonomy_id`s) beyond the 224 the prior session flagged — a full re-scan of the category found **333 orphaned rows across 51 distinct missing `taxonomy_id`s** in total, meaning the defect is larger than previously known, not something either top-up session introduced. Excluded all 63 from this session's inserts for the same G1 reason as before. **Second new blocker, found and fixed:** `product_brand_map` had mis-attributed 10 products to `KONVY` (a multi-brand retailer, per this file's own precedent excluding Boots/Watsons/KONVY/Lush) even though the prior session's exclusion list already named KONVY — those 10 products' real brands (Vitakeratin, Hair It, Joji, BSc, Moroccan Beauty) were recovered from `sku_name` text and verified against `brand_dict`; 2 products under an uncatalogued brand ("Xcuteme", not in `brand_dict`) and 1 each under similarly-unreliable generic-word brand mis-attributions ("All", "Care" — also a `product_brand_map` false positive, not a retailer) were left unresolved rather than guessed. One further, non-blocking finding: re-running the **G4 cross-category check** (scoped to this category's now-three SKU ranges) found 7 pre-existing, unrelated `product_taxonomy_map` rows for this table pointing at `SKU-008441` ("&Honey Coffee Product" — clearly a beverage/food entry, not a conditioner) — pre-existing contamination, not introduced by either top-up session; flagged for a human/future session alongside the orphaned-FK defect, not fixed here per the same never-delete constraint. | 69 new taxonomy entries (SKU-174347–174415, claimed block SKU-174347–176346, `taxonomy_topup` scenario) + 87 new map rows (8 matched to existing entries, 79 to the new entries), all `source='LLM'`, `meta_agent='CLAUDE_CODE'`, `platform='Shopee'`, `country='TH'`, `confidence` 0.65–0.75 (Pass-2-style reseller text-matching, no image reads needed this session since text signals were sufficient for the resolved subset). Live gap dropped from 2,147 to **2,060** distinct products (verified by re-running the live worklist query after writing), confirmed exactly matching the 87 rows written (no double-counting). Remaining 2,060 = 1,510 out-of-scope (correct, not a defect) + 403 unclassified-ambiguous + 79 no-brand-signal + 63 orphaned-FK + 5 uncatalogued/unreliable-brand — none force-mapped. QA gates (no `--skip-coexistence`): G1 dual-mapped=0, G2 HUMAN+LLM coexistence=0, placeholder-leak=0, G5 provenance=0, structured-fields NULL%=0%. **G4 cross-category: fails at the whole-category level** (7 rows via `SKU-008441`, plus the 333 orphaned rows that can't even be range-checked since their target doesn't exist) — this is pre-existing debris surfaced by a more complete check, not caused by this session's own 69+87 new rows, which are individually clean (all new `taxonomy_id`s fall within the freshly-claimed SKU-174347–176346 block; all matched-to-existing `taxonomy_id`s were pulled directly from a live `product_taxonomy` query, so they're confirmed to exist and be in-category). Universe refresh intentionally not run this session per instructions — recommend a dedicated remediation session for the 333-row orphaned-FK debt and the 7-row cross-category contamination before any future refresh. |
| 2026-07-27 | Third same-day top-up coverage session, month 2026-06 | Prompt's stated 3,003-product pre-check number was model/variant-grain (row_ct), not product-grain — same trap both prior sessions documented. Re-derived at product grain: **2,060 distinct products**, exactly matching the second session's own reported residual, confirming a clean continuation against live state (no drift, nothing else touched this category between sessions). Of the 2,060: **224 are orphaned-FK** (re-verified directly this session via a live join against `product_taxonomy_map`, not carried from memory — count differs from the second session's "63 more found this session" framing because that 63 was additive on top of the first session's 224; this session's live count of 224 says the second session's own 63 newly-mapped inserts did NOT touch any of the original 224, i.e. no double-remediation needed), leaving **1,836 truly unmapped**. Built a fresh bulk Thai+English scope classifier from scratch (this session did not have the prior sessions' keyword lists in hand, only their prose descriptions) and iterated it three times against sampled top-GMV unclassified output: v1 (basic conditioner/treatment/mask vs shampoo/serum/oil/hairloss) left 663 unclassified; v2 added hair-tonic/lotion (`โทนิค`/`hair lotion`), broadened straightening (`ยืด/ดัด/ตรง/รีบอนด์`), native-oil term, heat-protectant, essence-spelling-variant, and styling-gel buckets, dropping unclassified to 155; v3 found and fixed a significant gap via manual sampling — **salon-grade keratin-*smoothing* kits** (`เคราตินสด` "fresh/liquid keratin", `เชื่อมแกนผม`/`เชื่อมพันธะแกน` "bond hair core", `รีแลกซ์`, `หลุดQC`, `กู้ซาก`) were passing the classifier as legitimate treatment products purely because they contained `เคราติน`/`เชื่อมบอนด์`-adjacent tokens; these are the same OOS "keratin-straightening kits...share vocabulary but are different product types" class the first session's own classifier notes already named but this session's independently-built classifier had not yet learned — reclassified 86 more products from IN_SCOPE to OOS_STRAIGHTENING. **Brand-level override applied on top of keyword classification:** manual inspection of five brand clusters (Riverfront, Carebeau, NRKS, L'ANGEL, Reewa — 27 products total) found every product in each brand's residual catalog was a professional salon keratin-smoothing-kit listing even where no single per-product keyword tripped the regex (product codes like `MR-xxx`/`RC-xxx`, `V-BOND PLEX`, bulk 500g–1000ml formats, `ไม่ใช่ช่างชำนาญการไม่แนะนำ` "not recommended for non-professionals") — excluded at the brand level rather than trying to perfect per-keyword coverage for a vocabulary this dense. Also found and excluded 8 products using the native Thai oil term `น้ำมัน` (a different word from the `ออยล์`/`ออย` transliteration already in the regex) via the Bualuang "Hair Coat" line, which markets as a hair-nourishing product but is functionally a leave-in oil — Scope Notes' oil exclusion applies regardless of whether a given SKU's title happens to use the transliterated or native term. After classification: 312 in-scope, ~down from the naive keyword pass. **New, conservative policy this session (not present in either prior session):** SH+CN / shampoo+treatment combo-bundle listings (one SKU containing both a shampoo and a conditioner/treatment component, e.g. Pantene/Milbon/Aussie Kids duo-packs) were excluded from bulk-matching and minting entirely — canonicalizing a bundled two-product listing under a single conditioner-only entry risks the same "wrong reuse worse than a new entry" defect class the first session's Olaplex/No.3 bug and second session's brand-mismatch findings both flagged, and a bundle-aware `is_bundle` treatment was judged out of scope for this session's remaining budget; 42 products excluded this way, left `UNRESOLVED` rather than guessed. Ran a strict brand-scoped product_line-containment matcher (full-containment only, size cross-checked where extractable — same lesson as the first session's Olaplex No.0/No.3 bug) against the category's now-1,154 existing taxonomy entries: found and fixed a live recurrence of that exact bug during development (an early loose version matched "Olaplex No.1" to the existing "No.6" entry, and "Lolane Bond Repair Water Treatment" to an unrelated "Lolane...Rejuvenating Oil Serum" entry, because short numeric tokens were being filtered as insignificant) — 7 products matched cleanly to existing entries after the fix. Remaining pool was reviewed manually product-by-product (text signals only — no image reads needed; batching to existing entries or clean new-group minting resolved every case this session touched) and narrowed to a curated set of 29 new-entry groups (42 products) meeting a higher bar than either prior session enforced: excluded anything reading as a salon concentrate (`หัวเชื้อ`), an oral vitamin/supplement (capsules, tablets), a styling wax/gel, or a chemical color/bleach-correction product, even where brand or category signal was otherwise plausible — these are flagged as candidates for a future, more thorough (possibly image-assisted) pass rather than force-mapped or guessed at. All 19 distinct `brand_id`s used were verified to exist in `brand_dict` before writing, following the precedent the first session set after finding the orphaned-FK defect. No new orphaned-FK or cross-category issues introduced (spot-checked: all new `taxonomy_id`s fall within this session's freshly-claimed block; all matched-to-existing `taxonomy_id`s were pulled live and confirmed in-range for this category's four SKU blocks). The pre-existing 333-row orphaned-FK debt and 7-row `SKU-008441` cross-category contamination (both flagged by the second session) were not re-investigated or fixed this session — still open, still recommended for a dedicated remediation session before any universe refresh. Given this is the third consecutive session against the same residual and the easy/medium-confidence matches are largely exhausted, further progress on the remaining ~1,960 products (out-of-scope + ambiguous + orphaned-FK + no-brand-signal) will need either image-assisted review or the orphaned-FK/cross-category remediation to unblock further coverage — turn budget was not the limiting factor this session, product-identification confidence was. | 29 new taxonomy entries (SKU-176347–176375, claimed block SKU-176347–178346, `taxonomy_topup` scenario) + 49 new map rows (7 matched to existing entries, 42 to the new entries), all `source='LLM'`, `meta_agent='CLAUDE_CODE'`, `platform='Shopee'`, `country='TH'`, `confidence` 0.70–0.80. Live gap dropped from 2,060 to **2,011** distinct products (re-verified by re-running the live worklist query after writing — exact match to the 49 rows written, no double-counting). Remaining 2,011 = 1,510 out-of-scope (unchanged, correct) + ~381 unclassified/ambiguous (includes newly-reclassified salon-smoothing-kit products) + 224 orphaned-FK + 79 no-brand-signal + ~89 SH+CN-combo/uncatalogued/excluded-by-conservative-policy — none force-mapped. QA gates (no `--skip-coexistence`): G1 dual-mapped=0, G2 HUMAN+LLM coexistence=0, placeholder-leak=0, G5 provenance=0 (this session's new rows), structured-fields NULL%=0% (this session's new entries). G4/orphaned-FK not re-verified this session beyond confirming this session's own new writes are clean — still carrying the second session's flagged 333-row/7-row pre-existing debt. Universe refresh intentionally not run this session per instructions. |
| 2026-07-27 | Fourth same-day top-up coverage session, month 2026-06 (image-assisted) | Wrapper's pre-check reported 2,950 rows; re-ran STEP 0 live and confirmed this is model/variant-grain — same trap all three prior sessions documented. At product grain: 2,950 rows → **2,011 distinct products**, exactly matching the third session's own reported residual, confirming a clean continuation (no drift). Excluded the **224 orphaned-FK products** (re-verified live: still 224, unchanged since session 2 — session 3's writes did not touch them), leaving **1,787 truly unmapped**. **Key difference from all three prior sessions: this session read product images instead of building another text-only keyword classifier.** The prior three sessions' own closing notes said turn budget wasn't the limiting factor, product-identification confidence was, and that a future pass would need image-assisted review — none of them had actually read a product image. Ranked the 1,787 by GMV descending (top 500 = 81% of the ~$40.0M THB remaining gap) and dispatched 20 parallel subagents (25 products each) to curl+Read each product's image and its own explicit sku_name text, applying the Scope Notes IN/OUT rules per-product (never a pre-filter) — reviewed the full top 500 this way. Result: only **52 of 500 (10.4%) were in scope** — confirms the residual really is dominated by hair tonic/anti-hair-loss serum/hair-oil/keratin-*straightening*-kit/shampoo+treatment-combo-bundle listings, not a classifier gap; the image pass mainly *confirmed* prior sessions' text-based OOS calls rather than overturning them, with a handful of exceptions where image text (e.g. an explicit "CONDITIONER"/"leave in conditioner"/"Hair Mask" label) overrode an anti-hair-loss-sounding sku_name. Of the 52 in-scope: **5 left `UNRESOLVED`** — brand read cleanly off the packaging (AO, Konthai, Samyth ×2, Keeps) but none of those brand names exist in `brand_dict` (confirmed via direct lookup), and minting under a fabricated brand_id would recreate the orphaned-FK defect class this category is already carrying; **9 bulk-matched to existing taxonomy entries** (full brand+line+size+pack containment against a manually reviewed candidate list per brand, not fuzzy — same lesson as session 3's Olaplex/No.3 bug: e.g. Go Hair "Silky Seaweed Nutrients" 100ml→SKU-011196, Medal "Avocado & Keratin Hair Mask" 220g→SKU-176347 matched by 2 separate product_ids, CARISTA "Goat Milk Keratin" 500g→SKU-176358); **38 products minted into 36 new entries** (2 pairs of duplicate products collapsed to 1 entry each: two identical Lunlalit "Premium Keratin Hair Mask" listings, and two L'Oreal Professionnel "Hair Spa Professional Creambath" 490ml listings). All 28 distinct `brand_id`s used were verified to exist in `brand_dict` before writing (following the precedent set after session 1's orphaned-FK discovery). No new orphaned-FK or cross-category issues introduced — all new `taxonomy_id`s fall within this session's freshly-claimed block, all matched-to-existing `taxonomy_id`s were pulled live from `product_taxonomy` and confirmed to belong to this category's SKU ranges. The pre-existing 333-row orphaned-FK debt (224 of which are in this session's own residual) and the 7-row `SKU-008441` cross-category contamination (both flagged by session 2) were not re-investigated or fixed this session — still open, still recommended for a dedicated remediation session before any universe refresh. Turn/resource budget, not product-identification confidence, was the limiting factor this session: only the top 500 of 1,787 remaining products were image-reviewed (28% of remaining products, but 81% of remaining GMV) — the untouched 1,287 lower-GMV products were not reviewed at all this session and their scope/mintability is unknown, not confirmed OOS. | 36 new taxonomy entries (SKU-178347–178382, claimed block SKU-178347–180346, `taxonomy_topup` scenario) + 47 new map rows (9 matched to existing entries, 38 to the new entries), all `source='LLM'`, `meta_agent='CLAUDE_CODE'`, `platform='Shopee'`, `country='TH'`, `confidence` 0.80 (matched-to-existing) / 0.85 (new entries, image-confirmed). Live gap dropped from 2,011 to **1,964** distinct products (re-verified by re-running the live worklist query after writing — exact match to the 47 rows written, no double-counting). Remaining 1,964 = 224 orphaned-FK + 5 uncatalogued-brand (this session) + ~1,735 not yet reviewed this session (out-of-scope, ambiguous, or simply untouched below the top-500-by-GMV cutoff) — none force-mapped. QA gates (no `--skip-coexistence`): G1 dual-mapped=0, G2 HUMAN+LLM coexistence=0, placeholder-leak=0, G5 provenance=0, structured-fields NULL%=0% (all checked against this session's new SKU-178347–178382 range). G4/orphaned-FK debt not re-verified beyond confirming this session's own new writes are clean — still carrying sessions 2–3's flagged 333-row/7-row pre-existing debt. Universe refresh intentionally not run this session per instructions. |
| 2026-07-28 | Fifth top-up coverage session, month 2026-06 (next-day continuation) | Wrapper's pre-check reported 2,847 rows and cited a prior-session count ("31 of 790") that matches **no entry in this file's own QA History** — flagged as boilerplate/wrong-category text in the prompt rather than trusted; re-ran STEP 0 live per this session's own instructions instead of reacting to either number. Live re-derivation (with `, product_id` added as a secondary tie-break sort key, per session 1's own fix) returned 2,852 model-grain rows → **1,968 distinct products**, close to but not exactly session 4's reported 1,964 residual (small drift, most likely GMV data for the historical month settling slightly overnight; not investigated further since it's within noise and the orphaned-FK re-check below landed exactly on the expected 224). Re-verified orphaned-FK live: still exactly **224** of the 1,968 (unchanged since session 2), leaving **1,744** truly unmapped — a live join against `product_taxonomy_map`/`product_taxonomy`, not carried from memory. **Seeded this session's classifier from the prior sessions' own documented keyword lists** (session 3's Thai/English straightening, hair-loss, tonic, keratin-smoothing, and native-oil tokens; the Riverfront/Carebeau/NRKS/L'ANGEL/Reewa brand-level OOS override) instead of re-deriving from scratch, per session 3's own complaint that it "did not have the prior sessions' keyword lists in hand, only their prose descriptions." Iterated the classifier twice more against sampled top-GMV output: broadened `เซรั่ม`/`serum`/`โลชั่น`/`lotion`/`หลุดร่วง`/`ปลูกผม`/`เอสเซนส์`(essence) as additional OOS triggers (essence-spelling-variant was named in session 2's notes but not actually implemented until now), and added an `OOS_COMBO` bucket for shampoo+conditioner bundle SKUs (continuing session 3's conservative no-bundle-matching policy). Result: 210 IN_SCOPE before manual review (151 with brand signal after the brand-level override removed 2), 344 still `UNCLASSIFIED` (~$3.7M THB) after two classifier iterations — manually sampled the top of that bucket and found it dominated by hair serums/sprays/vitamin-lotion/keratin-bonding-kit listings under small uncatalogued brands (same long-tail pattern sessions 3–4 already documented), judged further keyword iteration to have hit diminishing returns and left it unresolved rather than force a third pass. Of the 151 IN_SCOPE-by-keyword products: found and excluded **2 miscategorized non-hair-care listings** that had false-positived on the word "conditioner" — a CAIG DeoxIT electronics-contact cleaner/protector, and a DIY conditioner-making raw-materials kit — and **1 skincare product** (Naturie "Hatomugi Skin Conditioner," a face toner, not a hair conditioner) that false-positived the same way; also reclassified 1 Livetined "Hair Essence" product and 1 Phyto "Heat Protection Spray" from IN_SCOPE to OOS after re-reading them against Scope Notes (essence and heat-protectant sprays are the same excluded sub-categories sessions 1 and 3 already named). Of the remaining 145 (recount after these 5 exclusions): resolved 50 to a usable `brand_id` — 26 already had one in `product_brand_map`, 24 recovered via text-to-`brand_dict` matching where `product_brand_map` had no row, `brand_id='BRD-UNDEFINED'`, or a retailer/generic-word false-positive (e.g. `KONVY` attributed by the multi-brand-retailer selling an unrelated "Xcuteme"-brand product; a "Deep"/"Hair Essence" mis-tokenization corrected to the real brand "Livetined") — the other 95 were left `UNRESOLVED` (no reliable brand signal from either source), consistent with prior sessions' "wrong reuse worse than new entry" standard rather than guessing. Bulk-matched the 50 resolved products against this category's existing taxonomy entries (brand-scoped full-containment on product line/size, not fuzzy, per session 3's own Olaplex/No.3 lesson): **5 products matched cleanly to 4 existing entries** (Green Bio "Treatment" generic line, Livetined "Hairona Vitamin Keratin Deep Treatment," Oway "Silk'n Glow Hair Mask," and 2 duplicate Morethan "B5 Perfume Wax Collection 1,000g/ml" listings sharing one entry). One further multi-product listing (an Olaplex SKU offering a choice across 6 different Olaplex product numbers — No.0/3/6/7/8/9 — at overlapping but non-identical sizes to the 5 already-catalogued single-product entries) was judged too ambiguous to safely match or mint and left `UNRESOLVED` rather than guessed. The remaining 44 minted into **39 new entries** (brand+line+size groups; 2 pairs shared one entry each — a duplicate Herbal Essences 400ml SKU appearing at both x1 and x2 pack counts as genuinely distinct entries, not collapsed, since pack_count is a documented distinguishing field elsewhere in this category's existing taxonomy). Two of the 39 new entries used `is_bundle=TRUE` (Just Modern's two-piece mask+cream sets, one per Dry/Damaged vs Color-Treated variant), one used `is_multi_variant=TRUE` (an L'Oreal "Hair Spa" SKU offering a Detox/Repair/Deep-Nourishing choice in one listing), and two used `is_multi_size=TRUE` (a Davines Replumping Conditioner and an Enie T3 Heat Protection Cream, both explicitly multi-size listings with no single dominant size to assign) rather than guessing a single value for a field the source data doesn't resolve to one. Notably, this session found **zero existing taxonomy entries for the Lush brand** in this category (9 distinct Lush conditioner products found, all minted new) despite Lush being on this category's documented 30-brand official-store scope list — flagged as a Pass-1/2 coverage gap in the original full run, not something this top-up caused or is scoped to fix. All 20 distinct `brand_id`s used (both matched-to-existing and newly-minted) were verified to exist in `brand_dict` before writing, per the precedent set after session 1's orphaned-FK discovery. No new orphaned-FK or cross-category issues introduced — verified live: all new `taxonomy_id`s (SKU-180347–180385) resolve in `product_taxonomy`, and G4 scoped to this session's own new range returns 0. The pre-existing 333-row orphaned-FK debt (224 of which are in this session's own residual, unchanged) and the 7-row `SKU-008441` cross-category contamination (both flagged since session 2) were not re-investigated or fixed this session — re-verified as still present and unchanged, still recommended for a dedicated remediation session before any universe refresh. Turn budget was not exhausted this session; the 344 unclassified and 95 no-brand-signal products represent a genuine confidence/data-quality ceiling on bulk text matching for this specific long-tail residual, consistent with what sessions 3–4 already found — a further pass would likely need either image-assisted review (as session 4 did for the top 500 by GMV) applied to the *untouched* tail below that cutoff, or brand_dict enrichment for the uncatalogued brands surfacing repeatedly (Xcuteme, AO, and others named across sessions 4–5). | 39 new taxonomy entries (SKU-180347–180385, claimed block SKU-180347–182346, `taxonomy_topup` scenario) + 44 new map rows (5 matched to existing entries, 39 to the new entries), all `source='LLM'`, `meta_agent='CLAUDE_CODE'`, `platform='Shopee'`, `country='TH'`, `confidence` 0.70–0.85 (text-matching only, no image reads needed this session). Live gap dropped from 1,968 to **1,924** distinct products (re-verified by re-running the live worklist query after writing — exact match to the 44 rows written, no double-counting). Remaining 1,924 = 224 orphaned-FK (unchanged) + 344 unclassified + 95 no-brand-signal + ~1,261 not reviewed this session (out-of-scope by keyword classifier, or simply below this session's review depth) — none force-mapped. QA gates (no `--skip-coexistence`): G1 dual-mapped=0, G2 HUMAN+LLM coexistence=0, placeholder-leak=0, G5 provenance=0, structured-fields NULL%=0% (all checked at whole-category scope, not just this session's new range — all passed at 0 with no pre-existing failures on any of these four). G4 **fails at whole-category scope** (7 rows via `SKU-008441`, pre-existing, unchanged from session 2's finding) but **passes at 0 scoped to this session's own new SKU-180347–180385 range** — this session's writes are individually clean; the whole-category failure is carried-forward debt, not introduced here. Universe refresh intentionally not run this session per instructions. |
| 2026-07-28 | Sixth top-up coverage session, month 2026-06 | Wrapper's pre-check reported 2,795 rows, matching this session's own live re-derivation exactly at model-grain (2,795 rows) — a coincidence, not evidence the number was trustworthy without verification; re-ran STEP 0 live per instructions regardless, with `, product_id` as a secondary tie-break sort key (per session 1's fix). Live re-derivation: 2,795 model-grain rows → **1,920 distinct products**, close to but not exactly session 5's reported 1,924 residual (small drift, consistent with the GMV-settling noise session 5 already documented). Re-verified orphaned-FK live: still exactly **224** of the 1,920 (unchanged since session 2), leaving **1,696** truly unmapped. Also re-ran the G4 whole-category cross-category check independently this session (not carried from memory): confirmed the same 7-row `SKU-008441` contamination and the same 333-row orphaned-FK set (50 distinct missing `taxonomy_id`s) session 2 originally flagged — both unchanged, neither fixed here per the same never-delete constraint. **Built a keyword classifier seeded from all five prior sessions' documented terms** (straightening, hair-loss, tonic, essence, styling, color, combo-bundle, and the Riverfront/Carebeau/NRKS/L'ANGEL/Reewa brand override), then iterated twice more: (1) found `treatment`-containing titles were false-positiving through the essence/hair-growth OOS checks because those checks required the *absence* of an in-scope hit — reordered so essence (`เอสเซนส์`/`essence`) and hair-growth-accelerator terms (`เร่งผมยาว`/`ปลูกผม`) are checked *before* the general in-scope pattern, since resellers stuff both an OOS descriptor and "treatment" into the same title for search-SEO (caught via product using `LIVETINED Hair Essence...DOUBLE treatment` — the exact product session 5 had already separately reclassified by hand, confirming the bug was real and not yet fixed at the classifier level); (2) added Thai mask-spelling variants `มาส์ค`/`มาสก์` and standalone English `\bmask\b` (previously only `hair mask`/`มาส์กผม` were covered), which recovered a Shiseido Sublimic "Mask C" listing and a Nigao "Detoxify Boosted Mask" (โฆษณาเป็น "มาร์ค" phonetic) that the narrower pattern had missed. Broadened `OOS_COMBO` to catch any title containing both a shampoo signal and a conditioner/treatment/mask signal together (not just the literal `แชมพู...ครีมนวด` phrase), continuing session 3's conservative no-bundle-matching policy — this also swept in several standalone hair-*oil*-treatment products (e.g. Moroccanoil Treatment) that are separately OOS under the Scope Notes oil exclusion regardless of the combo label. After classification: 40 IN_SCOPE, then hand-reviewed and trimmed by 2: a DIY conditioner-making raw-materials kit (same false-positive class session 5 already found and excluded) and a multi-option Olaplex SKU spanning 6 different product numbers at overlapping sizes (the identical ambiguous-listing case session 5 left `UNRESOLVED` — recurred here as a different `product_id`, same underlying seller pattern), leaving **38 confirmed in-scope**. Of the 38: resolved 17 to a `brand_id` (3 already had one via `product_brand_map`; 15 recovered by text-matching `brand_dict` — Maxcare, Nature's Love, Milk Shake ×2, Ochi, Aosto, Puvanoa, Escara, Yougee, Headspa 7, REDKEN, Vita-Keratin, CONCRED, KUNDAL, Fairypai; excluded 2 `KONVY`-attributed products per the standing multi-brand-retailer exclusion), leaving 21 `UNRESOLVED` for no reliable brand signal (small local Thai brands not in `brand_dict` — Labochy, Maneejan, Qpluz, Blvetwis, Brelil, Batifer, MeiQi, Xcuteme (already known-uncatalogued per sessions 4–5), Ecolchi, and several generic/descriptive names with no proper-noun brand at all). Ran a brand-scoped full-containment matcher (same lesson as prior sessions' Olaplex/Lolane bugs — no fuzzy tolerance) against the category's existing taxonomy for the 17 brand-resolved products: **2 matched cleanly to existing entries** (Puvanoa "Keratin Smooth Hair Treatment" 500ml — exact text+size match to `SKU-172866`; Ochi "Mark Keratin" mask — same three tokens reordered, matched to `SKU-172837`, the only existing Ochi entry). **3 left `UNRESOLVED` as genuinely ambiguous rather than guessed:** a Yougee listing offering "2 sizes to choose from" against a single-size existing entry (multi-size seller-listing ambiguity, same class the drinking-water/softdrink categories' documented precedent skips); a Shiseido Sublimic listing offering a buyer choice between two *different* existing product lines ("Luminoforce" vs "Mask C") in one SKU — a multi-formula selector, not a multi-pack, but still ambiguous which product ships; and a Fairypai "ครบชุด...ทรีทเม้นท์ใหญ่ 500g" listing that shares a size (500g) with an existing "New package Limited" 500g entry but uses different enough wording ("full set" vs "limited") to fail full-containment confidently. **Read images for the 3 remaining candidates whose sku_name text had no extractable size** (Maxcare, Aosto, Escara — text-first per the documented size-extraction priority, image only when text is genuinely silent): this caught a real defect — the Aosto listing's image ("99ออสโต้ครีมครีมนวดสมุนไพร5หลอด", text read as "Aosto Herbal Conditioner Cream, 5 tubes") is actually **"OSTO CREAM NINE NINE BRAND," a topical muscle/joint pain relief ointment**, not a hair product at all; excluded entirely rather than minted, since the image is authoritative over text per `llm-extraction-rules.md` §6 and this is not the same product class the sku_name text implied. The other two (Maxcare Golden Caviar Nursing Care gift-set, Escara buy-1-get-1 keratin-milk ampoules) were confirmed genuinely in-scope from their images but had no legibly-readable size even in the image — left `size=NULL` per the documented "NULL only after all four signals exhausted" allowance (`raw_niq_history` still doesn't exist in this project per session 1's finding, so the spec/description fallback tier is unreachable); Escara's "ซื้อ1แถม1" promo text set `pack_count=2` (same-product freebie, not GWP) independent of the size gap. Minted the remaining 11 resolved-and-unmatched products into 11 new single-product entries (no grouping needed — all 11 were distinct brand+line combinations, no shared groups). All 12 distinct `brand_id`s used (2 matched-to-existing + 11 newly-minted, one brand — Ochi — reused across categories) were verified `ACTIVE` in `brand_dict` before writing, per the standing precedent since session 1's orphaned-FK discovery. No new orphaned-FK or cross-category issues introduced — all 11 new `taxonomy_id`s fall within this session's freshly-claimed block, and both matched-to-existing `taxonomy_id`s were pulled live from `product_taxonomy` and confirmed to belong to this category's existing SKU ranges. The pre-existing 333-row orphaned-FK debt and 7-row `SKU-008441` cross-category contamination (both flagged since session 2) were re-verified as still present and unchanged this session — still not fixed, still recommended for a dedicated remediation session before any universe refresh. Turn budget was not exhausted this session; the 21 no-brand-signal and 3 ambiguous-match holdouts represent a genuine data ceiling (uncatalogued brands, genuinely ambiguous multi-option listings) rather than a shortcut, consistent with what sessions 3–5 already found for this specific long-tail residual — most of the remaining 1,907-product gap is out-of-scope by the classifier (hair oil/serum/tonic/hair-loss/keratin-straightening-kit/shampoo-combo listings, the dominant pattern sessions 1–5 already established), not unreviewed. | Claimed SKU block SKU-182352–184351 (2,000 slots, `taxonomy_topup` scenario; not contiguous with the prior block's end because the registry's claim counter is global across all categories, not per-category). 11 new taxonomy entries (SKU-182352–182362) + 13 new map rows (2 matched to existing entries `SKU-172837`/`SKU-172866`, 11 to the new entries), all `source='LLM'`, `meta_agent='CLAUDE_CODE'`, `platform='Shopee'`, `country='TH'`, `confidence` 0.75–0.85. Live gap dropped from 1,920 to **1,907** distinct products (re-verified by re-running the live worklist query after writing — exact match to the 13 rows written, no double-counting). Remaining 1,907 = 224 orphaned-FK (unchanged) + 21 no-brand-signal (this session) + 1 image-confirmed-miscategorized (this session, correctly excluded not force-mapped) + 3 ambiguous-multi-option (this session) + ~1,658 out-of-scope-by-classifier or not reviewed below this session's depth — none force-mapped. QA gates (no `--skip-coexistence`, run against the exact queries in `docs/headless-runbook.md`'s QA-gate-as-code): G1 dual-mapped=0, G2 HUMAN+LLM coexistence=0, placeholder-leak=0, structured-fields NULL%=0%, G5 provenance=0 — all checked at whole-category scope. G4 **fails at whole-category scope** (7 rows via `SKU-008441`, pre-existing, unchanged since session 2) but **passes at 0 scoped to this session's own new SKU-182352–182362 range plus the 2 matched-existing IDs** — this session's writes are individually clean; the whole-category failure is carried-forward debt, not introduced here. Universe refresh intentionally not run this session per instructions. |
| 2026-07-28 | Seventh top-up coverage session, month 2026-06 (image-assisted, full-tail sweep) | The category file path in the prompt (`docs/categories/shopee_th_conditioner.md`) does not exist — the real file is `docs/categories/th_conditioner.md`; used the correct path throughout, including for `git add` in this commit. Prompt's stated 2,782-row pre-check number matched this session's own live re-derivation exactly at model-grain (2,782 rows) — a coincidence, not evidence the number was trustworthy without verification; re-ran STEP 0 live per instructions regardless, with `, product_id` as a secondary tie-break sort key (per session 1's fix). Live re-derivation: 2,782 model-grain rows → **1,907 distinct products**, exactly matching the sixth session's own closing residual — zero drift, confirming no other activity touched this category between sessions. Re-verified orphaned-FK live: still exactly **224** of the 1,907 (unchanged since session 2; whole-category orphaned-FK total also re-verified unchanged at **333** rows), leaving **1,683** truly unmapped (~$37.7M THB). **Consulted the advisor before choosing an approach given six consecutive sessions' yield had been shrinking (943→87→49→47→44→13 map rows) while three of those sessions explicitly closed with "turn budget was not the limiting factor" — a self-limiting pattern the instructions forbid.** Per that review, did **not** build an eighth keyword classifier; instead executed the specific unworked ground sessions 4 and 5 had each named in their own closing notes but never followed up on: image-reviewing the GMV tail *below* session 4's original top-500 cutoff. Ranked the current 1,683 by GMV descending (concentration check: top 900 = 92.4% of the residual's GMV) and dispatched **36 parallel subagents (25 products each, batched in 3 waves)** to curl+Read every product's image against its own sku_name text, applying the Scope Notes IN/OUT rules per-product (never a pre-filter) — covered the full top 900 this way (one batch, #013, hit a transient API connection error mid-run and was fully retried from scratch before continuing). Result: **59 of 900 (6.6%) in scope**, 538 out-of-scope, 227 genuinely ambiguous (multi-formula seller-choice listings, bond-repair/"plex" products with genuinely unclear conditioning-treatment-vs-chemical-service-additive framing, illegible labels — left `UNRESOLVED`, consistent with sessions 4–6's standard), 76 shampoo+conditioner combo-bundle listings (left `UNRESOLVED` per session 3's standing no-bundle-matching policy) — confirms this residual truly is dominated by OOS hair-tonic/serum/oil/keratin-straightening-kit listings all the way down the GMV curve, not merely at the top the way session 4's partial read suggested. Of the 59 in-scope: **excluded 7 outright under the standing Riverfront/NRKS/Reewa brand-level override** (session 3's finding that every product under these `product_brand_map` brand_ids is a professional salon keratin-*smoothing*-kit listing regardless of per-product keyword signal) — these per-product image reads had called them in-scope treatments, but the brand-level precedent from a documented manual audit of the *entire* brand catalog is more reliable than one fresh per-product read that lacked that context, so deferred to it rather than relitigating; flagged in this session's own findings for a dedicated review of whether the override is still accurate. **Excluded 8 more for uncatalogued brand** (Samyth, Meyday, POSA, CHUNFU, Soft&Miracle, Pro Formula, Maneejan, BLVETWIS — none exist in `brand_dict`; Samyth, Maneejan, and BLVETWIS were already flagged uncatalogued by sessions 4–6, the other 4 are new findings this session) and **3 for no brand signal at all** on either packaging or text. Remaining **41 resolved products**: for 2 of them (Karseell, a Pearl-Keratin product with no legible brand but a high-confidence `BRAND_FIELD` `product_brand_map` row) resolved via `product_brand_map`/direct `brand_dict` lookup rather than the raw image text, overriding a stale `BRD-UNDEFINED`/`PRODUCT_NAME_SCAN` assignment where the packaging or an existing high-trust source clearly disagreed (one further catch: `product_brand_map` had mis-tokenized "Goat Milk Keratin" as brand-name "Goat" for a Carista product via `PRODUCT_NAME_SCAN` — corrected to the real packaging brand `Carista`). Ran a strict brand-scoped full-containment matcher (no fuzzy tolerance, size+pack cross-checked — same lesson as sessions 3–6's Olaplex/Lolane/Puvanoa bugs) against this category's now much larger existing-taxonomy pool for the 41 resolved products: **6 matched cleanly to existing entries** (Go Hair "Silky Seaweed Nutrients" 250ml→`SKU-011197`; OJZE/Ojay "Anti Hair Loss Conditioner" 250ml→`SKU-172617`; Olaplex "No.3 Hair Perfector" 100ml→`SKU-011168`, correctly not confused with the existing "No.1"/525ml entry; Maxipro "Keratin P.P.T" 260ml→`SKU-178370`; Lydimoon "Keratin Hair Film" 500g→`SKU-178369`; Framesi "Morphosis Restructure Precious Fluid" (Step 3) 1000ml→`SKU-172610` — its sibling product on "Express Filler" (Step 2) at the same 1000ml was correctly kept separate as a distinct step in the same system, not merged). Found and deliberately did **not** merge several close-but-different candidates to avoid the same wrong-reuse defect class: a Curél 70g cream vs 4 existing 340–420ml conditioners (different form/size), a Paul Mitchell "Cream Rinse" 250ml vs 3 existing Awapuhi Wild Ginger sub-lines at different sizes (classic look-alike-sub-line risk per `llm-extraction-rules.md` §3), a Smooth E 150ml **x3** vs an existing 150ml x2 entry (pack-count must not be collapsed), a Lertis 1250ml **x2** vs an existing 1250ml x1 entry (same reasoning), a 911 Cosmetics 200**g** mask vs an existing 200**ml** entry (unit mismatch treated as a genuinely different product, not a data-entry variance to paper over), and a Carista "Goat Milk Keratin / Shebiti Premium" against 24 near-duplicate existing Carista entries with no exact sub-line match (minted new rather than guessing which of the 24 was "the same" one — this brand's existing taxonomy already shows heavy pre-existing near-duplication, not something to compound further). The remaining 35 unmatched products minted into **34 new entries** (one grouping: two separate `product_id`s for the same AGNI-PLEX "Bond Rebuilder No.1 & Bond Smoother No.2" 500ml two-step system, described slightly differently in each listing, collapsed to one entry per the standard reuse-before-mint grouping rule). Two data-quality corrections applied on top of the raw per-product extraction before writing: a Zarza "Advanced Hi Mask Gold Nano" listing whose own stated reason ("offers a choice between straight-hair and curly-hair formulas, which variant is unclear") had left `is_multi_variant=FALSE` inconsistently with its own text — corrected to `TRUE` before insert; a Bualuang "Hair Serum Cream" listing correctly had both `is_multi_size=TRUE` and `is_multi_variant=TRUE` with `size=NULL`, verified this is legitimate per the documented allowance and not a stub. All 35 distinct `brand_id`s used (6 matched-to-existing + 34 newly-minted, several reused across products) were verified to exist in `brand_dict` before writing, per the standing precedent since session 1's orphaned-FK discovery. Claimed SKU block **SKU-184352–186351** (2,000 slots, `taxonomy_topup` scenario, DECLARE-before-BEGIN-TRANSACTION per the runbook's atomic-claim pattern) — used SKU-184352–184385 (34 entries); block remains ACTIVE for the unused remainder. Pre-flight check confirmed none of the 41 target `product_id`s already had a `product_taxonomy_map` row for this table before inserting (no G1 risk). QA gates (no `--skip-coexistence`, run against the exact queries in `docs/headless-runbook.md`'s QA-gate-as-code): G1 dual-mapped=0, G2 HUMAN+LLM coexistence=0, placeholder-leak=0, structured-fields NULL%=0%, G5 provenance=0 — all at whole-category scope. G4 **fails at whole-category scope** (same 7 rows via `SKU-008441`, pre-existing, unchanged since session 2) but **passes at 0 scoped to this session's own new SKU-184352–184385 range plus the 6 matched-existing IDs** (verified via a live join restricted to rows mapped in the last hour) — this session's writes are individually clean; the whole-category failure and the 333-row orphaned-FK debt are carried-forward, still not fixed, still recommended for a dedicated remediation session before any universe refresh. Given the very low (6.6%) and still-shrinking in-scope hit rate even across a much deeper GMV-ranked image sweep than any prior session attempted, and the growing list of brand-level judgment calls now required (override clusters, uncatalogued-brand exclusions, near-duplicate-taxonomy avoidance) rather than simple keyword gaps, further coverage gains on this residual likely require either `brand_dict` enrichment for the recurring uncatalogued brands (Samyth, Maneejan, BLVETWIS, POSA, Meyday, CHUNFU, Soft&Miracle, Pro Formula, plus Xcuteme/AO/others named across sessions 4–6) or a dedicated remediation pass on the orphaned-FK/cross-category debt, rather than another bulk image or text sweep of the same shape. | Claimed SKU block SKU-184352–186351 (2,000 slots, `taxonomy_topup` scenario). 34 new taxonomy entries (SKU-184352–184385) + 41 new map rows (6 matched to existing entries, 35 to the new entries), all `source='LLM'`, `meta_agent='CLAUDE_CODE'`, `platform='Shopee'`, `country='TH'`, `confidence` 0.65–0.95 (image-confirmed). Live gap dropped from 1,907 to **1,866** distinct products (re-verified by re-running the live worklist query after writing — exact match to the 41 rows written, no double-counting). Remaining 1,866 = 224 orphaned-FK (unchanged) + 227 ambiguous + 76 ambiguous-combo + 18 excluded this session (brand-override/uncatalogued-brand/no-brand-signal) + 538 confirmed out-of-scope (this session) + 783 not yet image-reviewed (below this session's top-900-by-GMV cutoff, ~7.6% of residual GMV) — none force-mapped. QA gates (no `--skip-coexistence`): G1=0, G2=0, placeholder-leak=0, structured-fields NULL%=0%, G5=0. G4 fails at whole-category scope (7 pre-existing `SKU-008441` rows, unchanged) but passes at 0 scoped to this session's own writes. Universe refresh intentionally not run this session per instructions. |
