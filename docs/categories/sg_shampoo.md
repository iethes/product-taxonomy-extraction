# shopee_sg_shampoo — Category Context

> First LLM extraction for this category — SG was 0/23 categories LLM-extracted per `docs/categories/STATUS.md`
> before this. Written as part of the headless-taxonomy-runbook worked example
> (`docs/superpowers/plans/2026-07-14-headless-taxonomy-runbook.md` Task 6). Two live headless attempts so far —
> see QA History for what each found. Attempt #2 (2026-07-15) actually wrote to production: Pass 1 complete,
> Pass 2 intentionally partial. Read the Brand Scope correction below before trusting the old "~20 brands"
> framing — the true 95%-GMV universe is ~190 brands, not ~20.

---

## Status

| Field | Value |
|-------|-------|
| LLM Pass 1 | ✅ Complete — 925 taxonomy entries, 965 official-store products mapped (all 23 allowlist brands) |
| LLM Pass 2 | ⏳ Partial — bulk text-matching (301 products) + minimal catch-alls for the 9 named no-official-store brands (1,155 products); ~158 further brands in the true 95%-GMV list are still unmapped |
| Keyword-seed (HUMAN) coverage | 1,408 rows remain, `source='HUMAN'` (down from 2,255 — the 847 that duplicated an LLM row were deleted 2026-07-16 per manager-confirmed policy; these 1,408 have no LLM row and stay permanently) |
| GMV Coverage (LLM) | 33.7% of category GMV (up from 0%) |
| Product-count coverage (LLM) | 9.8% (2,421 of 24,818 distinct products) — expected to lag GMV coverage since Pass 1 targets highest-GMV listings first |
| Universe refresh | **All 4 gates now pass** (0/0/0/0, verified live 2026-07-16 after the `product_line` backfill) — no longer blocked. **Not yet run** — see Reviewing this run's output / next steps. |
| Last run | 2026-07-15, attempt #2, `status='partial'`, verified against live BigQuery (not just claimed) |
| Current MAX taxonomy_id (as of 2026-07-15, before this session) | SKU-068015 — **re-verify live before claiming any new block, never trust this number** |

---

## Scale (verified live 2026-07-15)

- **1,615,309 total rows** in `master_clean_niq.shopee_sg_shampoo`; **24,818 distinct products**.
- **187,902 rows** with `merchant_badge = 'Shopee Mall'` (candidate Pass 1 pool before narrowing to the actual
  allowlist below — Pass 1 correctly used only the allowlist, not all 187,902).
- **The real 95%-GMV brand universe is ~190 distinct brands**, not the ~20 this file originally listed (that
  was a top-20-by-absolute-GMV snapshot, never an actual cumulative-GMV-threshold calculation — a real gap in
  the original research, caught by attempt #2's own live GWP-adjusted cumulative ranking). Of those ~190: 23
  have official stores (Pass 1, done), 9 more were explicitly named as no-official-store (got minimal
  catch-all coverage in Pass 2), and **~158 remain completely unaddressed** — several individually larger than
  brands already covered (e.g. Goldwell, Aveda, L'Oréal Paris, Nizoral, Hair+, Fayre Beauty, Dixmondsg).
- **SKU budget is the binding constraint for finishing Pass 2 properly, not effort.** Personal-care listings
  fragment into many near-unique SKUs — Pass 1 alone (965 official-store products) consumed 925 of the
  pre-assigned 1,000 slots, leaving only 75 for all of Pass 2. Extrapolating Pass 1's density, properly
  covering the remaining ~158 brands at real product-line/size granularity needs a **supplemental block on the
  order of 1,500–3,000 slots** — claim one via Shared mechanics § Atomic SKU block claim before a follow-up
  session; don't try to force this into the 66 slots left in the current block (`SKU-069935`–`SKU-070000`).

---

## SKU Blocks Assigned

| Block | Usage | Status |
|-------|-------|--------|
| SKU-069001–SKU-069934 | Attempt #2 — Pass 1 (925 entries) + Pass 2 catch-alls (9 entries) | Written 2026-07-15. Verified live: 934 `product_taxonomy` rows, 2,421 `product_taxonomy_map` rows (`source='LLM'`). |
| SKU-069935–SKU-070000 (66 slots, within the same claimed block) | Unused | Technically available but **not enough** for a proper Pass 2 completion pass — see Scale above. Leave unused, claim a fresh supplemental block instead of fragmenting further work across two small ranges. |

Registry row for this block is still `status='ACTIVE'` — normal, since the block wasn't exhausted or marked
`FAILED_QA`/`COMPLETE`. No new claim needed unless a follow-up session wants the leftover 66 slots for a small,
targeted fix (not a full Pass 2 completion).

---

## Brand Scope — corrected 2026-07-15

**The original "top 15–20 by GMV" list below was never an actual 95%-threshold calculation** — it was a
top-20-by-magnitude snapshot mislabeled as brand scope. Kept for reference (it's still useful as "highest
individual GMV brands," which is why they were correctly prioritized for Pass 1's official-store allowlist),
but **do not treat this as the Pass 2 scope boundary.** The real boundary is ~190 brands by cumulative GMV
share — recompute live before any follow-up Pass 2 session, do not reuse this snapshot as-is.

1. **Milbon** — SGD 95,043 GMV, 568 products
2. **Grafen** — SGD 73,619 GMV, 48 products
3. **MadeToBloom** — SGD 73,562 GMV, 9 products
4. **Dr.FORHAIR** — SGD 70,489 GMV, 76 products
5. **Kérastase** — SGD 62,069 GMV, 435 products
6. **Tsubaki** — SGD 57,941 GMV, 104 products
7. **Diane** — SGD 56,039 GMV, 208 products
8. **UNOVE** — SGD 55,836 GMV, 33 products
9. **Dr.ville** — SGD 55,428 GMV, 32 products
10. **NARD** — SGD 52,026 GMV, 33 products
11. **sukin** — SGD 50,789 GMV, 33 products
12. **Phytopecia** — SGD 41,863 GMV, 19 products
13. **Shiseido Professional** — SGD 41,261 GMV, 182 products
14. **Off&relax** — SGD 40,432 GMV, 104 products
15. **Suu Balm** — SGD 37,828 GMV, 20 products

`brand_id` values: resolved during Pass 1/2 for the 32 brands actually processed; not resolved for the
remaining ~158 in this snapshot.

---

## Official Store Allowlist (Pass 1) — used as-is, confirmed correct

Pulled live 2026-07-15 from `merchant_badge = 'Shopee Mall'` rows. Exact merchant names, not a
`LIKE '%official%'` wildcard, per `docs/llm-extraction-rules.md` §4. **All 23 entries below were used in Pass
1 with full coverage** (973 candidate products, 965 in-scope and mapped, 8 correctly left NULL as genuinely
out-of-scope pure-styling listings):

| Brand | Official Store Merchant Name |
|-------|------------------------------|
| Shiseido Professional | `Shiseido Professional` |
| Daeng Gi Meori | `Daeng Gi Meo Ri Official Store` |
| Grafen | `Grafen Korea Official Store` |
| KUNDAL | `KUNDAL SG` |
| NaturVital | `NaturVital Official Store` |
| Dr.FORHAIR | `Dr.FORHAIR KOREA OFFICIAL STORE` |
| Diane | `Mandom Official Store` |
| Schwarzkopf | `Schwarzkopf` |
| Himalaya | `Himalaya Official Store` |
| O'right | `O'right Official Store` |
| ＆honey | `Cosme Flagship Store` |
| Lakme | `LAKMÉ Official Store` |
| Klorane | `Klorane Official Flagship Store` |
| Fanola | `Fanola Official Store Singapore` |
| Ryo | `AMOREPACIFIC Hair&Beauty Shop` |
| Kevin Murphy | `WOOSHOP Singapore` |
| Herbal Essences | `P&G Beauty Official Store` |
| Bananal | `RAUM Korea` |
| Pantene | `P&G Official Store` |
| Dove | `Unilever Official Store` |
| Amos Professional | `Amos Professional SG Official Mall` |
| Head & Shoulders | `P&G Official Store` |
| NARD | `Nard Store` |
| Avalon Organics | `Avalon Organics X Official Store` |
| Kérastase | `Kerastase` |

**Multi-brand stores excluded from the allowlist** (per `docs/llm-extraction-rules.md` §4's explicit exclusion
list — Sasa is named there directly) — confirmed handled correctly, routed to Pass 2/bulk-text-matching instead:
- `Sasa Official Store`, `Nana Mall Official Store`, `Strawberrynet SG Official Store`, `KimageSalon Official Store`
  — all sell Kérastase among other brands.

P&G Official Store sells both Pantene and Head & Shoulders — parent-company store, both brands correctly kept
in Pass 1, disambiguated by product content per plan.

**Brands with no official store — got Pass 2 catch-all coverage 2026-07-15:** Milbon, MadeToBloom, Tsubaki,
UNOVE, Dr.ville, sukin, Phytopecia, Off&relax, Suu Balm. Each got exactly one `size=NULL,
is_multi_variant=TRUE` catch-all taxonomy entry (confidence 0.6) covering all shampoo-keyword-filtered products
for that brand — real non-NULL coverage, zero product-line/size granularity. Milbon alone has 768 distinct real
products collapsed into its single catch-all entry — a follow-up session with a proper SKU budget should split
these into real per-line entries.

---

## Scope — What's In vs Out

**In scope:** shampoo, 2-in-1 shampoo+conditioner (per the same pattern already established for
`th_body_wash`/`th_fabric_softener` 2-in-1 handling in `docs/llm-extraction-rules.md`).

**Dry shampoo: resolved 2026-07-15, IN SCOPE.** Real dry-shampoo products exist in this NIQ bucket (K18
AirWash, Diane UV Dry Shampoo) — no separate dry-shampoo NIQ category exists in this data, and NIQ itself
bucketed them here. Treated as in-scope during Pass 1/2.

**Out of scope (leave NULL):** standalone conditioner/hair treatment (separate NIQ category).

**Edge cases:**
- **Same-brand multi-component sets** (e.g. "Shampoo + Treatment" bundles from one brand) — found 2026-07-15,
  52 entries. These do **not** get `is_bundle=TRUE` — per `ARCHITECTURE.md`, that flag is schema-reserved for
  *cross-brand* bundles. Same-brand sets use `is_multi_variant=TRUE` plus a descriptive `canonical_name`
  instead. New precedent for this category; apply consistently to any future same-brand-set discoveries.
- **Brand `&herb`** (2 low-GMV `Cosme Flagship Store` products) is not registered in `brand_dict` under any
  casing checked — routed to `BRD-UNDEFINED` in this pass, flagged here for Stage 02 registration. Its sibling
  private label `&Honey` *is* registered (`BRD-GLOBAL-00237`) — worth checking if `&herb` should be under the
  same umbrella brand.
- **NARD's single highest-GMV product** ($9.76M, 82% of NARD's Mall GMV) is an 8ml shampoo+treatment sachet
  sampler, not a full-size bottle — confirmed via image read, not assumed from size text alone (a case where
  the size-text-wins-over-image rule in `docs/llm-extraction-rules.md` §2 didn't have clean text to trust, so
  image was the deciding signal by necessity, not by overriding a stated size).

---

## Taxonomy Design Notes

**Entry quality is tiered, not uniform** (2026-07-15 Pass 1 result, 925 entries total):
- **739 STANDARD** — clean brand + product line + size, extracted directly from text.
- **52 SET_WITH_SIZE** — same-brand multi-component sets with a real, confirmed size (see Edge cases above).
- **50 STANDARD_BRAND_FALLBACK** — size not stated in the specific listing's `sku_name`, but confirmed via
  `option_name` variant lists or sibling listings from the same brand/line rather than guessed.
- **124 MULTI_VARIANT_CATCHALL** — `size=NULL`, `is_multi_size=TRUE`, for listings offering many
  product-type/flavor choices in one SKU. **This is the real precision debt in Pass 1** — concentrated in
  low/zero-GMV long-tail listings, not documented per-row individually, not image-verified case by case.
  Candidate list for a future precision pass if these prove to matter for analysis.

**Image verification was used where text was genuinely ambiguous**, not by default: confirmed Kérastase 500ml
sample sizing, K18 AirWash at 118ml, Grafen 500ml×2, Kevin Murphy standard=250ml, Dungud standard=1000ml — all
via image read or `option_name` cross-reference, per `docs/llm-extraction-rules.md`'s image-as-tiebreaker
priority.

**Finding (2026-07-15, manual QA): `product_line`/`sub_line`/`variant` are NULL on all 934 entries — 100%,
not a tier-specific gap.** The "739 STANDARD — clean brand + product line + size" self-report above is
inaccurate on this specific point: the session wrote good, human-readable `canonical_name` values (e.g.
`"Amos Professional PURE SMART Line dandruff care Shampoo FRESH / MOIST / Deep Action / Pack 500ml"`, where
`"PURE SMART Line dandruff care Shampoo"` is clearly the on-label product line per
`docs/llm-extraction-rules.md` §3) but never decomposed that text back into the structured `product_line`
column the schema provides for exactly this purpose. Verified this is run-specific, not a pipeline-wide issue:
the rest of `product_taxonomy` (18,582 rows from shipped TH categories) sits at a normal 9.2% NULL rate for
`product_line` (legitimate catch-alls); this run alone is 100%.

Contributing factor: `sql/schema/product_taxonomy.sql` documents `product_line STRING NOT NULL`, but the live
column is actually nullable (`INFORMATION_SCHEMA.COLUMNS.is_nullable = 'YES'`) — another instance of the
schema-file-vs-live-table drift found repeatedly elsewhere this session. Nothing blocked this at write time.

**Backfilled 2026-07-16.** `product_line` populated for all 805 non-catch-all entries via a deterministic
SQL pass: strip the known brand prefix (`brand_dict.canonical_name`, case-insensitive; special-cased
`Kérastase`/`Kerastase` accent mismatch and one `myBoostars`/`my Boostars` spacing mismatch — only 1 of 805
rows didn't match either the brand prefix or Kérastase's alias, and that was the `myBoostars` case, handled)
and the known trailing size pattern (`\s+\d+(\.\d+)?\s*(ml|g|kg|l)\s*(x\d+)?\s*$`) from `canonical_name`; what
remains is `product_line`. Verified live: 805/805 updated, 0 empty-string results, spot-checked sample all
correct (e.g. `"Amos Professional PURE SMART Line dandruff care Shampoo FRESH / MOIST / Deep Action / Pack
500ml"` → `product_line = "PURE SMART Line dandruff care Shampoo FRESH / MOIST / Deep Action / Pack"`).

**The 129 `is_multi_size=TRUE` catch-all entries were deliberately left NULL** — no single product line
applies to a catch-all by definition; backfilling one would misrepresent the data.

**`variant` backfilled 2026-07-16 for 18 entries — judgment pass, not mechanical.** Unlike `product_line`,
there's no reliable structural pattern for `variant`/`sub_line`: checked existing shipped-category rows where
these fields are populated and found the convention itself is inconsistent (`variant` holds flavor names in
some categories, age ranges in others, size-class codes in others — no single learnable rule). Manually read
all 139 `product_line` values containing `/` (the only candidate subset with any chance of holding a real
variant list) and classified them:
- Most are **not** variants — `"Shampoo/Conditioner"`, `"Shampoo/Treatment/Hair Oil"` describe bundle
  *contents*, not a variant *choice*, and `"EXP date: 24/10"`-style fragments are date noise, not variants.
- 18 had a genuinely clear signal — real fragrance/formula name lists (e.g. `"Blue Jasmine / Grasse Rose /
  Orange Flower / Night Dream Tea"`), an explicit `"Assorted"` marker, or a `"Bundle of N / {Scent}"` pattern
  where the scent after the slash is real. These got `variant` populated and `product_line` trimmed to remove
  the now-separated variant text.

**`sub_line` left NULL for all 934 entries** — found no reliable signal anywhere in this category's data,
mechanical or judgment-based. The reference examples from shipped categories didn't suggest a pattern that
maps onto shampoo naming conventions specifically.

The remaining 121 slash-containing entries and all 666 non-slash entries were **not** individually reviewed for
variant — reading 787 more entries by hand is beyond reasonable scope for a backfill pass; a proper pass would
need image context or a real extraction re-run, not further manual text reading. NULL here is honest ("not
determined"), not "confirmed absent."

**Found a real bug in the QA gate while re-checking this**: Gate 4 (added 2026-07-15) counted `product_taxonomy_map`
rows, not distinct taxonomy entries — since the 129 catch-alls fan out to 1,284 products (one Milbon entry alone
covers 768), the gate showed 53% NULL even after all 805 real entries were correctly backfilled (true rate: 0%).
Fixed in `docs/headless-runbook.md` to count `COUNT(DISTINCT taxonomy_id)` and exclude `is_multi_size` catch-alls
from the denominator. All 4 gates now pass for this table (dual-mapped 0, coexistence 0, placeholder-leak 0,
structured-fields 0).

---

## Existing HUMAN rows — delete only where duplicated by LLM

2,255 `source='HUMAN'` rows existed in `product_taxonomy_map` for this category before 2026-07-16 (automated
keyword-routing from an earlier seed pass — per `docs/decisions/ADR-006`, `HUMAN` does **not** mean an actual
person reviewed them).

**Policy (confirmed by manager, 2026-07-16): delete a HUMAN row only if that same product also has an LLM
row.** Not a blanket supersede of every HUMAN row in the category (an earlier revision of this file said that,
then a later revision removed deletion entirely — both superseded by this narrower, confirmed policy). Of the
original 2,255 HUMAN rows, **847 duplicated an existing LLM row and have been deleted**; **1,408 remain** —
those have no LLM row and stay untouched permanently, since deleting them would leave that product with no
taxonomy at all, worse than the keyword-seed routing it already has.

**Delete query actually run 2026-07-16** (pre-verified 847 matched before running for real):
```sql
DELETE FROM `sincere-hearth-273704.magpie_reference.product_taxonomy_map`
WHERE master_table = 'shopee_sg_shampoo' AND source = 'HUMAN'
  AND product_id IN (
    SELECT product_id FROM `sincere-hearth-273704.magpie_reference.product_taxonomy_map`
    WHERE master_table = 'shopee_sg_shampoo' AND source = 'LLM'
  );
```

**Sequence** (see `docs/headless-runbook.md`'s Full Rebuild scenario steps 4–8 for the authoritative version):
1. Pass 1 + Pass 2 built the new LLM taxonomy (934 entries, 2,421 map rows). ✅ Done 2026-07-15.
2. `run_qa_gates shopee_sg_shampoo --skip-coexistence` — dual-mapped (scoped to LLM) and placeholder-leak both
   verified 0/0 live. ✅ Done 2026-07-15.
3. Deleted the 847 overlapping HUMAN rows (query above). ✅ Done 2026-07-16 — exactly 847 rows affected,
   matching the pre-check.
4. `run_qa_gates shopee_sg_shampoo` (no flag) — verified live 2026-07-16: dual-mapped 0, coexistence **0**
   (exactly as predicted — the delete targeted exactly the overlapping set), placeholder-leak 0. ✅ Gates 1–3
   pass. Gate 4 (structured-fields) initially showed 53% — traced to a bug in the gate itself (counted map
   rows, not distinct entries; fixed in `docs/headless-runbook.md`), not a real regression.
5. Backfilled `product_line` for all 805 non-catch-all entries (see Taxonomy Design Notes). Gate 4 re-checked
   with the fixed query: **0%.** ✅ All 4 gates now pass.
6. Universe refresh — **not yet run**, no longer blocked. Next action for this category.

Use "Reviewing this run's output" below to inspect the taxonomy directly.

---

## Reviewing this run's output (manual QA)

**Tables involved:**

| Table | What's in it for this run | Grain |
|-------|---------------------------|-------|
| `magpie_reference.product_taxonomy` | The 934 new canonical SKU entries (`SKU-069001`–`SKU-069934`) — brand, product line, size, pack, canonical name | One row per canonical SKU |
| `magpie_reference.product_taxonomy_map` | Links each real product to a `taxonomy_id`. Filter `master_table='shopee_sg_shampoo' AND source='LLM'` for the new rows, `source='HUMAN'` for the 2,255 rows being considered for deletion | One row per `(product_id, platform, country)` |
| `master_clean_niq.shopee_sg_shampoo` | Raw listings — `sku_name`, `merchant_name`, images, price, GMV — cross-reference to sanity-check what the LLM actually extracted against | One row per product/model variant |

**Query 1 — browse the new taxonomy entries themselves:**
```sql
SELECT taxonomy_id, brand_id, product_line, sub_line, variant, size, pack_count,
       canonical_name, is_bundle, is_multi_variant, is_multi_size
FROM `sincere-hearth-273704.magpie_reference.product_taxonomy`
WHERE taxonomy_id BETWEEN 'SKU-069001' AND 'SKU-069934'
ORDER BY taxonomy_id;
```

**Query 2 — new taxonomy joined back to the real product it was extracted from** (the most useful one for spot
checking — lets you see the LLM's canonical name next to the actual `sku_name` it read):
```sql
SELECT pt.taxonomy_id, pt.canonical_name, pt.size, pt.pack_count, m.confidence,
       src.sku_name, src.merchant_name, src.merchant_badge, src.gmv_monthly
FROM `sincere-hearth-273704.magpie_reference.product_taxonomy` pt
JOIN `sincere-hearth-273704.magpie_reference.product_taxonomy_map` m ON m.taxonomy_id = pt.taxonomy_id
JOIN `sincere-hearth-273704.master_clean_niq.shopee_sg_shampoo` src ON src.product_id = m.product_id
WHERE m.master_table = 'shopee_sg_shampoo' AND m.source = 'LLM'
ORDER BY src.gmv_monthly DESC;
```

**Query 3 — the 124 catch-all entries specifically** (the flagged precision debt — `is_multi_size=TRUE`, worth
a closer look since these collapse many real products into one generic entry):
```sql
SELECT taxonomy_id, canonical_name, brand_id
FROM `sincere-hearth-273704.magpie_reference.product_taxonomy`
WHERE taxonomy_id BETWEEN 'SKU-069001' AND 'SKU-069934' AND is_multi_size = TRUE
ORDER BY taxonomy_id;
```

**Query 4 — side-by-side old (HUMAN) vs new (LLM) for the same product**, where both exist — the most direct
before/after comparison for products that were routed under the keyword-seed pass and now have a real LLM
taxonomy assignment too:
```sql
SELECT m_llm.product_id, src.sku_name,
       pt_llm.canonical_name AS new_llm_canonical_name,
       pt_human.canonical_name AS old_human_canonical_name
FROM `sincere-hearth-273704.magpie_reference.product_taxonomy_map` m_llm
JOIN `sincere-hearth-273704.magpie_reference.product_taxonomy` pt_llm ON pt_llm.taxonomy_id = m_llm.taxonomy_id
JOIN `sincere-hearth-273704.magpie_reference.product_taxonomy_map` m_human
  ON m_human.product_id = m_llm.product_id AND m_human.source = 'HUMAN' AND m_human.master_table = 'shopee_sg_shampoo'
JOIN `sincere-hearth-273704.magpie_reference.product_taxonomy` pt_human ON pt_human.taxonomy_id = m_human.taxonomy_id
JOIN `sincere-hearth-273704.master_clean_niq.shopee_sg_shampoo` src ON src.product_id = m_llm.product_id
WHERE m_llm.master_table = 'shopee_sg_shampoo' AND m_llm.source = 'LLM';
```

**Query 5 — what's still NULL among the highest-GMV products** (useful for judging whether the ~158
unaddressed brands actually matter before deciding to claim a supplemental block):
```sql
SELECT src.product_id, src.sku_name, src.merchant_name, src.gmv_monthly
FROM `sincere-hearth-273704.master_clean_niq.shopee_sg_shampoo` src
LEFT JOIN `sincere-hearth-273704.magpie_reference.product_taxonomy_map` m
  ON m.product_id = src.product_id AND m.master_table = 'shopee_sg_shampoo'
WHERE m.taxonomy_id IS NULL
ORDER BY src.gmv_monthly DESC
LIMIT 50;
```

---

## QA History

| Date | Pass | Finding | Resolution |
|------|------|---------|------------|
| 2026-07-15 | Headless attempt #1 (Full Rebuild) | Session correctly stopped before writing: found 2,255 undocumented HUMAN rows (this file wrongly said 0), ambiguous instruction about whether the agent itself performs extraction or needs `ANTHROPIC_API_KEY` for a subprocess, and 187,902-row official-store pool too large for one session to vision-read. | This file and `docs/headless-runbook.md`'s Full Rebuild prompt rewritten before attempt #2. |
| 2026-07-15 | Headless attempt #2 (Full Rebuild) | Completed `status='partial'`, verified against live BigQuery. Pass 1 complete (925 entries, 965 products, all 23 allowlist brands). Pass 2 intentionally partial — SKU budget (1,000 slots) consumed almost entirely by Pass 1's fragmentation; only 75 slots left, 9 used for no-official-store catch-alls, 301 products bulk-text-matched, ~158 further brands (of the true ~190-brand 95% list, itself a correction to this file's original ~20-brand snapshot) left unmapped. Also caught a real bug: `headless-runbook.md`'s dual-mapped QA gate was unscoped and would have misfired (847, not 0) on legitimate mid-rebuild HUMAN+LLM coexistence. | `sg_shampoo.md` rewritten with corrected Brand Scope/Scale/Edge Cases (this revision). `headless-runbook.md`'s QA gate scoped to `source='LLM'`, `--skip-coexistence` flag added. HUMAN-row deletion + refresh still pending (see disposition policy above). |
| 2026-07-15 | Manual QA (post attempt #2) | `product_line`/`sub_line`/`variant` NULL on 100% of the 934 new entries (rest of `product_taxonomy` sits at a normal 9.2% NULL rate — run-specific, not pipeline-wide). Root cause: extraction wrote good `canonical_name` text but never decomposed it into the structured fields; the session's own quality-tier self-report ("739 STANDARD — clean... product line") was inaccurate on this point. Contributing factor: live `product_line` column is nullable despite `sql/schema/product_taxonomy.sql` documenting `NOT NULL` — another schema-vs-live drift instance. | `docs/headless-runbook.md`'s Full Rebuild prompt now explicitly requires populating these 3 fields, with worked examples. New QA gate (`structured_fields_missing`) added to `run_qa_gates()` to catch this automatically on future runs. Data for this run's 934 entries not backfilled yet — see Taxonomy Design Notes. |
| 2026-07-16 | HUMAN-row cleanup + gate re-run | Manager confirmed policy: delete HUMAN rows only where duplicated by an LLM row. Deleted 847 (verified against pre-check, exact match). Re-ran all 4 gates: dual-mapped 0, coexistence 0 (exactly as predicted — delete targeted precisely the overlapping set), placeholder-leak 0, **structured-fields still 100% (Gate 4 fails — expected, not yet backfilled)**. | Universe refresh deliberately held rather than overriding Gate 4 — decision made explicitly, not defaulted into. Next: backfill `product_line`/`sub_line`/`variant` or re-run Pass 1, then refresh. |
| 2026-07-16 | `product_line` backfill | Backfilled all 805 non-catch-all entries via deterministic strip (brand prefix + trailing size from `canonical_name`) — 805/805 updated, 0 empty results, spot-check clean. 129 catch-alls left NULL intentionally. Re-checked Gate 4: showed 53%, not the expected ~0% — traced to a bug in the gate query itself (counted `product_taxonomy_map` rows, which fan out per product, not distinct taxonomy entries; 129 catch-alls fan out to 1,284 products, e.g. one Milbon entry alone covers 768). | Gate 4 fixed to count `DISTINCT taxonomy_id` and exclude `is_multi_size` catch-alls from the denominator — re-verified against both `shopee_sg_shampoo` (now 0%, correct) and `shopee_th_body_wash` (still 0%, unaffected). All 4 gates now pass for `shopee_sg_shampoo`. Universe refresh no longer blocked. |
| 2026-07-23 | Top-up coverage session (month 2026-06) | Live worklist re-queried (never trusted the prompt's stale 2,510 figure): 2,510 model-grain rows / 1,473 distinct products / SGD 604,756 GMV still uncovered within the 95%-cumulative-GMV (GWP-zeroed) threshold. Bulk-first reuse-before-mint: grouped by `(brand_id, size, pack_count, normalized product_line)` via deterministic text extraction (regex size/pack parsing per `llm-extraction-rules.md` §1/§2 priority, brand recovery for 168 NULL/`BRD-UNDEFINED` products via `brand_dict` substring matching then manually verified — rejected ~35 false-positive brand matches caught by spot-checking, e.g. reseller-store-name leakage ("Beauty Language" suffix, exact §11 pattern), product-line words misread as brand ("Expert", "Care", "Genesis", "Silver" on Ryo/L'Oreal/Kérastase/Vikada products), and 3 genuinely OOS listings (a wound-dressing pad, a lice lotion, a board game — NIQ miscategorization) left NULL rather than force-mapped. A first grouping pass truncated the line-matching key to 30 chars and silently merged distinct products (different hair colors, different L'Oréal Professionnel lines) onto one taxonomy entry — caught before writing via spot-check, fixed by using the untruncated normalized line as the group key. Routed 11 products to 6 existing catch-all entries (Milbon, Suu Balm, etc.); minted 1,302 new entries (1,275 specific brand+line+size+pack entries, 27 new per-brand catch-alls for genuinely multi-product/multi-variant listings) in block `SKU-126556`–`SKU-127857` (claimed 2,000-slot block `SKU-126556`–`SKU-128555`, only ~65% used — block is a reservation, not a quota). Mapped 1,362 of 1,473 worklist products (92.5% of the count, 95.1% of the GMV gap); 111 products left NULL — 3 confirmed OOS, ~108 where neither brand nor product line could be confidently read from `sku_name` text alone (no images were read this session per the bulk-text-matching mandate; these are honest `UNRESOLVED`, not defects). QA gates re-verified live post-write: G1 dual-mapped 0, G2 HUMAN+LLM coexistence 0, G5 provenance 0, structured-fields-missing 0% among this session's new LLM entries. Placeholder-leak gate shows 2,622 (182 distinct taxonomy entries) containing "Multiple Variants" — confirmed **entirely pre-existing** from the 2026-07-15 attempt #2 entries (predates the 2026-07-22 rule that banned this phrasing unconditionally); verified 0 of this session's 1,302 new entries trip it. Per this session's explicit scope (coverage over precision), the pre-existing placeholder-leak debt was left untouched, not remediated. | Universe refresh **not run this session** — explicitly out of scope, left for independent QA verification first. The 182 pre-existing "Multiple Variants" entries remain open precision debt for a future `targeted_qa_fix.sh` pass (rename to real descriptive text per the 2026-07-22 rule, e.g. "{Brand} {real description}" with no "Multiple Variants" suffix). The 111 remaining NULL products are candidates for a future image-reading pass if their GMV proves material. |
| 2026-07-23 | Top-up coverage session #2 (month 2026-06) | Wrapper's pre-check claimed 175 still-NULL rows; live re-query in STEP 0 confirmed 175 model-grain rows / **111 distinct products** / SGD 29,754 GMV — i.e. essentially the exact 111 products the prior same-day session left `UNRESOLVED` (confirmed by spot-matching the 3 known OOS listings: board game, wound-dressing pad, lice lotion — all present again). This session did direct LLM reading (own multimodal text reasoning) of `sku_name` plus, new this time, bulk-pulled `raw_niq_history.shopee_sg_shampoo`'s `item_name`/`product_specifications`/`product_description` for all 111 (96 had rows) as an additional text signal before falling back to images — no images were actually needed; text + description resolved the great majority. Checked existing taxonomy first for exact reuse: found 5 direct matches (K18 Peptide Prep Detox 250ml, Kérastase Discipline Bain Fluidealiste 250ml from the same Beauty Language reseller, Head & Shoulders BARE Minimal Ingredients 400ml, and both Repit entries) — mapped via existing `taxonomy_id`, no new rows. For the remaining 106: identified 11 *existing* brand_dict brands (Kao, Seapuri, K18, Noah, JUNGSAEMMOOL, Arganmidas, Queen's, L'Oreal, KIGI, Melaleuca, Thann, Sabun) and reused their `brand_id`; found 49 real, confidently-read brand names with **no existing `brand_dict` row** (Ninazol, Vikada, Galtiscopio, Dr.SYS, Fortro, Somang, SH-RD, Wellsen, etc.) and registered them as new `BRD-SG-13459`–`BRD-SG-13507` entries (`country_scope='SG'`, `brand_level=2`) rather than force-mapping to `BRD-UNDEFINED`, since the brand name itself *was* legible from text/description — just previously unregistered; 15 products had no proper-noun brand anywhere in any text field (generic reseller descriptors, e.g. Beauty Garden's plant-named private-label line) and were correctly assigned `BRD-UNBRANDED`, not a guessed brand. Grouped the 100 resolvable/mintable products into 88 taxonomy entries (dedup groups: Ninazol Ketoconazole 100ml pack1/pack2, Dr.SYS Reset Cream Set, Galtiscopio Caviar Set, Craft Pre-Cleanser, Seven Green Soap — same brand+line+size+pack collapsed to one entry per group, never one-row-per-product). Left 6 products NULL: the same 3 confirmed OOS from the prior session (board game, dressing pad, lice lotion) plus 2 newly-recognized OOS (SLSA/DLS raw cosmetic surfactant ingredients — bulk chemical inputs for making bath bombs/shampoo, not finished shampoo products, caught via `raw_niq_history` description text on this pass) and 1 genuinely unresolved (a Korean red-ginseng product with zero brand signal in any text field, $656 GMV — flagged for a future image-read pass rather than guessed). Claimed a fresh 200-slot block `SKU-129556`–`SKU-129755` per the atomic-claim procedure (88 used, ~44%, block is a reservation not a quota) rather than reusing the prior session's still-`ACTIVE`, ~35%-unused `SKU-126556`–`SKU-128555` block — instructions specified claiming fresh, so followed as given even though the leftover block would also have fit. One post-write placeholder-leak gate hit on `SKU-129584` ("...Multiple Variants: Treatment/Mentol/Conditioner...") — caught and fixed in the same session (renamed to the real variant list without the banned phrase, `is_multi_variant=TRUE` already conveyed the semantic) before re-verifying gates. Live re-query after all writes confirmed the worklist gap closed from 111 to exactly the 6 intentional NULLs (SGD 1,926 remaining GMV, all legitimately OOS/unresolved). | QA gates re-verified live post-write, all pass: G1 dual-mapped 0, G2 HUMAN+LLM coexistence 0, G5 provenance 0, placeholder-leak 0 (after the SKU-129584 fix), structured-fields-missing 0/88. SKU block `SKU-129556`–`SKU-129755` marked `COMPLETE` in `sku_block_registry` (88/200 used). Universe refresh **not run this session** — explicitly out of scope per instructions, left for independent QA verification first. The 6 remaining NULL products (5 OOS, 1 unresolved-brand) are low-GMV (SGD 1,926 combined) and not worth a dedicated follow-up unless bundled with other categories' image-read passes. |
| 2026-07-23 | Automated Taxonomy Review #3 (targeted_qa_fix auto-discovery) | Worklist: 2,374 never-reviewed/unconfident entries. Tier 1 SQL sweep flagged 29 rows across `duplicate_brand` 5, `wrong_field_order` 2, `excess_content` 7, `canonical_field_mismatch` 8, `null_size` 9, `garbage_brand` 0, `stub_leak` 0, `brand_casing_mismatch` 0 (one row can trip >1 flag). Judged 14 of these as false positives, all matching precedent already documented from the prior two sessions: same-brand-set brand-name repetition (`Na`, `Plantur 39`, both `Phyto`/`Phytocyane` substring hits, `Sisley Hair Rituel by Sisley`), `BRD-UNBRANDED` products correctly lacking brand text (`wrong_field_order` on 2 entries), and same-brand multi-component sets stating a real per-component size for each item (4 Daeng Gi Meori, 1 Amos Professional, 1 anillo, 1 &Honey — the established `SET_WITH_SIZE` pattern from this category's original Pass 1). Fixed the 6 genuine `canonical_field_mismatch` rows (Clend, Placentinol, Ampulir, Sabun, Noah, one `BRD-UNBRANDED` purple-shampoo entry): each had a real, spec/description-confirmed size (`raw_niq_history.product_specifications`' `Volume`/`Weight` fields) that was never appended to `canonical_name` despite the `size` column being correctly populated — appended it in each case (e.g. `Placentinol Hairloss Shampoo` → `...Hairloss Shampoo 1kg`). Of the 9 `null_size` flags, resolved 3 via a direct product-image read (no text/spec signal existed for any of these three): EHD → 500ml (badge printed on the bottle), Seapuri → 300ml (stated on-label as "10.14 fl. oz. / 300 ml"), Melaleuca → 946ml (stated on-label as "32 FL OZ/946 mL"); left the remaining 6 honestly `UNRESOLVED` — Seven Green (bar-soap product, no gram weight in any available image), As I Am and Miriam Quevedo (the only image pulled was a generic brand-catalog/lineup shot, not the specific product — the cover-image caveat applies, no reliable size read possible), Ruyi (image shows a 2-bottle promo shot with ambiguous "买1送1 1000ml" text — could mean per-bottle or combined total, too ambiguous to guess pack_count/size), JUNGSAEMMOOL and Inebrya (very low GMV, no size signal found in `sku_name`/spec/description, not pursued further this session). STEP 2b promo-language sweep (18,368 model-grain rows / 176 distinct entries, mostly matching generic `x\d` / `free` patterns) was folded into the GMV-prioritized Tier 2 sample rather than reviewed exhaustively per design. Tier 2 GMV-ranked sample (top 60 entries by GMV) surfaced: the wider `SKU-002xxx` "{Brand} Shampoo" D1 Tier-C generic-stub family (35 of the top 60 by GMV alone, $247K–$2.1M each) — reconfirmed as the same still-unaddressed precision debt flagged by the prior session, needing a proper bulk-text-matching split, not a one-row fix; per instructions, judged `wrong` but left untouched (no `_meta` write) since not fixed this session. Also reconfirmed the 9 brand "Assorted Shampoo" catch-alls (Milbon, Off&relax, Tsubaki, UNOVE, sukin, Suu Balm, Phytopecia, Dr.ville, MadeToBloom) as the same intentional, previously-documented Pass 1 design tradeoff — left untouched. One new, real defect found and fixed: 3 Pantene products (`27724756684`/`29128383459`/`29667093846`, all clearly "Pantene ... Anti Dandruff Shampoo" in `sku_name`, 2 confirmed via image showing the genuine Pantene Pro-V Anti Dandruff bottle design) were mapped via a legacy `source='HUMAN'` keyword-seed row to `SKU-137196` — the `BRD-UNBRANDED` generic catch-all minted just one session prior — a genuine brand misassignment (Pantene is a long-registered, `BRD-GLOBAL-00090` brand with 40 existing taxonomy entries in this category; none matched a standalone "Anti Dandruff" line without "Head & Shoulders" co-branding). Claimed a fresh 200-slot block (`SKU-138128`–`SKU-138327`), minted 2 new entries (`Pantene Pro-V Anti Dandruff Shampoo 680ml`/`170ml`), and rerouted all 3 products to them (`source` updated `HUMAN`→`LLM`, `confidence='0.9'`, `meta_agent='CLAUDE_CODE'`). Verified via Tier 2 GMV sample and marked correct-as-is: 12 well-formed Tier-A entries (Dr.FORHAIR ×2, Grafen ×4, NARD ×3 — including the previously image-verified 8ml×10 sachet-sampler entry, andSons, Cheongdam Style) plus the `SKU-137196` catch-all entry itself (its own fields are correct; only the map-row routing into it was wrong). 9 taxonomy entries had content fixed (`_meta` reset to unreviewed); 2 new entries minted; 26 entries were genuinely Tier-1/Tier-2-judged and found already correct (12 promoted to `confident` on second agreeing review, 14 landed on `unconfident` as a first review). | QA gates re-verified live post-write: G1 dual-mapped 0, G2 HUMAN+LLM coexistence 0, placeholder-leak 0, G5 provenance 0, structured-fields-missing 0%. SKU block `SKU-138128`–`SKU-138327` left `ACTIVE` (2/200 used, reservation not a quota). Note: the two most-recent prior sessions' claimed blocks (`SKU-137196`–`SKU-137395`, `SKU-133846`–`134045`) now show `FAILED_QA` in the registry, contradicting this file's own record of them passing gates and being left `ACTIVE` — some intervening session/process changed their status; not investigated further this session since it didn't block this session's own claim or gates, flagged here for awareness. Universe refresh **not run this session** — explicitly out of scope per instructions. Residual precision debt for a future session, unchanged from the prior entry's ranking: (1) the `SKU-002xxx` brand-catch-all family, now reconfirmed twice as the single highest-GMV precision-debt item; (2) the 9 brand-level "Assorted Shampoo" catch-alls; (3) the Shiseido Sublimic `[Buy 2 Get 1]` family and `KUNDAL Nature Shampoo`'s bundle-vs-single fan-out, both still unfixed pending image confirmation; (4) 6 low/no-GMV `null_size` entries (Seven Green, As I Am, Ruyi, Miriam Quevedo, JUNGSAEMMOOL, Inebrya) needing a dedicated product-specific image read. The ~2,314 Tier-1-clean, non-sampled rows remain exactly as eligible as before — no fabricated review. |
| 2026-07-23 | Automated Taxonomy Review #4 (targeted_qa_fix auto-discovery) | Worklist: 2,364 never-reviewed/unconfident entries. Tier 1 SQL sweep flagged 12 rows: `wrong_field_order` 2 (both `BRD-UNBRANDED` false positives, same class confirmed by reviews #2/#3), `excess_content` 3 (Daeng Gi Meori same-brand sets stating real per-component sizes — `SET_WITH_SIZE` precedent; 2 verified correct, 1 — `SKU-069085` — found genuinely inconsistent with its sibling entries `SKU-069087`/`091` (missing `is_multi_variant=TRUE` despite being the same shampoo+treatment set pattern) and fixed), `canonical_field_mismatch` 1 (KUNDAL `Duo Set` phrasing in `product_line` not restated in `canonical_name` — benign, `x2` already conveys it, verified correct), `null_size` 6 — re-investigated independently rather than trusting review #3's write-up: 2 resolved this session (Inebrya `SKU-129623`, image showed two real size variants 300ml/1000ml sold as buyer-selectable models — `is_multi_size` was wrongly `FALSE`, corrected to `TRUE`; Ruyi `SKU-129633`, image showed "买1送1 1000ml" — two identical 1000ml bottles, buy-1-get-1 same product, not a size range — `pack_count` 1→2 and `size` NULL→1000ml), 4 reconfirmed genuinely unresolvable (As I Am, Miriam Quevedo — cover/catalog images only, not the specific product; Seven Green — bar soap, no weight in any image; JUNGSAEMMOOL — no size signal anywhere). STEP 2b promo sweep (14,580 model rows / 176 distinct entries) folded into the GMV-prioritized Tier 2 sample per design. Tier 2 GMV-ranked sample: rather than deferring the recurring high-GMV catch-all stub family a 4th time, checked `COUNT(DISTINCT product_id)` per catch-all first — found 12 of them were low-count/high-GMV (Fayre Beauty 11 products/$73.9K, MadeToBloom 6+2/$63K+$19K, Lassie Manna 1/$60K, Nizoral 18/$27.5K, Growus 17/$25.5K, JML 4/$25.8K, Har 10/$23K, Suu Balm 21/$30.4K, Phytopecia 18/$47.3K, ＆honey 21/$23.5K, Goldwell 21/$26.8K), genuinely tractable within a 200-slot block unlike the Milbon-scale (748 products) monsters that legitimately need a Full Rebuild pass. Claimed block `SKU-138754`–`SKU-138953` and split all 12 via bulk-first grouped text extraction (sku_name + `raw_niq_history` spec/description, `SET_WITH_SIZE`/`is_multi_variant` convention for same-brand multi-component bundles, 2 image reads only where text was genuinely silent on size — Lassie Manna's $1.95M-GMV product turned out to be a real 300ml shampoo+conditioner set): minted 108 new taxonomy entries (151 products rerouted, all 12 old catch-alls now correctly orphaned at 0 mapped products) and backfilled `product_line` on 48 of them that would otherwise have repeated the Pass-1-era "text in canonical_name only" bug. Also found and fixed a genuine brand misassignment surfaced during the Nizoral split: product `10206986547` ("Dandron Cetrimide 5% Shampoo 200ml") had been swept into the Nizoral catch-all by a keyword-only match on "dandruff shampoo" despite being a completely different, real brand — registered `Dandron` as `BRD-SG-13511` and minted its own entry rather than leaving it misattributed. After clearing the catch-all wall, continued down the GMV ranking per the review's own "don't let the catch-all wall truncate Tier 2" reminder and found a second, previously-undetected systematic defect: a family of entries (mostly from the `SKU-126xxx`/`127xxx` top-up-session range) with stray digit fragments in `product_line`/`canonical_name` — traced to genuine dual-size sku_names (e.g. "500/1000ml", "300/600 ml") that an earlier deterministic backfill resolved to only one size, leaving the other size's leading digit stranded as text noise. Fixed 17 of these: 12 converted to `is_multi_size=TRUE` with the stray digit cleared (Pantene ×2, Shiseido Professional, Good Virtues Co., Elabore, Helen Seward ×3 — careful here, the trailing digits "5"/"6"/"1" on the Helen Seward MEDITER line are real formula numbers from Helen Seward's own naming convention, not parsing noise, so `product_line` was left untouched and only the false single-size resolution was fixed), 5 were pure noise-stripping fixes unrelated to dual-size (MODAMODA's stray "KOREA 🇰🇷" flag-prefix, Sunsilk's trailing size-range digit, DAMOAE's fabricated "Exp: 03" fragment absent from the actual sku_name, Moroccanoil's similar fabricated "*Expiry 3", Olivia's mid-string "Expiry" noise, NC PETRA's "x Expiry Year 2026" noise, LUX's empty "( )" placeholder), plus one real D2 miss found along the way: Dove `SKU-127559`'s `product_line` had collapsed to the pack-count descriptor "Value Pack of 6" instead of the actual product name — corrected to "Ultra Care Shampoo", `pack_count` 1→6 (sku_name states "6 / 12" as a buyer choice; anchored to the conservative lower value absent an image to confirm which). 2 candidates (La CASTA's numbered soap-bar variants "82"/"35", a `Regan`/`Audace` brand-identity ambiguity) were left untouched — insufficient signal to resolve safely at their GMV scale. Investigated a possible G3-style type-conflict flag (conditioner-only sku_names mapped to shampoo entries, Tsubaki/Joico) but both turned out to be genuine shampoo/conditioner buyer-choice listings (sku_name varies by scrape month depending on which variant was recorded), not scope violations — no fix needed. 22 existing entries had content fixed (`_meta` reset to unreviewed); 108 new entries minted; 7 entries verified correct via genuine Tier 2 judgment (marked confident/unconfident per agreement with prior verdict). | QA gates re-verified live post-write: G1 dual-mapped 0, G2 HUMAN+LLM coexistence 0, placeholder-leak 0, G5 provenance 0, structured-fields-missing 0%. SKU block `SKU-138754`–`SKU-138953` left `ACTIVE` (108/200 used, reservation not a quota). Confidence distribution across the category's 2,438 distinct mapped entries: 2,282 unreviewed, 125 unconfident, 31 confident. Universe refresh **not run this session** — explicitly out of scope per instructions. Residual precision debt for a future session: (1) the remaining large catch-alls that didn't fit a 200-slot block this session — Milbon (748 products, $76.2K), UNOVE (39, $65.8K), Dr.ville (34, $64.6K), Tsubaki (125, $60.9K), Off&relax (103, $30K), sukin — these need a proper Full-Rebuild-scale bulk pass, not a targeted-fix block; (2) the wider pre-existing `SKU-002xxx` brand-catch-all family flagged by reviews #2/#3, still unaddressed; (3) La CASTA's 2 unresolved product-code-vs-size ambiguities and the `Regan`/`Audace` brand question; (4) the Shiseido Sublimic `[Buy 2 Get 1]` family and `KUNDAL Nature Shampoo`'s bundle-vs-single fan-out, still pending image confirmation. The ~2,282 unreviewed rows remain exactly as eligible as before — Tier 1's cheap sweep re-catches mechanical defects among them for free next run. |
| 2026-07-23 | Automated Taxonomy Review #2 (targeted_qa_fix auto-discovery) | Worklist: 2,387 never-reviewed/unconfident entries. Tier 1 SQL sweep flagged: `duplicate_brand` 13 (6 genuine — marketing/promo junk or bilingual-duplicate text: `SG INSTOCK+FREE DELIVERY+🎁`, `(ideal for use with REGAINE)`, Chinese-text repeat on MBA/ATREUS; 7 false positives — legitimate same-brand-set naming (`Na`/`Plantur 39`), a real sub-brand name (`Sisley Hair Rituel by Sisley`), and `Phyto`/`Phytocyane` substring matches), `wrong_field_order` 15 (all false positives — genuinely unbranded `BRD-UNBRANDED` products with no proper-noun brand anywhere in `sku_name`, same false-positive class the prior session's 15 confirmed), `excess_content`/`canonical_field_mismatch` 4/9 (5 real fixes: 3 Himalaya entries had a stray literal `\|` pipe character leaked into `product_line` from bad upstream parsing; 2 KUNDAL entries had promo text `Buy 1 Get 1!!` embedded directly in `product_line`; NARD/andSons/Small Pukong had parsing-artifact junk (`, / -`, `(3x )`) in `product_line`; Small Pukong's `is_multi_size=TRUE` was stale now that its size is populated — corrected to `FALSE`; remaining flags were legitimate same-brand bundle-set names with multiple stated component sizes, not a defect), `null_size` 31 (6 resolved to a real size via `raw_niq_history.product_specifications`' `Volume`/`Weight` fields; 13 set `is_multi_size=TRUE` — genuine multi-component "Set" listings with no single size, confirmed via spec/description cross-check per §11; 12 left NULL — single low-GMV products (combined <$8K) with zero size signal in `sku_name`, `product_specifications`, or `product_description`, honestly `UNRESOLVED` pending a future image-read pass, not force-flagged), `garbage_brand` 2 (brand `"107"` — confirmed genuine via the `[Brand]`-bracket sku_name convention used consistently across this dataset, distinct from `merchant_name`; no fix needed, same finding as the prior session's `"107"`/`"1o7"` entries). A brand-spelling variant the Tier 1 regex missed entirely (`brand_dict.canonical_name`="Daeng Gi Meori" vs. duplicated in-text "Daeng Gi Meo Ri", different spacing) was caught by manual inspection and fixed in bulk across the whole family — 51 entries had the duplicate brand text stripped from both `canonical_name` and `product_line` in one `REGEXP_REPLACE` pass. STEP 2b promo-language sweep (18,368 model-grain rows / 188 distinct entries) filtered to high-confidence explicit multiplier patterns (`buy N get M`, `xN`, `[Bundle of N]`) and cross-checked against per-entry product-fan-out and GMV before fixing, to avoid misapplying a multiplier from one mapped product onto a shared catch-all: fixed 6 Rausch DUO entries (`2x200ml` stated in source, `pack_count` stuck at 1), 2 NaturVital shampoo+conditioner sets (garbled `"( 2 x )"` canonical text plus missed `pack_count=2`), 1 Dr. Groot entry (reseller store name `"HEBELOFT"` — Hebeloft Official Store — leaked into `product_line`/`canonical_name`, a direct §11 violation, plus missed `pack_count=2` from `"Bundle of 2"`), and 3 Daeng Gi Meori entries (`pack_count` 1→2 for explicit `"Shampoo x2pcs"` bundles). Large `[Buy 2 Get 1]` Shiseido Sublimic family (19 entries, ~$40K combined GMV) and the `KUNDAL Nature Shampoo` catch-all's mixed bundle-vs-single fan-out (~$2.8K of its $145.9K GMV from 6 low-GMV `[Bundle of 3]` listings) were reviewed but left unchanged — ambiguous whether the promo language describes the physical unit count or a separate marketing tier, and not worth misapplying a guess at this GMV scale; flagged here for a future image-read pass. One genuine out-of-scope catch: `SKU-127179` ("Salon Professional Bottle Pump for Salon Size Shampoo 1000ml") was a dispenser-pump *accessory*, not a shampoo product — deleted (map row + taxonomy row; single-product entry, no fan-out risk) per `llm-extraction-rules.md` §5 rather than fixed in place. One genuine brand misattribution: `SKU-126766`, brand resolved to a fake entity `"Luxe"` (`BRD-TH-03142`), but the underlying product (`sku_name` `"[MILBON] Grand Linkage Shampoo, Treatment Silky Luxe, Willow Luxe, Velour Luxe 200ml, 500ml, 1.8L"`) is genuinely Milbon (`BRD-GLOBAL-00357`) with "Luxe" a texture-variant suffix in Milbon's own line naming, not the brand — reassigned `brand_id`, and since the listing is genuinely multi-size (3 sizes) and multi-variant (3 textures) with no single value for either, set `size=NULL`, `is_multi_size=TRUE`, `is_multi_variant=TRUE` and rewrote `canonical_name`/`product_line`/`variant` with real descriptive text (not "Multiple Sizes/Variants"). The largest single finding this session, surfaced via the GMV-prioritized Tier 2 sample: `SKU-002390`, a legacy `HUMAN` keyword-seed catch-all under a fabricated brand entity `"ANTI"` (`BRD-SG-08955` — never a real brand; created by a crude keyword scan matching the substring "anti" inside phrases like "Anti Hair Loss"/"Anti-Pelliculaire"), covering 44 completely unrelated real products (Kérastase, L'Oréal, Selsun, Head & Shoulders, Dove, Pantene, EHD, Fortro, plus many generic/unbranded reseller listings) worth a combined SGD ~15.82M GMV, ~95% of it concentrated in one dominant $15.05M generic/unbranded listing with zero extractable brand signal. Claimed a fresh 200-slot block (`SKU-137196`–`SKU-137395`) and remediated by GMV impact: registered 2 new brands (`DIVULGE` `BRD-SG-13509`, `VIKADA` `BRD-SG-13510`, both stated clearly in their own `sku_name` text, previously unregistered) with new taxonomy entries; created one new `BRD-UNBRANDED` catch-all (`SKU-137196`, real descriptive name, `is_multi_variant=TRUE`, `is_multi_size=TRUE`) for the 30 genuinely brand-less generic/marketing-spam listings including the dominant $15.05M product; created one new Selsun entry (already a registered real brand, `BRD-SG-00192`, with no existing catch-all in this category); rerouted the 4 Kérastase, 3 L'Oréal Paris, and 2 Head & Shoulders products to their existing category catch-alls (correct brand now, product-line granularity still deferred — same debt class as those catch-alls' own pre-existing Tier-C status), and 2 products (EHD, Fortro) to their own already-correct existing entries found via this session's null_size work. The old `SKU-002390` entry is now orphaned (zero mapped products) — left in place per the never-delete-existing-rows rule; only genuinely out-of-scope single-product entries (like `SKU-127179` above) were deleted. Beyond this one worked example, the wider `SKU-002xxx` keyword-seed brand-catch-all family (at least 12 more sampled, individually $753K–$2.1M GMV, all bare `"{Brand} Shampoo"` D1 Tier-C stubs with `is_multi_size=TRUE` already set from a prior session's remediation) remains unaddressed — real per-product-line splitting at this scale needs a proper bulk-text-matching pass, not one-by-one manual review; flagged here as the single highest-GMV precision-debt item in the category for a future dedicated session. 94 distinct taxonomy entries had content fixed (`_meta` reset to unreviewed); 4 new entries minted; 24 entries were genuinely Tier-1/Tier-2-judged and found already correct (marked `confident`/`unconfident` per agreement with prior verdict). | QA gates re-verified live post-write: G1 dual-mapped 0, G2 HUMAN+LLM coexistence 0, placeholder-leak 0, G5 provenance 0. SKU block `SKU-137196`–`SKU-137395` left `ACTIVE` (4/200 used, reservation not a quota). Confidence distribution left behind: 94 entries reset to unreviewed (re-checked fresh next session), 24 marked confident/unconfident, remainder of the ~2,387 distinct entries in this category unchanged from prior state. Universe refresh **not run this session** — explicitly out of scope per instructions. Residual precision debt for a future session, ranked by GMV: (1) the wider `SKU-002xxx` keyword-seed brand-catch-all family — bare `"{Brand} Shampoo"` D1 Tier-C stubs, individually $753K–$2.1M GMV, needs a bulk-text-matching split pass, not one-by-one review; (2) the Shiseido Sublimic `[Buy 2 Get 1]` family (19 entries, ~$40K) and `KUNDAL Nature Shampoo`'s bundle-vs-single fan-out (~$2.8K), both reviewed but left unfixed pending image confirmation of what the promo language actually describes; (3) the 12 low-GMV null_size entries with no size signal in any text field, candidates for a future image-read pass. The ~2,269 Tier-1-clean rows not sampled in this session's bounded Tier 2 GMV pass remain exactly as eligible as before (per design, no fabricated review) — Tier 1's cheap sweep will re-catch any mechanical defects among them for free next run. |
| 2026-07-23 | Top-up coverage session #3 (month 2026-06) | Wrapper's pre-check claimed 1,332 still-NULL rows; live re-query in STEP 0 confirmed this was accurate (not stale): 1,332 model-grain rows / 535 distinct products / SGD 495,429.94 GMV uncovered — a genuinely new gap, not the same 111/6 products the prior two same-day top-up sessions left, despite those having closed the gap down to 6 intentional NULLs earlier the same day. Root cause not fully diagnosed (new listings accreting into the live month-2026-06 slice is the most likely explanation, given `master_clean_niq` is a live warehouse table, not a frozen snapshot); verified before proceeding that this wasn't a stale-precheck false alarm by confirming prior sessions' work actually landed live (3,997 `source='LLM'` map rows present, claimed SKU blocks `SKU-126556`–`129755` etc. all had real taxonomy rows in range — the QA History narrative matched live DB state). Attempted automated bulk brand+size+type reuse-matching against the category's ~2,020 existing taxonomy entries for the worklist's 46 distinct brands first (per the reuse-before-mint mandate) — an initial loose token-overlap matcher produced unsafe false matches (e.g. routing a Sebamed anti-dandruff shampoo to an unrelated "Sebamed Baby Bubble Bath" entry, shampoo-to-conditioner mismatches) and was discarded; a stricter type-consistency + near-full product-line-token-containment version then found effectively zero safe matches, confirming these 535 are genuinely new listings (not re-listings of already-catalogued products) rather than a matcher-tuning problem — consistent with this category's own documented SKU fragmentation (personal-care listings rarely repeat verbatim across resellers). Pivoted to grouping+minting: deterministic text extraction (brand-name stripping incl. accent/spacing variants for Kérastase and Daeng Gi Meo Ri, size via `sku_name` regex per §2's priority chain, pack via `xN`/`Bundle of N`/`buy N get M` per §1, merchant-name-leakage stripped per §11) collapsed 530 in-scope products (5 confirmed OOS excluded — see below) into 503 taxonomy groups (27 literal-duplicate collapses, e.g. 4 separate Aveda "Rosemary Mint Purifying Shampoo 1000ml" resellers → one entry). `raw_niq_history.shopee_sg_shampoo` (documented in ARCHITECTURE.md/llm-extraction-rules.md as the spec/description fallback source, and used successfully by name in the prior session's write-up) does not actually exist in this project (`raw_niq_history_filter`/`raw_niq_history_region` datasets exist instead, neither containing a `shopee_sg_shampoo` table) — a real doc-vs-live-schema drift, flagged here rather than worked around; the 126 sku_name-silent-on-size rows therefore had no spec/description fallback available, so 2 of the highest-GMV cases (SMiB $8,085, Sebamed Anti-Dandruff $6,523) got a direct image read instead (SMiB resolved to 200ml from the on-bottle label; Sebamed's marketing banner had no visible size text, left unresolved) and the remaining zero-size rows were left honestly `size=NULL` rather than guessed. Brand recovery for 17 `BRD-UNDEFINED`/blank-brand worklist rows: 1 confirmed OOS (Guardian dressing pad — leave NULL); 5 collapsed into one `BRD-UNBRANDED` "Herbal Anti-Hair Loss Shampoo 400ml" entry (near-identical generic listing from 5 different resellers, no proper-noun brand in any text); 6 recovered to already-registered brands readable directly from `sku_name` (Kérastase ×2, L'Oreal Professionnel via "Serioxyl", DIVULGE, LABOH via "LABO-H", Ryo, Nizoral, Craft, MiseEnScene via "Perfect Serum" — the last 4 found by also correcting 6 further rows that `product_brand_map` had mis-bucketed under a scan-artifact brand `"Care"` (`BRD-GLOBAL-00623`, matching generic "Hair Care" text — same failure class as the previously-documented fake `"ANTI"` brand bucket); 2 registered as new brands, real proper-noun names legible in `sku_name` with no existing `brand_dict` row (`Flowery` `BRD-SG-13512`, `Nature Co` `BRD-SG-13513`); remaining rows resolved to `BRD-UNBRANDED` where no proper-noun brand was identifiable, or given a size/pack fix where text allowed it despite no brand (e.g. a Korean red-ginseng listing, GMV $656, previously flagged by a same-day session as brand-unresolvable — text this session did yield a real pack/size read of 730ml×2 that the prior pass missed, even though the brand itself remains genuinely unbranded-in-text). Confirmed 5 permanently out-of-scope listings recur verbatim from the prior two same-day top-up sessions' findings (Guardian wound-dressing pad, a board game, a lice-treatment lotion, 2 raw SLSA/DLS bath-bomb chemical inputs from `futurefood.sg`) — these will keep reappearing in every future live worklist query since they are correctly never mapped; left NULL again, consistent with precedent, not a regression. Claimed a 1,332-slot block (`SKU-139711`–`SKU-141042`) sized to the wrapper's stated gap; used 503 of 1,332 (~38%) for taxonomy entries, 530 map rows — left `ACTIVE` (reservation, not a quota, per established precedent). Live re-query after all writes confirmed the worklist gap closed from 1,332/535/$495,429.94 to exactly 7 rows / 5 products / $1,269.89 — the 5 confirmed-OOS products above, byte-for-byte matching the pre-write prediction. | QA gates re-verified live post-write: G1 dual-mapped 0 (LLM-scoped), G2 HUMAN+LLM coexistence 0 (category-wide, unscoped), G5 provenance 0, structured-fields-missing 0% (this session's 503 new entries, non-multi-size denominator), placeholder-leak 0 for this session's new entries **and** 0 category-wide — the previously-documented 2,622-row/182-entry "Multiple Variants" placeholder debt from the 2026-07-15 Pass 1/2 run has been fully resolved by an intervening session since the last QA History entry describing it as open (not remediated by this session; flagging the state change here since no entry recorded when it closed). New: `raw_niq_history.shopee_sg_shampoo` does not exist in this project despite being documented as a live fallback data source in `ARCHITECTURE.md` §"raw_niq_history", `docs/llm-extraction-rules.md` §2, and referenced by name in this file's own prior session write-up — worth a doc fix or a check of whether a differently-named successor dataset (`raw_niq_history_filter`/`raw_niq_history_region`, neither of which has this table) was meant. SKU block `SKU-139711`–`SKU-141042` left `ACTIVE` (503/1,332 used). 2 new brands registered (`BRD-SG-13512` Flowery, `BRD-SG-13513` Nature Co). Universe refresh **not run this session** — explicitly out of scope per instructions, left for independent QA verification first. Residual debt for a future session: (1) the `"Care"` (`BRD-GLOBAL-00623`) brand bucket in `product_brand_map` still contains other mis-scanned products beyond the 6 fixed here (a "hair care" text-match artifact, same class as the previously-found fake `"ANTI"` brand) — only this session's worklist-touched rows were corrected, the bucket itself was not swept; (2) one L'Oreal Paris entry (`SKU-...` "Mix N Match L oreal Sulfate Free...") retains a minor brand-text duplication the bulk alias-stripper didn't catch (an unusual "L oreal" spacing variant, 1 row, cosmetic only); (3) the 124 zero-size worklist rows beyond the 2 image-read this session remain honestly `size=NULL` (no `raw_niq_history` fallback available, and per-product image reads were out of scope for a coverage-first bulk pass) — candidates for a future `targeted_qa_fix.sh` pass if their combined GMV proves material. |
| 2026-07-23 | Top-up coverage session #4 (month 2026-06) | Wrapper's pre-check claimed 7 still-NULL rows; live re-query in STEP 0 (not trusted as-is, per instructions) confirmed byte-for-byte the same 7 model-grain rows / 5 distinct products / SGD 1,169.89 GMV as top-up session #3's closing state, not a new gap: `46904399352` (Magnetic Battle Chess board game, 3 model rows, DADA.SG Store), `4337623135` (Sodium Lauryl Sulfoacetate/SLSA raw surfactant powder, futurefood.sg), `3871042313` (Disodium Lauryl Sulfosuccinate/DLS raw surfactant powder, futurefood.sg), `28577794294` (Guardian Non-Adherent Dressing Pad, Guardian SG Official Store, Shopee Mall), `8017078811` (Neutralice Advance Lice Care Lotion, Health Euphoria). All 5 are OOS from `sku_name` text alone — a board game, two raw cosmetic-ingredient chemical inputs (not finished shampoo products), a wound-dressing pad, and a lice-treatment lotion — no ambiguity requiring an image read. These are the exact same 5 products flagged as "permanently out-of-scope, will keep reappearing in every future live worklist query" by top-up sessions #2 and #3 earlier the same day; their recurrence here is expected steady-state, not a regression or a missed extraction. No SKU block claimed (nothing in-scope to map, no writes needed) — claiming one would have burned a block for zero rows. | No writes made. QA gates re-verified live (no `--skip-coexistence`, per instructions since this category already shipped): G1 dual-mapped 0, G2 HUMAN+LLM coexistence 0, placeholder-leak 0, G5 provenance 0, structured-fields-missing 0%. Universe refresh not applicable — no changes to refresh. Category coverage remains at its prior steady-state (5 permanently-OOS products excluded by design); no residual action needed unless one of these 5 products' classification is ever reconsidered. |
| 2026-07-23 | Automated Taxonomy Review #5 (targeted_qa_fix auto-discovery) | Worklist: 2,910 never-reviewed/unconfident entries (grown since review #4 — several intervening top-up sessions minted new entries, e.g. `SKU-139711`–`141042`, none yet reviewed). STEP 1B pre-check gates: `canonical_name fields` 4 and `garbled brand text` 2 both cross-referenced against the live worklist and confirmed **entirely pre-existing**, outside this session's incremental scope (`SKU-069004/069303/127658/127136`, already `confident` from prior reviews) — untouched, as expected. Tier 1 SQL sweep flagged 170 rows: `duplicate_brand` 3, `wrong_field_order` 7 (all false positives — `BRD-UNBRANDED` no-brand-text class), `excess_content` 18 (false positives — `SET_WITH_SIZE` same-brand bundles stating real per-component sizes), `canonical_field_mismatch` 2 (`SKU-138810` genuine — bundle text read "2 x 480ml" instead of the established "480ml x2" order, reordered; `SKU-138812` false positive, bundle already states real per-component sizes), `null_size` 163 — by far the dominant class this session, traced to several intervening top-up sessions (`SKU-138xxx`–`140xxx` range) minting catch-all/bundle-style entries without populating `is_multi_size`. Split by structure rather than treated as one class: 121 genuine multi-item catch-alls (`Series`/`Full Range`/`Set`/`Bundle`/`Collection` naming or ≥2 distinct product-type words, e.g. "＆honey Full Range Set (Shampoo, Treatment, Conditioner, Mask, Oil)") further split by whether a real size was already embedded in the text — 11 had one (e.g. "H'ar Dr.Ran Shampoo 500ml + Dr.Mori N Tonic 100ml Set" → `size='500ml'`, the primary/first-component convention already established for `SET_WITH_SIZE` entries) and got that size extracted mechanically via regex; the remaining 115 had no size signal anywhere in the text and got `is_multi_size=TRUE`+`is_multi_variant=TRUE` (matching the `SKU-126766` Milbon-Luxe precedent) rather than a guessed value. 3 more had explicit multi-size/format text not caught by the structural classifier ("250ml/1000ml", "Comes in 2 Sizes", "Bottle/Refill") — `is_multi_size=TRUE`. For the remaining 34 genuinely single-product entries with no size in `sku_name`: confirmed `raw_niq_history.shopee_sg_shampoo` still does not exist (per top-up #3's finding, re-verified via `bq ls` — no `raw_niq_history` dataset at all now), so resolution was `sku_name` → image only. GMV-prioritized the top 15 (>$10K) for a direct image read (`curl` the `master_clean_niq.image` CDN URL, then `Read` the local file): 7 resolved to a real printed size (Coco & Eve 288ml ×2, Bee Choo Origin 420ml/250ml/250ml, Olaplex 250ml, Aveda Shampure 1L) plus a NaturVital "5 Colors" entry resolved to 300ml with `is_multi_variant=TRUE` (all 5 color variants share one bottle size, confirmed on-label) and a Paul Mitchell "x Gallon" listing resolved via `sku_name` text alone (`size='1 Gallon'`, the unit the regex extraction had never handled). The other 7 of the 15 were left honestly unresolved — generic brand-lineup/demo-shot images (Small Pukong ×2, Bee Choo Rebalance, Phytopecia P1P, Sebamed ×2) that don't confirm a size for the *specific* listing per the cover-image caveat, not a size miss. The remaining ~18 lower-GMV singles (plus 3 already-adjudicated-unresolvable from review #4 — As I Am, Miriam Quevedo, JUNGSAEMMOOL, not re-read) were left untouched, no `_meta` write, exactly as eligible next run. STEP 2b promo sweep (18,326 model-grain rows / 206 distinct entries, mostly generic `x\d`/`free` noise) folded into Tier 2 per design; filtering to high-confidence explicit patterns (`Bundle of N`, `Buy N Get M`) surfaced a real, previously uncaught D5 family: 19 entries with `[Bundle of N]` stated plainly in `sku_name` but `pack_count` stuck at 1 (Dr Groot, Plantur 39 ×3, AFC Shokaigan, Mise en scene, Cloversoft, SAFI ×1, Diane ×2, Organist ×2, LG Organist, Eversoft ×2, Pregaine, Lucido-L, KUNDAL, OGX, Palmolive — GMV $108K down to $5.8K), fixed `pack_count` 1→2 or 1→3 and appended `x{N}` to `canonical_name` for all 19; incidentally also cleaned up defects riding along in the same text: 2 literal duplicate-brand-text repeats ("Dr. Groot Dr Groot", "MiseEnScene Mise en scene"), 3 stray empty `"( )"` parenthetical placeholders (Plantur 39 family, Cloversoft — same noise class as review #4's LUX finding), 1 broken-bracket leak ("KUNDAL Bundle of 3]" — truncated promo text left a stray closing bracket in `canonical_name`), 1 garbled brand fragment ("REGAINE P" → the real product-line name "Pregaine", confirmed via the brand's own sibling `SKU-126627` "Pregaine Gel Shampoo" using the same spelling), and 1 banned-adjacent generic stub ("All Variations" replaced with the real scent list "Blue Jasmine / Grasse Rose / Night Dream Tea" pulled from a sibling `sku_name` variant, same rule as the "Multiple Sizes/Variants" ban). The previously-documented Shiseido Sublimic `[Buy 2 Get 1]` family (`SKU-069756`/`069757` among them) and the brand-level "Assorted Shampoo" catch-alls (UNOVE, Milbon, Dr.ville, Tsubaki, sukin, Off&relax) were left untouched — same established precedent, ambiguous/Full-Rebuild-scale debt, not this scenario's job. Tier 2 GMV-ranked sample (top 60 entries): found and fixed 4 more genuine defects beyond the STEP 2b family — `SOME BY MI SOMEBYMI` duplicate brand text ($197K GMV), `L'Oreal Paris L Oréal Paris` duplicate brand text with an accent-spelling variant the Tier 1 regex missed ($267K), a davines entry with `"100% Authentic Singapore Seller!"` marketing puffery leaked into `product_line`/`canonical_name` (not a literal `merchant_name` match — real seller is "TS Global Trading" — but still non-product text per the spirit of §11; also recovered real per-item sizes "1L"/"135ml" from `sku_name` that a prior session's extraction had missed entirely, correcting `is_multi_size` `TRUE`→`FALSE` with `size='1L'` now that a real value exists, $230K), and a stray `+` parsing artifact on a Grafen entry left over from a truncated GWP clause ($141K). One brand_dict correction: `BRD-SG-01009` `canonical_name` `"Har"` → `"H'ar"` — verified against 6 consistent `[H'ar Official]`-prefixed `sku_name`s in this category; the registered brand name was missing its apostrophe. 29 entries verified correct via genuine Tier 2 judgment, including reconfirming several already-documented precedents (NARD's 8ml sachet sampler, the Fayre Beauty/Grafen families, the Unbranded Herbal Anti-Hair Loss catch-all) — marked `confident`/`unconfident` per agreement with prior verdict. 165 distinct taxonomy entries had content fixed (`_meta` reset to unreviewed); 0 new entries minted (all fixes were in-place edits to existing rows, no SKU block claimed). | QA gates re-verified live post-write: G1 dual-mapped 0, G2 HUMAN+LLM coexistence 0, placeholder-leak 0, structured-fields-missing 0%, `canonical_name fields` 4 and `garbled brand text` 2 both confirmed unchanged and pre-existing (same 4 taxonomy_ids as STEP 1B's pre-check, outside this session's worklist). Universe refresh **not run this session** — explicitly out of scope per instructions. Residual precision debt for a future session, unchanged in ranking from review #4: (1) the brand-level "Assorted Shampoo" catch-alls (Milbon 748 products, UNOVE, Dr.ville, Tsubaki, sukin, Off&relax) still need a Full-Rebuild-scale bulk pass; (2) the Shiseido Sublimic `[Buy 2 Get 1]` family, still pending image confirmation of what the promo language actually describes; (3) ~18 lower-GMV `null_size` singles with no size signal in `sku_name` and no `raw_niq_history` fallback available, candidates for a future image-read pass if their combined GMV proves material; (4) a possible near-duplicate pair, `SKU-140133`/`SKU-140138`, both "Sebamed ... Anti-Dandruff Shampoo" — noticed in passing during this session's GMV sample but not investigated or merged (out of scope — this scenario never deletes/merges rows). The ~2,745 Tier-1-clean, non-sampled rows remain exactly as eligible as before — no fabricated review. |

| 2026-07-23 17:51 UTC | Automated review session (auto-discovery) | Worklist: 2,888 never-reviewed/unconfident entries. STEP 1B pre-check gates (canonical_name fields 4, garbled brand text 2) resolved first: 3 canonical_name-fields rows (SKU-138812 Suu Balm mixed-bundle x2, SKU-069004 Amos Professional SET_WITH_SIZE, SKU-069303 KUNDAL Duo Set) and the 'garbled brand text' brand '107' (BRD-SG-04404) were each re-confirmed false positives for the 2nd-3rd+ time against prior sessions' documented precedent -> closed permanently via qa_gate_exceptions. The 4th flagged row, SKU-140070 (Paul Mitchell), was a genuine never-before-caught defect: a stray 'x Gallon' regex-parsing artifact left in product_line -> fixed. Tier 1 SQL sweep on the incremental worklist flagged 45 rows (wrong_field_order 2, excess_content 18, canonical_field_mismatch 1, null_size 25). Investigating SKU-127717's wrong_field_order flag (missing apostrophe vs the corrected brand_dict 'H'ar') led to discovering a much larger, previously-undetected defect: the entire 'REGAINE'-branded family (BRD-SG-00937, 7 entries) was actually the Kenvue-listed 'Pregaine' product -- brand_dict itself had the wrong canonical_name, and 6 of 7 sibling entries carried a stray truncated 'REGAINE P' stub in canonical_name/product_line (the leftover 'P' from 'Pregaine' after incorrectly stripping 'REGAINE' as a brand prefix). Fixed brand_dict + all 6 siblings. The 18 excess_content flags were all the established SET_WITH_SIZE same-brand-multi-component-set false-positive pattern (Suu Balm, ＆honey, Phytopecia, Goldwell Kerasilk, Growus, H'ar, Daeng Gi Meori) except SKU-140203 (davines), which was missing is_multi_variant=TRUE like the SKU-069085 precedent from review #4 -- fixed. Of 25 null_size flags: SKU-139750 (Bee Choo Origin) had size '250ml' stated plainly in sku_name, missed by extraction -- fixed. Image-read the top-GMV remainder: 2 Small Pukong entries and 1 Bee Choo Origin Rebalance ($71,963/$39,729/$58,523) confirmed still unresolvable (marketing-collage images, no legible size) -- consistent with review #5 precedent, left honestly NULL. Investigating SKU-139815 (Hair+ brand, $113,530.98 GMV, null_size) led to the single largest finding this session: brand 'Hair+' (BRD-SG-04456, 54 taxonomy entries) is a fake catch-all bucket -- same failure class as the previously-resolved 'ANTI' and 'Care' fake brands -- mixing dozens of unrelated real brands (L'Oreal, Kerastase, Mise en Scene, OWAY, Somang, Craft, GALTISCOPIO, etc., confirmed via image read showing 'PHYTOPECIA' printed on-package, no 'Hair+' anywhere). Fixed the 3 highest-GMV Hair+-bucket entries this session: SKU-139815 and SKU-139813 (both genuine Phytopecia P1P multi-item listings, $113,530.98 + $224,657.93 GMV) reassigned to the real Phytopecia brand_id with honest multi-size/multi-variant naming; SKU-139820 ($26,233.90 GMV) was a genuine 'SHOU.PIN' (首品) brand, confirmed via image, registered as new brand BRD-SG-13522 and reassigned. STEP 2b promo/pack-count sweep (23,627 model rows / 273 distinct entries) filtered to high-confidence explicit patterns; the large majority (Shiseido Sublimic [Buy 2 Get 1] family, brand-level Assorted-Shampoo catch-alls, Herbal Essences/Pantene/Schwarzkopf/Dove '[Bundle of N] Shampoo+Conditioner' sets) were reviewed and correctly left unchanged -- these describe N *different* bundled items, not N units of one product, matching precedent already established across reviews #2/#4/#5. Two genuine same-product multiplier misses found via explicit '(NxSize)' parenthetical evidence: SKU-069275 KUNDAL ('(3x500ml)', pack_count 1->3) and SKU-140075 Paul Mitchell (missing is_multi_variant flag). GMV-prioritized Tier 2 sample (~55 top-GMV unflagged entries, cross-checked against source sku_name text): found and fixed SKU-126592 (Oribe) -- canonical_name/product_line falsely narrowed a 3-line assortment ('Gold Lust / Signature / Hair Alchemy') down to just 'Gold Lust', $161,381.72 GMV; and SKU-126698 (Dr. Groot) -- two structurally different products (a plain 3-item set and a genuine [Bundle of 2] of that set) had been merged onto one pack_count=2 entry, incorrectly overstating the plain listing's pack_count -- split via a newly minted SKU-159627 (pack_count=1) and rerouted the non-bundle product to it. Remaining ~55 sampled entries verified genuinely correct via sku_name cross-check (Dr.FORHAIR x4, NARD x4, Grafen x3, Klorane, Nizoral, Ryo, Kerastase, Sebamed, KUNDAL, Diane, etc.) or via established multi-session precedent (Unbranded Herbal catch-all, brand-level Assorted-Shampoo catch-alls left untouched by design). | 17 existing taxonomy entries had content fixed (_meta reset to unreviewed); 1 new entry minted (SKU-159627, Dr. Groot split); 2 new brands touched (BRD-SG-13522 SHOU.PIN registered; BRD-SG-00937 renamed REGAINE->Pregaine); 4 qa_gate_exceptions inserted (3 canonical_name-fields SET_WITH_SIZE/Duo-Set false positives, 1 garbled-brand-text numeral-brand false positive); 59 entries verified genuinely correct via real Tier 2 judgment (_meta updated confident/unconfident per agreement with prior verdict). QA gates re-verified live post-write via independent script/qa_report.sh: all 9 checks pass (dual-mapped 0, HUMAN+LLM coexistence 0, placeholder-leak 0, structured-fields-NULL% 0%, duplicate product_id 0, duplicate product+taxon 0, 'all variant/size' name 0, canonical_name fields 0, garbled brand text 0). SKU block SKU-159627-SKU-159826 left ACTIVE (1/200 used, reservation not a quota). Confidence distribution across the category's 2,942 distinct entries: 2,325 never reviewed, 443 fixed-pending-recheck, 111 unconfident, 63 confident. Universe refresh not run this session -- explicitly out of scope per instructions. Residual precision debt for a future session, ranked: (1) the wider 'Hair+' fake-brand bucket (BRD-SG-04456) -- ~51 entries still mixing dozens of unrelated real brands (L'Oreal, Kerastase, Mise en Scene, OWAY, Craft, GALTISCOPIO, ELENCE, and many Chinese-market products), only the top 3 by GMV fixed this session, needs a dedicated brand-disambiguation pass similar to the resolved 'ANTI'/'Care' buckets; (2) the established brand-level 'Assorted Shampoo' catch-alls (Milbon 748 products, UNOVE, Dr.ville, Tsubaki, sukin, Off&relax) still need a Full-Rebuild-scale bulk pass, unchanged in ranking across 4+ prior sessions; (3) the Shiseido Sublimic [Buy 2 Get 1] family, still pending image confirmation of what the promo language physically describes; (4) ~6 low-GMV null_size entries with no size signal anywhere (Seven Green, As I Am, Miriam Quevedo, JUNGSAEMMOOL, plus new low-GMV Goldwell/Aveda/Kerastase/LABOH/Olaplex/RENE FURTERER/COCO & EVE entries not pursued this session given GMV <$500 each); (5) SKU-069846 Dove Detox Nourishment Shampoo -- a bundle-vs-single fan-out issue (~$1,139 GMV) of the same class as the KUNDAL Nature Shampoo precedent, deferred consistent with established practice. |
| 2026-07-24 00:36 UTC | Automated review session (auto-discovery) | Automated Taxonomy Review #6 (targeted_qa_fix auto-discovery). Worklist: 2,879 never-reviewed/unconfident entries. Tier 1 SQL sweep flagged 37 rows: excess_content 13, canonical_field_mismatch 1, null_size 23 (all others 0). All 13 excess_content + the 1 canonical_field_mismatch (SKU-138812, already closed via a prior qa_gate_exceptions record) reconfirmed as the established SET_WITH_SIZE same-brand-multi-component-set false-positive pattern -- no fix needed. Of 23 null_size flags: direct image reads (curl+Read; raw_niq_history.shopee_sg_shampoo re-confirmed to still not exist) resolved 4 as genuinely unresolvable marketing-banner/lineup shots with no legible size anywhere (Sebamed SKU-140133/140134/140141, SHOU.PIN SKU-139820); 3 more (Small Pukong SKU-140171/140178, Bee Choo Origin SKU-139747) reconfirmed unresolvable for the 3rd-4th time, consistent with reviews #4/#5 and the immediately prior session -- left NULL, no _meta write (Tier-1-flagged-but-unfixable rows stay open rather than fabricating a reviewed state). Remaining 16 null_size rows are low-GMV (<$500 each) singles, not pursued, consistent with prior-session threshold. STEP 2b promo/pack-count sweep (17,926 model-grain rows / 201 distinct entries) folded into the GMV-prioritized Tier 2 pass per design; found and fixed 2 genuine same-product multiplier misses (SKU-126974, SKU-127073, both Legitime, sku_name states '2 x 300g' explicitly but pack_count was stuck at 1). The Shiseido Sublimic [Buy 2 Get 1] family, the brand-level Assorted-Shampoo catch-alls (UNOVE/Milbon/Dr.ville/Tsubaki/sukin/Off&relax), and Suu Balm's ambiguous 'Bundle of 2 or 3' buyer-tier language were reviewed and correctly left unchanged, matching established precedent. GMV-prioritized Tier 2 sample (top 80 worklist entries by GMV): found and fixed 6 further genuine defects -- SKU-127430 (Kerastase) had a wrong size baked in (15ml, actually the FREE gift sample's size, not the shampoo+fondant set's own size; image-confirmed the real set is 250ml Bain Riche + 200ml Fondant); SKU-139786 (Diane) had a stray-digit corruption ('(25/35 Types)' instead of the real '(3 Types)'); SKU-140075 (Paul Mitchell) had an orphaned '2x' token floating away from its '1000ml'; SKU-139876 (KUNDAL) had a stale is_multi_size=TRUE despite sku_name stating a real, consistent component size '(2x500ml)' -- corrected to size='500ml', is_multi_size=FALSE; SKU-140148 was a genuine brand misattribution -- sku_name plainly states 'Tsubaki Japan Premium Shampoo...' but was mapped to BRD-GLOBAL-00072 (Shiseido) instead of the real, already-registered Tsubaki brand BRD-SG-00050 -- reassigned; SKU-140019 (MUCOTA) had marketing puffery ('at Lowest Price') leaked into canonical_name/product_line plus a truncated 'Made' missing 'in Japan' -- both cleaned. A full-worklist sweep for a recurring stray-punctuation pattern (', / -' / ', -X' artifacts left over from mid-string size extraction) found 2 NARD entries (SKU-069514, SKU-069516, both >$120K GMV) -- cleaned. One duplicate-brand-text instance not caught by the Tier 1 regex (ratio threshold too strict for a single short repeat): SKU-140157 'Shiseido Professional Shiseido SUBLIMIC...' -- deduped. 75 further sampled entries verified genuinely correct via real Tier 2 judgment (brand consistency, marketing-text check, duplicate-brand check, D1-D5 structural completeness, cross-checked against source sku_name). 11 existing entries had content fixed (_meta reset to unreviewed); 0 new entries minted; 75 entries marked confident/unconfident per agreement with prior verdict. The ~2,793 Tier-1-clean, non-sampled rows remain exactly as eligible as before. | QA gates re-verified live post-write: G1 dual-mapped (LLM-scoped) 0, G2 HUMAN+LLM coexistence (no --skip-coexistence, category already shipped) 0, placeholder-leak 0, structured-fields-missing 0%, G5 provenance 0. No new qa_gate_exceptions inserted this session. No SKU block claimed. Confidence distribution across the category's 2,942 distinct mapped entries: 1,258 confident, 143 unconfident, 3,126 not-yet-covered rows in the never-reviewed pool remain from before this session's incremental pass. Universe refresh not run this session -- explicitly out of scope per instructions. Residual precision debt for a future session, unchanged in ranking: (1) the brand-level 'Assorted Shampoo' catch-alls (Milbon 748 products, UNOVE, Dr.ville, Tsubaki, sukin, Off&relax) still need a Full-Rebuild-scale bulk pass; (2) the wider 'Hair+' fake-brand bucket (BRD-SG-04456) -- only the top 3 by GMV fixed in a prior session; (3) the Shiseido Sublimic [Buy 2 Get 1] family, still pending image confirmation of what the promo language physically describes; (4) ~16 low-GMV null_size entries (<$500 each) with no fallback signal available; (5) Suu Balm SKU-138815's ambiguous 'Bundle of 2 or 3' buyer-tier language, left as pack_count=1, worth an image check if its ~$18.8K GMV proves material. |
| 2026-07-24 02:44 UTC | Automated review session (auto-discovery) | Worklist: 2,834 never-reviewed/unconfident entries. Tier 1 SQL sweep flagged: null_size 23, excess_content 12, canonical_field_mismatch 1, stub_leak/duplicate_brand/wrong_field_order/brand_casing_mismatch/garbage_brand all 0. All 12 excess_content + the 1 canonical_field_mismatch overlap were same-brand multi-component sets (Daeng Gi Meori, Kérastase, H'ar, Growus, Suu Balm, Phytopecia, ＆honey, davines) stating a real per-component size for each item — confirmed false positives matching the SET_WITH_SIZE precedent already documented for this category (Suu Balm/Daeng Gi Meori entries specifically re-confirm a pattern from the prior session). Of the 23 null_size rows: raw_niq_history.shopee_sg_shampoo does not exist at all in this project (not just empty for SG — a real gap vs ARCHITECTURE.md's documented size-fallback chain), so image was the only fallback signal after sku_name. Read all 23 product images directly: resolved 14 via a clearly legible on-label volume (Goldwell ×3, Aveda ×4, Kérastase ×2, Coco & Eve, Olaplex, Sebamed Everyday, Small Pukong Ginger ×2 — the second confirmed via a sibling listing's identical product name+brand rather than guessed). Left 9 honestly unresolved: 3 were generic brand-catalog/lineup shots not the specific product (As I Am, Miriam Quevedo — repeat of the exact prior-session finding for As I Am; JUNGSAEMMOOL/Bee Choo Origin/SHOU.PIN/LABO-H/RENE FURTERER/2 Sebamed SKUs had no legible volume text anywhere in the available product image. STEP 2b's promo-language pack_count sweep (pack_count=1 + promo regex hit) returned 238 rows / 190 distinct entries — far too many to review exhaustively, so combined the highest-signal explicit-multiplier subset with GMV-prioritized Tier 2 sampling rather than reviewing all 190. Image-confirmed the entire 16-entry Shiseido Sublimic '[Buy 2 Get 1]' family as a genuine false positive: the bracket is a checkout-level storewide multi-buy promo ('BUY 2 GET 1 EXCLUSIVE' banner), not a per-listing multiplier — the actual listing contents (1×250ml bottle + 1×450ml refill pouch + 1 free Wonder Shield GWP) exactly match the already-correct pack_count=1. Also confirmed false positive via image/text: KAFEN (Shampoo+Treatment 2-component set, not 2x-same-product), Diane (450ml bottle + free 330ml refill — GWP-style, different pack format not a same-size duplicate), 3 &honey option-list catch-alls ('Sachet/Sachetsx2/440ml/Refill' = N format options to choose from, not a bundled multiplier), Daeng Gi Meori Basic Treatment Bundle (x1+x1 exactly matches stated pack_count=1), and 'Na Shampoo Range' (an established generic multi-variant catch-all, same design tradeoff as this category's other 9 brand-level catch-alls). Found and confirmed via image 2 genuine pack_count misses: DEXE Black Hair Shampoo 400ml (image shows an explicit 'X2' badge + 2 identical bottles + 'Buy 1 Free 1') and NaturVital Sensitive Moisturiser Shampoo 300ml (image shows 2 identical bottles side by side under 'Buy 1 Get 1 FREE') — both were pack_count=1, should be 2. Left 3 SUBLIMIC 'Refill Duo Bundle (450ml x2)' entries (SKU-069781/069786/069788) and 2 other candidates (Ba Wang, KUNDAL 100ml x2) genuinely unresolved: the SUBLIMIC trio's own canonical_name embeds price text ('$92 [Worth $') and describes a different bundle composition than the flagged product's sku_name text, suggesting a possible wrong-entry match that needs dedicated investigation, not a quick pack_count patch; Ba Wang's image showed 3 different-formula bottles with no clear single-multiplier confirmation; KUNDAL's image showed a different scent than stated in sku_name at $0 GMV, not pursued. GMV-prioritized Tier 2 sample (top 40 worklist entries by GMV): confirmed 20 standalone entries as genuinely Tier-A complete (no defect). The remaining 20 of the top 40 (Tsubaki $337K, MUCOTA $260K, Phytopecia $225K, Oribe $161K, Goldwell Kerasilk/Dualsenses ×3, Kerasys, Fino, Oway, Paul Mitchell ×2, Shiseido Sublimic series, KUNDAL, Advante, Sunsilk, Botanist) are the same high-GMV multi-variant 'Shampoo/Conditioner/Treatment' catch-all family already flagged as the category's #1 precision-debt item by two prior sessions — reconfirmed a third time, still needing a dedicated bulk-text-matching split project, not a one-row fix. | Fixed 16 rows total: 14 null_size resolutions (size + canonical_name updated with image-confirmed volumes: SKU-138859/138860/138861 Goldwell, SKU-139732/139733/139743/139745 Aveda, SKU-139767 Coco & Eve, SKU-139929/139938 Kérastase, SKU-140052 Olaplex, SKU-140134 Sebamed, SKU-140171/140178 Small Pukong) and 2 pack_count corrections (SKU-126908 DEXE 1→2, SKU-069544 NaturVital 1→2, canonical_name appended with 'x2'). All 16 got meta_agent='CLAUDE_CODE' and _meta reset to unreviewed. Marked 55 rows reviewed-correct via the STEP 5 bulk _meta update (12 SET_WITH_SIZE false positives, 23 pack-sweep false positives, 20 GMV-sampled Tier-A confirmations) — each promoted to confident/unconfident per their prior verdict. The known multi-variant catch-all precision-debt family (~20 entries this session, $100K-$337K GMV each) was left untouched with no _meta write, per the established practice of not fabricating a review for a defect that isn't actually fixed this session — it remains open for a dedicated future bulk-reroute session. Final confidence distribution for the category: 136 confident, 89 unconfident, 2,717 unreviewed (of 2,942 distinct taxonomy entries mapped to this table). QA gates re-verified live post-write: dual-mapped (LLM) 0, HUMAN+LLM coexistence 0, placeholder-leak 0, structured-fields-missing 0%, provenance 0, duplicate product_id 0. Universe refresh not run — out of scope per instructions. |
| 2026-07-24 03:16 UTC | Automated review session (auto-discovery) | Worklist: 2,807 never-reviewed/unconfident entries. STEP 1B pre-check (canonical_name fields gate, 1 failure) resolved SKU-126908 (Dexe): product_line held raw promo/marketing text ('BUY 1 FREE 1 ❤ BLACK HAIR SHAMPOO...') instead of a real product line despite canonical_name being correct -- fixed. Investigating it surfaced a real brand-casing bug: brand_dict BRD-SG-08519 registered 'Dexe' as all-lowercase 'dexe'; image-confirmed (curl+Read on two separate Dexe product photos) the packaging consistently reads 'Dexe' (capital D only, gold script logo) -- fixed brand_dict plus SKU-126908's canonical_name casing. Tier 1 SQL sweep on the incremental worklist flagged 11 rows: stub_leak 0, duplicate_brand 0, wrong_field_order 0, brand_casing_mismatch 1 (the Dexe row above), excess_content 1 (SKU-127430 Kerastase 2-piece set, reconfirmed correct SET_WITH_SIZE false positive matching established precedent, no fix), canonical_field_mismatch 0, null_size 9, garbage_brand 0. Sibling row SKU-127473 (also brand BRD-SG-08519) was not itself Tier-1-flagged but was found opportunistically while investigating the brand casing fix and carried the identical promo-junk-in-product_line defect plus an unstated size and pack count -- image-read (curl+Read) confirmed 200ml stated on-label ('Net 200ml') and the sku_name's explicit '4 BOTTLES' phrase confirmed pack_count=4 (was 1, wrongly flagged is_multi_size=TRUE); fixed all fields. Of the 9 null_size flags: fresh image re-check on the highest-GMV one (Bee Choo Origin Rebalance, SKU-139747, $58,523) reconfirmed no legible size text anywhere in the product photo (generic 'What it does?' marketing-benefits image) -- consistent with 3-5 prior sessions' identical finding on this same set of 9 rows (JUNGSAEMMOOL, As I Am, Miriam Quevedo, Bee Choo Origin, SHOU.PIN, LABOH, RENE FURTERER, 2x Sebamed); left all 9 untouched, no _meta write, per established practice of not fabricating a review for a genuinely unresolvable defect. STEP 2b promo-language sweep (14,313 model rows / 187 distinct entries) folded into GMV-prioritized Tier 2 sampling per design rather than reviewed exhaustively. GMV-prioritized Tier 2 sample (top 100 worklist entries by GMV) found and fixed: (1) SKU-140150 (Tsubaki) mapped to Shiseido instead of the real, already-registered Tsubaki brand (BRD-SG-00050) -- same bug class as sibling SKU-140148 fixed last session -- reassigned. (2) The previously-documented 'Hair+' fake-brand bucket (BRD-SG-04456, only top-3-by-GMV fixed in a prior session, ~51 entries mixing unrelated real brands) surfaced again at high GMV in this session's sample: fixed 10 more entries with clearly identifiable, already-registered real brands (Kerastase x2 $49,339+$6,731, NATURIA x2 $14,616+$3,220, Phytopecia x4 $11,736+$4,441+$3,965 plus one already correctly named, Mise en Scene x1 $6,083, Craft x1 $8,086) -- reassigned brand_id and cleaned canonical_name/product_line on each; ~41 entries remain in the bucket, unaddressed, for a future dedicated brand-disambiguation pass. (3) SKU-069806 'Sunsilk Brand Box' -- a $97,275-GMV D1 Tier-C generic stub collapsing 4 distinct real products (a plain 625ml single, a genuine 'BUNDLE OF 3' 625ml x3, an ambiguous '[Bundle of 3/6/12]' buyer-tier selector at trivial GMV, and a distinct 'Sunsilk Collagen Brand Box' promotional set) into one non-descriptive entry -- split into 3 proper entries: reused SKU-069806 for the plain 625ml single (also absorbing the ambiguous bundle-tier selector product per the established 'N options to choose from -> pack_count=1' conservative-default convention, ~$858 combined GMV, too low-value to mint a 4th entry for), minted SKU-160887 (Sunsilk Shampoo 625ml x3) and SKU-160888 (Sunsilk Collagen Brand Box, is_multi_variant=TRUE) via a freshly claimed 200-slot block, and rerouted the 2 affected product_taxonomy_map rows. (4) 4 more marketing-puffery/parsing-artifact fixes: MissDear SKU-126624 (stripped 'BUNDLE DEAL' framing from product_line, set is_multi_variant=TRUE for its stated 14-color choice), Fanola SKU-126636 (stripped 'LOWEST Price in Market!' puffery, same defect class as davines/MUCOTA found in prior sessions), Alluora SKU-126653 (fixed a stray truncated '(GWP)' bracket artifact in canonical_name/product_line AND a genuine pack_count miss -- sku_name states 'Buy 1 Full Size Nourish Shampoo Get 1 Full Size Nourish Shampoo', a same-product buy-1-get-1 per SS1's rules, not GWP despite the listing's own '(GWP)' mislabel -- pack_count 1->2), Small Pukong SKU-140173 (stripped 'Best Selling'/'with FREE GIFTS' puffery). 4 further entries (SKU-127430, SKU-126557 'Nano Hair Growth Clinic Shampoo Range', SKU-140192 DIVULGE, SKU-140177 Small Pukong Ginger Bundle) verified genuinely correct via real Tier 2 judgment -- no fix needed. 18 existing taxonomy entries had content fixed (_meta reset to unreviewed); 2 new entries minted; 1 brand_dict correction; 4 entries verified correct via genuine Tier 2 judgment (_meta updated confident/unconfident per agreement with prior verdict). | QA gates re-verified live post-write via independent script/qa_report.sh: all 9 checks pass (dual-mapped 0, HUMAN+LLM coexistence 0, placeholder-leak 0, structured-fields-NULL% 0%, duplicate product_id 0, duplicate product+taxon 0, 'all variant/size' name 0, canonical_name fields 0, garbled brand text 0). SKU block SKU-160887-SKU-161086 left ACTIVE (2/200 used, reservation not a quota). Confidence distribution across the category's 2,944 distinct mapped entries: 2,717 never reviewed, 90 unconfident, 137 confident. Universe refresh not run this session -- explicitly out of scope per instructions. Residual precision debt for a future session, ranked: (1) the 'Hair+' fake-brand bucket (BRD-SG-04456) -- ~41 entries still remain after this session's 10 fixes and a prior session's 3, needs a dedicated brand-disambiguation sweep rather than piecemeal GMV-sampled fixes; (2) the brand-level 'Assorted Shampoo' catch-alls (Milbon 748 products, UNOVE, Dr.ville, Tsubaki, sukin, Off&relax) still need a Full-Rebuild-scale bulk pass, unchanged across 6+ prior sessions; (3) the wider high-GMV multi-variant 'Shampoo/Conditioner/Treatment' catch-all family (Tsubaki, MUCOTA, Oribe, Goldwell, Kerasys, Fino, etc., visible throughout this session's top-100 GMV sample) remains the single largest unaddressed precision-debt class, needing a dedicated bulk-text-matching split project; (4) the Shiseido Sublimic [Buy 2 Get 1] family, still pending image confirmation of what the promo language physically describes; (5) the same 9 low/no-GMV null_size entries (JUNGSAEMMOOL, As I Am, Miriam Quevedo, Bee Choo Origin, SHOU.PIN, LABOH, RENE FURTERER, 2x Sebamed) reconfirmed unresolvable for the 4th-6th+ time -- candidates for permanent qa_gate_exceptions closure if a future session reaches the same conclusion again; (6) product 43155032120 (Sunsilk [Bundle of 3/6/12] selector) mapped to the conservative pack_count=1 default rather than its own entry -- genuinely ambiguous, ~$858 GMV, low priority. |
| 2026-07-24 03:53 UTC | Automated review session (auto-discovery) | Worklist: 2,808 never-reviewed/unconfident entries. STEP 1B: all 9 qa_report.sh gates already pass live, no exceptions needed. Tier 1 SQL sweep flagged only 12 rows (down sharply from prior sessions' 30-170+, reflecting how much this category has already been swept): wrong_field_order 1, excess_content 2, null_size 9 (stub_leak/duplicate_brand/brand_casing_mismatch/canonical_field_mismatch/garbage_brand all 0). The wrong_field_order row (SKU-126750, MiseEnScene) led to a scope-boundary catch: brand BRD-GLOBAL-00509 has 37 taxonomy entries total, but only SKU-126750 belongs to shopee_sg_shampoo -- the other 36 (with a real, separate duplicate-brand-text defect, 'MiseEnScene Mise En Scene...') belong to shopee_sg_hair_conditioner_or_treatment and were correctly left untouched, out of this session's scope. SKU-126750 itself is correct as-is (brand_dict's unspaced 'MiseEnScene' vs the real spaced on-package form is a brand_dict issue affecting a different category's rows, not a defect in this row). The 2 excess_content flags (SKU-139823 Kerastase, SKU-139835 Craft) reconfirmed as established false-positive patterns (SET_WITH_SIZE real per-component sizes; genuine size-option-list respectively). Of the 9 null_size rows: fresh image reads on the top 2 by GMV (Bee Choo Origin $58,523, Sebamed $36,347) reconfirmed both are marketing-benefit/lineup shots with no legible volume anywhere -- consistent with 4-6 prior sessions' identical finding on this same set of 9 (JUNGSAEMMOOL, As I Am, Miriam Quevedo, Bee Choo Origin, SHOU.PIN, LABOH, RENE FURTERER, 2x Sebamed); left untouched, no _meta write. STEP 2b promo sweep (14,310 model rows / 186 distinct entries) filtered to 26 high-confidence explicit-multiplier candidates -- all were either already fixed in a prior session or established false positives (Shiseido Sublimic [Buy 2 Get 1] family, cross-component bundles, ambiguous buyer-tier selectors); no new genuine pack_count misses found this pass. Investigating the Sublimic Adenovital/Airy Flow 500ml entries (SKU-069746/069751, top-40 GMV) surfaced a minor fan-in issue: a handful of individually-unambiguous '450ml Refill' listings are mapped to the 500ml entry instead of the existing 450ml sibling (SKU-069758) -- real but ~$167 combined GMV, not pursued given the effort/impact ratio; the dominant $66,929-GMV listing in that entry is a genuine multi-size option-list (250/450/500/1000/1800ml) defaulted to 500ml, consistent with this pipeline's existing precedent for NARD and other option-list products elsewhere in this category. Similarly SKU-069827 (Clear Ice Cool Menthol) has 2 trivially-low-GMV (~$7 combined) products with mismatched sizes (2x70ml, 330ml) fanned into its 650ml entry -- not pursued. GMV-prioritized Tier 2 sample (top 60 worklist entries by GMV, excluding Tier-1-flagged rows): ~30 specific-size entries (Dr.FORHAIR x5, NARD x2, Shiseido Sublimic x2, Grafen, Oway, Selsun Blue, Pola, BiOSys, Ferveor, DASHU, Goldwell, COCO&EVE x2, Daeng Gi Meori, Julyme, Kerasys, LebeL, Olaplex x2, Paul Mitchell x2, Ryo x2, Small Pukong) cross-checked against sku_name text and verified genuinely correct; the remaining ~30 of the top 60 are the already-documented high-GMV multi-variant 'Shampoo/Conditioner/Treatment' catch-all family (Tsubaki, MUCOTA, Phytopecia, Oribe, Goldwell x3 more, Fino, KUNDAL, Advante, Kumano, Botanist, Plantur 39, Moroccanoil, Yolu, Dr.Melaxin, Kracie, Vichy, Aromase) -- reconfirmed correctly structured as designed (is_multi_size/is_multi_variant=TRUE), same #1 precision-debt item flagged unchanged across 4+ prior sessions, still needing a dedicated bulk-text-matching split project rather than piecemeal review. SKU-139830 (Hair+ brand bucket, 300ml) was noted but not touched -- its sku_name shows no 'Hair+' branding at all, consistent with the already-documented ~41-entry residual 'Hair+' fake-brand bucket (BRD-SG-04456) from prior sessions, left for a future dedicated brand-disambiguation pass. No genuine defects requiring a fix were found this session -- everything sampled was either already correct, an established/reconfirmed false positive, or a known low-GMV residual item not worth pursuing given effort/impact. | 0 rows required content fixes; 0 new taxonomy entries minted; no SKU block claimed. 66 entries verified genuinely correct via real Tier 2/Tier 1 judgment this session and promoted via the bulk _meta update (confident/unconfident per agreement with prior verdict). The 9 null_size rows and the 2 minor low-GMV fan-in mismaps (Sublimic ~$167, Clear Ice Cool Menthol ~$7) were left with no _meta write, per established practice of not fabricating a review for genuinely unresolved/low-value items. QA gates re-verified live post-session: G1 dual-mapped (LLM-scoped) 0, G2 HUMAN+LLM coexistence 0, placeholder-leak 0, structured-fields-missing 0%, G5 provenance 0, duplicate product_id 0 -- all pass. Confidence distribution across the category's 2,944 distinct mapped entries: 156 confident, 118 unconfident, 2,670 never reviewed. Universe refresh not run this session -- explicitly out of scope per instructions. Residual precision debt for a future session, unchanged in ranking: (1) the brand-level 'Assorted Shampoo'/multi-variant catch-all family (Tsubaki, MUCOTA, Phytopecia, Oribe, Goldwell, Milbon 748 products, UNOVE, Dr.ville, sukin, Off&relax, etc.) still needs a Full-Rebuild-scale bulk-text-matching split project; (2) the 'Hair+' fake-brand bucket (BRD-SG-04456) -- ~41 entries still unaddressed after prior sessions' partial fixes; (3) the Shiseido Sublimic [Buy 2 Get 1] family, still pending dedicated image confirmation; (4) the same 9 low/no-GMV null_size entries, now reconfirmed unresolvable for the 5th-7th+ time -- worth considering for a qa_gate_exceptions-style permanent closure mechanism if one gets wired up for Tier-1 (not just qa_report.sh) flags in a future design revision; (5) two newly-noted, very-low-GMV (~$167 and ~$7) size fan-in mismaps in the Shiseido Sublimic and Clear Ice Cool Menthol entries, not pursued given effort/impact. |
| 2026-07-25 16:47 UTC | Automated review session (auto-discovery) | Worklist: 2,788 never-reviewed/unconfident entries (down from 2,808 last session). STEP 1B fast-lane: 447 'fixed pending recheck' rows Tier-1-swept, 446 came back clean and were bulk re-confirmed (1 stayed flagged, null_size, folded into main sweep). STEP 2 Tier 1 SQL sweep over the full worklist flagged only 15 rows: wrong_field_order 1 (SKU-126750, MiseEnScene canonical missing its brand-token prefix entirely -- genuine defect, not the usual false-positive pattern), brand_casing_mismatch 3 (Appelles/MooGoo/Kustie all-caps or wrong-case brand tokens), excess_content 2 (both verified as false positives on inspection: SKU-139823 Kerastase is a documented SET_WITH_SIZE multi-component set, SKU-139835 Craft states two real sizes '300ml or 1000ml' rather than a generic stub), null_size 9 (all investigated via product image -- none had size printed/visible on packaging, and the raw_niq_history dataset documented in ARCHITECTURE.md does not actually exist for shopee_sg_shampoo in live BigQuery, so product_specification/product_description fallback signals are unavailable; left NULL as genuinely unresolvable this session, not skipped). STEP 2b D5 pack-count sweep found 195 pack_count=1 rows with promo/multiplier language; text-filtered to 8 genuine same-product-multiplier candidates (excluding GWP/different-product bundles): fixed SKU-127389 (BaWang 'Buy 1 Free 1' -> pack_count 2, GMV $731); left SKU-126560 unfixed (a Na Shampoo is_multi_size/is_multi_variant catch-all covering products with genuinely different real pack counts -- a structural limitation of the catch-all pattern itself, consistent with this category's documented MULTI_VARIANT_CATCHALL precision debt, not a one-row fix); left 3 zero-current-GMV Shiseido refill-bundle entries and 1 'NOT FOR SALE' KUNDAL sample listing unfixed per GMV-impact triage. Also surfaced (not part of the prescribed sweeps, found while investigating a D5 candidate): SKU-139824 is a genuine brand_mismatch -- product_brand_map resolved brand_id to 'Hair+' via PRODUCT_NAME_SCAN, but the product's own sku_name says 'Maycreate' and its actual product images show neither brand printed on the packaging (one model shows a Chinese-brand can, a later relisted model shows 'LIFUSHA' printed on the can) -- flagged for Stage 03 (brand_dict/product_brand_map) remediation per docs/brand-extraction.md, not touched here since fixing it requires changes outside product_taxonomy's scope. STEP 3 Tier 2: GMV-prioritized sample of the top 80 remaining un-flagged worklist rows (GMV range ~$32.5K-$337K) cross-checked against their highest-GMV mapped product's actual sku_name/merchant -- all 80 confirmed as correct shampoo-category matches with accurate brand/structure, mostly legitimate is_multi_size/is_multi_variant catch-alls listing real sub-line/size option text (not banned generic phrasing). | Fixed 5 taxonomy entries: SKU-126750 (prepended missing 'MiseEnScene' brand token), SKU-126574/SKU-127001/SKU-127690 (brand casing corrected to match brand_dict, plus stripped a generic '(2 Sizes)' stub suffix from SKU-127001 and a broken '( + )' template artifact from SKU-127690), SKU-127389 (pack_count 1->2 and canonical_name suffixed 'x2' for a missed 'Buy 1 Free 1' promo). All 5 had meta_agent='CLAUDE_CODE' and _meta reset to unreviewed for next-session re-confirmation. Bulk-confirmed 446 fast-lane rows plus 82 rows given genuine Tier 2 judgment this session (2 Tier-1 false-positives judged correct + 80 GMV-sampled rows) via one bulk _meta UPDATE each, per the two-consecutive-agreement confidence model. Live confidence distribution after this session: 214 confident (up from 156), 529 unconfident (up from 564 net of promotions), 2,201 unreviewed (down from 2,224). Post-fix hard-gate self-check (STEP 10, no --skip-coexistence): dual-mapped-LLM 0, HUMAN+LLM coexistence 0, placeholder-leak 0, structured-fields NULL% 0, duplicate product_id 0, duplicate product+taxon 0, garbled-brand-text 2 (both SKU-127136/SKU-127658, brand '107' -- already covered by the standing qa_gate_exceptions row confirmed across 3+ prior sessions, not a new finding). All gates pass. |
| 2026-07-26 04:36 UTC | Automated review session (auto-discovery) | Worklist: 2,730 never-reviewed/unconfident entries. STEP 1B: all 9 qa_report.sh gates already pass live, no exceptions needed. STEP 1C fast-lane (8 map rows / 6 distinct 'fixed pending recheck' entries): 4 bulk-promoted clean (SKU-127001, SKU-127690, SKU-127389, SKU-126750); SKU-139820 re-flagged null_size and fixed; SKU-126574 left untouched (its canonical_field_mismatch flag evaluated to SQL NULL, not FALSE, due to NULL-propagation for a legitimate multi-variant catch-all with NULL product_line — genuinely ambiguous, deferred rather than guessed). STEP 2 Tier 1 sweep over the full worklist: only null_size fired, 9 rows. STEP 2b pack-count sweep: 12,224 raw hits on pack_count=1 + promo-language regex, reviewed the top 60 by GMV — overwhelmingly false positives from ingredient claims ('sulfate-free', 'paraben-free', 'silicone-free') matching the bare 'free' pattern; found 2 genuine buy-N-get-M misses (SKU-126676 'Buy 1 Free 1', SKU-126621 'Buy1 Free3') and one misleading canonical-name artifact (SKU-140053 Olaplex stated 'x3pcs' in the name despite pack_count=1 being correct for that specific listing option). New defect class found and fixed, not previously documented anywhere in llm-extraction-rules.md or the QA gates: 65 entries carried a literal empty '( )' placeholder in canonical_name/product_line (e.g. 'Anti-Hair Loss Shampoo ( ) 1000ml') — an artifact of a variant/scent field never being filled during extraction while the wrapping parens were retained; none of the existing Tier 1 regexes catch this shape. Also found via the size-cross-validation lens (§11): SKU-069520 (NARD Scalp Cooling Shampoo) had size hard-coded to '500ml' but its one underlying product_id is a genuine buyer-selectable 500ml/1000ml listing (option_name breakdown: 1000ml carries 61% of GMV) — a size mismap Tier 1's null_size check can't catch since size wasn't NULL. STEP 3 GMV-prioritized sample (60 additional top-GMV worklist rows, judged against llm-extraction-rules.md in full including §11 and quality-standards.md D1-D5): all correct except the SKU-069520 mismap above; spot-checked several multi-product/multi-size catch-all listings (Aussie Miracle Curls, As I Am, Miriam Quevedo, Sebamed dual-strength lines) and confirmed is_multi_size=TRUE was the right call given genuinely mixed option_name sizes/products, not a lazy default. | Fixed 78 taxonomy entries total via bq DML (meta_agent='CLAUDE_CODE' on every write, _meta reset to unreviewed for next-session recheck): 65 via a bulk REGEXP_REPLACE stripping the empty '( )' placeholder from canonical_name/product_line (one, SKU-127851, needed a follow-up spacing fix for an adjacent '+'); 9 null_size rows resolved per the size extraction priority chain (2 found a real size via product_specification's Volume field — SKU-139747→250ml, SKU-129571→400ml; 1 required an image read that still found no size anywhere after exhausting all four signals, converted to is_multi_size=TRUE as the schema's only sanctioned NULL-size path; 6 were genuine multi-size/multi-product listings confirmed via option_name, converted to is_multi_size=TRUE); SKU-069520 converted to is_multi_size=TRUE after confirming via option_name/GMV breakdown; SKU-126676 and SKU-126621 pack_count corrected to 2 and 4 per their explicit buy-N-get-M title text; SKU-140053 canonical_name cleaned to drop the misleading 'x3pcs' suffix. 115 additional entries (59 from the STEP 3 GMV sample, 57 from STEP 2b, minus 1 double-counted) received genuine Tier 2 judgment, found correct, and were promoted per the two-consecutive-agreement rule (mix of newly-confident and unconfident-pending-another-review, per each row's own prior verdict). All hard gates re-verified live post-fix: dual-mapped 0, HUMAN+LLM coexistence 0, placeholder-leak 0, structured-fields NULL% 0, duplicate product_id 0, duplicate product+taxon 0. Confidence distribution left behind: 111 rows newly confident/unconfident from this session's correct-verdicts, 78 rows reset to unreviewed pending their next recheck, 1 row (SKU-126574) left exactly as before for a future session's judgment slot, and 2,730-194=2,536 rows in the worklist untouched (Tier-1-clean but not sampled this session — per instructions, no _meta write was fabricated for rows that didn't get real Tier 2 judgment). |
---

## Scripts

| Script | Purpose |
|--------|---------|
| `pipeline/05_product_taxonomy/llm_shopee_sg_shampoo/build_taxonomy.py` | Pass 1 extraction (external pipeline repo — not in this repo; this category's actual Pass 1/2 was done directly by a headless `claude -p` session instead, per `docs/headless-runbook.md`, not by this script) |
| `pipeline/05_product_taxonomy/llm_shopee_sg_shampoo/build_p2_taxonomy.py` | Pass 2 routing (same — not used; headless session did this directly) |

---

## Map Row Counts (verified live 2026-07-16, after HUMAN-row cleanup)

| Source | Count | Notes |
|--------|-------|-------|
| LLM | 2,421 | 965 Pass 1 (official-store) + 301 Pass 2 bulk-text-matched + 1,155 Pass 2 catch-all (9 no-official-store brands) |
| HUMAN | 1,408 | Keyword-seed routing. Down from 2,255 — the 847 that duplicated an LLM row were deleted 2026-07-16; these 1,408 have no LLM row and stay permanently |
| NULL (unmapped) | remainder of 24,818 distinct products | ~158 brands of the true 95%-GMV universe still unaddressed — see Scale section |
