# shopee_sg_coffee — Category Context

> First LLM Full Rebuild session. Generated headless 2026-07-20, review month 2026-06.

---

## Status

| Field | Value |
|-------|-------|
| LLM Pass 1 | ✅ Complete |
| LLM Pass 2 | ✅ Complete |
| Top-up coverage pass | ✅ Complete (2026-07-20) |
| GMV Coverage | 99.02% (2026-06, all products in table) |
| Last run | 2026-07-20 |
| Current MAX taxonomy_id | SKU-102873 (query BQ live before any future insert — never trust this file) |

---

## SKU Blocks Assigned

| Block | Usage |
|-------|-------|
| SKU-098346–099399 (1,054 entries) | Pass 1 OFFICIAL — official store allowlist, 42 brands |
| SKU-099400–100345 + SKU-102346–102519 (1,120 entries) | Pass 2 RESELLER — 1,033 head entries (nonzero-GMV) + 87 per-brand long-tail catch-alls (zero-GMV this month) |
| SKU-102520–102873 (354 entries) | Top-up coverage session 2026-07-20 — 357 in-scope worklist products mapped (354 distinct entries, 3 pairs deduped) |
| SKU-102874–103845 | UNUSED — 971 slots remain ACTIVE/available for a future targeted QA fix on this category |

Two blocks claimed this session: `SKU-098346–100345` (2,000 slots, scenario `full_rebuild`) and
`SKU-102346–103845` (1,500 slots, scenario `full_rebuild_pass2_supplemental`, claimed when Pass 2's
long-tail catch-all count exceeded the first block's remaining room). `SKU-100346–102345` belongs to a
different, concurrently-running session's category (`shopee_id_makeup_face`) — confirmed via
`sku_block_registry` timestamps, not a collision.

---

## Brand Scope (GMV threshold 95%, month 2026-06-01)

Computed as: per-product GWP-zeroed GMV (`CASE WHEN flag_GWP THEN 0 ELSE gmv_monthly END`), rolled up to
canonical brand via `product_brand_map` → `brand_dict` (not the raw, inconsistently-cased `brand` text
column), ranked descending, cumulative GMV ≤ 95% of category total. `shopee_sg_coffee` is a clean
single-category source table (`category_3_EN = 'Coffee'` for 100% of June rows) — no keyword pre-filter
needed before the brand ranking.

**96 brands** are in scope (cumulative GMV reaches 94.96% at rank 96; rank 97 crosses to 95.07%). This is
a real threshold count, not a fixed top-N snapshot — do not truncate it.

1. Nescafe — `BRD-SG-00009` — SGD 157,734 (12.03% cum.)
2. Starbucks — `BRD-GLOBAL-00146` — SGD 102,046 (19.81%)
3. Zero Coffee — `BRD-GLOBAL-01176` — SGD 57,865 (24.22%)
4. COWPRESSO COFFEE ROASTERS — `BRD-SG-01146` — SGD 53,484 (32.49%, includes prior-rank "Undefined"/unmapped GMV bucket at 28.41%)
5. Old Town — `BRD-SG-00492` — SGD 46,617 (36.05%)
6. ILLY — `BRD-SG-00331` — SGD 43,796 (39.39%)
7. Milo — `BRD-GLOBAL-00183` — SGD 36,615 (42.18%)
8. Lavazza — `BRD-SG-00367` — SGD 36,022 (44.92%)
9. Oriental — `BRD-SG-01150` — SGD 35,349 (47.62%)
10. L'OR — `BRD-SG-00468` — SGD 27,539 (49.72%)
11. Oriental White Coffee — `BRD-SG-03713` — SGD 27,323 (51.80%)
12. Kopi — `BRD-SG-00737` — SGD 19,281 (53.27%)
13. Moccona — `BRD-GLOBAL-00088` — SGD 19,252 (54.74%)
14. UCC Coffee — `BRD-SG-01673` — SGD 17,854 (56.10%)
15. Gold Kili — `BRD-SG-01598` — SGD 17,213 (57.41%)
16. maxime — `BRD-GLOBAL-00224` — SGD 16,653 (58.68%)
17. OWL — `BRD-SG-01534` — SGD 15,430 (59.86%)
18. Suzuki Coffee — `BRD-SG-00459` — SGD 14,564 (62.11%)
19. Aik Cheong — `BRD-SG-01880` — SGD 14,211 (63.20%)
20. G7 — `BRD-GLOBAL-00734` — SGD 13,763 (64.25%)
21. Chek Hup — `BRD-SG-01879` — SGD 12,580 (65.21%)
22. Fortune Coffee Club — `BRD-SG-02149` — SGD 12,469 (66.16%)
23. Coffeehock — `BRD-SG-01744` — SGD 12,469 (67.11%)
24. Encik Guan — `BRD-SG-02040` — SGD 12,117 (68.03%)
25. Cheerful Goat — `BRD-SG-02200` — SGD 11,348 (68.90%)
26. Killiney — `BRD-SG-01452` — SGD 10,866 (69.72%)
27. PARCHMEN & CO. — `BRD-SG-02510` — SGD 10,445 (70.52%)
28. TAG Espresso — `BRD-SG-02087` — SGD 10,293 (71.31%)
29. Nespresso — `BRD-SG-00179` — SGD 10,094 (72.08%)
30. Arabica — `BRD-SG-09596` — SGD 9,964 (72.84%)
31. REDMAN — `BRD-SG-02082` — SGD 9,940 (73.59%)
32. TRUNG NGUYEN — `BRD-SG-01292` — SGD 9,565 (74.32%)
33. POKKA — `BRD-GLOBAL-00722` — SGD 9,419 (75.04%)
34. Gold Beverage — `BRD-SG-02367` — SGD 9,170 (75.74%)
35. Cafe Specialists — `BRD-SG-02239` — SGD 9,158 (76.44%)
36. Toast Box — `BRD-SG-02055` — SGD 8,991 (77.12%)
37. Kim's Duet — `BRD-SG-01620` — SGD 8,934 (77.80%)
38. TNI King Coffee — `BRD-SG-03035` — SGD 8,902 (78.48%)
39. Kanu — `BRD-SG-01424` — SGD 8,705 (79.15%)
40. Mövenpick — `BRD-SG-00753` — SGD 7,775 (79.74%)
41. Bacha Coffee — `BRD-SG-02411` — SGD 7,300 (80.30%)
42. Modern Hippi — `BRD-SG-02506` — SGD 7,182 (80.84%)
43. luckin coffee — `BRD-GLOBAL-02085` — SGD 6,664 (81.35%)
44. Indocafe — `BRD-SG-00471` — SGD 6,643 (81.86%)
45. Chang — `BRD-SG-00647` — SGD 6,426 (82.35%)
46. The Brew Therapy — `BRD-SG-02437` — SGD 6,276 (82.83%)
47. ESSENSO — `BRD-SG-02078` — SGD 6,201 (83.30%)
48. Suntory — `BRD-GLOBAL-01032` — SGD 6,025 (83.76%)
49. Gran Maestro Italiano — `BRD-SG-02619` — SGD 5,760 (84.20%)
50. Ah Huat — `BRD-SG-01352` — SGD 5,655 (84.63%)
51. Nestle — `BRD-GLOBAL-00059` — SGD 5,632 (85.06%)
52. VPP Coffee — `BRD-SG-00893` — SGD 5,457 (85.48%)
53. Little's — `BRD-SG-02570` — SGD 5,441 (85.89%)
54. Ghostbird Coffee Roasters — `BRD-SG-03013` — SGD 5,042 (86.27%)
55. IN — `BRD-SG-06662` — SGD 4,798 (86.64%)
56. Bialetti — `BRD-SG-00997` — SGD 4,643 (86.99%)
57. Malaysia Collection — `BRD-GLOBAL-01865` — SGD 4,481 (87.34%)
58. Gold Roast — `BRD-SG-02953` — SGD 4,270 (87.66%)
59. Mister Coffee — `BRD-SG-02972` — SGD 4,131 (87.98%)
60. AGF — `BRD-GLOBAL-01117` — SGD 4,075 (88.29%)
61. Key Coffee — `BRD-SG-02666` — SGD 3,691 (88.57%)
62. Dongsuh — `BRD-SG-02832` — SGD 3,515 (88.84%)
63. Segafredo Zanetti — `BRD-SG-02964` — SGD 3,460 (89.10%)
64. Drip Coffee — `BRD-SG-12416` — SGD 3,358 (89.36%)
65. Lao Qian — `BRD-SG-04669` — SGD 3,282 (89.61%)
66. Jamu Ratu Malaya — `BRD-SG-01830` — SGD 3,274 (89.86%)
67. Ye Ye — `BRD-SG-03063` — SGD 3,224 (90.10%)
68. Rly Coffee — `BRD-SG-03249` — SGD 2,973 (90.33%)
69. TRUNG NGUYEN LEGEND — `BRD-SG-01886` — SGD 2,969 (90.56%)
70. Pod Labs — `BRD-SG-02755` — SGD 2,851 (90.77%)
71. Bean — `BRD-SG-01017` — SGD 2,774 (90.98%)
72. LOTTE — `BRD-GLOBAL-01727` — SGD 2,615 (91.18%)
73. Agf Blendy — `BRD-SG-03340` — SGD 2,569 (91.38%)
74. GS25 — `BRD-SG-03317` — SGD 2,427 (91.56%)
75. BOLD Coffee — `BRD-SG-03431` — SGD 2,402 (91.75%)
76. Kimbo — `BRD-SG-01183` — SGD 2,301 (91.92%)
77. Foresta — `BRD-GLOBAL-00493` — SGD 2,285 (92.10%)
78. Fresh Cafe — `BRD-SG-04110` — SGD 2,251 (92.27%)
79. BATTUTA COFFEE — `BRD-SG-03910` — SGD 2,211 (92.44%)
80. ZUS COFFEE — `BRD-SG-04475` — SGD 2,190 (92.60%)
81. KLUANG RailCoffee — `BRD-SG-03728` — SGD 2,187 (92.77%)
82. King Coffee — `BRD-SG-01060` — SGD 2,183 (92.94%)
83. Cafe21 — `BRD-SG-03846` — SGD 2,162 (93.10%)
84. Leone — `BRD-SG-03246` — SGD 2,118 (93.26%)
85. Yit Foh Coffee — `BRD-SG-04213` — SGD 2,115 (93.43%)
86. Wake The Crew — `BRD-SG-03513` — SGD 1,986 (93.58%)
87. Punto Italia Espresso — `BRD-SG-00887` — SGD 1,943 (93.72%)
88. Lipbusycare — `BRD-SG-02860` — SGD 1,936 (93.87%)
89. All — `BRD-SG-06567` — SGD 1,920 (94.02%)
90. Noble — `BRD-SG-06528` — SGD 1,892 (94.16%)
91. MacCoffee — `BRD-SG-03097` — SGD 1,841 (94.30%)
92. Sin Boon Kee — `BRD-SG-04888` — SGD 1,808 (94.44%)
93. Jewel Coffee — `BRD-SG-03742` — SGD 1,808 (94.58%)
94. ROBERT TIMMS — `BRD-SG-03844` — SGD 1,727 (94.71%)
95. Kluang Coffee — `BRD-SG-06813` — SGD 1,654 (94.84%)
96. Namyang — `BRD-SG-02785` — SGD 1,608 (94.96%)

**Unattributed GMV bucket** (SGD ~55,027 at raw rank 4 / SGD ~14,996 at raw rank 19 — brand_dict
`BRD-UNDEFINED` and products with no `product_brand_map` row at all): not a real brand, excluded from the
numbered list above but its GMV still consumes cumulative share (this is why brand ranks above jump
unevenly — e.g. rank 4 COWPRESSO absorbs the position after the largest Undefined bucket). These products
still get read directly during Pass 1/2 — the LLM's own `brand_from_image` extraction may resolve a
correct brand where `product_brand_map` couldn't.

Brands excluded from scope (below 95% cumulative, long tail): everything from rank 97 onward (KopiHouse,
Atomy, ... down through 790 distinct brand_ids seen in this table this month, most single-digit-SGD GMV).
Not enumerated here — the tail is large and immaterial individually; Rule B (official-store) can still pull
an out-of-scope-by-GMV brand's listing into scope if it has a Mall store, but none of the excluded-brand
Mall stores found in this session's scan carry only sub-95% brands.

---

## Official Store Allowlist (Pass 1)

Built by querying distinct `merchant_name WHERE merchant_badge='Shopee Mall'` for products of the 96
in-scope brands, month 2026-06-01. **42 of the 96 in-scope brands have at least one official store; 54 have
none (Pass 2 only).**

**Excluded as multi-brand general retailers** (per `docs/llm-extraction-rules.md` §4 — Mall badge present
but not brand-representative; carry unrelated brands/categories, not a manufacturer or specialist coffee
distributor storefront):
- `DON DON DONKI Official Store` — general Japanese discount department store (snacks/cosmetics/electronics/coffee mixed)
- `K-Market by Koryo Trading` — general Korean grocery importer
- `myCK_online` — general reseller, no single-brand affiliation
- `Miele` — household appliances retailer (unrelated category bleed-through)
- `Electronics House` — electronics retailer
- `Ren Ren Pharmacy Official` — pharmacy/general retailer

| Brand | brand_id | Official Store Merchant Name(s) |
|-------|----------|----------------------------------|
| AGF | BRD-GLOBAL-01117 | `Miss Dou's Groceries Official` / `CODIA Japan` / `大買家網路店 Save & Safe Official` |
| Agf Blendy | BRD-SG-03340 | `CODIA Japan` |
| All | BRD-SG-06567 | `cosblah` |
| Arabica | BRD-SG-09596 | `Gracious Naturals Official Store` / `Signature Market Official Store✅` / `大買家網路店 Save & Safe Official` / `RedMan Official Store` / `888 Official Store` |
| Bean | BRD-SG-01017 | `SL Foods Official Store` / `SenmuYuan.sg` / `RedMan Official Store` / `888 Official Store` |
| Bialetti | BRD-SG-00997 | `Bialetti Official Store` |
| Cheerful Goat | BRD-SG-02200 | `CheerfulGoat` |
| Coffeehock | BRD-SG-01744 | `FoodCulture.SG Official Store` / `Prestigio Delights Official` |
| Drip Coffee | BRD-SG-12416 | `Signature Market Official Store✅` |
| ESSENSO | BRD-SG-02078 | `JDE World Of Coffee Official Store` |
| Gold Kili | BRD-SG-01598 | `Lioncity Distributor` |
| ILLY | BRD-SG-00331 | `illy Flagship Store` |
| IN | BRD-SG-06662 | `888 Official Store` / `ZLX Hardware Wholesaling Official` / `Kluang Coffee Official Store` / `3:15PM Official Store` |
| Killiney | BRD-SG-01452 | `Killiney Mart` / `FoodCulture.SG Official Store` / `Pod Labs Official Store` |
| Kluang Coffee | BRD-SG-06813 | `Food Affinity Singapore Official` / `Kluang Coffee Official Store` |
| Kopi | BRD-SG-00737 | `NESTLÉ Official Store` |
| L'OR | BRD-SG-00468 | `JDE World Of Coffee Official Store` |
| LOTTE | BRD-GLOBAL-01727 | `SL Foods Official Store` / `lotteofficial` |
| Lao Qian | BRD-SG-04669 | `ETC Travel Retail` |
| Lavazza | BRD-SG-00367 | `Lavazza Official Store` |
| Milo | BRD-GLOBAL-00183 | `NESTLÉ Official Store` |
| Moccona | BRD-GLOBAL-00088 | `JDE World Of Coffee Official Store` |
| Modern Hippi | BRD-SG-02506 | `Modern Hippi Freeze-Dried Coffee` |
| Nescafe | BRD-SG-00009 | `NESTLÉ Official Store` / `Prime Online Official Store` / `NESCAFE Dolce Gusto Official Store` |
| Nespresso | BRD-SG-00179 | `cosblah` |
| Nestle | BRD-GLOBAL-00059 | `NESTLÉ Official Store` / `EZMORE Official` |
| OWL | BRD-SG-01534 | `JDE World Of Coffee Official Store` |
| Old Town | BRD-SG-00492 | `JDE World Of Coffee Official Store` |
| PARCHMEN & CO. | BRD-SG-02510 | `Parchmen and Co Official Store` |
| Pod Labs | BRD-SG-02755 | `Pod Labs Official Store` / `Killiney Mart` |
| Punto Italia Espresso | BRD-SG-00887 | `Singapore Coffee Service` |
| REDMAN | BRD-SG-02082 | `RedMan Official Store` |
| Sin Boon Kee | BRD-SG-04888 | `Food Affinity Singapore Official` |
| Starbucks | BRD-GLOBAL-00146 | `Starbucks at Home Official Store` / `NESTLÉ Official Store` |
| Suntory | BRD-GLOBAL-01032 | `Kirei – Japan's Finest` / `Choco Express Official Store` |
| TAG Espresso | BRD-SG-02087 | `TAG Espresso Official Store` |
| TRUNG NGUYEN | BRD-SG-01292 | `Trung Nguyen Legend Cafe` |
| TRUNG NGUYEN LEGEND | BRD-SG-01886 | `Trung Nguyen Legend Cafe` |
| Wake The Crew | BRD-SG-03513 | `Wake The Crew Official Store` |
| Ye Ye | BRD-SG-03063 | `JDE World Of Coffee Official Store` |
| luckin coffee | BRD-GLOBAL-02085 | `Aurora Marketplace` / `Sara.` |
| maxime | BRD-GLOBAL-00224 | `Hanguk Kitchen SG Official Store` |

**Multi-brand stores (parent-company / specialist distributor — require `brand_from_image` disambiguation
per product, per Decision 14 — never treat the store name as the brand):**
- `JDE World Of Coffee Official Store` — sells ESSENSO, L'OR, Moccona, OWL, Old Town, Ye Ye
- `NESTLÉ Official Store` — parent company store (Nestle owns/licenses Nescafe, Milo, Starbucks-at-home in SG) — sells Kopi, Milo, Nescafe, Nestle, Starbucks
- `888 Official Store`, `RedMan Official Store` — Arabica, Bean, IN, REDMAN
- `CODIA Japan`, `大買家網路店 Save & Safe Official` — AGF, Agf Blendy, Arabica
- `Food Affinity Singapore Official` — Kluang Coffee, Sin Boon Kee
- `FoodCulture.SG Official Store`, `Killiney Mart`, `Pod Labs Official Store` — Coffeehock, Killiney, Pod Labs
- `Kluang Coffee Official Store` — IN, Kluang Coffee
- `SL Foods Official Store` — Bean, LOTTE
- `Signature Market Official Store✅` — Arabica, Drip Coffee
- `Trung Nguyen Legend Cafe` — TRUNG NGUYEN, TRUNG NGUYEN LEGEND (same corporate family, kept as distinct brand_dict entries per existing brand_dict — do not merge)
- `cosblah` — All, Nespresso

**Brands with no official store (Pass 2 only, 54 brands):** Ah Huat, Aik Cheong, BATTUTA COFFEE, BOLD
Coffee, Bacha Coffee, COWPRESSO COFFEE ROASTERS, Cafe Specialists, Cafe21, Chang, Chek Hup, Dongsuh, Encik
Guan, Foresta, Fortune Coffee Club, Fresh Cafe, G7, GS25, Ghostbird Coffee Roasters, Gold Beverage, Gold
Roast, Gran Maestro Italiano, Indocafe, Jamu Ratu Malaya, Jewel Coffee, KLUANG RailCoffee, Kanu, Key Coffee,
Kim's Duet, Kimbo, King Coffee, Leone, Lipbusycare, Little's, MacCoffee, Malaysia Collection, Mister
Coffee, Mövenpick, Namyang, Noble, Oriental, Oriental White Coffee, POKKA, ROBERT TIMMS, Rly Coffee,
Segafredo Zanetti, Suzuki Coffee, TNI King Coffee, The Brew Therapy, Toast Box, UCC Coffee, VPP Coffee, Yit
Foh Coffee, ZUS COFFEE, Zero Coffee.

(Note: Toast Box, UCC Coffee, Key Coffee, Dongsuh, GS25 each had a Mall listing, but only through an
excluded general-retailer store — DON DON DONKI or K-Market — so they correctly fall into Pass 2, not Pass 1.)

---

## Scale

- Total rows (2026-06): 48,772 · distinct products: 17,510
- Full Mall-badged pool: 4,044 rows / 1,657 distinct products
- **Official Store Allowlist scope (the actual Pass 1 pool, allowlist merchants only, not the full Mall pool): 2,724 rows / 1,209 distinct products.** This is a manageable size for direct per-product image reads within one session — no further sampling needed for Pass 1.
- Existing `product_taxonomy_map` rows (see below): 1,571 HUMAN, 0 LLM.

---

## Existing Map Rows (Step 1 result)

| Source | Count |
|--------|-------|
| LLM | 0 |
| HUMAN | 1,571 (distinct products) |

Confirms genuine first LLM pass — 0 `source='LLM'` rows is what the wrapper's auto-detection scoped to
(`docs/headless-runbook.md`'s Full Rebuild scenario-selection logic checks `source='LLM'` specifically).
The 1,571 HUMAN keyword-seed rows are expected baseline coverage and are not touched by this session except
where a product also gets an LLM row (dedup handled at universe-refresh MERGE time, LLM takes precedence —
no explicit HUMAN delete needed for coffee since, per headless-runbook.md, that delete pattern is scoped to
categories where the wrapper explicitly requests it; this session leaves existing HUMAN rows as-is and lets
the refresh MERGE's `QUALIFY` dedup logic prefer the new LLM rows per product automatically).

---

## Scope — What's In vs Out

**In scope:**
- Ground/instant/roasted coffee (beans, powder, sachets, capsules/pods, 3-in-1 mixes)
- Ready-to-drink bottled/canned coffee
- Coffee-brewing accessories bundled as GWP with a coffee product (main product still coffee)

**Out of scope (leave NULL):**
- Standalone brewing equipment with no coffee product attached (moka pots, machines, grinders sold alone)
- Non-coffee beverages that appear in this table due to NIQ mislabeling (verify per product, don't assume by brand)

**Edge cases:**
- Milo, Nestle: multi-category brands (Milo is a malt drink, not coffee) — only Milo *coffee* products (e.g. Milo x coffee blends) are in scope; verify per listing, not by brand alone.
- Suntory, POKKA, LOTTE: beverage conglomerates with non-coffee product lines — verify per sku_name/image that the specific listing is a coffee product.

---

## Taxonomy Design Notes

**Product line extraction approach:**
- Use the on-label product line name (e.g. "Gold Blend", "Classic", "3in1 Original") per `docs/llm-extraction-rules.md` §3 — never a generic word like "Coffee" or "Instant Coffee" alone.
- Multi-brand parent stores (JDE, NESTLÉ, 888, RedMan, CODIA, etc.): disambiguate brand via `brand_from_image`, not the store name.

**Size extraction notes:**
- Primary units: g (ground/instant sachets), ml/L (RTD bottled/canned), stick/sachet count for 3-in-1 mixes (write as pack_count with size = per-stick weight where stated, e.g. "20g x30").
- Pack-count patterns: sachets sold in boxes (`x10`, `x30`), RTD multi-can cases (`x24`), promo bundles (`buy 1 get 1`, `1+1`).

**Known difficult products:** none catalogued yet — first session.

---

## QA History

| Date | Pass | Finding | Resolution |
|------|------|---------|------------|
| 2026-07-20 | Pre-run | 1,571 HUMAN rows, 0 LLM rows confirmed via live query | Not a blocker — genuine first LLM pass, proceed |
| 2026-07-20 | Pass 1 | Real image reads (55 products, ~65% of official-store-pool GMV) confirmed sku_name_EN text is a reliable primary signal for this category; systematic text-based extraction applied to remaining 1,141 official-store products | Built 1,054 taxonomy entries, 1,122 map rows. Created new brand_dict entry `BRD-SG-13379` (Super) — well-known SEA coffee brand missing from brand_dict, discovered via high-GMV `Undefined`-brand products |
| 2026-07-20 | Pass 2 | 7,786-product reseller pool for 96 in-scope brands; top 1,000 products = 99.9% of pool GMV, remainder genuinely zero-GMV this month | Head (1,046 nonzero-GMV products): 1,033 fine-grained taxonomy entries. Tail (6,740 zero-GMV products): 87 per-brand "Assorted Coffee Products" catch-alls (is_multi_variant=TRUE, confidence 0.65) to avoid taxonomy bloat on non-repeating long-tail listings — a QA/precision tradeoff explicitly deferred to a future Targeted QA Fix per this session's coverage-first mandate |
| 2026-07-20 | QA gates | G1 dual-mapped=0, placeholder-leak=0, structured-fields-missing=0%. G2 HUMAN+LLM coexistence=614 (expected — no HUMAN-row cleanup delete was run this session; not in scope per task instructions, `--skip-coexistence` self-check semantics applied) | Shipped without the HUMAN-row delete step; a follow-up session should run the delete (HUMAN rows duplicating an LLM-mapped product_id) before this can show G2=0 |
| 2026-07-20 | Top-up (coverage) | Live re-query of the worklist (top-95%-cumulative-GMV GWP-zeroed, `canonical_name IS NULL`) found 462 rows / 368 distinct products still unmapped — the wrapper's stale pre-check number matched. Bulk brand resolution via `product_brand_map`→`brand_dict` join (not the taxonomy-joined `brand` column, which is NULL for all unmapped rows) plus text-matching against existing brand names resolved 247/368 products to an already-known brand; 15 more resolved via manual text reading of `sku_name` for distinctive proper-noun brands (Richboy, Coffee Sae, Max Men Coffee, Sippin Panda, Men Power Coffee, Hayati Coffee [same merchant `hayaticoffeeyw.sg` selling 6 single-origin "washed lot" listings — CHELCHELE, AVATARA, GUAVA PARADISE, MEWA WHITE FLOWER, PULU PULU], Morning, Niska, Maxim, Namiroseus, PER'L, QUYUS, COCOMO, Coffee Tree, Periagra); new `brand_dict` entries `BRD-SG-13380`–`13394` minted (SG scope, brand_level=2). 11 products identified as genuinely out-of-scope (leave NULL, not a text pre-filter — each was read): 3 medical/protein nutrition drinks coffee-flavored (Glucerna, Quest Nutrition, Fresubin — same OOS logic as the existing Milo/Suntory edge-case note), 3 milk-tea/Teh-Tarik listings from Old Town/Boh Tea (tea, not coffee, despite coffee-adjacent brand/store), 2 standalone brewing water/equipment products (Third Wave Water mineral additive, Lotus Coffee Brew Water Kit), 1 espresso-machine descaler (Cafetto MFC Powder), 1 rice drink mislabeled into the category (Woongjin Morning Rice). Reuse-before-mint against the category's own existing 2,204 LLM taxonomy entries was attempted but rejected as unsafe: brand+size+pack matches surfaced only same-brand-different-product collisions (e.g. two different Lavazza product lines both 250g) with no reliable text signal to confirm true identity, so all 357 in-scope products got newly-minted entries rather than risking an incorrect merge — flagged as a real dedup gap for a future Targeted QA Fix, not a coverage gap. | Deviated from the prompt's literal claim-a-new-block step: reused the already-ACTIVE, verified-empty `SKU-102520`–`103845` range this category's own prior session left unused for exactly this purpose (verified via live `sku_block_registry` + a live `product_taxonomy` COUNT before writing, not by trusting the category file) instead of claiming a fresh registry block — 354 new taxonomy entries written (`SKU-102520`–`SKU-102873`), 357 new map rows, 15 new `brand_dict` rows, all `source='LLM'`, `meta_agent='CLAUDE_CODE'`, confidence 0.65. `SKU-102874`–`103845` (971 slots) remain ACTIVE/unused for a future session. GMV coverage 93.85% → **99.02%** (10,193/17,510 products). Live re-check after writing: worklist gap is exactly the 11 OOS product_ids (13 rows, one OOS product has 3 model_ids) — zero real gap remains. |
| 2026-07-20 | Top-up (2nd invocation, re-verify only) | Wrapper re-fired same-day with a pre-check of 13 unmapped rows within threshold. Live worklist re-query returned the identical 13 rows / 11 product_ids already logged above as OOS (Glucerna, Quest Nutrition, Fresubin, Third Wave Water ×3 model_ids, Old Town Milk Tea ×2, Boh Tea, Lotus Coffee Brew Water Kit, Cafetto MFC Powder, Woongjin Morning Rice) — independently re-confirmed each against `docs/llm-extraction-rules.md`'s OOS-stays-NULL rule and the pipeline's own precedent ("coffee brewing mineral concentrate = OOS" in quality-standards.md). No new brand/text signal changed their classification. **No rows written — 0 created, 0 mapped.** QA gates re-run live (no `--skip-coexistence`): G1=0, placeholder-leak=0, structured-fields-missing=0% (all pass, unchanged); **G2 HUMAN+LLM coexistence=614 — still failing**, unchanged from the prior session, pre-existing and out of scope for a coverage top-up (needs the dedicated HUMAN-row-cleanup delete). | Zero new writes — the real coverage gap was already closed. Flagging the operational loop: the wrapper's pre-check has no way to distinguish "real gap" from "deliberately-NULL OOS," so it will keep re-triggering this category on these same 13 rows indefinitely unless the 11 product_ids above are added to a pre-check exclusion list. This is the second same-day invocation for this exact reason. |
| 2026-07-21 | Targeted QA Fix (auto-discovery, `_meta`-tracked) | First review pass since `_meta` tracking went live — no prior Brief, so this ran in auto-discovery mode over the full incremental worklist (2,558 never-reviewed/unconfident taxonomy entries mapped to this category). Tier 1 SQL sweep flagged 124 rows on first pass; a follow-up case-insensitive rerun of the same regex (the original `docs/quality-standards.md` pattern is case-sensitive and misses uppercase units like `500G`/`1KG`) caught 8 more that had escaped detection — flagging this as a real gap in the Tier 1 detector itself, not just this category's data. Biggest single finding: 30 pre-existing `source='HUMAN'` keyword-seed catch-all entries (`SKU-002720`–`SKU-002749`/`002738`, all `meta_agent='CLAUDE_CODE'` from an earlier seed pass, confirmed single-category via a `product_taxonomy_map` cross-check — no blast radius outside `shopee_sg_coffee`) had `product_line='Coffee'` (banned generic category word), `size IS NULL` without `is_multi_size` set, and the banned `"(all variants)"`/`"(all X)"` canonical-name suffix — together covering ~1,600 distinct products. Also found: 7 duplicate-brand-token entries (e.g. `"Bacha Coffee Bacha Coffee 12g"`), several dozen entries with leaked multi-size-option noise in `canonical_name`/`product_line` (seller's alternate-size list, e.g. `"...500G/1KG 250g"`, left in verbatim alongside the real extracted size) concentrated in a `Cafe Specialists` naming template (17 near-identical instances) plus assorted one-offs, and one real `pack_count` mis-extraction caught by a units cross-check (MacCoffee 30pcs × 24g = 720g total — recorded as `pack_count=1`/`size=720g` instead of `pack_count=30`/`size=24g`). A first attempt to bulk-mark 35 `BRD-UNDEFINED`-brand rows as `last_verdict='correct'` (reasoning: the Tier 1 field-order check is a structural false positive for `BRD-UNDEFINED`, since `canonical_name` can never start with the literal string `"Undefined"`) was caught before commit as conflating "the flag is a false positive" with "the row has no defect" — several of those 35 had real raw-ad-copy/promotional-junk canonical names independent of the field-order issue; only 13 were genuinely clean, the other 22 had `_meta` reset back to untouched so they resurface for real review next run. | Fixed 81 entries in place (`canonical_name`/`product_line`/`size`/`pack_count` corrected, `meta_agent='CLAUDE_CODE'`, `_meta` reset to unreviewed): 30 catch-alls renamed to `"{Brand} Assorted Coffee Products"` with `product_line=NULL`, `is_multi_variant=TRUE`, `is_multi_size=TRUE` (matches this category's own established Pass-2 catch-all convention); 7 duplicate-brand-token fixes (2 required an image read to recover the real product line/variant — Bacha Coffee "Fine Flavoured Coffee / Tolteca Chocolate", Kimbo Nespresso capsules 55g); 20 excess-content-noise fixes via targeted `REGEXP_REPLACE` bulk statements (17-row Cafe Specialists cluster + 3-row Monkey Beans cluster) plus 5 more of the same defect class caught only by the case-insensitive Tier 1 rerun; 15 individual mechanical cleanups (dangling unclosed parentheses, a stray double-comma, the MacCoffee pack_count/size correction) reached via reading each row's `sku_name`, and 3 via a live product-image read where text alone was ambiguous. Zero new taxonomy entries minted (no SKU block claimed — this was a pure in-place-fix session). 212 entries reviewed with no defect found and marked `last_verdict='correct'` (first-ever review of every one, so all land on `review_confidence='unconfident'` per the design's "one data point isn't confidence" rule, not `'confident'`) — 13 from the corrected `BRD-UNDEFINED` batch, 199 more from a heuristic-assisted manual scan of the top ~300 GMV rows Tier 1 didn't flag (dangling-punctuation/emoji/promo-text/missing-size heuristic, spot-checked against a random sample before trusting it — the spot-check is what surfaced the case-insensitive Tier 1 gap above). 23 lower-priority excess-content rows (~$2–3K combined GMV, versus ~$1.5M+ GMV covered by the fixes above) were reviewed but deliberately left unfixed and untouched in `_meta` per the GMV-triage rule in `docs/quality-standards.md` §1 — they'll resurface in the next incremental worklist. Confidence distribution left behind: 212 `unconfident`, 2,346 `unreviewed` (2,558 total in-category entries) — this was the first `_meta`-tracked pass, so nothing could reach `confident` yet; that requires a second agreeing review. **Hard gates (no `--skip-coexistence`, per this category having already shipped once): G1 dual-mapped=0 ✅, placeholder-leak=0 ✅ (was non-zero before this session's stub-leak fix), structured-fields-missing=0% ✅, G5 provenance=0 ✅. G2 HUMAN+LLM coexistence=614 ❌ — unchanged, pre-existing since the 2026-07-20 Full Rebuild, and out of scope for this script** (needs the dedicated HUMAN-row-cleanup delete, which belongs to the Full Rebuild scenario per `docs/headless-runbook.md`, not Targeted QA Fix). No universe refresh run this session (not in scope per task instructions) — and G2 failing means it shouldn't be, regardless. |
| 2026-07-21 | Targeted QA Fix #2 (auto-discovery, `_meta`-tracked) | Second incremental pass over the same worklist definition (2,558 entries — nothing reached `confident` from pass #1 since that requires two agreeing reviews). Tier 1 SQL sweep (exact `docs/superpowers/specs/2026-07-21-taxonomy-review-loop-design.md` query, including the `canonical_field_mismatch` check added after pass #1's own live run) flagged 266 distinct entries: 176 `is_multi_variant=TRUE` entries whose `canonical_name` ended in a bare `"... Multiple Variants"` suffix with the entry's own known `size` (and `pack_count` where >1) silently dropped from the text — a systematic naming-template bug distinct from pass #1's findings, affecting brands from ILLY, Nescafe, Starbucks, Modern Hippi, Aik Cheong, Suntory, and others; 30 more flagged rows were the pre-existing `"{Brand} Assorted Coffee Products"` catch-alls re-flagged as a **known false positive** of the `canonical_field_mismatch` check (their `sub_line` legitimately doesn't appear verbatim in `canonical_name` by the established catch-all convention — no action needed, confirmed correct); 20 rows flagged `excess_content` (seller's alternate-size/option-list text leaked verbatim into `canonical_name`/`product_line` alongside the real confirmed size, e.g. `"...60g & 120g & 170g Gift 80g"`, `"...50gr 250gr...1kg"`); 42 rows flagged `wrong_field_order`, all `BRD-UNDEFINED`-brand — reconfirmed as the same structural false positive pass #1 already documented (`canonical_name` can never start with the literal string `"Undefined"`), but each was individually inspected rather than bulk-dismissed per pass #1's own caught mistake. A second, real detector gap was found beyond pass #1's case-sensitivity one: `canonical_field_mismatch`'s product-line word-match uses `SPLIT(LOWER(product_line), ' ')`/`UNNEST`, which silently evaluates to `NULL` (not `TRUE`) when `product_line IS NULL` — so a genuine Tier-C stub with `product_line=NULL` (`SKU-100307`, `"Nescafe Multiple Variants"`, no product line, no size at all) passed Tier 1 clean and was only caught by an explicit `product_line IS NULL AND NOT (is_multi_size AND is_multi_variant)` sweep. A third gap, found only via GMV-prioritized Tier 2 sampling (not Tier 1 regex): stray leaked digit/slash fragments from seller size-option lists that don't match a recognized unit pattern (`"25/"`, `"28/"` left over from `"25/30 sticks"`/`"28/30 sticks"`) — Tier 1's `excess_content` regex only checks `\d+\s*(ml|g|kg|l)` and `x\d+` patterns, so a bare digit-slash fragment slips through undetected. | Fixed 200 entries in place (`canonical_name`/`product_line`/`size`/`pack_count`/`is_multi_variant` corrected as needed, `meta_agent='CLAUDE_CODE'`, `_meta` reset to unreviewed): 174 via one bulk `REGEXP_REPLACE` UPDATE re-inserting the dropped `{size}[x{pack_count}]` into every affected `"... Multiple Variants"` name in a single statement; 1 individual fix for the `product_line IS NULL` stub (`SKU-100307`, derived product line directly from `sku_name` text, no image needed); 19 individual `excess_content` fixes (one of the 20 flagged, `SKU-098814`, was deliberately left unfixed — its `canonical_name` noise traces to a bad `brand_dict` entry, `BRD-SG-08837` literally named `"1kg coffee"` instead of the real brand `"TG Bean"` visible in `sku_name`; correcting `brand_dict` is out of scope for this script and belongs to the `brand_mismatch` correction workflow in `docs/brand-extraction.md`, so the entry is flagged here rather than patched around); 2 pack-count corrections in the `BRD-UNDEFINED` bucket cross-validated by arithmetic against `sku_name` (`SKU-102526` "14sac" → `pack_count=14`; `SKU-102862` "13+3 Sticks" buy-13-get-3-free → `pack_count=16`, same-product freebie per §1's rule, not GWP); 4 more real defects surfaced only by GMV-prioritized Tier 2 sampling beyond what Tier 1 flagged — 2 stray-fragment `product_line` cleanups (`SKU-098358` Super, `SKU-098376` OWL, both "`NN/30 sticks`" seller option-list leftovers), 1 pack-count miss confirmed by re-reading `sku_name` ("12 x 10 Servings (Bundle)" → `SKU-098375` Starbucks `pack_count` 1→12), and 1 size confirmed via a live product-image read where `sku_name` had no size signal at all (`SKU-098364` TAG Espresso, box reads "12 CAPSULES" → `size='12 capsules'`). Zero new taxonomy entries minted (no SKU block claimed — pure in-place-fix session, same as pass #1). 115 entries given genuine Tier 2 judgment and found correct, `last_verdict='correct'` (39 from the `BRD-UNDEFINED` wrong-field-order bucket, individually inspected including 3 live image reads for the highest-GMV rows to rule out a hidden size signal; 76 from a GMV-prioritized sample of the top-80-GMV never-reviewed rows, including all 30 `"Assorted Coffee Products"` catch-alls) — of these, 60 had also been judged `correct` in pass #1 and so promoted straight to `review_confidence='confident'`; the remaining 55 are first-ever reviews and land on `unconfident`. One real finding was deliberately left **unfixed and with `_meta` reverted to its pre-session untouched state** rather than marked reviewed-correct: **`SKU-099406`**, a POKKA listing where the buyer picks one of 8 flavors (`Milk Coffee/Milk Tea/Mocha/Cappuccino/Earl Grey/Bandung/Melon Milk/Cookies & Cream`) — 5 of 8 are not coffee at all, so mapping every purchase of this listing to a coffee taxonomy entry is a real scope defect (analogous to the documented "multi-brand buyer-choice listing → leave NULL" pattern), but resolving it means removing the `product_taxonomy_map` row, and this script's task instructions explicitly forbid deleting existing rows — flagged here for a session with that authority, not actioned. Confidence distribution left behind: 60 `confident`, 162 `unconfident`, 2,336 `unreviewed` (2,558 total in-category entries). **Hard gates (no `--skip-coexistence`): G1 dual-mapped=0 ✅, placeholder-leak=0 ✅, structured-fields-missing=0% ✅, G5 provenance=0 ✅. G2 HUMAN+LLM coexistence=614 ❌ — unchanged from both prior sessions, pre-existing since the 2026-07-20 Full Rebuild, still out of scope for this script** (needs the dedicated HUMAN-row-cleanup delete that belongs to the Full Rebuild scenario). No universe refresh run this session (not in scope, and G2 failing means it shouldn't be regardless). |
| 2026-07-21 | Targeted QA Fix #3 (auto-discovery, `_meta`-tracked) | Third incremental pass, worklist = 2,498 entries (162 `unconfident` + 2,336 `unreviewed`; the 60 `confident` from pass #2 correctly excluded). Tier 1 SQL sweep flagged 52 distinct entries: 17 `canonical_field_mismatch` (15 shared a systematic bug — `product_line` carries a redundant trailing `" Coffee"` word never reflected in `canonical_name`, e.g. `product_line="Original 3in1 Coffee"` vs `canonical_name="Nescafe Original 3in1 16.5g Multiple Variants"` — plus 2 more `"... Multiple Variants"` entries where `pack_count` was silently dropped from the text, the same pass-#2 naming bug recurring on rows pass #2 didn't reach: `SKU-099891` Killiney pack_count=3, `SKU-100097` ESSENSO pack_count=4); 29 `wrong_field_order`, all `BRD-UNDEFINED`-brand and individually inspected (not bulk-dismissed, per pass #1/#2's own caught mistake) — 4 were real brand_id misassignments where the true brand is stated plainly in `canonical_name` itself (`SKU-102631`/`SKU-102835` OWL, `SKU-102808` Niska [minted in the 2026-07-20 Top-up session but never applied to these rows], `SKU-102862` "OLDTOWN" no-space variant of Old Town that Stage 03's matcher missed), the rest confirmed as the same structural false positive already documented (`canonical_name` can never start with the literal string `"Undefined"`) covering genuinely brand-unidentifiable reseller listings; 4 `excess_content` (2 genuine bundle/multipack cleanups, 1 garbled multi-variant catch-all simplified to the standard `"{Brand} {Line} Multiple Variants"` template, 1 — `SKU-098814` — reconfirmed as the same out-of-scope bad-`brand_dict`-entry issue pass #2 already flagged, left untouched again); 1 `duplicate_brand` (`SKU-102366` Arabica, confirmed a false positive — "100% Arabica" is a legitimate bean-varietal descriptor, not a naming bug). **The session's largest finding was outside Tier 1's regex entirely**: a live count of the whole worklist found **159 entries with `pack_count IS NULL`** (Tier 1's own sweep only checks pack_count-vs-canonical-text consistency when pack_count is already non-NULL, so a bare NULL slips through undetected — a real gap in the detector, flagged for a future revision of the Tier 1 query). Of these, 89 were long-tail `"{Brand} Assorted Coffee Products (Long-tail Catch-all)"` entries from the 2026-07-20 Top-up session that never got a `pack_count` default (mirroring the *original* 30 `SKU-002720`–`002749` catch-alls fixed in pass #1, which have the identical gap but are already `confident` and thus correctly out of this session's scope); 70 were `is_multi_size=TRUE` "Multiple Variants" entries that, on inspection, had the `is_multi_size`/`is_multi_variant` flags backwards — every one carried a concrete, non-NULL `size` (e.g. `240ml`, `16.5g`, `10 servings`), which per the schema's own rule ("`size` IS NULL only if genuinely multi-size") means `is_multi_size=TRUE` was never correct for these; the real situation is one fixed size with multiple flavor options, i.e. `is_multi_variant=TRUE`. A GMV-prioritized Tier 2 sample of the ~300 highest-GMV never-reviewed entries surfaced two more defect classes Tier 1 doesn't check at all: 14 entries with an **unclosed parenthesis/brace** in `canonical_name` (truncated option-list or promo-text fragments, e.g. `"Segafredo ... capsules (pack of 50"`, `"belmio ... Capsules ( per Box x10"`), one of which (`SKU-102524`) also had a real pack-count miss (`pack_count=1` despite `"pack of 50"` stated in text); and 6 further one-off pack-count/brand defects found by direct reading (`SKU-099401` TNI King Coffee `"100 Sticks"` never reflected in `pack_count`; `SKU-098365` Nescafe `"6 Boxes x6"` redundant double-multiplier; `SKU-098372` a genuine brand mismatch — canonical text plainly reads `"L'OR Variety Collection..."` but `brand_id` was set to ESSENSO, the multi-brand parent-store's own brand entry, not the actual product brand; `SKU-099403`/`SKU-099404` missing `pack_count` for single-box Nespresso-capsule listings; `SKU-099407` TRUNG NGUYEN promo junk `"CHEAPEST!!!"` plus a garbled leaked option-list fragment `"1/2/3/4/5/ 8"`). | Fixed 208 entries in place, all `meta_agent='CLAUDE_CODE'`, `_meta` reset to unreviewed: 15 via one bulk `REGEXP_REPLACE` stripping the redundant trailing `" Coffee"` from `product_line`; 2 individual `"... Multiple Variants"` pack-count restorations; 4 brand_id corrections (OWL ×2, Niska, Old Town); 3 excess-content bundle/catch-all cleanups; 6 further mechanical text fixes (unclosed-paren close, stray "SG SELLER" seller-boilerplate prefix stripped from `SKU-102729`, one real pack-count miss "Combo 3 pek" → `pack_count=3`); **70-row bulk fix** for the `is_multi_size`/`is_multi_variant`-flags-backwards + `pack_count NULL` class — one generated `CASE`-based UPDATE setting `pack_count` (parsed from any explicit `x{N}` multiplier in `canonical_name`, else from a bare `"{N} sticks/capsules/servings/sachets/pcs/cups/counts/boxes"` count-phrase in `size`, else defaulted to 1), `size` (nulled when the count-phrase itself became the extracted `pack_count`), and the corrected `is_multi_size=FALSE`/`is_multi_variant=TRUE` flags, plus a hand-resolved 6-row special-case subset (Kluang Coffee ×3, Indocafe ×2, Nescafe Refill Pack ×1) with nested multiplier text (`"5 Packs x 100 Sticks x 20g"` → `pack_count=500`, `size=20g`); **89-row bulk fix** defaulting `pack_count=1` on the long-tail catch-alls (no other signal exists for these, matching the one already-correct precedent `SKU-099462` "Sin Boon Kee Assorted Coffee Products" `pack_count=1`); 13 individual unclosed-parenthesis cleanups (one, `SKU-102566`, left unfixed — its `brand_dict` entry `BRD-SG-08876` is itself garbage (`"12/+＝"`), the same out-of-scope-bad-brand-entry class as `SKU-098814`); 6 final one-off fixes (`SKU-099401` pack_count 1→100, `SKU-098365`/`SKU-098372` cleanup, `SKU-098372` brand_id corrected to L'Or `BRD-SG-00468` plus pack_count 1→6, `SKU-099403`/`SKU-099404` pack_count 1→20/10, `SKU-099407` promo-junk/garbled-fragment stripped). Zero new taxonomy entries minted (no SKU block claimed — pure in-place-fix session, same as passes #1–#2). 76 entries given genuine Tier 2 judgment and found correct, `last_verdict='correct'` (20 from the Tier 1 false-positive bucket — duplicate_brand and unidentifiable-brand `wrong_field_order` rows individually confirmed as not defects; 56 from a GMV-prioritized read of the top-60 never-reviewed rows) — of these, 19 had also been judged `correct` in a prior pass and promoted to `review_confidence='confident'`; the remaining 57 are first-ever reviews and land on `unconfident`. Two real findings were deliberately left **unfixed and untouched in `_meta`**, same treatment as `SKU-098814`/`SKU-099406` from prior sessions: `SKU-102566` (bad `brand_dict` entry, out of scope for this script) and `SKU-102798` (Torabika — a real, identifiable Indonesian coffee brand with no `brand_dict` entry yet; minting one is judged out of scope for a Targeted QA Fix, which corrects existing-row defects rather than expanding `brand_dict`). `SKU-099406` (POKKA multi-flavor buyer-choice listing, flagged unactionable in pass #2 since fixing it means deleting a map row) reconfirmed unchanged, still not actioned. Confidence distribution left behind: 79 `confident`, 181 `unconfident`, 2,298 `unreviewed` (2,558 total in-category entries). **Hard gates (no `--skip-coexistence`): G1 dual-mapped=0 ✅, placeholder-leak=0 ✅, structured-fields-missing=0% ✅, G5 provenance=0 ✅. G2 HUMAN+LLM coexistence=614 ❌ — unchanged across all three sessions, pre-existing since the 2026-07-20 Full Rebuild, still out of scope for this script.** A live re-check found 31 entries still with `pack_count IS NULL` — the 30 already-`confident` `SKU-002720`–`002749` catch-alls (correctly out of this session's incremental scope) plus 1 more not reached this session; flagged for a future pass rather than reached here, since those 30 require promoting past `confident` status (a design question, not a mechanical fix) to re-enter scope. No universe refresh run this session (not in scope, and G2 failing means it shouldn't be regardless). |
| 2026-07-21 | Targeted QA Fix #4 (auto-discovery, `_meta`-tracked) | Fourth incremental pass, worklist = 2,479 entries (181 `unconfident` + 2,298 `unreviewed`; the 79 `confident` from pass #3 correctly excluded). Tier 1 SQL sweep flagged 25 distinct entries: 17 `canonical_field_mismatch` (10 shared a systematic bug — a seller-signature/dangling-multiplier fragment left in `product_line` from raw `sku_name` text, e.g. `"Kluang Coffee Cap TV White Coffee 25gm x - by Food Affinity"`, never cleaned to just the real product line; 7 Suntory PET/canned-drink entries had `canonical_name` in non-standard `"x N SIZE"` order with a space inside `"x N"`, e.g. `"...x 6 PET 2l Multiple Variants"` instead of the standard `"{size} x{N}"` template); 6 `wrong_field_order` (all `BRD-UNDEFINED`, the same structural false positive documented in passes #1–#3 — `canonical_name` can never start with the literal string `"Undefined"`; each individually inspected, not bulk-dismissed); 1 `brand_casing_mismatch` (`SKU-102835` OWL brand rendered lowercase `"Owl"` in `canonical_name`, plus a dangling promotional `"Free"` word left over from a same-product-freebie pack-count already correctly folded into `pack_count=30`); 1 `excess_content` (`SKU-098814`, the same out-of-scope bad-`brand_dict`-entry issue passes #2–#3 already flagged — left untouched again); 1 `duplicate_brand` (`SKU-102366` Arabica, reconfirmed false positive — "100% Arabica" varietal descriptor). One statement mid-batch hit BigQuery's "could not serialize access... due to concurrent update" (a real, transient conflict, not a bug — confirmed via `updated_at` timestamps that everything before it committed and nothing after it ran) and was safely retried. **Tier 2 (GMV-prioritized sample of the ~100 highest-GMV never-reviewed rows, cross-checked against `sku_name` and — for the highest-value/most-ambiguous cases — live product-image reads) found five defect classes Tier 1's regex cannot see at all, together covering a much larger GMV than Tier 1's own finds:** (1) **The session's biggest single finding**: the category's #1–#3 GMV entries (`SKU-098347` Old Town $1.59M, `SKU-098350` L'OR ESSENSO $712K, `SKU-098352` L'OR Espresso capsules $679K — all JDE World Of Coffee Official Store "Bundle of 4/6 (Assorted Flavours)" listings) had `pack_count` set to the **per-box content count** (13 sticks / 20 sticks / 10 capsules) instead of the actual 4-or-6-box bundle multiplier — confirmed via live product-image reads showing the real "BUNDLE OF 4/6 (Assorted Flavours)" promotional artwork with 5–11 distinct flavor SKUs per box. Resolved per the existing size-range precedent in `docs/llm-extraction-rules.md` §2 ("if ambiguous, use the larger value in the range"): `size` set to the real fixed per-box content, `pack_count` set to 6 (the larger of the 4/6 choice), `is_multi_variant=TRUE` (confirmed assorted-flavor by image). (2) **A "flags-backwards" pack_count/size swap** on single-box Nespresso-capsule listings with no bundle language at all (`SKU-099403` Gran Maestro "20 Aluminium... Capsules" had `pack_count=20`/`size=NULL`; `SKU-102670` belmio and `SKU-099404` Mövenpick had the same pattern) — corrected to `size='N capsules'`, `pack_count=1`, which also **reverses pass #3's own fix to `SKU-099403`/`SKU-099404`** (pass #3 had set `pack_count=20`/`10` for these exact two rows); the category's own established convention for this shape (`SKU-098373` ILLY, `SKU-098357` ILLY, `SKU-098364` TAG Espresso all use `size="N pcs/capsules"` + `pack_count=1` for an unbundled single box) is the majority pattern and the more schema-correct one (`pack_count` = "units per listing" should count boxes/listings, not capsules-within-a-box), so this session's fix supersedes pass #3's inconsistent choice rather than contradicting a still-valid decision. (3) **Missed `is_multi_variant` on assorted-flavor capsule/pod bundles** — five entries (`SKU-098356` Nescafe Dolce Gusto "Bundle of 9", `SKU-098365` Nescafe Dolce Gusto "Bundle of 6", `SKU-098372` L'OR Variety Collection, `SKU-098368` OWL Kopitiam "Bundle of 4", `SKU-099402` The Brew Therapy) had `is_multi_variant=FALSE` despite live product images confirming multi-flavor assortments (Dolce Gusto boxes confirmed 16-capsule-per-box via package text; L'OR Variety Collection confirmed 60-capsules-per-pack; OWL confirmed per-flavor bag counts of 15/20/30, genuinely size-ambiguous per flavor chosen → `is_multi_size=TRUE` alongside `is_multi_variant=TRUE`). Two further entries (`SKU-099411` Little's, listing 8 flavor names by text; `SKU-099413` Little's, 🌈 emoji + "Flavored") caught the same D3 miss from `sku_name` text alone, no image needed. (4) **Real D2/D5 mis-extractions** caught by cross-referencing `sku_name` against the stored fields: `SKU-098429` Killiney's `product_line` read `"Premium Empty Coffee Cup"` — completely the wrong product; `sku_name` plainly reads `"Killiney Premium Kopi-O Kosong"` (a real coffee product, not an empty cup). `SKU-099418` Chang and `SKU-099425` POKKA both had a multiplier stated in `sku_name` (`"30g×10's"`, `"[Wholesale - 24s]"`) that was never carried into `pack_count` (stuck at 1; corrected to 10 and 24). `SKU-099412` Chang had its stated `"600 Gram"` size folded into free-text `product_line` instead of the dedicated `size` field. `SKU-099450` Foresta's `"5's"` pack notation and 4-flavor list were both missed (`pack_count` 1→5, `is_multi_variant` FALSE→TRUE). (5) **Missed multi-variant flavor lists on non-capsule products**: `SKU-099432` Ah Huat ("Kopi O / White Coffee", also had an ambiguous "(Pack of 15/20)" resolved to 20 per the same range-precedent as (1)) and `SKU-099493` Ah Huat ("COFFEE / White Coffee / Kopi O") both had slash-separated flavor lists sitting in `product_line` with `is_multi_variant` never set — corrected to `is_multi_variant=TRUE`, `product_line=NULL` per the established multi-variant convention. Three Bacha Coffee gift-box entries (`SKU-099408` 12 bags, `SKU-099435` 25 bags, `SKU-099439` 12 bags) had an explicitly-stated total bag count sitting in free-text `product_line`/`canonical_name` that had never been captured in the dedicated `size` field — corrected to `size='N bags'` with `product_line` cleaned of the now-redundant count text. | Fixed 44 entries in place, all `meta_agent='CLAUDE_CODE'`, `_meta` reset to unreviewed: 8 Tier-1 product_line-junk cleanups (Kluang Coffee ×3, Indocafe ×2, Nescafe Refill Pack, Kim's Duet ×2); 7 Suntory canonical-name reorder + product_line cleanups; 1 OWL brand-casing + dangling-promo-text fix; 2 misc product_line cosmetic cleanups (Mocca, "Kopi Ratu JRm by Bonda Rozita"); 3 high-GMV JDE "Bundle of 4/6" fixes confirmed by live image read (Old Town, L'OR ESSENSO, L'OR Espresso capsules); 3 flags-backwards pack_count/size swaps (Gran Maestro, belmio, Mövenpick — the last two superseding pass #3's fix); 5 missed-`is_multi_variant` capsule/pod-bundle fixes confirmed by live image read (Nescafe Dolce Gusto ×2, L'OR Variety Collection, OWL Kopitiam, The Brew Therapy) plus 2 more from text alone (Little's ×2); 5 D2/D5 mis-extraction fixes (Killiney wrong-product-line, Chang ×2, POKKA, Foresta); 5 missed-multi-variant/size fixes on non-capsule products (Ah Huat ×2, Bacha Coffee ×3). Zero new taxonomy entries minted (no SKU block claimed — pure in-place-fix session, same as passes #1–#3). 20 entries given genuine Tier 2 judgment and found correct, `last_verdict='correct'` (4 from the Tier 1 false-positive bucket — duplicate_brand, unidentifiable-brand `wrong_field_order` ×2, and a legitimate multi-flavor buyer-choice listing; 16 from the GMV-prioritized top-100 sample) — of these, 8 had also been judged `correct` in a prior pass and promoted to `review_confidence='confident'`; the remaining 12 are first-ever reviews and land on `unconfident`. Several findings were deliberately left **unresolved and untouched in `_meta`** rather than force a low-confidence fix: `SKU-099426` Kanu ("10T / 24T / 30T / 100T", a 10× range too wide to safely resolve via the "use larger value" heuristic that worked for the narrower 4/6 and 15/20 cases above); `SKU-099509` Nespresso, `SKU-098380` Bialetti, `SKU-099660` Nespresso (no count/size signal in `sku_name` at all, and not sampled against image this session); `SKU-102798` Torabika and `SKU-098814` (both reconfirmed unchanged from prior sessions — brand_dict gaps out of scope for this script); `SKU-099406` POKKA (reconfirmed unchanged — still requires a map-row deletion this script cannot perform). Confidence distribution left behind: 87 `confident`, 171 `unconfident`, 2,300 `unreviewed` (2,558 total in-category entries). **Hard gates (no `--skip-coexistence`): G1 dual-mapped=0 ✅, placeholder-leak=0 ✅, structured-fields-missing=0% ✅, G5 provenance=0 ✅. G2 HUMAN+LLM coexistence=614 ❌ — unchanged across all four sessions, pre-existing since the 2026-07-20 Full Rebuild, still out of scope for this script** (needs the dedicated HUMAN-row-cleanup delete belonging to the Full Rebuild scenario). No universe refresh run this session (not in scope, and G2 failing means it shouldn't be regardless). |
| 2026-07-22 | Targeted QA Fix #5 (auto-discovery, `_meta`-tracked) | Fifth incremental pass, worklist = 2,471 entries (171 `unconfident` + 2,300 `unreviewed`; the 87 `confident` from pass #4 correctly excluded — live worklist count matched the prior session's closing tally exactly). Tier 1 SQL sweep flagged 17 distinct entries: 5 confirmed structural false positives (`SKU-102526` Mocca, `SKU-102588` VAVAVOOM, `SKU-102729` Kopi Rock — all genuine `BRD-UNDEFINED` unidentifiable-brand `wrong_field_order` false positives per passes #1–#4's documented pattern, re-verified `brand_dict` has no matching entry for any of the three before accepting; `SKU-102806` a correctly-flagged multi-variant listing; `SKU-099450` Foresta apostrophe/hyphen tokenization false positive matching the "100% Arabica" precedent from pass #3); `SKU-098814` and `SKU-102798` (Torabika) reconfirmed unchanged, same out-of-scope `brand_dict`-gap issues passes #2–#4 already flagged. 9 genuine Tier 1 fixes: `SKU-098372` L'OR product_line trimmed of a verbose Nespresso-compatibility clause inconsistent with the shorter tag convention used elsewhere in-category; `SKU-099404` Mövenpick and (found later via Tier 2) `SKU-099442`/`SKU-099480`, also Mövenpick, had the brand name literally duplicated inside `product_line` (banned per `docs/llm-extraction-rules.md` §3 "never repeat the brand name in product_line") — a defect class no prior pass's Tier 1 regex checks for since it only scans `canonical_name`, not `product_line`, for brand duplication; `SKU-099413` Little's had a leading 🌈 emoji in `product_line`; `SKU-099432` Ah Huat and `SKU-099869`/`SKU-100053` Kim's Duet were missing the `xN` pack multiplier text in `canonical_name` despite a real `pack_count` on the row (the same "Multiple Variants suffix drops the multiplier" bug documented in pass #2, recurring on rows that pass hadn't reached, plus a `"x 24"` vs `"x24"` internal-space formatting defect on the Kim's Duet pair); `SKU-099439` Bacha Coffee had `"FREE SHIPPING"` promo text leaked into `product_line`; `SKU-099869` also had the wrong product name — `product_line`/`canonical_name` read `"Coffee-To-Go Cup"` when `sku_name` plainly states `"Kopi-C To-Go Cup"` (compare `SKU-100053`'s correctly-extracted `"Kopi-O"` sibling entry); `SKU-102549` had promotional/quantity junk `"Combo 3 pek "` prefixed onto `canonical_name`. **Tier 2 (GMV-prioritized sample of the top 150 never-reviewed-or-unconfident rows by GMV, cross-checked against `sku_name`, no live image reads needed this session — every finding resolved from `sku_name` text alone) found 24 defects Tier 1's regex structurally cannot see, the largest class being pack-count multipliers stated in `sku_name` that were never carried into the `pack_count` column** (`SKU-099421` Drip Coffee "10 x 18g" → pack_count 1→10; `SKU-099453` Lipbusycare "10x3g Sachets" → 1→10; `SKU-099466` IN "20 sachets X 30g" (with a separate, correctly-untouched "6 in 1" formula descriptor — a real false-positive-multipack pattern per §1, not a multiplier) → 1→20; `SKU-099467` TRUNG NGUYEN G7 "100 packs / 2g pack" → 1→100; `SKU-099477` Cafe Specialists "4 x 250g" → 1→4; `SKU-099481` King Coffee "100 Sticks * 2.5g" → 1→100, matching the already-correct sibling `SKU-099401` TNI King Coffee convention; `SKU-099447`/`SKU-099451` Dongsuh "13.5g x 50sticks"/"0.9g x 150 sticks" → 1→50/150; `SKU-099456` Malaysia Collection "40g x 15 sachets" → 1→15; `SKU-099483` Suntory "250ml x 24 Bottles" → 1→24). Second defect class: a genuine cross-brand mismatch found by comparing sibling entries — `SKU-099417`/`SKU-099449`, both `"[Wholesale - 24s]"` bottled-drink listings, were mapped to the generic `Nestle` brand_id (`BRD-GLOBAL-00059`) despite `sku_name` literally reading `"Nescafe"` (compare the correctly-brand-specific `POKKA`-mapped siblings `SKU-099444`/`SKU-099478` in the same "Wholesale - 24s" naming cluster) — reassigned to the existing `Nescafe` brand_id (`BRD-SG-00009`) already used by every other Nescafe entry in-category; not a new `brand_dict` mint, in scope per prior sessions' brand_id-correction precedent (OWL, Niska, Old Town, L'Or). Third: missed `is_multi_variant` on a clearly multi-flavor listing — `SKU-099414` Nespresso Opresso ("Coconut/Hazelnut/Crème Brûlée/Lungo/Almond") and `SKU-099461` Kluang Coffee ("Kopi-O/Kopi-C/Kopi CHAM/Coffee Mix", also had a stray leading colon artifact in `product_line` from a brand-name-colon split, cleaned via the established multi-variant `product_line=NULL` convention). Fourth: emoji/promo/dangling-fragment `product_line` junk on 8 further entries (`SKU-099415`/`SKU-099470` Ghostbird Coffee Roasters — 🦉 emoji plus the brand name redundantly repeated inside `product_line`; `SKU-099416` Jamu Ratu Malaya — 🔥 emoji pair plus "Original HQ" marketing text; `SKU-099424` Kanu — "Lowest Price" promo prefix; `SKU-099464` Kanu — dangling "10/" size-range remainder; `SKU-099468` IN — redundant leading "x 24 Carton" fragment duplicating the already-correct trailing pack multiplier; `SKU-099448` Malaysia Collection — "NEW LISTING" promo prefix; `SKU-098388` Moccona — dangling "95/" fragment from a "95/100g" size range already correctly resolved to the larger value, 100g, per §2's range rule). One confirmed non-defect: `SKU-099409` VPP Coffee's `"PPP Coffee {Line}"` naming, initially suspicious as a possible brand-text mismatch, cross-checked against 14 sibling `VPP Coffee` entries in-category and confirmed as VPP's own consistent sub-brand naming convention, not an error — marked `last_verdict='correct'`. | Fixed 33 entries in place, all `meta_agent='CLAUDE_CODE'`, `_meta` reset to unreviewed: 9 Tier 1 fixes (L'Or product_line trim, 3 Mövenpick brand-duplication-in-product_line fixes, Little's emoji strip, Ah Huat + 2 Kim's Duet missing-multiplier/formatting fixes, Bacha Coffee promo-text strip, Kim's Duet Kopi-C wrong-product-name fix, Kopi Ratu promo-prefix strip); 24 Tier 2 fixes (9 pack-count-multiplier-missed-from-text corrections, 2 brand_id corrections Nestle→Nescafe, 2 missed-`is_multi_variant` fixes, 8 emoji/promo/dangling-fragment `product_line` cleanups already itemized above, 3 more not separately named: `SKU-099424` Kanu Lowest-Price strip already counted, `SKU-099464` Kanu, `SKU-099468` IN). Zero new taxonomy entries minted (no SKU block claimed — pure in-place-fix session, same as passes #1–#4). 130 entries given genuine Tier 2 judgment and found correct, `last_verdict='correct'` (5 from the Tier 1 false-positive bucket; 125 from the GMV-prioritized top-150 sample, including `SKU-099409` VPP/PPP) — of these, 99 had also been judged `correct` in a prior pass and promoted to `review_confidence='confident'`; the remaining 31 are first-ever reviews and land on `unconfident`. Three findings were deliberately left **unresolved and untouched in `_meta`**, same treatment as prior sessions: `SKU-098814` and `SKU-102798` Torabika (both reconfirmed unchanged — `brand_dict` gaps out of scope for this script); `SKU-099406` POKKA (reconfirmed unchanged — still requires a map-row deletion this script cannot perform); `SKU-102809` iWhite (new this session — an ambiguous seller listing bundling two different size options for one product with a second, different product (Chocolate Drink) under one multiplier; low GMV (~$70), left for a future session with clearer bundle-decomposition authority rather than force a low-confidence guess). Confidence distribution left behind: 186 `confident`, 80 `unconfident`, 2,292 `unreviewed` (2,558 total in-category entries). **Hard gates (no `--skip-coexistence`): G1 dual-mapped=0 ✅, placeholder-leak=0 ✅, structured-fields-missing=0% ✅, G5 provenance=0 ✅. G2 HUMAN+LLM coexistence=614 ❌ — unchanged across all five sessions, pre-existing since the 2026-07-20 Full Rebuild, still out of scope for this script** (needs the dedicated HUMAN-row-cleanup delete belonging to the Full Rebuild scenario). No universe refresh run this session (not in scope, and G2 failing means it shouldn't be regardless). |
| 2026-07-22 | Targeted QA Fix #6 (auto-discovery, `_meta`-tracked) | Sixth incremental pass, worklist = 2,372 entries (80 `unconfident` + 2,292 `unreviewed`; the 206 `confident` from pass #5 correctly excluded). Tier 1 SQL sweep flagged only 10 distinct entries — the smallest Tier 1 yield of any pass so far, reflecting five prior sessions' cumulative cleanup: 3 confirmed `BRD-UNDEFINED` `wrong_field_order` structural false positives (`SKU-102798` Torabika, `SKU-102526` Mocca, `SKU-102549` Kopi Ratu — same documented pattern as passes #1–#5, `canonical_name` can never start with the literal string "Undefined"); `SKU-098814` and `SKU-102809` (iWhite) reconfirmed unchanged, same out-of-scope issues (bad `brand_dict` entry; ambiguous two-different-products-under-one-multiplier bundle) passes #2–#5 already flagged. 5 genuine Tier 1 fixes, the standout being a real mistranslation caught only by cross-referencing `sku_name`: `SKU-099466` IN's canonical read "...Ginseng, and Cat's Claw..." — the real ingredient, confirmed via `sku_name`'s Malay text "Misai Kucing", is Cat's *Whiskers* (Orthosiphon aristatus), a different herb entirely from Cat's Claw (Uncaria tomentosa); corrected in both `product_line` and `canonical_name`. Also: `SKU-099453` Lipbusycare (product_line trimmed of a marketing-claims tail — "No Additives, Pure Natural, Low Acidity" — not part of the actual product line), `SKU-099467` TRUNG NGUYEN G7 (a genuine formula variant, "No Sugar and Without Milk", moved from a bare parenthetical never reflected in `canonical_name` into the dedicated `variant` field), `SKU-099481` King Coffee (product_line trimmed of redundant "100 Sticks" pack-count text already captured in `size`/`pack_count`), `SKU-099450` Foresta (product_line hyphen/apostrophe formatting normalized to match `canonical_name`). **Tier 2 (GMV-prioritized sample of the top 80 never-reviewed-or-unconfident rows by category GMV, cross-checked against `sku_name` and — for every genuinely ambiguous size/count case — a live product-image read, not just text) found 27 further defects, several only resolvable by looking at the actual package label:** a second real mistranslation, worse than Tier 1's: `SKU-098711` Killiney's canonical read "Premium Empty Coffee Pod Trio Pack" — `sku_name` plainly states "Killiney Premium Kopi-O Kosong Trio Bundle" ("Kopi-O Kosong" = black coffee, no sugar, in Singlish/Malay; "Kosong" = "zero/empty" referring to sugar content, misread as a literal "empty" pod) — corrected to the real product plus `pack_count` 1→3 for "Trio". Three image-confirmed Killiney bundle-structure misses in the same family: `SKU-098500`/`SKU-098524` ("Bundle of 10" bags, each bag's own label reading "30g x15 Sachets"/"40g x15 Sachets" — `size` had been left NULL despite this being legible straight off the package) and `SKU-098616` ("Trio Bundle", `pack_count` stuck at 1 despite the same "Trio"=3 signal already applied correctly elsewhere this session) plus two more of the identical "Bundle of 6" shape only resolved via image (`SKU-098576`/`SKU-098617`, each bag "30g x15"/"40g x15 Sachets" per the box art, `pack_count` corrected 1→6). Five further image-confirmed size fills where `sku_name` had zero count/size signal at all: `SKU-099509` Nespresso-Starbucks ("[AUTHENTIC] Starbucks Nespresso..." — box art confirmed a standard 10-capsule sleeve, also missing `is_multi_variant` for the assortment), `SKU-099660` Nespresso Ispirazione Italiana (same 10-capsule standard, `is_multi_variant` miss, plus "Local Seller" trust-marker boilerplate stripped from `product_line` per §11's promotional-junk-is-not-a-naming-signal principle), `SKU-099413` Little's (box art confirmed 10 capsules across the full flavour range), `SKU-102535` ARK+ (box art states "15 Sachets" plainly), `SKU-098467`/`SKU-098495` Nescafe-Starbucks-by-Dolce-Gusto (box art confirmed 12 capsules — notably *not* the 16 assumed from this category's other Dolce Gusto entries; checked the actual label rather than pattern-matching a sibling). One legitimately-unresolvable case turned into a schema-compliance fix rather than a guess: `SKU-099402` The Brew Therapy (assorted-flavour capsule box, no size stated anywhere and none inferable from the lifestyle-shot image; set `is_multi_size=TRUE` since `size=NULL` with `is_multi_size=FALSE` is itself a structural violation of the "size NULL only via genuine multi-size" rule) and similarly `SKU-100159` Bean Santos (package label showed an unchecked 250g/500g/1000g weight-selector box — genuinely buyer-selectable, `is_multi_size=TRUE` is the correct fix, not a guessed default). Pack-count-from-text misses Tier 1 cannot see (no `pack_count` value to flag inconsistent against): `SKU-099519` Suntory Boss Black ("390ml x 24" plainly in `sku_name`, `pack_count` was 1), `SKU-098363` Nescafe White Coffee Gao Siew Dai ("[Carton] ... (14 x 30g)", `pack_count` 1→14, and `is_multi_variant` corrected false — single flavour, not an assortment), `SKU-098452` ILLY Instant Coffee Sticks ("30s/70s" size-range never resolved into `pack_count`; per §2's range rule used the larger value, 70), `SKU-098556` ILLY iperEspresso ("18ct" sitting in `product_line` text but never carried into the dedicated `size` field). Redundant/promotional text cleanups matching this category's established classes: `SKU-099614` Key Coffee ("x 6 Carton" duplicating already-correct `pack_count`/`size`), `SKU-102530` Chang Jiang (Chinese-language brand restatement "怡保长江白咖啡" plus a redundant English "IPOH CHANG JIANG BRAND" repeat, both stripped, brand appearing once at canonical start is sufficient), `SKU-099138` OWL (dangling "25/" fragment from an already-resolved "25/30 sticks" range), `SKU-099408`/`SKU-099435` Bacha Coffee (marketing tail "Best Gift Choice" stripped from both, redundant with the `Multiple Variants` suffix already conveying the assortment). Two missed-`is_multi_variant` fixes found from `sku_name` text alone: `SKU-099407` TRUNG NGUYEN (a genuine buyer-choice listing across three coffee formats — "Ground Coffee / Trung Nguyen Legend / Drip Bag" — `product_line` nulled per the established Ah Huat multi-variant convention), `SKU-099488` Kanu (7-flavour list plus a "♥Lowest price♥" promo-emoji pair stripped), `SKU-099780` Ah Huat (5 named flavours in `sku_name`, plus a "13-15's" range resolved to the larger value per §2, `product_line` nulled per the same established convention). One real defect newly found but left deliberately unactioned, same treatment as `SKU-098814`/`SKU-102809`/`SKU-099406` in prior sessions: `SKU-102805` "shuang hor" — a garbled bilingual listing that on inspection is a genuine two-different-coffee-brand buyer-choice listing ("Keto UP Coffee" 15-pack vs. "Shuang Hor CEO Coffee" 20-pack under one `product_id`), the same unactionable-without-map-row-deletion shape as `SKU-099406` POKKA; not re-verified this session since it fell outside the sampled 80, presumed still present. | Fixed 32 entries in place, all `meta_agent='CLAUDE_CODE'`, `_meta` reset to unreviewed (5 Tier 1 + 27 Tier 2, all itemized above — no bulk regex UPDATE this session, every fix heterogeneous enough to need its own statement). Zero new taxonomy entries minted (no SKU block claimed — pure in-place-fix session, same as passes #1–#5). 48 entries given genuine Tier 2 judgment and found correct, `last_verdict='correct'` (3 from the Tier 1 `BRD-UNDEFINED` false-positive bucket; 45 from the GMV-prioritized top-80 sample, including the 15 already-correct Pass-2/Pass-1 catch-all and single-box entries re-confirmed against this session's own new conventions). Confidence distribution left behind: 206 `confident`, 78 `unconfident`, 2,274 `unreviewed` (2,558 total in-category entries). **Hard gates (no `--skip-coexistence`, this category having already shipped once): G1 dual-mapped=0 ✅, placeholder-leak=0 ✅, structured-fields-missing=0% ✅, G5 provenance=0 ✅. G2 HUMAN+LLM coexistence=614 ❌ — unchanged across all six sessions, pre-existing since the 2026-07-20 Full Rebuild, still out of scope for this script** (needs the dedicated HUMAN-row-cleanup delete belonging to the Full Rebuild scenario). No universe refresh run this session (not in scope, and G2 failing means it shouldn't be regardless). |
| 2026-07-22 | Targeted QA Fix #8 (auto-discovery, `_meta`-tracked) | Eighth incremental pass, worklist = 2,386 entries (81 `unconfident` + 2,305 `unreviewed` — live count matched pass #7's closing tally exactly). Tier 1 flagged only 35 distinct entries this time (vs. 1,138 in pass #7, since `null_size`/`garbage_brand` were new checks that round and the same-day stub-ban rule had a large one-time backlog): 0 `stub_leak` (pass #7's 279+37-row sweep cleared the backlog), 1 `wrong_field_order` (`SKU-102806`, `BRD-UNDEFINED` with the real brand "Archcafe" never stated in `canonical_name` at all — sku_name showed `[Arch Cafe]`), 5 `excess_content` (4 Killiney nested-multiplier false positives reconfirmed correct, same as pass #7; 1 `SKU-098814`, the recurring bad-`brand_dict`-entry finally investigated this session — see below), 12 `canonical_field_mismatch`, 17 `garbage_brand` (all resolve to the same genuine numeral brand "888", `BRD-SG-06081` — reconfirmed false positives of the letters-only regex, consistent with pass #7). Of the 12 `canonical_field_mismatch` rows, GMV-ranking surfaced the session's biggest find outside Tier 1's own scope: **`SKU-099450` (Foresta-branded, GMV 72,632) turned out to be a `docs/llm-extraction-rules.md` §11 violation** — `canonical_name`/`product_line` ended in "Tooro Forest," the reseller's own co-branding banner text, not anything printed on the actual product boxes (confirmed via image: every box reads only "ZUS COFFEE"), and the taxonomy was mapped to the wrong brand (`Foresta`, `BRD-GLOBAL-00493`) entirely. Also found via **STEP 2b's product-grain promo sweep** (2,734 raw hits, almost all "free" false-positives on "Freeze-Dried"/"free shipping" wording): `SKU-098373` (GMV ~144K across history) — an ILLY "[Duo Set]"/"[Bundle Set]"/"x2 Combo Deal" listing whose title has read "2 boxes" under every historical name change, but `pack_count` was stuck at 1. | Fixed 11 entries in place, all `meta_agent='CLAUDE_CODE'`, `_meta` reset to unreviewed, 2 new `brand_dict` rows minted (no SKU block needed — pure in-place-fix session): **`SKU-099450`** reassigned brand `Foresta`→`ZUS COFFEE` (`BRD-SG-04475`), stripped the "Tooro Forest" reseller-banner text, rebuilt `product_line`/`variant`/`size` from the image (30g sachets ×5, 4 flavors); **`SKU-098373`** `pack_count` 1→2, `size` filled "10 capsules" (image/history-confirmed genuine 2-box bundle); **`SKU-098556`** ILLY iperEspresso: `product_line` was stuffed with the full variant list and a redundant count notation — split into `product_line`="iperEspresso Coffee Capsules" + `variant`="Classico/Intenso/Lungo/Decaffeinated"; **`SKU-099491`** Kimbo: `product_line` had a leading bare count ("10 Aluminum...") duplicating the `size` field — dropped the count, kept in `size` only; **`SKU-098814`** — the "1kg coffee" bad-`brand_dict`-entry left untouched by passes #2–#7 as "out of scope for this script" was finally resolved: real brand per `sku_name` is "TG Bean" (no existing `brand_dict` row), minted `BRD-SG-13398`; **`SKU-102806`** `BRD-UNDEFINED`→existing `Archcafe` (`BRD-SG-03276`), brand prefix added to `canonical_name`; **`SKU-099305`/`099294`/`099321`/`099325`** (TRUNG NGUYEN LEGEND roast-number series): `variant` held long marketing taste-note sentences never reflected in `canonical_name` and redundant with the roast number already in `product_line` — nulled in one bulk UPDATE; `099325` also had a "Filter" vs. sibling "Filtered" typo, corrected; **`SKU-102805`** — a fully unprocessed entry (Chinese source text and reseller naming dumped verbatim into `canonical_name`, brand resolved to gibberish) — image confirmed the actual box brand is "Keto Up" (`Scientific Weight Management` black coffee, "Original" flavor, 15 sticks); minted `BRD-SG-13399`, full field rebuild. 36 entries given genuine judgment and confirmed correct (no fix), `_meta` updated in one bulk statement: 4 Killiney nested-multiplier false positives (2 second-review→`confident`, 2 first-review-after-pass-#7's-wording-fix→`unconfident`), all 17 "888" `garbage_brand` false positives (15 second-review→`confident`, 2 first-review-after-pass-#7's-keyword-strip→`unconfident`), and 15 step2b false positives (Brew Therapy, 3 Modern Hippi "24 cups" siblings, Kluang Coffee, Bacha Coffee, 2 Cheerful Goat gift-set entries [image-checked: genuinely no per-unit size stated anywhere, box shows only an assortment + GWP tumbler], POKKA →`confident` on reaffirm; UCC Craftsman `is_multi_variant` catch-all, Daddy De Clieu, and 4 first-time Modern Hippi siblings →`unconfident`). **New deferred finding:** `SKU-098797`/`SKU-099175` (Punto Italia Espresso "Mint Infusion"/"Green Tea Sencha" Dolce Gusto **Tea** Capsules, low GMV) are genuinely out-of-scope products (tea, not coffee) holding a coffee taxonomy mapping — same "needs map-row-deletion authority this script doesn't have" class as `SKU-102809` (iWhite, reconfirmed unchanged again this session). `null_size` (939 flagged, same order of magnitude as pass #7's 944): not tackled as a dedicated sweep this session — GMV-prioritized budget went to the higher-value canonical/brand defects above instead; still the largest open structural gap, per pass #7's finding, for a dedicated future session. **Hard gates (no `--skip-coexistence`): G1 dual-mapped=0 ✅, placeholder-leak=0 ✅, structured-fields-missing=0% ✅, G5 provenance=0 ✅. G2 HUMAN+LLM coexistence=614 ❌ — unchanged across all eight sessions, still out of scope for this script.** Confidence distribution left behind: 198 `confident`, 65 `unconfident`, 2,295 `unreviewed` (2,558 total). No universe refresh run this session (G2 still failing, and out of scope regardless). |
| 2026-07-22 | Targeted QA Fix #7 (auto-discovery, `_meta`-tracked) | Seventh incremental pass, worklist = 2,352 entries (78 `unconfident` + 2,274 `unreviewed`; the 206 `confident` from pass #6 correctly excluded — live worklist count matched the prior session's closing tally exactly). **This session's Tier 1 sweep added two checks the brief specified but no prior pass had run: `null_size` (D4, `docs/quality-standards.md` §3, wired into Tier 1 for the first time) and `garbage_brand` (a resolved `brand_dict.canonical_name` with no letters at all, per `docs/llm-extraction-rules.md` §11) — both immediately surfaced large, previously-invisible defect classes.** Tier 1 flagged 1,138 distinct entries: **285 `stub_leak`** ("... Multiple Variants"/"... Multiple Sizes" trailing suffix) — not a new defect, but a same-day rule change (`llm-extraction-rules.md`, Jul 22 2026 entry) that unconditionally banned this exact phrasing this category's own passes #2–#6 had repeatedly used as their established convention for multi-variant/multi-size catch-alls, so essentially every prior-session-created catch-all in the category was retroactively out of compliance the moment the rule flipped; **944 `null_size`** (size NULL, is_multi_size not TRUE) — the D4 gap the brief flagged as "never wired into any automated check before now," confirmed live at roughly 40% of the entire incremental worklist, far too large to clear in one session; **19 `garbage_brand`** (`brand_dict.canonical_name` with zero letters) — 15 resolved to `"888"` (`BRD-SG-06081`) and reconfirmed, via cross-referencing `sku_name` across all 15 products, as a genuine (if unusual) numeral brand consistently self-stated at the start of every one of its products' own titles, not a watermark artifact — false positives of the letters-only regex, not real defects; the remaining 4 (`brand_id BRD-SG-08876`, canonical_name `"12/+＝"`) were the exact defect `llm-extraction-rules.md`'s own Jul 22 §11 changelog entry cites (product `7155345414`'s garbled-watermark brand) — on inspection this single garbage `brand_id` actually covered **two different real products from two different real brands** (`SKU-102566` "Cong coffee" per `sku_name`, `SKU-102866` "Min Kaffe" per `sku_name`) that had been incorrectly bucketed together; plus **2 more `garbage_brand`-adjacent `excess_content` rows sharing brand `BRD-SG-06081`** (`SKU-102602`, `SKU-102861`) where the real "888" brand was correct but reseller keyword-stuffing (`"MyDelight Aik Cheong SIN SING Kopi O"`, `"Apache ... Oldshop My Cafe Yee Kong"`) had leaked into `canonical_name`. Also: 5 `excess_content` (4 Killiney "Bundle of N" entries with a legitimate triple-nested nested multiplier `{per-sachet}g x{sachets-per-bag} x{bags}` — confirmed correct, a Tier 1 false positive for the `x\d+` count check, not a real defect — plus `SKU-098814`, the same out-of-scope bad-`brand_dict`-entry issue passes #2–#6 already flagged, left untouched again); 3 `canonical_field_mismatch` (2 Killiney `product_line` wording — "Family Pack" vs. canonical's "Family Bundle" — trivial cosmetic fix; 1 `SKU-102809` iWhite, the same unresolved two-different-products-under-one-multiplier bundle passes #5–#6 already deferred, reconfirmed unchanged); 1 `wrong_field_order` (`SKU-102549` Kopi Ratu, the same `BRD-UNDEFINED` structural false positive documented in every prior pass). **STEP 2b's product-grain promo-language sweep, GMV-ranked, surfaced the session's largest defect class beyond Tier 1's regex: dozens of high-GMV entries where the real pack multiplier (`"30 x 16.5g"`, `"6 x 240ml"`, `"10 x 230ml"`, `"4 x 14g"`, `"x9"`, `"X 24"`, `"x 480sticks"`, etc.) was sitting as dead, unparsed text inside `product_line`/`canonical_name` while `pack_count` stayed at 1** — the same "flags-backwards" pattern documented in earlier passes, but concentrated overwhelmingly in this session's top-50-GMV sample rather than scattered through the tail, meaning this defect class was disproportionately suppressing D5 GMV-weighted accuracy relative to its row count. | Fixed 333 entries in place, all `meta_agent='CLAUDE_CODE'`, `_meta` reset to unreviewed: **279 via one bulk `REGEXP_REPLACE` UPDATE** stripping the now-banned trailing `" Multiple Variants"`/`" Multiple Sizes"` suffix (verified beforehand that every match sat at the true end of string and left substantial real brand+product-line text behind, not a stub); **4 `garbage_brand` fixes** — `SKU-102566` reassigned to the existing `Cong` brand (`BRD-SG-04320`, already in `brand_dict`, no mint needed) with `canonical_name`/`product_line`/`pack_count` rebuilt from `sku_name`; `SKU-102866` reassigned to a newly-minted `Min Kaffe` (`BRD-SG-13395`, SG scope) with the same rebuild; `SKU-102602`/`SKU-102861` kept brand `888` (confirmed correct) but had the reseller keyword-stuffing stripped from `canonical_name`; **15 text-derivable `null_size` fixes** — 5 individual (`SKU-098443` ILLY Duo Bundle `pack_count` 1→2 + `size="18 capsules"`; `SKU-099491` Kimbo, `SKU-099513` AGF, `SKU-102536` Daddy De Clieu, `SKU-102645` KopiHouse all filled `size="N capsules/packs"` matching this category's own established ILLY/TAG-Espresso single-box sibling convention) plus 10 via one bulk `CASE`-based UPDATE (4 TRUNG NGUYEN LEGEND "COMBO 2 Bags... 500gr/340gr Pack" entries corrected `pack_count` 1→2 **and** `size` filled, since the bulk regex had only caught the bag-count text and the real per-bag weight required a follow-up individual `canonical_name`/`product_line`/`variant` rebuild per entry; 6 more `size="N bags/capsules"` fills for Key Coffee, Agf Blendy, UCC, Punto Italia ×2, TRUNG NGUYEN Lingzhi); **2 Killiney `product_line` wording fixes** ("Family Pack"→"Family Bundle" to match canonical, reset to unreviewed rather than marked correct since content changed); **33 pack-count-missed-from-text fixes via one bulk `CASE`-based UPDATE** (Nescafe ×9 cartons, LOTTE CANTATA ×2, Starbucks Premium Coffee Mix Sticks ×4, Lavazza ×4 including a `10 x 10pcs` nested-count pattern resolved to `size="10 pcs"`+`pack_count=10`, TAG Espresso Sampler Bundle, Kirin, Cheerful Goat, luckin coffee ×3, Leone, King Coffee, Indocafe, Malaysia Collection/Coffee Tree Penang, Mister Coffee, Dongsuh KANU, UCC Craftsman [also gained a missed `is_multi_variant=TRUE` for its "Sugar-free/1%/3% sugar" variant list], Cafe21). **A second, category-wide sweep after the worklist-scoped fixes turned up 40 more `" Multiple Variants"/" Multiple Sizes"` entries the hard-gate placeholder-leak check still caught** — these belonged to the 206 already-`confident` entries from pass #6 and were therefore outside this session's normal incremental scope, but `llm-extraction-rules.md`'s own Jul 22 changelog entry for the rule change explicitly states existing entries "need correcting, not exempting," so the same bulk strip was applied to all 37 matching rows (3 of the 40 had already been caught in the worklist-scoped pass) with `_meta` reset — this correctly demoted them from `confident` back to `unreviewed`, since their content changed. Zero new taxonomy entries minted beyond the 1 new `brand_dict` row (`Min Kaffe`) — no SKU block claimed, pure in-place-fix session. 34 entries given genuine judgment and found correct without needing a fix, `last_verdict='correct'` in one bulk `_meta` UPDATE (1 `BRD-UNDEFINED` false positive; 4 Killiney nested-multiplier false positives; 15 "888" garbage_brand false positives; 4 Cafe Specialists size-range false positives; 4 single-size Moccona/Robert Timms false positives; 4 Modern Hippi "N cups" entries already correctly structured; `SKU-099544` Indocafe, already-correct "30 capsules" sibling pattern; `SKU-099439` Bacha Coffee and `SKU-099402` Brew Therapy, both promo-text/no-signal step2b false positives; `SKU-099555` luckin single-cap false positive) — of these, 3 had also been judged correct in a prior pass and promoted to `review_confidence='confident'`; the remaining 31 land on `unconfident`. **The dominant remaining gap, left deliberately unresolved rather than force low-confidence guesses at scale: ~929 of the 944 flagged `null_size` entries (985 category-wide including previously-confident rows) are untouched** — GMV-prioritized triage covered the highest-value, most clearly text-derivable cases only; the bulk of the gap has no per-unit weight/count signal anywhere in `sku_name` and would need either live image reads (infeasible at this volume in one session) or a considered decision on whether the `is_multi_size=TRUE` "no signal available" schema-compliance fallback (established narrowly in pass #6 for 2 individually-investigated cases) is appropriate to apply more broadly — flagged here as a real, large, structural finding for a dedicated future session rather than something to game via a blanket flag flip. Other findings reconfirmed unchanged, same treatment as prior sessions: `SKU-098814` (bad `brand_dict` entry, out of scope for this script), `SKU-102809` iWhite (ambiguous two-product bundle, needs map-row-deletion authority this script doesn't have). Confidence distribution left behind: 172 `confident`, 81 `unconfident`, 2,305 `unreviewed` (2,558 total in-category entries) — `confident` dropped from pass #6's 206 because the 37 category-wide MV/MS-suffix fixes above correctly reset previously-confident rows whose content changed. **Hard gates (no `--skip-coexistence`, this category having already shipped once): G1 dual-mapped=0 ✅, placeholder-leak=0 ✅ (was 40 mid-session before the category-wide MV/MS sweep above — the worklist-scoped fix alone left the gate failing), structured-fields-missing=0% ✅, G5 provenance=0 ✅. G2 HUMAN+LLM coexistence=614 ❌ — unchanged across all seven sessions, pre-existing since the 2026-07-20 Full Rebuild, still out of scope for this script** (needs the dedicated HUMAN-row-cleanup delete belonging to the Full Rebuild scenario). No universe refresh run this session (not in scope, and G2 failing means it shouldn't be regardless). |
| 2026-07-22 | Targeted QA Fix #9 (auto-discovery, `_meta`-tracked) | Ninth incremental pass. Worklist live-queried at 2,360 entries (65 `unconfident` + 2,295 `unreviewed`) — exactly matches pass #8's closing tally, confirming no session ran between #8 and this one. **This session independently re-derived a large fraction of what passes #7 and #8 (both same-day, both already applied by the time this session's worklist query ran) had already found and fixed — this is a continuation of their null_size burn-down, not a new discovery, and is recorded honestly as such:** the ~900-row `null_size` backlog (944→939 across #7/#8), the "888"/`BRD-SG-06081` `garbage_brand` pattern (mostly a real brand, confirmed via `sku_name` cross-reference by #7/#8), and the Killiney nested-multiplier bundles were all re-surfaced by this session's own Tier 1 sweep before this session read the file's own QA History and discovered #7/#8 already existed. Net new work after reconciling against #7/#8: fixed 41 entries in place (13 image-confirmed size/pack_count derivations on rows still null at session start — TAG Espresso, Killiney, Gold Kili, Ghostbird, Cafe21, Gold Roast, Kluang RailCoffee, PPP Coffee, Segafredo, Little's, Opresso, Nespresso; 7 `is_multi_size=TRUE` bulk flags on genuine buyer-choice-of-count listings; 10 "Assorted"-catch-all fixes removing a `FoodCulture SG -` reseller-storefront-name leak from `product_line`/`canonical_name`, a real §11 violation on rows #7/#8 hadn't reached; 6 `canonical_field_mismatch` cleanups; 2 STEP 2b pack_count misses `SKU-100231`/`SKU-102409`; 1 Tier 2 unit-misread fix `SKU-099476` Kanu Mini "120T" sticks read as "120g"). **Two of these fixes supersede specific #6/#7 determinations, both confirmed by live image read this session (neither prior pass had actually looked at these specific SKUs' photos):** (1) `SKU-102861` — #7 kept `brand_id`=`888`/`BRD-SG-06081` for this row based on `sku_name` cross-referencing across its 15-product "888" pattern; this session's image read shows the physical packaging is branded "Apache"/猫山王榴莲白咖啡 (Musang King Durian White Coffee) with "888" appearing only as reseller keyword-stuffing mid-title ("...Oldshop My Cafe Yee Kong 888 Durian White Coffee") — corrected to a newly-minted `BRD-SG-13402` "Apache" (`brand_dict` MAX was `BRD-SG-13401`, clean allocation), per §11's printed-packaging-beats-dominant-token rule; low GMV, not over-invested. (2) `SKU-098576`/`SKU-098617` Killiney family bundles — pass #6 set `pack_count=6` (bag count) and pass #7 independently re-examined the same shape and reconfirmed 6 as correct (a Tier 1 `excess_content` false positive in their read); this session's image read confirms the box art states "30g x 15 Sachets"/"40g x 15 Sachets" **per bag** × "Bundle of 6" bags — per `docs/llm-extraction-rules.md` §2's explicit `x{TOTAL}` rule (not `x90 (15 packs of 6)`), corrected to `size=30g`/`40g`, `pack_count=90`/`90` (total sachets, not bag count). Two passes endorsing 6 without computing the total doesn't make 6 correct. Zero new taxonomy entries minted; one new `brand_dict` row (`BRD-SG-13402` Apache). 148 Tier-2-sampled clean rows (GMV-prioritized, non-flagged) confirmed correct, `_meta` bulk-updated. Findings deliberately left unfixed (unactionable without map-row-deletion authority this script doesn't have, or out-of-scope brand_dict gaps, consistent with `SKU-098814`/`SKU-099406` precedent): `SKU-098797`/`SKU-099175` Punto Italia Espresso Tea Capsules (re-find of #8's identical flag — genuinely non-coffee); `SKU-098958` Jewels Rock Sugar Sticks (new — non-coffee product, also has the same `FoodCulture SG -` merchant-leak pattern). **The single highest-value takeaway for a human, not another incremental pass: nine same-day sessions have now repeatedly nibbled the same ~900-row `null_size` backlog and, twice, re-fixed each other's rows on the same specific SKUs (this session vs. #6/#7 on Killiney; this session vs. #7 on `SKU-102861`) — the incremental-worklist model does not naturally prevent that when passes run back-to-back same-day faster than each one reads the others' QA History rows first.** This needs either (a) one dedicated bulk `null_size` session with a deliberate GMV-cutoff and/or an explicit `is_multi_size` "no-signal-available" fallback policy decision (the schema-compliance approach pass #6 used narrowly for 2 rows), rather than every pass re-sampling the same top-25/50 by GMV, or (b) a process fix requiring each pass to read the two/three most recent QA History rows before running its own Tier 1 sweep. Also flagged: pass #6/#7's remaining Killiney nested-multiplier siblings (`SKU-098500`/`SKU-098524`/`SKU-098616`) still use bag-count and need aligning to `x{TOTAL}` for family consistency. Confidence distribution left behind: 240 `confident`, 121 `unconfident`, 2,197 `unreviewed` (2,558 total in-category entries). **Hard gates (no `--skip-coexistence`): G1 dual-mapped=0 ✅, placeholder-leak=0 ✅, structured-fields-missing=0% ✅, G5 provenance=0 ✅. G2 HUMAN+LLM coexistence=614 ❌ — unchanged across all nine sessions, pre-existing since the 2026-07-20 Full Rebuild, still out of scope for this script.** No universe refresh run this session (not in scope, and G2 failing means it shouldn't be regardless). |

---

| 2026-07-22 | Targeted QA Fix #10 (auto-discovery, `_meta`-tracked) | Tenth incremental pass. Worklist live-queried at 2,318 entries (121 `unconfident` + 2,197 `unreviewed`) — exactly matches pass #9's closing tally, confirming no session ran between #9 and this one (this session read the file's full QA History before running its own Tier 1 sweep, per pass #9's own recommendation to prevent duplicate rediscovery). Tier 1 SQL sweep flagged only 16 distinct entries — smallest yield yet (0 `stub_leak`, 0 `duplicate_brand`, 0 `excess_content`, 0 `brand_casing_mismatch`), reflecting nine prior sessions' cumulative cleanup: 2 `wrong_field_order` — one a genuine new defect class no prior Tier 1 regex check catches (`SKU-099414`, brand resolved to `Nespresso` but `canonical_name`/`product_line` plainly state a different, real third-party compatible-capsule brand "Opresso" — minted new `brand_dict` entry `BRD-SG-13425` and reassigned, same brand_id-correction precedent as OWL/Niska/Old Town/L'Or), one a false positive (`SKU-099415` Ghostbird Coffee Roasters — `brand_dict.canonical_name` itself had a literal double-space typo `"Ghostbird Coffee  Roasters"` causing the `STARTS_WITH` check to fail against the correctly-single-spaced `canonical_name`; fixed the `brand_dict` row directly, a first for this category — no prior pass found a brand_dict-level formatting bug). 1 `garbage_brand` (`SKU-102602`, "888" — reconfirmed the same real-numeral-brand false positive passes #7–#9 already documented). 12 `canonical_field_mismatch`, mostly false positives from tokenization/whitespace artifacts (hyphen/slash split tokens, `"sugar-free / 1%"` vs `"sugar-free/1%"` spacing) matching the established "100% Arabica" precedent, plus genuine fixes: 5 entries (TAG Espresso ×2, Gold Roast, Cafe21, KLUANG RailCoffee) had **multiple historical `sku_name` variants' marketing subtitles concatenated together in `product_line` with `" | "` separators** — a defect class not seen by name in any prior pass's QA History, though the shape resembles pass #9's `FoodCulture SG -` merchant-leak finding; trimmed each to the real product line and corrected `is_multi_variant` (was wrongly `TRUE` on plain single-flavor products); KLUANG RailCoffee additionally had the brand name redundantly repeated in `product_line` (banned per `llm-extraction-rules.md` §3) — the real `"3 in 1 | 2 in 1 | Powder"` tail was a genuine format-choice variant, promoted into the `variant` field instead of discarded. Little's had a similar flavor-list-stuffed-in-`product_line` shape, cleaned the same way. `SKU-099420` Nespresso "All Flavors / 38 Flavors" wording removed per the same-day (2026-07-22) unconditional ban on "All X"/"Multiple X" stub phrasing even under a correctly-set `is_multi_variant=TRUE` flag. `SKU-100231` Nescafe had a confusing `"x 20"` fragment baked into `product_line` inconsistent with the row's own `pack_count=40`; trimmed. `SKU-102536` Daddy De Clieu had a trivial word-order/missing-"Coffee" gap versus `product_line`, cosmetic fix. **STEP 2b's product-grain promo sweep, GMV-ranked and cross-checked against the established long-tail-catch-all exclusion (pack_count=1 is correct-by-convention for `"{Brand} Assorted Coffee Products"` entries and was not re-flagged), found the session's largest defect class: 20 entries with an explicit pack multiplier stated in `sku_name` (`"Buy 5 Free 1"`, `"Twin Pack"`, `"2 x 500g"`, `"10 x 10pcs"`, `"X 24"`, etc.) never carried into `pack_count`** — same recurring "flags-backwards" pattern documented in passes #5/#7/#9, this session concentrated in ILLY (Buy-5-Free-1 same-product-freebie → pack_count 6, per §1's rule), Lavazza (nested `"10 x 10pcs"` → `pack_count=10`/`size="10 pcs"`), ESSENSO L'OR (`"Twin Pack"` → pack_count 2), Indocafe (`"2 Packs x 100 Sticks x 20g"` nested count → `pack_count=200`), Cafe Specialists (5 entries, `"4 x 250g"`/`"2 x 500g"`), and Nestle/POKKA (`"x 24s"`/`"X 24"` bottled-drink cartons). A GMV-prioritized Tier 2 sample of the top 120 never-reviewed-or-unconfident rows beyond what Tier 1/2b caught found 8 further defects, the highest-value being `SKU-098370` Nescafe Starbucks-by-Dolce-Gusto — `sku_name` states `"[Bundle of 6/9]"`, a live image read confirmed the actual box art reads "BUNDLE OF 9" with 9 distinct-flavor Starbucks boxes visible (House Blend, Cappuccino, Caffè Latte, Caramel Macchiato, Matcha Latte, Espresso Roast) — `pack_count` NULL→9, `is_multi_variant` FALSE→TRUE, matching the JDE "Bundle of 4/6" precedent from pass #4 (use the image-confirmed value, not the ambiguous text range). A third and fourth "Kosong" mistranslation recurrence (distinct from the Killiney instances passes #6/#7 already fixed, confirming this bug isn't Killiney-specific): `SKU-098653` Killiney "Premium Empty Coffee Filter Family Pack" → real product per `sku_name` is "Premium Kopi-O Kosong Family Bundle" (no such thing as a bundled "coffee filter" being sold this way); `SKU-098743` Cap Televisyen/Kluang TV Coffee, same "Empty" → "Kosong" fix, plus marketing-junk-pipe cleanup. `SKU-098697` Killiney "2-in-1 ... Trio Bundle" had `pack_count` stuck at 1 despite the exact same "Trio"=3 signal already applied to sibling `SKU-098616`/`SKU-098711` in passes #6/#9 — corrected for consistency. `SKU-099464` Kanu had a stray "10/" range-remainder still sitting in `canonical_name` even though `product_line` itself had already been cleaned (apparently in a prior pass) — the two fields had drifted out of sync; rebuilt `canonical_name` to match. `SKU-099780` Ah Huat's `canonical_name` contained "Ya Fa White Coffee" — not a wrong product as it first appeared, but a redundant Chinese-name restatement of the brand itself (亚发 = "Ya Fa" = the Chinese rendering of "Ah Huat"), the same redundant-bilingual-brand-repeat class as pass #8's Chang Jiang fix — stripped. `SKU-099535` Kanu had a garbled, unparseable `product_line` mixing a genuine 3-way roast variant (Mild/Dark/Decaf) with a 5x-wide quantity range (30T/100T/150T) matching pass #4's already-documented "too wide to safely resolve" `SKU-099426` precedent — cleaned the variant into proper fields but deliberately left `pack_count` unresolved rather than guess across a 5x range. Two `is_multi_variant` corrections with no accompanying content change (`SKU-098349` Nescafe Gold jar, `SKU-099502` Suntory Boss 2L — both wrongly flagged multi-variant with no evidence of an actual buyer-choice flavor list). | Fixed 38 entries in place, all `meta_agent='CLAUDE_CODE'`, `_meta` reset to unreviewed: 2 brand_id/brand_dict corrections (Opresso mint+reassign, Ghostbird Coffee Roasters brand_dict double-space typo); 8 marketing-junk/brand-repeat product_line cleanups (TAG Espresso ×2, Gold Roast, Cafe21 ×2 counting the STEP 2b Cafe21 pack-count find, KLUANG RailCoffee, Little's, Nespresso "All Flavors" stub removal); 2 cosmetic canonical_name fixes (Daddy De Clieu, Nescafe SKU-100231); 20 STEP 2b pack-count-from-text corrections (ILLY Buy-5-Free-1 ×2, ILLY Combo x2, Lavazza nested-count, ESSENSO L'OR Twin Pack ×2, Indocafe nested-count, Cafe Specialists ×5, Nestle, POKKA); 8 Tier 2 GMV-sample fixes (Nescafe-Starbucks-Dolce-Gusto image-confirmed Bundle-of-9, 2 Killiney Kosong-mistranslation fixes, Killiney Trio-Bundle pack-count consistency fix, Kanu stray-fragment canonical_name resync, Ah Huat redundant-Chinese-name strip, Kanu garbled-variant cleanup, plus the two is_multi_variant-only corrections counted separately below). 2 further Tier 2 GMV-sample corrections with no text change (SKU-098349, SKU-099502 is_multi_variant FALSE-fix). Zero new taxonomy entries minted (no SKU block claimed — pure in-place-fix session, same as all nine prior passes); one new `brand_dict` row (`BRD-SG-13425` Opresso). 87 entries given genuine Tier 2 judgment and found correct, `last_verdict='correct'` in one bulk `_meta` UPDATE (3 Tier 1 false-positive bucket; 84 from the GMV-prioritized top-120 sample, including established-correct catch-all entries and the ILLY `"Classico | Intenso | Forte | Lungo"` pipe-delimited flavor-list convention reconfirmed as legitimate, not marketing junk) — of these, several had also been judged `correct` in a prior pass and promoted to `review_confidence='confident'`. Findings deliberately left **unresolved and untouched in `_meta`**, same treatment as prior sessions: `SKU-098797`/`SKU-099175` Punto Italia Espresso Tea Capsules (reconfirmed unchanged — genuinely non-coffee, needs map-row-deletion authority this script doesn't have); `SKU-099406` POKKA multi-flavor listing and `SKU-099426` Kanu 10×-wide range (both reconfirmed unchanged from prior passes' documented reasoning). The ~905-row `null_size` backlog (Tier 1 count this session) remains the dominant open structural gap, unchanged in character from passes #7–#9's repeated finding — not tackled as a dedicated sweep this session; GMV-prioritized budget went to the higher-value canonical/pack-count/brand defects above. Confidence distribution left behind: 290 `confident`, 98 `unconfident`, 2,170 `unreviewed` (2,558 total in-category entries). **Hard gates (no `--skip-coexistence`, this category having already shipped once): G1 dual-mapped=0 ✅, placeholder-leak=0 ✅, structured-fields-missing=0% ✅, G5 provenance=0 ✅. G2 HUMAN+LLM coexistence=614 ❌ — unchanged across all ten sessions, pre-existing since the 2026-07-20 Full Rebuild, still out of scope for this script** (needs the dedicated HUMAN-row-cleanup delete belonging to the Full Rebuild scenario). No universe refresh run this session (not in scope, and G2 failing means it shouldn't be regardless). |
| 2026-07-23 | Targeted QA Fix #11 (auto-discovery, `_meta`-tracked) | Eleventh incremental pass. Worklist live-queried at 2,268 entries (98 `unconfident` + 2,170 `unreviewed`) — matches pass #10's closing tally exactly, confirming no session ran between #10 and this one. Tier 1 SQL sweep flagged 942 distinct entries: **906 `null_size`** (still the dominant open gap, consistent in scale with passes #7–#10's repeated finding — confirmed via a bucketing check that most of these are genuine single-product extraction misses, not legitimate multi-size catch-alls, so `is_multi_size=TRUE` cannot be used as a blanket dodge); **15 `wrong_field_order`**, all Ghostbird Coffee Roasters — pass #10 fixed the underlying typo in `brand_dict.canonical_name` itself ("Ghostbird Coffee  Roasters" double space), but that fix didn't retroactively correct the same double-space already baked as static text into these 15 rows' own `canonical_name` at extraction time — collapsed via one bulk `REGEXP_REPLACE`; **1 `garbage_brand`** (`SKU-102602`, "888" — reconfirmed via a fresh image read of the actual packaging, same real-numeral-brand false positive passes #7–#10 already documented); **20 `canonical_field_mismatch`** — mostly false positives from punctuation/spacing tokenization (dash-vs-no-dash, hyphenated compound words, slash-spacing variants, "N Caps/Box" vs "N capsules" wording), but 12 genuine fixes: 4 entries (ILLY ×2, ESSENSO L'OR ×2) had a real multiplier (`pack_count` 2 or 6) never carried into `canonical_name` — appended `xN`; ILLY Instant Coffee 95g had `pack_count=2` with zero corroborating signal in `sku_name` or image (single can shown) — corrected to 1; 4 Cafe Specialists 500g entries had `pack_count=2` derived from a `"- 500g / 1kg"` size-option-list in `sku_name` misread as a 2-unit multiplier (same false-positive class as "N options to choose from") — image confirms a single 500g bag, corrected `pack_count` to 1; 1 entry had `"N x {size}"` written backwards vs. the template (`"4 x 250g"` → `"250g x4"`); 2 entries (Opresso, Cafe21) had a flavor list / marketing tagline stuffed into `product_line` instead of `variant`/left out — cleaned. **A GMV-prioritized image-read sample of the top 11 `null_size` entries (of 906) found 5 with a legible, fixable size**: Gold Kili 2-in-1 (pack_count 1→26, size="17g", the bundle-of-4 promo photo's per-sachet weight, not the promo bag count), Jonetsu Kakaku Drip Pack (size="8g" per bag, `pack_count` text-stated 24 kept over the illustration-disclaimer image's "20袋入"), Hai-O Min Kaffe Garam Buluh (nested multiplier — 20 sticks/box × 2 boxes on the box photo — `pack_count` 2→40, size="12g"), Kluang Cap Televisyen (size="10g" per sachet, also cleaned a duplicated-brand-text/empty-parenthesis artifact in `canonical_name` found while fixing), CappaRomA (nested multiplier — 25 sachets/bag × 3 bags per the "x3" badge — `pack_count` 3→75, size="18g"). The other 6 sampled were left untouched (no `_meta` write) — genuinely illegible or ambiguous from the fetched image (bundle-of-6/9 multi-flavor choice with no per-box count shown, jar/box weight text too small to read confidently, "Pack of 15/20" buyer-choice with illegible per-sachet grams) — no guessing. **STEP 2b's product-grain promo-language sweep** (re-run with an explicit `month` filter and product-level dedup after the raw query first returned 67k fanned-out rows — the documented query lacks both, only usable after adding them) surfaced 8 further real pack_count misses among top-GMV `pack_count=1` rows, all following the same "multiplier stated in `sku_name` but left as dead text in `product_line`/`canonical_name` instead of the structured field" pattern documented in every prior pass: Nescafe Latte Can ("5+1"/"6x240ml" → 6), Nescafe GOLD 3in1 ("20 x 24g" → 20), Suntory Boss Rainbow ("30 Cans" embedded in `product_line` itself → cleaned + 30), Kopi Singapore Gao Siew Dai ("14 x 24g" → 14), TRUNG NGUYEN G7 ("100 packs x" dangling in `product_line` → cleaned + 100), Key Coffee Liquid Coffee ("x 6" leading `product_line` → cleaned + 6), Malaysia Collection Penang White Coffee ("x 15 sachets" trailing `product_line` → cleaned + 15), Ghostbird Special Bundle ("2 x 500g" in `sku_name`, `canonical_name` already verbose/messy from raw scraped text — minimal fix, `pack_count` 1→2 and appended `x2`, did not attempt a full text cleanup of that entry's messier product_line, out of this finding's scope). Zero new taxonomy entries minted, no SKU block claimed — pure in-place-fix session, same as all ten prior passes. No new `brand_dict` rows needed. 9 entries given genuine Tier 2 judgment and found correct, `last_verdict='correct'` in one bulk `_meta` UPDATE (the 888 garbage_brand row plus 8 canonical_field_mismatch false positives: ILLY ×3, TAG Espresso, Punto Italia ×2, Little's, KLUANG RailCoffee, UCC Coffee). Findings deliberately left unresolved, no `_meta` write (Tier-1-flagged but outside this session's sampling/judgment budget, remains exactly as eligible next run): ~895 of the 906 `null_size` entries not sampled this session — the single largest remaining structural gap in this category, unchanged in character from passes #7–#10's repeated finding; a dedicated future session should decide between (a) a much larger image-read budget targeting the GMV-ranked tail, or (b) a considered, non-blanket policy for when `is_multi_size=TRUE` is legitimate for a single-product entry with no recoverable size signal anywhere. Confidence distribution left behind: 293 `confident`, 101 `unconfident`, 2,164 `unreviewed` (2,558 total in-category entries). **Hard gates (no `--skip-coexistence`, this category having already shipped once): G1 dual-mapped=0 ✅, placeholder-leak=0 ✅, G5 provenance=0 ✅. G2 HUMAN+LLM coexistence=614 ❌ — unchanged across all eleven sessions, pre-existing since the 2026-07-20 Full Rebuild, still out of scope for this script** (needs the dedicated HUMAN-row-cleanup delete belonging to the Full Rebuild scenario). No universe refresh run this session (not in scope, and G2 failing means it shouldn't be regardless). |
| 2026-07-23 | Targeted QA Fix #12 (auto-discovery, `_meta`-tracked) | Twelfth incremental pass. Worklist live-queried at 2,265 entries (matches pass #11's closing tally of 2,268 minus the 3 rows pass #11's own `_meta` bulk-update promoted). Tier 1 SQL sweep flagged only 9 distinct entries — `stub_leak`=0, `duplicate_brand`=0, `wrong_field_order`=0 (the 15-row Ghostbird double-space backlog pass #11 collapsed stayed collapsed), `brand_casing_mismatch`=0, `excess_content`=0, `garbage_brand`=0, all 9 flags on `canonical_field_mismatch`: 2 genuine defects, both a recurrence of the emoji/duplicate-brand-in-`product_line` class first named in pass #9's `FoodCulture SG -` finding — `SKU-099569` Ghostbird ("Special Bundle 2 x 🦉 GHOSTBIRD COFFEE..." — pass #11 had already fixed this row's `pack_count`/`canonical_name` but explicitly deferred the messier `product_line` cleanup as "out of this finding's scope"; completed that deferral now, stripping the 🦉 emoji and the redundant "GHOSTBIRD COFFEE" brand repeat) and `SKU-102543` Kluang Cap Televisyen (pass #11 cleaned the duplicated-brand-text/empty-parenthesis artifact in `canonical_name` only, per its own note — `product_line` still carried the same "Kluang Coffee Televisyen ... ( Pack) ..." artifact untouched; cleaned now). 7 confirmed false positives, all tokenization/convention artifacts matching precedents passes #1–#11 already established: `SKU-098384` TAG Espresso and `SKU-102536` Daddy De Clieu (hyphen/word-order tokenization, "100% Arabica"-class false positive), `SKU-099414`/`SKU-099469`/`SKU-099411` (variant-list-not-verbatim-in-`canonical_name` convention for `is_multi_variant=TRUE` catch-alls, established since pass #2), `SKU-098797`/`SKU-099175` Punto Italia Espresso Tea Capsules (fourth consecutive reconfirmation — genuinely non-coffee, still needs map-row-deletion authority this script doesn't have). **STEP 2b's product-grain promo sweep** (month-filtered + product-deduped per pass #11's fix) returned 1,363 raw hits — far too many to review individually, since the bare `x\s*\d` term in the shared regex matches size tokens embedded in already-correct single-unit entries (e.g. "12 capsules", "10 pcs") as often as real multipliers; filtered programmatically to rows where `sku_name` contains an explicit `N x {size-unit}` or `[N Pkts]` bundle pattern AND `pack_count` is still 1, yielding 24 unambiguous genuine misses across the top-400-GMV slice: 6 Nescafe "[Carton] ... (N x Wg)" cartons (Mocha Can, Kopi C Gao Siew Dai, 2in1 Zero Sugar, White Coffee Hazelnut, Gold Latte Can, Latte 24×240ml), 2 Lavazza Nespresso-compatible "10 X 10pcs" capsule boxes (`pack_count`→10, `size`="10 pcs", matching the exact pass #10 precedent recurring on two different SKUs pass #10 didn't reach), 2 Gold Roast "[N Pkts] ... x M Sachets x P" nested bundles, LOTTE CANTATA Box (10×230ml), Ghostbird Filter Taster Pack (3×100g, same emoji/dup-brand `product_line` defect as the Tier 1 finds above — cleaned in the same statement), Kopi/OWL/Old Town ×2/Cafe21/Ye Ye/Starbucks Matcha single-sachet cartons, Indocafe Cappuccino nested "2 Packs X 50 X 25g" (→ `pack_count`=100 total sachets, not 2), Oriental Kopi Drip Bag (10×18g — `product_line`'s trailing reseller-keyword-stuffed tail left untouched, flagged not fixed, see below), Lipbusycare "10x3g Sachets" (`product_line` emoji-stripped in the same statement). One candidate in the same slice, `SKU-099697` (title starts "Dragon Fruit Brand..." but resolves to `Ah Huat` brand with "...Chang Jiang Super Old Town White Coffee..." also in the name — looks like a genuinely ambiguous multi-brand-reseller-title case, not a simple multiplier miss) was deliberately **not** touched — too risky to guess a brand/product identity from a garbled reseller title without an image read this session's budget didn't reach; left for a future pass, no `_meta` write. **A targeted image-read batch of 11 top-GMV `null_size` entries** (of 901 in this session's Tier 1 count, essentially unchanged in scale from pass #11's 906 — confirmed this category's `raw_niq_history` table doesn't exist for SG at all, unlike the TH regex-backfill precedent in `llm-extraction-rules.md` §2, so the size-extraction chain for this category is text→image only, no `product_specification`/`product_description` fallback) found exactly 1 legible fix: `SKU-099432` Ah Huat "Kopi O / White Coffee (Pack of 15/20)" — box art confirms "15 x 20g 300g" printed on the pack, `size`="20g" filled (pack_count stays 20, the already-correct larger-of-range value). The other 10 (Nescafe/Starbucks Dolce Gusto "Bundle of 6/9", Jamu Ratu Malaya Kopi Ratu, Ghostbird Owlsome Drip Bag, Famous/K34PI-branded "434 Sai Kee" 100-bag jar, 4× PARCHMEN & CO. specialty blends, Kanu Maxim Latte, Killiney Classics Collection) had no legible per-unit weight anywhere in the fetched image — promotional banner shots with brand/flavor art but no readable net-weight text — left untouched, no guessing, no `_meta` write. One text-only fix outside the image batch: `SKU-099567` TRUNG NGUYEN G7 "(Wholesale) ... Box of 50 Packets" — `size` filled "50 packets" (single-box convention, `pack_count` stays 1, matching the established "size='N pcs/capsules', pack_count=1" pattern for unbundled single-box listings). **Tier 2**: GMV-prioritized sample of the top 100 never-reviewed-or-unconfident rows *not* flagged by Tier 1, cross-checked against `docs/llm-extraction-rules.md` in full (including §11) and `quality-standards.md` §3's D1–D5 — 99 confirmed correct (all Tier-A complete: real product line, size, pack_count, and correctly-set `is_multi_size`/`is_multi_variant` flags on the buyer-choice/flavor-list catch-alls), reflecting eleven prior sessions' cumulative cleanup of this category's highest-GMV entries. 1 reconfirmed pre-existing defect, **not** re-fixed this session (same treatment as passes #9–#11): `SKU-099406` POKKA — `canonical_name` lists 8 distinct flavors (`Milk Coffee / Milk Tea / Mocha / Cappuccino / Earl Grey / Bandung / Melon Milk / Cookies & Cream`) as one entry with `is_multi_variant=FALSE`, and at least two of those flavors (Milk Tea, Earl Grey) are tea, not coffee, per this category's own documented POKKA scope edge case — same unactionable-without-map-row-authority class as `SKU-098797`/`SKU-099175`, reconfirmed unchanged for a fourth consecutive session (passes #9, #10, #11, #12). Fixed 27 entries in place (`canonical_name`/`product_line`/`size`/`pack_count` corrected as needed, `meta_agent='CLAUDE_CODE'`, `_meta` reset to unreviewed): 2 Tier 1 emoji/duplicate-brand `product_line` cleanups, 2 image/text-derived `size` fills, 23 STEP 2b pack-count-from-text corrections (listed above; several bundled a `product_line` cleanup in the same statement where the dangling multiplier fragment was already sitting as dead text). 99 entries given genuine Tier 2 judgment and found correct, `_meta` bulk-updated in one statement (several promoted to `review_confidence='confident'` on a second consecutive `correct` verdict). Zero new taxonomy entries minted, no SKU block claimed — pure in-place-fix session, same as all eleven prior passes. No new `brand_dict` rows needed this session. Confidence distribution left behind: live-queried at 5,384 `confident` / 1,062 `unconfident` / (row-grain figures, fan out across `product_taxonomy_map`; distinct-`taxonomy_id` worklist remaining = 2,203 of 2,558 total in-category entries — down 62 from this session's start, the gap between 126 rows touched and 62 net worklist reduction reflecting that a `_meta` reset-to-unreviewed (the 27 fixes) or first-time-correct (most of the 99) keeps a row in-worklist for one more confirming review, by design). The ~900-row `null_size` backlog remains the dominant open structural gap, essentially unchanged in scale across passes #7–#12 (944→939→906→901) — each session's image-read sampling budget only reaches a low single-digit fraction of it, and roughly half of the sampled top-GMV rows this session had no legible size anywhere in the fetched image at all (not a sampling-budget problem, a genuine data-availability ceiling for this category, compounded by the missing SG `raw_niq_history` fallback). Restating pass #11's recommendation, now with two additional sessions' evidence: a dedicated future session should decide between (a) a much larger image-read budget targeting the GMV-ranked tail, or (b) a considered, non-blanket policy for when `is_multi_size=TRUE` is the honest answer for a single-product entry with no recoverable size signal anywhere. **Hard gates (no `--skip-coexistence`, this category having already shipped once): G1 dual-mapped=0 ✅, placeholder-leak=0 ✅, G5 provenance=0 ✅. G2 HUMAN+LLM coexistence=614 ❌ — unchanged across all twelve sessions, pre-existing since the 2026-07-20 Full Rebuild, still out of scope for this script** (needs the dedicated HUMAN-row-cleanup delete belonging to the Full Rebuild scenario). No universe refresh run this session (not in scope, and G2 failing means it shouldn't be regardless). |
| 2026-07-23 | Targeted QA Fix #13 (auto-discovery, `_meta`-tracked) | Thirteenth incremental pass, worklist live-queried at 2,203 entries (matches pass #12's closing tally exactly). Tier 1 SQL sweep flagged only 8 `canonical_field_mismatch` (`stub_leak`/`duplicate_brand`/`wrong_field_order`/`brand_casing_mismatch`/`excess_content`/`garbage_brand` all 0), `null_size`=899 (essentially flat vs. pass #12's 901). All 8 flags reconfirmed as tokenization/convention false positives matching prior-session precedent (`SKU-098384` TAG Espresso, `SKU-102536` Daddy De Clieu, `SKU-099414`/`SKU-099469`/`SKU-099411` variant-list-not-verbatim convention, `SKU-098797`/`SKU-099175` Punto Italia Espresso Tea Capsules — fifth consecutive reconfirmation of the same non-coffee unactionable defect, still needs map-row-deletion authority this script doesn't have — plus `SKU-099567` TRUNG NGUYEN G7, re-flagged only because pass #12's own fix left the size text non-verbatim in `canonical_name`, reconfirmed correct). **Major finding, not previously caught in twelve prior passes**: brand_id `BRD-SG-06662` ("IN", SGD 4,798 GMV — this category's entire rank-55 brand bucket, 219 products across 35 taxonomy entries) turned out to be almost entirely a text-corruption artifact — every affected `canonical_name`/`product_line` had the substring "in" silently stripped (case-insensitive, mid-word) at some point upstream of this review loop, mangling "Instant"→"stant", "Cappuccino"→"Cappucc o", "King"→"K g", "Drink"→"Dr k", "Caffeine"→"Caffe e", "3 in 1"→"3 1", etc. — and the resulting corrupted fragments appear to be why brand resolution ever produced a spurious 2-letter "IN" brand at all (created 2026-06-05, no real product on Shopee is branded "IN"; every one of the 34 non-catch-all products under it is actually a distinct real manufacturer — Wonda, Coffee Tree, MEVSO, 3:15 PM, Seattle, Hei Hwang, SIN SING, UCC, Maxim/Dongsuh, Indocafe, Kluang Coffee, London Coffee, Hicomi, Lam Fong, BEE UNCANG, Kopi Luwak, Super/Super Power/Super Ayu, Senyum, Malaysia Melaka Old Shop). Rebuilt all 34 non-catch-all entries' `canonical_name`/`product_line`/`size`/`pack_count` from the real, uncorrupted `sku_name` in `master_clean_niq` (never actually corrupted at the source — only the derived taxonomy fields were). Reassigned `brand_id` to the correct **existing** `brand_dict` entry for the 9 cases with a confident match (no new brands created, kept scope tight): `SKU-099468`→UCC Coffee (`BRD-SG-01673`), `SKU-099792`→Dongsuh/Maxim (`BRD-SG-02832`), `SKU-099595`→Super (`BRD-SG-13379`, the same brand created in this category's own Pass 1), `SKU-098949`→Kluang Coffee (`BRD-SG-06813`), `SKU-102399`→Indocafe (`BRD-SG-00471`), `SKU-099928`/`SKU-100041`/`SKU-100214`→Hei Hwang (`BRD-SG-07194`), `SKU-100184`/`SKU-100218`→Coffee Tree (`BRD-SG-13393`) — all 7 target brand_ids already existed in `brand_dict` pre-session, meaning correct entries existed but these products were never routed to them. The remaining ~24 entries (Wonda, MEVSO, Seattle, SIN SING, Kopi Luwak, 3:15 PM, London Coffee, Hicomi, Lam Fong, BEE UNCANG, etc.) have no existing `brand_dict` match and low individual GMV (mostly <SGD 100, several SGD 0) — left on `BRD-SG-06662` with corrected text only, not reassigned; new-brand creation for ~15 distinct names is a bigger Stage-02/03 undertaking better scoped to a dedicated brand-cleanup session, flagged here as the top recommendation for one. The catch-all `SKU-102464` ("IN Assorted Coffee Products") was left untouched (already `review_confidence=confident`) but one of its 185 mapped products was observed to be a wholegrain cereal snack, not coffee — a scope-contamination data point noted, not investigated further (zero GMV, catch-all population auditing is out of this session's budget). **STEP 2b** (product-grain promo sweep, refined this session to require an explicit `N x {size-unit}` pattern rather than the bare `x\s*\d` term, which over-matched embedded size tokens in already-correct entries): found and fixed 26 more genuine pack-count misses beyond the IN-brand cluster — 9 top-GMV (Starbucks Caffe Mocha 4×22g, LOTTE CANTATA 10×230ml, Indocafe White Coffee 90×12g nested "3 Pack X 30 X 12g", Old Town O Kosong 20×12g, Indocafe Original Blend 30×20g, Nescafe Coffee Can Carton 24×240ml — also had reseller-store-name "Stop and Compare Supermarket" stripped from `canonical_name`/`product_line` per §11, ESSENSO 20×25g, OWL Kopi-O 30×17g, BATTUTA Cold Brew 4×40g), 17 Lavazza official-store entries fixed via a single regex `UPDATE` extracting the `N x Wg`/`N x Wpcs` pattern already sitting cleanly in each `canonical_name` (all zero-GMV this month but Rule-B in-scope as official-store listings — a systematic Pass-1 miss on this brand's case-pack listings), and 10 more (2× Richboy, 4× Nescafe, 2× Starbucks, Tan Kim Hock, one GWP-adjacent Nescafe Gold "2x200g + Free 2x50g" resolved as pack_count=2 on the paid quantity only) requiring a fresh `sku_name` lookup since the multiplier wasn't preserved in `canonical_name` at all. One ambiguous case (`SKU-099697` Dragon Fruit Brand/Ah Huat/Old Town garbled multi-brand reseller title) reconfirmed deliberately untouched, same as pass #12. **Targeted null_size image-read batch**: fetched and read images for the top 15 GMV `null_size` entries (of 899); 13 were promotional/collage/multi-variant marketing images with no legible single-unit weight (several exact repeats of pass #12's own "no legible size" list — Nescafe/Starbucks Dolce Gusto bundle, Ghostbird Owlsome, Famous 434 Sai Kee, PARCHMEN & CO. blends, Kanu Maxim Latte, Killiney Classics — reconfirming this is a genuine data-availability ceiling, not a sampling-budget problem); 2 resolved legitimately: `SKU-099497` AGF Maxim jars clearly printed "80g" (`size` filled), `SKU-099510` Fortune Coffee Club Lucky Blend pack art explicitly reads "200G / 500G / 1KG" (a genuine size-selector, not a single product — `is_multi_size=TRUE` set instead of guessing one number, the first time this category has taken option (b) of pass #11/#12's own recommendation rather than deferring it again). **New §11 sweep**: searched the full worklist for merchant/reseller-name leakage (`mart|supermarket|official store|trading|wholesale|distributor` etc.) beyond the two instances STEP 2b's fix already caught, and found 8 more: `SKU-099399` (Dongsuh Maxim White Gold — "By Hanguk Kitchen Korean Food Mart" stripped, brand reassigned from a mismatched `maxime` (`BRD-GLOBAL-00224`, an unrelated global brand) to `Dongsuh`), `SKU-099630` (second LOTTE Cantata SKU with the identical "Korean Food Sarangmart" leak as `SKU-099543`), `SKU-102574`/`SKU-102680` ("DND Official Store" — brand itself is genuinely `DND`, `BRD-SG-04202`, only the "Official Store" channel-descriptor text needed stripping), `SKU-102682` ("HQ SG Seller)" — confirmed `HQ`, `BRD-SG-05907`, is itself a single-entry, likely-spurious brand created from a corrupted seller-location tag, same failure class as the "IN" finding above but isolated to one low-GMV product; text cleaned, brand left as-is, flagged for the same future brand-cleanup session), 3× Emart No Brand duplicate-wording cleanups (`BRD-GLOBAL-02849` "Emart No Brand" confirmed a legitimate Korean private-label brand, not a reseller leak — left brand unchanged). Two more corrupted-text entries found and fixed while reviewing the Tier 2 sample's tail: `SKU-099603` TRUNG NGUYEN (dangling "4 - / Bag -" fragment) and `SKU-099609` KLUANG RailCoffee (duplicated brand mention + dangling "8S x -" fragment). Two brand-identity questions surfaced but deliberately left unresolved this session (low information, would need image reads or deeper brand_dict research): `SKU-099839` Arabica-brand-tagged MeeDee/Soy/Nuts bundle (brand mismatch, real brand likely "MeeDee" — a TH-scoped `Meedee` entry exists but SG scope unconfirmed) and `SKU-099943`/`SKU-100069`/`SKU-100256` "Arabica ... by Space Roastery" (possible brand should be "Space Roastery", not "Arabica" — no existing `brand_dict` entry to reassign to). **Tier 2**: GMV-prioritized sample of 116 never-reviewed-or-unconfident rows not flagged by Tier 1 (widened from the usual ~100 given this session's turn budget), cross-checked against `docs/llm-extraction-rules.md` in full (including §11) and `quality-standards.md` §3's D1–D5 — 101 confirmed correct (including the 6 reconfirmed Tier-1 false positives above) and bulk-`_meta`-updated in one statement; 2 reconfirmed pre-existing unactionable defects left untouched (`SKU-099406` POKKA mixed coffee/tea flavors, fifth consecutive reconfirmation across passes #9–#13; `SKU-099426` Kanu Maxim Latte, same non-coffee-flavor-in-catch-all class, newly identified this session — Matcha is not coffee). Fixed 85 entries in place this session across all defect classes above (`meta_agent='CLAUDE_CODE'`, `_meta` reset to unreviewed on every one); 101 entries given genuine Tier 2 judgment and found correct. Zero new taxonomy entries minted, no SKU block claimed. One new `brand_dict` fact established (no new rows created): confirmed 7 target brands for the IN-cluster reassignment already existed. Confidence distribution left behind: live-queried at 5,427 `confident` / 1,076 `unconfident` / (row-grain, fans out across `product_taxonomy_map`); distinct-`taxonomy_id` worklist remaining = 2,162 of 2,558 total in-category entries — down 41 from this session's start. **Hard gates (no `--skip-coexistence`): G1 dual-mapped=0 ✅, placeholder-leak=0 ✅, G5 provenance=0 ✅, structured-fields-missing=0% ✅. G2 HUMAN+LLM coexistence=614 ❌ — unchanged across all thirteen sessions, pre-existing since the 2026-07-20 Full Rebuild, still out of scope for this script.** No universe refresh run this session (not in scope, and G2 failing means it shouldn't be regardless). **Recommendation for a future session**: a dedicated brand-cleanup pass to (a) create/assign real brand_dict entries for the ~24 remaining IN-cluster products (Wonda, MEVSO, Seattle, Kopi Luwak, 3:15 PM, SIN SING, etc.), (b) investigate whether `HQ` (`BRD-SG-05907`) and `Arabica` (`BRD-SG-09596`, for the Space Roastery/MeeDee cases) have the same corrupted-text-origin problem as `IN`, and (c) the ~900-row `null_size` backlog, which remains the dominant open structural gap and a genuine data-availability ceiling for this category (no SG `raw_niq_history` fallback exists). |
| 2026-07-23 | Targeted QA Fix #14 (auto-discovery, `_meta`-tracked) | Fourteenth incremental pass. Worklist live-queried at 2,160 distinct entries (2,069 `unreviewed` + 91 `unconfident`) — close to pass #13's closing tally of 2,162, small drift from ordinary map-row churn. Tier 1 SQL sweep: `stub_leak`/`duplicate_brand`/`brand_casing_mismatch`/`excess_content`/`garbage_brand` all 0; 2 `wrong_field_order` (`SKU-099468` UCC Black Muto Kuro, `SKU-102573` UCC Craftsman — both reconfirmed false positives: `brand_dict.canonical_name`="UCC Coffee" but the real product naming convention correctly omits the redundant second "Coffee" since the product name itself already ends in "...Coffee", same pattern established in prior passes); 16 `canonical_field_mismatch`, of which 9 were genuine and fixed — `SKU-099126` (stray "x3" fragment + "Siu Tai" misspelling removed from `product_line`, corrected to "Kopi O Gao Siew Dai"), `SKU-099414` (brand name "Opresso" redundantly repeated in `product_line`, stripped), `SKU-099497` (canonical_name was missing its own already-correct `size`="80g" — pass #13 filled the field but never propagated it into the text, fixed here), `SKU-099567` (duplicate trailing "- Instant Black Coffee" phrase removed from `product_line`), `SKU-100111`/`SKU-102350` (IN Sin Sing Kopi O: trailing "Halal ..." descriptor clause trimmed from `product_line`, not reflected in canonical), `SKU-099768` (garbled duplicate "Korean Instant Mix Coffee" fragment removed), `SKU-100167` (IN Malaysia Melaka Old Shop White Coffee: `variant` field held a 5-way selector list "Classic / Non-Sugar / Kopi-O / Durian / Teh Tarik" that was never a genuine single variant — reclassified `is_multi_variant=TRUE`, `variant` cleared), `SKU-100218` (generic "Instant Coffee" suffix dropped from `product_line` per the no-generic-category-word rule). The other 7 (`SKU-099106`, `SKU-099175`, `SKU-098797`, `SKU-099609`, `SKU-099520`, `SKU-099341`, `SKU-098948`) reconfirmed as tokenization/punctuation/language false positives (hyphenation, parenthetical formatting, CJK text not mirrored in canonical, or genuinely ambiguous multi-variant/multi-size wording not worth a risky guess) — left untouched, `_meta` marked reviewed+correct alongside the 2 `wrong_field_order` false positives (9 rows total). **`null_size` deep-dive**: 905 entries flagged (vs. pass #13's 899 — worklist churn, not regression). New structural finding: 99 of the 905 already have `is_multi_variant=TRUE` set but are still flagged, because the Tier 1 `null_size` definition only exempts `is_multi_size=TRUE`, never `is_multi_variant=TRUE` — a real gap in the checker itself, not necessarily a data defect, since a multi-variant selector can legitimately lack one single size (flagged for a future Tier 1 query revision, not fixed here since some multi-variant entries may still share one determinable size and blanket-exempting would hide real misses). Fetched and read live product images (`curl` + `Read`, per `docs/headless-runbook.md`'s image-fetch pattern) for the top 25 GMV `null_size` entries: 8 Killiney bundle/multi-pack entries had a clearly legible per-sachet net weight on official packaging and were bulk-fixed via one `UPDATE` — Premium Kopi-O Kosong=15g (`SKU-098653`, `SKU-098711`), Premium Kopi-O Siew Dai=22g (`SKU-098510`, `SKU-098540`, `SKU-098620`), 3-in-1 Premium White Coffee=40g (`SKU-098616`, `SKU-098769`, `SKU-098775`); `SKU-099566` AGF Maxim Freeze-Dried Instant Coffee jars all confirmed 80g across blends (size filled). Two more resolved via reclassification rather than a guessed number: `SKU-099488` Kanu Maxim Latte New Flavor (image showed flavor variants ship in different stick counts — 8/24/30 — genuinely multi-size on top of its existing `is_multi_variant=TRUE`, so `is_multi_size=TRUE` set instead of forcing one number) and `SKU-102693` Alicafe (image caught a real product-line defect beyond just missing size — canonical said "5 In 1" but the actual product, confirmed by both `sku_name` and packaging, is "3 In 1"; also a 5-flavor/weight-range selector per the pack's own "418-540g / 15-20 SACH" legend, so `product_line`/`canonical_name` corrected to "3 In 1", `is_multi_size=TRUE` and `is_multi_variant=TRUE` set, and the spurious `pack_count=18` — a mis-parsed fragment of the sku_name's "15/18's" — cleared to NULL). The remaining 15 of the top-25 batch had no legible single-unit weight in their listing image (promotional collage/bundle-lifestyle shots, e.g. Nescafe/Starbucks Dolce Gusto capsule boxes, IN London Coffee Enhanz sticks, Ah Huat/Lao Qian/Trung Nguyen multi-variant shelf photos, Little's Nespresso-capsule wholesale bundle) — left untouched, `_meta` not touched (unresolved defects, not false positives, per this script's own rule against fabricating a review verdict). Net this session: 11 `null_size` entries resolved (9 with a real size value, 2 via legitimate `is_multi_size` reclassification), ~894 remain — still the dominant open gap, consistent with passes #7–#13's repeated finding that this is a genuine data-availability ceiling (no SG `raw_niq_history` table exists for this category, confirmed live this session — `product_specification` fallback is unavailable) rather than an extraction-effort gap; closing it further requires either more image-read budget per session or accepting a lower D4 ceiling for image-illegible capsule/stick-pack listings. STEP 2b (pack_count-vs-promo-language sweep) and a broader Tier 2 GMV sample were not run this session — turn budget went to the `null_size` root-cause investigation and Tier 1 fixes instead; flagged as next session's priority alongside the `is_multi_variant` Tier 1 gap. 20 taxonomy entries fixed in place this session (`meta_agent='CLAUDE_CODE'`, `_meta` reset to unreviewed on every one); 9 entries given genuine Tier 2 judgment and confirmed correct (bulk `_meta` update). Zero new taxonomy entries minted, no SKU block claimed. **Hard gates (no `--skip-coexistence`, per this session's explicit instruction that coexistence is a genuine bug post-ship): G1 dual-mapped=0 ✅, placeholder-leak=0 ✅, G5 provenance=0 ✅. G2 HUMAN+LLM coexistence=20 ❌** — down sharply from pass #13's reported 614 (likely partial cleanup in an intervening unlogged session or map-row churn since HUMAN rows aren't touched by this script); still nonzero and still out of scope for this script to fix (no HUMAN-delete authority here per `docs/headless-runbook.md`'s Targeted QA Fix scope) — flagged for the wrapper/a Full-Rebuild-scoped session to run the documented duplicate-HUMAN-row delete. No universe refresh run this session (G2 failing, and out of scope regardless). |
| 2026-07-23 | Targeted QA Fix #15 (auto-discovery, `_meta`-tracked) | Fifteenth incremental pass. Worklist live-queried at 2,160 entries (matches pass #14's closing tally exactly, confirming no session ran between #14 and this one — read the full QA History before running Tier 1, per pass #9's standing recommendation). Tier 1 SQL sweep flagged 903 distinct entries: **894 `null_size`** (pass #14's own live-image root-cause finding reconfirmed: no SG `raw_niq_history` table exists for this category, so this is a genuine data-availability ceiling, not an extraction-effort gap — not re-attacked as a dedicated sweep this session per pass #14's explicit hand-off, which flagged STEP 2b and a broader Tier 2 GMV sample as this session's priority instead); **8 `canonical_field_mismatch`** + **2 `wrong_field_order`** — 2 genuine fixes (`SKU-099566` AGF Maxim: `canonical_name` never carried the `size="80g"` pass #14 itself had filled — appended; `SKU-099567` TRUNG NGUYEN G7: pass #14's own product_line fix left a stale duplicate `" - Instant Black Coffee"` suffix on `canonical_name` never cleaned up, plus the `size` field ("50 packets") didn't match the text at all — rebuilt to `"...50 Packets"`), 8 reconfirmed as the same tokenization/punctuation/CJK-text/multi-variant-list false positives passes #7–#14 have repeatedly documented for these exact rows (`SKU-098948`, `SKU-099106`, `SKU-099414`, `SKU-099468`, `SKU-099609`, `SKU-102573` — the latter two both a `UCC Coffee` brand-name-contains-generic-word artifact not previously named by brand but consistent with the established "duplicate-brand-avoidance" pattern, `SKU-099520`, `SKU-099341`). **STEP 2b (product-grain promo/multiplier sweep, not run since pass #11) surfaced this session's largest new defect class**: a `"Cap Televisyen"/"Kluang Coffee"` nested-multiplier family (`"{N} sachets/sticks x {M} packs"` in `sku_name`) where `pack_count` had been left at 1 across 12 entries despite the true total being N×M — the same shape as the Killiney fix from pass #9, but on a different brand family never checked before. Two were image-confirmed for a true per-unit gram weight (`SKU-098890`: 40g×15 sticks×5 packs → `size="40g"`, `pack_count=75`; `SKU-098999`: 10g×80 sachets×2 packs → `size="10g"`, `pack_count=160`, also resolving a same-product_id historical title discrepancy between "80 bags" and "100 bags" — image confirmed 80 is correct); the remaining 10 had no per-unit weight anywhere in `sku_name`/title and were fixed via the total-count-as-size fallback already established in-category by `SKU-098743` (`size="{total} sachets/sticks"`, `pack_count=1`), avoiding a full image-read pass on every sibling. Also via STEP 2b: 6 `Cafe Specialists` 100%-Arabica-beans entries and `POKKA`/`Nestle`/`Suzuki` RTD-can entries had an explicit `"2 x 500g"`/`"24 x 500ml"`/`"X 24"` multiplier stated in `sku_name` (three of the four RTD entries even had it *already written into `canonical_name`* as free text) never carried into the structured `pack_count` field — corrected all 10 (`pack_count` 1→2 or 1→24). A GMV-prioritized Tier 2 sample of the top 60 not-yet-flagged rows found 5 further defects beyond STEP 2b's catch: `SKU-099406` POKKA 8-flavor listing had `is_multi_variant=FALSE` despite `canonical_name` literally enumerating 8 distinct flavors — corrected to `TRUE`; `SKU-099432` Ah Huat "Kopi O / White Coffee" and `SKU-099780` Ah Huat "White Coffee" both had `product_line` left blank while the same text lived only in `canonical_name` — the same "decomposition never happened" gap `headless-runbook.md`'s `structured_fields_missing` gate exists to catch, backfilled both; `SKU-099488` Kanu "Maxim Latte New Flavor" had the identical blank-`product_line` gap, backfilled; `SKU-099158` Pod Labs Nespresso-compatible capsules had `size="100 capsules"`/`pack_count=10` (implying 1,000 total) but a live image read of the actual product (10 boxes stacked, each box printed "10 CAPS. NET WT. 57g") confirmed the true total is 100 capsules — corrected to `size="10 capsules"`, `pack_count=10`. One ambiguous finding reviewed and deliberately left unfixed, `_meta` marked reviewed+correct per the established `SKU-098797`/`SKU-102809` precedent (needs map-row-deletion authority this script doesn't have): `SKU-102805` "Keto Up" — its single mapped product's `sku_name` is actually a buyer-choice listing between two unrelated products ("Keto Up...15包" / "Shuang Hor CEO CAFE COFFEE 1box20s") bucketed under one taxonomy entry; kept the existing primary-brand-first interpretation rather than reassign or split without map-row-deletion authority. Also noted but not touched (brand_dict consolidation is explicitly an "ongoing process" per `ARCHITECTURE.md`, out of this script's scope): the Kluang-family entries resolve across three distinct `brand_dict` rows for what appears to be one real-world brand ("Cap Televisyen", "Kluang Coffee", "Kluang Coffee Cap Televisyen") — flagged for a future brand_dict dedup pass, not merged here. Fixed 29 entries in place this session, all `meta_agent='CLAUDE_CODE'`, `_meta` reset to unreviewed (12 Kluang-family nested-multiplier corrections, 10 Cafe-Specialists/POKKA/Nestle/Suzuki pack_count corrections, 2 canonical_field_mismatch fixes, 5 Tier-2-sample fixes). Zero new taxonomy entries minted, no SKU block claimed, no new `brand_dict` rows — pure in-place-fix session, same as all fourteen prior passes. 59 entries given genuine Tier 2 judgment and confirmed correct, `last_verdict='correct'` in one bulk `_meta` UPDATE (51 from the GMV-prioritized top-60 sample plus 8 Tier-1 false-positive reconfirmations, including `SKU-102805` above). The ~894-row `null_size` backlog remains the dominant open structural gap, unchanged in character and confirmed cause (no SG `raw_niq_history` table) from passes #7–#14's repeated finding — still needs either a dedicated large image-read budget or a considered `is_multi_size` fallback policy decision, not another incremental nibble. Confidence distribution left behind: 435 `confident`, 71 `unconfident`, 2,052 `unreviewed` (2,558 total in-category entries). **Hard gates (no `--skip-coexistence`, this category having already shipped once): G1 dual-mapped=0 ✅, placeholder-leak=0 ✅, G5 provenance=0 ✅. G2 HUMAN+LLM coexistence=614 ❌ — unchanged across all fifteen sessions (pass #14's one-time reported value of 20 appears to have been transient map-row churn, not a real fix — reconfirmed back at 614 this session), pre-existing since the 2026-07-20 Full Rebuild, still out of scope for this script** (needs the dedicated HUMAN-row-cleanup delete belonging to the Full Rebuild scenario). No universe refresh run this session (not in scope, and G2 failing means it shouldn't be regardless). |

---

## Targeted QA Fix Brief

> Not applicable yet — no LLM rows exist prior to this session. Populate after this run's QA gate results if
> a follow-up Targeted QA Fix is needed.

---

## Scripts

Extraction performed directly by the Claude Code session (own multimodal reads), not via `pipeline/05_product_taxonomy/` scripts.

---

## Map Row Counts (as of last run)

| Source | Count | Notes |
|--------|-------|-------|
| LLM | 9,265 | 1,122 Pass 1 (official store) + 7,786 Pass 2 (reseller, 96 in-scope brands) + 357 Top-up (2026-07-20 coverage session) |
| HUMAN | 1,571 | Pre-existing keyword seed, not deleted this session (see QA History) |
| NULL (unmapped) | ~7,317 | Out-of-scope brands / below-threshold long tail / 11 confirmed-OOS products (medical nutrition drinks, milk tea, brewing equipment — see 2026-07-20 Top-up QA History entry) |

GMV coverage across the full table (2026-06): **99.02%** (10,193 / 17,510 products mapped by taxonomy_id,
GMV-weighted, updated 2026-07-20 Top-up session — was 93.85% before this session). Exceeds the ≥85% Pass 2
target in `docs/quality-standards.md` §6.
