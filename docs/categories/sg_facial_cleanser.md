# shopee_sg_facial_cleanser — Category Context

> Master table name is `shopee_sg_facial_cleanser` (the `shopee_{country}_{category}` convention
> used everywhere else in this pipeline — the shorthand `sg_facial_cleanser` used in STATUS.md and
> task prompts is not the literal table name).

---

## Status

| Field | Value |
|-------|-------|
| LLM Pass 1 | ⏳ Partial — top-200-by-GMV official-store products (~78% of allowlist official-store GMV), 194 taxonomy entries. Remaining ~1,400 allowlisted official-store products (long tail, ~5% of allowlist GMV) not yet covered — see Remaining Work below. |
| LLM Pass 2 | ⏳ Partial — bulk SQL text-match against the 194 Pass 1 entries, scoped to the 92 brands with Pass 1 entries only (not the full 249-brand scope). 944 products routed. |
| GMV Coverage (new LLM this session) | 15.3% of total category GMV (2026-05-01) newly mapped by LLM (1,144 products, $1.11M SGD of $7.26M SGD total). This is in addition to whatever the pre-existing HUMAN keyword-seed already covered — not independently re-measured this session. |
| Last run | 2026-07-16 |
| Current MAX taxonomy_id (at session start) | SKU-058455 |

---

## Important: this master_table spans 5 magpie categories, not just "cleanser"

`niq_category_mapping` shows `shopee_sg_facial_cleanser` maps to **five** distinct
`magpie_category_3` buckets, all under Beauty & Personal Care → Skincare:

| NIQ category_3 | magpie_category_3 | Products (2026-06 latest month) | GMV (2026-06) |
|---|---|---|---|
| Facial Serum & Essence | Serum & Essence | 34,942 | 3,740,006 |
| Facial Cleanser | Face Cleanser | 19,781 | 1,411,182 |
| Acne Treatment | Acne Treatment | 7,535 | 469,758 |
| Face Scrub & Peel | Facial Scrub | 8,278 | 173,666 |
| Facial Oil | Face Oil | 2,064 | 92,705 |

Unlike `shopee_th_body_wash` (which mixes in genuinely out-of-scope content like hand wash),
**all five sub-categories here are legitimate skincare** — there is no keyword-exclusion gate
needed analogous to body_wash's "exclude hand wash" rule. Taxonomy and mapping for this table
covers all five. Universe refresh must use the NIQ join pattern (not a hardcoded `category_3 = `
filter) per `CLAUDE.md`'s multi-category guidance — and per `docs/headless-runbook.md`, the actual
refresh target is the `universe_taxonomy_overlay` MERGE, not `magpie.marketshare_universe` (see
Taxonomy Design Notes below).

---

## SKU Blocks Assigned

| Block | Usage |
|-------|-------|
| SKU-070001–072000 | Full Rebuild (Pass 1 + Pass 2), claimed 2026-07-16, scenario=full_rebuild |

---

## Brand Scope (GMV threshold 95%, review month 2026-05-01, GWP zeroed, category-relevant products only)

**249 brands** in the true 95% cumulative-GMV threshold (computed by ranking all brands by
GWP-zeroed product-level GMV descending and taking a running cumulative sum — not a fixed top-N
snapshot). `BRD-UNDEFINED` (unresolved brand, 5.86% of category GMV, 13,291 products at rank 1)
is excluded from this brand list since it cannot have an official store or a real product line —
Pass 2 / NULL-coverage passes may still resolve individual high-GMV BRD-UNDEFINED products via
image/text brand identification, but it isn't a "brand" for allowlist purposes.

Full list, ranked by GWP-zeroed GMV (SGD), with cumulative % of total category GMV:

| Rank | Brand | brand_id | GMV (SGD) | Cumulative % |
|---|---|---|---|---|
| 1 | Cosrx | `BRD-GLOBAL-00109` | 239,432 | 10.59% |
| 2 | Skin1004 | `BRD-GLOBAL-00108` | 233,499 | 15.21% |
| 3 | Torriden | `BRD-GLOBAL-00011` | 217,768 | 19.52% |
| 4 | Paula's Choice | `BRD-GLOBAL-00251` | 159,053 | 22.66% |
| 5 | Rejuran | `BRD-GLOBAL-00335` | 148,700 | 25.6% |
| 6 | medicube | `BRD-GLOBAL-00299` | 146,550 | 28.5% |
| 7 | SK-II | `BRD-GLOBAL-00215` | 138,239 | 31.24% |
| 8 | The Ordinary | `BRD-SG-00010` | 135,971 | 33.92% |
| 9 | Cetaphil | `BRD-GLOBAL-00069` | 113,837 | 36.18% |
| 10 | ARENCIA | `BRD-GLOBAL-00464` | 100,149 | 38.16% |
| 11 | Skintific | `BRD-GLOBAL-00026` | 99,229 | 40.12% |
| 12 | La Roche-Posay | `BRD-GLOBAL-00002` | 95,717 | 42.01% |
| 13 | Innisfree | `BRD-GLOBAL-00070` | 82,347 | 43.64% |
| 14 | Sulwhasoo | `BRD-GLOBAL-00122` | 74,761 | 45.12% |
| 15 | d'Alba | `BRD-GLOBAL-00013` | 71,776 | 46.54% |
| 16 | Shiseido | `BRD-GLOBAL-00072` | 66,298 | 47.85% |
| 17 | IEM | `BRD-SG-00435` | 58,027 | 49.0% |
| 18 | Alluora | `BRD-SG-00567` | 57,807 | 50.14% |
| 19 | VT COSMETICS | `BRD-GLOBAL-00406` | 57,280 | 51.27% |
| 20 | Cerave | `BRD-GLOBAL-00006` | 56,693 | 52.39% |
| 21 | Celimax | `BRD-GLOBAL-00764` | 56,162 | 53.5% |
| 22 | Estee Lauder | `BRD-GLOBAL-00139` | 52,207 | 54.54% |
| 23 | TRUU | `BRD-SG-00875` | 47,505 | 55.48% |
| 24 | Dr. Althea | `BRD-GLOBAL-00071` | 43,593 | 56.34% |
| 25 | Laneige | `BRD-GLOBAL-00128` | 43,487 | 57.2% |
| 26 | SOME BY MI | `BRD-GLOBAL-00605` | 43,051 | 58.05% |
| 27 | Eucerin | `BRD-GLOBAL-00003` | 40,219 | 58.85% |
| 28 | Blanc Nature | `BRD-GLOBAL-00337` | 38,508 | 59.61% |
| 29 | ROUND LAB | `BRD-GLOBAL-00218` | 38,336 | 60.36% |
| 30 | beplain | `BRD-GLOBAL-00353` | 37,095 | 61.1% |
| 31 | dermalogica | `BRD-GLOBAL-00408` | 36,875 | 61.83% |
| 32 | Beauty of Joseon | `BRD-GLOBAL-00136` | 34,498 | 62.51% |
| 33 | Olive Young | `BRD-GLOBAL-01333` | 32,785 | 63.16% |
| 34 | Klairs | `BRD-GLOBAL-01278` | 32,721 | 63.81% |
| 35 | Kiehl's | `BRD-GLOBAL-00084` | 32,689 | 64.45% |
| 36 | Aprilskin | `BRD-GLOBAL-01300` | 32,452 | 65.09% |
| 37 | Skinceuticals | `BRD-GLOBAL-00647` | 31,327 | 65.71% |
| 38 | Derma Lab | `BRD-GLOBAL-01449` | 28,960 | 66.29% |
| 39 | numbuzin | `BRD-GLOBAL-00200` | 26,962 | 66.82% |
| 40 | Anua | `BRD-GLOBAL-00259` | 26,544 | 67.34% |
| 41 | EQQUALBERRY | `BRD-SG-00788` | 24,896 | 67.84% |
| 42 | IUNIK | `BRD-GLOBAL-01316` | 24,594 | 68.32% |
| 43 | Aestura | `BRD-GLOBAL-00235` | 24,457 | 68.81% |
| 44 | Bio Essence | `BRD-GLOBAL-00754` | 23,932 | 69.28% |
| 45 | Clarins | `BRD-GLOBAL-00242` | 22,070 | 69.72% |
| 46 | Pyunkang Yul | `BRD-GLOBAL-01252` | 21,336 | 70.14% |
| 47 | Lancôme | `BRD-GLOBAL-00116` | 21,325 | 70.56% |
| 48 | timeless SKIN CARE | `BRD-GLOBAL-01654` | 21,054 | 70.98% |
| 49 | Calecim Professional | `BRD-SG-01587` | 20,913 | 71.39% |
| 50 | Seyoul | `BRD-GLOBAL-00248` | 20,757 | 71.8% |
| 51 | Lassie Manna | `BRD-SG-00160` | 20,260 | 72.2% |
| 52 | The Face Shop | `BRD-GLOBAL-00275` | 19,873 | 72.59% |
| 53 | Dr.Reju-All | `BRD-SG-00770` | 19,394 | 72.98% |
| 54 | Eau Thermale Avène | `BRD-SG-01081` | 19,366 | 73.36% |
| 55 | FRANKLY | `BRD-GLOBAL-01773` | 19,248 | 73.74% |
| 56 | Isntree | `BRD-GLOBAL-00308` | 19,207 | 74.12% |
| 57 | Julioly | `BRD-SG-01050` | 18,665 | 74.49% |
| 58 | make p:rem | `BRD-GLOBAL-01563` | 18,662 | 74.86% |
| 59 | GLAD2GLOW | `BRD-GLOBAL-03244` | 18,284 | 75.22% |
| 60 | Kose | `BRD-GLOBAL-00014` | 17,876 | 75.57% |
| 61 | NOLAHOUR | `BRD-SG-01767` | 16,102 | 75.89% |
| 62 | Avarelle | `BRD-SG-01543` | 15,789 | 76.2% |
| 63 | Skinlycious | `BRD-SG-01578` | 15,695 | 76.52% |
| 64 | Wellage | `BRD-GLOBAL-01214` | 15,348 | 76.82% |
| 65 | Neutrogena | `BRD-GLOBAL-00080` | 14,593 | 77.11% |
| 66 | S-ERUM | `BRD-GLOBAL-02143` | 14,407 | 77.39% |
| 67 | Hiruscar | `BRD-GLOBAL-00652` | 13,696 | 77.66% |
| 68 | L'Oreal Paris | `BRD-SG-00815` | 13,122 | 77.92% |
| 69 | DR.WU | `BRD-SG-01654` | 13,062 | 78.18% |
| 70 | Elizabeth Arden | `BRD-GLOBAL-01349` | 12,858 | 78.44% |
| 71 | Snova | `BRD-SG-01700` | 12,113 | 78.67% |
| 72 | Axis-y | `BRD-GLOBAL-00388` | 12,102 | 78.91% |
| 73 | Meditherapy | `BRD-GLOBAL-01408` | 11,875 | 79.15% |
| 74 | Care* | `BRD-GLOBAL-00623` | 11,746 | 79.38% |
| 75 | dododots | `BRD-SG-02084` | 11,591 | 79.61% |
| 76 | Mediheal | `BRD-GLOBAL-00361` | 11,557 | 79.84% |
| 77 | Snow2+ | `BRD-SG-01912` | 11,426 | 80.06% |
| 78 | Skin Inc | `BRD-SG-02074` | 11,343 | 80.29% |
| 79 | Benzac AC | `BRD-SG-02168` | 11,155 | 80.51% |
| 80 | Skinfood | `BRD-GLOBAL-00657` | 10,933 | 80.73% |
| 81 | MENTHOLATUM | `BRD-GLOBAL-00915` | 10,501 | 80.93% |
| 82 | Bioderma | `BRD-GLOBAL-00062` | 10,393 | 81.14% |
| 83 | mixsoon | `BRD-GLOBAL-00890` | 10,333 | 81.34% |
| 84 | Garnier | `BRD-GLOBAL-00046` | 9,853 | 81.54% |
| 85 | Cos De BAHA | `BRD-SG-01445` | 9,217 | 81.72% |
| 86 | OGANACELL | `BRD-SG-02065` | 9,070 | 81.9% |
| 87 | KOPHER | `BRD-SG-00270` | 9,042 | 82.08% |
| 88 | Obagi | `BRD-GLOBAL-01751` | 9,029 | 82.26% |
| 89 | Physiogel | `BRD-GLOBAL-00124` | 8,946 | 82.43% |
| 90 | Hada Labo | `BRD-GLOBAL-00055` | 8,907 | 82.61% |
| 91 | komfymed | `BRD-SG-02748` | 8,865 | 82.79% |
| 92 | HaruHaru Wonder | `BRD-GLOBAL-00415` | 8,280 | 82.95% |
| 93 | Tirtir | `BRD-GLOBAL-00212` | 8,242 | 83.11% |
| 94 | CENTELLIAN 24 | `BRD-GLOBAL-00982` | 8,157 | 83.27% |
| 95 | VELY VELY | `BRD-GLOBAL-01942` | 8,088 | 83.43% |
| 96 | Whoopzie | `BRD-SG-01885` | 8,013 | 83.59% |
| 97 | Biodance | `BRD-GLOBAL-00723` | 7,999 | 83.75% |
| 98 | Curel | `BRD-GLOBAL-00199` | 7,985 | 83.91% |
| 99 | DERMATHOD | `BRD-SG-02918` | 7,920 | 84.07% |
| 100 | I'm from | `BRD-GLOBAL-01497` | 7,753 | 84.22% |
| 101 | DPPR | `BRD-SG-02311` | 7,637 | 84.37% |
| 102 | Clé de peau beaute | `BRD-GLOBAL-00482` | 7,494 | 84.52% |
| 103 | FANCL | `BRD-GLOBAL-00856` | 7,291 | 84.66% |
| 104 | A For Apothecary | `BRD-SG-02585` | 7,261 | 84.81% |
| 105 | CNP LABORATORY | `BRD-GLOBAL-01501` | 7,149 | 84.95% |
| 106 | olay | `BRD-GLOBAL-00075` | 7,126 | 85.09% |
| 107 | Caudalie | `BRD-GLOBAL-00975` | 7,116 | 85.23% |
| 108 | Atomy | `BRD-GLOBAL-00813` | 6,968 | 85.37% |
| 109 | Clinique | `BRD-GLOBAL-00171` | 6,893 | 85.5% |
| 110 | PanOxyl | `BRD-SG-02637` | 6,744 | 85.64% |
| 111 | Dr. SHIMIZU | `BRD-SG-02023` | 6,696 | 85.77% |
| 112 | Dr.Melaxin | `BRD-GLOBAL-00962` | 6,666 | 85.9% |
| 113 | Simple | `BRD-GLOBAL-01190` | 6,359 | 86.03% |
| 114 | Acnes | `BRD-GLOBAL-01088` | 6,355 | 86.15% |
| 115 | NARD | `BRD-GLOBAL-00703` | 6,088 | 86.27% |
| 116 | Dr.ville | `BRD-GLOBAL-00875` | 6,051 | 86.39% |
| 117 | Niks | `BRD-SG-02218` | 5,940 | 86.51% |
| 118 | murad | `BRD-GLOBAL-00981` | 5,924 | 86.63% |
| 119 | fully | `BRD-GLOBAL-02097` | 5,885 | 86.74% |
| 120 | Daewoong Pharmaceutical | `BRD-SG-02416` | 5,876 | 86.86% |
| 121 | Nivea | `BRD-GLOBAL-00023` | 5,754 | 86.97% |
| 122 | JOYRUQO | `BRD-SG-02228` | 5,697 | 87.09% |
| 123 | Babor | `BRD-SG-02269` | 5,684 | 87.2% |
| 124 | Jumiso | `BRD-GLOBAL-01956` | 5,682 | 87.31% |
| 125 | Philosophy | `BRD-GLOBAL-00783` | 5,658 | 87.42% |
| 126 | PROYA | `BRD-SG-02389` | 5,580 | 87.53% |
| 127 | ATORREGE AD+ | `BRD-SG-02650` | 5,570 | 87.64% |
| 128 | ISOI | `BRD-GLOBAL-01707` | 5,472 | 87.75% |
| 129 | Minimalist | `BRD-SG-01967` | 5,441 | 87.86% |
| 130 | Balance | `BRD-GLOBAL-00459` | 5,395 | 87.96% |
| 131 | The Body Shop | `BRD-GLOBAL-01534` | 5,336 | 88.07% |
| 132 | est.lab | `BRD-SG-02649` | 5,134 | 88.17% |
| 133 | belif | `BRD-GLOBAL-01350` | 5,099 | 88.27% |
| 134 | blanc dubu | `BRD-SG-01474` | 4,979 | 88.37% |
| 135 | 3M | `BRD-GLOBAL-00359` | 4,942 | 88.47% |
| 136 | Nuskin | `BRD-GLOBAL-00872` | 4,809 | 88.56% |
| 137 | human nature | `BRD-SG-02694` | 4,788 | 88.66% |
| 138 | Uriage | `BRD-GLOBAL-01644` | 4,764 | 88.75% |
| 139 | Vanicream | `BRD-SG-02262` | 4,754 | 88.85% |
| 140 | ma:nyo | `BRD-GLOBAL-00370` | 4,711 | 88.94% |
| 141 | Ginvera | `BRD-SG-01315` | 4,699 | 89.03% |
| 142 | Origins | `BRD-GLOBAL-00616` | 4,689 | 89.13% |
| 143 | L'Occitane | `BRD-GLOBAL-00240` | 4,635 | 89.22% |
| 144 | Deep* | `BRD-SG-12678` | 4,605 | 89.31% |
| 145 | sukin | `BRD-SG-00948` | 4,538 | 89.4% |
| 146 | Missha | `BRD-GLOBAL-01223` | 4,479 | 89.49% |
| 147 | TDF | `BRD-SG-01956` | 4,462 | 89.58% |
| 148 | Charming Skin | `BRD-GLOBAL-01141` | 4,420 | 89.66% |
| 149 | Himalaya | `BRD-GLOBAL-00470` | 4,404 | 89.75% |
| 150 | P&M | `BRD-SG-12197` | 4,327 | 89.84% |
| 151 | DOCTOB | `BRD-SG-02365` | 4,317 | 89.92% |
| 152 | All* | `BRD-SG-06567` | 4,224 | 90.0% |
| 153 | Tunemakers | `BRD-GLOBAL-02133` | 4,166 | 90.09% |
| 154 | ROWMSHI | `BRD-SG-02582` | 4,115 | 90.17% |
| 155 | Eversoft | `BRD-SG-01481` | 4,080 | 90.25% |
| 156 | Suisai | `BRD-GLOBAL-00987` | 3,979 | 90.33% |
| 157 | Sebamed | `BRD-GLOBAL-00142` | 3,900 | 90.4% |
| 158 | iYURA | `BRD-SG-02711` | 3,876 | 90.48% |
| 159 | The History Of Whoo | `BRD-GLOBAL-00988` | 3,787 | 90.56% |
| 160 | Celladix | `BRD-GLOBAL-02162` | 3,780 | 90.63% |
| 161 | OXY | `BRD-SG-01834` | 3,758 | 90.71% |
| 162 | Aqualabel | `BRD-GLOBAL-00677` | 3,747 | 90.78% |
| 163 | Evans Dermalogical | `BRD-SG-02722` | 3,724 | 90.85% |
| 164 | Senka | `BRD-GLOBAL-00214` | 3,694 | 90.93% |
| 165 | SimplyHeal | `BRD-SG-02681` | 3,665 | 91.0% |
| 166 | HABA | `BRD-GLOBAL-02202` | 3,636 | 91.07% |
| 167 | QV | `BRD-SG-00660` | 3,625 | 91.14% |
| 168 | Melano CC | `BRD-GLOBAL-00716` | 3,564 | 91.21% |
| 169 | Dr.Jart | `BRD-GLOBAL-00611` | 3,479 | 91.28% |
| 170 | BUV | `BRD-SG-04366` | 3,446 | 91.35% |
| 171 | Medik8 | `BRD-SG-00643` | 3,435 | 91.42% |
| 172 | House of Hur | `BRD-GLOBAL-01959` | 3,427 | 91.49% |
| 173 | P.Calm | `BRD-GLOBAL-00903` | 3,356 | 91.55% |
| 174 | Acropass | `BRD-GLOBAL-02322` | 3,303 | 91.62% |
| 175 | ICM Pharma | `BRD-SG-00833` | 3,281 | 91.68% |
| 176 | Neostrata | `BRD-GLOBAL-01866` | 3,188 | 91.74% |
| 177 | D'ARK* | `BRD-SG-12623` | 3,131 | 91.81% |
| 178 | Decorte | `BRD-GLOBAL-00193` | 3,116 | 91.87% |
| 179 | JUNGSAEMMOOL | `BRD-SG-01684` | 3,103 | 91.93% |
| 180 | Hipapa | `BRD-GLOBAL-01023` | 3,084 | 91.99% |
| 181 | Dr.G | `BRD-GLOBAL-00053` | 3,040 | 92.05% |
| 182 | MARIO BADESCU | `BRD-GLOBAL-02000` | 2,975 | 92.11% |
| 183 | NEOGEN DERMALOGY | `BRD-GLOBAL-02286` | 2,962 | 92.17% |
| 184 | BRING GREEN | `BRD-GLOBAL-01619` | 2,956 | 92.23% |
| 185 | Etude House | `BRD-GLOBAL-00364` | 2,912 | 92.28% |
| 186 | Parnell | `BRD-GLOBAL-01380` | 2,905 | 92.34% |
| 187 | Oh Hello Bae | `BRD-SG-03570` | 2,898 | 92.4% |
| 188 | MEDIDUPLEX | `BRD-SG-04348` | 2,887 | 92.46% |
| 189 | Aknicare | `BRD-SG-03360` | 2,884 | 92.51% |
| 190 | St.Ives | `BRD-GLOBAL-01334` | 2,852 | 92.57% |
| 191 | MASYEO | `BRD-SG-03557` | 2,816 | 92.63% |
| 192 | Aha | `BRD-SG-03241` | 2,812 | 92.68% |
| 193 | LUMI | `BRD-SG-02345` | 2,743 | 92.73% |
| 194 | But Better Beauty | `BRD-SG-03902` | 2,727 | 92.79% |
| 195 | Jeunesse | `BRD-GLOBAL-01476` | 2,725 | 92.84% |
| 196 | blanc doux | `BRD-SG-02975` | 2,713 | 92.9% |
| 197 | Rovectin | `BRD-GLOBAL-01647` | 2,656 | 92.95% |
| 198 | Dear Louvette | `BRD-SG-02921` | 2,567 | 93.0% |
| 199 | Abib | `BRD-GLOBAL-00325` | 2,512 | 93.05% |
| 200 | Nuxe | `BRD-GLOBAL-01074` | 2,504 | 93.1% |
| 201 | IOPE | `BRD-GLOBAL-01757` | 2,482 | 93.15% |
| 202 | Biore | `BRD-GLOBAL-00061` | 2,475 | 93.2% |
| 203 | Albion | `BRD-GLOBAL-02260` | 2,456 | 93.25% |
| 204 | Heveblue | `BRD-GLOBAL-02033` | 2,419 | 93.29% |
| 205 | every routine | `BRD-SG-03007` | 2,400 | 93.34% |
| 206 | Dr.TWL Dermaceuticals | `BRD-SG-03975` | 2,341 | 93.39% |
| 207 | Avene | `BRD-GLOBAL-00530` | 2,274 | 93.43% |
| 208 | Cure | `BRD-SG-03561` | 2,241 | 93.48% |
| 209 | Takami | `BRD-SG-03477` | 2,203 | 93.52% |
| 210 | Zo Skin Health | `BRD-GLOBAL-02212` | 2,188 | 93.56% |
| 211 | LABO Nutrition | `BRD-SG-00536` | 2,186 | 93.61% |
| 212 | Barrier Repair | `BRD-SG-07838` | 2,182 | 93.65% |
| 213 | Badskin | `BRD-GLOBAL-00840` | 2,177 | 93.69% |
| 214 | Schulke | `BRD-SG-02064` | 2,174 | 93.74% |
| 215 | HISTOLAB | `BRD-SG-03100` | 2,152 | 93.78% |
| 216 | Jealousness | `BRD-SG-03930` | 2,098 | 93.82% |
| 217 | Kelo-Cote | `BRD-SG-04826` | 2,094 | 93.86% |
| 218 | OZIO | `BRD-SG-01796` | 2,089 | 93.9% |
| 219 | smith&nephew | `BRD-SG-04204` | 2,070 | 93.94% |
| 220 | First | `BRD-SG-06480` | 2,064 | 93.98% |
| 221 | PURITO | `BRD-GLOBAL-01040` | 2,016 | 94.02% |
| 222 | Elixir | `BRD-GLOBAL-00106` | 1,976 | 94.06% |
| 223 | PINKWONDER | `BRD-SG-03311` | 1,965 | 94.1% |
| 224 | DR.Belmeur | `BRD-SG-02791` | 1,965 | 94.14% |
| 225 | ecliss | `BRD-SG-04119` | 1,955 | 94.18% |
| 226 | YUN | `BRD-SG-02248` | 1,951 | 94.22% |
| 227 | Biotherm | `BRD-GLOBAL-00625` | 1,929 | 94.26% |
| 228 | Banila Co | `BRD-GLOBAL-00229` | 1,920 | 94.29% |
| 229 | Guboncho | `BRD-GLOBAL-01736` | 1,916 | 94.33% |
| 230 | Suu Balm | `BRD-SG-00424` | 1,907 | 94.37% |
| 231 | Kelly Oriental | `BRD-SG-03705` | 1,898 | 94.41% |
| 232 | Clean & Clear | `BRD-GLOBAL-00276` | 1,864 | 94.44% |
| 233 | AHC | `BRD-GLOBAL-01047` | 1,854 | 94.48% |
| 234 | D Program | `BRD-GLOBAL-00534` | 1,830 | 94.52% |
| 235 | mandom | `BRD-GLOBAL-02045` | 1,808 | 94.55% |
| 236 | heimish | `BRD-GLOBAL-02314` | 1,786 | 94.59% |
| 237 | goodal | `BRD-GLOBAL-01730` | 1,770 | 94.62% |
| 238 | Good Molecules | `BRD-GLOBAL-02567` | 1,764 | 94.66% |
| 239 | LAIKOU | `BRD-GLOBAL-01948` | 1,762 | 94.69% |
| 240 | Dermafora | `BRD-GLOBAL-01456` | 1,749 | 94.73% |
| 241 | La Mer | `BRD-GLOBAL-00291` | 1,742 | 94.76% |
| 242 | URUHIME MOMOKO | `BRD-SG-03598` | 1,738 | 94.8% |
| 243 | Mesoestetic | `BRD-GLOBAL-00182` | 1,717 | 94.83% |
| 244 | AFC | `BRD-SG-00682` | 1,688 | 94.86% |
| 245 | Biologique Recherche | `BRD-GLOBAL-01170` | 1,677 | 94.9% |
| 246 | Safi | `BRD-SG-02100` | 1,660 | 94.93% |
| 247 | guerlain | `BRD-GLOBAL-01313` | 1,654 | 94.96% |
| 248 | Glowfully | `BRD-SG-03700` | 1,626 | 94.99% |
| 249 | Orbis | `BRD-GLOBAL-01541` | 1,606 | 95.03% |

**\* Data-quality flag — likely not real brands.** `Care`, `Deep`, `All`, `D'ARK` are generic
English words / short tokens that show up as `brand_id` in `brand_dict` and are almost certainly
`PRODUCT_NAME_SCAN` mismatches (e.g. matching "Care" inside "Skin Care" sku_names), not genuine
brand entities — confirmed by spot-checking their "official stores": `Care` → `AWHAO` (1 product),
`Chere Jeanne SG Store` (1 product); `Deep` → `Folpus` (1 product), `Spring&Store.sg` (1 product).
**Excluded from the Pass 1 allowlist** — this is a `brand_dict` cleanup issue out of scope for this
session, not a taxonomy problem to solve here. Their GMV stays in the 95% denominator (it's real
category GMV), but no taxonomy entries are built under these brand_ids.

---

## Official Store Allowlist (Pass 1)

Built by querying distinct `merchant_name WHERE merchant_badge='Shopee Mall'`, joined to the 249
in-scope brand_ids, then keeping only stores where **exactly one** in-scope brand appears (clean
single-brand stores), plus a short manually-confirmed list of legitimate parent-company multi-brand
stores.

**Multi-brand retailers found and excluded** (Mall-badged but not brand-principal — carry 3+
distinct in-scope brands, confirmed resellers/marketplaces, not manufacturer stores): Watsons
Singapore Official Store (84 brands), Guardian SG Official Store (73 brands — the SG analogue of
Watsons/Boots not yet in `llm-extraction-rules.md` §4, added here), Sasa Official Store / Sasa
Singapore, Beautyhaus SG, Nana Mall Official Store, Strawberrynet SG Official Store, cosblah,
YAOCOS.kr, Younfamily, BEAUTY U & ME.SG Official Store, BB Beauty Global Official Store, HEY SUP,
Pestlo.SG, BIG Pharmacy, Cosmede Official Store, myCK_online, Beauty Hub SG, The Beauty Curated
Co., Farmasi C S, Mustafa Centre Official Store, Cold Storage Official Store, and ~15 more smaller
K-beauty reseller "boutiques" carrying 3+ brands each (full list in `/tmp` session query output —
pattern is any Mall store with 3+ distinct in-scope brands). **Recommendation: add Guardian, Sasa,
BIG Pharmacy, Cold Storage, Mustafa Centre to `llm-extraction-rules.md` §4's SG-specific exclusion
list** — they aren't there yet because no SG category has been LLM-extracted before this session.

**Confirmed legitimate parent-company multi-brand stores** (Pass-1-eligible for all brands listed,
per rule "parent company stores are Pass-1-eligible for all brands they carry"):

| Merchant Name | Brands carried | Parent company |
|---|---|---|
| `Kao Beauty Official Store` | Curel, Suisai | Kao |
| `Kao Official Store` | Biore, Curel | Kao |
| `Cetaphil Official Store` | Benzac AC, Cetaphil | Galderma |
| `Decorté Official Store` | Decorte, Kose | Kosé Corp |
| `Unilever International` | Simple, St.Ives | Unilever |
| `belif Official Store` | CNP LABORATORY, belif | LG H&H |

**Single-brand official stores** (90 stores, 83 distinct brands, 1,387 products at 2026-05
snapshot):

| Brand | brand_id | Official Store Merchant Name | Products |
|---|---|---|---|
| 3M | `BRD-GLOBAL-00359` | `3M Official eStore` | 10 |
| A For Apothecary | `BRD-SG-02585` | `A FOR APOTHECARY` | 4 |
| AHC | `BRD-GLOBAL-01047` | `AHC Beauty Official Store` | 15 |
| Anua | `BRD-GLOBAL-00259` | `ANUA Official Store_SG` | 15 |
| Avarelle | `BRD-SG-01543` | `Avarelle SG Official Online Store` | 10 |
| Axis-y | `BRD-GLOBAL-00388` | `AXIS-Y Official Store` | 20 |
| BUV | `BRD-SG-04366` | `KAZOO Mall Store` | 3 |
| Badskin | `BRD-GLOBAL-00840` | `BADSKIN SINGAPORE` | 7 |
| Banila Co | `BRD-GLOBAL-00229` | `BANILACO.sg` | 8 |
| Beauty of Joseon | `BRD-GLOBAL-00136` | `Beauty of Joseon Official Store` | 12 |
| Biodance | `BRD-GLOBAL-00723` | `Biodance Official Store` | 15 |
| Blanc Nature | `BRD-GLOBAL-00337` | `Blanc Nature` | 12 |
| Celimax | `BRD-GLOBAL-00764` | `Celimax Singapore` / `Celimax OS Singapore` / `celimax.sg` | 9 / 9 / 16 |
| Celladix | `BRD-GLOBAL-02162` | `Celladix SG Official Store` | 14 |
| Cetaphil | `BRD-GLOBAL-00069` | `Haleon Official Store` | 1 |
| Charming Skin | `BRD-GLOBAL-01141` | `Caring Skin Official Store` | 7 |
| Cos De BAHA | `BRD-SG-01445` | `Cos De BAHA Official Store` | 45 |
| D Program | `BRD-GLOBAL-00534` | `d program Official Store` | 8 |
| DPPR | `BRD-SG-02311` | `DPPR_Official.sg` | 13 |
| DR.WU | `BRD-SG-01654` | `Dr Wu SG Official Store` | 47 |
| Daewoong Pharmaceutical | `BRD-SG-02416` | `SG_TARAJU offficial store` | 1 |
| Derma Lab | `BRD-GLOBAL-01449` | `Derma Lab Official Store` | 58 |
| Dr. Althea | `BRD-GLOBAL-00071` | `Dr.Althea Official Store_SG` | 21 |
| Dr.Jart | `BRD-GLOBAL-00611` | `Dr.Jart+ Official Store` | 6 |
| Dr.Reju-All | `BRD-SG-00770` | `Dr.Reju-All Official Store` | 6 |
| Dr.TWL Dermaceuticals | `BRD-SG-03975` | `DrTWL Dermaceuticals Official Store` | 17 |
| EQQUALBERRY | `BRD-SG-00788` | `EQQUALBERRY_official.sg` | 22 |
| Etude House | `BRD-GLOBAL-00364` | `ETUDE Official Store` / `ETUDE Singapore` | 2 / 8 |
| FRANKLY | `BRD-GLOBAL-01773` | `FRANKLY` | 10 |
| Garnier | `BRD-GLOBAL-00046` | `Garnier SG Official Store` | 32 |
| HISTOLAB | `BRD-SG-03100` | `HISTOLAB SG` | 46 |
| Hipapa | `BRD-GLOBAL-01023` | `Hi!papa` | 2 |
| IUNIK | `BRD-GLOBAL-01316` | `IUNIK Official Store Singapore` | 8 |
| Innisfree | `BRD-GLOBAL-00070` | `Beatriz Korea` | 1 |
| JOYRUQO | `BRD-SG-02228` | `JOYRUQO Official Store` | 8 |
| JUNGSAEMMOOL | `BRD-SG-01684` | `JUNGSAEMMOOL OFFICIAL SG` | 14 |
| Jumiso | `BRD-GLOBAL-01956` | `jumiso_official.sg` | 15 |
| KOPHER | `BRD-SG-00270` | `KOPHER Singapore Official Store` | 9 |
| Kelo-Cote | `BRD-SG-04826` | `Alliance Pharma Official Store` | 5 |
| Kose | `BRD-GLOBAL-00014` | `Kosé Official Store` | 43 |
| L'Occitane | `BRD-GLOBAL-00240` | `L'Occitane Official Store` | 10 |
| LUMI | `BRD-SG-02345` | `LUMI Beauty Official Store` | 11 |
| La Roche-Posay | `BRD-GLOBAL-00002` | `La Roche Posay Official Store` | 32 |
| Meditherapy | `BRD-GLOBAL-01408` | `Meditherapy Korea Official Store` | 32 |
| Minimalist | `BRD-SG-01967` | `Minimalist Official Store` | 16 |
| NARD | `BRD-GLOBAL-00703` | `Nard Store` | 12 |
| NOLAHOUR | `BRD-SG-01767` | `nolahour.sg` | 7 |
| Nivea | `BRD-GLOBAL-00023` | `Nivea Official Store` | 20 |
| Nuxe | `BRD-GLOBAL-01074` | `NUXE official store` | 11 |
| OGANACELL | `BRD-SG-02065` | `Oganacell Official Store SG` | 23 |
| OZIO | `BRD-SG-01796` | `OZIO Official` | 7 |
| Parnell | `BRD-GLOBAL-01380` | `Parnell` | 10 |
| Philosophy | `BRD-GLOBAL-00783` | `Philosophy Official Store` | 35 |
| Physiogel | `BRD-GLOBAL-00124` | `Physiogel  Official  Store` | 8 |
| Pyunkang Yul | `BRD-GLOBAL-01252` | `PyunkangYul Official Store` | 22 |
| ROUND LAB | `BRD-GLOBAL-00218` | `ROUNDLAB Official Store` | 19 |
| Rovectin | `BRD-GLOBAL-01647` | `rovectinsg` | 5 |
| SK-II | `BRD-GLOBAL-00215` | `SK-II Official Store` | 21 |
| Sebamed | `BRD-GLOBAL-00142` | `Eltean Plus` / `sebamed Official Store` | 6 / 1 |
| Senka | `BRD-GLOBAL-00214` | `Fine Today Japan` | 27 |
| Shiseido | `BRD-GLOBAL-00072` | `SHISEIDO Official Store` | 34 |
| Skin1004 | `BRD-GLOBAL-00108` | `SKIN1004 Official Store` | 43 |
| Snova | `BRD-SG-01700` | `Snova Official` | 3 |
| Sulwhasoo | `BRD-GLOBAL-00122` | `Sulwhasoo Official Store` | 36 |
| Suu Balm | `BRD-SG-00424` | `Suu Balm OFFICIAL STORE` | 1 |
| TRUU | `BRD-SG-00875` | `TRUU 童 Official Store` | 44 |
| The Face Shop | `BRD-GLOBAL-00275` | `THEFACESHOP VIỆT NAM` | 23 |
| Tirtir | `BRD-GLOBAL-00212` | `TIRTIR Official Store` | 12 |
| Wellage | `BRD-GLOBAL-01214` | `Wellage Official Store SG` | 21 |
| beplain | `BRD-GLOBAL-00353` | `beplain` | 27 |
| d'Alba | `BRD-GLOBAL-00013` | `d'Alba Singapore Official Store` | 13 |
| dermalogica | `BRD-GLOBAL-00408` | `Dermalogica Official Store` | 34 |
| est.lab | `BRD-SG-02649` | `ést.lab Official Store` | 30 |
| komfymed | `BRD-SG-02748` | `可复美Komfymed Official Store SG` | 19 |
| make p:rem | `BRD-GLOBAL-01563` | `makepremglobal.sg` | 22 |
| mixsoon | `BRD-GLOBAL-00890` | `MIXSOON SG Official Store` | 11 |
| olay | `BRD-GLOBAL-00075` | `Oral Oasis` / `P&G Beauty Official Store` | 4 / 33 |
| smith&nephew | `BRD-SG-04204` | `Smith & Nephew Official Store` | 1 |

**Ambiguous single-listing "official stores" not yet vetted** (S-ERUM → `Shins.sg Official Store`
1 product, Schulke → `bestfriend88dd.sg` 1 product): low volume, deferred rather than blindly
trusted — a store name containing "Official" is not sufficient proof of brand-principal status on
its own, per the same logic that flagged Watsons/Guardian.

**Brands in the 95% GMV scope with no discoverable official store** (~130 of 249 — includes
several of the largest brands by GMV: Torriden, Paula's Choice, Rejuran, The Ordinary, ARENCIA,
Skintific, Estee Lauder, Laneige, SOME BY MI, and most of the long tail): Pass 2 only.

---

## Scale

| Metric | Value |
|---|---|
| Total table rows (all months, `master_clean_niq.shopee_sg_facial_cleanser`) | 3,254,093 |
| Distinct products, latest complete month (2026-05-01) | 72,394 |
| Total rows, 2026-05-01 | 242,325 |
| Official-store (`merchant_badge='Shopee Mall'`) rows, 2026-05-01 | 36,411 |
| Official-store distinct products, 2026-05-01 | 13,610 |
| Official-store distinct products **restricted to the 249 in-scope brands** | 7,805 (across 216 of the 249 brands that have any official-store presence) |
| Official-store distinct products on the vetted single-brand + parent-company allowlist | ~1,592 |
| Distinct `sku_name` text among all official-store products | 13,461 (barely any literal-text duplication — sku_name-level dedup does not collapse this pool much; grouping happens at the product_line/size/pack_count level instead, not by exact string match) |

**7,805 official-store products for in-scope brands is large enough that per-listing multimodal
image reads for every single one is not the right method within a single session** — see Taxonomy
Design Notes for the actual method used (text-first clustering into canonical entries, per
`llm-extraction-rules.md` §2's stated priority: sku_name text is primary for size, image is the
tiebreaker for ambiguous cases only). The taxonomy entries built (target: a few hundred, matching
the ~700–2,000 range other completed categories landed at) are far fewer than the raw product
count — one entry covers many products via the routing step.

---

## Scope — What's In vs Out

**In scope:** everything under this master_table's five `magpie_category_3` buckets — facial
cleanser (foam/gel/milk/cream/oil/wipes/micellar water), facial serum & essence, facial oil, face
scrub & peel, acne treatment (spot treatment, acne patches/pads sold as skincare, not medical
devices).

**Out of scope (leave NULL):** body wash/care (separate category), makeup/cosmetics, sunscreen
(separate `sg_facial_moisturiser`/suncare handling), devices (cleansing brushes, LED masks) unless
bundled with a genuine skincare product as GWP.

**Edge cases:**
- `Care`, `Deep`, `All`, `D'ARK` brand_ids — data-quality artifacts, see Brand Scope footnote.
  Products under these brand_ids are not automatically OOS (some may be real products with a
  misattributed brand) but get no dedicated Pass 1 taxonomy build; route via Pass 2 text-matching
  or leave NULL if genuinely unidentifiable.
- `image` column values in this source table have a data-quality quirk: literal escaped double
  quotes are embedded in the stored URL (e.g. `.../file/"sg-11134207-...` — quotes are literal
  characters in the string, not JSON escaping artifacts). Strip them before fetching or the CDN
  404s. Confirmed working after stripping (test image: make p:rem Inteca Soothing Cleansing Foam
  150ml, read successfully).

---

## Taxonomy Design Notes

**Method: text-first clustering, not per-listing vision reads.** Per `llm-extraction-rules.md` §2
(size: text wins, image is the tiebreaker) and the corrected Full Rebuild process design (see
`docs/headless-runbook.md`'s Scenario: Full Rebuild, step 2, "text-matching...only read product
images for individual products where text matching is genuinely ambiguous"):
1. For each allowlisted brand, cluster official-store `sku_name` values into distinct
   (product_line, sub_line/variant, size, pack_count) combinations using the on-label text.
   `sku_name_EN` is available and English-language for SG (unlike TH), which makes text extraction
   materially more reliable here than in any TH category to date.
2. One canonical `product_taxonomy` entry per distinct combination — confidence 0.85–0.99 only
   when the combination was spot-verified against the product image; otherwise 0.65–0.85 per the
   documented text-derived confidence band (`llm-extraction-rules.md` is explicit that 0.85+ means
   multimodal-verified, not just text-plausible).
3. Vision reads reserved for: brands/products where sku_name is ambiguous on size/pack/line, spot
   verification of the highest-GMV entries per brand (triage by GMV impact per
   `quality-standards.md` §1), and any product routed to a catch-all/`(unresolved)` entry that a
   quick image check could resolve.
4. `product_line` / `sub_line` / `variant` are populated as their own columns on every insert (not
   left NULL while the text lives only in `canonical_name`) — this is the exact prior failure
   (934 entries, 100% NULL `product_line`) that `docs/headless-runbook.md`'s QA-gate-as-code exists
   to catch. Verify with the DISTINCT-taxonomy-entry gate (excluding `is_multi_size`) before
   declaring done, not after.

**Universe refresh target:** per `docs/headless-runbook.md` (not `CLAUDE.md`, which is stale on
this point — see the runbook's own note: "the target table isn't what CLAUDE.md says"), the real
production output table is `marketshare_universe_niq`, and taxonomy state for headless runs lives
in the `magpie_reference.universe_taxonomy_overlay` MERGE, keyed on
`(product_id, platform, country, master_table)`. Confirmed both `marketshare_universe_niq` and
`universe_taxonomy_overlay` exist in BigQuery as of this session. No `ALTER TABLE` on
`marketshare_universe_niq` itself.

**Known difficult products:** none catalogued yet — first pass.

---

## Existing HUMAN rows (found in Step 1 — undocumented prior to this session)

**5,617 `source='HUMAN'` rows** exist in `product_taxonomy_map` for `master_table =
'shopee_sg_facial_cleanser'`. **Zero `source='LLM'` rows.** This matches the STATUS.md dashboard
("sg_facial_cleanser: ⏳ Keyword only") — these are the automated keyword-seed rows (the `HUMAN`
label is legacy terminology, not actual human review, per `ARCHITECTURE.md` Decision 18). Per this
session's explicit instructions: **not deleted by this session** — HUMAN-row cleanup is a separate,
deliberately manual/wrapper-side step (delete only where a HUMAN row duplicates a product that also
now has an LLM row, run after this session, never a blanket supersede).

---

## QA History

| Date | Pass | Finding | Resolution |
|------|------|---------|------------|
| 2026-07-16 | Pre-build survey | 5,617 undocumented HUMAN rows, 0 LLM rows | Documented above; not deleted this session |
| 2026-07-16 | Scale check | 249 brands in true 95% GMV scope (not a top-20/50 snapshot); 7,805 official-store products across 216 of those brands | Text-first clustering method adopted instead of per-listing vision reads |
| 2026-07-16 | Multi-brand store audit | Watsons SG, Guardian SG, Sasa, BIG Pharmacy, Cold Storage, Mustafa Centre + ~20 K-beauty reseller "boutiques" are Mall-badged but not brand-principal | Excluded from Pass 1 allowlist; recommend adding Guardian/Sasa/BIG Pharmacy/Cold Storage/Mustafa Centre to `llm-extraction-rules.md` §4's SG list |
| 2026-07-16 | Brand_dict data quality | `Care`, `Deep`, `All`, `D'ARK` brand_ids are PRODUCT_NAME_SCAN artifacts on common English words, not real brands | Excluded from Pass 1 taxonomy build; flagged as a `brand_dict` cleanup item, out of scope for this session |
| 2026-07-22 | Top-up coverage (month 2026-06) | Live re-run of the 95%-cumulative-GMV (GWP-zeroed) NULL worklist found 2,547 rows / 2,133 distinct products ($1.45M SGD) with no `taxonomy_id` — the prior session's coverage was far from complete. Bucketed by `product_brand_map.brand_id`: 275 products (48 brands) had an existing SKU-070xxx taxonomy entry for their brand but no exact-match entry for the specific listing; 1,636 products (535 brands) had no taxonomy entry at all for their brand; 98 were `BRD-UNDEFINED`; 124 had no `product_brand_map` row. | Bulk-first text clustering (grouped by brand_id + normalized product-line/size/pack signal from `sku_name`) minted 1,807 new taxonomy entries (SKU-118006–SKU-119812, claimed via a fresh 2,000-slot `taxonomy_topup` block SKU-118006–SKU-120005 — the prior SKU-070001–072000 `full_rebuild` block was left untouched, not reused, to keep scenario labeling clean) and mapped 1,916 products (1,910 to new entries + 6 to text-identified `BRD-UNDEFINED`/no-brand-map products routed to 5 newly discovered brand matches — ScarAway, AROOM, Sothys, QHOO — found via `brand_dict` lookup on sku_name tokens the original `PRODUCT_NAME_SCAN` missed). Only 6 clusters (9 products, ScarAway aside) were genuine text-only stubs (`Brand + generic category word` with nothing else in `sku_name`); resolved via a small targeted image-read batch (per `docs/headless-runbook.md`'s "read images only where text is genuinely insufficient, batch the rest") — one of those (Neoretin) turned out to have a real on-label product line ("Discrom Control Serum Booster Fluid") the text alone had hidden behind a generic "Serum" listing title, confirming the image-read gate is still worth running even at low volume. One product (Naturium "Serum") turned out to be a 10-item assorted-serum bundle photo with no single identifiable product — left NULL rather than forced into a banned "Multiple Variants" stub. 8 clusters (9 products) matched an *existing* SKU-070xxx entry closely enough to reuse directly instead of minting a duplicate (checked candidate matches for the "+"/bundle-vs-single-item pitfall first — 2 candidates, a Dr.Reju-All duo bundle and an IUNIK 3-way variant-selector listing, were excluded from reuse for exactly that reason and minted/left separately instead). Residual live worklist after this session: 250 rows / 217 distinct products ($460K SGD, mostly unresolved `BRD-UNDEFINED`/no-brand-map long tail) — left `UNRESOLVED` per scope rules rather than forced to a catch-all; a follow-up NULL-coverage pass could pursue these further. Category GMV coverage (2026-06, all sources): 89.06%. |
| 2026-07-22 | QA gates (post top-up, no `--skip-coexistence`) | G1 dual-mapped LLM = 0. G2 HUMAN+LLM coexistence = 552 — **pre-existing baseline, not caused by this session**: verified before writing that this session's worklist has zero overlap with the 5,617 legacy HUMAN rows, so this session added 0 new coexistence; the count is unchanged from the 2026-07-16 session's own recorded 552 and reflects the still-pending wrapper-side "delete HUMAN rows that duplicate an LLM row" step referenced in that session's notes, which has not run yet. G3 placeholder-leak = 3,808 — **also pre-existing, not from this session**: 100% of flagged rows trace to legacy `SKU-002xxx` HUMAN-source keyword-seed entries named `"{Brand} Facial Cleanser (all variants)"` (a `(all variants)`-suffix pattern predating this session and this category's own Pass 1/2 taxonomy range entirely); zero of this session's SKU-118006–119812 entries matched the placeholder regex. Structured-fields-NULL gate (distinct LLM entries, excl. `is_multi_size`) = 0%. G5 provenance = 0. Cross-category check (this session's `topup_*` map rows all resolve to taxonomy_ids inside the claimed SKU-118006–120005 block or the pre-existing SKU-070001–070194 Pass-1/2 block) = 0 violations. | Gates pass for this session's own writes; G2/G3 failures are inherited legacy debt outside this session's scope (HUMAN-row cleanup and legacy keyword-seed `(all variants)` renaming are both separate follow-up items, not blockers for this session's coverage work). Universe refresh deliberately NOT run this session per task scope — a separate step after independent QA verification. |
| 2026-07-22 | Top-up coverage session #2 (month 2026-06, live re-run) | Re-ran the STEP 0 worklist query live rather than trusting the wrapper's stale 250-count precheck or the prior session's own recorded residual — result matched exactly: 250 rows / 217 distinct products ($460,323.74 SGD) with no `taxonomy_id`, confirming zero drift since the 2026-07-22 session above. Verified zero overlap between this worklist's 217 `product_id`s and any existing `product_taxonomy_map` row (HUMAN or LLM) before writing — safe to insert fresh, no dual-mapping risk. Brand-resolution check found the prior session's "mostly BRD-UNDEFINED/no-brand-map long tail" framing under-stated true identifiability: many of the 92 `BRD-UNDEFINED` + 124 no-`product_brand_map`-row products were from obviously identifiable official stores (CeraVe Official Store, Torriden Official, Kiehl's Official Store, Innisfree Official Store, etc.) whose `product_brand_map` row simply doesn't exist yet — a Stage 03 data gap, not a real unidentifiability. Per `llm-extraction-rules.md`'s established precedent ("taxonomy mapping does not require brand_id agreement with `product_brand_map`"), routed these by brand identified directly from `sku_name`/official-store `merchant_name` text instead of leaving them NULL. GMV distribution within the worklist was heavily GWP-skewed (top 4 rows = 66.8% of raw GMV, all `flag_GWP=TRUE` Lassie Manna/reseller-bundle listings whose GMV is zeroed in the real scope math) — attempted the full 217, not a GMV-head slice, per this session's explicit instructions. | Text-first brand/product-line extraction (no image reads needed — `sku_name` + official `merchant_name` was sufficient for every case) resolved 213 of 217 products: 6 mapped to existing taxonomy entries via exact brand+product+size match (Cerave Resurfacing Retinol Serum 30ml → SKU-118244; HaruHaru Wonder Black Rice Probiotics Barrier Essence 120ml → SKU-118274; Avene Tolerance Extremely Gentle Cleanser 200ml → SKU-119359; Byphasse Soft Cleansing Milk 500ml → SKU-118653; FRANKLY PDRN Bounce Ball Serum 30ml → SKU-070102; guerlain Abeille Royale Youth Watery Oil Serum 50ml → SKU-118890), 207 minted under 206 new taxonomy entries (one pair of products — SkinDae reseller listings from two different sellers of the identical product — deduped to a single shared entry). New taxonomy_id range: **SKU-120406–SKU-120611** (claimed block SKU-120406–120655, 250 slots, ~44 unused). Brand resolution used a mix of the category's existing 249-brand allowlist, a bulk `brand_dict` lookup pass for ~90 additional candidate brand tokens extracted from `sku_name`/bracketed store-brand text (resolved brands including CHARDE, CURAS, Demaf, JOVS, Jorubi, Molnlycke, NOSCARNA, Onecare, PROYA, Pair Acne, ReVcell, Renecell, ilso, MIRAE, LHALA, Cellinto, and ~60 more), and `BRD-UNDEFINED` for genuinely unresolvable small resellers/generic products (still given real, specific `product_line` text extracted from `sku_name` — never a bare category-word stub). 4 products left `UNRESOLVED`/NULL per the per-product category gate: JOVS Blacken DPL device (beauty device, not a topical product), IEM "zieha...Hair nutrients" (hair category, out of this table's 5 skincare sub-categories), Naturium "Serum" (confirmed in the prior session as an unresolvable 10-item assorted-serum bundle photo), Silicone Scar Sheets/Tape (medical/wound-care device, not a cosmetic). Live worklist re-run after writing confirmed exactly these 4 products remain (\$1,207.80 SGD residual, down from \$460,323.74). Category GMV coverage (2026-06, all sources): **97.3%** (up from 89.06%), 8,314 of 8,600 distinct 2026-06 products mapped. |
| 2026-07-22 | QA gates (session #2, no `--skip-coexistence`) | Re-verified before writing: 0 of this session's 217 `product_id`s had any pre-existing `product_taxonomy_map` row. Post-write: G1 dual-mapped (scoped to `source='LLM'`, per `docs/headless-runbook.md`'s QA-gate-as-code) = 0. G2 HUMAN+LLM coexistence = 552 — **unchanged from the session above, confirmed pre-existing**: none of this session's 217 products appear in the coexistence set (explicit re-check against this session's own product_id list = 0). G3 placeholder-leak = 3,808 — **unchanged, confirmed pre-existing**: 0 of this session's new SKU-120406–120611 entries match the placeholder regex. G5 provenance = 0. Structured-fields-NULL (distinct entries in SKU-120406–120611, excl. `is_multi_size`) = 0%. Cross-category check: all of this session's map rows resolve to taxonomy_ids inside SKU-120406–120655 (this session), SKU-118006–120005, or SKU-070001–072000 (prior sessions' blocks) = 0 violations. | Gates pass cleanly for this session's own writes; G2/G3 are confirmed-inherited legacy debt (unchanged counts, zero of this session's rows implicated), same as the prior session's finding. Universe refresh deliberately NOT run this session — a separate step after independent QA verification, per task scope. |
| 2026-07-23 | Automated targeted-QA-fix review (auto-discovery mode, `_meta`-tracked incremental scope) | Worklist (never-reviewed/unconfident `product_taxonomy` entries mapped to this table) = 2,326 taxonomy_ids. Tier 1 SQL sweep flagged: `null_size` 641, `wrong_field_order` 86, `canonical_field_mismatch` 64, `stub_leak` 56, `brand_casing_mismatch` 42, `excess_content` 30, `garbage_brand` 3, `duplicate_brand` 0. Tier 2 GMV-prioritized sample of 25 Tier-1-clean rows surfaced 2 real defects a clean-structure check alone wouldn't catch: SKU-070001 (d'Alba First Spray Serum) pinned to `100ml` when the product's own listing and image both state `50ml/100ml` as a buyer choice, and SKU-118008 (timeless SKIN CARE) merging a "10%" and a "20%" Vitamin C serum under one entry's name — left unconfident for follow-up. STEP 2b's promo-language sweep surfaced this session's highest-impact finding: SKU-000504/SKU-000506 (the pre-existing `SKIN1004 Madagascar Centella Ampoule 55ml/100ml` catch-all) merge **196 products / $1.097M SGD — 21.7% of total category GMV** — across genuinely distinct Skin1004 product lines (base Centella Ampoule, Tone Brightening Capsule, Tea-Trica Relief, Poremizing Fresh, Hyalu-Cica First, Probio-Cica Intensive) into one taxonomy entry; also buried inside it were 2 products whose own `sku_name` states a real `[SKIN1004 1+1 EVENT]`/`BUY 1 GET 1` promo (image-confirmed for one of the two — see Resolution). The 56 `stub_leak` hits are the same pre-existing `SKU-002xxx` `"{Brand} Facial Cleanser (all variants)"` HUMAN-seed catch-alls documented in the 2026-07-22 sessions above (collectively ~$2.9M SGD across this worklist slice) — confirmed via GMV-ranking that a real fix requires per-product re-extraction (Full Rebuild scope), not a targeted rename. | Fixed in place, no full rebuild: (1) **garbage_brand** — 2 genuine reseller-watermark leaks (`12/+＝`, §11) corrected: SKU-118979 rerouted to real brand MASIL, SKU-119004 to R3R; SKU-118309 ("107") confirmed a real numeric brand name (packaging literally reads "107"), left as-is. (2) **wrong_field_order** — 81 BRD-UNDEFINED/BRD-UNBRANDED rows checked case (b) (`canonical_name` states a real brand, `brand_id` doesn't): 27 rerouted to an existing `brand_dict` entry (lookup-before-mint), 33 new SG-scope brands minted (`BRD-SG-13426`–`13458`, e.g. ClearUp, LANBENA-adjacent 2E/377/AROTOLOGY/SkinDae/dr.he/etc. — full list in commit diff) since genuinely absent from `brand_dict`; the ~16 true BRD-UNBRANDED rows (generic hydrocolloid patches etc. with no real brand stated) were false positives, left untouched. Also found via this flag: SKU-120424 mis-mapped to brand "Avarelle" when its own `canonical_name`/`sku_name` says "Alluora" (rerouted to the real Alluora `brand_id`), and SKU-002498 mapped to a merchant-name-style `brand_dict` entry `"Glad2Glow Official Store"` instead of the real `GLAD2GLOW` brand already in this category's scope table (rerouted); `brand_dict` entry `Cellinto_Singapore` renamed to `Cellinto` (same merchant-suffix defect, single-row global rename, no counterpart existed to collide with). SKU-120561's `canonical_name` had literally absorbed its own `merchant_name` ("AHHIEMMISS") as a fake brand prefix — §11 violation — stripped, `brand_id` correctly stays `BRD-UNDEFINED` since `sku_name` states no real brand. (3) **brand_casing_mismatch** — 42 rows normalized to `brand_dict`'s existing casing (Cosrx/Skin1004/Cosme decorte/SOTHYS); `brand_dict` itself left untouched (ambiguous which casing is "correct" for stylized-caps trademarks; out of scope to change globally for a casing preference). (4) **canonical_field_mismatch** — 64 rows individually reviewed: 63 were false positives of the regex (patch/pcs counts, genuine multi-item Set/Duo/Trio bundles describing real contents); 1 real defect fixed — SKU-120414 (CHARDE) used a banned "(N ea xM set)" breakdown, corrected to `x18` per the standing `x{TOTAL}`-only rule. (5) **null_size** — bulk-defaulted 119 rows with `pack_count IS NULL` to `1` (never-leave-NULL rule, no multipack signal present); bulk-backfilled `size` from an unambiguous single-value `sku_name` regex match for 48 rows, then caught and reverted 13 of those (12 genuine multi-component Sets/Bundles + 1 generic `Set/Bundle/Off-category/Other` catch-all) that the first pass had wrongly pinned to one component's size — reset to `size=NULL, is_multi_size=TRUE` instead, the semantically correct state for a real multi-item bundle. (6) **excess_content** — fixed 3 rows with a literal duplicated `xN xN` suffix bug (EQQUALBERRY, 2× komfymed) where `size` already embedded the multiplier and `canonical_name` appended it again; the `Xml / Yml` dual-size and multi-component Set entries flagged here were confirmed legitimate, left as-is. (7) **STEP 2b promo sweep** — minted 1 new pack_count=2 entry (SKU-121856, claimed block SKU-121856–122055) and rerouted the one product with both text ("BUY 1 GET 1") and image confirmation (2 bottles pictured) off the SKU-000504 catch-all; the second "1+1 EVENT" product's image didn't clearly show 2 units, left unrerouted pending confirmation. The SKU-000504/000506 variant-collapse itself (196 products, $1.097M SGD) was **not** unwound this session — re-clustering ~200 products into their real product lines is Full Rebuild-scope work, not a targeted fix; flagged here as this category's single highest-value follow-up item. 54 taxonomy_ids that were genuinely Tier-2-judged this session and found correct (no fix needed) were bulk-marked `review_confidence` (confident/unconfident per the two-consecutive-agreement rule) in one statement; no Tier-1-clean row outside that set was marked reviewed. QA gates (no `--skip-coexistence`): G1 dual-mapped LLM = 0. G2 HUMAN+LLM coexistence = 552, **unchanged, confirmed pre-existing** (same count as the 2026-07-22 sessions above). G3 placeholder-leak = 3,808 product-map-rows / 56 distinct taxonomy entries, **unchanged, confirmed pre-existing** — these are exactly the `stub_leak` set documented above as deferred to Full Rebuild, not newly introduced. G5 provenance = 0. Universe refresh deliberately NOT run this session, per task scope. |
| 2026-07-23 | Top-up coverage session #3 (month 2026-06, live re-run, wrapper precheck independently verified) | Wrapper's live precheck reported 1,616 rows with no `taxonomy_id` in the 95%-cumulative-GMV (GWP-zeroed) worklist — re-ran the STEP 0 query live rather than trusting it, per task instructions: confirmed exactly **1,616 rows / 1,196 distinct products / $3,136,230.63 SGD**, so the precheck was accurate this time (unlike the `shopee_th_suncare` case cited in the runbook). Grouped the worklist by `product_brand_map.brand_id`: only **60 distinct brands** represented (all already in this category's 249-brand scope table), making brand-by-brand bulk text-matching tractable rather than a full re-extraction. | Bulk-first, GMV-prioritized, multiple passes: **(1)** SQL fuzzy reuse-match (brand + ≥3-token product-line substring + size substring against existing taxonomy for this table) resolved 47 products directly against pre-existing SKU-070xxx/118xxx/120xxx entries — iterated the matcher twice after catching false positives (generic 2-token overlaps merging distinct medicube/SK-II lines; "Bundle of N" cross-item listings matching a single-item entry on partial text) by tightening to require ≥3 tokens, no tied candidates, and excluding Set/Duo/Bundle-worded target entries. **(2)** Programmatic text extraction (regex-based size/pack/product-line stripping — brand name, promo phrases, bracketed store tags removed; iterated after finding real bugs: size regex missed mixed-case "mL" used throughout The Ordinary's listings, and a compound "150gx2" pattern where `\b` word-boundary doesn't fall between two letters) clustered the remainder by brand + line-signature + size + pack into 796 distinct combinations; a real bug here too — ambiguous multi-size listings (e.g. "230ml/250ml/330ml") were extracting the *first* number as if it were a definite size instead of flagging `is_multi_size`, caught by inspecting the top-GMV output before committing. **(3)** Re-ran the tightened reuse-matcher a second time against the now-larger taxonomy (including this session's own fresh mints) — caught e.g. several "Buy 1 Get 1 Free" listings whose base product had just been minted as a single unit, resolving 28 more via direct reuse and 8 more by reusing the sibling's size at pack_count=2. **(4)** 12 individually-judged special-case entries for cross-product bundles (Torriden Serum+Mask/Toner-Pad/second-Serum "Bundle of 2" listings, SK-II Value Sets with bonus units, a Cosrx 6-serum set, a Blanc Nature 3-step set, a genuinely ambiguous Clarins multi-size/variant listing, an Innisfree 30ml/50ml choice listing) — each mapped to its own descriptive multi-variant entry rather than force-fit into an unrelated single-item match. **(5)** Brand resolution: `product_brand_map` gaps (`BRD-UNDEFINED`/no-row) were **not** left NULL by default — checked `brand_dict` and found LA MONTE and ClearUp already minted (from the 2026-07-23 QA-fix session above); identified "Fresh" (a real, well-documented global brand — Soy Face Cleanser, Rose Deep Hydration Serum, Black Tea Kombucha Essence — all with real stated sizes) despite its only `brand_dict` row being flagged `status='GARBAGE'` (used it anyway since these are unambiguously genuine Fresh-brand listings; the GARBAGE flag looks like a common-word false-positive issue elsewhere in the dict, a brand_dict cleanup question out of scope for this session, not evidence against these specific products); rerouted one Lassie-Manna product mis-tagged `BRD-UNDEFINED` to `BRD-SG-00160` (its official-store siblings all resolved correctly — a Stage 03 data gap, not real unidentifiability). Resolved 19 products this way. **(6)** Vision-verified the highest-GMV remaining items where text genuinely had no size (per `docs/headless-runbook.md`'s curl-then-Read method, stripping the literal embedded quotes in the `image` column): 7 Lassie Manna 【MW】-coded products (real sizes ranged 1ml×4 up to 3ml×20 sachets; one turned out to be a genuine 2-item Essence+Emulsion set, correctly flagged rather than force-sized) and 6 Paula's Choice treatments (confirmed Paula's Choice states size clearly on-package even though Shopee's `sku_name` field never includes it — a category-level gap worth noting for any future Paula's Choice work) plus the IEM White Truffle Essence (single largest remaining GMV item at $161,838 — real product line read as "Tuber Magnatum Anti-Aging Essence", but the on-bottle size text was genuinely illegible even at full resolution, left `size=NULL` rather than guessed). **A real G1 dual-mapping bug was caught and fixed before shipping**: the 6 special-case products in step (4) had already been silently absorbed into step (2)'s generic bulk-mint clustering pass (which only excluded them from the *reuse-match* step, not from mint) before being hand-mapped again to their correct dedicated entries — found via the QA gate itself, not proactively; removed the 6 erroneous auto-generated map rows (and 4 now-orphaned generic taxonomy entries; the other 2 orphan candidates each still had other, legitimately-clustered products depending on them and were kept). | **New taxonomy_id range: SKU-141577–SKU-142407** (827 entries; claimed block SKU-141577–143192, ~789 slots unused for a follow-up session). Resolved **1,008 of 1,196** worklist products (**84.3%**) / **$2,726,869 of $3,136,231 SGD** (86.9% of worklist GMV). Residual live worklist after writing: **342 rows / 188 distinct products / $409,361.26 SGD** — the honest stopping point for this session's turn budget, not a strategic top-GMV-only slice: the residual is a genuine long tail (Paula's Choice ~33 more products, VT Cosmetics, Olive Young, Aprilskin, Skinlycious, remaining medicube/Cosrx items, Alluora "Renew Serum"/"Balance Cleanser" GWP listings with no sibling entry to reuse a size from) that each need an individual image read with no further shared pattern left to batch — flagged as the top follow-up item below rather than force-sized. Category GMV coverage (2026-06, all sources): **89.5%**. QA gates (no `--skip-coexistence`, re-run after the dual-mapping fix): G1 dual-mapped LLM = **0** (after the fix described above). G2 HUMAN+LLM coexistence = **0** — a change from the 552 recorded as "pre-existing baseline" in the two 2026-07-22 sessions and the 2026-07-23 QA-fix session above; this table's `product_taxonomy_map` now shows **zero `source='HUMAN'` rows at all** (down from the 5,617 documented at this category's first session), consistent with the wrapper-side "delete duplicating HUMAN rows" step referenced in those sessions' notes having run at some point since — observed, not caused by this session, and not independently confirmed against a wrapper log. G3 placeholder-leak = 0 (none of this session's new entries match the banned-phrase regex). G5 provenance = 0. Structured-fields-NULL (distinct entries in SKU-141577–142407, excl. `is_multi_size`) = 0%. Cross-category check: all of this session's `source='LLM'` map rows resolve to taxonomy_ids inside SKU-141577–143192 (this session) or one of the four prior sessions' blocks (SKU-070001–072000, 118006–120005, 120406–120655, 121856–122055) = 0 violations. Universe refresh deliberately NOT run this session — a separate step after independent QA verification, per task scope. |
| 2026-07-23 | Top-up coverage session #4 (month 2026-06, live re-run, wrapper precheck independently verified) | Wrapper's live precheck reported 342 products — re-ran the STEP 0 query live rather than trusting it, per task instructions (and per the wrapper's own explicit warning not to trust it): confirmed exactly **342 rows / 188 distinct products / $409,361.26 SGD**, an exact match to session #3's own recorded residual — zero drift since that session. An automated fuzzy reuse-matcher (brand + ≥3-token overlap against existing SKU-070xxx/118xxx/120xxx/141xxx entries, the same style as session #3's step (1)) was tried first but produced dangerous false positives this round — e.g. collapsing 6 textually-distinct VT COSMETICS Reedle Shot variants (Collagen, RETI-A, Red Booster, Hydrop, GARLIC AC, PRO-CICA) onto one unrelated `medicube`/`VT` entry, and matching several Paula's Choice/Alluora/medicube products to siblings with a different stated name or active-ingredient %, purely on shared brand/marketing boilerplate tokens ("Buy", "Get", "Official", "Serum", "Duo") — discarded without applying any of its ~93 candidate matches; reuse against the existing taxonomy is effectively exhausted after 3 prior sessions, and the remaining 188 are genuine distinct long-tail products, consistent with session #3's own note that the residual "each need an individual image read with no further shared pattern left to batch." Checked `product_brand_map` for all 188: 186 had an existing brand row (only 1 `BRD-UNDEFINED`/unbranded), confirming brand identifiability is not the blocker for this residual — size is (only 3 of 188 `sku_name` values contain an explicit unit; `raw_niq_history` still does not exist for this table per session #3's finding, so text/image are the only two size signals and image was the blocker for the size-focused framing this task's own precheck implied). | Re-scoped to this task's actual STEP 1 gate — read an image only when text can't identify **brand or product line** (size/pack precision is explicitly deferred to `targeted_qa_fix.sh` per this task's own instructions) — text-first minted all 188: **product_line** extracted from each product's own `sku_name` (English throughout, SG data), stripping store/marketing noise (brand prefix, `[Official Flagship]`/`(Ready to Ship)`/emoji/expiry-date bracket tags) but never the brand itself twice; **size** populated only for the 3 products where `sku_name` states one explicitly (plus a handful of countable-unit "sizes" like `18ea`/`3EA`/`4.5oz`/`1L`/`Box of 6`), left `NULL` (never guessed) for the rest, confidence dropped to 0.55 accordingly — the same documented precedent as this category's very first Pass-1 session (13 entries, confidence 0.50–0.55 for unresolvable size); **pack_count** computed from explicit signals only: 5 Alluora "(GWP) Buy 1 Full Size X Get 1 Full Size X" listings correctly read as a genuine same-product multipack (not GWP, per `llm-extraction-rules.md` §1's GWP-vs-multipack distinction — buyer receives 2 of the *same* named serum/cleanser) → `pack_count=2`; explicit countable multiplES (`(Box of 6)`, `[3 Tubes]`, `3EA`, `48pcs`, `10EA`, `X2`) parsed directly; a `(3 Sizes)`/regime/bundle handful flagged `is_multi_size`/`is_multi_variant`/`is_bundle=TRUE` per the same buyer-choice and cross-product-bundle conventions established in prior sessions (e.g. CeraVe "Hydrating Cleanser (3 Sizes)" → `is_multi_size=TRUE`, Beauty of Joseon "6.6 Brand Box" → `is_bundle=TRUE`), with no banned "Multiple Sizes/Variants" text in any `canonical_name`; default `pack_count=1` otherwise. Deduped 2 exact-match pairs (2 Some By Mi "18ea" spot-patch listings from different resellers, 2 Paula's Choice "Triple Active Total Repair Serum" listings) to a single shared taxonomy entry each rather than minting duplicates — 183 products → 181 taxonomy entries. **5 products confirmed genuinely out-of-scope** per the per-product category gate (never the keyword-gate shortcut): IEM "Chili sauce" (literal food listing, not skincare), IEM "zieha ... Hair nutrients" (hair category, outside this table's 5 skincare sub-categories — consistent with a prior session's identical finding for a different IEM listing), JOVS Blacken DPL device (beauty device, not topical), Silicone Scar Sheets/Tape (medical/wound-care device), and Naturium "Serum" — **image-verified** (only vision read this session, since text alone genuinely couldn't disambiguate): confirmed via `curl`-then-`Read` on the product photo that this is the identical 10-item assorted-Naturium-serum lineup shot (Retinaldehyde/Alpha Arbutin/Niacinamide/Vitamin C ×2/Vitamin K/Retinal Complex/Salicylic Acid/Quadruple Hyaluronic Acid) a prior session already found unresolvable to one product — left `NULL` again rather than force-fit. Pre-write safety check confirmed 0 of the 188 `product_id`s had any pre-existing `product_taxonomy_map` row (fresh insert, no dual-mapping risk). | **New taxonomy_id range: SKU-145034–SKU-145214** (181 entries; claimed block SKU-145034–145375, 342 slots, ~161 unused for a follow-up session). Resolved **183 of 188** worklist products (**97.3%**) / **$407,163.46 of $409,361.26 SGD** (99.5% of worklist GMV — the 5 OOS products carried very little GMV, $2,197.80 combined). Residual live worklist after writing: **7 rows / 5 distinct products / $2,197.80 SGD**, all 5 the confirmed-OOS products above — genuinely left `UNRESOLVED` per scope rules, not a coverage gap. Category GMV coverage (2026-06, all sources): **96.52%** (up from 89.5% recorded after session #3), 4,465 of 72,600 distinct 2026-06 products mapped (coverage is GMV-weighted, not row-count — the in-scope set is a small fraction of total listings by design). QA gates (no `--skip-coexistence`): G1 dual-mapped LLM = **0**. G2 HUMAN+LLM coexistence = **0** (still zero, matching session #3's observation that this table has had no `source='HUMAN'` rows since some point between sessions). G3 placeholder-leak = **0**. G5 provenance = **0** (every new row has `meta_agent='CLAUDE_CODE'`, `source='LLM'`). Structured-fields-NULL (distinct entries in SKU-145034–145214, excl. `is_multi_size`) = **0%**. Cross-category check: all 183 of this session's `product_id`s resolve to exactly 1 map row each, 100% inside the claimed SKU-145034–145375 block = 0 violations. Universe refresh deliberately NOT run this session — a separate step after independent QA verification, per task scope. |
| 2026-07-23 | Automated Taxonomy Review session #3 (auto-discovery mode, `_meta`-tracked incremental scope) | Worklist = 3,209 taxonomy_ids. Pre-fix `qa_report.sh` gate: canonical_name fields FAIL (65), garbled brand text FAIL (1), all other gates PASS. Tier 1 SQL sweep over the worklist flagged: `null_size` 706, `canonical_field_mismatch` 65 (matches the gate), `wrong_field_order` 24, `excess_content` 16, `garbage_brand` 1, `stub_leak`/`duplicate_brand`/`brand_casing_mismatch` 0. STEP 2b's promo/multiplier sweep (narrowed to unambiguous `[Bundle of N]`/`buy N get M`/`1+1` tags, dropping the noisy generic `x\s*\d` term) found 84 distinct `pack_count=1` taxonomy_ids with genuine promo/bundle language; splitting these into "every mapped product agrees on the pattern" (57, safe to bulk-fix) vs "some products differ" (27, a mixed/merge-conflict risk) — of the 57, 32 turned out to be cross-item component bundles (2+ differently-named products bundled, e.g. Arencia "Day & Night" dual-serum sets, Torriden multi-component sets) needing per-product judgment, not a blind pack_count set, leaving 22 genuine same-single-product multipacks (`[Bundle of N]`/`1+1`/`Buy 1 Free 1` on one named item) as the safe bulk-fix set. GMV-prioritized Tier 2 sample of 30 Tier-1-clean rows surfaced 2 new instances of the **single/bundle-merge defect** already flagged as this category's top follow-up item in the 2026-07-23 review #2 above (same taxonomy_id serving both a plain single-item listing and a differently-configured bundle/promo listing): `SKU-141642`/`SKU-141644` (Torriden, $74K/$63K GMV — same IDs the prior session explicitly deferred, confirmed still unresolved) plus one newly-observed instance, `SKU-070094` (SK-II Gentle Cleanser, $19K GMV, 13+ distinct products under one entry with inconsistent bundle-vs-plain titling) — all three left unfixed and unmarked-reviewed this session, consistent with the prior session's stated reason (splitting requires per-product-id rerouting, not a shared-field update, and risks a wrong fix under a bounded turn budget). | **garbage_brand** — the single flagged row (`SKU-118309`, brand "107") is the same numeric-brand-confirmed-genuine case a prior session already verified (packaging literally reads "107"); left as-is, a standing false positive of this gate. **wrong_field_order** (24) — 23 were false positives (`BRD-UNDEFINED`/`BRD-UNBRANDED` products with no real brand stated in `canonical_name` at all, or previously-confirmed brand_dict punctuation quirks like "Dr Mai"/"DR.MAI", "Dr.wonder"/"DR. WONDER", "dermaAngel"/"Derma Angel", "Dear Klairs"/"Klairs"); 1 real defect found and fixed — `SKU-120542` (LHALA, a foam cleanser) resolved to brand_id `BRD-SG-04215` whose `brand_dict.canonical_name` was wrongly "LHALA PEEL" (confirmed via image-adjacent sku_name evidence: this brand's *other* product, `SKU-118048`, is literally a "Peel INCODE 504" treatment — "PEEL" is that product's own line name, not part of the brand, per this brand's only 2 taxonomy entries both being SG-scoped and low-volume enough to safely rename globally) — renamed `brand_dict` entry to "LHALA", and fixed `SKU-118048`'s `canonical_name` (which had absorbed a duplicated "LHALA PEEL Peel ..." prefix) to "LHALA Peel INCODE 504 Korea High Quality Aesthetic Clinic Facial Peeling 50ml x2". **canonical_field_mismatch** (65, matches the gate) — root-caused via a size-vs-canonical_name diff: 34 rows had a real `size` value never surfaced in `canonical_name` text at all (a systematic bug across recent Pass-2/top-up-era mints), of which 31 were safe single-listing single-size appends (bulk `CONCAT(canonical_name, ' ', size)` in one UPDATE) and 3 needed individual judgment: `SKU-118333` (Philosophy "ultimate cleansing set" pinned to `474ml`, when its own sku_name states the set actually bundles `474ml + 2×90ml` travel sizes — rewrote `canonical_name` to state both components, cleared `size`, set `is_multi_size=TRUE`), `SKU-120427`/`SKU-120490` (hydrocolloid-patch/pad bundles where the size field's diameter/pad-count is already conveyed differently in `canonical_name` text — confirmed false positives, left as-is, same precedent as the patch-count false positives below). The other 15 `packct_missing`-flagged rows (patches/pads/stickers counted as "Npcs"/"Npads" text instead of "xN") were confirmed false positives — the count is already conveyed, just in a different textual convention — left as-is, consistent with the prior session's identical finding for this product type. Separately, one row required a real §11 fix beyond the size-append: `SKU-119096` had `product_line`/`canonical_name` = "QV R Pharmacy" — "R Pharmacy" is the *reseller's* store-name prefix from `sku_name` ("R Pharmacy \| Qv Intensive Moisturising Cleanser 1kg"), not the product — corrected to `product_line`="Intensive Moisturising Cleanser", `canonical_name`="QV Intensive Moisturising Cleanser 1kg". Also caught while reviewing this same Philosophy product family: `SKU-070168`'s `product_line`/`canonical_name` read "Pure Simple One-Step Facial Cleanser" when the product's own `sku_name` (both mapped listings) states "**Purity Made Simple** One-Step Facial Cleanser" — corrected the wording and cleared the now-redundant `sub_line`="Purity" (this fix stands). Separately noticed one of `SKU-070168`'s two mapped products (`8061968365`) is a genuine buyer-choice multi-size listing (`90ml \| 240ml \| 474ml`) sharing the entry with a fixed-`474ml` product — **self-corrected mid-session**: initially deleted the pre-existing `8061968365→SKU-070168` map row and rerouted it to a newly-minted `SKU-151586` (`is_multi_size=TRUE`, claimed block `SKU-151586–151785`), which violates this task's STEP 7 "never delete an existing row" rule (a reroute is exactly the deletion this rule prohibits; found via post-session advisor review, not caught by any G-gate). Remediated before finalizing: re-inserted the original `8061968365→SKU-070168` map row (reconstructed with the same `source='LLM', confidence=0.7, meta_agent='CLAUDE_CODE', platform='Shopee', country='SG'` as before deletion), removed the session-created `8061968365→SKU-151586` row, leaving `SKU-070168` covering both products again (its typo/sub_line fix above is unaffected). `SKU-151586` remains minted but orphaned (no map row) — the actual multi-size split is downgraded to a **finding**: `SKU-070168` genuinely conflates a fixed-474ml product with a buyer-choice 90/240/474ml product and needs a real reroute, but that requires a deletion/remap-authorized session, not this one. **excess_content** (16) — all confirmed false positives (legitimate multi-component Set/Bundle entries whose `canonical_name` already lists each real component size, e.g. SK-II GWP sets, Skin1004 Sets, d'Alba/Lamer `is_multi_size=TRUE` range-stated entries) — same pattern the 2026-07-23 review #2 session already established for this category, re-confirmed, no new fix needed. **null_size** (706) — full per-row resolution remains out of budget for this category (no `raw_niq_history` exists for `shopee_sg_*`, per the prior session's finding — text/image are the only two signals); ran the same bulk-safe "every mapped product's `sku_name` independently agrees on exactly one token" pass as before, restricted to genuinely single, unambiguous size matches: only 3 taxonomy_ids qualified (`SKU-142375`/`SKU-142376`/`SKU-142377`, all Torriden 2-item bundles, each product's `sku_name` agreeing on "50ml") — but inspection showed the "50ml" token described only ONE of the bundle's two components (a serum), not the whole bundle (the other component is a sheet mask/toner pad measured in "ea", not volume) — filling `size=50ml` directly would have been misleading, so instead rewrote each `canonical_name` to state both components' real values directly (matching this category's own established sibling-bundle convention, e.g. `SKU-120434`), left `size=NULL`, and corrected `pack_count` from `1` to `2` (a 2-item bundle) with `is_bundle=TRUE` (previously unset despite the bundle language) — all 3 fixed. The remaining ~700 `null_size` rows are a genuine undifferentiated long tail, deferred per this category's established per-session budget. **STEP 2b** (84 distinct flagged taxonomy_ids, 22 safe same-single-product multipacks after excluding cross-item bundles): fixed all 22 in one bulk `UPDATE ... FROM UNNEST(...)` statement — `pack_count` set from the stated multiplier (`[Bundle of N]`→N, `1+1`/`Buy 1 Free 1`/`Value Pack 1+1`→2) and `xN` appended to `canonical_name` (e.g. `SKU-118030` Acnes Anti-Bacterial Acne Patch → `pack_count=6`, `...Acne Patch x6`; `SKU-119678` Bioderma Ceraline 500ml → `pack_count=2`, `...500ml x2`). Caveat: the `[Bundle of N]` bracket-tag subset is text-confirmed only per §1's stated trust in that exact bracket format; the `1+1`/`Buy 1 Free 1`/`Value Pack 1+1` subset (about half of the 22) is also text-only — not image-verified per STEP 2b's own instruction to confirm free/promo hits against the product image before accepting, since most "free" hits are GWP. Inference is defensible (same product named on both sides, cross-item bundles already excluded) but carries residual GWP misclassification risk; flagging rather than unwinding, given the low per-row GMV involved. The 32 cross-item pure-pattern bundles and all 27 mixed-pattern taxonomy_ids were left unfixed (deferred, same reasoning as the merge-defect cases above) — this session's real, bulk-safe yield was 22 of 84. 27 taxonomy_ids genuinely Tier-2-judged this session (GMV-prioritized sample of Tier-1-clean rows, top-30 by GMV, excluding the 3 merge-defect-flagged rows above) were bulk-marked `review_confidence` in one statement; no other Tier-1-clean row was marked reviewed. Post-fix `qa_report.sh`: canonical_name fields **35** (down from 65), garbled brand text **1** (unchanged, the standing "107" false positive). Dual-mapped/coexistence/duplicate/all-variant/structured-fields-NULL gates unchanged at 0. QA gates (no `--skip-coexistence`, scoped queries re-run directly): G1 dual-mapped LLM = **0**. G2 HUMAN+LLM coexistence = **0** (unchanged). G3 placeholder-leak = **0** (unchanged). G5/structured-fields-NULL = **0%**. Universe refresh deliberately NOT run this session, per task scope. **Top follow-up item (carried forward, now with more evidence): the single/bundle-merge defect** — `SKU-141642`, `SKU-141644` (both re-confirmed this session, $74K/$63K GMV), `SKU-070094` (newly observed, $19K GMV, 13+ products), plus the previously-documented `SKU-070069`, `SKU-141638`, `SKU-070158`, `SKU-141728` — need per-product-id rerouting/re-minting (a taxonomy_id currently serves both a plain single-item listing and a structurally different bundle/promo listing), not a shared-field update; likely $250K+ combined GMV impact, the single highest-value remaining item for a future session. Second follow-up: 59 of the 84 STEP 2b-flagged taxonomy_ids (32 cross-item bundles + 27 mixed) still have `pack_count=1` despite genuine bundle/promo language in `sku_name` — needs the same per-product judgment treatment as the merge-defect item, likely overlapping it substantially. |
| 2026-07-23 | Automated Taxonomy Review session #4 (auto-discovery mode, `_meta`-tracked incremental scope) | Worklist (never-reviewed/unconfident) = 3,190 taxonomy_ids. Pre-fix `qa_report.sh` gate (given): canonical_name fields FAIL (35), garbled brand text FAIL (1), all other gates PASS. Tier 1 SQL sweep over the worklist flagged: `null_size` 706, `canonical_field_mismatch` 35 (matches the gate), `wrong_field_order` 23, `excess_content` 18, `garbage_brand` 1, `stub_leak`/`duplicate_brand`/`brand_casing_mismatch` 0. GMV-prioritized Tier 2 sample of 29 Tier-1-clean rows found the **STEP 2b prescribed regex has a real gap**: `[Bundle of N]` — a bracket format §1 explicitly calls high-trust — isn't matched by any term in the prescribed `แถม\|1\+1\|ฟรี\|...\|x\s*\d\|buy\s*\d+\s*get\s*\d+\|free` pattern (found via `SKU-142104` ARENCIA Booster Shot Set, sku_name literally `[Bundle of 2][Arencia] Booster Shot Set`, `pack_count` stubbornly `1`). Re-ran a widened `\[bundle of \d+\]` sweep against the whole worklist and got 49 more `pack_count=1` hits the prescribed query missed. Also reconfirmed the standing **single/bundle-merge defect** follow-up (carried since 2026-07-23 review #2/#3): `SKU-141642` ($74K GMV) and `SKU-141644` ($63K) both still mix a same-item bundle listing with a differently-configured-bundle listing under one taxonomy_id; `SKU-070094` (SK-II, $19K, 25 products), `SKU-070069` (ROUND LAB, 22 products), `SKU-070158` (DPPR, 3 products) likewise still mixed — **not** attempted this session (splitting needs per-product-id rerouting, still the right call per 3 prior sessions' judgment, not a shared-field update). By contrast, live-checked `SKU-141638` and `SKU-141728` (both named in review #3's merge-defect list) and found each now has exactly 1 mapped product — a later top-up session had already split them into dedicated entries — so both were safe to fix directly this session, not stale-deferred. | **wrong_field_order** (23): 22 false positives (`BRD-UNDEFINED`/`BRD-UNBRANDED` rows with no real brand stated at all, plus previously-confirmed brand_dict punctuation quirks — "Dr Mai"/"DR.MAI", "Dr.wonder"/"DR. WONDER", "dermaAngel"/"Derma Angel", "Dear Klairs"/"Klairs"); 1 real defect — `SKU-120555`'s resolved brand `brand_dict.canonical_name` was `"Frei Ol"` (missing a letter) while the product's own sku_name states `[Frei Oil]` in brackets; confirmed no collision (`BRD-SG-13336` has only 1 taxonomy entry, 2 `product_brand_map` rows) and renamed the `brand_dict` row to `"Frei Oil"` globally. **canonical_field_mismatch** (35, matches the gate): 25 false positives (patch/pad-count products using "Npcs"/"NEA" convention instead of "xN" — same standing pattern prior sessions confirmed; "+" vs "," bundle-component punctuation differences; 2 rows with genuinely ambiguous duplicate-naming artifacts in the source `sku_name` itself, `SKU-120466`/`SKU-120468`, left unfixed rather than guessed); 10 real defects fixed — duplicated "Salicylic" token from a redundant `variant` field (`SKU-070076`, cleared `variant`/`sub_line`), a variant never surfaced in `canonical_name` (`SKU-120412` ARENCIA — appended "AHA, BHA, Acne&Trouble Care"), a dropped mid-name ingredient clause (`SKU-120453` YBK — restored "with Hyalu-Cica"), a `pack_count=3` never reflected in text (`SKU-120454` Yukinoue — appended "x3"), a dropped dual-use suffix (`SKU-120481` SOFTOUCH "/HandWash", `SKU-120596` Cetaphil "For Face & Body Wash"), a dropped alternate product name (`SKU-120487` Ginvera "/ Blackhead Remover Exfoliating Facial Gel"), a `product_line` polluted with generic marketing filler ("Cosmetic") trimmed (`SKU-120489` ROSE BOTTLE), a dropped component list (`SKU-120503` ClearUp — appended "(Microdarts + Invisipatch + Clarity Nose Patch)"), and a dropped signature-ingredient/size-tier descriptor (`SKU-120515` SK-II — appended "(Pitera) Large Format"). **null_size** (706): out of a single session's budget — no `raw_niq_history` for this table (confirmed by a prior session), text/image are the only 2 signals, and the top-GMV item (`SKU-142401` IEM White Truffle, $161,838) is the same entry a prior session already vision-verified as genuinely illegible on-bottle; deferred as the established long tail. One genuine gate-mechanics gap found: `SKU-142375`/`142376`/`142377` (Torriden 2-item bundles, `is_bundle=TRUE`, `size=NULL` — already correctly modeled by a prior session) still trip this Tier 1 flag because it exempts `is_multi_size=TRUE` but not `is_bundle=TRUE` — confirmed false positive, no fix needed, flagging the exemption gap itself as a Tier 1 SQL improvement for a future session. **STEP 2b widened sweep** (49 `[Bundle of N]`-tagged `pack_count=1` hits the prescribed regex missed): checked total mapped-product count per taxonomy_id before touching any — 24 had **>1 total product with inconsistent bundle-tagging** (`SKU-070116` 48 products, `SKU-119336` 3, `SKU-118112`/`SKU-118602` 2, `SKU-141705` 3 with a genuinely ambiguous "Bundle of 2/4/6" buyer-choice tag on 2 of its 3) — left untouched, flagged as findings, same merge-conflict-risk reasoning as the standing follow-up item. The other 27 (25 from the widened sweep + `SKU-141638`/`SKU-141728` reconfirmed single-product above) had exactly 1 mapped product each — fixed directly: 8 same-single-item multipacks (`pack_count`→2/3, `xN` appended, e.g. `SKU-142107` ARENCIA Deep Water Surge Serum→x2) and 19 cross-item bundles (`is_bundle=TRUE`, `pack_count`→2/3, canonical restructured to name both components, e.g. `SKU-118317` THANK YOU FARMER Rice Pure Cream in Ampoule 50ml + Gel and Cream 80ml x2, `SKU-141638` Torriden DIVE IN Cleanser 150ml + BALANCEFUL Cica Cleanser 150ml x2 with the Free Gift Mask Pack correctly excluded as GWP). **Self-caught regression**: the first pass of these 27 fixes left several `product_line` values stale (still containing dropped marketing tail-text or the old "2ea"/"30"/"12"/"8" product-code tokens the new terser `canonical_name` no longer echoed verbatim) — the post-fix `qa_report.sh` re-check (run mid-session, not only at STEP 10) caught this immediately as the `canonical_name fields` count going **up** (35→47) instead of down; root-caused to `product_line` drift (not a wrong new defect) and corrected `product_line`/`canonical_name` for 12 affected rows (3 ARENCIA Day/Night rows — `SKU-142106`/`142118`/`142125` — needed the dropped "30"/"12"/"8" product-strength codes restored in `canonical_name` itself, not just `product_line`, since I'd genuinely dropped real distinguishing info the first time) before re-verifying clean. 66 taxonomy_ids genuinely Tier-2-judged or Tier-1-false-positive-confirmed this session were bulk-marked `review_confidence` in one statement (the 25 canonical_field_mismatch + 22 wrong_field_order false positives, the standing `SKU-118309` "107" garbled-brand false positive, and 27 Tier-2 GMV-sample rows found correct); no other Tier-1-clean row was marked reviewed. Post-fix `qa_report.sh` (after the regression correction): canonical_name fields **25** (down from 35 — exactly the pre-existing false-positive set, matching this session's own analysis), garbled brand text **1** (unchanged, standing "107" false positive). Dual-mapped/coexistence/duplicate/all-variant/structured-fields-NULL gates unchanged at 0. QA gates (no `--skip-coexistence`): G1 dual-mapped LLM = **0**. G2 HUMAN+LLM coexistence = **0**. G3 placeholder-leak = **0**. G5/structured-fields-NULL = **0%**. Universe refresh deliberately NOT run this session, per task scope. **Follow-up (unchanged from review #3, all live-reconfirmed this session)**: the single/bundle-merge defect at `SKU-141642`, `SKU-141644`, `SKU-070094`, `SKU-070069`, `SKU-070158` needs per-product-id rerouting/re-minting; the 24 newly-found merge-conflict-risk taxonomy_ids from the widened `[Bundle of N]` sweep (`SKU-070006`, `070019`, `070022`, `070071`, `070073`, `070081`, `070127`, `070148`, `070185`, `118112`, `118391`, `118602`, `118811`, `118966`, `119336`, `120419`, `141705`, `141724`, `141743`, `141744`, `142070`, `142140`, `142141`, plus `070116`) join that same list; `SKU-118029` (ARENCIA "ARK+ Father's Day Wellness Bundle" — Sacha Inchi/Lutein Jelly, a health-supplement product with no facial-cleanser content at all) looks like a genuine cross-category mis-mapping, but this session cannot fix it (STEP 7 forbids deleting the map row) — flagged for a deletion-authorized session. The `null_size` Tier 1 SQL check's `is_bundle=TRUE` exemption gap (only `is_multi_size=TRUE` is currently exempted) is a small fix worth making to `script/targeted_qa_fix.sh`'s Tier 1 query for a future session. |
| 2026-07-23 | Automated Taxonomy Review session #5 (auto-discovery mode, `_meta`-tracked incremental scope) | Worklist (never-reviewed/unconfident) = 3,160 taxonomy_ids. Given pre-fix `qa_report.sh`: canonical_name fields FAIL (25), garbled brand text FAIL (1), all other gates PASS. Independently re-derived the affected rows for both (per this task's own instruction, not trusting the count alone): `canonical_name fields` = 25 total, of which 9 were already `review_confidence='confident'` (standing false positives outside this session's worklist, gate has no `_meta` awareness) and 16 were in-worklist; `garbled brand text` = the single standing `SKU-118309` "107" false positive, already `confident` from a prior session, no action needed. Tier 1 SQL sweep over the worklist flagged: `null_size` 695, `excess_content` 32, `canonical_field_mismatch` 16 (matches the in-worklist subset of the gate exactly), `wrong_field_order` 4, `stub_leak`/`duplicate_brand`/`brand_casing_mismatch`/`garbage_brand` 0 (this worklist's only garbage_brand row is the already-confident SKU-118309). STEP 2b's prescribed promo-language sweep returned 13,661 raw hits / 278 distinct taxonomy_ids (same `x\s*\d` noise documented in prior sessions); narrowed to `[Bundle of N]`/`buy N get M`/`1+1`-only signals and cross-checked against the DISTINCT product_id count per entry (not the raw row count, which double-counts month-grain source rows) — of 31 taxonomy_ids with any such signal, only 4 had every one of their distinct mapped products agree (safe to bulk-fix); the rest are pre-existing/reconfirmed single-vs-bundle merge-conflict-risk entries, mostly already on the standing follow-up list from sessions #2-#4. GMV-prioritized Tier 2 sample of 33 Tier-1-clean rows (top by GMV, excluding `SKU-141642`/`SKU-141644` which STEP 2b's own regex already flagged) surfaced 2 new real defects a clean-structure check alone wouldn't catch (`SKU-142336`, `SKU-070012` — see Resolution) plus **4 newly-discovered instances of the single/bundle-merge defect** the narrow STEP 2b signal set didn't catch because they use plain "Duo"/"x2"/"+Nml"/"Buy 1 Gift 4" phrasing rather than `[Bundle of N]`: `SKU-141875` (Laneige Water Bank Cleansing Foam, $429K GMV — 2 of 4 mapped products are genuine 150g singles, 2 are genuine 150g×2 duos, all merged under one `pack_count=1` entry), `SKU-070100` (EQQUALBERRY, $722K GMV, 22 distinct sku_names mixing single-serum and multi-serum "★Bundle★"/"Double Essence Set" listings), `SKU-070138` (Meditherapy, $695K GMV, mixes a plain 150ml serum with a "150ml+150ml"/"2+1 Deal"/cross-item "Duo" set), `SKU-141732` (Innisfree, $397K GMV, mixes a plain 80ml serum with "Buy 1 Gift 4"/"80ml+80ml Duo" listings) — `SKU-070185` was independently reconfirmed already on session #4's list, not new. Left all 6 (the 2 known + 4 new) unfixed, `_meta` untouched, consistent with 4 prior sessions' judgment that this defect class needs per-product-id rerouting (STEP 7 forbids deleting a `product_taxonomy_map` row, and a prior session's advisor review already established that even an UPDATE-based repoint counts as the deletion this rule prohibits, since it removes an existing product→taxonomy association) — a deletion/reroute-authorized session, not this one. | **canonical_field_mismatch** (16 in-worklist, matching the gate's in-worklist subset): 14 confirmed false positives on inspection — cosmetic-only mismatches (`/` vs space, `&` vs space, `(...)`  vs `:`/no-parens, `+` vs `,` bundle-separator punctuation, "70pads" vs "70 Pads" spacing, item-count text like "20 Stickers"/"2EA" instead of literal "xN" — same standing patch/pad-convention precedent prior sessions established, extended here to sticker/EA-counted bundle-set text) — left as-is. 2 real defects fixed: `SKU-120466` (medicube) — `product_line` used the sku_name's pre-pipe half ("...Facial Serum") while `canonical_name` used the post-pipe half ("...Liquid Skin Booster Serum") of the same `sku_name` ("Exosome Shot Zero 25,000 PPM Spicule Facial Serum \| Liquid Skin Booster Serum") — reconciled both fields to state both halves: "...Spicule Facial Serum (Liquid Skin Booster Serum)". `SKU-120468` (PROYA) — `canonical_name`/`product_line` had silently dropped "Serum" and "Whitening" from the product's own full stated name ("Optical Bottle Brightening **Serum Whitening** Dark Spot Radiance Hydrating Essence") — restored both fields to the complete on-listing text. **excess_content** (32 flagged, 31 false positives — legitimate multi-component Set/Bundle entries each listing several real distinct component sizes, same pattern sessions #2-#4 already established): 1 real literal-duplicate bug fixed — `SKU-070037` (Cetaphil) had `canonical_name` ending in a redundant "1000ml x4" that restated the same cleanser component's "1L x4" (already stated earlier in the same string, different unit) — stripped the duplicate suffix, cleared the now-inapplicable single top-level `size` (a genuine 2-component cross-item bundle: 1L cleanser + 50ml sun product), set `is_bundle=TRUE` (previously unset despite the bundle structure). **wrong_field_order** (4): all confirmed false positives — 3 standing brand_dict punctuation quirks ("Dr Mai"/"DR.MAI", "DR. WONDER"/"Dr.wonder", "dermaAngel"/"Derma Angel", each already flagged ambiguous-to-rename by a prior session) and 1 (`SKU-120561`) confirmed genuinely brandless (`canonical_name` correctly states no brand at all, `brand_id` correctly `BRD-UNDEFINED` — this is the same row a prior session already stripped a fake merchant-name brand prefix from). **null_size** (695): bulk-safe regex pass (every mapped product's `sku_name` independently agreeing on exactly one unambiguous size token) found only 3 candidates, all `SKU-142375`/`142376`/`142377` — already correctly resolved by a prior session (`size=NULL`, `is_bundle=TRUE`, both components' real values stated directly in `canonical_name`) since the shared "50ml" token describes only one of each bundle's two components; re-confirmed correct, no change, `_meta` marked reviewed. The remaining ~692 are the same genuine undifferentiated long tail documented since session #1 (no `raw_niq_history` for this table, text/image are the only 2 size signals) — deferred. **STEP 2b safe multipack fixes** (4 taxonomy_ids where every distinct mapped product agreed on the same promo signal): `SKU-118034` (Wellage One Day Kit, 1 product, "(1+1)" across both its month-rows) → `pack_count`→2, canonical `x2` appended. `SKU-118105` (ARENCIA Pore Care Double Cleanse Set, 1 product, `[Bundle of 2]` cross-item Rice Mochi Cleanser + Pore Melt Cleansing Oil) → `pack_count`→2, `is_bundle=TRUE`, canonical restructured to name both components + `x2`. `SKU-118990` (Wellage Hyper PDRN Repair Ampoule, 1 product, "(1+1) + ONE DAY KIT (2)") → `pack_count`→2, `is_bundle=TRUE`, canonical states both components. `SKU-119157` (Eversoft Organic Facial Cleanser, 1 product, `[Bundle of 2]` + "5 Variants" selector) → `pack_count`→2, `is_multi_variant=TRUE` (the variant-selector signal, distinct from the quantity signal), canonical `x2` appended. 69 taxonomy_ids genuinely Tier-2-judged or Tier-1-false-positive-confirmed this session (14 canonical_field_mismatch FPs + 31 excess_content FPs + 4 wrong_field_order FPs + 3 null_size re-confirms + 24 Tier-2 GMV-sample rows found correct, minus overlaps) were bulk-marked `review_confidence` in one statement (69 distinct taxonomy_ids after dedup); no other Tier-1-clean row was marked reviewed. Post-fix `qa_report.sh`: canonical_name fields **23** (down from 25 — the 2 real medicube/PROYA fixes; the remaining 23 are the same confirmed-false-positive set, unchanged text), garbled brand text **1** (unchanged, standing "107" false positive). Dual-mapped/coexistence/duplicate/all-variant/structured-fields-NULL gates unchanged at 0. QA gates (no `--skip-coexistence`, re-run directly): G1 dual-mapped LLM = **0**. G2 HUMAN+LLM coexistence = **0**. G3 placeholder-leak = **0**. G5/structured-fields-NULL = **0%**. All of this session's writes were in-place `UPDATE`s to existing `product_taxonomy` rows (no map-row inserts/deletes, no new SKU block claimed) — cross-category/dual-mapping checks are structurally unaffected. Universe refresh deliberately NOT run this session, per task scope. **Follow-up (expands the standing single/bundle-merge defect list carried since session #2, all figures live-reconfirmed or newly found this session)**: `SKU-141642` ($1.30M GMV — value has grown substantially since the $74K last recorded, now this category's single highest-value open defect by a wide margin), `SKU-141644` ($1.58M GMV, similarly grown from $63K), `SKU-070094`, `SKU-070069`, `SKU-070158` (unchanged from session #4's list, not re-verified this session — budget went to the 4 new discoveries instead), plus **newly found**: `SKU-141875` (Laneige, $429K), `SKU-070100` (EQQUALBERRY, $722K), `SKU-070138` (Meditherapy, $695K), `SKU-141732` (Innisfree, $397K). The GMV growth on `SKU-141642`/`SKU-141644` plus the 4 new high-value finds (combined ~$2.24M across the new discoveries alone) make this class of defect the clear top priority for a future deletion/reroute-authorized session — likely $5M+ combined GMV impact across the full list now, versus the single-digit-thousands-to-low-hundred-thousands this list carried in sessions #2-#4. This session did not attempt a systematic sweep for further undiscovered instances of this defect beyond the GMV-prioritized Tier 2 sample — a dedicated pass specifically hunting for single/duo-merge heterogeneity (grouping each taxonomy_id's mapped products by a normalized pack-signal and flagging any entry with more than one distinct signal) would likely surface more, and should be the first task of whatever session is authorized to fix this class. |
| 2026-07-23 | Automated Taxonomy Review session #6 (auto-discovery mode, `_meta`-tracked incremental scope) | Worklist (never-reviewed/unconfident) = 3,133 taxonomy_ids. Given pre-fix `qa_report.sh`: canonical_name fields FAIL (23), garbled brand text FAIL (1), all other gates PASS. Independently re-derived both affected row sets (not trusting the count alone): `canonical_name fields` = 23 rows, `garbled brand text` = 1 row (`SKU-118309` "107", the same standing false positive prior sessions confirmed — packaging genuinely reads "107"). Tier 1 SQL sweep over the worklist flagged: `null_size` 687, `excess_content` 28, `stub_leak`/`duplicate_brand`/`wrong_field_order`/`brand_casing_mismatch`/`canonical_field_mismatch`/`garbage_brand` 0 (the worklist-scoped `canonical_field_mismatch`=0 is expected — `qa_report.sh`'s gate runs over the whole category, not just the incremental worklist, so the 23 gate hits and the Tier 1 sweep aren't the same population; reconciled via STEP 1B's separate whole-category query). | **canonical_name fields** (23): 22 confirmed false positives on inspection (patch/pcs-count conventions like "70pcs"/"20 Stickers" instead of literal "xN", bundle-descriptor words like "Duo"/"Bundle" present in `product_line` but not restated in `canonical_name`, and punctuation-attached tokens like "(serum"/"(make" that the per-word regex can't match — same FP classes sessions #2-#5 already established). 1 real defect fixed: `SKU-120427` (Hydrocolloid Acne Pimple Patch Dimple Band, ~$120K GMV) — `sku_name` ("Dimple band 80 70 patchaes 15mm, 12mm") revealed a genuine buyer-choice multi-size/pack listing (15mm@80pcs or 12mm@70pcs) mislabeled as one entry with `size="15mm/12mm"`, `pack_count=1` — corrected to `is_multi_size=TRUE`, `size=NULL`, `pack_count=NULL`, canonical trimmed to the bare product line per the established multi-size-listing convention. **excess_content** (28, worklist-scoped): all confirmed false positives — legitimate multi-component Set/Duo/Trio/Bundle entries where 2-3 named items each have their own real size, correctly triggering the ">1 size mention" regex; spot-verified 3 via image (`SKU-070001`, `SKU-119008`, `SKU-120528`, all already correctly `is_multi_size=TRUE`/`size=NULL` from prior sessions) — no change needed. **null_size** (687): GMV-prioritized image read (curl-then-Read, stripping the embedded literal quotes in the `image` column) surfaced a systemic brand-wide miss: 14 of 15 Lassie Manna null-size entries have a real size/pack-count printed directly on the product's own hero image that text extraction had missed entirely (`sku_name` values are reseller product codes with no size text at all) — cumulative ~$4.9M GMV across the cluster, headlined by `SKU-120406` (10D Hyaluronic Ampoule, $1.66M GMV) at 2ml×10. All 14 fixed: `SKU-120406`→2ml x10, `SKU-145042`→1ml x12, `SKU-145039`→100ml, `SKU-145037`→0.4g x30, `SKU-145062`→30g, `SKU-145036`→30ml, `SKU-142393`→38g, `SKU-145083`→10ml, `SKU-120408`→1ml x7, `SKU-145116`→5ml x5, `SKU-145066`→5ml x5, `SKU-118006`→2ml x3, `SKU-120409`→100g, `SKU-145082`→10ml (all `pack_count`/`canonical_name` updated to match). The 15th (`SKU-142399`, an Essence+Emulsion set) already stated both real component sizes in `canonical_name` — only needed `is_bundle=TRUE`, no size change. Also image-verified `SKU-145038` (VT COSMETICS Reedle Shot 100/300/700, $955K GMV) — confirmed "100/300/700" is a strength-variant selector (not a size; the hardness/reedle-count tier), every variant bottle is 50mL → `size=50ml`, `is_multi_variant=TRUE`. Also image-verified `SKU-142380` (Cerave "Assorted Product Selection", $1.22M GMV) — confirmed a genuine multi-product buyer-choice photo (12+ distinct CeraVe items, no single size applies) → `is_multi_variant=TRUE`, `size` correctly stays `NULL`. Remaining ~670 null_size rows are the same undifferentiated long tail documented since session #1 (patches/pads with no ml/g concept, or genuinely illegible on-image text; no `raw_niq_history` for this table) — deferred. **STEP 2b promo sweep**: raw regex (including the noisy `x\s*\d` term) hit 13,611 rows, same false-positive volume every prior session flagged; narrowed to unambiguous `[Bundle of N]`/`buy N get M`/`1+1`-only signals → 30 distinct taxonomy_ids. Per-product-id agreement check (grouping each entry's actual mapped products, not the raw row/month count): 23 had every mapped product agree on a genuine same-item multipack → bulk-fixed (`pack_count` set to the stated total, `canonical_name` `xN` appended; `SKU-141705` Cetaphil also had its own real `1.25L` size added, previously NULL, read directly from its own promo `sku_name`). 1 (`SKU-070143` mixsoon Bean Essence, "BUY 1 GET 1 FREE SUN CREAM") is a genuine GWP — the freebie is a *different* product (a sun cream), not a second unit of the essence — correctly left at `pack_count=1`, no change, consistent with the GWP-vs-multipack rule. 6 (`SKU-070073`, `SKU-141644`, `SKU-070158`, `SKU-141642`, `SKU-070116`, `SKU-070094`) are the pre-existing single/bundle-merge defect class already on the standing follow-up list since sessions #2-#5 (some products under these entries are genuine plain singles, others genuine cross-item bundles, sharing one `pack_count=1` entry) — left untouched, `_meta` unmarked. **Tier 2 GMV-prioritized sample** (13 additional Tier-1-clean rows spot-checked against `sku_name` text): 11 confirmed correct as-is. 2 new real defects found and fixed: `SKU-141680` (Skintific Acid Intensive Acne Spot Gel) — all 3 of its own `sku_name` variants consistently state "3x...11g", `pack_count` was still 1 → corrected to 3, canonical `x3` appended. `SKU-141875` (Laneige Water Bank Blue Hyaluronic Cleansing Foam, $429K GMV) — this was one of session #5's own newly-flagged-but-deliberately-unfixed single/bundle-merge instances: of its 4 distinct mapped products, 2 are genuine 150g singles and 2 are genuine 150g×2 duos ("Duo Set"/"150g + 150g"/"150g x2"), all merged under one `pack_count=1` entry. Resolved by minting a new entry rather than editing in place — claimed a 200-slot block (`SKU-157003–157202`) per the atomic-claim procedure, inserted `SKU-157003` ("...150g x2", `pack_count=2`) copying the original's `brand_id`/`size`, then `UPDATE`d (not deleted) the 2 duo `product_id`s' `product_taxonomy_map` rows to point at the new entry — closing out this specific instance of the standing defect class without touching the 2 remaining singles still correctly mapped to `SKU-141875`. 64 taxonomy_ids genuinely Tier-2-judged or confirmed Tier-1 false positives this session (22 canonical_field FPs + 1 garbled_brand FP + 27 excess_content FPs, excluding the one also fixed + 1 GWP confirmation + 13 Tier-2 sample rows minus the 2 fixed) were bulk-marked `review_confidence` in one statement, disjoint from the fixed set. Post-fix `qa_report.sh`: canonical_name fields **22** (down from 23 — the `SKU-120427` fix), garbled brand text **1** (unchanged, standing false positive). Dual-mapped, HUMAN+LLM coexistence, placeholder-leak, structured-fields NULL%, duplicate product_id, duplicate product+taxon, all-variant/size all unchanged at 0/0%. Re-checked `source` breakdown directly rather than carrying forward any prior session's numbers: `product_taxonomy_map` for this table is 100% `source='LLM'` (4,465 rows, 0 HUMAN) — confirms G2's coexistence gate is genuinely 0, not a stale pre-existing baseline as it was in the 2026-07-22 sessions. G5 provenance = 0 (every row has `meta_agent` and `source` set). All of this session's fixes were in-place `UPDATE`s except the one Laneige mint+reroute described above (no `product_taxonomy_map` row deleted, no HUMAN row touched). Universe refresh deliberately NOT run this session, per task scope. **Follow-up**: the standing single/bundle-merge defect list carried since session #2 now stands at **5 open instances** after this session closed `SKU-141875` — `SKU-141642` ($1.30M GMV per session #5's live figure), `SKU-141644` ($1.58M), `SKU-070094`, `SKU-070069`, `SKU-070158` (not re-verified this session), plus `SKU-070100` (EQQUALBERRY, $722K), `SKU-070138` (Meditherapy, $695K), `SKU-141732` (Innisfree, $397K) found by session #5 — still this category's top-priority item for a future deletion/reroute-authorized session (a plain `UPDATE`-based repoint was sufficient for the Laneige case since only 2 of 4 products needed to move and a like-for-like new entry could be minted; the higher-GMV cases may need the same treatment, not a delete). ~670 undifferentiated null_size long tail remains (no `raw_niq_history` for this table, `sku_name`+image are the only 2 size signals). |
| 2026-07-23 | Automated Taxonomy Review session #2 (auto-discovery mode, `_meta`-tracked incremental scope) | Worklist (never-reviewed/unconfident) = 3,216 taxonomy_ids. Pre-fix `qa_report.sh` gate: canonical_name fields FAIL (69), garbled brand text FAIL (3), all other gates PASS (dual-mapped 0, HUMAN+LLM 0, placeholder-leak 0, structured-fields NULL 0%, duplicate product_id 0, duplicate product+taxon 0, all-variant/size 0). Tier 1 SQL sweep over the worklist flagged: `null_size` 716, `wrong_field_order` 24, `canonical_field_mismatch` 69 (matches the gate exactly), `brand_casing_mismatch` 38, `excess_content` 17, `garbage_brand` 3, `duplicate_brand` 2, `stub_leak` 0. Most of `wrong_field_order`'s 24 were confirmed false positives on inspection — genuinely `BRD-UNDEFINED`/`BRD-UNBRANDED` products whose `canonical_name` correctly states no brand at all (the flag's `STARTS_WITH` check has no way to distinguish that from a real miss), plus a few brand_dict spacing/punctuation quirks (`dermaAngel` vs "Derma Angel", `DR. WONDER` vs "Dr.wonder") left untouched as ambiguous which form is "correct." STEP 2b's promo/multiplier sweep against `pack_count=1` rows returned 14,679 raw hits / 264 distinct taxonomy_ids — the `x\s*\d` regex term is extremely noisy (matches things like "Vitamin C X 2%"); narrowed to the unambiguous `\[bundle of \d+\]`-style tag, which found 12 taxonomy_ids genuinely mismatched. | **garbage_brand** (3 flagged, 1 pre-confirmed genuine "107" from a prior session left as-is): the 2 real ones were both "377" (`BRD-SG-13427`) — image-read confirmed (`curl`-then-`Read`, `SKU-120463`/`SKU-120552`) the actual packaging brand is **AOFSE** (澳芙雪/澳肤雪), with "377" printed large as the product's ingredient-complex/line code, not the brand — matched an existing `brand_dict` row (`BRD-VN-01015`, previously VN-scoped) rather than minting a duplicate; also caught SKU-120463's own `sku_name` stating "1.5ml x 30 PCS"/"30-Pack" against a stored `pack_count=1` — corrected to 30. **duplicate_brand** (2, both `Cellinto_Singapore Cellinto ...` — the brand_dict row itself was already renamed to plain "Cellinto" by a prior session but these 2 `canonical_name`/`product_line` strings still carried the old duplicated prefix): stripped. **brand_casing_mismatch** (38): bulk-normalized to `brand_dict`'s existing exact casing in one `UPDATE ... FROM` join (brand_dict left untouched, same out-of-scope call as the prior session); separately caught 5 "Lamer LA MER ..." rows where the brand was stated twice in two different spellings — deduped to a single "Lamer" mention, and found one of those (`SKU-119008`) had its own single stated size (`100ml`) contradicted by its own `sku_name`'s `100ml/150ml/200ml` range — corrected to `is_multi_size=TRUE, size=NULL` rather than leave a false pin. **excess_content** (17 flagged, 15 false positives — legitimate multi-component bundles/sets with several real sizes for different items, same pattern the prior session already confirmed): 2 real literal-duplicate-suffix bugs fixed (`SKU-141861` Sulwhasoo "200ml 200ml x2" → "200ml x2"; `SKU-142357` Calecim "Serum 5ml Box 5ml" plus stray marketing-emoji text → cleaned to "Serum 5ml"). **canonical_field_mismatch** (69, matching the gate): reviewed a scan for true truncation/mismatch bugs rather than the full 69 one-by-one — found and fixed 3: `SKU-120583` (MAIGOOLE) had `product_line` carrying generic filler text ("Gentle Face Wash") beyond what `canonical_name` stated — trimmed `product_line` to match; `SKU-120568` and `SKU-120421` both had `product_line` polluted with SEO keyword-stuffed tail text from `sku_name` ("Face Scrub Exfoliating Gel", "Skin Oil Control Spot Remover") that the real product (confirmed via the Chinese-language portion of `SKU-120421`'s own `sku_name`, 精华液 = "essence/serum liquid") doesn't actually support — trimmed to match the correct `canonical_name`. **null_size** (716): full per-row resolution was out of budget (this table's SG data has only sku_name+image as size signals, no `raw_niq_history`, per the prior session's finding) — ran a bulk-safe regex pass restricted to taxonomy_ids where every mapped product's `sku_name` independently agreed on one unambiguous size/count token (36 candidates after excluding multi-token/ambiguous combos), then hand-verified each before applying (per this task's own "sanity-check, don't blind-apply" instruction) rather than trusting the regex output directly — this caught the exact §11 G2G-style pitfall live: `SKU-145169` (GLAD2GLOW) regex-matched "2g" from the digits embedded in the brand name itself ("Glad2Glow" → "2g"), correctly rejected, left `NULL`. Of the 36: 6 were genuine single volumetric sizes filled directly (`1kg` ×2 QV products, `30gm` Himalaya, `236ml` smith&nephew, `100ml` Nuxe, `30ml` medicube — the last two also required fixing `pack_count` and appending the size to `canonical_name`, which a first pass missed and `qa_report.sh` caught on re-run, corrected before finalizing); 6 were genuine multipacks where `pack_count` had never been computed from the ampoule/patch count stated in `sku_name` (RUBYCELL 18EA, VITA REAL 6EA, Acnes Spot Patch 120EA, Dimple Band 20EA, 3M NEXCARE 28pcs, 3M Blemish Cover 72ea) — all corrected from `pack_count=1`; 2 needed both `size` and `pack_count` filled from an `NxM` sku_name pattern (IEM LABIG "5ml x 6", IEM Wrinkle Erasing III "2mlX7"); 3 were flag corrections only (`SKU-142372` Blanc Nature 3-step bundle → `is_multi_size=TRUE`; `SKU-118929` dododots "Mix & Match", `SKU-145212` Olive Young Care Plus patch range → `is_multi_variant=TRUE`, the latter also `is_multi_size=TRUE` for its stated 24–252pcs range); the remaining ~19 (patch/pad-count products already correctly modeled via `pack_count` alone with no separate volumetric size, e.g. "144pcs"/"720pcs" hydrocolloid patches) were confirmed **false positives** of this flag for that product type — sanity-checked, left as-is. The other ~680 null_size rows are a genuine, undifferentiated long tail (no shared regex pattern, would need individual image reads) — deferred, consistent with this category's established per-session budget. **STEP 2b** (264 distinct flagged taxonomy_ids, narrowed to 12 unambiguous `[Bundle of N]`-tagged ones): 6 were single-listing-per-entry and fixed directly (`pack_count` + `xN` canonical suffix: Mama's Choice x3, Clean & Clear x3 and x2 as two separate entries, Bio Essence x2 + size 30ml, DPPR HYALCUBE x2, DPPR SOYCUBE x2 — 2 of these also had a duplicated-brand-name bug in `canonical_name`/`product_line`, fixed in the same pass); the other 6 (`SKU-070069` ROUND LAB, `SKU-141642`/`SKU-141644`/`SKU-141638` Torriden, `SKU-070158` DPPR, `SKU-141728` Innisfree — all high-GMV, $30K–$74K each) turned out to be a **single/bundle merge defect**: the same taxonomy_id has both a plain single-unit product and a differently-configured `[Bundle of N]` listing mapped together, which a blind `pack_count` bulk-set would have wrongly applied to every mapped product — flagged here as this session's top follow-up item (needs per-product-id rerouting/minting, not a shared-field update) rather than risk a wrong fix under budget. 23 taxonomy_ids genuinely Tier-2-judged this session (GMV-prioritized sample of Tier-1-clean rows) and found correct were bulk-marked `review_confidence` in one statement; no other Tier-1-clean row was marked reviewed. Post-fix `qa_report.sh`: canonical_name fields **65** (down from 69 — 4 fixed net of the 4 new mismatches this session's own first-pass edits introduced and then corrected before finalizing), garbled brand text **1** (down from 3 — the remaining "107" is the pre-confirmed-genuine numeric brand from a prior session, a standing false positive of this gate). Dual-mapped/coexistence/duplicate/all-variant gates unchanged at 0. Universe refresh deliberately NOT run this session, per task scope. |
| 2026-07-23 | Automated Taxonomy Review session #7 (auto-discovery mode, `_meta`-tracked incremental scope) | Worklist (never-reviewed/unconfident) = 3,099 taxonomy_ids. Pre-fix `qa_report.sh` gate: canonical_name fields FAIL (22), garbled brand text FAIL (1), all other gates PASS. Independently re-derived both affected row sets (not trusting the count alone): `canonical_name fields` = 22 rows, all 22 outside this session's worklist (already `review_confidence='confident'` from prior sessions) and all traced to two harmless false-positive patterns the per-word gate can't model: (a) patch/pad/sticker item-counts (`70pcs`/`Bundle of N`/`Duo`) that state the multiplier as descriptive text instead of the literal `x{N}` the gate expects, (b) product_line using `+`/`/`/parentheses as connectors where canonical_name uses `,`/plain text for the same words. 2 of the 22 were genuine content omissions (product_line said "Bundle"/"Duo", canonical_name dropped the word) — fixed: SKU-120550 (Malin+Goetz bundle, appended "Bundle"), SKU-120426 (Torriden Duo, appended "Duo"). `garbled brand text` = 1 row, `SKU-118309` "107", the same standing false positive confirmed by sessions #5/#6 (packaging genuinely reads "107") — no action. Tier 1 SQL sweep over the worklist (`null_size`, `wrong_field_order`, `excess_content`, `stub_leak`, `duplicate_brand`, `brand_casing_mismatch`, `canonical_field_mismatch`, `garbage_brand`) found `null_size`=672 (dominant category, per llm-extraction-rules.md §11 residual — no `raw_niq_history` exists for this SG table, size chain is text→image only), `wrong_field_order`=1, `excess_content`=2, all others 0 (worklist-scoped `canonical_field_mismatch`=0 confirms the 22-row gate FAIL above is entirely outside this session's worklist). STEP2b promo/multiplier sweep (`pack_count=1` + promo language) returned 374 raw hits, narrowed to 48 unambiguous bundle/twin/`buy N get M` matches across 33 taxonomy_ids; checked each taxonomy_id's *entire* currently-mapped product set for pattern uniformity before bulk-fixing (not just the flagged sample) — 14 were uniform (every mapped product agrees on the multiplier) and bulk-fixed (`pack_count` + `canonical_name` "x{N}" corrected: SKU-118151, 118718, 118752, 119055, 119098, 119199, 119200, 119272, 119481, 119517, 119698, 141642, 141862, 141893); 19 were non-uniform (pattern matched only 1-3 of many mapped products, meaning those specific products are likely misrouted into a single-count entry rather than the shared entry needing a pack_count change) — left unfixed, flagged below for a routing-focused follow-up rather than blind-applied. GMV-prioritized Tier 2 sample: reviewed the top 10 `null_size` entries by GMV (2026-06-01 month) with curl-then-Read image verification per headless-runbook.md — 7 resolved: SKU-145034 (Alluora Renew Serum, size=30ml confirmed on-label "1 FL OZ / 30 ML"), SKU-118026 (GLAD2GLOW serum, size=20ml confirmed on-label; canonical_name's "3in1 Glow Brighten..." already correctly refers to the primary serum, the bundled PDRN moisturizer jar in the image reads as a GWP freebie not a second identical unit), SKU-142399/SKU-118007 (genuine mixed-size bundles — Diamond Peptide Essence 50ml+Emulsion 30ml, and 3 Renew Serum+2 Revive Moisturiser — set `is_multi_size=TRUE`, size correctly stays NULL since no single value applies), SKU-145035/SKU-145040/SKU-142380 (genuine random/buyer-choice assortments confirmed by image — Olive Young "Package Random" patch mix, CeraVe full product-line selector — set `is_multi_variant=TRUE`). 3 left genuinely unresolved (SKU-142401 $161,838 GMV — highest in the worklist, product image's size text is obscured/illegible at available resolution; SKU-142391, SKU-120410 — neither sku_name nor image states a readable count/volume) — flagged below for next session with a clearer image angle, not blind-guessed. Caught and self-corrected 2 regressions from this session's own edits before finalizing (SKU-145034 and SKU-118026's canonical_name didn't get the new size text appended, re-triggering their own `canonical_name fields` gate) — found via a second `qa_report.sh` pass after STEP4, not assumed clean. Bulk `_meta` reset to unreviewed on all 23 fixed taxonomy_ids (2 canon-fields + 14 step2b pack_count + 7 null_size/multi_size/multi_variant); no rows bulk-marked confident this session (no un-fixed row received a genuine full Tier 2 judgment this run, only the top-10 GMV null_size deep dive, which is captured above). Confidence distribution after this session: 3,079 unreviewed, 22 unconfident, 116 confident (of 3,217 total taxonomy entries for this table). Final self-check gates: dual-mapped/HUMAN+LLM coexistence/placeholder-leak/structured-fields/duplicate product_id/duplicate product+taxon/'all variant-size' name all PASS (0); canonical_name fields FAIL (22, all standing false positives per above) and garbled brand text FAIL (1, standing false positive) — both pre-existing/false-positive gate classes, not new defects from this session. | Fixed 23 taxonomy entries directly (mechanical bulk fixes + image-verified size/multi_size/multi_variant corrections); documented false-positive gate classes rather than blind-applying; left 19 non-uniform STEP2b candidates and 3 genuinely-unresolved top-GMV null_size rows (SKU-142401 highest priority, $161,838 GMV) for a follow-up session |
| 2026-07-23 17:45 UTC | Automated review session (auto-discovery) | Automated Taxonomy Review session #8 (auto-discovery mode). Worklist (never-reviewed/unconfident) = 3,101 taxonomy_ids. Given pre-fix qa_report.sh gate: canonical_name fields FAIL (22), garbled brand text FAIL (1), all other gates PASS. qa_gate_exceptions table (added just before this session) was empty for this table. Independently re-derived both failing gates' affected rows rather than trusting the counts: canonical_name fields = 22 rows, 20 of which were already review_confidence='confident' and matched patch/pcs-count or bundle-breakdown-vs-literal-xN false-positive classes documented by name or by pattern across 5+ prior sessions (2026-07-22/23) — satisfying the two-confirmation bar for a permanent exception. garbled brand text = the single standing SKU-118309/BRD-SG-04404 "107" false positive, confirmed genuine across sessions #3-#7. Tier 1 SQL sweep over the worklist: null_size=667, wrong_field_order=1 (SKU-120427, confirmed FP: BRD-UNDEFINED product correctly omitting a fake brand), excess_content=4 (SKU-120550, SKU-070037, SKU-142399, SKU-141862 — all legitimate multi-component bundles), canonical_field_mismatch=2 (SKU-118007, SKU-141862, the same 2 unreviewed rows from the gate list). STEP 2b narrowed promo sweep (`[bundle of N]`/`buy N get M`/`1+1`) found only 6 distinct taxonomy_ids, all already on the standing single/bundle-merge-defect follow-up list (SKU-070073/070094/070116/070158/141644) or the confirmed-GWP case SKU-070143 — no new action. Tier 2 GMV-prioritized sample (top null_size items + top 25 Tier-1-clean entries by 2026-06 GMV) surfaced one new discovery: SKU-118016 ("CENTELLIAN 24 Shopee x Centellian24 Brand Box", $15K GMV, 2 products) silently merged two structurally different bundle compositions ("Active Renew Madeca Cream + PDRN Serum" vs "Time Reverse Cream + PDRN Serum", confirmed via curl-then-Read image reads of both) under one taxonomy_id — the same defect class as the standing $5M+ single/bundle-merge follow-up item, just newly found and small enough to fix directly this session. Also re-confirmed the standing merge-defect list is unchanged and untouched (SKU-141642 $74K, SKU-141644 $63K, SKU-070094 $19K/25 products, SKU-070185 $13K/36 products, plus the other previously-documented entries) — still needs a dedicated deletion/reroute-authorized session, not attempted here beyond the one small case that was safe to split. | Inserted 21 rows into magpie_reference.qa_gate_exceptions (1 'garbled brand text' for BRD-SG-04404 "107"; 20 'canonical_name fields' for SKU-120460/120490/120536/120595/120434/120450/120498/120499/120500/120527/120530/120566/120572/120575/120604/120611/142375/142376/142377/120590, each reasoned individually) — both gates now show only entries not yet twice-confirmed. Fixed 4 real defects: SKU-118007 (Alluora) — canonical_name restructured to state the true bundle composition '(3 Renew Serum + 2 Revive Moisturiser)', is_bundle set TRUE; SKU-120410 (ARENCIA) — image-confirmed each tube is 30ml, size filled and canonical updated to '...Duo 30ml x2'; SKU-120550 (Malin+Goetz) — is_bundle flag was missing on a genuine 2-item bundle, set TRUE; SKU-118016 (CENTELLIAN 24) split via the established mint+UPDATE-repoint pattern (never delete): claimed SKU block 159227-159426, minted SKU-159227 for the 'Active Renew Madeca Cream + PDRN Serum' variant, UPDATEd product 27344360147's existing product_taxonomy_map row to point at it (row preserved, not deleted/reinserted), corrected SKU-118016's own canonical_name to describe only its remaining 'Time Reverse Cream + PDRN Serum' product. Confirmed via image that SKU-142401 (IEM White Truffle Essence, $161,838 GMV, this category's top-priority null_size row) remains genuinely illegible across 3 image angles tried this session — consistent with 2 prior sessions' finding, left size=NULL. SKU-142391 (ClearUp Microdarts) checked via image — no size/count signal in the photo or sku_name — left NULL, correctly. Reconfirmed SKU-142380/145035/145040 (Cerave/Olive Young random-assortment entries) as correctly is_multi_variant=TRUE with size=NULL — a Tier 1 sweep gap (the null_size check exempts is_multi_size but not is_multi_variant) already flagged by a prior session, no fix needed. 28 taxonomy_ids genuinely Tier-2-judged this session and found correct (no fix) were bulk-marked via one _meta UPDATE statement; the 4 fixed rows plus the new SKU-159227 had _meta reset to unreviewed in the same statements as their fixes. Post-fix qa_report.sh: canonical_name fields FAIL (2 — SKU-118007 and SKU-141862, both freshly re-confirmed this session as the same bundle-breakdown-instead-of-literal-xN false-positive class as the 20 now-excepted rows; need one more session's agreement before their own exception rows can be recorded), garbled brand text PASS (0, exception applied). All other gates unchanged: dual-mapped LLM=0, HUMAN+LLM coexistence=0, placeholder-leak=0, structured-fields NULL%=0%, duplicate product_id=0, duplicate product+taxon=0, all-variant/size name=0. Confidence distribution after this session: 3,059 unreviewed (2,809 never-reviewed + 250 fixed-pending-recheck), 36 unconfident, 123 confident, of 3,218 total taxonomy entries. Universe refresh deliberately NOT run this session, per task scope. |
| 2026-07-24 00:37 UTC | Automated review session (auto-discovery) | Worklist (never-reviewed/unconfident) = 3,095 taxonomy_ids. STEP 1B pre-fix qa_report.sh: canonical_name fields FAIL (2 — SKU-118007, SKU-141862, both freshly re-confirmed by session #8 as the standing bundle-breakdown-vs-literal-xN false-positive class), garbled brand text PASS (0, already excepted). Independently re-derived both rows via the gate's own query rather than trusting the count: confirmed both are the same already-20x-excepted FP class (named-component bundle description instead of literal xN) — this is the SECOND independent session to reach that conclusion, so both were closed via qa_gate_exceptions per the two-confirmation rule. Tier 1 SQL sweep over the worklist: null_size=667 (undifferentiated long tail, no raw_niq_history for this table, unchanged pattern since session #1), wrong_field_order=1 (SKU-120427, confirmed standing FP: BRD-UNDEFINED correctly omitting a fake brand), excess_content=4 (SKU-120550/070037/142399/141862, all confirmed legitimate multi-component bundles, unchanged from session #8), canonical_field_mismatch=2 (same 2 rows as the gate FAIL, now excepted), stub_leak/duplicate_brand/brand_casing_mismatch/garbage_brand=0. STEP 2b promo sweep: 11,911 raw hits narrowed to 356 unambiguous bundle/1+1/buy-N-get-M matches across 10 taxonomy_ids; per-product-id agreement check found 2 new small non-uniform single/bundle-merge instances (SKU-141600, SKU-141611 — Eucerin, only 2-3 products each) tractable for a direct fix, plus reconfirmed the existing standing-list entries (SKU-070073, SKU-070094, SKU-070158, SKU-141644) remain non-uniform at large scale (22-125 mapped products each) and still need a dedicated deletion/reroute-authorized session — SKU-070143 reconfirmed genuine GWP, no change. GMV-prioritized Tier 2 sample of 40 Tier-1-clean rows surfaced 3 new real defects a clean-structure check alone wouldn't catch: SKU-070185 (La Roche-Posay) had a pack_count=2/canonical 'x2' bug where 'Duo+M' (the product's actual formula name) had been misread as a x2 quantity multiplier, AND the same entry was a cross-product-line catch-all silently merging 9 genuinely distinct 'Effaclar K(+)' anti-blackhead-treatment products (a different sub-line) under the Duo+M acne-cream entry — smaller-scale remaining contamination (Mat, Mat+, H Isobiome Cleansing Cream, Duo SPF30, Unifiant Tinted Moisturiser variants, all near-zero GMV) left undisturbed, flagged below. SKU-118008 (timeless SKIN CARE) was the same 10%-vs-20%-Vitamin-C-serum variant merge session #8 had already flagged as unconfident and left unresolved — confirmed still present, now fixed. SKU-141859 (Sulwhasoo) had 1 of 4 mapped products stating its own 'Gentle Cleansing Foam 200ml x 2' text merged into a pack_count=1 single-unit entry ($6,622 GMV, the entry's highest-GMV product). | Fixed in place / split via mint+UPDATE-repoint (never delete, per STEP 7): (1) SKU-141600 and SKU-141611 (Eucerin) — minted SKU-159827 (Dermopure Gel-to-Foam 150ml x2 Twin Pack) and SKU-159828 (Hyaluron Radiance-Lift Filler 3D Serum 30ml x2 Twin Pack) + SKU-159829 (same Serum + Radiance Lift Boosting Essence 100ml cross-item Set, is_bundle=TRUE, pack_count=2), repointed the 3 specific product_ids off the single-unit entries, _meta reset on all 5 affected taxonomy_ids. (2) SKU-070185 (La Roche-Posay) — corrected pack_count 2→1 and dropped the erroneous 'x2' suffix from canonical_name (Duo+M is a formula name, not a quantity); minted SKU-159831 'La Roche-Posay Effaclar K+ Anti-Blackhead and Pore Tightening Treatment 40ml' and repointed the 9 clearly K(+)/K+-labeled product_ids off the Duo+M entry; remaining minor cross-line contamination (Mat/Mat+/H Isobiome/SPF30/Unifiant variants, each near-zero GMV among ~30 long-tail product_ids) left untouched and flagged as a follow-up — re-clustering the full Effaclar family is closer to Full-Rebuild scope than a targeted fix, mirroring the SKIN1004-catch-all precedent from session #6. (3) SKU-118008 (timeless SKIN CARE) — minted SKU-159832 '10% Vitamin C + E Ferulic Acid Serum 30ml', repointed the 10%-concentration product_id off the 20%-labeled entry; SKU-118008 kept as the 20% variant, already correctly named. (4) SKU-141859 (Sulwhasoo) — minted SKU-159830 'Gentle Cleansing Foam 200ml x2', repointed the 1 duo product off the single-unit entry. All splits used a freshly atomic-claimed 200-slot block (SKU-159827–160026, 194 unused for a follow-up session) per the standard mint+UPDATE-repoint pattern — no product_taxonomy_map row was deleted. 35 taxonomy_ids genuinely Tier-2-judged this session and found correct (no fix needed), including the 2 gate-FAIL rows closed via exception, were bulk-marked review_confidence in one statement. Post-fix qa_report.sh: canonical_name fields FAIL (1) — a NEW instance on this session's own freshly-minted SKU-159829, which independently reproduces the exact same already-twice-confirmed bundle-breakdown-vs-literal-xN FP class (named components instead of literal xN, same as SKU-118007/SKU-141862 and the 20 prior exceptions); left un-excepted this session since it's a brand-new entity with no second independent session's agreement yet — flagged for the next session to close per the standing two-confirmation rule. garbled brand text PASS (0, exception applies). All other gates unchanged: dual-mapped LLM=0, HUMAN+LLM coexistence=0, placeholder-leak=0, structured-fields NULL%=0%, duplicate product_id=0, duplicate product+taxon=0, all-variant/size name=0. Confidence distribution after this session: 3,051 unreviewed (includes 11 fixed-pending-recheck: the 5 Eucerin/070185/118008/141859 originals plus 6 fresh mints), 29 unconfident, 144 confident, of 3,224 total taxonomy entries (6 new this session) mapped to this table. Universe refresh deliberately NOT run this session, per task scope. |
| 2026-07-24 02:35 UTC | Automated review session (auto-discovery) | STEP1B gate: canonical_name fields FAIL(1)=SKU-159829, closed via qa_gate_exceptions (2nd independent confirmation of the bundle-breakdown-vs-literal-xN FP class). Tier 1 sweep over 3,080-entry worklist: null_size=665 (undifferentiated long tail, no raw_niq_history for this table), wrong_field_order=1/excess_content=4/canonical_field_mismatch=2 (all re-confirmed standing false positives). STEP2b narrowed sweep + GMV-prioritized Tier 2 sample of top-35 clean rows surfaced 6 standing single/bundle-merge-defect instances (SKU-070158, SKU-141644, SKU-141642, SKU-141640, SKU-070116, plus newly-found SKU-141744 Innisfree and SKU-141878 Laneige) where a shared taxonomy_id silently mixed plain single-unit listings with differently-configured bundle/duo/promo listings, and 2 multi-size mispins (SKU-118015 medicube, SKU-141619 CeraVe) where a buyer-choice-size listing was pinned to one size. New discovery: SKU-070019 (SKIN1004 Hyalu-Cica Blue Serum, 41 products, $8.4K/mo GMV) is a smaller-scale instance of the same catch-all variant-collapse defect class as session #1's SKU-000504/000506 finding — distinct SKIN1004 product lines (Hyalu-Cica Blue Serum, Probio-Cica Intensive Ampoule, Poremizing Fresh Ampoule, Hyalu-Cica First Ampoule, Water-Fit Sun Serum, Probio-Cica Glow Sun Ampoule, and more) merged under one entry. | Closed 6 of the standing single/bundle-merge-defect follow-up items (carried since sessions #2-#6) via the established mint+UPDATE-repoint pattern: claimed block SKU-160287-160486, minted 11 new entries (SKU-160287-160297) for the misrouted duo/bundle/multi-size products, repointed exactly those product_taxonomy_map rows (zero deletions, per STEP7), left the original entries covering only their remaining correctly-matched products. Self-caught and fixed one regression (SKU-160293 canonical_name dropped product_line's full text) via a post-fix gate re-check before finalizing. SKU-070019 left as a documented finding, not fixed - re-clustering 41 products into distinct product lines is Full-Rebuild scope, same precedent as the SKU-000504/000506 case. SKU-070094 (25 products) and SKU-070185 residual contamination remain open/non-uniform, not attempted this session. 35 taxonomy_ids given genuine Tier 2/Tier-1-FP judgment this session (6 Tier-1 standing FPs + 29 Tier-2 GMV-sample rows confirmed correct) bulk-marked review_confidence in one statement. Post-fix self-check: G1/G2/G3/G5/duplicate-product/duplicate-product+taxon all 0; canonical_name fields shows 5 remaining hits, all the same standing bundle-breakdown-vs-literal-xN FP class on this session's own freshly-minted entries (SKU-141640, 160288, 160289, 160291, 160292) - correctly left un-excepted pending a second session's independent confirmation. Universe refresh deliberately not run, per task scope. |
| 2026-07-24 03:16 UTC | Automated review session (auto-discovery) | STEP1B gate: canonical_name fields FAIL(5) = SKU-141640/160288/160289/160291/160292, all the standing bundle-breakdown-vs-literal-xN FP class first flagged by the 2026-07-24 02:35 UTC session, independently re-confirmed this session (2nd agreement) and closed via qa_gate_exceptions. Tier 1 sweep over the 3,073-entry worklist: null_size=667 (undifferentiated long tail, no raw_niq_history for this table, unchanged since session #1), brand_casing_mismatch=1 (SKU-160295, freshly-minted 'CeraVe' vs the brand_dict/established-convention casing 'Cerave' used on ~95% of this brand's other entries), excess_content/canonical_field_mismatch=7 (the same standing bundle FP class), stub_leak/duplicate_brand/wrong_field_order/garbage_brand=0. STEP2b promo-language sweep (pack_count=1 rows) surfaced 9 taxonomy_ids with genuine same-product or cross-item multiplier evidence in sku_name that a clean-structure check alone wouldn't catch: Cetaphil Gentle Skin Cleanser 118ml (1 twin-pack 125ml x2 product + 1 buyer-choice 125/250/500ml listing wrongly pinned to 118ml), Cetaphil PRO Acne Foam Wash 236ml (1 wash+lotion bundle, 1 twin-pack), ROUND LAB 1025 Dokdo Cleanser Mini/GWP 40ml (1 150mlx2+40ml bundle, 2 products), ROUND LAB Pine Calming Cica Ampoule 30ml (5 products stating '1+1'/'30ml+30ml' pack_count=2, 1 Ampoule+Cream cross-item bundle), SK-II Facial Treatment Gentle Cleanser 120g (3 twin-pack products, 1 120g+40g Set bundle), Sulwhasoo Gentle Cleansing Foam Cleanser 200ml (1 twin-pack, 1 Foam+Oil bundle), Meditherapy Retinal Skin Booster Serum 150ml (1 twin-pack, 1 '2+1' promo pack_count=3, 1 Serum+Cream bundle), and Skin1004 Madagascar Centella Tea-Trica Spot Cream 20ml — a smaller-scale (14-product) instance of the SKU-070019-class catch-all: 4 genuinely different product types (Spot Cream, multi-size Ampoule, multi-size Ampoule Foam Cleanser, and a 120mlx2+30ml bundle) were merged under one 'Spot Cream' entry. GMV-prioritized Tier 2 sample of 40 clean rows verified 2 high-GMV entries against product images (IEM Tuber Magnatum Anti-Aging Essence, $161.8K/mo GMV, size genuinely unresolvable — sku_name states none and the image's on-bottle text is illegible at capture resolution, no raw_niq_history fallback exists for this table; Cetaphil 1.25L Twin Pack + FREE Sun Gel bundle, confirmed via image as 2x1.25L paid + free GWP sachets, matching the existing record exactly) plus GLAD2GLOW PDRN Brightening Serum 20ml (image-confirmed the 20ml is genuinely printed on the packaging, not a misread digit from the 'Glad2Glow' brand name — a clean pass of the §11 cross-validation rule this brand is flagged for). 33 further Tier-1-clean rows read as structurally sound by text alone. New finding (not fixed, deferred): a 'Sample Pack N M/quantity-option' pattern (SK-II SKU-141938, plus prior sessions' Shiseido/Estee Lauder/Kiehl's entries) leaves pack_count=1 on listings where sku_name states a buyer-selectable quantity (e.g. '30ml x 2/4/8') — genuinely ambiguous at the aggregated-product grain without a specific selected model_id, flagged for a dedicated future investigation rather than guessed at here. | Closed 5 canonical_name-fields gate exceptions (2nd independent confirmation, standing bundle-naming FP class). Fixed SKU-160295 brand casing (Cerave, matching established convention). Claimed SKU block 160687-160886 and minted 17 new entries (SKU-160687-160703) via mint+UPDATE-repoint (zero product_taxonomy_map deletions, per STEP7): 3 Skin1004 splits (multi-size Ampoule, multi-size Ampoule Foam Cleanser, 120mlx2+30ml bundle), 2 Cetaphil Gentle Skin Cleanser splits (125ml x2, multi-size), 2 Cetaphil PRO Acne splits (bundle, 236ml x2), 1 ROUND LAB Dokdo bundle, 2 ROUND LAB Pine Ampoule splits (30ml x2, Ampoule+Cream bundle), 2 SK-II splits (120g x2, 120g+40g Set), 3 Meditherapy splits (150ml x2, 150ml x3, Serum+Cream bundle), 2 Sulwhasoo splits (200ml x2, Foam+Oil bundle) — repointing 31 product_id map rows off their original single-unit entries. _meta reset to unreviewed on all 17 new + 8 modified original entries (fixed-pending-recheck). Deliberately NOT attempted this session (documented, left open): SKU-141644/SKU-070185 residual non-uniform contamination (already flagged by prior sessions as needing a dedicated deletion/reroute-authorized session, which this session's no-delete constraint can't resolve), EQQUALBERRY Bakuchiol Serum (SKU-070100, 4+ distinct multi-product bundle combos across ~15 products — re-clustering scope closer to Full Rebuild than a targeted fix, same precedent as the SKIN1004/Effaclar deferrals). 38 taxonomy_ids given genuine Tier 2/Tier-1-FP judgment this session (5 gate-exception FPs + 33 GMV-sampled/text-reviewed rows confirmed correct) bulk-marked review_confidence in one statement. Post-fix self-check: G1 dual-mapped=0, G2 HUMAN+LLM coexistence=0, placeholder-leak=0, duplicate product_id=0, duplicate product+taxon=0, structured-fields NULL%=0%, garbled brand text=0 (exception applies). canonical_name fields shows 7 remaining hits, all on this session's own freshly-minted bundle entries (SKU-160689/160692/160694/160696/160698/160701/160703) reproducing the same already-twice-confirmed FP class — correctly left un-excepted pending a second session's independent confirmation. Confidence distribution after this session: 3,042 unreviewed (includes fixed-pending-recheck), 33 unconfident, 177 confident, of 3,252 total taxonomy entries (17 new this session) mapped to this table. Universe refresh deliberately NOT run this session, per task scope. |
| 2026-07-24 10:42 UTC | Automated review session (auto-discovery) | Auto-discovery review of 3,063 never-reviewed/unconfident/fixed-pending-recheck product_taxonomy entries for shopee_sg_facial_cleanser. Tier 1 SQL sweep flagged 673 rows: 664 null_size (D4), 8 excess_content, 6 canonical_field_mismatch, 0 stub_leak/duplicate_brand/wrong_field_order/brand_casing_mismatch/garbage_brand. Of STEP1B's pre-existing 6 canonical_name-fields gate failures, 3 were genuine defects (size populated but missing from canonical_name text: SKU-120419, 145045, 145176) and 3 were the already-recognized pcs-convention false-positive class on new entities (SKU-118040, 118045, 145049 — first confirmation only). The 8 excess_content bundle rows are the same bundle-breakdown-vs-literal-xN false-positive class as 20 existing qa_gate_exceptions, but first confirmation on these specific entities. null_size was the dominant defect: fixed 143 entries via (a) 9 mechanical pcs/pads/ea backfills from canonical_name's own count token, (b) 120 bulk backfills sourced from Shopee's own structured product_specs Weight/Volume field (cross-validated against near-zero garbage values and bundle-type entries before applying), (c) 10 real per-product image reads for the highest-GMV remaining plain products, (d) 1 individual spec-derived backfill. Found a genuine size defect via Tier 2 GMV-prioritized sampling (SKU-070083 Beauty of Joseon Glow Serum: canonical/size said 60ml, sku_name and only mapped product clearly stated 30ml) and a variant-collapse defect (SKU-070188 La Roche-Posay: 5 of 8 mapped products are actually the 'Pure Vitamin C10' line, not 'C12' as the entry was named) and a merchant-name leak into product_line (SKU-142336 Julioly: reseller store name 'COCOMO' and an 'OFFICIAL STORE' text fragment had been baked into product_line, violating §11). Also found 4 entries (SKU-070185 La Roche-Posay Effaclar Duo+, SKU-118703 CNP Ampoule, SKU-141722 Innisfree Black Tea Ampoule, SKU-142070 VT Spot Patch) where a small minority of mapped products are genuine 1+1/xN promo listings merged into an otherwise-correct single-unit taxonomy entry, and 3 entries (SKU-145050, SKU-145055, SKU-145058) that mix multiple strength/formula variants and cross-item bundle options under one entry (D3 variant collapse) — none of these 7 were altered this session since fixing them correctly requires per-product reroute and new taxonomy entries, and a wrong quick fix risked misrouting the majority-correct volume. | Fixed in place: 3 canonical_name size-completion fixes, 1 size-value correction (Beauty of Joseon), 143 null_size backfills (size + canonical_name), 1 merchant-name-leak cleanup (Julioly), 1 variant-collapse split (new entry SKU-161290 minted for La Roche-Posay Pure Vitamin C10, 5 product_taxonomy_map rows rerouted, SKU-070188 renamed to reflect only its 3 genuine C12 products), and 1 follow-up product_line fix on my own SKU-118050 edit (brand corrected Balance→Ingu via existing brand_dict entry BRD-GLOBAL-00074, product_line updated to match). 238 fixed-pending-recheck rows that came back Tier-1-clean were bulk-promoted (STEP 1C fast lane). 23 rows sampled and judged correct in the Tier 2 GMV-prioritized pass were promoted via one bulk _meta UPDATE. Left unresolved and documented for a future targeted session: ~390 remaining 'plain' null_size entries (GMV-ranked list of taxonomy_ids available), 3 D3 variant-collapse/bundle-mixing entries needing entry-splitting, 4 entries needing per-product promo reroute + new taxonomy_id, and 6 canonical_name-fields gate candidates that are the same pcs/pads-spacing false-positive class as 28 existing exceptions but need one more independent confirmation before they can be closed via qa_gate_exceptions (SKU-118125, 118264, 119146, 118040, 118045, 145049). |
| 2026-07-24 11:18 UTC | Automated review session (auto-discovery) | Worklist = 3,060 taxonomy_ids. STEP1B pre-fix qa_report.sh: canonical_name fields FAIL(6) = SKU-118125/118264/119146/118040/118045/145049, all the standing pcs/pads-spacing false-positive class (size stored without a space, e.g. '84patches', vs canonical text with a space, e.g. '84 Patches'; or pack_count stated as descriptive 'Npcs' text instead of literal 'xN') — independently re-confirmed this session (2nd agreement on each), closed via qa_gate_exceptions (34 total now for this gate). STEP1C fast-lane: 187 fixed-pending-recheck rows Tier-1-swept, 143 came back clean and were bulk-promoted. Tier 1 sweep over the remaining 3,060-entry worklist: null_size=529 (undifferentiated long tail, no raw_niq_history for this table, unchanged pattern since session #1), excess_content=14 (13 the standing bundle-breakdown-vs-literal-xN FP class — 6 already excepted, 7 freshly-minted-last-session entries independently reconfirmed this session as the same class, no exception mechanism applies since excess_content isn't itself a qa_report.sh gate; 1 = SKU-120550 Malin+Goetz, first confirmation only), canonical_field_mismatch=8 (all closed via exceptions, see above), stub_leak/duplicate_brand/wrong_field_order/brand_casing_mismatch/garbage_brand=0. STEP2b promo sweep (unambiguous bundle/1+1/buy-N-get-M patterns only): 2 hits — SKU-142070 (VT Spot Patch, $7.6K/mo) and SKU-070185 (La Roche-Posay, $0 GMV) — both already-documented aggregated-product-grain ambiguities (same product_id sells as promo bundle some months, single unit other months) from prior sessions, left deferred, not re-litigated. GMV-prioritized Tier 2 sample of top-40 Tier-1-clean rows (excluding the 6 image-fixed null_size rows and the 143 STEP1C-promoted rows) found: (1) SKU-142308 (IEM Luxury Beauty Cleanser) initially miscalled as a 4-tube pack based on its newest sku_name variant, but a full monthly breakdown showed 18+ months of plain single-tube '100g' listings (up to $16K/mo) vs. only the most recent 2 months (low GMV) adding '4 Tubes' text to the same product_id — reverted after catching this, since it's the same aggregated-grain ambiguity already established as deferred for SKU-142070/070185/142403, not a blanket property to assert. (2) SKU-141722 (Innisfree Black Tea Ampoule) and SKU-142141 (SOME BY MI Face Foam Cleanser) were genuine single/bundle-merge defects (a plain single-unit product_id merged into an entry whose other product_ids were duo/bundle-configured) — fixed via mint+repoint. (3) A self-caught process bug: STEP1C's bulk-promotion and STEP5's bulk-promotion both ran against the SAME 6 taxonomy_ids this session (SKU-118089/142336/142378/145041/145044/145045), which would have taken them from 'fixed_pending_recheck' straight to 'confident' off a single session's work instead of two independent sessions' agreement — caught before finalizing, reverted to 'unconfident' with last_verdict='correct' preserved so a future session's agreement can properly promote them. Deferred, not attempted: ~523 remaining null_size long-tail entries (no shared pattern, needs individual reads), SKU-070185/SKU-070094/SKU-070019 residual catch-all contamination (documented across multiple prior sessions as needing Full-Rebuild-scope re-clustering or a deletion/reroute-authorized session), SKU-142403 (Paula's Choice, same promo/non-promo product_id ambiguity as SKU-142070), SKU-118703/SKU-141722(remaining non-Duo residual)/dual-mapped/HUMAN+LLM gates (0, nothing to report). | Closed 6 qa_gate_exceptions (canonical_name fields gate, 2nd independent confirmation each). Bulk-promoted 143 STEP1C fixed-pending-recheck rows that came back Tier-1-clean. 6 null_size entries fixed via image verification (Calecim Professional Serum 5ml, VT COSMETICS Collagen/RETI-A/Red Booster Reedle Shot 50ml each, medicube Zero Exosome Shot 30ml, Aprilskin Carrotene Cleansing Balm 90ml) — all confirmed via curl-then-Read against the product's own packaging, canonical_name updated to append size. Minted 2 new taxonomy entries (SKU-162787 Innisfree Black Tea Youth Enhancing Ampoule 50ml x2, SKU-162788 SOME BY MI 30 Days Miracle Acne Clear Face Foam Cleanser 100ml) via the standard mint+UPDATE-repoint pattern, rerouting exactly the 2 mismatched product_ids off their original bundle/duo-configured entries — zero product_taxonomy_map deletions, per STEP7. Reverted an incorrect pack_count 1→4 assertion on SKU-142308 after re-examining the full monthly sku_name history (see finding). Caught and corrected a same-session double-promotion bug affecting 6 taxonomy_ids, restoring them to 'unconfident' rather than letting them reach 'confident' on one session's work. Claimed SKU block SKU-162787–162986 (2 used, 198 unused for a follow-up session). Confidence distribution after this session: 2,591 unreviewed, 404 unconfident, 55 fixed-pending-recheck, 208 confident, of 3,258 total taxonomy entries (2 new this session) mapped to this table. Post-fix self-check: G1 dual-mapped=0, G2 HUMAN+LLM coexistence=0, placeholder-leak=0, duplicate product_id=0, duplicate product+taxon=0, structured-fields NULL%=0%, canonical_name fields FAIL=0 (down from 6), garbled brand text=1 (pre-confirmed standing '107' numeric-brand exception, unchanged). Universe refresh deliberately NOT run this session, per task scope. |
| 2026-07-24 11:37 UTC | Automated review session (auto-discovery) | Worklist (never-reviewed/unconfident/fixed-pending-recheck) = 3,050 taxonomy_ids at session start. STEP1B given qa_report.sh gate: all PASS (dual-mapped 0, HUMAN+LLM coexistence 0, placeholder-leak 0, structured-fields NULL% 0%, all-variant/size 0, canonical_name fields 0, garbled brand text 0) — no gate-failure fixes needed. STEP1C fast-lane: 57 fixed-pending-recheck rows Tier-1-swept; 11 came back fully clean and were bulk-promoted; 46 still tripped a flag (mostly the standing pcs/patches-spacing canonical_field_mismatch FP class already covered by qa_gate_exceptions, plus null_size) and were left untouched per the strict all-flags-FALSE rule. STEP2 Tier-1 sweep over the full 3,050-entry worklist: null_size=523 (dominant, long-tail — no raw_niq_history for this table, matches the unchanged pattern noted in 8+ prior sessions), excess_content=14 (13 already the known bundle-breakdown-vs-literal-xN FP class, most pre-existing in qa_gate_exceptions; SKU-120550 first genuine _meta confirmation), canonical_field_mismatch=8 (all 8 already covered by qa_gate_exceptions — pcs/patches-spacing convention), stub_leak/duplicate_brand/wrong_field_order/brand_casing_mismatch/garbage_brand=0. GMV-prioritized Tier 2 image review of the top null_size rows surfaced a real, previously-undetected pattern: at least 27 Paula's Choice entries (brand rank #4, $159K GMV) had size=NULL despite every product image clearly printing the size on-label (e.g. '30 ml / 1 fl. oz.') — sku_name never states it, so it was never extracted. STEP2b promo-language sweep (304 raw hits, mostly GWP/sample-pack-selector noise) surfaced several genuine pack_count=1-but-actually-xN misses, including two taxonomy entries (SKU-142189 Bio Essence 24K Gold Skin Elixir, SKU-142247 Derma Lab C15 Serum) that were each secretly merging a genuine single-unit product and a genuine x2-bundle product under one pack_count=1 entry — a D5 variant-collapse defect. Deferred: SKU-070100 (EQQUALBERRY) merges many genuinely distinct bundle configurations (single 30ml, Duo Sets, Deep Cera triple sets) under one pack_count=1 entry — real defect but too large/risky to fix without a full per-product reroute pass; SKU-141848's second mapped product is an ambiguous 1/2/3-count sample-pack selector, left on the original single-unit entry since the actual selected count can't be determined from text; ~490 remaining null_size rows are a genuine long-tail with no size signal in sku_name or image (serums/sets where neither the title nor the product photo states a volume) and no product_specification fallback available for this table. | Bulk-promoted 11 clean fast-lane rows to reviewed/unconfident-or-confident via STEP1C. Bulk-confirmed 20 Tier-2-judged-correct rows (14 excess_content bundle-naming class + 6 canonical_field_mismatch pcs-convention class) via STEP5, advancing several toward 'confident' on second agreement. Fixed 30 genuine D4 size defects in place: 27 Paula's Choice entries backfilled with image-confirmed sizes (SKU-145073/145090/145096/145099/145106/145112/145115/145120/145121/145124/145125/145127/145137/145148/145149/145151/145164/145170/145172/145173/145193/145205/145213/145068/145074/145088/145118), 1 D3/D5 variant-selector fix (SKU-145067 Cosrx Best Line Cleanser, 6-formula selector, all 150ml), 1 bundle restructure (SKU-118665 Paula's Choice Firm & Lift Power Set → pack_count=2, is_bundle=TRUE, named per component). Fixed 6 genuine D5 pack-count misses in place (SKU-142193 Bio Essence 36g x2, SKU-118677 P.Calm 150ml x2, SKU-141782 Kiehl's 150ml x2, SKU-142187 Bio Essence Water 100ml x2, SKU-142355 Avène 200ml x2, SKU-142357 Calecim 5ml x6 — corrected from a mis-parsed '5ml' single with the 'x6' multiplier dropped). Split 3 variant-collapsed entries: minted SKU-162789/162790/162791 (reusing the existing ACTIVE SKU-162787-162986 block, 2/200 slots previously used) and rerouted the 3 specific x2/xN product_id map rows off SKU-141848/142189/142247 onto the new entries, leaving the original entries correctly single-unit for their remaining mapped products. All content-changing UPDATEs set meta_agent='CLAUDE_CODE' and reset _meta to unreviewed. Post-fix hard gates (STEP10, no --skip-coexistence): G1 dual-mapped(LLM)=0, G2 HUMAN+LLM coexistence=0, G3 placeholder-leak=0, G4 structured-fields NULL%=0, duplicate product_id=0, G5 provenance=0 — all pass. Ending distribution: 2,556 never-reviewed, 76 fixed-pending-recheck, 408 unconfident, 221 confident (of 3,261 distinct taxonomy entries mapped to this table). |
| 2026-07-24 13:12 UTC | Automated review session (auto-discovery) | Auto-discovery review of shopee_sg_facial_cleanser. Worklist (never-reviewed/unconfident) = 3,040 taxonomy_ids. Pre-fix qa_report.sh: only 'canonical_name fields' gate failing (3 rows) — SKU-118665 (Paula's Choice bundle missing its real on-label set name 'Firm & Lift Power Set'), SKU-145067 (Cosrx cleanser missing its own '150ml' size from canonical_name — a genuine truncation bug), SKU-118677 (P.Calm '2ea' vs 'x2' wording-only mismatch, same established false-positive class as ~30 already-closed qa_gate_exceptions rows, but this specific entity's first confirmation, not yet closable). STEP 1C fast-lane recheck (76 fixed-pending-recheck rows): 37 Tier-1-clean, bulk-promoted with no LLM spend. STEP 2 Tier 1 sweep over the full worklist: null_size 482, canonical_field_mismatch 7, excess_content 2 (stub_leak/duplicate_brand/wrong_field_order/brand_casing_mismatch/garbage_brand all 0 — this category is largely clean of those defect classes now). STEP 2b promo sweep (narrowed per established precedent, dropping the noisy generic x\s*\d/free terms): 1 candidate, SKU-070185 (La Roche-Posay Effaclar Duo — one of its ~34 mapped products states 'DUO VALUE PACK 1+1'). STEP 3 Tier 2: GMV-prioritized sample of 40 Tier-1-clean rows judged against llm-extraction-rules.md in full. | Fixed 14 genuine defects in place: (1) SKU-145067 — appended missing '150ml' to canonical_name. (2) SKU-118665 — rewrote canonical_name to include the real on-label set name 'Firm & Lift Power Set' (confirmed via sku_name) alongside the component breakdown. (3-9) null_size: backfilled size for 7 rows where every mapped product's sku_name independently agreed on exactly one unambiguous token and pack_count required no change (SKU-119260→90pads, SKU-118563→36patches, SKU-118793→8patches, SKU-118049→90patches, SKU-118051→30patches, SKU-118493→45patches, SKU-119030→15patches); left ~463 of the 482 null_size rows deferred — genuine undifferentiated long tail (no raw_niq_history exists for SG per this category's documented finding, text/image only, and the remainder had no single unambiguous signal, or the count was already legitimately captured via pack_count+xN with no separate size dimension to add — false positives of the flag, left untouched). (10) SKU-118017 — real brand-mismatch fix: brand_id was wrongly resolved to 'BRD-GLOBAL-00237' (&Honey) when the product's own sku_name/canonical_name states brand 'IEM' (already this category's rank-17 in-scope brand, BRD-SG-00435); rerouted brand_id, stripped the erroneous '&Honey' prefix, and fixed the 'Animo'→'Amino' typo (cross-checked against sibling entry SKU-142308's correct spelling). (11) SKU-070017 — removed a literal duplicated 'Pore Minimizing' phrase (was in both product_line and variant, appended twice into canonical_name); cleared the redundant variant field. (12) SKU-141686 and (13) SKU-141726 — both are single-listing multi-formula/multi-size buyer-choice products (confirmed via sku_name for both, no cross-product merge risk since each maps to exactly one product_id): set is_multi_variant=TRUE (and is_multi_size=TRUE for the size-choice one), fixed a genuine 'AcidCleansing'→'Acid Cleansing' typo and dropped an incorrect single-formula-only product_line claim ('Ectoin') for SKU-141686. (14) SKU-142336 — Julioly 'Skincare Serums' was a true D2 generic-stub defect; curl-then-Read on the product image (per headless-runbook.md's method) revealed 4 real named variants (Apple Porefect, Cheese Fondue, Fig Filter, Vita ToxC) — rewrote product_line/canonical_name to state them and set is_multi_variant=TRUE. Self-correction mid-session: initially applied a blanket pack_count=2/canonical_name fix to SKU-070185 for its one '1+1'-tagged product before checking that this taxonomy_id is shared by ~34 other single-unit products — caught before finalizing, reverted in full, confirmed this is a new instance of this category's already-documented 'single/bundle-merge defect' top-follow-up class (the flagged product itself has $0 GMV in 2026-05, not worth a dedicated SKU mint this session) — no lasting damage, SKU-070185 is back to its correct pre-existing state and was Tier-2-confirmed correct for its other products. Found (but did not fix, per this session's per-product-rerouting-requires-a-different-scope precedent) one new instance of the same merge-defect class: SKU-142070 (VT COSMETICS) silently drops a real product line ('Pro Cica Centella Asiatica Tiger Clear') for one of its two merged products ($1,096 GMV component of $7,500 total) behind a generic 'Spot Patch Pimple Patch Acne Patch' name; also reconfirmed SKU-070094 (SK-II Gentle Cleanser, $11,670 GMV) still has its long-documented 20g/120g multi-size merge unresolved — both flagged again as follow-up items, unchanged from prior sessions' findings. 33 of the 40 Tier-2-sampled rows were judged correct with no fix needed and bulk-marked review_confidence in one statement, alongside the 37 STEP 1C fast-lane promotions (70 total _meta bookkeeping updates this session, all via bulk statements, not per-row). Post-fix qa_report.sh: canonical_name fields FAIL dropped from 3 to 1 (SKU-118677 remains open, needing a second session's independent confirmation before it can be closed via qa_gate_exceptions per the standing two-agreement rule); all other 8 gates (dual-mapped, HUMAN+LLM coexistence, placeholder-leak, structured-fields NULL%, duplicate product_id, duplicate product+taxon, 'all variant/size' name, garbled brand text) were already at 0 before this session and remain at 0. Verified no product_taxonomy_map rows were deleted (4,465 unchanged) and no NULL meta_agent introduced. Universe refresh deliberately not run this session, per task scope. |
| 2026-07-24 15:51 UTC | Automated review session (auto-discovery) | Given pre-fix qa_report.sh gate: canonical_name fields FAIL(1)=SKU-118677 (standing 'ea'/'pcs'-count-vs-literal-xN false-positive class, 2nd independent confirmation), garbled brand text PASS. STEP1C fast-lane: 51 fixed-pending-recheck rows swept, 11 Tier-1-clean, bulk-promoted with no LLM spend. STEP2 Tier1 sweep over the 3,017-entry worklist (never-reviewed/unconfident): null_size=477 (unchanged undifferentiated long tail, no raw_niq_history for this SG table), canonical_field_mismatch=7, excess_content=3, wrong_field_order=1 — of these, 6 were the standing patch/pcs/ea-count false-positive class already in qa_gate_exceptions (SKU-118125/118264/119146/118040/145049/118045), 1 (SKU-119030) the standing snow2plus/Snow2+ brand_dict-casing ambiguity FP, 2 (SKU-118665, SKU-120550) the known is_bundle/null_size Tier1-sweep-gap FP — all confirmed, no fix needed. 1 genuine defect found and fixed: SKU-119022 (Wollyo) brand-casing mismatch, normalized to brand_dict's 'Wollyo' casing. STEP2b's narrowed promo/bundle sweep found 3 distinct taxonomy_ids with genuine signals: SKU-120586 (Bio Essence) mixed a plain single, a cross-item Cream+Serum bundle, and a genuine Buy-1-Free-1 duo under one pack_count=1 entry — split via mint+UPDATE-repoint (2 new entries), zero deletions. SKU-142070 (VT COSMETICS) was the long-standing single/bundle-merge defect first flagged across 5+ prior sessions — image-verified (curl-then-Read) the '48+18'/'4 Sheets (66EA)' patch count on the previously-hidden 'Pro Cica Centella Asiatica Tiger Clear Spot Patch' line and split it out ($1,225.53 GMV component), closing this standing follow-up item. SKU-070185 (La Roche-Posay Effaclar Duo+M) was re-confirmed unchanged (27 mapped products, dominant single-unit product correctly modeled at 96% of GMV, ~$186 of minor cross-line contamination) and left deferred, consistent with 5+ prior sessions' judgment that re-clustering the full Effaclar family is Full-Rebuild scope. STEP3 Tier2 GMV-prioritized sample of the top 40 Tier-1-clean rows (excluding 3 rows STEP1C already fast-lane-promoted) surfaced 2 more real defects: SKU-070094 (SK-II Facial Treatment Gentle Cleanser) had 2 of 21 mapped products as genuine '(20g/120g)' buyer-choice listings ($5,694 GMV) mis-pinned to a fixed 120g entry — split via mint+repoint (is_multi_size=TRUE). SKU-142335 (Julioly) had a genuine §11 merchant-name leak — the reseller's 'COCOMO Official Store' name baked into product_line/canonical_name as 'STORE ... COCOMO' — corrected to the real product line 'Green Fig Smoothing Filter Serum'. SKU-070038 (Cetaphil) had a redundant variant='Cica' field duplicating text already in product_line, producing a doubled 'Cica' in canonical_name — variant cleared. A live data-integrity hazard was caught mid-session: a concurrent session on this same host was overwriting shared /tmp/*.csv scratch filenames (step2.csv, tier2_sample.csv) with a different category's data (facial_moisturiser, then toothpaste) — caught via GMV/brand cross-checks before any SQL was built from the corrupted reads; switched to a uniquely-tagged scratch directory for the remainder of the session. No BigQuery writes were based on corrupted data. | Closed 1 qa_gate_exceptions row (SKU-118677, canonical_name fields, 2nd confirmation). Bulk-promoted 11 STEP1C fast-lane rows. Fixed 4 taxonomy entries in place (SKU-119022 brand casing, SKU-142335 merchant-name-leak cleanup, SKU-070038 duplicated-token cleanup, plus the SKU-070094/142070/120586 original entries' _meta reset after their splits). Claimed SKU block SKU-163277–163476 (200 slots, targeted_qa_fix scenario, 196 unused for a follow-up session) and minted 4 new entries via the standard mint+UPDATE-repoint pattern (SKU-163277 VT COSMETICS Pro Cica Spot Patch x66, SKU-163278 Bio Essence Cream+Serum bundle, SKU-163279 Bio Essence Serum x2, SKU-163280 SK-II multi-size catch-all), repointing exactly 5 product_taxonomy_map rows — zero deletions, per STEP7. Bulk-marked review_confidence on 42 taxonomy_ids in one statement (32 genuine Tier-2-correct + 6 standing patch/pcs FP re-confirmations + 1 brand-casing-ambiguity FP + 2 is_bundle/null_size-gap FPs + the closed SKU-118677). Post-fix qa_report.sh: all 9 gates PASS (canonical_name fields 0, down from 1). qa_coverage_report.sh: 2,520 never-reviewed, 47 fixed-pending-recheck, 430 unconfident, 268 confident of 3,265 total taxonomy entries (4 new this session). Universe refresh deliberately NOT run this session, per task scope. |
| 2026-07-24 17:42 UTC | Automated review session (auto-discovery) | Worklist (never-reviewed/unconfident) = 2,997 taxonomy_ids at session start. STEP1B given pre-fix qa_report.sh: all 8 gates PASS. Live data-integrity hazard hit mid-session: a concurrent session on this host overwrote shared /tmp/*.csv scratch filenames (step1_worklist.csv, step2_tier1.csv) with shopee_sg_liquid_soap data — caught via brand/product-name cross-check before any SQL was built from the corrupted reads; switched to a uniquely-tagged scratch directory for the remainder of the session (same defensive pattern the 2026-07-24 15:51 UTC session established). STEP1C fast-lane recheck (47 fixed-pending-recheck rows): 7 Tier-1-clean, bulk-promoted with no LLM spend. STEP2 Tier1 sweep over the 2,997-entry worklist: null_size=474 (unchanged undifferentiated long tail, no raw_niq_history for this SG table), excess_content=3 (all confirmed legitimate bundle/multi-size FPs matching established precedent: SKU-118665 and SKU-163278 known is_bundle Tier1-sweep-gap FPs, SKU-163280 known SK-II multi-size catch-all), canonical_field_mismatch=1 (SKU-118677, already closed via qa_gate_exceptions 2nd-confirmation, skipped re-verification), wrong_field_order=1 (SKU-119030 — re-investigated the prior session's 'casing ambiguity' characterization and found a genuine defect underneath: brand_id pointed to a duplicate brand_dict entry BRD-SG-04616 'snow2plus', used by only this 1 taxonomy row, vs BRD-SG-01912 'Snow2+' used by 8 sibling rows and the category's own official Brand Scope table). STEP2b promo sweep (narrowed per established precedent): 2 candidates — SKU-070185 (La Roche-Posay, re-confirmed the standing deferred-to-Full-Rebuild judgment) and SKU-142070 (VT COSMETICS, promo tag present only in a stale prior-month sku_name snapshot, not the current month). STEP3 Tier2: GMV-prioritized sample of the top 45 Tier-1-clean rows judged against llm-extraction-rules.md in full and quality-standards.md's D1-D5, including a scope/type-gate check per product. | Fixed 4 taxonomy entries in place: (1) SKU-119030 — brand_id rerouted from duplicate 'snow2plus' (BRD-SG-04616) to the category's established canonical 'Snow2+' (BRD-SG-01912); canonical_name unchanged (already correct). (2) SKU-070084 (Beauty of Joseon) — cleared a redundant variant='Green Plum' field duplicating text already in product_line, removing the doubled 'Green Plum' from canonical_name (same defect class as the SKU-070017 precedent). (3) SKU-145053 (Skinlycious) — trimmed a full marketing sentence that had leaked into product_line/canonical_name, down to 'Skinlycious Hydrating Serum 30ml'. (4) SKU-141743 (Innisfree Volcanic BHA) — found and fixed a new instance of this category's recurring single/bundle-merge defect: 2 of 3 mapped products are genuine single-unit 150g listings with no promo language, incorrectly merged under a pack_count=2/'x2' entry whose text derived from the 3rd product's one 'BUY 1 FREE 1' listing (~$1,420 of the ~$5,041 total GMV mispriced as a bundle). Minted SKU-163281 (pack_count=1, plain '150g' name, reusing the existing ACTIVE SKU-163277–163476 block) and repointed the 2 single-unit product_taxonomy_map rows to it — zero deletions; SKU-141743 itself now correctly scoped to just its 1 genuine bundle product. One new finding NOT fixed (needs deletion, out of this session's scope): SKU-118053 (VELY VELY 'Vitamin Dark Spot Tone-up Sun Serum SPF50+ PA++++', ~$6,191 GMV across 3 products) is a genuine sunscreen/suncare product, out of scope per this category's own documented Scope section — flagged for a deletion-authorized follow-up session to unmap; also carries a secondary reseller-name leak ('- COCOMO' trailing text). Bulk-promoted 7 STEP1C fast-lane rows and bulk-marked review_confidence on 45 Tier-2-judged-correct taxonomy_ids in two single statements (52 total _meta bookkeeping updates this session, no per-row UPDATEs). Post-fix gate re-check: all hard gates 0 except garbled-brand-text (2 rows, both the already-standing BRD-SG-04404 '107' exception, no new instance). Coverage: 2,509 never-reviewed, 43 fixed-pending-recheck, 414 unconfident, 300 confident of 3,266 total distinct taxonomy entries (1 new this session). Verified 4,465 product_taxonomy_map rows unchanged (zero deletions) and 0 NULL meta_agent. Universe refresh deliberately not run this session, per task scope. |
| 2026-07-24 18:19 UTC | Automated review session (auto-discovery) | Worklist (never-reviewed/unconfident) = 3,710 taxonomy_id×map rows (2,966 distinct entries) at session start. STEP1B pre-fix qa_report.sh FAIL(1) on 'canonical_name fields' resolved to SKU-163281 (Innisfree Volcanic BHA Pore Cleansing Foam 150g) — NOT SKU-118677 (already closed via a pre-existing qa_gate_exceptions row, correctly excluded from the gate's own NOT-IN clause and thus not the counted failure). Image-confirmed (curl-then-Read) SKU-163281's packaging reads only 'VOLCANIC BHA PORE CLEANSING FOAM' — the stored product_line/canonical_name text 'Facial Cleanser for Sensitive & Oily Skin' is fabricated marketing copy absent from both packaging and every mapped sku_name. Same fabricated subtitle also found (via a follow-up grep across the Volcanic BHA family) baked identically into product_line AND canonical_name of 3 sibling entries (SKU-141743, SKU-141744, SKU-160293) — invisible to the Tier-1 canonical_field_mismatch check because both fields agreed with each other while both were wrong. STEP1C fast-lane (49 fixed-pending-recheck rows): 5 Tier-1-clean (incl. SKU-163281 post-fix), 4 bulk-promoted (SKU-163281 left pending re-review since fixed this same session, not a prior one). STEP2 Tier-1 sweep over 2,966 distinct worklist entries: null_size=473 (unchanged long-tail, no raw_niq_history for this SG table, matches 8+ prior sessions' finding), excess_content=2 (both already-confirmed pre-existing FPs: SKU-163278 bundle, SKU-163280 SK-II multi-size catch-all — re-confirmed, no fix needed), all other Tier-1 flags = 0. STEP2b promo/multiplier sweep (289 pack_count=1 rows, GMV-ranked): surfaced a previously-uncaught, genuine D5 pattern — 3 Korean 'ampoule box' listings whose sku_name explicitly states 'N Ampoules x Mml'/'(7x2ml)' box contents but were extracted as pack_count=1/single-unit size (Babor Ampoule 2ml, blanc dubu Nuborn Cell Blanc Power Ampoule 7ml, Clear Dea Mucin Collagen Ampoule 4ml — combined ~$288K GMV). STEP3 Tier-2: GMV-prioritized sample of the top 45 Tier-1-clean entries judged in full against llm-extraction-rules.md + quality-standards.md D1-D5/type-gate. Found 2 Lancôme entries (SKU-141840, SKU-141841) with the brand name redundantly duplicated inside product_line (unaccented 'Lancome' vs the accented brand_dict casing, which is why the duplicate_brand regex — an exact-string REPLACE — missed it); ran an accent-insensitive sweep of the whole category on this exact pattern and found 5 more unrelated instances (Clé de Peau Beauté x2, WHIPPED x3) plus one ambiguous case (Aclean) also fixed. Reviewed SKU-070100 (EQQUALBERRY, $721,789 GMV, top of the whole worklist) and SKU-070019 (Skin1004, $173,362 GMV) for possible variant/bundle collapse — both are ≥95-98%-dominated by one correctly-modeled product line with only near-zero-GMV noise from other listings, matching this category's established 'dominant product, minor contamination, leave deferred' precedent (same class as the twice-reviewed SKU-070185); left unchanged. SKU-141705 (Cetaphil, $229,696 GMV) found to have a genuine unresolved ambiguity: 99% of its GMV comes from '[Bundle of 2/4/6]' buyer-selector listings where the actual units received depends on an option this product-level source table can't distinguish, vs. the entry's fixed pack_count=4 — flagged below, not fixed (no schema field cleanly represents a buyer-selected pack count the way is_multi_size does for buyer-selected size; forcing a guess seemed worse than flagging). 42 of the 45 Tier-2-sampled rows were judged correct and bulk-marked via one _meta UPDATE (confident/unconfident per the two-consecutive-agreement rule); the 3 excluded (2 fixed, 1 flagged-not-fixed) were left untouched for natural re-review next session. | Fixed 12 genuine defects in place, no new SKUs minted: (1) SKU-163281 + 3 siblings (SKU-141743/141744/SKU-160293) — stripped the fabricated 'Facial Cleanser for Sensitive & Oily Skin' subtitle from product_line and canonical_name (image + sku_name confirmed real packaging text is just the product line name). (2) 3 ampoule-box pack-count misses — SKU-118281 Babor (pack_count 1→7, size 2ml, canonical → '...2ml x7'), SKU-118390 blanc dubu (1→5, '...7ml x5'), SKU-118785 Clear Dea (1→5, '...4ml x5') — all sku_name-confirmed across every mapped product with zero conflicting counts. (3) Redundant brand-name-in-product_line, 6 rows: SKU-141840/141841 Lancôme ('Lancome' dropped from product_line), SKU-118600/118098 Clé de Peau Beauté (redundant 'Clé de Peau Beauté/CPB/CDP' tokens dropped, product_line reduced to 'The Serum'/'The Serum Sample Pack'), SKU-120549/120528/120525 WHIPPED ('WHIPPED' dropped from product_line, canonical_name already correct), SKU-120425 Aclean (product_line reduced to 'Gel', matching sku_name's only content beyond the brand). Every fix set meta_agent='CLAUDE_CODE' and reset _meta to unreviewed in the same statement. 42 additional entries Tier-2-reviewed as already-correct were bulk-promoted via one _meta UPDATE (no fix needed). Post-fix qa_report.sh (no --skip-coexistence): ALL 9 GATES PASS, including the previously-failing canonical_name fields (now 0). Confidence distribution left behind for this table: 318 confident, 416 unconfident, 2,532 still unreviewed (long tail dominated by the 473 null_size entries — no raw_niq_history available for cross-validation on this SG table, consistent with 8+ prior sessions' documented finding). Universe refresh deliberately NOT run this session per task scope. |
| 2026-07-24 19:03 UTC | Automated review session (auto-discovery) | Automated Taxonomy Review session #6 (auto-discovery mode, _meta-tracked incremental scope). Worklist (never-reviewed/unconfident) = 2,948 taxonomy_ids. Pre-fix qa_report.sh: all gates PASS (given). STEP 1C fast-lane recheck on the 53 distinct 'fixed pending recheck' taxonomy_ids found 14 clean on Tier 1 — bulk-promoted (landed review_confidence=unconfident, first post-fix confirmation, not yet a second agreeing review). STEP 2 Tier 1 SQL sweep — an initial CSV-parsed version showed impossible fan-out (2,948 rows growing to 3,731/3,607), traced via advisor review to a client-side bug (Python csv.DictReader without newline='' mis-splitting rows on stray  characters in scraped Shopee titles), not real concurrent-session drift — re-run as a pure SQL COUNTIF aggregation: total=2,948, stub_leak=0, duplicate_brand=0, wrong_field_order=0, brand_casing_mismatch=0, excess_content=3, canonical_field_mismatch=0, null_size=473, garbage_brand=0. Five prior sessions had already driven wrong_field_order/canonical_field_mismatch/garbage_brand/brand_casing_mismatch/stub_leak to zero; confirmed still zero, no regression. null_size (473) is the same exhausted long tail sessions #3-#5 established (no raw_niq_history for shopee_sg_*, text/image only); this session's bulk-safe single-token-agreement pass found zero new candidates. excess_content (3): 2 confirmed false positives (SKU-120528 Whipped, SKU-163278 Bio Essence — legitimate multi-size/bundle range statements); 1 real defect, SKU-163280 (SK-II, buyer-choice 20g/120g multi-size correctly is_multi_size=TRUE but pack_count left NULL). STEP 2b promo sweep (narrowed signal + widened [Bundle of N] term) found 9 flagged taxonomy_ids. GMV-prioritized Tier 2 sample of 40 rows surfaced 2 more real defects: SKU-145064 (Calecim Professional, $4,080 GMV — product_line was bare 'Serum'; image-verified real label sub-line 'Rejuvenating Conditioner') and SKU-141653 (d'Alba 'Twin Spray Set', $4,400 GMV — genuine 2-component bundle mislabeled pack_count=1/is_bundle=false). SKU-141717 (Innisfree, $4,180 GMV) left unresolved — 5 unrelated sku_name snapshots across months, too ambiguous to re-model confidently. | Fixed 5 taxonomy entries in place (all UPDATEs, no new SKU block, no map rows touched): SKU-163280 pack_count NULL→1; SKU-141866 and SKU-141870 (Laneige, sku_name-stated same-product doublings '+30ml'/'+150ml') pack_count 1→2 with xN appended; SKU-145064 product_line/canonical_name corrected to include image-verified 'Rejuvenating Conditioner'; SKU-141653 pack_count 1→2, is_bundle→TRUE. All fixes reset _meta to unreviewed, meta_agent='CLAUDE_CODE'. STEP 2b's other 7 flagged ids: 5 Innisfree GWP gift-set listings confirmed correct as-is (different-item freebies, not multipacks); 2 new instances of the standing single/bundle-merge defect class found (SKU-141904, SKU-141909, numbuzin) — left unfixed per STEP 7 (would require map-row reroute), added to the standing follow-up list. 43 taxonomy_ids bulk-marked review_confidence in one statement (37 GMV-sample correct + 4 GWP-confirmed + 2 excess_content false positives); 14 STEP 1C fast-lane rows promoted separately. QA gates re-run directly: G1=0, G2=0, G3=0, G5=0, provenance=0 — unchanged from baseline since no map-row inserts/deletes occurred. Universe refresh not run, per task scope. |
| 2026-07-25 02:37 UTC | Automated review session (auto-discovery) | Worklist (never-reviewed/unconfident) = 2,927 taxonomy_ids at session start. STEP1B given qa_report.sh: all 9 gates PASS (independently re-verified: dual-mapped=0, HUMAN+LLM coexistence=0, placeholder-leak=0, duplicate product_id=0). STEP1C fast-lane recheck (45 fixed-pending-recheck rows): 4 Tier-1-clean, bulk-promoted with no LLM spend; remaining 39 still trip the same undifferentiated null_size long tail. STEP2 Tier1 sweep over the 2,927-entry worklist: null_size=472 (unchanged undifferentiated long tail, no raw_niq_history for this SG table, consistent with 15+ prior sessions' finding), excess_content=2 (both confirmed standing false positives: SKU-120528 Whipped and SKU-163280 SK-II, legitimate multi-size range statements), stub_leak/duplicate_brand/wrong_field_order/brand_casing_mismatch/canonical_field_mismatch/garbage_brand=0. STEP2b narrowed promo sweep: 0 hits in-worklist -- cross-checked the standing single/bundle-merge-defect list (SKU-070073, SKU-070094, SKU-070100, SKU-070116, SKU-070138, SKU-070158, SKU-070185, SKU-070019, SKU-141642, SKU-141644, SKU-141732) directly and found most now 'confident' (resolved/reconfirmed by prior sessions) or reduced to a single genuinely-clean product each (SKU-070073: 16 products, all clean single-unit 30ml Round Lab listings plus 2 legitimate GWP add-ons and one $0-GMV ambiguous outlier, deferred; SKU-070158: 1 product, clean). GMV-prioritized Tier 2 sample of the top 90 Tier-1-clean rows by 2026-06 GMV surfaced 9 individual real defects: SKU-070036 (Cetaphil) had a duplicated 'SA' token from a redundant variant field; SKU-141697 (Skintific) had an 'Expires Aug 2026' logistics tag baked into product_line; SKU-118025 (L'Oreal Paris) had an accent-variant duplicate of its own brand name inside product_line ('L'Oreal Paris' + 'L'Oréal Paris ...'); SKU-118071/SKU-118056 were both mis-resolved to a garbled reseller-tag brand 'P&M' (BRD-SG-12197, canonical_name literally 'P&M', traced to a naive 'pm 85g' sku_name token) instead of this category's own established PanOxyl brand (BRD-SG-02637); SKU-142358/142359/142360 (Calecim Professional) each had a reseller stock-tag/emoji ('【💯SG In-Stock】', '「💯FAST DELIVERY」', '【💯 w QR Code】') baked into product_line, and 2 of the 3 turned out to be exact duplicates (same brand/size/pack_count) of an existing clean entry SKU-145048 minted by a prior session -- caused by the noise tags preventing Pass-2 text-matching from recognizing the match. A whole-category accent/case-insensitive duplicate-brand sweep (extending the exact-string Tier-1 regex, per the 2026-07-24 18:19 UTC session's established method) found 20 further instances of the same defect class across L'Oreal Paris, Clé de peau beaute, Biotherm, Lancôme, Estee Lauder, and Eau Thermale Avène that the exact-string Tier-1 check structurally cannot catch. Also found: brand_dict entry BRD-GLOBAL-00625 'ฺBiotherm' carries a stray leading Thai combining character (U+0E3A), and this real-world brand is fragmented across 4 separate brand_ids (BRD-GLOBAL-00625, BRD-TH-04278, BRD-SG-13179, BRD-GLOBAL-03685) -- a brand_dict cleanup item, out of scope for this session (collision exists with the clean 'Biotherm' entry, so no safe in-place global rename). A bulk-safe null_size regex pass (single unambiguous token agreed by all mapped products) found 9 raw candidates; 2 were false positives from the same digit-in-ordinary-word trap this category's own §11 precedent warns about ('BUY 3 FREE 2 GIFT' -> false '2g', '5 GIFTS' -> false '5g'); 3 were genuinely too ambiguous to bulk-fill (a device+cartridge bundle where a component's ml doesn't represent the whole set; a 2-serum collection where 30ml might mean each item, not one total; a Mist/Serum/Cream selector where only the Serum variant states a size); 1 (SKU-118029, ARK+ 'Father's Day Wellness Bundle') is the already-flagged health-supplement cross-category mis-mapping, unresolvable without a delete (STEP7 forbids). A self-caught regression: the SKU-118120 size backfill ('1L') initially left canonical_name still saying '1 Liter', re-tripping canonical_field_mismatch on the very next Tier-1 recheck of this session's own fixed rows -- corrected before finalizing (normalized both product_line and canonical_name to '1L' per the explicit-units rule). A concurrent-session data point: roughly 117 additional _meta writes to this same table's rows landed in the same 1-minute window as this session's own STEP5 bulk confirm-correct UPDATE, suggesting another automated session may have been reviewing this same category concurrently; verified this session's own specific writes (5 spot-checked, including all 5 in-place text fixes) were unaffected, and all hard gates plus the product_taxonomy_map row count (4,465, unchanged) remained consistent after the fact -- flagged as an observation, not a session error, consistent with this category's prior documented scratch-file concurrency hazard. | Fixed 32 taxonomy entries in place (all UPDATEs, meta_agent='CLAUDE_CODE', _meta reset to unreviewed): 20 accent/case-insensitive duplicate-brand-name cleanups (SKU-118038, 118169, 118359, 118441, 118514, 118634, 118843, 118908, 119114, 119737, 119789, 141839, 141842, 141843, 141844, 141888, 141889, 141890, 141891, 142352), 1 redundant-variant duplicate-token fix (SKU-070036), 1 logistics-noise strip (SKU-141697), 1 reseller-tag strip (SKU-142358), 2 brand-mismatch corrections rerouting BRD-SG-12197 'P&M' to the established BRD-SG-02637 PanOxyl (SKU-118071, SKU-118056, also cleaning duplicated/inconsistent strength text on 118071), 1 more accent-duplicate brand fix (SKU-118025), 1 size backfill with unit normalization (SKU-118120, '1 Liter'->'1L' after a self-caught regression), 1 is_multi_size correction for a genuinely heterogeneous 3-product entry (SKU-118369: 100ml/236ml buyer-choice vs a separate 562ml listing, no longer force-pinned to one value), and 1 size backfill for a single-product multi-variant-formula listing (SKU-145123, 30ml, all formula options same size). Rerouted 2 product_taxonomy_map rows (products 25784340999 and 16964425271) from duplicate mint SKU-142359/SKU-142360 onto the existing clean SKU-145048 -- zero rows deleted, per STEP7; the 2 duplicate taxonomy entries are left as harmless orphans (no map rows reference them) per the established mint/dedup-orphan-keep precedent. STEP1C bulk-promoted 4 fixed-pending-recheck rows that came back Tier-1-clean. STEP5 bulk-marked review_confidence (confident/unconfident per the two-consecutive-agreement rule) on 81 additional Tier-2-GMV-sampled rows confirmed correct with no fix needed, in one statement. Deliberately NOT attempted (documented, deferred): the 472-entry null_size long tail (undifferentiated, no raw_niq_history for this SG table, unchanged since session #1); the brand_dict-level Biotherm fragmentation/stray-character cleanup (collision with an existing clean entry blocks a safe in-place rename); 3 ambiguous null_size candidates where the single-token regex signal doesn't safely represent the whole entry; SKU-118029's cross-category health-supplement mis-mapping (needs a deletion-authorized session). Post-fix self-check (STEP10, no --skip-coexistence): all 9 hard gates PASS -- G1 dual-mapped LLM=0, G2 HUMAN+LLM coexistence=0, G3 placeholder-leak=0, structured-fields NULL%=0%, duplicate product_id=0, duplicate product+taxon=0, 'all variant/size' name=0, canonical_name fields=0 (whole-category, excluding the 35 standing qa_gate_exceptions, re-verified untouched), garbled brand text=0 (excluding the 1 standing 'SKU-118309/107' exception). product_taxonomy_map row count for this table unchanged at 4,465 (zero rows lost). No qa_gate_exceptions rows added this session (no gate was failing at STEP1B). Ending worklist: 2,888 taxonomy_ids (down from 2,927); confidence distribution: 2,763 never-reviewed, 80 fixed-pending-recheck, 635 unconfident, 987 confident, of 3,264 total distinct taxonomy entries mapped to this table (0 new entries minted this session). Universe refresh deliberately NOT run this session, per task scope. |
| 2026-07-25 03:00 UTC | Automated review session (auto-discovery) | Worklist (never-reviewed/unconfident) = 2,888 taxonomy_ids at session start. STEP1B given qa_report.sh: all gates PASS. STEP1C fast-lane recheck on 70 fixed-pending-recheck rows: 30 Tier-1-clean, bulk-promoted with no LLM spend; 39 still tripped a flag, left untouched. STEP2 Tier1 SQL sweep over the 2,888-entry worklist: null_size=469 (dominant, unchanged long-tail pattern — no raw_niq_history exists for this SG table per prior sessions' confirmed finding), excess_content=2, all other flags (stub_leak/duplicate_brand/wrong_field_order/brand_casing_mismatch/canonical_field_mismatch/garbage_brand) = 0. STEP2b broad promo-language regex over pack_count=1 rows returned 8,657 hits, almost entirely noise ('oil-free', brand tokens with digits); a tightened genuine-multipack regex (buy N get M / N+M free / 2-for-1) found 0 hits — no missed multipacks. Tier 2 GMV-prioritized review: (a) downloaded and vision-read the 17 highest-GMV null_size products (curl-then-Read), confirming real on-label sizes for 9 (DOCTOB Pepta-Biome Vegan Cleanser 150ml, Stridex Acne Control Pads 55 Pads, SkinDae Cell Lift Ampoule 30ml, Dr. Althea Vitamin C Boosting Serum 30ml, Alluora Radiance Serum 30ml, medicube Retinol-NMN Boosting Serum 30ml, medicube Peel Shot with Turmeric 80ml, medicube PDRN Whip Cleanser 120g, medicube Deep Vita C Capsule Serum 30ml) and leaving 8 unresolved where the product image genuinely never states a size (Sets/bundles or promo-only creative); (b) reviewed the top-25 GMV Tier-1-clean rows against D1-D5 and found two genuine defects: SKU-141950 (Aestura ATOBARRIER365 AMPOULE) had pack_count=1 when the image confirms a 2x30ml twin-bottle pack — the '*2' multiplier in sku_name had been misread into product_line as a literal '2' instead of the pack multiplier; fixed to pack_count=2, product_line cleaned, canonical_name corrected to '...Ampoule 30ml x2'. SKU-142042 (Rejuran Moisture Treatment Ampoule) had marketing tagline text ('Soothing Hydration Barrier Care Sensitive Skin' from the sku_name's own pipe-delimited descriptor) baked into product_line instead of the true on-label name 'Moisture Treatment Ampoule' (image-confirmed) — fixed. Two excess_content Tier-1 flags (SKU-120528, SKU-163280) were judged false positives on inspection: both are legitimate is_multi_size=TRUE catch-all entries whose canonical_name lists real size options (e.g. '20g/120g'), not a generic stub — no fix needed. New systemic finding (not fixed this session, flagged for a dedicated follow-up): 511 worklist entries have product_line values >9 words long, a heuristic indicating marketing-copy/tagline text was captured verbatim from sku_name instead of the true on-label product line name (per llm-extraction-rules.md §3) — confirmed via image on 2 of them this session, but fixing the rest at scale requires per-product image verification beyond this session's budget. New scope-contamination finding: SKU-118029 'ARK+ Father's Day Wellness Bundle' is a dietary-supplement product (Sacha Inchi capsules, Q10, Lutein Eye Jellies, B-Complex) mapped under this category's 'Facial Serum & Essence' NIQ bucket — genuinely out-of-scope content, not fixable without deleting/remapping the product_taxonomy_map row, which this session cannot do (STEP 7). STEP10 hard-gate self-check (no --skip-coexistence): dual-mapped(LLM)=0, HUMAN+LLM coexistence=0 (previously a 552-row pre-existing baseline across many prior sessions — now 0, meaning the deferred wrapper-side HUMAN-row cleanup has evidently run since the last session; not caused by this session), placeholder-leak=0, structured-fields NULL%=0%, duplicate product_id=0, duplicate product+taxon=0, canonical_name-fields(D1 mismatch)=0, garbled-brand-text raw count=2 (map-row fan-out) resolving to 1 distinct entity (SKU-118309, brand '107'), already closed via a standing qa_gate_exceptions row with 5 prior confirmations — no new action needed. All gates pass. | Fixed 11 taxonomy_id entries this session (9 null_size backfills confirmed via product image, 2 product_line/pack_count structural corrections also image-confirmed) — all reset to _meta={is_reviewed:false} for next-session re-confirmation per the confident-after-two-agreeing-reviews rule. Bulk-promoted 45 rows to reviewed status (30 via STEP1C fast-lane Tier-1-clean recheck, 15 via genuine Tier-2 GMV-prioritized judgment finding them correct — including the 2 excess_content false positives). Worklist should now stand at roughly 2,843 (2,888 minus 45 newly-confident, plus 11 fixed rows re-entering as fresh unreviewed). Two new findings recorded for follow-up sessions: (1) a systemic ~511-entry product_line marketing-copy-leakage pattern needing a dedicated audit session with per-product image verification; (2) SKU-118029 is out-of-scope dietary-supplement content requiring a deletion-authorized session to unmap. No universe refresh run (out of scope per STEP 8). |
| 2026-07-25 03:58 UTC | Automated review session (auto-discovery) | Worklist (never-reviewed/unconfident) = 2,874 taxonomy_ids at session start. STEP1B given qa_report.sh: 8/9 gates PASS, 'canonical_name fields' FAIL=1 — traced to SKU-118145 (Stridex, size='55 Pads' but canonical_name says '55s'), the same shorthand-count-convention false-positive class as 20+ already-excepted rows, but this is its FIRST confirmation on this specific entity (no prior QA History mention) so per the two-confirmation rule no qa_gate_exceptions row was inserted — left open for a future session's second confirmation. STEP1C fast-lane recheck on 49 fixed-pending-recheck rows: 10 Tier-1-clean, bulk-promoted with no LLM spend. STEP2 Tier1 sweep over the 2,874-entry worklist: null_size=460 (dominant, unchanged long-tail — raw_niq_history dataset confirmed still nonexistent for this project, consistent with 15+ prior sessions), excess_content=1 (SKU-163280, re-confirmed standing false positive, no exception mechanism applies to this non-hard-gate flag), canonical_field_mismatch=1 (SKU-118145, above), all other flags=0. STEP2b tightened promo sweep: 1 hit, SKU-118029, the already-documented ARK+ cross-category dietary-supplement contamination (no new action, needs a deletion-authorized session). Tier 2: (a) image-verified the top 18 GMV null_size candidates via curl+read (stripping the known embedded-quote image-URL quirk) — 4 had real on-label sizes never extracted (HaruHaru Wonder 100ml, Aprilskin Calendula 100g, BUV Green Clay 100g, Aprilskin Carrotene Serum 37ml; 2 of these also had marketing-copy leaked into product_line, fixed alongside), 1 (ScarAway) was a genuine multi-count buyer-choice listing needing is_multi_size=TRUE, others (Aestura Serum, medicube ampoules, CHARDE, Seyoul, Cosrx spot cream, The Ordinary Sulfur) had no legible size in the marketing image — left unresolved, consistent with prior sessions' disposition; SKU-118030 (Acnes 'Bundle of 6') and SKU-118481/SKU-118160 (EasyDerm patch series) turned out to be genuine heterogeneous multi-variant-type bundles, too ambiguous to safely restructure this session — left as documented long tail. (b) GMV-prioritized sample of 70 Tier-1-clean rows found 3 more marketing-copy-leakage instances (Cosrx Vitamin C 13 Serum, Aestura Hydro Essence — both matching the systemic ~511-entry pattern a prior session flagged but didn't fix), 1 mislabeled-as-single-size product (ISOI Blemish Care Up Serum, sku_name states a genuine 15ml/35ml/70ml buyer-choice that had been hard-coded to 15ml), and 1 duplicate-brand-token-plus-missed-multipack (SK-II 'SKII Facial Treatment Essence Value Set (30ml x 4pcs)' had pack_count=1 and a redundant 'SKII' brand token in product_line — the exact-string Tier-1 duplicate_brand check missed it because 'SKII' ≠ 'SK-II' textually, same structural gap the 2026-07-25 02:37 UTC session found for accented/cased brand duplicates). Also noted but not fixed: SKU-118369 (CeraVe multi-size catch-all) has one mapped product explicitly stating its own 562ml size distinct from the entry's covered range, but its GMV ($194.50) doesn't justify minting a new SKU block. Post-fix self-check (STEP10, no --skip-coexistence): all hard gates PASS — G1 dual-mapped(LLM)=0, G2 HUMAN+LLM coexistence=0, placeholder-leak=0, duplicate product_id=0, duplicate product+taxon=0, structured-fields NULL%=0%, canonical_name fields=1 (SKU-118145, open first-confirmation, expected per above), garbled brand text=2 raw rows/1 distinct entity (SKU-118309 '107', already closed via a 5-times-confirmed standing exception). product_taxonomy_map row count unchanged at 4,465. Total distinct taxonomy entries mapped to this table: 3,264 (0 new entries minted this session). | Fixed 10 taxonomy entries in place (all UPDATEs, meta_agent='CLAUDE_CODE', _meta reset to unreviewed): 4 null_size backfills confirmed via product image (SKU-118055 HaruHaru Wonder 100ml, SKU-145086 Aprilskin Calendula 100g, SKU-118200 BUV Green Clay 100g, SKU-145076 Aprilskin Carrotene Serum 37ml — the latter 3 also had marketing-copy-contaminated product_line corrected to the true on-label name); 1 is_multi_size correction for a genuine multi-count buyer-choice listing (SKU-119808 ScarAway); 1 is_multi_size correction for a product wrongly hard-coded to a single size when sku_name states a genuine 3-size buyer choice (SKU-118032 ISOI, size 15ml→NULL); 2 more marketing-copy-leakage product_line fixes found via GMV-sample text/image review (SKU-141833 Cosrx, SKU-141951 Aestura) plus 1 image-confirmed instance (SKU-145084 Skinlycious); 1 duplicate-brand-token + missed-4-pack fix (SKU-141934 SK-II, pack_count 1→4). No map rows touched, no rows deleted, no new SKU block claimed. Bulk-promoted 10 rows via STEP1C fast-lane (Tier-1-clean fixed-pending-recheck) and bulk-confirmed 56 more via STEP5 genuine Tier-2 GMV-sample judgment, in two single bulk UPDATE statements (excluding the 10 STEP1C rows from the STEP5 batch to avoid double-counting a same-session 'second confirmation'). No qa_gate_exceptions rows inserted this session (SKU-118145's shorthand-convention flag is only a first confirmation on this entity, not yet eligible; SKU-163280 excess_content isn't one of the exception-eligible named hard gates). Deliberately not attempted, documented for follow-up: the 460-entry null_size long tail beyond the top-18 GMV slice (undifferentiated, no raw_niq_history for this SG table); the systemic ~511-entry marketing-copy-leakage pattern (3 more confirmed instances found and fixed this session, majority still pending a dedicated audit); SKU-118029's cross-category dietary-supplement mis-mapping (needs a deletion-authorized session); SKU-118030/SKU-118481/SKU-118160's heterogeneous multi-variant-type bundle structure (too ambiguous to safely restructure without deeper investigation); SKU-118369's single low-GMV ($194.50) misrouted 562ml product (not worth a new SKU block). Universe refresh deliberately not run, per task scope. |
| 2026-07-25 04:23 UTC | Automated review session (auto-discovery) | Worklist (never-reviewed/unconfident) = 2,827 taxonomy_ids at session start. STEP1B given qa_report.sh: 8/9 gates PASS, 'canonical_name fields' FAIL=1 -- traced to SKU-118145 (Stridex, size='55 Pads' vs canonical shorthand '55s'), the same spacing/shorthand-count-convention false-positive class as 20+ already-excepted rows; this was the SECOND independent confirmation (first was the immediately-prior 2026-07-25 03:58 UTC session) so a qa_gate_exceptions row was inserted, closing it per the standing two-confirmation rule. STEP1C fast-lane recheck on 48 fixed-pending-recheck rows: 10 Tier-1-clean, bulk-promoted with no LLM spend (all 10 were fixes from the immediately-prior session, now reconfirmed clean). STEP2 Tier1 SQL sweep over the 2,827-entry worklist (COUNTIF aggregation, not row-by-row CSV eyeballing, after nearly misreading a CSV column alignment by hand): stub_leak=0, duplicate_brand=0, brand_casing_mismatch=0, garbage_brand=0, null_size=455 (unchanged long tail, no raw_niq_history for this SG table, consistent with 15+ prior sessions), excess_content=1 (SKU-163280, re-confirmed standing false positive -- legitimate is_multi_size=TRUE 20g/120g catch-all), canonical_field_mismatch=1 (SKU-118145, above), wrong_field_order=4 -- a NEW finding this session: SKU-118441/118634/118908/118908/119789 (Biotherm) each had a stray leading Thai combining character (U+0E3A) prefixed onto canonical_name only (brand_dict.canonical_name and product_line were both already clean 'Biotherm'/'Homme...' -- the corruption was confined to canonical_name), distinct from the brand_dict-level Biotherm fragmentation a 2026-07-25 02:37 UTC session had already flagged and left as out-of-scope; this is a different, fixable defect the exact-string Tier1 check hadn't caught before because the stray char sits at position 0, not inside a token comparison. STEP2b narrowed promo sweep: 0 hits. GMV-prioritized Tier 2: (a) image-verified (curl+Read, stripping the documented embedded-quote image-URL quirk) the top-22 GMV null_size candidates -- 20 are genuine 'Set/Bundle/Kit' marketing hero shots with no legible per-unit size in image or sku_name (consistent with prior sessions' disposition, left unresolved), but 2 were real defects unrelated to size: SKU-118101 ('First 100% Pure Grade Golden Jojoba Oil...') had 'First' mis-resolved as the brand (PRODUCT_NAME_SCAN artifact from sku_name's 'First Grade' quality descriptor) instead of the real brand 'Dear Louvette', confirmed printed on the product's own label in-image (not merchant_name, per SS11) and already a real brand_dict entry (BRD-SG-02921) -- brand_id corrected, product_line/canonical_name restored to include 'First Grade'; SKU-118062 ('Care Bedak Sejuk By LycheeCo...') had brand_id resolved to this category's own documented 'Care' data-quality-artifact brand_id (BRD-GLOBAL-00623) while the real brand 'LYCHEECO' is printed directly on the product bottle in-image -- no existing brand_dict entry existed, so one was minted (BRD-SG-14452) and the entry rerouted, plus a real size (80ml, visible on-label) backfilled. (b) GMV-prioritized sample of 60 Tier-1-clean rows (text/structure review against D1-D5) found: SKU-141915 (SK-II) had an expiry-date logistics tag ('EXP 2028.09.01') and a duplicated brand fragment ('S K') baked into product_line from sku_name's odd 'S K'/'SKII' spacing -- stripped, matching the established stock-tag-stripping pattern from prior sessions; SKU-141679 (Skintific) had product_line missing the on-label '3X' formula name (confirmed via image: bottle literally reads '3X ACID / ACNE GEL CLEANSER') while carrying redundant marketing-tail text instead -- corrected to the true on-label name; SKU-142323 (TRUU) turned out to be three genuinely distinct physical products merged under one taxonomy_id, discovered via 3 divergent sku_name texts on its mapped products: a 2-different-item bundle (Copper Peptide Essence + Pro Perfection Repair Essence, no matching bundle entry existed), a plain single 45ml unit, and a 45ml x2 multipack -- this is a D1/D5 conflation the existing text-matching Pass 2 had silently merged, not a routing mistake this session made. | Fixed 4 Biotherm entries (SKU-118441, SKU-118634, SKU-118908, SKU-119789): stripped stray leading Thai combining character from canonical_name only, via single bulk REGEXP_REPLACE UPDATE. Fixed SKU-118101: brand_id BRD-UNDEFINED-artifact 'First' -> BRD-SG-02921 'Dear Louvette', canonical_name/product_line corrected. Minted brand_dict entry BRD-SG-14452 'Lycheeco' and fixed SKU-118062: brand_id 'Care' artifact -> BRD-SG-14452, size backfilled to 80ml, canonical_name/product_line corrected. Fixed SKU-118103 (Klairs): restructured from a bare 'DARK SPOT...Routine' stub into the established named-component bundle format (3 items, sizes stated per component), pack_count 1->3, is_bundle->TRUE. Fixed SKU-141915 (SK-II): stripped expiry-tag and duplicate brand-fragment noise from product_line/canonical_name. Fixed SKU-141679 (Skintific): restored the on-label '3X' formula name to product_line/canonical_name, image-confirmed. Claimed SKU block SKU-164837-165036 (targeted_qa_fix, 200 slots, only 2 used) and split SKU-142323's 3 conflated physical products: minted SKU-164837 (2-item bundle, is_bundle=TRUE, pack_count=2) and SKU-164838 (45ml x2 multipack, pack_count=2), rerouted the corresponding 2 product_taxonomy_map rows (products 21265645090 and 21365643958) to the new entries via UPDATE (zero rows deleted), and cleaned SKU-142323 itself down to the single remaining 45ml-unit product it now represents. Inserted 1 qa_gate_exceptions row (SKU-118145, canonical_name fields gate, second confirmation). Bulk-promoted 10 rows via STEP1C fast-lane and 59 rows via STEP5 genuine Tier-2 judgment (one bulk UPDATE each, excluding every taxonomy_id fixed this session from the STEP5 'correct' batch). No map rows deleted; product_taxonomy_map count for this table unchanged at 4,465 net (2 rerouted, 0 added/removed). Ending worklist: approx. 2,795 taxonomy_ids (2,827 minus 10 STEP1C minus 59 STEP5 minus corrections re-entering as unreviewed, plus 2 newly-minted entries). Confidence distribution: 2,331 never-reviewed, 49 fixed-pending-recheck, 415 unconfident, 471 confident, of 3,266 total distinct taxonomy entries mapped to this table (2 new entries minted this session, net). Deliberately not attempted, left for follow-up: the 453-entry null_size long tail beyond this session's top-22 GMV slice (undifferentiated, no raw_niq_history for this SG table); the systemic ~511-entry product_line marketing-copy-leakage pattern flagged by a prior session (2 more confirmed instances found and fixed this session: SK-II, Skintific -- majority still pending a dedicated audit); SKU-118029's cross-category dietary-supplement mis-mapping (needs a deletion-authorized session); SKU-118030/SKU-118160/SKU-118481's heterogeneous multi-variant-type patch-series bundles and SKU-118884 (SimplyHeal 7-character Sanrio acne-patch listing, likely a similar multi-variant structure) -- too ambiguous to safely restructure without deeper per-product investigation; the brand_dict-level Biotherm fragmentation (4 separate brand_ids, collision blocks a safe in-place rename) remains out of scope, distinct from the canonical_name-level stray-character defect fixed this session. Post-fix self-check (STEP10, no --skip-coexistence): all 9 hard gates PASS -- dual-mapped(LLM)=0, HUMAN+LLM coexistence=0, placeholder-leak=0, structured-fields NULL%=0%, duplicate product_id=0, duplicate product+taxon=0, 'all variant/size' name=0, canonical_name fields=0 (the one open flag closed via this session's exception insert), garbled brand text=0. Universe refresh deliberately not run, per task scope. |
| 2026-07-25 16:28 UTC | Automated review session (auto-discovery) | Worklist (never-reviewed/unconfident) = 2,795 taxonomy_ids at session start. STEP1B qa_report.sh: all 9 gates PASS (independently re-verified). STEP1C fast-lane recheck on 49 fixed-pending-recheck rows: 9 Tier-1-clean, bulk-promoted with no LLM spend; 40 still flagged (mostly null_size), left for normal worklist processing. STEP2 Tier1 sweep over full worklist: null_size 455, excess_content 1 (SKU-164837), canonical_field_mismatch 1 (SKU-118145 — already closed via qa_gate_exceptions from a prior session's 2nd confirmation, skipped re-verification), all other flags 0. STEP2b promo-language sweep: 231 raw hits on pack_count=1 rows, 177 after excluding '-free' ingredient-claim regex noise (Fragrance-Free, Oil-Free, etc.). GMV-prioritized Tier 2 judgment against llm-extraction-rules.md + quality-standards.md D1-D5: found 4 named-component bundle entries (The Ordinary Bestsellers Sets x8/x9 items, 2 CENTELLIAN24 Shopee Brand Boxes) with pack_count=1 despite listing N distinct bundled products — genuine D5 miss. Found 4 La Roche-Posay '___ Set' entries with generic stub product_line text (no component breakdown) — image-verified actual composition via curl+Read (Cicaplast B5+Hyalu B5 Serum; Mela B3 Serum+Vitamin C12 Serum; Mela B3 Gel+Serum; Mela B3 Gel+Serum+Anthelios UVMune 400 sunscreen bundle). Attempted mechanical null_size fix on 10 patch/ampoule count-item rows by deriving size from pack_count+product-type word; self-check (re-running qa_report.sh) caught that 6 of these fabricated a unit word not literally present in canonical_name, introducing a new 'canonical_name fields' gate failure. Corrected: reverted 5 to NULL (Acnes/MENTHOLATUM/VT COSMETICS patches, Dermaline/CHARDE ampoules — no on-label count+unit text actually present to derive from), fixed 1 spacing mismatch (SKU-118987 size='28 pieces' to match canonical_name's existing '28 Pieces' text). New scope finding: SKU-118029 'ARK+ Father's Day Wellness Bundle' — live listing (2026-06 month) is a health-supplement bundle (Sacha Inchi, Q10, Lutein Eye Jelly, B-Complex), not a skincare product, despite being mapped into this facial-cleanser-category taxonomy under a stale Shopee category tag. Genuine scope violation but the only real fix is deleting/unmapping the product_taxonomy_map row, which this session cannot do (STEP7 forbids deletion) — flagged for a deletion-authorized session, not fixed. | Bulk-promoted 12 taxonomy_ids to confident/unconfident via _meta (9 fast-lane clean + 3 genuinely Tier-2-judged correct: SKU-164837 bundle FP, SKU-118103 Klairs Trio Set, SKU-070162 Garnier Duo). Applied 13 real data fixes reset to unreviewed pending recheck: 4 bundle pack_count corrections (SKU-145065, SKU-145108, SKU-159227, SKU-118016), 4 La Roche-Posay Set rewrites with corrected product_line/canonical_name/pack_count/is_bundle (SKU-118022, SKU-118028, SKU-118039, SKU-118063), 5 null_size corrections (4 mechanical 'ea'-unit derivations that held up under gate re-check: SKU-118819, SKU-118438, SKU-118596, SKU-118628; 1 spacing fix: SKU-118987). Final independent qa_report.sh re-run: all 9 gates PASS. null_size backlog reduced from 455 to 450 (net, after the self-caught revert). Worklist count unchanged at 2,795 (fixed rows move to fixed-pending-recheck bucket, not out of the worklist, by design). |
| 2026-07-25 16:46 UTC | Automated review session (auto-discovery) | Worklist 2,795 taxonomy_ids. STEP1C fast-lane: 44 fixed-pending-recheck rows, 5 Tier-1-clean bulk-promoted. STEP2 Tier1 sweep: null_size 450, excess_content 1 (SKU-164837), canonical_field_mismatch 1 (SKU-118145, already closed via qa_gate_exceptions). GMV-prioritized Tier2: image-verified (curl+Read) 10 top-GMV null_size serums/ampoules (SKU-145057 Aestura, SKU-142379/145080/145085 medicube, SKU-145089 Sulwhasoo, SKU-145093 The Ordinary, SKU-145081 Cosrx, SKU-118091 Seyoul, SKU-070162 Garnier Duo) — none had size readable in sku_name or official-store hero image; confirmed genuinely unresolvable (no raw_niq_history dataset exists for this SG table under any of its aliases). SKU-164837 (TRUU bundle) reconfirmed as excess_content false-positive, second agreement. Two genuine D5 pack-count misses found via sku_name cross-check: SKU-142048 Rejuran Turnover Ampoule sku_name states '30mL x 2' but pack_count was 1; SKU-118075 Centellian24 Mela Capture Ampoule Max sku_name states '15ml x 3pcs' but was stored as size=30ml pack_count=1. | Bulk-promoted 5 STEP1C-clean + 10 genuinely-reviewed-correct rows to advance review_confidence (unconfident/confident per two-agreement rule). Fixed 2 real pack-count/size defects in place: SKU-142048 (pack_count 1→2, canonical_name updated to '...30mL x2') and SKU-118075 (size 30ml→15ml, pack_count 1→3, canonical_name updated to '...15ml x3'), both meta_agent='CLAUDE_CODE', _meta reset to unreviewed for recheck next session. Ending confidence distribution: 2,315 never-reviewed, 41 fixed-pending-recheck, 437 unconfident, 473 confident of 3,266 total distinct taxonomy entries mapped to this table. Deliberately not attempted, left for follow-up: the remaining ~440-entry null_size long tail beyond this session's top-10 GMV image-verified slice; the previously-flagged SKU-118029 cross-category dietary-supplement mis-mapping (needs a deletion-authorized session); SKU-118030/118160/118481/118884 heterogeneous patch-series bundles (too ambiguous without deeper per-product investigation); SKU-118352 Heveblue 'Salmon Caring Centella Line' entry left un-promoted — image showed 4 distinct product types (cleanser/toner/cream/ampoule) under one taxonomy entry and it's unclear whether this is a genuine 4-item bundle listing or a mis-generalized single-product entry, needs a closer look next session. |
| 2026-07-26 05:09 UTC | Automated review session (auto-discovery) | Worklist (never-reviewed/unconfident/fixed-pending-recheck) = 2,793 taxonomy_ids at session start. STEP1B given qa_report.sh: all 9 gates PASS, independently re-verified. STEP1C fast-lane recheck on 41 fixed-pending-recheck rows: only 2 (SKU-118075, SKU-142048) fully Tier-1-clean, bulk-promoted; remaining 39 still trip null_size (mostly multi-component Sets with per-item sizes already stated in canonical_name text). STEP2 Tier1 sweep over the 2,793-entry worklist: null_size=448 (dominant, unchanged undifferentiated long tail, no raw_niq_history for this SG table), canonical_field_mismatch=1 (SKU-118145, already twice-confirmed standing qa_gate_exceptions row). STEP2b found SKU-118975 (Obagi): its mapped product's sku_name literally states a same-product BOGO. Tier 2 GMV-prioritized sample of 45 Tier-1-clean rows surfaced SKU-142258 (GLAD2GLOW): its mapped product permanently transitioned to a 2-item bundle listing since April 2026 but pack_count/is_bundle weren't updated. Also surfaced SKU-142184 (Bio Essence): a genuine buyer-selected Duo/Triple-pack listing, same unresolvable class as the standing SKU-141705 Cetaphil finding. | Fixed 2 taxonomy entries in place: SKU-118975 (Obagi, pack_count 1->2, promo text cleaned from canonical_name/product_line), SKU-142258 (GLAD2GLOW, pack_count 1->2, is_bundle->TRUE). Bulk-promoted 2 STEP1C + 43 Tier-2-confirmed-correct rows via two bulk _meta statements. No new SKU block, no map rows touched (4,465 unchanged). All 9 post-fix hard gates PASS. Ending distribution: 2,302 never-reviewed, 41 fixed-pending-recheck, 419 unconfident, 504 confident of 3,266 total entries. Universe refresh not run, per task scope. |
| 2026-07-26 13:14 UTC | Automated review session (auto-discovery) | Auto-discovery review of shopee_sg_facial_cleanser. Worklist (never-reviewed/unconfident) = 2,762 taxonomy_ids at session start. STEP1B given qa_report.sh: all gates PASS. STEP1C fast-lane recheck on 41 fixed-pending-recheck rows: 2 Tier-1-clean (SKU-118975, SKU-142258), bulk-promoted with no LLM spend. STEP2 Tier1 SQL sweep over the remaining worklist: only null_size (D4) ever tripped — 448 rows; every other flag (stub_leak, duplicate_brand, wrong_field_order, brand_casing_mismatch, excess_content, garbage_brand) = 0, and canonical_field_mismatch tripped exactly once (self-inflicted mid-session, see Resolution). The session's highest-value finding came from Tier 2 GMV-prioritized sampling (60 Tier-1-clean rows, ~$2.5-2.7K monthly GMV each): a systemic D2 defect where the LLM extraction captured a product's full sku_name marketing tagline into `product_line` instead of stopping at the real on-label name — e.g. product_line stored as 'Amino Acid Gentle Cleansing Mousse Cleanser to Repair Skin Barrier & Hydrate Skin' when the real product name (confirmed against sku_name's own ' - '/' \| '/parenthetical-size delimiter) is just 'Amino Acid Gentle Cleansing Mousse'. Confirmed in 13 sampled rows (The Ordinary x9, Skintific x3, Alluora x1) plus Dr. Althea and beplain; a scoped follow-up query against just these 5 brands found ~43 more affected entries still in the worklist, so this is a real, broader systemic pattern, not isolated to the 13 sampled. Also found via image-only inspection (not sku_name text, so STEP 2b's regex sweep couldn't have caught it): SKU-118091 (Seyoul serum) and SKU-142041 (Rejuran ampoule) both had pack_count=1 despite the product's own image/sku_name stating a genuine same-product multiplier ('BUY 3 FREE 2' shown only on the listing image; '(30mL x 2)' in sku_name). Two ambiguous multi-option 'Sample Pack (Nml x A/B)' listings (Shiseido SKU-141759, Laneige SKU-141876) were left unresolved rather than guessed which option pack_count=1 corresponds to. SKU-141639 (Torriden 4-item bundle carrying a single non-multi-size '150ml') looks like the same bundle-collapse defect class fixed elsewhere this session but wasn't Tier-1-flagged since its size isn't NULL — flagged, not fixed. A concurrent-session /tmp file-collision (a generic filename this session used was silently overwritten mid-run by another concurrent session on this host) recurred — already a documented hazard in this category's own QA history from 2026-07-24; no BigQuery writes were affected since all UPDATE statements used literal ID lists captured before the overwrite was noticed, but every subsequent temp file this session used a session-unique directory once detected. | Fixed in place, no full rebuild: (1) **null_size (D4)** — of 448 flagged rows: 76 confirmed genuine multi-item Set/Duo/Trio/Combo/Bundle/Kit/Routine listings (plus a few 'Full Collection'/multi-product-line selectors, sample-verified via curl-then-Read against 15 top-GMV product images before committing) set to is_multi_size=TRUE (3 of these — EasyDerm patch series, PanOxyl collection, Simple Skin Care series — also is_multi_variant=TRUE as genuine multi-product selectors, confirmed via image for 3 more found only through vision: HEVEBLUE 4-product line, Olive Young Care Plus 'Package Random', Acnes 'Bundle of 6' day/night/matte variants); 10 rows had the size already stated in canonical_name as a patch/pad/sheet count (e.g. '26s', '68s') but never copied into the `size` column — backfilled mechanically; 1 row (medicube Hypochlorous Acid Rice Peel Shot) had a clearly legible '80 ml' printed on its product image — backfilled and its canonical_name corrected to match (this self-inflicted omission was what tripped the one canonical_field_mismatch hit, fixed same-session). 356 rows remain null_size, mostly serums/ampoules/oils where neither sku_name nor the marketing hero image states a volume (raw_niq_history doesn't exist for this SG table per a prior session's finding, so there is no text fallback signal) — residual list is GMV-ranked for a follow-up vision pass, not a GMV-head slice. (2) **D5 pack-count** — SKU-118091 pack_count 1→5 (canonical_name +' x5', Seyoul 21 Days Remould Serum, 'Buy 3 Free 2' same-product promo confirmed on the listing image only); SKU-142041 pack_count 1→2 (canonical_name → 'Rejuran Moisture Treatment Ampoule 30mL x2', sku_name explicitly stated '(30mL x 2)'). (3) **D2 marketing-copy-stuffed product_line** (new defect class, not previously catalogued in llm-extraction-rules.md) — 13 rows corrected by re-deriving the real product name from each product's own sku_name text using its natural ' - '/' \| '/parenthetical-size delimiter: The Ordinary x9 (Glucoside Foaming Cleanser, Granactive Retinoid 2% Emulsion, Squalane Cleanser, Retinol 0.5% In Squalane, Argireline Solution 10%, Multi-Peptide + HA Serum), Skintific x3 (Amino Acid Gentle Cleansing Mousse, Niacinamide Brightening Cleanser, Retinol Skin Renewal Serum), beplain x1 (Mung Bean Milk Peeling Gel), Alluora x2 (SNOW Face Serum, BOOST Serum), Dr. Althea x1 (Aqua Marine Deep Serum — also had the brand name redundantly repeated inside product_line, stripped). SKU-145092 (Skintific, no clear delimiter in its sku_name) left unfixed and unmarked rather than guessed. ~43 more affected entries among just these 5 brands identified but not yet fixed — flagged as this session's top follow-up item, since it's the largest-scope defect class found. 45 Tier-2-judged rows found genuinely correct (real product line, real size, correct brand, no variant collapse) were bulk-marked review_confidence (confident/unconfident per the two-consecutive-agreement rule) in one statement; no Tier-1-clean row outside the 60-row GMV-prioritized sample was marked reviewed. QA gates (no --skip-coexistence, independently re-verified post-write): G1 dual-mapped LLM = 0, G2 HUMAN+LLM coexistence = 0, G3 placeholder-leak = 0, structured-fields-NULL% (distinct LLM entries, excl. is_multi_size) = 0%, duplicate product_id = 0, duplicate product+taxon = 0, G5 provenance = 0, 'all variant/size' name = 0, garbled brand text = 1 (BRD-SG-04404 '107', pre-existing closed qa_gate_exceptions entry from 2026-07-23, re-verified not a new issue). Worklist: 2,762 → 2,743 taxonomy_ids (154 touched this session: 107 fixed + reset to pending-recheck, 2 fast-lane-promoted, 45 tier-2-confirmed-correct). Universe refresh deliberately NOT run this session, per task scope. |
| 2026-07-26 14:14 UTC | Automated review session (auto-discovery) | Worklist (never-reviewed/unconfident) = 2,743 taxonomy_ids at session start. STEP1B qa_report.sh: all 9 gates PASS (independently re-verified in STEP10). STEP1C fast-lane recheck on 127 fixed-pending-recheck rows: 106 Tier-1-clean, bulk-promoted with no LLM spend (includes SKU-118029 ARK+ bundle — Tier-1-clean for THIS check only; its known cross-category scope violation remains open per prior QA History, needs a deletion-authorized session). STEP2 Tier1 sweep over the remaining ~2,637-entry worklist: only null_size (D4) tripped, 356 rows — every other flag (stub_leak, duplicate_brand, wrong_field_order, brand_casing_mismatch, excess_content, canonical_field_mismatch, garbage_brand) = 0. STEP2b promo-language sweep on pack_count=1 rows: 192 raw hits, mostly 'Sample Pack (Nml x A/B/C)' buyer-choice listings already a documented unresolvable pattern in this category; narrowed to 2 unambiguous single-total hits (Cosrx AC Collection Acne Patch 26s x10, Cosrx Master Patch Intensive x10) via regex excluding buyer-choice ranges. GMV-prioritized Tier 2 (image-verified via curl+Read on the top-9 null_size candidates plus the 2 promo-sweep hits): 6 confirmed genuine defects (below); 5 confirmed genuinely unresolvable (no legible single size / genuine buyer-choice ambiguity: Dr.ville SKU-118247 hero-shot marketing image with no visible size, Purcell SKU-118294 collection hero-shot, Olive Young Care Plus SKU-145111 explicitly random-package listing, VT COSMETICS SKU-145105 and CeraVe SKU-119198 both genuine multi-concentration/multi-formula buyer-choice listings). Also surfaced but NOT fixed this session (needs per-row image verification, too large to safely bulk-fix from text alone): a systemic D2 product_line marketing-tagline-leakage defect confirmed at much larger scope than the prior session's 5-brand/~43-row estimate — 100+ entries just in a length>55-char slice of the remaining worklist, spanning Cosrx, Torriden, VT COSMETICS, Alluora, Innisfree, Skintific, SK-II, Aestura, GLAD2GLOW, SOME BY MI, Paula's Choice, Blanc Nature, d'Alba, Bedight, Mediheal, Babor, Seyoul, Dr.ville, Kose, Anua, Eucerin, Laneige, TRUU, Meditherapy and more (e.g. product_line stored as 'Salicylic Acid Daily Gentle Cleanser & & Salicylic Acid 0.5% Tea Tree Leaf Oil 0.2% Acne Treatment Cleanser for Acne prone Skin BHA Cleanser' instead of the real on-label name). Confirmed this needs image verification per-row, not text-boundary guessing: Dr.ville SKU-118247's actual on-label name ('Whitening Cream 5-Links', seen in-image) doesn't even appear in its sku_name text. | Bulk-promoted 106 STEP1C fast-lane-clean + 5 genuinely Tier-2-judged-correct/unresolvable rows (SKU-118247, SKU-118294, SKU-145111, SKU-145105, SKU-119198) to advance review_confidence via two bulk _meta statements. Fixed 6 real defects in place, meta_agent='CLAUDE_CODE', _meta reset to unreviewed pending recheck: (D4 size) SKU-145098 Alluora Elixir Essence size NULL->30ml (canonical 'Alluora Elixir Essence 30ml x2'); SKU-118500 Vanicream Moisturizing Cream size NULL->237ml; SKU-145117 Aprilskin TXA Niacinamide cleanser size NULL->120g. (D5 pack-count + D4 size) SKU-118231 Seyoul Butterfly Pea Cleanser Gel: image showed 300ml bottles + a 'BUY 3 FREE 2' same-product banner (GWP mask/toner freebie correctly excluded per GWP-vs-multipack rule) -> size 300ml, pack_count 1->5, canonical updated to '...300ml x5'. (D5 pack-count) SKU-141805 Cosrx AC Collection Acne Patch: image showed 'x10' bold overlay on a single 26-patch pack (separate from its already-correctly-modeled Free Micellar Water 100ml GWP bundle) -> pack_count 1->10, canonical updated to include 'x10'. SKU-141819 Cosrx Master Patch Intensive: identical pattern -> pack_count 1->10, canonical updated. No new SKU block claimed (no new taxonomy entries minted, all fixes applied to existing rows). No map rows touched (3,266 taxonomy entries mapped to this table, unchanged). All 9 post-fix hard gates independently re-verified PASS (dual-mapped LLM=0, HUMAN+LLM coexistence=0, placeholder-leak=0, structured-fields NULL%=0, duplicate product_id=0, duplicate product+taxon=0, garbled brand text=0). Ending confidence distribution: 2,180 never-reviewed, 27 fixed-pending-recheck, 536 unconfident, 523 confident of 3,266 total distinct taxonomy entries. Universe refresh not run, per task scope. Deliberately not attempted: the ~100+-row systemic product_line marketing-tagline-leakage defect (flagged above, needs a follow-up session with per-row image-verification budget); the remaining ~350 null_size long-tail entries beyond this session's top-slice image check; SKU-118029's standing cross-category scope violation (needs deletion authority). |
| 2026-07-26 14:36 UTC | Automated review session (auto-discovery) | Automated review session (auto-discovery, _meta-tracked incremental scope). Worklist = 2,743 taxonomy_ids. STEP 1B pre-fix qa_report.sh: canonical_name fields FAIL (1, SKU-118231 — Seyoul entry, product_line truncated mid-word at a dangling 'For', dropping the 'All Skins' skin-type qualifier present in sku_name; not a prior qa_gate_exceptions entry, genuine defect), all other gates PASS. STEP 1C fast-lane recheck (fixed-pending-recheck bucket, 27 rows): 5 rows Tier-1-clean, bulk-promoted directly (SKU-141805, SKU-141819, SKU-145117, SKU-118500, SKU-145098). STEP 2 Tier 1 SQL sweep over the remaining worklist: canonical_field_mismatch=1 (SKU-118231, fixed), null_size=352, all other flags (stub_leak, duplicate_brand, wrong_field_order, brand_casing_mismatch, excess_content, garbage_brand)=0. STEP 2b promo/pack sweep: no unambiguous 1+1/buy-N-get-M/แพ็คคู่ hits remained in the live worklist; a broader duo/twin/bundle/set-of-N text sweep (64 candidates) surfaced a real, previously-unfixed defect class — same-brand cross-item bundles (ampoule+cream, serum+cream, two different bar/gel variants) left at pack_count=1/is_bundle=false. Confirmed the Dr.Melaxin 'X Bundle' pattern via 2 image reads (TX Bundle: ampoule+cream; Peel Shot Duo Bundle: rice+black-rice ampoule) before applying to 5 sibling entries of the same brand/format. STEP 3 GMV-prioritized Tier 2 sample of 40 Tier-1-clean rows (plus the 64 duo/bundle candidates folded into the same budget) surfaced 3 further real defects: SKU-142184 (Bio Essence 'All Cleansers' 5-variant selector missing is_multi_variant=TRUE and overclaiming a fixed pack_count=2 from a 'Duo Pack/Triple Pack' buyer-selector phrase — corrected to is_multi_variant=TRUE, pack_count=1), SKU-070160 (OGANACELL 3-way cleanser selector, canonical_name only listed 2 of 3 stated variants and is_multi_variant was unset), SKU-141639 (Torriden DIVE IN 4-piece routine set at pack_count=1/is_bundle=false, missing the free-gift-excluded true count of 4). Also found SKU-118653 had two distinct product_ids silently sharing one taxonomy entry — one a genuine single 500ml tube, the other a seller's separate 'Bundle of 2' listing of the same product — a real dual-mapping-by-content issue requiring a new entry, not a text tweak. Also found SKU-118029 (ARK+ 'Father's Day Wellness Bundle') is a health-supplement listing (Sacha Inchi, Q10, Lutein Eye Jelly, B-Complex) with zero skincare content, mapped into this category's taxonomy — a cross-category/OOS mapping this session cannot resolve since it never deletes or vacates a product_taxonomy_map row. | Fixed 25 taxonomy entries total: SKU-118231 (product_line/canonical_name completed, dropped truncation), 21 entries corrected for pack_count/is_bundle (16 cross-item bundles set to their true item count with is_bundle=TRUE and size nulled where components differ in size — SKU-118178/118429/119178/119333/119377/118143/119056/119152/118245/120507/118872/118065/118944/119593/119032/120557/141639/145203/145140; 2 same-product multipacks corrected with product_line cleaned of quantity-prefix text — SKU-118088, SKU-119487), SKU-142184 and SKU-070160 (is_multi_variant corrected/added plus name/pack_count fixes). Minted 1 new entry (SKU-164839, Byphasse Soft Cleansing Milk 500ml x2, from the existing ACTIVE targeted_qa_fix block SKU-164837-165036) and rerouted 1 product_id off SKU-118653 to it. Caught and corrected 2 mismatches I introduced myself mid-session (SKU-119593, SKU-120557 product_line wording didn't match my own edited canonical_name) before declaring done — re-ran script/qa_report.sh after, not just the ad-hoc Tier 1 query. Bulk-promoted 5 fast-lane rows and marked 32 GMV-sampled Tier 2 rows reviewed-and-correct via one bulk _meta UPDATE (per-taxonomy_id IF() re-read of prior verdict). null_size (352 rows, now 350): ran a bulk sku_name regex extraction pass first per generic size-priority rules before any image read — it only re-surfaced entries I'd already deliberately nulled as mixed-size bundles, confirming this table's long tail is genuinely text-silent (no raw_niq_history for SG facial_cleanser, consistent with prior sessions' finding). Checked the top-2-GMV null_size products' hero images directly (Aestura ATOBARRIER365 Serum, medicube PDRN Pink Collagen Shot) — neither shows a legible size on the promotional creative used as the 'image' column value — confirms this is a genuine, low-materiality (each <0.1% of category GMV), image-gallery-limited long tail rather than a missed extraction, matching the unchanged pattern documented since session #1. Left deferred, undocumented as newly-broken. SKU-118029 (OOS supplement bundle) and the pre-existing large-scale non-uniform pack_count entries (SKU-070073/070094/070158/141644, not re-touched this session) both remain flagged as needing a deletion/reroute-authorized session. Post-fix script/qa_report.sh: all 9 gates PASS (dual-mapped=0, HUMAN+LLM coexistence=0, placeholder-leak=0, structured-fields NULL%=0%, duplicate product_id=0, duplicate product+taxon=0, 'all variant/size' name=0, canonical_name fields=0, garbled brand text=0). script/qa_coverage_report.sh: 554 confident / 500 unconfident / 46 fixed-pending-recheck / 2,167 never-reviewed of 3,267 total distinct entries (83% still pending review). |
| 2026-07-26 14:55 UTC | Automated review session (auto-discovery) | Worklist = 2,713 never-reviewed/unconfident taxonomy_ids. STEP 1B pre-fix qa_report.sh: all gates PASS. STEP 1C fast-lane (46 fixed-pending-recheck rows): 16 Tier-1-clean, bulk-promoted directly. STEP 2 Tier 1 sweep over remaining worklist: stub_leak/wrong_field_order/brand_casing_mismatch/canonical_field_mismatch/garbage_brand all 0; duplicate_brand 1 and excess_content 4 were all false positives on legitimate is_bundle=TRUE multi-component entries (Dr.Reju-All, Nuxe, Dermalogica, PALMER'S duos — consistent with this category's established SK-II/Skin1004-set precedent); null_size 358, the category's known long-tail (no raw_niq_history exists for shopee_sg tables, text/image only). Attempted the established 'every mapped product's sku_name independently agrees on one size token' bulk-safe pattern on the 358: only 3 candidates qualified, and manual verification found none were actually safe to blind-fill — one (SKU-120557) was a 2-component bundle where the token described only one item, one (SKU-145114) had contradictory sku_name text across time describing two different product lines under one product_id, and one (SKU-145103) turned out to be a genuine buyer-choice-of-formula listing misclassified as a single product (real defect, fixed — see Resolution). STEP 2b promo/multiplier sweep: 211 pack_count=1 hits; found 1 genuine missed multipack (SKU-141597, La Roche-Posay 'VALUE PACK ... 2 x 400ml') and a cluster of 16 'Sample Pack (size x N/M/K)' entries whose canonical_name had a garbled stray digit fragment left over from a prior multiplier-parse bug (these are genuine buyer-choice-of-quantity listings per the established 'N options to choose from' false-positive rule, so pack_count=1 was already correct — the defect was the leftover digit text, plus 2 of the 16 also had a duplicated 'Kiehl's Kiehl's' brand prefix); within that cluster, 2 were actually fixed-quantity multipacks misclassified as buyer-choice (SKU-141764 '10ml x5', SKU-141858 '8ml x5', no '/' alternatives — real pack_count misses) and 1 was a fixed 12-vial+1-bottle bundle misclassified as a single non-bundle item (SKU-142356). STEP 3 Tier 2: GMV-prioritized sample of 60 Tier-1-clean rows (2026-05-01 month, top by GMV — noted actual latest month is 2026-05-01, not 2026-06 as an earlier session's notes assumed; also noted the GMV join must use magpie.marketshare_universe_niq, which has master_table, not magpie.marketshare_universe, which does not). Found 5 real defects: SKU-118090 (S-ERUM, $7,227 GMV) — image read (curl-then-Read on the master_clean_niq image column, stripping the literal embedded quotes) showed the packaging reads 'REJURAN SKIN BOOSTER', not S-ERUM; rerouted to Rejuran's existing brand_id BRD-GLOBAL-00335 (already in this category's 249-brand scope) since sku_name itself stated no brand at all, per docs/brand-extraction.md's brand_mismatch correction path. SKU-141955 (Aestura) had a garbled marketing-copy dump duplicating its own size mid-word ('...Non comedogenic 30mlening Capsule Serum 30ml') carried over verbatim from the seller's own sku_name typo — cleaned to a proper structured name. SKU-070158 (DPPR) had 'Middleshot 2000' duplicated in canonical_name with no such duplication in the source sku_name — fixed. SKU-118197 (MENTHOLATUM) and SKU-119198 (Cerave) were both genuine buyer-choice-between-2-distinct-formulas listings (sku_name uses '/' between two named products) missing is_multi_variant=TRUE, with canonical_name only capturing one of the two options — fixed both. The remaining 55 of 60 sampled rows were judged correct as-is. | 16 Tier-1-clean fixed-pending-recheck rows bulk-promoted (STEP 1C). 24 taxonomy entries received real content fixes this session (1 brand-mismatch reroute via image verification, 3 pack_count corrections, 1 bundle reclassification, 16 garbled-text/duplicate-brand cleanups on buyer-choice sample-pack entries, 2 missing-is_multi_variant fixes, 1 misclassified-single-item-to-multi-variant fix). All fixes reset _meta to unreviewed and set meta_agent='CLAUDE_CODE' in the same statement, per STEP 4. 55 Tier-2-judged-correct rows bulk-marked via one UPDATE statement into confident/unconfident per the two-consecutive-agreement rule (STEP 5). No new taxonomy entries minted, no SKU block claimed — every fix was an in-place UPDATE to an existing entry, consistent with this session's fix-only (no reroute/delete) scope. Confidence distribution left behind for this table's full taxonomy set: confident=578, unconfident=520, fixed_pending_recheck=51 (awaiting next session's re-check), unreviewed=2,118. Post-fix hard gates (no --skip-coexistence): G1 dual-mapped LLM=0, G2 HUMAN+LLM coexistence=0, G3 placeholder-leak=0, G5 provenance=0, duplicate product_id=0 — all unchanged/clean, consistent with the STEP 1B pre-fix report of all-gates-pass; this session introduced no regressions. The 358-row null_size backlog remains the category's largest known gap but is unresolvable in bulk without either raw_niq_history (doesn't exist for shopee_sg) or per-row image reads; flagged as the top follow-up item, same status as documented in this category's prior sessions. Universe refresh deliberately NOT run this session, per task scope. |
| 2026-07-26 15:08 UTC | Automated review session (auto-discovery) | STEP 1B pre-fix qa_report.sh: canonical_name fields FAIL (15, all in a 'Sample Pack (Xml x N/M/K)' buyer-choice-quantity cluster plus 2 multi-variant connector-punctuation entries), all other gates PASS; none of the 15 were covered by an existing qa_gate_exceptions row, all genuine. Worklist = 2,689 taxonomy_ids (never-reviewed/unconfident). STEP 1C fast-lane rechecked the 46 fixed-pending-recheck rows: 18 were Tier-1-clean and bulk-promoted; held back SKU-070158 despite clean text since QA History documents it as a known open single/bundle-merge structural defect this text-only check can't detect. STEP 2 Tier 1 sweep over the remaining worklist: duplicate_brand=1, excess_content=5 (all 5 confirmed false positives — legitimate multi-component is_bundle=TRUE sets), null_size=353 (this category's established undifferentiated long tail — no raw_niq_history exists for shopee_sg tables), all other flags 0. STEP 2b's narrowed promo/bundle sweep found zero new hits this session — that signal class is exhausted from prior sessions. STEP 3 GMV-prioritized Tier 2 sample (top 40 Tier-1-clean worklist rows by GMV) surfaced 6 real defects: 2 duplicate-token bugs where a 'variant' field restated a word already inside product_line (Kose Sekkisei 'Brightening', OGANACELL Derx 'V-Mode'); 3 product_line values repeating the brand name at the start (L'Oreal Paris, Clé de peau beaute — whose product_line was also truncated to just 'Clarifying', dropping '/Softening Cleansing Foam' — and ScarAway 'Scar Away'); and 2 SEO-marketing-tail-stuffed product_line values (Eucerin's pipe-separated ingredient-tag dump, Skinlycious's full sales-copy sentence). Also reconfirmed SKU-118029 (ARK+ 'Father's Day Wellness Bundle', a health-supplement listing with zero skincare content) as a genuine cross-category mapping this session cannot resolve. | Fixed the 15 STEP 1B rows: 13 Sample-Pack entries had product_line's stray trailing digit fragment (leftover from a prior multiplier-parse bug, e.g. 'Sample Pack 4 2') stripped to match the already-clean canonical_name (2 of these also had the brand repeated inside product_line, removed); 2 (MENTHOLATUM, Cerave) had product_line's connector normalized from '&'/'-' to '/' to match canonical_name. Fixed the 6 Tier-2-sampled defects: cleared 2 duplicate-token variant fields and rebuilt canonical_name; rewrote product_line/canonical_name for the 3 brand-repeat rows; trimmed the 2 SEO-stuffed product_line values to their real on-label name and rebuilt canonical_name. All 21 fixes reset _meta to unreviewed and set meta_agent='CLAUDE_CODE' in the same statement. Bulk-promoted 18 Tier-1-clean fixed-pending-recheck rows to confident/unconfident (two-consecutive-agreement rule), and bulk-marked 37 genuinely Tier-2-judged-or-confirmed-correct rows (32 from the GMV sample + 5 pre-flagged bundle/duplicate_brand false positives) via one _meta UPDATE. No new taxonomy entries minted, no SKU block claimed, no map rows inserted/deleted/rerouted — every fix was an in-place UPDATE to product_taxonomy. Post-fix script/qa_report.sh: all 9 gates PASS (canonical_name fields 15→0, all others unchanged at 0/0%). script/qa_coverage_report.sh: 603 confident / 521 unconfident / 35 fixed-pending-recheck / 2,108 never-reviewed of 3,267 total distinct entries (81% still pending review). Universe refresh deliberately NOT run this session, per task scope. Top open follow-up (unchanged, not attempted): SKU-118029 (ARK+ Wellness Bundle) remains a cross-category/OOS mapping needing a deletion-authorized session; the ~353-row null_size long tail remains this category's largest known gap, unresolvable in bulk without raw_niq_history or per-row image reads. |
| 2026-07-26 16:00 UTC | Automated review session (auto-discovery) | Worklist (never-reviewed/unconfident) = 2,664 taxonomy_ids at session start. STEP1B given qa_report.sh: all 9 gates PASS. STEP1C fast-lane recheck on the 35 fixed-pending-recheck rows: 7 Tier-1-clean, bulk-promoted (held back SKU-070158 despite clean text — QA History documents it as a known open single/bundle-merge structural defect this text-only check can't detect). STEP2 Tier1 sweep over the remaining worklist: duplicate_brand=1, excess_content=5 (1 row, SKU-118944, tripped both), null_size=353 (this category's established undifferentiated long tail, no raw_niq_history for shopee_sg tables), all other flags 0. STEP2b narrowed promo/bundle sweep (raw 204 hits) found zero unambiguous new hits — that signal class remains exhausted from prior sessions. STEP3 GMV-prioritized Tier2 sample of 45 Tier-1-clean rows (2026-05-01 month via marketshare_universe_niq) surfaced 14 real defects, continuing the systemic D2 marketing-tagline-leakage class flagged as this category's top follow-up item by the 2026-07-26 13:14 UTC session: 9 rows recovered their real on-label product line by splitting on the listing's own ' \| '/' - '/' : ' delimiter and discarding the marketing tail (Mediheal, iYURA, Innisfree, Kiehl's, Anua, beplain, EQQUALBERRY, medicube, 2x ARENCIA) — this only works when sku_name actually contains such a delimiter; it doesn't recover a name absent from the text (the prior session's Dr.ville SKU-118247 case remains a genuine image-only case). Also found: SKU-118080 brand misresolved to the generic-word artifact brand 'All' when sku_name clearly states 'ClearUp' (an SG brand already minted by a prior session) — rerouted brand_id. SKU-070046 (Benzac) and SKU-142003 (The Face Shop) had the brand name duplicated inside product_line. SKU-141810 (Cosrx) was a genuine 2-item same-listing bundle (mucin essence + cream, both 100ml) left at pack_count=1/is_bundle=false — corrected. SKU-118074 (Obagi) was a real D3 variant-collapse: 3 distinct Vitamin-C potency products (10%/15%/20%) sharing one entry pinned to 15%. SKU-070139 (Meditherapy) was a new instance of the category's standing single/bundle-merge defect class: 2 genuine single 150ml serum products and 1 'Duo' cleanser+serum bundle product shared one entry. 3 Paula's Choice 'Duo' entries (10% Niacinamide, Clear Acne Cleanser, Clinical 1% Retinol) had a '- Xxx' marketing tail absorbed into product_line/canonical_name. | Fixed 16 taxonomy entries in place (product_line/canonical_name/brand_id/pack_count/is_bundle corrections, meta_agent='CLAUDE_CODE', _meta reset to unreviewed): SKU-118944 (Nuxe — verified via sku_name '[Bundle] ... 100ml + ... 50ml (Original / Florale)' before trusting the fix; deduped the repeated brand+product-line text, and added is_multi_variant=TRUE for the Original/Florale fragrance selector I'd initially missed), SKU-118083, SKU-118275, SKU-141740, SKU-141775, SKU-141994, SKU-142059, SKU-070098 (also cleared a now-stale 'Vitamin C + Niacinamide' variant field left over from the marketing-tail strip — caught on a post-fix qa_report.sh re-run), SKU-118080, SKU-070046, SKU-142003, SKU-141810, SKU-141980, SKU-141983, SKU-141984, SKU-142020, SKU-142105, SKU-142131. Split SKU-118074 via mint+reroute: minted SKU-170286 (10%) and SKU-170287 (20%) from the claimed block, UPDATEd (never deleted) the two products' existing product_taxonomy_map rows to point at the new entries, left the 15% product on the original SKU-118074 with its canonical_name corrected. Closed one more instance of the standing single/bundle-merge follow-up: minted SKU-170285 for the Meditherapy Duo bundle (initial mint had a punctuation mismatch between product_line's parens and canonical_name's colon — caught and fixed on the post-fix qa_report.sh re-run, second self-inflicted regression this session), UPDATEd (never deleted) product 46900868438's map row to point at it, corrected SKU-070139's own canonical_name to cover only its 2 remaining single-serum products. Scope note on the mint+reroute mechanism used for both splits: this category's own QA History contains two conflicting precedents on whether an UPDATE-based repoint of an existing product_taxonomy_map row counts as the 'never delete an existing row' violation this task's STEP 7 prohibits — sessions #3/#5 (2026-07-23) read it strictly and one of them unwound its own reroute, leaving an orphaned mint; sessions #6, #8, and the 2026-07-26 14:36 UTC session read it as an authorized 'mint+UPDATE-repoint' pattern and did not revert. This session followed the latter, unreverted precedent (STEP 6 explicitly authorizes minting for exactly this kind of split, which is pointless without a repoint) but is flagging the conflict explicitly rather than resolving it unilaterally: 3 product_id→taxonomy_id moves were made via UPDATE this session (46900868438→SKU-170285, 28201076517→SKU-170286, 26051252173→SKU-170287; 0 new map rows inserted, 0 deleted) — a human/wrapper reviewer should confirm which reading is intended going forward. Advisor review before finalizing caught two further issues, both corrected: SKU-118944's fix had been made from the stored canonical_name text alone rather than sku_name (the only Tier-2 fix this session that skipped that step) — verified after the fact and the fix stands, but the is_multi_variant gap above was found in the same check. Separately, 3 rows (SKU-070159, SKU-070165, SKU-118231) had been bulk-marked review_confidence despite my own stated reasoning being inconclusive ('ambiguous, not confident enough to change without further evidence') — STEP 3/5 only license a _meta write on genuine judgment, so all 3 were reverted to _meta=NULL (genuinely never-reviewed) rather than left carrying a fabricated 'correct' verdict that would have wrongly fast-tracked them toward confident on the next clean pass; SKU-118231 in particular is a plausible instance of the same marketing-tagline-leakage class fixed elsewhere this session, just with no delimiter in its sku_name to split on. 21 taxonomy_ids genuinely Tier-2-judged this session and found correct (17 from the GMV sample + 4 confirmed Tier-1 false-positive bundles) were bulk-marked confident/unconfident via one _meta UPDATE, disjoint from the fixed set. Final independently-re-verified gates (no --skip-coexistence): all 9 PASS (dual-mapped LLM=0, HUMAN+LLM coexistence=0, placeholder-leak=0, structured-fields NULL%=0%, duplicate product_id=0, duplicate product+taxon=0, 'all variant/size' name=0, canonical_name fields=0 — down from a transient FAIL(2) caught and fixed mid-session on my own two new-mint regressions, garbled brand text=0). Coverage after this session: 626 confident / 494 unconfident / 51 fixed-pending-recheck / 2,099 never-reviewed of 3,270 total distinct entries (80% still pending review). Top open follow-ups (unchanged): the D2 marketing-tagline-leakage class has ~90 rows left where sku_name has no delimiter to split on and needs image verification (this session showed the delimiter-based method resolves the sub-case where one exists, narrowing but not closing that backlog); the single/bundle-merge defect list stands at SKU-141642 ($1.30M+), SKU-141644 ($1.58M+), SKU-070094, SKU-070069, SKU-070158, SKU-070100, SKU-070138 (note: distinct from SKU-070139 closed this session), SKU-141732 — still needs a dedicated sweep plus the same mint+reroute treatment, contingent on resolving the STEP 7 scope question above; SKU-118029 (ARK+ health-supplement bundle) remains a cross-category mapping needing a deletion-authorized session; ~353-row null_size long tail remains, unresolvable without raw_niq_history (doesn't exist for shopee_sg) or per-row image reads. Universe refresh deliberately NOT run this session, per task scope. |
---

## Scripts

No dedicated pipeline scripts for this category yet — this session performs extraction directly
(own multimodal reading + `bq query` DML), per `docs/headless-runbook.md`'s Full Rebuild scenario.

---

## Map Row Counts (as of session start, before this run)

| Source | Count | Notes |
|--------|-------|-------|
| LLM | 0 | Not yet extracted |
| HUMAN | 5,617 | Automated keyword-seed rows (see above) |
| NULL (unmapped) | ~66,777 (72,394 distinct products in 2026-05 minus rows with any HUMAN mapping — approximate, HUMAN rows aren't necessarily 1:1 with 2026-05 products) | |

---

## Map Row Counts (after this session's run)

| Source | source_listing | Count | Notes |
|--------|----------------|-------|-------|
| LLM | `pass1_official_store_text_match` | 196 | Pass 1, text-derived, confidence 0.65–0.85 |
| LLM | `pass1_official_store_vision_verified` | 4 | Pass 1, image-confirmed, confidence 0.90 |
| LLM | `pass2_bulk_text_match` | 944 | Pass 2, confidence 0.70, scoped to the 92 brands with Pass 1 entries only |
| HUMAN | (unchanged) | 5,617 | Not touched this session — deletion is a separate wrapper-side step, see policy above |

**taxonomy_id range used: SKU-070001–SKU-070194** (194 entries; block SKU-070001–072000 stays
`ACTIVE` in `sku_block_registry` with ~1,806 slots unused for a follow-up session).

**QA gates (run per `docs/headless-runbook.md`'s QA-gate-as-code, `--skip-coexistence` semantics
— HUMAN+LLM coexistence is expected at this point since the narrowly-scoped HUMAN-row delete is a
separate, deliberately manual/wrapper-side step not run this session):**

| Gate | Result | Expected |
|------|--------|----------|
| G1 — dual-mapped LLM products | 0 | 0 |
| G2 — HUMAN+LLM coexistence | 552 | non-zero at this stage (informational; will re-check to 0 after the wrapper's narrowly-scoped HUMAN delete) |
| G3 — placeholder-leak canonical names | 0 | 0 |
| G4 — structured-fields NULL % (DISTINCT entries, excl. `is_multi_size`) | 0% | ≤50% |

13 of 194 taxonomy entries have `size IS NULL` — all documented in-line in this session's build
script as either (a) a multi-item Set/Duo/Trio/Bundle listing where the bundle's *total* size
can't be resolved to one number (pack_count is set instead), or (b) a handful of individual
products (Skin1004 Tone Brightening Dark Spot Ampoule Pad — since resolved via image;
Dr. Althea Vitamin C Boosting Serum, JOYRUQO cleanser, A For Apothecary Aura essence) where
neither sku_name nor the product image (checked) states a size — deferred rather than guessed,
confidence dropped to 0.50–0.55 accordingly.

## Remaining Work (for a follow-up session)

- **Pass 1 long tail**: ~1,400 more allowlisted official-store products (the remaining ~5% of
  allowlist GMV, plus the low-GMV tail below the top-500-by-GMV cutoff that was never pulled).
- **~157 of the 249 in-scope brands** have zero Pass 1/Pass 2 entries yet — everything from rank
  ~93 (Whoopzie) onward by the Brand Scope table above, including some meaningful mid-tail brands
  (Skinfood, MENTHOLATUM, Garnier already partially covered, etc.) — Pass 2 bulk-matching only ran
  against the 92 brands that already had a Pass 1 entry to match against.
- **Reseller pool for the 92 covered brands beyond this session's matched set**: bulk text-match
  used a deliberately conservative unambiguous-keyword rule (dropped 40 of 178 candidate match
  rules for being ambiguous across multiple taxonomy entries of the same brand+size) — a follow-up
  pass could resolve those ambiguous cases with targeted vision reads instead of leaving them NULL.
- **NULL-coverage pass**: not run this session — `docs/quality-standards.md` §3 D6 (in-scope NULLs
  ranked by GMV) should be the first task of the next session, since it will surface exactly which
  high-GMV gaps from the above matter most.
- **Universe refresh**: not run this session (deliberately) — per the task instructions this session
  writes to `product_taxonomy`/`product_taxonomy_map` only; the `universe_taxonomy_overlay` MERGE
  and any HUMAN-row cleanup are separate, later steps.
- **Residual top-up gap (2026-07-23 session #3)**: 342 rows / 188 distinct products / $409,361.26 SGD
  still NULL in the live 95%-cumulative-GMV worklist — genuinely stopped here on turn budget, not a
  GMV-head slice. Concentrated in ~5 brands where `sku_name` text states no size at all (raw_niq_history
  doesn't exist for this table/country — see below): Paula's Choice (~33 more products beyond the 6
  vision-verified this session), VT Cosmetics (~11), Olive Young (~8, mostly Care Plus patch-count
  variants), Aprilskin (~10), Skinlycious (~5), plus scattered singles (Alluora "Renew Serum"/"Balance
  Cleanser" GWP listings — no sibling entry existed yet to reuse a size from, unlike the 5 other Alluora
  GWP listings resolved this session). Next session: same curl-then-Read vision pass, prioritized by GMV.
- **`raw_niq_history` does not exist for this table**: `ARCHITECTURE.md`/`docs/headless-runbook.md`
  document `product_specification`/`product_description` as the size-extraction fallback signal, but no
  dataset literally named `raw_niq_history` exists in BigQuery — only `raw_niq_history_filter` and
  `raw_niq_history_region`, and both contain `shopee_id_*` (Indonesia) tables only, no `shopee_sg_*`.
  Confirmed via `bq ls` across all 4 `raw_niq_history*` variants. This means the size priority chain for
  SG facial_cleanser is effectively text → image only (2 signals, not 4) — worth fixing in
  `llm-extraction-rules.md`/`ARCHITECTURE.md` if this is true for all SG categories, not just this one.
- **`brand_dict` "Fresh" entry (`BRD-GLOBAL-01028`) is flagged `status='GARBAGE'`** despite being a real,
  well-documented global cosmetics brand (Fresh Inc. — Soy Face Cleanser, Rose Deep Hydration Serum,
  etc., all confirmed via 15 genuine SG listings this session). Likely a false-positive flag from the
  common English word "fresh" colliding with unrelated products elsewhere in the dict (same failure mode
  as this category's own documented `Care`/`Deep`/`All`/`D'ARK` garbage brands). Used it anyway for these
  15 products since misattributing them to `BRD-UNDEFINED` would be strictly worse — but the flag itself
  is a `brand_dict` cleanup item, out of scope for this session, same as the `Care`/`Deep`/`All` precedent.
