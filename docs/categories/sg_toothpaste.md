# shopee_sg_toothpaste — Category Context

> Full Rebuild session, 2026-07-16. First LLM extraction for this category — was "⏳ Keyword only"
> per `docs/categories/STATUS.md` before this run. Source table confirmed as `shopee_sg_toothpaste`
> in `master_clean_niq` (the task that kicked off this file used the short form `sg_toothpaste`,
> which does not exist as a table — `master_table` values everywhere use the full `shopee_sg_toothpaste`
> name, matching the pattern already established for `sg_shampoo` / `shopee_sg_shampoo`).

---

## Status

| Field | Value |
|-------|-------|
| LLM Pass 1 | ✅ Complete — 519 taxonomy entries, 535 official-store products mapped |
| LLM Pass 2 | ✅ Complete (with documented gaps) — 146 bulk-text-matched + 436 no-official-store-brand catch-all mapped; `Care`/`Hygiene`/`Deep` noise brands and non-matching officially-stored-brand resellers left unmapped |
| GMV Coverage | 92.3% (June 2026), 2,589 of 9,566 distinct products — exceeds the ≥85% Pass 1+2 target in `docs/quality-standards.md` §6 |
| Last run | 2026-07-16 |
| Current MAX taxonomy_id (pre-session, per STATUS.md — NOT trusted, re-verified live before claim) | SKU-058455 (stale — live check at claim time found SKU-070194) |

---

## SKU Blocks Assigned

| Block | Usage |
|-------|-------|
| SKU-072001–SKU-074000 (2000 slots, claimed atomically 2026-07-16, `sku_block_registry` status ACTIVE) | Pass 1 OFFICIAL + Pass 2 RESELLER + structured-field work. Live `MAX(taxonomy_id)` at claim time was SKU-070194 — no collision. |
| SKU-137441–SKU-137743 (303 slots, claimed atomically 2026-07-23, scenario `taxonomy_topup`, `sku_block_registry` status ACTIVE) | 2026-07-23 top-up session. Only SKU-137441–SKU-137641 (201 entries) actually used; SKU-137642–SKU-137743 (102 slots) unused and left in the block. |

---

## Scale (verified live 2026-07-16)

- **697,334 total rows** in `master_clean_niq.shopee_sg_toothpaste`; **12,455 distinct products**;
  data spans `2025-02-01`–`2026-06-01` (17 months). Review month for GMV ranking: **2026-06-01** (latest).
- **73,882 rows** with `merchant_badge = 'Shopee Mall'` (**1,858 distinct Mall products**) — the
  candidate pool before narrowing to the actual allowlist below. Pass 1 must scope to the allowlist
  only (584 distinct products), not the full Mall-badged pool.
- NIQ category tagging within this table is clean: 100% of rows fall under `category_3_EN='Oral Care'`/
  `'Dental Care'` with `category_4/5_EN` = `Toothpastes`/`Toothpaste` — no cross-category contamination
  bucket (unlike e.g. TH body_wash mixing in hand wash). However, individual `sku_name`s inside this
  clean bucket still contain the standard toothpaste-adjacent OOS patterns (row counts, all months):
  oil pulling 145, candy/lozenge 2,881, denture 977, mouthwash-as-main 237, toothbrush-as-main 2,542.
  These are excluded from both the brand-scope GMV ranking and taxonomy building (see Scope below).
- **Pass 1 pool: 584 distinct products** across the 20-entry Official Store Allowlist below.
- **Pass 2 pool: 3,950 distinct products** — remaining products under the 44 in-scope brands, sold
  outside the allowlisted stores (resellers, multi-brand Mall retailers, non-Mall sellers).
- Remaining ~7,950 distinct products in the category are below the 95%-GMV brand threshold or
  `BRD-UNDEFINED`/long-tail — legitimately left `UNRESOLVED` per `docs/llm-extraction-rules.md` §5.

---

## Existing map rows (Step 1 — do not assume 0/0)

```sql
SELECT source, COUNT(*) FROM product_taxonomy_map WHERE master_table = 'shopee_sg_toothpaste' GROUP BY source;
```

| Source | Count |
|--------|-------|
| HUMAN | 1,534 |
| LLM | 0 |

1,534 pre-existing `source='HUMAN'` rows (automated keyword-seed routing, per `ARCHITECTURE.md`
Decision 18 — not actual human review). Per the manager-confirmed policy already applied to
`sg_shampoo` (2026-07-16): **do not delete any HUMAN rows during this session.** Deletion of
HUMAN rows that duplicate a newly-created LLM row for the same product is a separate, deliberately
manual/wrapper-side step performed after this session, never something this session does itself.

---

## Brand Scope (real cumulative-GMV 95% threshold, GWP-zeroed, review month 2026-06-01)

Computed by ranking brands by `SUM(IF(flag_GWP, 0, gmv_monthly))` over **in-scope sku_names only**
(excludes oil pulling / candy-lozenge / denture / mouthwash-as-main / toothbrush-as-main rows —
see Scope section), joined through `product_brand_map`. Cumulative fraction crosses 95% at rank 44
(Sun Star, 95.09%) — **44 brands in the real threshold**, not a fixed top-N snapshot.

| # | Brand | brand_id | GMV (June 2026, SGD) | Cum. % |
|---|-------|----------|----------------------|--------|
| 1 | Sensodyne | BRD-GLOBAL-00101 | 76,195 | 19.70% |
| 2 | Colgate | BRD-GLOBAL-00043 | 74,419 | 38.94% |
| 3 | Dr.ville | BRD-GLOBAL-00875 | 46,391 | 50.93% |
| 4 | Elgydium | BRD-SG-01398 | 29,279 | 58.50% |
| 5 | Pearlie White | BRD-GLOBAL-01781 | 16,033 | 62.65% |
| 6 | Darlie | BRD-GLOBAL-00420 | 11,639 | 65.66% |
| 7 | *(BRD-UNDEFINED — 10,818 GMV, 68.45% cum.; not a real brand, counted in the denominator only, excluded from allowlist work)* | | | |
| 8 | GUM | BRD-GLOBAL-00990 | 8,570 | 70.67% |
| 9 | Zenyum | BRD-SG-02801 | 6,445 | 72.33% |
| 10 | KORMESIC | BRD-GLOBAL-01932 | 6,344 | 73.98% |
| 11 | Dentiste | BRD-GLOBAL-00064 | 6,136 | 75.56% |
| 12 | Parodontax | BRD-GLOBAL-00323 | 6,105 | 77.14% |
| 13 | GC | BRD-SG-01154 | 5,694 | 78.61% |
| 14 | Marvis | BRD-GLOBAL-00363 | 4,980 | 79.90% |
| 15 | Ora2 | BRD-GLOBAL-00809 | 4,601 | 81.09% |
| 16 | Bioniq | BRD-SG-02874 | 4,448 | 82.24% |
| 17 | Oral-B | BRD-GLOBAL-00081 | 3,882 | 83.24% |
| 18 | BOTANICA CULTURE | BRD-SG-02817 | 3,463 | 84.14% |
| 19 | Apagard | BRD-SG-00601 | 3,353 | 85.00% |
| 20 | Care *(brand_dict noise — see Edge cases)* | BRD-GLOBAL-00623 | 2,901 | 85.75% |
| 21 | Curaprox | BRD-GLOBAL-00246 | 2,529 | 86.41% |
| 22 | Atomy | BRD-GLOBAL-00813 | 2,365 | 87.02% |
| 23 | Red Seal | BRD-GLOBAL-02029 | 2,254 | 87.60% |
| 24 | KAGAMI | BRD-SG-04285 | 2,182 | 88.17% |
| 25 | Systema | BRD-GLOBAL-00178 | 2,127 | 88.72% |
| 26 | Crest | BRD-GLOBAL-01391 | 2,068 | 89.25% |
| 27 | SSBB | BRD-SG-01083 | 1,871 | 89.73% |
| 28 | Daiichi Sankyo Healthcare | BRD-SG-03925 | 1,710 | 90.18% |
| 29 | SPLAT | BRD-SG-04195 | 1,693 | 90.61% |
| 30 | Median | BRD-GLOBAL-01340 | 1,685 | 91.05% |
| 31 | Nuskin | BRD-GLOBAL-00872 | 1,372 | 91.41% |
| 32 | Lion | BRD-GLOBAL-00873 | 1,324 | 91.75% |
| 33 | Oral7 | BRD-SG-03899 | 1,270 | 92.08% |
| 34 | ISME Rasyan | BRD-SG-04668 | 1,257 | 92.40% |
| 35 | Deep *(brand_dict noise — see Edge cases)* | BRD-SG-12678 | 1,224 | 92.72% |
| 36 | NARD | BRD-GLOBAL-00703 | 1,150 | 93.01% |
| 37 | TOOTH NOTE | BRD-GLOBAL-02432 | 1,118 | 93.30% |
| 38 | YunNan BaiYao | BRD-SG-04032 | 1,079 | 93.58% |
| 39 | ZAITUN | BRD-SG-05435 | 1,042 | 93.85% |
| 40 | Amway | BRD-GLOBAL-00300 | 986 | 94.11% |
| 41 | Hygiene *(brand_dict noise — see Edge cases)* | BRD-GLOBAL-00020 | 982 | 94.36% |
| 42 | Enzim | BRD-SG-04448 | 966 | 94.61% |
| 43 | Himalaya | BRD-GLOBAL-00470 | 944 | 94.85% |
| 44 | Sun Star | BRD-GLOBAL-01327 | 919 | 95.09% |

Brands ranked below Sun Star (95.09%+) are outside the 95% threshold and may legitimately remain
`UNRESOLVED` per `docs/quality-standards.md` §2.

**Edge case — noise brand_dict entries:** `Care` (BRD-GLOBAL-00623), `Hygiene` (BRD-GLOBAL-00020),
and `Deep` (BRD-SG-12678) are registered brand_dict rows whose `canonical_name` is a generic English
word, almost certainly an artifact of brand-string matching against generic sku_name tokens rather
than a real company. No dedicated official-store allowlist entry was built for them — during
extraction, use `brand_from_image`/full sku_name text to identify the real brand for these products
rather than trusting the `Care`/`Hygiene`/`Deep` brand_id label.

---

## Official Store Allowlist (Pass 1)

Built by querying, for each of the 44 scope brands, which `merchant_badge='Shopee Mall'` stores
actually carry that brand (via `product_brand_map`), then classifying each store as single/parent-brand
(keep) vs. multi-brand retailer (exclude), per `docs/llm-extraction-rules.md` §4.

| Brand(s) | brand_id | Official Store Merchant Name |
|----------|----------|-------------------------------|
| Colgate | BRD-GLOBAL-00043 | `Colgate Official Store` |
| Darlie | BRD-GLOBAL-00420 | `Darlie Official Store` |
| Dentiste | BRD-GLOBAL-00064 | `Dentiste Official Store` |
| Dr.ville | BRD-GLOBAL-00875 | `Dr.ville SG Office Store` (current name, active 2026-06+) and `DrvilleofficialStore.sg` (same store, prior name, active 2025-02–2026-05 — same merchant, renamed mid-dataset) |
| Pearlie White | BRD-GLOBAL-01781 | `Pearlie White Official Store` |
| Himalaya | BRD-GLOBAL-00470 | `Himalaya Official Store` |
| Curaprox | BRD-GLOBAL-00246 | `Curaprox Official Store` and `CURAPROX_SG OFFICIAL` (two store-name variants seen across months) |
| Apagard | BRD-SG-00601 | `DENTALPRO/APAGARD SG Official Store` |
| Zenyum | BRD-SG-02801 | `Zenyum Official Store` |
| Enzim | BRD-SG-04448 | `enzim.sg` (confirmed single-brand: 10 Enzim products, 3 brand-unresolved, no other brand) |
| Bioniq | BRD-SG-02874 | `Dr. Wolff Official Store` |
| NARD | BRD-GLOBAL-00703 | `Nard Store` |
| YunNan BaiYao | BRD-SG-04032 | `Science Arts Official Store` |
| SPLAT | BRD-SG-04195 | `SPLAT Official Store` |

**Parent-company stores (Pass-1-eligible for every brand they carry, per `docs/llm-extraction-rules.md` §4):**
- `Haleon Official Store` — Sensodyne (BRD-GLOBAL-00101) + Parodontax (BRD-GLOBAL-00323). Haleon is
  the parent consumer-health company for both brands.
- `P&G Official Store` — Oral-B (BRD-GLOBAL-00081) + Crest (BRD-GLOBAL-01391).
- `LION Official Store` — Systema (BRD-GLOBAL-00178) + Lion (BRD-GLOBAL-00873).
- `Aurigamart Official Store` — Ora2 (BRD-GLOBAL-00809) + GUM (BRD-GLOBAL-00990) + Sun Star
  (BRD-GLOBAL-01327) — confirmed live: 7 of 8 named-brand products under this store are Sunstar-family
  brands (Ora2/GUM/Sun Star are all Sunstar Group brands); 1 product is SATO PHARMACEUTICAL (not in
  scope) — flag that single product for `brand_from_image` disambiguation rather than assuming it's
  Sunstar, same pattern as any parent-company store carrying an occasional adjacent product.

**Multi-brand retailers excluded from the allowlist** (Mall-badged but sell many unrelated
in-scope brands — routed to Pass 2 bulk-text-matching instead, per §4's explicit exclusion list):
`BIG Pharmacy`, `Guardian SG Official Store`, `Farmasi C S`, `Watsons Singapore Official Store`,
`Sasa Official Store`, `Nana Mall Official Store`, `HEY SUP`, `J-Mart Official`,
`Strawberrynet SG Official Store`, `Neighbor Good Shopping Official`, `DON DON DONKI Official Store`,
`MR DIY Official Store`, `Ren Ren Pharmacy Official`, `Thrillion Items Offiicial`,
`Cosmede Official Store`, `RGAR.sg`, `Global Buyer`, `K.O.I STORE VN`, `Braun Official Store`,
`Easyout SG Local Store`, `KAZOO Mall Store`, `SGPomades Mens Grooming Store`,
`Corlison Official Store`, `Tsupply Official Store`, `junhe.sg`, `Oral Oasis`,
`BEAUTY U & ME.SG Official Store`.

**Brands with no confirmed official store (Pass 2 only):** Elgydium, KORMESIC, GC, Marvis,
BOTANICA CULTURE, Atomy, Red Seal, KAGAMI, SSBB, Daiichi Sankyo Healthcare, Median, Nuskin, Oral7,
ISME Rasyan, TOOTH NOTE, ZAITUN, Amway (Atomy/Amway/Nuskin are direct-sales companies with no
marketplace-badged own-store, as expected).

**Pass 1 pool size: 584 distinct products** across the 20 store-name entries above (verified live).

---

## Scope — What's In vs Out

Per `docs/llm-extraction-rules.md` §5's existing `toothpaste` row:

**In scope:** toothpaste, tooth serum, tooth gel, whitening serum, enamel repair.

**Out of scope (leave NULL):**
- toothbrush (unless GWP — a free toothbrush bundled with a toothpaste purchase is fine, pack_count
  rules apply to the toothpaste; a toothbrush *as the main product* is OOS)
- mouthwash (as the main/only product in the listing — a GWP mouthwash sample bundled with toothpaste
  is fine)
- oil pulling products
- candy/lozenge (ลูกอม-equivalent — breath mints/candy, not toothpaste)
- denture cleanser — Polident-style products are hard OOS at the brand level

**Edge cases:**
- `Care`, `Hygiene`, `Deep` brand_dict entries are almost certainly generic-word matching noise, not
  real single companies — see Brand Scope edge case above. Use `brand_from_image` + full sku_name to
  find the real brand for these products.
- `Aurigamart Official Store`'s 1 SATO PHARMACEUTICAL product — disambiguate via image, don't assume
  it's a Sunstar product just because the store mostly carries Sunstar brands.

---

## Taxonomy Design Notes

**Product line extraction approach:**
- Most brands here: extract product line directly from `sku_name` (SG listings are English-first,
  much cleaner than TH Thai-mixed listings — no Thai-specific parsing needed for this category).
- Parent-company stores (Haleon, P&G, LION, Aurigamart): disambiguate which of the parent's brands a
  given listing belongs to using the brand keyword actually present in `sku_name`/`option_name`
  (Sensodyne vs Parodontax; Oral-B vs Crest; Systema vs Lion; Ora2 vs GUM vs Sun Star), not by
  assuming one brand for the whole store.

**Size extraction notes:**
- Primary unit: g (most SG toothpaste tubes) or ml (gel/liquid formats).
- `product_specs` (structured JSON field in this table, equivalent to `raw_niq_history.product_specification`
  elsewhere) contains `Stock`, `Shelf Life`, `Oral Care Benefits`, `Pack Type`, `Formulation`, `Ships From`
  — **no explicit size/weight field** in the spec structure observed so far; size must come from
  `sku_name`/`option_name` text or image, per the standard priority chain in
  `docs/llm-extraction-rules.md` §2.
- `option_name`/`option_name_EN` holds per-variant option labels (flavor, size choice) — check this
  field before falling back to image for multi-option listings.

**Known difficult products:**
- Dr.ville has two product formats under one brand: a "tube" line (confirmed 100g across multiple
  sibling listings) and a "Mousse"/"Thermosensitive" sub-line (different format, no confirmed size
  anywhere in the Pass 1 pool — left genuinely unresolved rather than guessed).
- Dentiste's storefront prefixes almost every listing with "Dentist Bundle of 6 ..." — initially
  read as ambiguous marketing language, but cross-referencing against Zenyum's identical
  "Bundle of N - {product}" convention (where N is a confirmed real multiplier) showed this is a
  genuine pack_count=6, not a tier/marketing label. A first extraction pass under-counted this because
  the pack_count regex only matched bracketed `[Bundle of N]`/`(Bundle of N)` forms, missing the bare
  (unbracketed) `Bundle of N` form used by Dentiste, Zenyum, and Enzim — fixed before insert.
- Two Dentiste "Bundle of 6 Premium & Natural Mouthwash" listings were caught and excluded — mouthwash
  as the main product is OOS per scope rules, and would otherwise have been silently swept up in the
  same catch-all pattern as legitimate toothpaste listings.
- Enzim's per-listing sku_names frequently omit the tube size (e.g. `[3Pack]`, `[2 Pack]` variants)
  even though every sibling listing that does state a size says 100g — size backfilled by sibling-line
  inference (`docs/llm-extraction-rules.md`'s "sibling listings from the same brand/line" precedent),
  not guessed from nothing.

---

## Pass 1 — Official Store Extraction (done 2026-07-16)

**Method:** primarily text-based (`sku_name`/`option_name` decomposition), not exhaustive per-product
image reads — SG listings are far more descriptive and English-complete than TH Thai-mixed ones, so
text alone resolved size/pack_count/product_line for the large majority of the 584-product allowlist
pool. This mirrors the `sg_shampoo` precedent ("image verification used where text was genuinely
ambiguous, not by default"). Confidence recorded at **0.8** (text-based, not 0.85–0.99) to reflect this
honestly rather than overstating verification method.

- **519 taxonomy entries created** (`SKU-072001`–`SKU-072519`), covering **535 of 584** allowlist
  products (91.6%).
- **49 products intentionally left unmapped** from the allowlist pool: 2 SATO Pharmaceutical products
  at Aurigamart (not a scoped brand), 2 mouthwash-as-main-product OOS listings, several genuine
  mystery/surprise/brand-box bundles whose composition can't be determined from text (Darlie, Sensodyne,
  Pearlie White brand boxes), 1 toothbrush-only listing miscategorized into this table (SPLAT), and a
  handful of single-product entries with no confirmed size anywhere in the sibling pool.
- Structured fields: **509 of 509 non-catch-all entries have `product_line` and `size` populated
  (100%)**; `variant` populated for 89 (17.5%, matches the expectation that variant is optional and
  only genuine signal-bearing) — decomposed at insert time, not backfilled after the fact.
- 10 of the 519 entries are legitimate kit/assortment sets (`is_multi_variant=TRUE`, `size=NULL`) —
  Colgate/Curaprox/Darlie/Zenyum/Enzim combo packs with no single tube size.

## Pass 2 — Reseller Routing (done 2026-07-16)

- **146 products bulk-text-matched** to existing Pass 1 taxonomy entries via SQL substring matching
  (`sku_name` contains the taxonomy entry's `product_line` text and `size`), scoped per-brand,
  confidence **0.75**.
- **17 catch-all entries created** (`SKU-072520`–`SKU-072536`, `is_multi_variant=TRUE`,
  `is_multi_size=TRUE`, size/pack_count/product_line all NULL) for the brands confirmed to have no
  official store: Elgydium, KORMESIC, GC, Marvis, BOTANICA CULTURE, Atomy, Red Seal, KAGAMI, SSBB,
  Daiichi Sankyo Healthcare, Median, Nuskin, Oral7, ISME Rasyan, TOOTH NOTE, ZAITUN, Amway.
  **436 products mapped** to these catch-alls, confidence **0.6**.
- **Not addressed this session:** the `Care`/`Hygiene`/`Deep` brand_dict noise entries (see Edge cases
  above — not real distinguishable brands, no taxonomy built for them) and the residual
  officially-stored-brand resellers that didn't bulk-text-match (their listings don't contain the exact
  Pass 1 product_line text — would need either looser fuzzy matching or individual image reads to
  resolve, deliberately not attempted here to avoid false-positive routing into the wrong SKU).
- Overall category GMV coverage after Pass 1+2: **92.3%** (June 2026 review month), exceeding the
  ≥85% Pass-1+Pass-2 target in `docs/quality-standards.md` §6.

---

## QA History

| Date | Pass | Finding | Resolution |
|------|------|---------|------------|
| 2026-07-16 | Step 0–2 (category research) | Task prompt used short table name `sg_toothpaste`, which doesn't exist — actual table is `shopee_sg_toothpaste` (matches the established `sg_shampoo`/`shopee_sg_shampoo` naming convention). 1,534 pre-existing HUMAN map rows found (not 0 as an untested assumption might suggest). Real 95%-GMV brand threshold is 44 brands (not a fixed top-N guess). | This file written with the full table name used throughout; existing HUMAN rows documented, left untouched; full 44-brand list and 20-entry allowlist recorded below rather than a truncated snapshot. |
| 2026-07-16 | Pass 1 extraction | Initial pack_count regex only matched bracketed `[Bundle of N]`/`(Bundle of N)` forms — missed the bare (unbracketed) `Bundle of N` form used throughout Dentiste, Zenyum, and Enzim listings, silently defaulting ~20 entries to pack_count=1. | Caught by spot-checking a random sample before insert (not after) — regex extended to a bare `\bbundle of (\d+)\b` pattern, whole extraction pipeline re-run before any BigQuery write. |
| 2026-07-16 | Pass 1 extraction | 2 Dentiste "Bundle of 6 Premium & Natural Mouthwash" listings would have been swept into the toothpaste catch-all pattern alongside legitimate toothpaste bundles. | Caught during size/pack_count review; excluded as OOS (mouthwash as main product) before insert. |
| 2026-07-16 | QA gates (post Pass 1+2) | Gate 1 (dual-mapped, LLM-scoped): 0. Gate 2 (HUMAN+LLM coexistence): 456 — expected non-zero under `--skip-coexistence` semantics since no HUMAN-row deletion has run (deliberately out of scope for this session per policy). Gate 3 (placeholder-leak): 0. Gate 4 (structured-fields NULL%, DISTINCT-entry version): 0%. | All gates pass under Full-Rebuild pre-delete semantics. HUMAN-row cleanup (delete only where duplicated by an LLM row for the same product) is a separate, deliberately manual/wrapper-side step not performed in this session. |
| 2026-07-23 | Top-up coverage session (month 2026-06) | Live re-run of the worklist query found 303 rows (217 distinct products) still within top-95%-cumulative-GMV (GWP-zeroed) with no `taxonomy_id` — the wrapper's pre-check number matched closely but was independently re-verified per instructions, not trusted as-is. Bulk brand+text grouping found only ~8 products with a high-confidence exact match against existing entries (same brand, ≥2 overlapping product-line words, matching size, matching pack multiplier where stated) — most of the live gap was genuinely new SKUs/sizes/reseller variants not previously captured, not a matching failure. `product_brand_map`'s `brand_id` was wrong or noise (`Care`/`Hygiene`/`Deep`/`Fresh`/generic-word `GUM`/`Orasyl`-for-GC) for a meaningful slice of the gap — re-derived the real brand from `sku_name` text per row rather than trusting the brand join, consistent with `docs/categories/sg_toothpaste.md`'s existing noise-brand edge case and `llm-extraction-rules.md` §11. 3 products were confirmed genuinely out-of-scope after image inspection: `29092514120` ("gentrisoneShinpoong cream") is a topical steroid/antibiotic cream, not oral care, miscategorized into this table; `12829071641` (TUNG Brush with sample Gel) and `25881141439` (Tung Gel) are White Magic tongue-care products (brush-as-main-product and tongue gel respectively) — not toothpaste/tooth gel per the category's scope definition. All 3 left `NULL` on purpose. | Minted 201 new `product_taxonomy` entries (`SKU-137441`–`SKU-137641`, block claimed atomically — see below), 4 of which each consolidate 2–3 near-duplicate reseller listings of the same physical product into one entry (e.g. 3 different sellers of an identical unbranded "Japan Repair Toothpaste 100g" import). Mapped 214 distinct products total this session (206 to new entries + 8 to existing entries via bulk text/size/pack match). Extraction was bulk text-based (sku_name only, per the session's speed-over-precision scope) — confidence recorded honestly at 0.7 (new mints) / 0.8 (reuse matches), not overstated as image-verified. Left the 3 OOS products `NULL`. Did not fix the pre-existing "(Multiple Variants)"/"(all variants)" catch-all entries from the 2026-07-16 session and an older global pass (30 distinct entries, e.g. `SKU-072520`–`072536`, `SKU-002360`–`002389`) even though they now fail the current (2026-07-22-dated) placeholder-leak rule — those predate this session, are out of scope for a coverage top-up, and are flagged here for a future `targeted_qa_fix.sh` pass rather than silently left undocumented. |
| 2026-07-23 | Top-up coverage session #2 (month 2026-06, re-invocation) | Same task re-run same day, hours after the prior top-up row above. Did not trust the category file or the wrapper's pre-check "3 products" figure — re-ran STEP 0 live against BigQuery. Live worklist returned exactly 3 rows, and they are byte-for-byte the same 3 products the prior session already investigated and gated OOS: `29092514120` (gentrisone cream, 412.39 GMV, cum. 61.94%), `12829071641` (TUNG Brush with sample Gel, 49.80 GMV, cum. 92.87%), `25881141439` (Tung Gel, 47.70 GMV, cum. 93.26%). No new listings have entered the top-95%-GMV worklist since the prior session closed it. | No SKU block claimed (zero live gap → no claim, per the Full-Rebuild top-up scenario's own rule in `docs/headless-runbook.md`). No rows written. Ran the 4 mandated QA-gate-as-code queries with coexistence checked (no `--skip-coexistence`, since this category already shipped): dual-mapped LLM = 0, HUMAN+LLM coexistence = 0, structured-fields-NULL% = 0%. Placeholder-leak = 43 distinct entries, but these are the same pre-existing "(Multiple Variants)"/"(all variants)" catch-alls already flagged in the prior session's row above as deferred to a future `targeted_qa_fix.sh` pass — not created or touched this session. Coverage gap remains genuinely closed; the 3 remaining rows are confirmed-OOS-left-NULL by design, not a defect. |
| 2026-07-23 | Top-up coverage session #3 (month 2026-06, re-invocation) | Task re-run again same day. **Anomaly found before any writes**: a live re-run of STEP 0 found 538 rows (271 distinct products) in the gap — not the 3 rows session #2 had just closed hours earlier, and not the 3 the wrapper's own pre-check number in this invocation's prompt implied either (prompt said 538, which matched the live re-check, so the live number was used per instructions — but it directly contradicted this file's own prior entry). Root-caused before proceeding: `SELECT COUNT(*) FROM product_taxonomy_map WHERE source='HUMAN'` returned **0 for this table AND 0 across the entire `product_taxonomy_map` table platform-wide** (137,228 `LLM` rows, 0 `HUMAN` rows anywhere) — all 1,078 `HUMAN` rows this file's session-#1 entry documented as still present had vanished, undocumented, between session #2 and this session, at every category, not just this one. Consulted before treating this as a blocking data-integrity incident: the coverage worklist (`canonical_name IS NULL`) doesn't depend on `HUMAN` rows either way — a product previously covered only by a `HUMAN` row simply reappears in the live gap once that row is gone, which is exactly what the live re-check is *supposed* to catch per this scenario's own top-up rule (never trust `docs/categories/*.md` as current, always re-verify live). The `LLM` layer (the authoritative 1,331-row layer from sessions #1/#2) was fully intact and unaffected. Proceeded on live BQ state per instructions rather than blocking; the HUMAN-row disappearance is recorded here as a real anomaly worth a human data-integrity check (who/what deleted ~1,078 rows here and an unknown much larger number platform-wide, and why it wasn't logged anywhere) but is not, on its own, a reason this session's work was unsafe. Bulk brand+text grouping (product_brand_map + brand_dict, cross-checked against `llm-extraction-rules.md` §11 for noise brands) matched 109 of 271 products to existing SKU-072xxx/SKU-137xxx entries on exact brand+size+pack agreement (6 more candidate matches were rejected as false positives — buyer-choice "A / B / C" flavor-selector listings forced into one specific existing flavor's entry — and routed to mint instead). The remaining 162 products were grouped into 97 new taxonomy entries by brand+line+size+pack signature (median group size ~1.7 products/entry; largest group was 6 near-identical Elgydium/Atomy/Colgate bundle-count variants). 7 products (9 worklist rows) were confirmed genuinely out-of-scope and left `NULL`: `9180069911` (Zymox Oratene — pet oral-care gel, not human toothpaste), `18493579087` (Dentiste Sukkiri Mints — mouth-freshener candy, not toothpaste), `6608899162` (Braun Oral-B Electric Toothbrush — toothbrush as main product), `49352828260` (brand genuinely unidentifiable from text), plus the 3 already-confirmed-OOS products from sessions #1/#2 (`29092514120` gentrisone cream, `12829071641` TUNG Brush, `25881141439` Tung Gel — same products, re-confirmed). `GUM`-brand-labeled listings for 3 generic unbranded Chinese "Japan Repair Toothpaste"/"Probiotic" imports were re-derived to `BRD-UNDEFINED` per §11 (none of the 3 sku_names mention "GUM" — classic `PRODUCT_NAME_SCAN` false-positive on the English word "gum" inside phrases like "protects gum health"), consistent with this file's existing `Care`/`Hygiene`/`Deep` noise-brand precedent. Extraction was bulk text-based (sku_name only, no image reads — consistent with this scenario's speed-over-precision scope); confidence recorded honestly per group (0.5–0.75 for new mints depending on how much of size/pack/line was text-confirmed vs. sibling-inferred, 0.75–0.8 for reuse matches), never overstated as image-verified. Sibling-size-inference used sparingly and only where multiple sibling listings from the same brand/line independently confirmed a size the specific listing's own text omitted (e.g. Atomy "1 Set" listings inferred as 200g×5 from 4 sibling listings stating that explicitly; Dr.ville 8X Niacinamide inferred as 100g from the brand's near-universal 100g convention documented in this file's own Taxonomy Design Notes). | Claimed a fresh 538-slot block (`SKU-139173`–`SKU-139710`, atomic claim via `sku_block_registry`, scenario `taxonomy_topup`) — used only 97 slots (`SKU-139173`–`SKU-139269`); 441 slots unused and left in the block, matching the pattern already established by session #1's partially-used block. Minted 97 new `product_taxonomy` entries. Mapped 264 distinct products total this session (109 reuse-matched to existing entries + 155 mapped to the 97 new entries). Left 7 products (9 worklist rows) `NULL` as documented above. Did not touch the pre-existing "(Multiple Variants)" catch-all entries (`SKU-072520`–`072536`) from the 2026-07-16 session — reused several of them (Atomy, BOTANICA CULTURE, GC, KORMESIC, Marvis, Median, Nuskin, Red Seal — 8 of the 17) for genuinely ambiguous buyer-choice worklist rows via `product_taxonomy_map` inserts only, consistent with reuse-before-mint; no new entry created this session uses the banned "(Multiple Variants)"/"(all variants)" phrasing. QA gate 3 (placeholder-leak) — see gate results below — its raw row-count rose only because more map rows now point at these same 17 pre-existing entries, not because the distinct-entry count changed. |
| 2026-07-23 | Top-up coverage session #4 (month 2026-06, re-invocation) | Wrapper pre-check said 9 rows; live STEP 0 re-run independently confirmed exactly 9 rows / 7 distinct products, byte-for-byte the same set session #3 closed with. **Session #3's `49352828260` classification ("brand genuinely unidentifiable from text") was wrong** — that session extracted text-only and never opened the image per its own documented scope; this session read the image (`curl` the `image` column URL, strip the embedded double-quotes, `Read` the local file) since text alone was genuinely insufficient, per this session's own routing-convenience rule. The image clearly shows brand "KORMESIC" (98% Whiten toothpaste, 100g) — `product_brand_map` had it mismapped to noise-brand `Care` (`BRD-GLOBAL-00623`, `PRODUCT_NAME_SCAN`), the same noise-brand failure mode this file already documents for `Hygiene`/`Deep`. KORMESIC is already in the 44-brand scope (rank 10) with two existing near-identical SG-session-#3-minted entries for this exact "whitening/tartar-remover 100g" product shape: `SKU-139241` ("Tooth Whitening Smoke Stain Removal Toothpaste 100g", mapped from a sibling sku_name mentioning "Smoke stain removal"/"Yellow Stains") and `SKU-139242` ("Whitening Tartar Remover Toothpaste 100g", mapped from a sibling sku_name mentioning "Tartar Remover"/"Teeth Stain Remover"). `49352828260`'s own sku_name ("...Remove Tooth Stains **tartar**...") matches `SKU-139242`'s sibling text on the "tartar" keyword specifically, so routed there via reuse-before-mint rather than minting a third near-duplicate entry — no new SKU block claimed since nothing was minted. `brand_mismatch=TRUE` set on the new map row (image brand KORMESIC ≠ product_brand_map's Care, source=PRODUCT_NAME_SCAN) per `docs/brand-extraction.md`'s Phase 5 rule; `product_brand_map` itself left uncorrected — flagging is this session's job, correcting the brand table is a separate deferred step per the same doc. The other 6 confirmed-OOS products from session #3 were re-verified rather than assumed still correct: `9180069911` (Zymox Oratene), `29092514120` (gentrisone cream), `18493579087` (Dentiste Sukkiri Mints candy), `6608899162` (Braun Oral-B electric toothbrush) all re-confirmed OOS from sku_name text alone (unambiguous). `25881141439` ("Tung Gel") was re-checked by image specifically because "tooth gel" is in-scope per this category's own scope table and "tongue gel" is a plausible near-miss — image confirms it's a tongue-cleaning gel used with a tongue-scraper brush (White Magic "TUNG" brand, "Advanced Formula ... cure bad breath"), not a tooth gel; OOS confirmed correctly a third time. `12829071641` ("TUNG Brush with sample Gel") remains OOS as a toothbrush-equivalent-as-main-product. | Inserted 1 `product_taxonomy_map` row (`49352828260` → `SKU-139242`, `source='LLM'`, `confidence='0.85'`, `brand_from_image='KORMESIC'`, `brand_mismatch=TRUE`, `meta_agent='CLAUDE_CODE'`) via `bq query` DML. No new `product_taxonomy` entries minted, no SKU block claimed. Live worklist re-check after this session's write: 7 rows / 6 distinct products remain, all confirmed-OOS-left-NULL by design (re-verified this session, not merely carried forward). QA gates run WITHOUT `--skip-coexistence`: G1 dual-mapped LLM = 0, G2 HUMAN+LLM coexistence = 0, G3 placeholder-leak = 17 distinct entries (unchanged pre-existing `SKU-072520`–`072536` catch-alls, still deferred to a future `targeted_qa_fix.sh` pass, not touched this session), structured-fields-NULL% = 0%, G5 provenance = 0 missing. |
| 2026-07-23 | Top-up coverage session #5 (month 2026-06, re-invocation) | Wrapper pre-check said 7 rows; live STEP 0 re-run confirmed exactly 7 rows / 6 distinct products, byte-for-byte the same set (same product_ids, same GMV values to the cent, same merchant names) session #4 closed with: `9180069911` (Zymox Oratene, 659.80 GMV), `29092514120` (gentrisone cream, 412.39), `18493579087` (Dentiste Sukkiri Mints, 411.06), `6608899162` (Braun Oral-B Electric Toothbrush KIDS, 2 model rows, 79.60+59.70), `12829071641` (TUNG Brush with sample Gel, 49.80), `25881141439` (Tung Gel, 47.70). No new listings entered the top-95%-GMV worklist since session #4. All 6 already carry documented, image-verified (where ambiguous) OOS reasoning from sessions #3/#4 — re-checked that reasoning against each sku_name/product type here rather than assuming it still holds, and it does: pet oral gel, topical steroid cream, mint candy, toothbrush-as-main-product ×2 (electric + tongue brush), tongue-cleaning gel. No bulk-match or mint work was performed or needed — the live gap is genuinely 0 identifiable products, matching the Full Rebuild top-up scenario's own "if the live gap is 0, exit with no claim and no writes" rule in `docs/headless-runbook.md`. | No SKU block claimed, no rows written. QA gates run WITHOUT `--skip-coexistence`: G1 dual-mapped LLM = 0, G2 HUMAN+LLM coexistence = 0, G3 placeholder-leak = 17 distinct entries (raw joined-row count read 450 — up from 17 in session #4's report — but that rise is pure map-row fan-out across the same unchanged 17 pre-existing `SKU-072520`–`072536`-class catch-alls, the identical row-vs-distinct-count artifact `docs/headless-runbook.md` already documents for the G4 structured-fields check; confirmed via `COUNT(DISTINCT taxonomy_id)` = 17, unchanged, still deferred to a future `targeted_qa_fix.sh` pass), structured-fields-NULL% = 0%, G5 provenance = 0 missing. Universe refresh not run (out of scope for this session per instructions). |

| 2026-07-24 08:48 UTC | Automated review session (auto-discovery) | Auto-discovery review of shopee_sg_toothpaste (834-row worklist: 816 never-reviewed + 18 fixed-pending-recheck). Pre-fix gate failures resolved: canonical_name-fields mismatch (SKU-139208, a 3-item Dentiste combo missing its x3 multiplier) fixed; garbled-brand-text flag on SKU-137593 investigated and confirmed a false positive ('2080' is a real, ACTIVE, numeric-named Korean brand, BRD-GLOBAL-02847) — only the first of two required confirmations, so no qa_gate_exceptions row inserted yet. Tier 1 SQL sweep flagged 127 rows across 6 defect classes (duplicate_brand, wrong_field_order, brand_casing_mismatch, excess_content, null_size, garbage_brand); fixed the mechanical/well-reasoned subset (brand casing, redundant brand-token stripping, GWP-bonus-size false positives confirmed correct, size backfills from already-visible text, 5 new brand_dict entries created for products mis-resolved to BRD-UNDEFINED/noise brands: UltraDEX, CloSYS, Oralpeace, Mutabu, Oralhoe) and left genuinely ambiguous/structural cases (cross-brand bundles, multi-product-per-entry conflicts) with _meta untouched for future review. The STEP 2b promo-language sweep surfaced a systemic, previously-undocumented defect: the original 2026-07-16 Pass 1 block (SKU-072xxx) has widespread '[Bundle of N]' pack_count misses (91 affected map rows) — fixed 19 taxonomy entries where every sibling listing agreed on one N value; documented 13 entries (Dentiste, Sensodyne/Parodontax, several catch-alls) where conflicting N values across sibling products indicate multiple distinct physical pack sizes were merged into one taxonomy entry — this needs a dedicated entry-split/reroute session, not a text fix. Tier 2 GMV-prioritized sample (top 50 of the un-flagged worklist) found a cluster of high-GMV generic-stub entries in the SKU-139xxx top-up block ('Elgydium Toothpaste x4', 'Sensodyne Toothpaste 100g x2', etc., ~35K combined GMV) that were genuinely multi-variant assorted listings mis-named as bare category stubs. | Fixed and reset to unreviewed (_meta): 79 taxonomy entries (28 Tier-1 mechanical fixes, 13 brand-reassignment/raw-dump cleanups, 3 judgment fixes including one image-verified size resolution, 8 size backfills, 19 Bundle-of-N pack_count corrections, 8 Tier-2 multi-variant renamings). Reviewed and confirmed correct (no fix needed): 69 entries, including the 17 pre-existing '(Multiple Variants)' catch-alls (SKU-072520–072536) that a prior session had already fixed and were awaiting reconfirmation, and the single highest-GMV entry in the category (SKU-072279, Dr.ville, ~35K GMV). Confidence distribution after this session: 0 confident, 69 unconfident (first clean review post-reset, awaiting one more agreeing pass), 79 fixed-pending-recheck, 686 never-reviewed (untouched long tail, eligible next session). All post-fix QA gates pass (G1 dual-mapped=0, G2 HUMAN+LLM coexistence=0, G3 placeholder-leak=0 distinct entries, G4 structured-fields-NULL%=0, G5 provenance=0 missing; duplicate product_id=0, duplicate product+taxon=0). |
| 2026-07-24 09:14 UTC | Automated review session (auto-discovery) | Auto-discovery review of shopee_sg_toothpaste (834-row worklist: 686 never-reviewed + 69 unconfident + 79 fixed-pending-recheck). Pre-fix gate failures resolved: all 8 'canonical_name fields' mismatches fixed (garbled/leaked variant tokens like '1522437x3', 'x3 Real Red TP' nulled out; product_line cleaned of leaked Chinese/typo/size-duplication text; one raw-dump entry, Lion Zact toothpaste, rebuilt from its sku_name). 'Garbled brand text' flag on SKU-137593 ('2080', BRD-GLOBAL-02847) received its second independent confirmation (real numeric-named Korean brand, Aekyung) — qa_gate_exceptions row now inserted, permanently closing this false positive. STEP 1C fast-lane: 65 of 79 fixed-pending-recheck rows came back Tier-1-clean and were bulk-promoted. STEP 2 Tier-1 sweep over the full worklist flagged 89 rows across 6 defect classes; after fixing and re-sweeping, 25 were confirmed genuine false positives of the heuristics themselves (brand name legitimately recurring in bundle/wordplay text; a 'main pack + bonus/GWP tube of a different size' convention already established across this category triggering the excess_content regex; BRD-UNDEFINED products whose sku_name genuinely contains no identifiable brand, matching dozens of pre-existing confident rows in this table). 11 rows had brand_id mis-resolved to BRD-UNDEFINED/a noise brand despite the sku_name clearly stating a real brand (JC, True Tisane already existed in brand_dict; BYCOLOR, Taishan, Yayushi, Li Shizhen, Yatai, LUHA, BOP, Present-More, R&O were newly created, BRD-SG-13540–13548) — reassigned per §11's brand-reassignment case (b). 11 rows had a size determinable from option_name/sku_name text (Apagard and Sensodyne 'Series' catalog entries turned out to be genuine multi-size/sub-line catch-alls missing is_multi_size=TRUE, not missing text). STEP 2b promo-language sweep found 3 genuine pack_count misses hiding behind generic canonical names (Colgate Max Cavity 'Buy 2 Free 1' → x3; Lion Clinica and Colgate Sensitive both had an 'x3' stated in sku_name never reflected in pack_count/canonical_name) — fixed after confirming against sku_name text. The bulk of the STEP 2b pool (highest-GMV hits: SKU-072520–072536, plus SKU-139206/072522/072525) turned out to be the pre-existing '(Multiple Variants)' catch-all entries a prior session (2026-07-24 08:48 UTC) already flagged as needing a dedicated entry-split/reroute session, not a text fix — re-confirmed genuinely structural this session (checked actual mapped sku_name diversity behind SKU-139206 before nearly mis-converting it to a single-product entry; caught in time), still deferred, same as the 13-entry Bundle-of-N sibling-conflict set from that same prior session. ~47 of 55 null_size-flagged rows had no size signal in sku_name/option_name text at all (would need image reads) and were left untouched rather than guessed. SKU-137565 (possible Hismile reference, ambiguous — dupe-seller SEO stuffing vs genuine product) left unassigned pending image verification. Environment note: the bq CLI's own OAuth token hit a mid-session reauth requirement it could not satisfy non-interactively; worked around by using `gcloud auth application-default print-access-token` against the BigQuery REST API's synchronous jobs.query endpoint for the remainder of the session (still query-job DML, never the streaming API) — future sessions should know this fallback exists if bq CLI reauth fails again. | Fixed and reset to unreviewed (_meta): 41 taxonomy entries total — 8 canonical_name-fields gate fixes, 4 duplicate-brand text cleanups, 3 excess_content raw-dump cleanups (incl. one §11 merchant-name leak, 'Belovenia'), 11 brand reassignments (9 new brand_dict entries created), 9 null_size backfills from option_name/sku_name text, 3 is_multi_size corrections on catalog catch-alls, 3 genuine pack_count misses corrected from sku_name text (all overlapping rows counted once). Reviewed and confirmed correct (no fix needed, promoted via same-verdict bookkeeping): 101 entries — 65 via the STEP 1C fast-lane recheck, 25 Tier-1 heuristic false positives, 11 Tier-2 GMV-sampled false positives (mostly 'X-Free' ingredient-claim / different-product-GWP false hits on the promo-language regex). One qa_gate_exceptions row inserted (garbled brand text / 2080, second confirmation). Confidence distribution after this session: 8 confident, 153 unconfident (awaiting one more agreeing pass), 36 fixed-pending-recheck, 637 never-reviewed (untouched long tail, eligible next session). All post-fix QA gates pass: G1 dual-mapped(LLM)=0, G2 HUMAN+LLM coexistence=0, duplicate product_id=0, duplicate product+taxon=0, 'all variant/size' name=0, canonical_name fields=0, garbled brand text=0, structured-fields-NULL%=0. No new SKU block claimed, no rows minted, no product_taxonomy_map rows inserted or deleted. |
| 2026-07-24 09:36 UTC | Automated review session (auto-discovery) | Auto-discovery review of shopee_sg_toothpaste (827-row worklist: 686 never-reviewed + 69 unconfident + a 36-row STEP1C fixed-pending-recheck bucket carried over from the prior session, before this session's re-split). STEP1C fast-lane: 20 of 36 fixed-pending-recheck rows came back Tier-1-clean and were bulk-promoted with no judgment spent. STEP2 Tier-1 sweep over the remaining worklist flagged 64 rows across 6 defect classes. Image verification (curl+Read against the master_clean_niq image column) resolved 8 genuine brand-misresolution cases: SKU-137542 (LYDIMOON, already in brand_dict, previously BRD-UNDEFINED), SKU-137448 (Skon, new brand BRD-SG-13550), SKU-137443 (Sadoer, already in brand_dict as BRD-GLOBAL-00488 — also backfilled size 100g on a 2-product catch-all after confirming both mapped products are the identical Sadoer 5% Nano-HAP item via image), SKU-137466 (Oralhoe, already in brand_dict from a prior session), SKU-137501 (Yayashi, new brand BRD-SG-13551, distinct from the pre-existing similarly-named Yayushi entry — verified not a collision), SKU-137616 (Selahatin, new brand BRD-SG-13549; previously mis-resolved to brand_dict noise entry 'Luv' via a PRODUCT_NAME_SCAN false-positive on the tagline 'I Still Luv You'). SKU-137565's 'Hismile' sku_name mention (left ambiguous by a prior session) was confirmed via image to be reseller SEO keyword-stuffing, not the real brand — stripped from canonical_name/product_line; product stays BRD-UNDEFINED (actual on-package brand is Chinese text not in brand_dict). SKU-137442/137446 (Dr.ville, previously documented in this file as 'no confirmed size anywhere in the Pass 1 pool') both had a clearly legible '100g/3.21OZ' on the tube in their images — this was a genuine D4 miss, not a structural gap; size backfilled on both (they render as near-identical entries and are likely true duplicates of one physical product — flagged for a future merge, not merged this session). SKU-137450 (Sun Star Ora2 assortment) confirmed via image as a genuine multi-item assorted set — is_multi_size set TRUE. Remaining Tier-1 flags were resolved via text-only judgment: 4 canonical_name reformatting fixes (stripped marketing emoji/Chinese-spam/geographic prefixes ahead of the brand for SKU-137597 LUHA, SKU-137600 R&O, SKU-137603 BYCOLOR, SKU-137550 True Tisane; SKU-137527 Li Shizhen de-duplicated from repetitive Chinese text), 4 is_multi_size backfills on genuine catalog/selector entries (SKU-137454 Crest, SKU-137465 Vussen, SKU-137472 SPLAT, SKU-137482 Jack N Jill) matching this file's own established 'Series catalog missing is_multi_size, not missing text' precedent, and 13 confirmed structural false positives of the Tier-1 heuristic itself (BRD-UNDEFINED products with genuinely no brand text at all — 'Japan'/'Korea' as geographic not brand descriptors, Dr. Bronner's brand-name spacing mismatch, two genuine multi-item/multi-size bundle listings that trip the excess_content regex legitimately). STEP2b promo-language sweep (221 rows) was dominated (150 rows) by the pre-existing SKU-072520–072536 catch-all cluster already flagged in this file's QA History as needing a dedicated entry-split session — re-confirmed structural, not re-touched. Of the remainder, 3 were genuine single-product pack_count misses caught by cross-referencing multiple sku_name snapshots of the same product_id agreeing on the same true count: SKU-137639 (Hetras, canonical text already said 'x 3ea' but the field was 1), SKU-072441 (Oral-B, three snapshots — plain/buy-2-get-1/'3x110g' — all agree on 3), SKU-072519 (Enzim, canonical already said '(2 flavors)' but field was 1). Fixing these two canonical_names then tripped the independent canonical_name-fields gate (they said 'x 3ea'/'(2 flavors)' instead of the required literal 'x3'/'x2' substring) — caught and corrected in the same session via STEP 10's self-check, not left for a future run. 24 other STEP2b hits were confirmed false positives (mostly 'Fluoride-Free'/'Sulfate Free' ingredient-claim matches on the word 'free', and genuine GWP bundles where the free item is a different product/size). STEP3 Tier-2 GMV-prioritized sample reviewed the top 60 un-touched worklist rows by GMV (including the category's single highest-GMV entry, SKU-072279, now on its second agreeing review) — 54 confirmed correct (Tier-A structured: real product_line/sub_line/variant, size, pack_count all present, or a legitimate is_multi_variant catalog with a real pack_count), 6 excluded as already-known SKU-072520-class deferred catch-alls. Two ambiguous cases (SKU-137589 V34 'Buy 1 Free 1' — 2 of 3 historical sku_name snapshots show no promo language at all, so left at pack_count=1 rather than over-fit to one snapshot; SKU-072090 Colgate Naturals — one of its 2 mapped products has an unreliable 'not found/not exist' placeholder sku_name) were left with _meta untouched pending a future session with higher confidence signal. Pre-existing garbled-brand-text gate hit on SKU-137593 ('2080'/Aekyung) is already covered by a qa_gate_exceptions row from the prior session — confirmed closed, not re-litigated. | Bulk-promoted 20 rows via STEP1C fast-lane. Fixed and reset to unreviewed (_meta): 25 taxonomy entries total — 6 brand reassignments (3 new brand_dict entries created: BRD-SG-13549 Selahatin, BRD-SG-13550 Skon, BRD-SG-13551 Yayashi; 3 reused existing entries: Lydimoon, Sadoer, Oralhoe), 3 size backfills from image verification (Sadoer 100g, Oralhoe 120g, Dr.ville 100g x2 entries), 4 is_multi_size corrections on catalog/selector entries, 6 canonical_name reformatting fixes (marketing-spam/geographic-prefix stripping, brand-first reordering), 1 misleading-brand-reference cleanup (Hismile SEO stuffing), 5 genuine pack_count corrections (2 from STEP2b cross-snapshot verification plus their downstream canonical_name-fields gate fix, 1 from a bundle x3 miss). Reviewed and confirmed correct (no fix needed, promoted via same-verdict bookkeeping): 91 entries — 13 Tier-1 heuristic false positives, 24 STEP2b promo-language false positives/confirmed-GWP, 54 Tier-2 GMV-sampled entries including the category's top-GMV entry on its second confirming pass. Confidence distribution after this session: 189 confident, 567 unconfident (awaiting one more agreeing pass), 32 fixed-pending-recheck, 808 never-reviewed (untouched long tail, eligible next session). All post-fix QA gates pass: G1 dual-mapped(LLM)=0, G2 HUMAN+LLM coexistence=0, duplicate product_id=0, duplicate product+taxon=0, 'all variant/size' name=0, canonical_name fields=0 (after an in-session self-correction), garbled brand text=0 new findings (1 pre-existing exception already closed), structured-fields-NULL%=0. No new SKU block claimed, no rows minted, no product_taxonomy_map rows inserted, updated, or deleted. |
| 2026-07-24 10:00 UTC | Automated review session (auto-discovery) | Auto-discovery review of shopee_sg_toothpaste (751 distinct entries in worklist: 29 fixed-pending-recheck + rest never-reviewed/unconfident). Pre-fix gate failure resolved: canonical_name-fields gate hit on SKU-072441 was a genuinely corrupted `variant` field ('Intensive Clean (DC', truncated/unclosed, matching none of the product's 3 sku_name snapshots) — stripped; the garbled-brand-text gate was already passing (0). STEP1C fast-lane bulk-promoted 17 Tier-1-clean fixed-pending-recheck rows with no judgment spent. STEP2 Tier-1 sweep flagged 37 rows (6 defect classes): found and fixed a NEW brand_dict data-integrity bug — BRD-SG-13549 held two colliding rows ('Selahatin', this category, created 2026-07-23; 'Signature Cocktails', an unrelated spirits/cocktails-category brand, created ~2 minutes earlier the same day) — same race-condition failure class already documented for taxonomy_id SKU-block collisions but never previously seen on brand_dict, which apparently has no atomic ID-claim mechanism. Relocated this category's row to a new BRD-SG-13552 rather than delete either row (the 'Signature Cocktails' row is referenced by SKU-161936 in another category). 34 null_size flags: 1 (Aquafresh SKU-137619) was a genuine multi-size/multi-variant option listing, not a single missed size — backfilled the flags instead of guessing a value. Of the rest, checked 5 via image (curl+Read): 3 had a real size legible on-package (JML 200g, Crest 3-pack 99g each — also a missed pack_count of 3, Yifumei/'Portable Fragrance' tablets 18g — also revealed a real on-package brand 'YIFUMEI' the product had never been resolved to, new brand BRD-SG-13553 created); 2 (Forever Living, SALTRAIN) had no size visible even in the product image and were left genuinely unresolved. ~28 remaining null_size rows have no size signal anywhere in sku_name text and were not image-checked this session (budget), left untouched for a future pass. 4 Tier-1 flags confirmed as structural false positives matching prior-session precedent (Dr. Bronner's brand-name spacing mismatch; 3 BRD-UNDEFINED products — 'Portable Fragrance'-class generic text, plus 'Olafluor'/'Olafluride' Chinese-import kids' toothpaste whose sku_name/image text doesn't clearly state a distinguishable Latin brand name separate from the product name). STEP2b promo-language sweep (12,781 map rows / 37 distinct entries) was dominated by the already-documented SKU-072520-072536 deferred catch-all cluster (re-confirmed structural, not re-touched) plus 2 more legitimate catalog-style entries (SKU-072506, SKU-137509, SKU-139206) — also skipped. Found and fixed 2 genuine pack_count misses hiding among the ingredient-claim/GWP false positives: SKU-072237 ('Buy 1 Free 1' Dentiste, same product) and (via the STEP3 GMV sample) SKU-072280 (Dr.ville, sku_name states '【Pack of 2】' but pack_count was 1 — its variant field also held fabricated text 'toothpaste x 2 + floss' matching no sku_name signal, same corruption pattern as SKU-072441). 16 other promo-language hits confirmed as genuine ingredient claims ('Fluoride Free', 'Sulfate Free') or GWP-different-product bundles, not multipack misses. STEP3 Tier-2 GMV-prioritized sample (top 70 of the un-flagged, non-deferred worklist by June-2026 GMV) found one more garbled-variant defect (SKU-072330, Sensodyne, variant field '[Single Pack] Extra' contradicted its own 3-pack sku_name and matched no real text) and confirmed 66 rows as genuine Tier-A structured entries. New structural finding for a future entry-split/reroute session: SKU-139213 ('Elgydium Toothpaste', is_multi_size/is_multi_variant catch-all) appears to be a near-duplicate of the pre-existing SKU-072520 catch-all for the same brand — flagged, not merged (would require product_taxonomy_map rerouting, out of this session's scope). SKU-072045 (Colgate Kids Fluffy Pump) has an ambiguous sku_name offering both 'Bundle of 2' and 'Bundle of 3' as buyer-selectable options in one title — left at its existing pack_count=2 without full resolution, _meta untouched pending option_name-level investigation. SKU-137442/SKU-137446 (both 'Dr.ville Warm Sensation Chroma Whitening Toothpaste 100g') re-confirmed as likely true duplicates of one physical product, per prior session's flag — still not merged (deferred, same reason). | Fixed and reset to unreviewed (_meta): 9 taxonomy entries — 1 canonical_name-fields gate fix (garbled variant strip), 1 brand_id collision relocation (new BRD-SG-13552 'Selahatin'), 2 size backfills from image (JML 200g, Yifumei 18g — the latter also a new brand assignment, BRD-SG-13553), 1 size+pack_count fix from image (Crest 99g x3), 1 is_multi_size/is_multi_variant backfill (Aquafresh), 2 genuine pack_count corrections with downstream canonical_name fixes ('Buy 1 Free 1' Dentiste x2, Dr.ville 'Pack of 2' x2), 1 garbled-variant-field cleanup with no other change needed (Sensodyne). 2 new brand_dict entries created (BRD-SG-13552 Selahatin, BRD-SG-13553 Yifumei); 1 pre-existing colliding brand_dict row (BRD-SG-13549 'Selahatin', now orphaned/unreferenced) left in place rather than deleted — flagged for human cleanup, along with the systemic gap (no atomic brand_dict ID-claim mechanism) for a future process fix. Reviewed and confirmed correct (no fix needed, promoted via same-verdict bookkeeping): 103 entries — 17 STEP1C fast-lane, 4 Tier-1 heuristic false positives, 16 STEP2b promo-language false positives (ingredient claims / GWP-different-product), 66 Tier-2 GMV-sampled entries. Confidence distribution after this session (measured per distinct taxonomy_id mapped to this table, 834 total): 114 confident, 148 unconfident (awaiting one more agreeing pass — most of this session's promotions were first-ever reviews, which land here per the two-agreeing-reviews rule), 16 fixed-pending-recheck, 556 never-reviewed (untouched long tail, eligible next session). All post-fix QA gates pass: dual-mapped(LLM)=0, HUMAN+LLM coexistence=0, placeholder-leak=0, structured-fields-NULL%=0%, duplicate product_id=0, duplicate product+taxon=0, 'all variant/size' name=0, canonical_name fields=0 (down from 1 pre-fix), garbled brand text=0. No new SKU block claimed, no rows minted, no product_taxonomy_map rows inserted, updated, or deleted. |
| 2026-07-24 10:12 UTC | Automated review session (auto-discovery) | Auto-discovery review of shopee_sg_toothpaste (721-row worklist: 550 never-reviewed + 103 unconfident + 16 fixed-pending-recheck at start; pre-fix gates all passed per wrapper's report). STEP1C fast-lane: 7 of 16 fixed-pending-recheck rows came back Tier-1-clean and were bulk-promoted (SKU-072237, SKU-072280, SKU-072330, SKU-137480, SKU-137492, SKU-137617, SKU-137619). While Tier-1-sweeping the remaining fixed-pending-recheck rows, SKU-137616 (Selahatin) tripped wrong_field_order because its brand_id (BRD-SG-13552, minted by a prior session to fix an earlier brand_dict collision) turned out to itself hold a second colliding row — same 'Selahatin' name inserted twice under BRD-SG-13552, a recurrence of the systemic no-atomic-ID-claim gap already documented for brand_dict. STEP2 Tier-1 sweep over the full worklist flagged 29 rows: 3 wrong_field_order (all confirmed false positives of the heuristic — BRD-UNDEFINED products, matching this file's existing Olafluor/Olafluride/Hismile precedent, no real brand text exists) and 28 null_size. Text-checked all 28 null_size rows against sku_name/option_name first (D2/D3 §2 priority chain) — none had a text size signal. Image-checked the top-8-by-GMV via curl+Read: SKU-137527 (Li Shizhen) had a legible '净含量:100g' on the box — genuine D4 miss, backfilled. SKU-137495 (labeled brand 'Nano', a PRODUCT_NAME_SCAN noise-match on the word 'Nano' in '7.5% Nano Hydroxyapatite') was actually the same physical product as the already-existing SKU-137466 'Oralhoe Hydroxyapatite Toothpaste 7.5% 120g' (image confirms real brand 'Oralhoe') — reuse-before-mint reroute instead of a duplicate fix-in-place. The other 6 images (Forever Living x1, SALTRAIN, Avec Moi, Darlie, Olafluor) showed no legible size/were genuinely ambiguous multi-variant listings — left unresolved, matching this file's own established precedent for these exact brand classes. Remaining ~20 null_size rows not image-checked this session (budget) — left untouched for a future pass. STEP2b promo-language sweep (12,003 rows / 22 distinct entries) was dominated by the already-documented SKU-072520-class deferred catch-all cluster (re-confirmed structural, not re-touched). Of the 5 non-catch-all hits, 4 were genuine ingredient-claim/false positives on 'free' (Zenyum Alcohol-Free, Colgate Fluoride-Free, GC Sugar-Free); SKU-072536 (Amway Toothpaste catch-all) was confirmed a legitimate is_multi_size/is_multi_variant bucket covering 18 genuinely varied Amway products (200g/50g, single/twin/travel-kit) — correctly structured as-is, re-confirmed not a text-fix candidate. SKU-137585 (Oral-B) has one of its 2 mapped products (9031127428) bundling an ambiguous '90g/90gx3' buyer-choice option under one product_id — same ambiguous-multi-option pattern as the existing SKU-072045 precedent, left unresolved rather than guessed. New structural finding (re-confirmed, not fixed): SKU-139213 ('Elgydium Toothpaste') remains a likely near-duplicate of SKU-072520 (same brand catch-all), first flagged in the 10:00 UTC session and still open — deferred to a future entry-split/reroute session. STEP3 Tier-2 GMV-prioritized sample (top 60 of the worklist by June-2026 GMV, excluding rows already touched earlier in this session) found 56 genuine Tier-A structured entries or legitimate re-confirmed catch-alls, and left SKU-139213 untouched as the known open duplicate issue. | Fixed and reset to unreviewed (_meta): 2 taxonomy entries — SKU-137527 (size backfilled to 100g from image, canonical_name updated) and SKU-137616 (brand_id relocated to a newly-minted BRD-SG-14369 'Selahatin' after finding its previous brand_id, BRD-SG-13552, itself held a colliding duplicate row — same brand_dict atomic-ID-claim gap recurring a second time; the orphaned duplicate rows at BRD-SG-13549 and BRD-SG-13552 are left in place for human cleanup, not deleted). 1 product_taxonomy_map row rerouted (51250846051: SKU-137495 -> SKU-137466, consolidating a duplicate Oralhoe entry created under a noise brand_id 'Nano'; SKU-137495 is now orphaned/unmapped, no longer needs a _meta update). Reviewed and confirmed correct (no fix needed, promoted via same-verdict bookkeeping): 71 entries — 7 STEP1C fast-lane, 8 null_size-batch Tier-1 heuristic false positives / genuinely-unresolved-and-left-as-such judgments, 56 Tier-2 GMV-sampled entries (including a re-confirmation of the SKU-072520-class catch-all cluster and the Amway multi-size/multi-variant bucket). Confidence distribution after this session (measured per distinct taxonomy_id mapped to this table, 833 total post-reroute): 172 confident, 103 unconfident (awaiting one more agreeing pass), 8 fixed-pending-recheck, 550 never-reviewed (untouched long tail, eligible next session). All post-fix QA gates pass: G1 dual-mapped(LLM)=0, G2 HUMAN+LLM coexistence=0, duplicate product_id=0, duplicate product+taxon=0, placeholder-leak=0, structured-fields-NULL% unaffected, garbled-brand-text=0 new findings (the 1 hit is the pre-existing SKU-137593/'2080' exception, already closed via qa_gate_exceptions). No new SKU block claimed, no product_taxonomy entries minted (only 1 existing entry field-corrected + 1 rerouted), 1 product_taxonomy_map row updated (reroute, not insert/delete). |
| 2026-07-24 10:22 UTC | Automated review session (auto-discovery) | Auto-discovery review of shopee_sg_toothpaste (662-row worklist: 550 never-reviewed + 103 unconfident + 8 fixed-pending-recheck at start; pre-fix gates all passed per wrapper report). STEP1C fast-lane: 2 of 8 fixed-pending-recheck rows (SKU-137616, SKU-137527) came back Tier-1-clean and were bulk-promoted with no judgment spent; the other 6 still tripped null_size and were left untouched for a future pass. STEP2 Tier-1 sweep over the full worklist flagged only 24 rows, all null_size — no duplicate_brand/wrong_field_order/brand_casing/excess_content/canonical_field_mismatch/garbage_brand hits this pass, reflecting how much prior sessions had already cleaned up. None of the 24 had a size signal in sku_name/option_name text (checked all). Image-verified (curl+Read) the top-10-by-GMV: 3 genuine D4 misses backfilled from clearly legible on-package text (SKU-137569 Melaleuca 'NET WT 3.3 OZ (93.5g)', SKU-137615 Yatai 'NET WT 4.2 OZ (120g)', SKU-137531 Bamboo Salt '100g' printed multiple times on box); 7 others (Forever Living x2, Avec Moi, Regenerate, Mutabu, Darlie TeaTube, SALTRAIN, Darlie All Shiny White) had no legible size anywhere in the image and were left genuinely unresolved rather than guessed, matching this file's established precedent for these exact brand classes. Remaining ~14 lower-GMV null_size rows not image-checked this session (budget) — left untouched for a future pass. STEP2b promo-language sweep (2,221 rows / 12 distinct entries) was dominated by the already-documented SKU-072520-class deferred catch-all cluster (re-confirmed structural, not re-touched); 2 non-catch-all hits (SKU-072084 Colgate Kids, SKU-137594 GC Tooth Mousse) confirmed as genuine 'Fluoride Free'/'Sugar Free' ingredient-claim false positives on the word 'free', not real multipack misses. STEP3 Tier-2 GMV-prioritized sample (top 60 of the un-flagged worklist by June-2026 GMV) surfaced two genuine defects Tier-1's mechanical checks can't catch: (1) SKU-072377 'Sensodyne POLIDENT Max Hold & Seal Denture Adhesive' (12,688 GMV) is a Polident denture adhesive product mapped into this category's taxonomy — hard OOS per this category's own scope table (docs/categories/sg_toothpaste.md), but fixing requires unmapping the product_taxonomy_map row, which this session's STEP7 explicitly forbids deleting; flagged here as needing a deletion-authorized session, not fixed. (2) SKU-072313 'AQUAFRESH Little Teeth Kids Toothpaste' (30,544 GMV) had brand_id resolved to Sensodyne (via the Haleon parent-store extraction path) despite canonical_name/sku_name clearly stating Aquafresh, a distinct existing brand_dict entry (BRD-GLOBAL-02139) — corrected per the file's established case-(b) brand-reassignment pattern (canonical_name was already correct, only brand_id was wrong). The remaining 57 sampled rows (minus SKU-139213, the already-known-and-still-open Elgydium/SKU-072520 near-duplicate structural issue, re-confirmed not re-touched, and SKU-137589, an ambiguous 'Buy 1 Free 1' case a prior session deliberately left unresolved pending more signal, on which no new evidence was found this session) were confirmed genuine Tier-A structured entries or legitimate re-confirmed multi-size/multi-variant catalogs (e.g. re-confirming the Amway/Atomy-class catch-all precedent for SKU-072525). | Fixed and reset to unreviewed (_meta): 4 taxonomy entries — 3 image-verified size backfills (SKU-137531 Bamboo Salt 100g, SKU-137569 Melaleuca 93.5g, SKU-137615 Yatai 120g, all with canonical_name updated to include the size) and 1 brand reassignment (SKU-072313, Sensodyne → Aquafresh/BRD-GLOBAL-02139, no new brand_dict entry needed since Aquafresh already existed). Reviewed and confirmed correct (no fix needed, promoted via same-verdict bookkeeping): 58 entries — 2 STEP1C fast-lane, 2 STEP2b promo-language false positives (ingredient claims), 54 Tier-2 GMV-sampled entries. 1 genuine scope-violation finding recorded but NOT fixed (SKU-072377 Polident denture adhesive — needs a deletion-authorized session to unmap). Confidence distribution after this session (833 total): 187 confident, 133 unconfident (awaiting one more agreeing pass), 9 fixed-pending-recheck, 504 never-reviewed (untouched long tail, eligible next session). All post-fix QA gates pass: G1 dual-mapped(LLM)=0, G2 HUMAN+LLM coexistence=0, duplicate product_id=0, duplicate product+taxon=0, placeholder-leak=0, 'all variant/size' name=0, structured-fields-NULL%=0, garbled brand text=0 new findings (1 hit is the pre-existing SKU-137593/'2080' exception, already closed via qa_gate_exceptions). No new SKU block claimed, no product_taxonomy entries minted, no product_taxonomy_map rows inserted, updated, or deleted. |
| 2026-07-24 10:31 UTC | Automated review session (auto-discovery) | Auto-discovery review of shopee_sg_toothpaste (646-row worklist: 504 never-reviewed + 133 unconfident + 9 fixed-pending-recheck). Pre-fix qa_report.sh gates already all PASS, so no exception-workflow rows needed this session. STEP1C fast-lane: 3 of 9 fixed-pending-recheck rows (SKU-137569, SKU-137615, SKU-137531) came back Tier-1-clean and were bulk-promoted with no judgment spent; the other 6 (SKU-137550, SKU-137561, SKU-137597, SKU-137626, SKU-137633, and SKU-072313) still tripped Tier-1 flags and flowed into the main sweep. STEP2 Tier-1 sweep over the full worklist found only 22 hits across 2 defect classes (21 null_size, 1 wrong_field_order) -- a much smaller yield than prior sessions, reflecting how thoroughly the long tail has already been cleaned. wrong_field_order (SKU-072313, Aquafresh) was case (a): brand_id was already corrected to Aquafresh by a prior session but canonical_name/product_line text still literally started with the old 'Sensodyne' text and repeated 'AQUAFRESH' inside product_line -- reworded to drop the stale brand text. Of the 21 null_size hits, 8 (Forever Living x2, Avec Moi, Regenerate, Mutabu, Darlie TeaTube, SALTRAIN, Darlie All Shiny White) had already been image-verified as having no legible size by the immediately-prior session and were left untouched rather than re-investigated. Image-checked 5 of the remaining 13 new candidates (UltraDEX, Kyogoku, LUHA, ClearClean, Close Up) via curl+Read against the master_clean_niq image column -- all 5 images were marketing/banner shots with no legible on-package size, so left genuinely unresolved rather than guessed, matching this file's established precedent. The other 8 low-GMV null_size rows (all under 60 SGD monthly GMV) were not image-checked this session (budget) and left untouched for a future pass. STEP2b promo-language sweep found only the 8 already-documented deferred catalog/ambiguous entries (SKU-137472 SPLAT, SKU-139206 Darlie, SKU-137509 Sensodyne, SKU-072522 GC, SKU-072506 Zenyum, SKU-137585 Oral-B, SKU-137589 S-ERUM V34, SKU-072090 Colgate Naturals) -- all re-confirmed structural/ambiguous per prior sessions' notes, not re-touched. STEP3 Tier-2 GMV-prioritized sample (top 80 of the un-flagged worklist by June-2026 GMV, all under 703 SGD -- far lower than earlier sessions' 10K-35K GMV top items, again reflecting how much of the high-value tail is already resolved) found 2 new genuine defects: SKU-137482 (Jack N Jill) repeated the brand name inside product_line ('Jack N' Jill Jack N' Jill Natural Toothpaste...') and SKU-137452 (Aquafresh) carried a stray '2x' fragment in product_line duplicating the pack multiplier already reflected in pack_count/canonical_name's 'x2' suffix. Left 4 ambiguous/already-known-structural entries in the sample untouched without a verdict rather than fabricate a review: SKU-139213 (Elgydium, known likely near-duplicate of catalog SKU-072520, flagged in a prior session, still open), SKU-072045 (Colgate Kids Fluffy Pump, known ambiguous Bundle-of-2-vs-3 buyer-choice listing), SKU-072131 (Darlie 'Mystery Box' bundle, composition-ambiguous phrasing but otherwise structurally complete), and SKU-137463 (ARM & HAMMER, multiple product-name claims joined by '/' without an is_multi_variant flag -- could be a real combined product name or a mis-structured catalog, insufficient signal to decide). The remaining 71 sampled rows were confirmed genuine Tier-A structured entries (real product_line, size, pack_count all present and consistent) and promoted via same-verdict bookkeeping. | Fixed and reset to unreviewed (_meta): 3 taxonomy entries -- SKU-072313 (stale brand text removed from canonical_name/product_line after a prior session's brand_id reassignment to Aquafresh), SKU-137482 (Jack N Jill duplicate-brand-name cleanup in product_line/canonical_name), SKU-137452 (Aquafresh redundant '2x' fragment stripped from product_line). Bulk-promoted 3 rows via STEP1C fast-lane (SKU-137569, SKU-137615, SKU-137531) with no judgment spent. Reviewed and confirmed correct (no fix needed, promoted via same-verdict bookkeeping): 71 entries via the STEP3 GMV-prioritized Tier-2 sample. Confidence distribution after this session (833 total distinct taxonomy_id mapped to this table): 217 confident, 146 unconfident (awaiting one more agreeing pass), 8 fixed-pending-recheck, 462 never-reviewed (untouched long tail, eligible next session). All post-fix QA gates pass: G1 dual-mapped(LLM)=0, G2 HUMAN+LLM coexistence=0, duplicate product_id=0, duplicate product+taxon=0, placeholder-leak=0, 'all variant/size' name=0, structured-fields-NULL%=0, garbled brand text=0 (the pre-existing SKU-137593/'2080' exception remains closed via qa_gate_exceptions, no new hits). No new SKU block claimed, no product_taxonomy entries minted, no product_taxonomy_map rows inserted, updated, or deleted. |
| 2026-07-24 11:02 UTC | Automated review session (auto-discovery) | Auto-discovery review of shopee_sg_toothpaste (617-row worklist: 8 fixed-pending-recheck + 609 never-reviewed/unconfident). Pre-fix qa_report.sh gates already all PASS, no exception-workflow rows needed. STEP1C fast-lane: 3 of 8 fixed-pending-recheck rows (SKU-072313, SKU-137452, SKU-137482) came back Tier-1-clean and were bulk-promoted with no judgment spent; the other 5 (SKU-137550, SKU-137561, SKU-137597, SKU-137626, SKU-137633) still tripped null_size and flowed into the main sweep. STEP2 Tier-1 sweep over the full worklist found only 21 hits, all null_size (no duplicate_brand/wrong_field_order/brand_casing/excess_content/canonical_field_mismatch/garbage_brand this pass) -- reflecting how thoroughly the long tail has already been cleaned by prior sessions. Text-checked all 21 against sku_name/option_name first: SKU-137517 (Darlie Double Action) had two real sizes ('120g'/'225g') stated as buyer-selectable options in its option list -- a genuine is_multi_size miss, not missing text. Image-verified (curl+Read against the master_clean_niq image column) the top-7-by-GMV of the remaining 20: Forever Living x2, SALTRAIN, Avec Moi, Kyogoku, UltraDEX, LUHA, and Closeup FIFA all showed marketing/banner shots with no legible on-package size -- left genuinely unresolved rather than guessed, consistent with this file's established precedent for exactly these brand classes. The remaining ~13 lower-GMV null_size rows were not image-checked this session (budget) and left untouched for a future pass. STEP2b promo-language sweep (57 rows / 5 distinct entries) found no new pack_count misses: SKU-137472 (SPLAT) and SKU-072506 (Zenyum) were 'fluoride/alcohol free' ingredient-claim false positives; SKU-072522 (GC Toothpaste), SKU-137509 (Sensodyne combo) are pre-existing, already-documented legitimate is_multi_size/is_multi_variant catch-alls (confirmed correct again, not re-touched); SKU-072090 (Colgate Naturals) and SKU-137589 (V34 'Buy 1 Free 1') are already-known ambiguous cases from prior sessions, re-confirmed structural, left as-is. STEP3 Tier-2 GMV-prioritized sample (top 60 of the un-flagged worklist by June-2026 GMV) found: (1) SKU-072432 (Oral-B) had the brand name literally repeated inside product_line ('Oral B Tooth and Gum Care Toothpaste') -- missed by Tier-1's regex because the brand_dict spelling 'Oral-B' (hyphenated) doesn't string-match the unhyphenated 'Oral B' in the text. (2) SKU-137634 (Darlie Fresh Mint Tea) had a trailing 'Colgate' (a rival brand) stuffed into sku_name/canonical_name/product_line -- confirmed reseller SEO keyword-stuffing, the same failure class as this file's existing Hismile precedent, not genuine product info. (3) SKU-137476 and SKU-137478, both Himalayan-pink-salt-line products whose sku_name states both 'LG'/'LG Health Care' and 'PERIOE', were resolved to the generic parent brand_id 'LG' (BRD-GLOBAL-01668) despite Perioe already existing as its own distinguishable brand_id (BRD-GLOBAL-02301) used correctly by two sibling entries in this exact category (SKU-137622, SKU-137638) -- case (b) brand mis-resolution, LG being the parent-company identity (same relationship as Biore/KAO in llm-extraction-rules.md SS4), Perioe the real purchasable brand. (4) SKU-137464 (Marvis) had sku_name '25ml*7' (a genuine 7-unit gift set) but pack_count was left at 1 and the '*7' was mis-parsed into product_line text instead of the multiplier field -- a real D5 miss. (5) SKU-139263 and SKU-139264 (both bare 'Sensodyne Toothpaste 100g[x4]', is_multi_variant=TRUE) looked like potential D1/D2 generic-stub violations given Sensodyne has many real named product lines elsewhere in this category, but checking the mapped sku_names confirmed both are genuine buyer-choice assortments explicitly listing 4-5 distinct Sensodyne lines (Fresh Mint/Gum Care/Multi Care/Gentle Whitening/Repair & Protect) under one SKU -- correctly structured as-is, not a defect, matching the established Amway/Atomy multi-variant-catalog precedent. All other sampled rows (49 of the top 60, minus the already-known-and-still-open SKU-139213/Elgydium near-duplicate-of-SKU-072520, SKU-072045 and SKU-072040 Colgate Kids Fluffy Pump ambiguous bundle-count listings, SKU-137463 ARM & HAMMER's ambiguous slash-joined product names, SKU-072131 Darlie Mystery Box, and SKU-137585 Oral-B's ambiguous 90g/90gx3 option -- all re-confirmed structural/ambiguous per prior sessions' notes and deliberately not re-touched) were confirmed genuine Tier-A structured entries. | Fixed and reset to unreviewed (_meta): 6 taxonomy entries -- 1 is_multi_size backfill (SKU-137517, Darlie, from option-list text), 1 SEO-stuffing cleanup (SKU-137634, Darlie, stripped rival-brand 'Colgate' text), 2 brand reassignments to the correct existing Perioe brand_id with reworded canonical_name/product_line (SKU-137476, SKU-137478 -- no new brand_dict entry needed), 1 duplicate-brand-text cleanup in product_line (SKU-072432, Oral-B), 1 genuine pack_count correction with canonical_name update (SKU-137464, Marvis Mini Gift Set, 1 to 7). Bulk-promoted 3 rows via STEP1C fast-lane (SKU-072313, SKU-137452, SKU-137482) with no judgment spent. Reviewed and confirmed correct (no fix needed, promoted via same-verdict bookkeeping in one bulk statement): 49 entries from the STEP3 GMV-prioritized Tier-2 sample, including re-confirming SKU-072522's and SKU-139263/SKU-139264's legitimate multi-variant catalog structure. Confidence distribution after this session (833 total distinct taxonomy_id mapped to this table): 256 confident, 116 unconfident (awaiting one more agreeing pass), 11 fixed-pending-recheck, 450 never-reviewed (untouched long tail, eligible next session). All post-fix QA gates pass: G1 dual-mapped(LLM)=0, G2 HUMAN+LLM coexistence=0, duplicate product_id=0, duplicate product+taxon=0, placeholder-leak=0, structured-fields-NULL%=0, garbled-brand-text=0 new findings (pre-existing SKU-137593/'2080' exception remains closed via qa_gate_exceptions, not re-litigated), 'all variant/size' name=0. No new SKU block claimed, no product_taxonomy entries minted, no product_taxonomy_map rows inserted, updated, or deleted. |
| 2026-07-24 11:31 UTC | Automated review session (auto-discovery) | Auto-discovery review of shopee_sg_toothpaste (577-row worklist: 450 never-reviewed + 116 unconfident + 11 fixed-pending-recheck). Pre-fix qa_report.sh gates already all PASS, no exception-workflow rows needed. STEP1C fast-lane: 5 of 11 fixed-pending-recheck rows (SKU-137464, SKU-137476, SKU-137517, SKU-137478, SKU-072432) came back Tier-1-clean and were bulk-promoted; the other 6 (SKU-137550, SKU-137561, SKU-137597, SKU-137626, SKU-137633, SKU-137634) still tripped null_size and flowed into the main sweep. STEP2 Tier-1 sweep over the full worklist found only 20 hits, all null_size (no duplicate_brand/wrong_field_order/brand_casing/excess_content/canonical_field_mismatch/garbage_brand this pass) -- reflecting how thoroughly the long tail has already been cleaned by prior sessions. 8 of the 20 (Forever Living x2, SALTRAIN, Avec Moi, Kyogoku, UltraDEX, LUHA, Closeup FIFA) were already image-verified as having no legible size by the immediately-prior session and were not re-checked. Image-verified (curl+Read against the master_clean_niq image column) the 12 new candidates: 5 genuine D4 misses backfilled from clearly legible on-package text (SKU-137561 KIRATOTH 50g, SKU-137578 Sensodyne Pronamel for Kids 113g, SKU-137602 EverFresh Bamboo Charcoal 70g, SKU-137604 3M ESPE Clinpro 5000 113g, SKU-137633 BOP 100g); 6 had no legible size in the image and were left genuinely unresolved (SKU-137550 True Tisane -- also a 'Tooth Mousse' format, ambiguous whether even in-scope as toothpaste; SKU-137632 CNI WINZ generic banner; SKU-137626 Mutabu; SKU-137530 REGENERATE; SKU-137634 Darlie Fresh Mint Tea; SKU-137622 Perioe Total 7). One image (SKU-137537, 'ClearClean Premium White Clear Pack Toothpaste (7 uses)') revealed a genuine scope violation Tier-1 can't catch: the product is actually a Kao dissolving teeth-whitening film/strip pack, not a paste/gel/serum -- out of this category's scope per its own scope table, same failure class as the previously-documented SKU-072377 Polident denture-adhesive finding, but fixing requires unmapping the product_taxonomy_map row, which this session's STEP7 explicitly forbids -- flagged as needing a deletion-authorized session, not fixed. STEP2b promo-language sweep (1,019 rows / 6 distinct entries) found no new pack_count misses -- all 6 hits (SKU-139206, SKU-072090, SKU-072506, SKU-137509, SKU-137585, SKU-137589) are the same already-documented deferred/ambiguous entries from prior sessions, re-confirmed structural, not re-touched. STEP3 Tier-2 GMV-prioritized sample (top 80 of the remaining un-flagged worklist by June-2026 GMV, all under 540 SGD -- far lower than earlier sessions' 10K-35K GMV top items, confirming the high-value tail is already resolved) found one new genuine defect Tier-1's mechanical checks can't catch: SKU-137485 ('Watsons DR BRONNER'S Peppermint Toothpaste 140 G 140g') had brand_id resolved to BRD-GLOBAL-00640 ('Watsons', a multi-brand reseller) via a Sec.11-class merchant-name-as-brand failure, despite canonical_name/sku_name clearly stating the real brand 'DR BRONNER'S' -- reassigned to BRD-GLOBAL-00934 ('Dr.Bronner's'), the existing brand_id already used by 2 sibling entries in this exact category (SKU-137467, SKU-137575). The remaining 79 sampled rows: 74 confirmed genuine Tier-A structured entries (real product_line, size, pack_count all present and consistent, including SKU-072056's ambiguous 1s/2s/3s Colgate Charcoal Travel Kit correctly structured as is_multi_size/is_multi_variant=TRUE) and promoted via same-verdict bookkeeping; 5 left untouched as already-known ambiguous/deferred per prior sessions' notes (SKU-139213 Elgydium/SKU-072520-class near-duplicate, SKU-072045 & SKU-072040 Colgate Kids Fluffy Pump ambiguous Bundle-of-2-vs-3, SKU-137463 ARM & HAMMER ambiguous slash-joined product names, SKU-072131 Darlie Mystery Box composition-ambiguous phrasing). | Fixed and reset to unreviewed (_meta): 6 taxonomy entries -- 5 image-verified size backfills with canonical_name updated (SKU-137561, SKU-137578, SKU-137602, SKU-137604, SKU-137633) and 1 brand reassignment (SKU-137485, Watsons to Dr.Bronner's/BRD-GLOBAL-00934, no new brand_dict entry needed, canonical_name/product_line corrected to remove the reseller-name prefix). Bulk-promoted 5 rows via STEP1C fast-lane (SKU-137464, SKU-137476, SKU-137517, SKU-137478, SKU-072432) with no judgment spent. Reviewed and confirmed correct (no fix needed, promoted via same-verdict bookkeeping in one bulk statement): 74 entries from the STEP3 GMV-prioritized Tier-2 sample. 1 genuine scope-violation finding recorded but NOT fixed (SKU-137537 ClearClean whitening-film product -- needs a deletion-authorized session to unmap, same class as the pre-existing SKU-072377 Polident finding). Confidence distribution after this session (833 total distinct taxonomy_id mapped to this table): 291 confident, 125 unconfident (awaiting one more agreeing pass), 10 fixed-pending-recheck, 407 never-reviewed (untouched long tail, eligible next session). All post-fix QA gates pass: G1 dual-mapped(LLM)=0, G2 HUMAN+LLM coexistence=0, duplicate product_id=0, duplicate product+taxon=0, placeholder-leak=0, structured-fields-NULL%=0, garbled brand text=0 (pre-existing SKU-137593/'2080' exception remains closed via qa_gate_exceptions, no new hits), 'all variant/size' name=0. No new SKU block claimed, no product_taxonomy entries minted, no product_taxonomy_map rows inserted, updated, or deleted. |
| 2026-07-24 13:13 UTC | Automated review session (auto-discovery) | Auto-discovery review of shopee_sg_toothpaste (542-row worklist: 407 never-reviewed + 125 unconfident + 10 fixed-pending-recheck). Pre-fix qa_report.sh gates already all PASS. STEP1C fast-lane: 6 of 10 fixed-pending-recheck rows (SKU-137485, SKU-137578, SKU-137604, SKU-137633, SKU-137561, SKU-137602) came back Tier-1-clean and were bulk-promoted; the other 4 (SKU-137550, SKU-137597, SKU-137626, SKU-137634) still trip null_size, no legible size confirmed across 2+ sessions now. STEP2 Tier-1 sweep found 15 null_size hits, all matching low-GMV (under 850 SGD) long-tail resellers already image-verified as having no legible size in the immediately-prior same-day session -- deferred, no new information. STEP2b promo-language sweep found 6 hits; independent per-row verification (image reads) found the immediately-prior session's blanket 'all 6 re-confirmed structural, not re-touched' conclusion was wrong for 4 of them: SKU-139206 (Darlie assortment) had product_line='Toothpaste' (banned generic category word) instead of NULL per this category's own multi-variant-catch-all convention; SKU-137589 had brand mis-assigned to brand_dict noise-entry 'S-ERUM' (a generic-word match off 'serum' in sku_name) when the packaging image clearly reads 'YASENSHI', plus pack_count=1 despite an image-confirmed 'Buy 1 Get 1 Free' same-product promo; SKU-137585 (Oral-B) had pack_count=1 despite the listing's dominant option_name (93% of this product's GMV) being an explicit 3-pack; SKU-137509 (Sensodyne) had is_multi_variant=FALSE despite genuinely spanning 22 distinct formula options per option_name. A full-worklist scan for the '[Bundle N]'/'[Bundle of N]' bracket pattern (same defect class as this category's own prior live finding) found 3 more genuine misses beyond STEP2b's narrower regex: SKU-072048 (Colgate '[Bundle 2] Value pack' stated, pack_count was 1), SKU-072515/SKU-072516 (Enzim '[5+2 FREE] Bundle of 5' = 7 same-product units, pack_count was 6), and product 42303203391 (Darlie Lime Mint, sku_name explicitly 'Buy 2 Get 1 Free...3X140G'=3) mismapped into an existing x6 entry when a correct existing x3 entry (SKU-072136) was available. A full-worklist scan for product_line values that are bare category words (a stub class the prescribed Tier-1 regex doesn't check, since it only tests name-vs-field consistency, not whether product_line itself is generic) found SKU-072223 ('Dentiste Toothpaste 100g x2', no multi-size/multi-variant flag) silently absorbing 13 genuinely distinct Dentiste sub-lines (Nighttime, Natural, Plus White, Anticavity, Citrus Fresh, Hygienic & Herbapeutic, etc.) under one mismatched generic entry -- its highest-GMV contributor (product 10329828955, $131.10, 'Nighttime...100gx2+FREE 60g') didn't match the x2 pack_count either. STEP3 Tier-2 GMV-prioritized review covered the top 150 in-scope rows by June-2026 GMV (down to ~79 SGD). One genuine scope-violation from the immediately-prior session (SKU-137537, ClearClean whitening-film product -- not paste/gel/serum) remains open, still needing a deletion-authorized session since this session's STEP7 forbids unmapping rows. | Fixed 8 taxonomy entries in place, each with canonical_name/product_line/pack_count/brand_id/is_multi_variant corrected and _meta reset to unreviewed for next-session recheck: SKU-139206 (product_line -> NULL), SKU-137589 (brand reassigned to new brand_dict row BRD-SG-14371 'Yasenshi', pack_count 1->2, canonical_name updated), SKU-137585 (pack_count 1->3, canonical_name updated), SKU-137509 (is_multi_variant FALSE->TRUE), SKU-072048 (pack_count 1->2, canonical_name updated), SKU-072515 (pack_count 6->7, canonical_name updated), SKU-072516 (pack_count 6->7, canonical_name updated), SKU-072223 (product_line -> NULL, is_multi_variant FALSE->TRUE, matching this category's established generic-catch-all convention). Rerouted 2 product_taxonomy_map rows to better-matching existing entries -- no rows deleted, no new SKU block claimed: product 42303203391 -> SKU-072136 (existing x3 entry); product 10329828955 -> SKU-072225 (existing 'Dentiste Dentist Nighttime Toothpaste 100g x2' entry). Bulk-promoted 6 fixed-pending-recheck rows via STEP1C fast-lane (no judgment re-spent). Bulk-confirmed 135 Tier-2-reviewed rows as correct via one bookkeeping statement (excludes SKU-137485, already promoted via STEP1C this same session, to avoid double-counting a same-session re-check as two independent reviews). Confidence distribution after this session (833 total distinct taxonomy_id mapped to this table): 343 confident (was 291), 160 unconfident (was 125), 12 fixed-pending-recheck (was 10), 318 never-reviewed (was 407, untouched long tail eligible next session). All post-fix QA gates pass: G1 dual-mapped(LLM)=0, G2 HUMAN+LLM coexistence=0, duplicate product_id=0, duplicate product+taxon=0, placeholder-leak=0 (existing exceptions still closed, no new hits), structured-fields-NULL%=0, garbled brand text=0, 'all variant/size' name=0, provenance=0. |
| 2026-07-24 15:51 UTC | Automated review session (auto-discovery) | 834-entry incremental worklist. STEP1B: 1 gate-directed canonical_name-fields fix (SKU-072048, bracket-bundle leak in product_line). STEP1C fast-lane: 5 clean rows bulk-confirmed. Tier 1 SQL sweep flagged 15 null_size rows and 4 pack_count-promo-language rows (2 distinct after STEP1C overlap); Tier 2 GMV-prioritized sample covered the top 30 remaining rows by GMV. Found: a generic-stub entry (Elgydium 'Toothpaste' alone), a merchant-name leak ('Beauty Language' baked into canonical_name, docs §11 violation), two expiry-date/marketing-text leaks into product_line, a duplicate-unit-text artifact, two Colgate Kids Fluffy Pump pack_count/text-artifact defects, a Crest pack_count miss (1 vs dominant-option 5), a mixed-size Darlie bundle needing is_bundle=TRUE, and 7 null_size rows resolved via sibling-listing inference / product image / master_raw_niq description fallback (raw_niq_history as documented doesn't exist in this project). Also surfaced a brand_dict duplicate-PK bug (BRD-SG-14371) and two scope-violation entries (UltraDEX oral-rinse-dominant, ClearClean whitening-film) miscategorized into this taxonomy. | Fixed 17 taxonomy entries in place (content/size/pack_count/canonical_name corrections, all meta_agent=CLAUDE_CODE, _meta reset to unreviewed for next-session confirmation). Bulk-confirmed 29 entries as Tier-2-reviewed-correct (review_confidence bookkeeping only, no content change). Self-caught and fixed 3 gate regressions introduced by my own edits before finishing. Post-fix qa_report.sh: all gates pass (was 1 failure — canonical_name fields — at session start). Confidence distribution left behind: 17 entries reset to unreviewed (await next session's confirming re-review), 29 entries advanced one step toward confident (need one more agreeing review each), 2 entries (UltraDEX, ClearClean) left untouched pending a deletion/reroute-authorized session, brand_dict PK collision left untouched pending a dedup session. |
| 2026-07-24 17:46 UTC | Automated review session (auto-discovery) | Auto-discovery review of shopee_sg_toothpaste (475-row worklist: 363 never-reviewed + 111 unconfident + 18 fixed-pending-recheck at start; pre-fix qa_report.sh gates all PASS per wrapper report, only pre-existing exception was the confirmed-permanent '2080'/Aekyung garbled-brand-text case, no new exception rows needed). STEP1C fast-lane: 15 of 18 fixed-pending-recheck rows bulk-confirmed Tier-1-clean; 3 (SKU-072223 null product_line, SKU-137634 null_size, SKU-139206 excess_content) flowed into the normal fix path. STEP2 Tier1 sweep flagged 10 rows (8 null_size, 1 excess_content, 1 wrong_field_order). STEP2b promo-language sweep: 2 pack_count=1 hits, both false positives ('x1' literal single-unit text; a zero-GMV variant-selector listing). Tier 2 GMV-prioritized sample covered ~80 rows including all Tier1 flags plus the top-80-by-GMV clean rows (images fetched via curl+Read for 6 long-tail products with no text-derivable size, confirming genuinely unresolvable sizes rather than missed extractions). | Fixed 11 real defects: SKU-072223 Dentiste NULL product_line -> 'Natural Toothpaste' (grounded in sku_name/description text); SKU-137537 ClearClean NULL size -> '7 uses' (count-based film product, no ml/g unit exists); SKU-137463 ARM & HAMMER, SKU-137585 Oral-B, SKU-137522 GC MI Paste, SKU-137505 Lion CLINICA: is_multi_variant FALSE->TRUE (each listing genuinely bundles/selects among multiple distinct flavors or product lines in one sku_name, confirmed via full sku_name text); SKU-139199 Crest: is_multi_size TRUE->FALSE + size NULL->'90g' (only one product ever mapped to this entry and its sku_name plainly states 90g -- was incorrectly modeled as multi-size); SKU-137512 Weleda: pack_count 1->3 (sku_name explicitly '[3-Pack]', also stripped a leaked expiry-date fragment from product_line/canonical_name); SKU-072109 Curaprox: size '2oz'->'60ml' (sku_name explicitly states 60ml, text wins over an inconsistent unit); SKU-137534 Colgate: stripped merchant_name 'Beauty Language' leaking into canonical_name/product_line, a genuine section-11 signal-provenance violation (confirmed against s.merchant_name); SKU-137513 Avec Moi: stripped 'BEST PRICE ALWAYS!' SEO-spam prefix from product_line/canonical_name. All other Tier1-flagged and Tier2-sampled rows (60 total) were judged correct as-is and promoted via the confident/unconfident bookkeeping UPDATE. Two items left unresolved and NOT fixed, flagged as findings requiring a follow-up session: (1) SKU-137529 UltraDEX is genuinely out-of-scope (oral rinse/spray, not toothpaste) but unmapping it requires deleting a product_taxonomy_map row, which this session cannot do -- needs a deletion-authorized session; (2) brand_dict has a duplicate primary key BRD-SG-14371 with two conflicting canonical_name values ('Jiyu' and 'Yasenshi'), causing join fan-out in any brand_dict join -- a cross-category data-integrity bug outside this session's scope, found via a false-positive wrong_field_order flag on SKU-137589. Also noted: ARCHITECTURE.md/llm-extraction-rules.md document a 'raw_niq_history' dataset that does not exist in this project; the real extended-text/image data lives in master_raw_niq.shopee_sg_toothpaste. Post-fix hard gates re-verified: G1 dual-mapped=0, G2 HUMAN+LLM coexistence=0, G3 placeholder-leak=0, G4 structured-fields-NULL%=0%, G5 provenance=0 missing, garbled-brand-text=0 (after excluding the pre-confirmed '2080' exception). Worklist reduced from 475 to 416 (363 never-reviewed + 111 unconfident + 25 fixed-pending-recheck -> 1097 now confident). |
---

## Map Row Counts (verified live 2026-07-23, after session #4's top-up write)

| Source | Count | Notes |
|--------|-------|-------|
| LLM | 1,596 | 1,331 from sessions #1/#2 + 264 from session #3 (109 reuse-matched + 155 newly minted) + 1 from session #4 (`49352828260` → `SKU-139242`, reuse-matched) |
| HUMAN | 0 | All `HUMAN` rows for this category — and platform-wide across every category in `product_taxonomy_map` — were already gone at the start of session #3, undocumented. See session #3's QA History row above; not caused by this session, still an open data-integrity anomaly for human follow-up. |
| NULL (unmapped) | remainder of distinct products in `master_clean_niq.shopee_sg_toothpaste` | Live worklist re-check after session #4's write: 7 rows / 6 distinct products remain — all confirmed-OOS-left-NULL by design (re-verified this session), not a defect. |

**QA gate results (session #4, run WITHOUT `--skip-coexistence` per `docs/headless-runbook.md`):**

| Gate | Result | Note |
|------|--------|------|
| G1 dual-mapped LLM | 0 | Pass |
| G2 HUMAN+LLM coexistence | 0 | Pass |
| G3 placeholder-leak | 17 distinct entries | Unchanged pre-existing `SKU-072520`–`072536` catch-alls, still deferred to a future `targeted_qa_fix.sh` pass — not touched this session. |
| G4 structured-fields-NULL% | 0% | Pass |
| G5 provenance | 0 missing | Pass — the 1 new map row this session has `meta_agent='CLAUDE_CODE'`, `source='LLM'` |
