# makanananjing_my — Category Context

> Custom (non-NIQ) source: `sincere-hearth-273704.makanananjing.9_makanananjing_my_daily`
> (Shopee Malaysia, daily grain summed to monthly). Not a `master_clean_niq` table — different
> schema (daily grain, `category_1`/`category_2` fixed to `Pet`/`Pet Food` for every row regardless
> of actual product, no `model_id`/`month`/official-store-badge-driven data the way NIQ tables have
> it). Taxonomy state is keyed under `master_table = 'makanananjing_my'`, not the source table name.

---

## Status

| Field | Value |
|-------|-------|
| LLM Pass | ✅ Complete (Full Rebuild, first run) + 5 top-up sessions |
| GMV Coverage | Not recomputed this session — stale since first run; this session closed 278 of 291 live-worklist gap rows (see QA History 2026-07-28) |
| Last run | 2026-07-28 (5th top-up) |
| Current MAX taxonomy_id (this category) | SKU-190467 — **query BQ directly before trusting this**, per `CLAUDE.md`'s SKU Block Management warning; this field lags real time between sessions |

---

## SKU Blocks Assigned

| Block | Usage |
|-------|-------|
| SKU-149586–SKU-151585 | Claimed block (2,000 slots, `sku_block_registry`, scenario `custom_full_rebuild`) |
| SKU-149586–SKU-150711 | Actually used (684 taxonomy entries) — remainder of block left unused |
| SKU-151786–SKU-153022 | Claimed block (1,237 slots, `custom_topup`, 1st top-up) — 124 used |
| SKU-155718–SKU-156802 | Claimed block (1,085 slots, `custom_topup`, 2nd top-up) — 10 used |
| SKU-156803–SKU-157002 | Claimed block (200 slots, `custom_topup`, 3rd top-up) — 1 used (`SKU-156803`, Tarokun) |
| SKU-157957–SKU-159026 | Claimed block (1,070 slots, `custom_topup`, 4th top-up) — 1 used (`SKU-157957`, PETdiatric) |
| SKU-190352–SKU-190642 | Claimed block (291 slots, `custom_topup`, 5th top-up) — 116 used (SKU-190352–SKU-190467) |

---

## Brand Scope

75 distinct brands mapped (46 reused from existing `brand_dict`, 29 newly minted as `BRD-MY-*`
local/house brands — a wider first pass of 47 new brands was minted, but 18 turned out unused after
the cat-scope correction below). Top brands by product count:

1. **Royal Canin** — `BRD-GLOBAL-00001` — 108 products
2. **Probalance** — `BRD-GLOBAL-01915` — 44
3. **Brit** — `BRD-SG-02158` — 42
4. **Chunk Bits** — `BRD-MY-01034` (new) — 36
5. **Alps Natural** — `BRD-SG-02056` — 32
6. **Nature's Protection** — `BRD-SG-00100` — 31
7. **Pedigree** — `BRD-SG-00016` — 26
8. **Notti** — `BRD-SG-02108` — 23
9. **Natural Core** — `BRD-SG-00136` — 23
10. **Back2Nature** — `BRD-SG-09726` — 17
11. **Monge** / **JerHigh** / **Rosy Fresh** — 15 each
12. **Hill's** / **Amelisa Pet & Co** — 14 each

No official-store allowlist — this source table has no `merchant_badge`/official-store signal;
Pass 1 (official-store) was skipped, going straight to bulk keyword-matched routing per the session
brief.

---

## Scope — What's In vs Out

**In scope:** dog dry food, dog wet food (can/pouch/tray), dog treats, dental chews, freeze-dried
dog snacks, dog nutritional supplements.

**Out of scope (left NULL/unmapped):**
- **Cat food of any kind.** A sibling category, `docs/categories/makanankucing_my.md`, already
  covers cat food comprehensively from a separate source table
  (`makanankucing.9_makanankucing_my_daily`) — see "Cross-category discovery" below. 413 cat-scoped
  products (explicit `cat`/`kucing` text, or known cat-exclusive brands like Sheba, Aixia, Nekko,
  Snappy Tom, Kucinta, Fancy Feast, Felix, Friskies, ME-O, Kit Cat, Royal Canin Kitten) were excluded
  from this category's mapping.
- Non-pet-food contamination: dairy/milk powder (Dutch Lady, Fernleaf), coffee (Nescafe, Moccona),
  snacks (Julie's, Maggi, Quaker), laundry detergent (Top, Downy), toothpaste (Colgate), skincare
  (Olay), baby diapers (Huggies, Merries, Diapex), toys. This source table's `category_1`/`category_2`
  are fixed to `Pet`/`Pet Food` for every row regardless of actual product — a large share of the
  95%-cumulative-GMV worklist (539 of 1,968 products, ~$9.85M of $31.5M) is general-merchandise
  leakage from mixed-catalog sellers (`Shopee Supermarket`, `Lotus's`), not pet food at all.
- Pet non-food/medicine: cat/dog litter, flea/tick treatment (Frontline, Advantage, Bayer, Drontal
  dewormer), pet training pads, cages, carriers, grooming shampoo/wipes.

**Edge cases:**
- Dual-species listings (e.g. "NOTTI [Treat] ... Dogs & Cats", "Greenies Pill Pockets for Cats &
  Dogs") were kept in this (dog) category rather than excluded — they are not cat-exclusive.
- 46 genuinely dog-labeled products (Royal Canin, Hill's, Orijen, Brit, etc. explicitly "Dog"/"Canine"
  in the title) were found already mapped under `master_table='makanankucing_my'` — see below. These
  were dropped from this session's mapping to avoid a dual-map (product_id, platform, country)
  violation, deferring to the pre-existing (if mis-scoped) row rather than editing another category's
  already-shipped data.

---

## Cross-category discovery (significant finding)

Mid-session, a collision check against the true dedup key `(product_id, platform, country)` — not
just `master_table` — found that **370 of this session's initially-mapped 1,190 "pet food" products
were already mapped under `master_table='makanankucing_my'`**, a same-day sibling Full Rebuild session
covering cat food from a *different* source table (`makanankucing.9_makanankucing_my_daily`). Its
category doc confirms its scope is "cat dry food, cat wet food, cat treats" with dog food explicitly
listed as out-of-scope for it.

This meant this session's original scope decision — "any identifiable pet food, dog or cat" — was too
broad given the sibling category already owns cat food. Corrected scope to **dog food only**
(matching the literal source table name, "makanan anjing" = dog food in Malay), removing 413
cat-scoped products from this session's mapping (see Scope section above).

A residual 46 collisions remained after that correction: genuinely dog-labeled products that
`makanankucing_my` had *also* mapped, despite its own documented scope excluding dog food. Rather than
edit that category's already-shipped, already-QA'd data (out of scope for this session, and its
universe-refresh state is unknown from here), this session's duplicate rows were dropped and the
existing `makanankucing_my` rows were left standing. **This is a real residual defect**: those 46
products are dog food sitting under a "cat food" master_table, and the correct long-term fix is a
`targeted_qa_fix.sh` (or manual) pass against `makanankucing_my` to re-route them here — flagged in
Targeted QA Fix Brief below.

---

## Taxonomy Design Notes

**Extraction method:** bulk SQL/regex text-matching on `sku_name` only — no merchant/official-store
data, no image reads (all 731 mapped products were resolved from title text alone; this is a lower
signal-richness pass than categories with image verification). Brand identified via an ~110-entry
curated keyword dictionary (longest-match-first, iteratively built against the GMV-ranked unmatched
tail across three refinement passes). Size and pack-count extracted via regex over `sku_name`
(kg/g/ml/L/lb units incl. plural/uppercase forms, `x{N}`/`*{N}` and `({N} cans/pcs/pouches)`
multiplier patterns).

**product_line:** derived by stripping the matched brand token, retailer/reseller noise (`GUN PET`,
`POODEE`, `Borong`, `MAIN`, `ORIGINAL`, bracketed promo tags, `WH\d+` warehouse codes), generic
category words (dog/food/dry/wet/premium/anjing/makanan), and the extracted size/pack tokens from the
cleaned `sku_name`; the remainder is written to `product_line`. This is a bulk-first, coverage-over-
precision pass per `docs/headless-runbook.md`'s Full Rebuild guidance — long-tail entries'
`product_line` text sometimes retains noise (stray retained words, embedded Chinese descriptive text)
that a `targeted_qa_fix.sh` pass would clean up. `sub_line`/`variant` were left NULL throughout (no
reliable bulk signal to split product_line from a genuine sub-line/variant at this text-only,
no-image extraction depth).

**Brand registry note:** ~47 new `BRD-MY-*` brand_dict rows were minted this session for pet-food
brands not previously in the global registry (Sarar, Vet-Pro, Furlari, Rich.Co, Josera, Wilderness
Legend, NutriEdge, etc.) — `brand_dict` has no atomic-claim mechanism analogous to
`sku_block_registry` for `taxonomy_id`; `MAX(BRD-MY-*)+1` was queried immediately before each insert
batch, no collision was detected against this session's own inserts, but see the sibling category's
own doc for a same-day collision it hit against a different parallel session at the same ID range —
this class of race is a known gap for future sessions.

---

## QA History

| Date | Pass | Finding | Resolution |
|------|------|---------|------------|
| 2026-07-28 | Top-up (coverage gap, 6th) — **zero writable rows; residual worklist is genuinely exhausted** | Live re-run of the 95%-cumulative-GMV worklist (not trusted from the wrapper's pre-check) found 13 still-unmapped products — same count the wrapper reported, confirming the 5th top-up's closing figure held. Classified all 13 individually (small enough for direct per-row review, no bulk grouping needed): all 13 are the *exact* residual categories the 5th top-up session already documented, plus one newly-surfaced item: 5 cat-scoped (`Makanan Kucing`/`Kucing`/`Cat` explicit in title — Carnilove, PETLUSCI, Notti, Smartheart, a 50-pouch cat wet food listing), 3 general-merchandise contamination (10PCS Nordic Floor Mat — home decor; AJ-B.PLUS BIOPLUS — confirmed livestock/cattle-poultry feed per 5th top-up's image verification, same product_id; Pentavite Multivitamin "Untuk Lelaki" — human men's vitamin), 2 pet-medicine/non-food (Cassiel parasite-control dewormer chew for cats & dogs; Herbavetinari MAGTOX wound-treatment liquid — same products as 5th top-up's "parasite-control dewormer chew" and "veterinary wound-treatment liquid"), 1 not-for-sale ("[Gift/NotForSale] Mystery Gift Dog Food"), 1 genuinely unresolved multi-species listing (Rich Choice Holistic 2kg — same product as 5th top-up's flagged item, still mixes a cat-exclusive "Cheek Booster Kitten" option with 3 dog-leaning options in one buyer-selectable SKU), and 1 newly-classified item not in the 5th top-up's list: "Pet Molar Grass Stick 25pcs...Timothy Carrot Oat Fruit Orchard Small animal snacks chew toy stick" — Timothy-hay-based, explicitly labeled "small animal snacks" (rabbit/rodent chew), not dog food — out of scope on species grounds, same exclusion class as the cat-scoped items but for a different non-dog species. Net: **0 in-scope dog-food products** — every row in this session's live worklist is a confirmed, defensible exclusion. **Notable data-quality finding**: every `url`/image CDN URL in this session's worklist points to `tiktok.com`/`ibyteimg.com`/`tiktokcdn.com`, not Shopee, despite this doc's header describing the source table as "Shopee Malaysia." A follow-up count confirmed the source table itself is mixed-platform: 20,313 Shopee-URL rows vs 3,108 TikTok-URL rows (of 23,421 total) — the doc's "Shopee Malaysia" framing is incomplete, not wrong per se, but every prior session's `platform`/`country` provenance assumptions for `product_taxonomy_map`/ADR-006 composite-key collision checks against this master_table should be revisited against actual per-row platform, not assumed Shopee-only. Flagged for a future session; not resolved here (out of scope for a coverage top-up). Also notable: the Cassiel dewormer's `gmv_monthly` (384M+) is ~186K/unit for 2,061 units sold — a clear outlier that alone accounts for 24.5% of the category's cumulative GMV and likely distorts the 95%-cutoff denominator (making the true in-scope tail beyond rank 13 potentially larger than a clean distribution would suggest). Recorded for awareness; not investigated or corrected this session, per STEP 0's instruction to use the query's output as-is. | **No writes.** STEP 2's SKU block claim was deliberately skipped — classification of the full 13-row live worklist yielded zero products requiring a new or reused taxonomy entry, so there was nothing to write and claiming a 200-slot block would only add unused registry noise (this category already has 5 mostly-unused prior blocks). Pre-write true-composite-key collision check on all 13 product_ids against every `master_table` returned 0 rows (none previously mapped anywhere) — recorded for completeness even though nothing was written. Self-check QA gates (G1, G2 without `--skip-coexistence`, placeholder-leak, structured-fields-NULL%, G5, G4 full cross-`master_table`): G1=0, G2=0, placeholder-leak=0, structured-fields-NULL%=1% (well under the 50% threshold, unchanged from 5th top-up since no new rows were written), G5=0, G4=1 — confirmed via direct lookup to be the same pre-existing residual (`24442421432`) documented in every prior top-up session's entry, not a new collision. Live gap unchanged at 13 — this is not a stalled session; every one of the 13 was individually reviewed and has a defensible exclusion reason. Coverage for this category's live worklist is now genuinely exhausted; further top-up passes against this exact worklist will keep returning the same 13 (plus whatever the TikTok-platform/GMV-outlier findings above eventually surface) until either (a) the Targeted QA Fix Brief's cross-category re-routing task against `makanankucing_my` is executed, or (b) the platform-mix/GMV-outlier findings are investigated. |
| 2026-07-28 | Top-up (coverage gap, 5th) — **collision-free worklist, coverage was the real bottleneck again** | Live re-run of the 95%-cumulative-GMV worklist found 291 still-unmapped products (matched the wrapper's pre-check number, re-verified live per instructions rather than trusted). A true-composite-key cross-`master_table` collision check run *before* any classification work (lesson from the 2nd/3rd top-up sessions) found **zero** of the 291 already mapped anywhere — unlike every prior top-up session, this worklist was not dominated by the `makanankucing_my` collision. Classified all 291 by species/scope signal (regex initially mis-handled plural forms "dogs"/"puppies"/"cats" — fixed and full set re-classified): 262 dog-signal, 16 genuine dual-species (dog+cat, kept per this doc's precedent), 5 cat-only, 3 general-merchandise contamination (a "Nordic Floor Mat" listing, a human multivitamin, and — image-verified — "AJ-B.PLUS BIOPLUS" which turned out to be **livestock/cattle-poultry feed supplement**, not pet food, despite surfacing in this pet-labeled source table), 2 pet-medicine/non-food (a parasite-control dewormer chew, a veterinary wound-treatment liquid), 1 not-for-sale mystery-gift listing (recurring excluded pattern), and 1 left genuinely unresolved (a "Rich Choice Holistic" multi-variant seller listing whose 4 buyer-selectable options mix a cat-exclusive "Cheek Booster Kitten" formula with 3 dog-leaning options in one SKU — can't determine what the buyer actually receives, same rationale as this doc's multi-size-seller-listing precedent). Net: **278 in-scope products**, the largest single-session in-scope yield since the first Full Rebuild. | Reuse-before-mint via bulk brand+size+pack_count matching against the category's existing 820 taxonomy entries (with a life-stage conflict guard added after an early pass silently matched several Royal Canin Puppy-formula listings to existing Adult-formula entries at the same size — token-overlap scoring alone doesn't distinguish life stage since "adult"/"puppy" are common short tokens): **151 products reused** 112 distinct pre-existing entries; **127 products** grouped by brand+size+pack+life-stage/diet-signal into **116 new taxonomy entries** (one further false-merge caught and fixed: a "Multivitamin" Popoo chew had matched an unrelated "Hip & Joint Support" Popoo entry on incidental flavor-word overlap). Minted 14 new `brand_dict` rows (`BRD-MY-01216`–`BRD-MY-01229`: BOCO, Furiri, Klington, Midalac, MoeMagic, Royal Bites, Town Dog, Trendy Woof, Viasana, ZAPRO, ZULPET, Meat Up, Pawny, Petto) plus reuse of 8 brands already in global `brand_dict` but not yet used in this category (Masti, Mr.Vet, Pet Record, Lemo, Optimum, Petlusci, Urbanwolf, Pet8) — found by querying `brand_dict` directly before minting, avoiding duplicate brand rows. Notable new-brand find: the "Celebrity Pet Group" seller's ungrouped listings ("SALMON RECIPE HOMEMADE DOG FOOD", "Super premium fussy eater Chicken", etc. — no brand token in any `sku_name`) were image-verified to all be **Viasana** ("Viasana for DOGS" / "Pawganic" home-cooked line / "Fussy Eater" super-premium line), confirmed via the product image rather than the seller's storefront name (per `llm-extraction-rules.md` §11, merchant_name is never a naming signal — the image itself carries the on-package brand). Claimed SKU block SKU-190352–SKU-190642 (291 slots) — 116 used, 175 returned unused. Self-check QA gates (G1, G2 without `--skip-coexistence`, placeholder-leak, structured-fields-NULL%, G5, G4 full cross-`master_table` on this session's own 278 product_ids): **all 0** except structured-fields-NULL% at 1% (well under the 50% threshold) — zero collisions, zero dual-mapping, zero HUMAN+LLM coexistence, zero placeholder leaks, zero provenance gaps. Live gap reduced 291 → 13 (the 13 legitimately-excluded/ambiguous products above). |
| 2026-07-23 | Top-up (coverage gap, 4th) | Live re-run of the 95%-cumulative-GMV worklist found 1,070 still-unmapped products (matched the wrapper's pre-check number, re-verified live per instructions). Classified all 1,070 by species/collision signal: 431 already mapped elsewhere (checked via true composite-key `(product_id, platform, country)` cross-table join — all against `makanankucing_my`, the same recurring collision this doc's Cross-category discovery section documents), 144 cat-only (has_cat/cat-brand keyword), 3 dog-keyword matches (1 not-for-sale mystery gift, 2 dog diapers — pet-non-food, both excluded), and 492 with no species keyword at all. Swept the no-species-keyword bucket for known dog/pet-food brand tokens and any `pet`-substring text: found 9 petfood-signal hits (2 Royal Canin Wet Food Pouch listings — same GMV figures already confirmed **Feline** by image in the 2nd top-up session; 1 Wanpy Creamy Lickable Treats — already confirmed cat-scoped in the 2nd top-up session; Royal Canin German Shepherd Adult 11kg and Orijen Small Breed Marine Fish — both already sitting under `makanankucing_my` per the 3rd top-up session's finding; MOCCONA/Boncafe freeze-dried coffee — false-positive from the `freeze-dried` regex token, general merch; 1 "Smartz Choice Booster Gemuk" freeze-dried treat — image-verified this session, packaging shows cats on every jar, cat-scoped) plus 2 more `pet`-substring hits requiring an image check: a "dehydrated chicken feet/duck feet pet treat" (image showed no species-identifying context, left unresolved as genuinely ambiguous) and "PETdiatric MAXIMUS-JOINT UC-II 90 Tablets" (image confirmed "For Cat & Canine" veterinary joint supplement — dual-species, kept in this dog category per this doc's existing dual-species-listing precedent, and had zero pre-existing mapping under any `master_table`). Net: **1 new in-scope product** (PETdiatric); everything else in the entire 1,070-product worklist is either the known cross-category collision, cat-only, general-merchandise contamination, or genuinely species-ambiguous. | Minted brand `BRD-MY-01215` (PETdiatric, no prior `brand_dict` presence) and taxonomy entry `SKU-157957` ("PETdiatric Maximus Joint UC-II 90 Tablets", product_line "Maximus Joint UC-II Veterinary Dietary Supplement", size "90 tablets", pack_count 1), mapped product `15414535234` to it (`source='LLM'`, `meta_agent='CLAUDE_CODE'`, confidence 0.85, `brand_from_image='PETdiatric'`). Claimed SKU block SKU-157957–SKU-159026 (1,070 slots per the live worklist size at claim time — only 1 slot used, 1,069 returned unused, consistent with this table's now-established pattern of the live gap being dominated by non-dog-food noise rather than unresolved dog-food coverage). Self-check QA gates: G1 dual-mapped = 0, G2 HUMAN+LLM coexistence (no `--skip-coexistence`) = 0, placeholder-leak = 0, structured-fields-NULL% = 1% (well under 50% threshold), G5 provenance = 0, G4 full cross-table (true composite key, all `master_table`s) = 2 — both confirmed pre-existing and unrelated to this session's write (`24442421432`, the already-documented residual from the 2nd/3rd top-up sessions, and `24279117321`, an unrelated `shopee_sg_shampoo`/`shopee_sg_hair_conditioner_or_treatment` collision from a different category pair, first visible only because this check is now unscoped to just this table) — zero net-new collisions introduced. Live gap effectively unchanged (1,070 → 1,069): confirms this doc's 3rd top-up conclusion that the coverage gap is no longer the bottleneck for this category — the outstanding cross-category re-routing task (see Targeted QA Fix Brief) remains the higher-priority unresolved item, not further top-up passes against this worklist. |
| 2026-07-23 | Top-up (coverage gap, 2nd) | Live re-run of the 95%-cumulative-GMV worklist found 1,085 still-unmapped products (matched the number cited in the session brief, but re-verified live rather than trusted). Classified all 1,085 via scope rules from this doc: 509 cat-only, 20 dog+cat dual (mostly non-food accessories/flea treatment/sample-pack giveaways), 53 dog-only, 503 neither-keyword (mostly general-merchandise contamination — Glade, Nestle, Prego, Tenten, Libresse, Pokka — plus a handful of brand-only Royal Canin/Orijen/Wanpy/Ciao Churu listings resolved by brand-name matching). Image-verified 5 ambiguous high-GMV candidates: 2 "Royal Canin Wet Food Pouch" listings (232,818 + 39,823 MYR) turned out to be **Feline** wet food despite no cat keyword in the title — excluded; 1 Wanpy "Creamy Lickable Treats" listing confirmed cat-scoped via a sibling listing's explicit "Cat Treat / Makanan Kucing" text — excluded; 1 "Myfoodie Meat Package" confirmed dog (poodle on packaging) — included; 1 "NEW Pudding Fresh Goat Milk Jelly ...Dog/Cat" listing, despite "Dog treat" in the title, was confirmed **cat-scoped** via image text "使用对象：12月龄以上猫" (for cats 12mo+) — excluded after already being provisionally scoped in, catching a keyword-stuffed false positive. Net 58 products classified in-scope dog food/treats/dental-chews/supplements. | Mapped via reuse-before-mint: matched 9 products to pre-existing `makanananjing_my` taxonomy entries (same brand+line+size/pack) by grepping the category's existing 808 entries; minted 43 new entries for the remainder (merging near-duplicate listings sharing brand+line+size/pack, e.g. 2 "GUN PET Royal Canin Poodle Pouch 85g" resellers → 1 entry, 3 "DentDoc Dental Stick" resellers → 1 entry). Claimed SKU block SKU-155718–SKU-156802 (1,085 slots, `custom_topup`). |
| 2026-07-23 | Top-up (coverage gap, 2nd) — **cross-category collision, caught pre-refresh** | After the initial 43-entry / 57-map-row write, a true-composite-key (`product_id, platform, country`, per ADR-006) collision check against **all** `master_table`s — not just this one — found 35 of the 58 just-mapped products (33 of the 43 new taxonomy entries, plus 8 of the 9 reuse-mapped products) were **already mapped under `master_table='makanankucing_my'`** (the sibling cat-food category, same-day session, `mapped_at` ~08:00–08:55, i.e. before this session started writing). Spot-checked several: their `makanankucing_my` canonical_names literally read e.g. "Royal Canin Dog Adult Large Breed 13kg" and "Back2Nature ... Dog 95g" — genuinely dog-scoped products that a same-day `makanankucing_my` session mapped despite its own documented scope excluding dog food, a large-scale recurrence of the single-digit residual this doc's Cross-category discovery section already flagged. This session's own G1/G2 gate queries didn't catch it because they're scoped to `master_table='makanananjing_my'` only, and none of these 35 products had any pre-existing row *under this table* (that's exactly why they were in the live gap) — the collision is only visible by joining across `master_table`s on the true composite key, which this session's SQL didn't do until after writing. | Per this doc's established precedent (Cross-category discovery section): dropped this session's own 35 duplicate `product_taxonomy_map` rows and deleted the 33 taxonomy entries that were left with zero remaining map rows after the drop (0 products referencing them); left the pre-existing `makanankucing_my` rows standing untouched (out of scope to edit this session, unknown QA/refresh state). Final surviving write: **10 new taxonomy entries, 14 map rows** (13 new-mint + 1 reuse of a pre-existing entry). Re-ran G1/G2/G4(full cross-table)/G5/placeholder-leak after the fix — all 0 except one **pre-existing** G4 residual (product `24442421432`, mapped under both tables at 08:45/08:55, before this session touched anything) — confirmed this session introduced zero net-new collisions. Live gap reduced 1,085 → 1,071. **Flagged for future work**: the same re-routing task this doc already tracks in Targeted QA Fix Brief (46 products) has grown substantially — a same-day `makanankucing_my` session remapped a large batch of genuinely dog-scoped products; a full audit of `makanankucing_my`'s cross-table collisions (not just the ones this session happened to touch) is now overdue. |
| 2026-07-23 | Top-up (coverage gap, 3rd) — **collision now dominates the entire live gap** | Live re-run of the 95%-cumulative-GMV worklist found 1,071 still-unmapped products (matched the wrapper's pre-check number, but re-verified live rather than trusted, per instructions). Classified all 1,071 via this doc's established bulk text rules: 453 cat-only (has_cat, no dog signal), 159 general-merchandise-only signal (Milo, Glade, Shokubutsu, Aik Cheong, Tong Garden, Gillette, Dettol, Nescafe, etc. — dozens of unrelated FMCG/cosmetics/electronics brands), 61 pet-non-food/medicine (flea/tick treatment, litter, wee-wee pads, pet wipes, reptile/bird/rabbit equipment — none of it dog food), 6 not-for-sale (sample-pack/mystery-gift giveaways, incl. the recurring "[Gift/NotForSale] Mystery Gift Dog Food" listing already excluded in the first Full Rebuild), and 332 signal-neither products manually reviewed in full (overwhelmingly further general-merchandise contamination — Nestle, Biscoff, Kotex, Oral-B, reptile UVB lamps, human cosmetics/electronics — plus 4 with a genuine dog signal missed by the keyword regex: 2 breed names not caught by `\bdog\b` matching (Royal Canin German Shepherd, Orijen Small Breed Marine Fish), 1 explicit dual-species phrase ("Royal Canin Recovery Liquid ... for cats and dogs"), and 1 image-verified case (Tarokun "Little Bun/Mix Bone Shape" pet biscuit — no dog keyword in `sku_name`, but the product image's own packaging text reads "ドッグフード" / "Healthy Biscuit ... for Dog" with dog-breed illustrations, confirming dog scope per this doc's image-priority rule; the reseller's own "EasyMEOW" watermark on the image was correctly not treated as a brand/species signal per `llm-extraction-rules.md` §11). This produced 44 candidate dog-scoped products in total (37 from the `\bdog\b`/`anjing`/`puppy`/`canine` regex sweep + 3 genuine dual-species treats/kibble/dental-chew listings from the has_cat+has_dog bucket + 4 from the manual sweep above). **A true-composite-key collision check run *before* writing anything this time** (lesson learned from the 2nd top-up session's post-write discovery) found **43 of these 44 already mapped under `master_table='makanankucing_my'`** — spot-checked canonical_names confirm genuine dog scope (e.g. `9642444110` → "Royal Canin Dog Adult Large Breed 13kg", `52958728091` → "Orijen Fish Dog 2kg", `10756858966` → "Greenies Dog Dental Treatpak ... Dog Dental"), all `mapped_at` ~2026-07-23 08:00–08:46, i.e. the same same-day `makanankucing_my` session already implicated in the prior top-up's collision finding. Only 1 of the 44 (Tarokun) had zero pre-existing row anywhere. | Wrote only the 1 genuinely clean product: minted brand `BRD-MY-01186` (Tarokun, a Japanese-import dog-biscuit brand with no prior `brand_dict` presence) and taxonomy entry `SKU-156803` ("Tarokun Little Bun Mix Bone Shape Dog Biscuit", size NULL — not stated in title or clearly legible in the pack-jar image, pack_count=1), mapped product `2641777624` to it (`source='LLM'`, `meta_agent='CLAUDE_CODE'`, confidence 0.85). The other 43 were left untouched — **not written here** — per this doc's established precedent of not editing `makanankucing_my`'s already-shipped data out-of-scope for this session. Claimed SKU block SKU-156803–SKU-157002 (200 slots per `headless-runbook.md`'s topup floor, sized to the verified need rather than the stale 1,071-worklist-row figure — only 1 slot used, 199 returned unused). Self-check QA gates (G1, G2 without `--skip-coexistence`, G4 full cross-table, G5, placeholder-leak, structured-fields-NULL%): all 0 except structured-fields-NULL% at 1% (well under the 50% threshold) and G4 at 1 — confirmed via direct lookup to be the same **pre-existing** residual (`24442421432`) already documented in the prior session's entry, not a new collision. Live gap reduced 1,071 → 1,070 — the smallest single-session reduction so far, because **the coverage gap is no longer the bottleneck**: of everything left in the worklist, cat/general-merch/pet-non-food exclusions are genuinely out of scope, and nearly all remaining genuine dog-food signal is phantom — already sitting under `makanankucing_my`. Further sessions against this table's live worklist will keep finding a near-identical result until the cross-category re-routing task below is actually executed. |
| 2026-07-23 | Full Rebuild (first run) | Confirmed 0 pre-existing `product_taxonomy_map` rows for `master_table='makanananjing_my'` — genuine first run | Proceeded per Full Rebuild scenario |
| 2026-07-23 | Full Rebuild | Source table is dramatically mixed-category (`category_1`/`category_2` fixed to `Pet`/`Pet Food` for every row regardless of actual product) — snacks, dairy, coffee, detergent, diapers, skincare all present | Built a pet-food scope keyword classifier (positive pet+food signal AND NOT hardware/medicine AND NOT human-FMCG signal), iteratively refined across 3 passes against the GMV-ranked unmatched tail |
| 2026-07-23 | Full Rebuild | Cross-category collision: 370 of 1,190 initially-mapped products were already mapped under sibling category `makanankucing_my` (cat food, different source table) | Corrected scope from "any pet food" to "dog food only"; removed 413 cat-scoped products (see Cross-category discovery above) |
| 2026-07-23 | Full Rebuild | Residual 46 collisions: genuinely dog-labeled products also mapped (out-of-scope) under `makanankucing_my` | Dropped this session's duplicate rows rather than edit the other category's shipped data; flagged for future re-routing (see Targeted QA Fix Brief) |
| 2026-07-23 | Full Rebuild | Self-check QA gates (G1, G2, G5, placeholder-leak, structured-fields-NULL%, cross-master_table dual-map, dog/cat type-conflict spot-check) | All passed after corrections: 0 dual-mapped, 0 HUMAN+LLM coexistence, 0 provenance gaps, 0 placeholder leaks, 1% NULL `product_line`, 0 cross-master_table collisions, 0 dog↔cat type conflicts |
| 2026-07-23 | Top-up (coverage gap) | Live re-run of the 95%-cumulative-GMV worklist found 1,237 still-unmapped products (not 0, despite Status showing "Complete") — re-verified live per instructions rather than trusting the doc. Classified all 1,237 via bulk text rules: 405 already mapped under sibling `makanankucing_my` (cross-category, left untouched), 117 cat-scoped, 82 pet-non-food (flea/tick treatment, diapers, playpens, oral gel), 268 general-merchandise contamination (dairy, coffee, snacks, detergent, tissue, tea), 199 residual unresolved (mostly further general-merch/ambiguous-species long-tail, sampled and confirmed not dog-specific), 14 explicitly excluded (not-for-sale mystery-gift/sample-pack giveaways — one image-verified given its outsized nominal GMV, chocolate-candy contamination, non-food furniture/diapers, cat-dominant keyword-stuffed listing) | Mapped the remaining 152 genuine dog-food/treat/supplement products via bulk brand+size+pack grouping into 124 taxonomy entries (33 reused an existing global `brand_dict` brand incl. `BRD-UNBRANDED` for 9 genuinely brand-less generic listings, 41 new `BRD-MY-01128`–`BRD-MY-01168` brands minted for regional/house brands with zero prior taxonomy presence, e.g. Cesar, Addiction, Blue Bay, Elitas, BilJac — several long-established global pet-food brands that had never been mapped in this category before). Claimed SKU block SKU-151786–SKU-153022 (1,237 slots, `custom_topup`, only 124 used); wrote via `bq query` DML only, `meta_agent='CLAUDE_CODE'`, `source='LLM'`, no existing rows touched. Live gap reduced 1,237 → 1,085 (all reduction is genuine dog-scope coverage; the residual 1,085 is legitimately out-of-scope or unresolved, not an artifact). Self-check QA gates (G1, G2 without `--skip-coexistence`, G4 cross-category, G5, placeholder-leak, structured-fields-NULL%): all 0 except structured-fields-NULL% at 1% (well under the 50% threshold) |

| 2026-07-28 08:08 UTC | Automated review session (auto-discovery) | Automated Tier 1 SQL sweep over the 936-row never-reviewed/unconfident worklist found: 21 brand_casing_mismatch, 16 wrong_field_order (15 BRD-UNBRANDED entries missing the Unbranded prefix + 1 brand_id-casing gap), 1 duplicate_brand (brand token repeated inside product_line), 1 canonical_field_mismatch, and 99 null_size. STEP 2b found 5 genuine pack-count misses among 150 promo-language hits (rest correctly GWP). STEP 2c found 8 taxonomy buckets with a single consistent size/multiplier silently left at pack_count=1 (aggregation-checked, safe to write in place). A 30-row GMV-prioritized Tier 2 sample surfaced 5 entries wrongly on BRD-UNBRANDED despite a real, consistent packaging brand visible in the product image (Meals by StreetPaw, not previously in brand_dict), a generic-stub product_line, a triplicated token, 2 stray GWP-prefix leaks, and one cross-reseller duplicate stub. | Fixed 73 taxonomy entries (all _meta reset to unreviewed for next-run re-confirmation): 21 brand-casing corrections, 16 field-order/prefix corrections, 1 duplicate-brand cleanup, 4 canonical/product_line mismatch corrections (1 original + 3 self-caused regressions caught by re-running the sweep before finishing), 19 null_size backfills, 5 pack_count corrections for missed multipacks, 8 shared-bucket pack_count/size corrections, 1 new brand_dict entry (BRD-MY-01284, Meals by StreetPaw) with 5 taxonomy entries corrected off BRD-UNBRANDED, and 2 product_taxonomy_map rows rerouted from a duplicate stub to the correct fuller entry (no rows deleted). 24 additional sampled entries reviewed and confirmed correct, promoted unreviewed->unconfident (first review under this new _meta mechanism; none reached confident yet). Self-check gates: G1=0, G2=0, placeholder-leak=0, structured-fields-NULL%=1%, G5=0, G4=1 (confirmed identical to the pre-existing documented 24442421432 residual, not new). No qa_gate_exceptions needed. Residual: 80 null_size entries remain (down from 99), mostly lacking any legible weight signal in text or available image; deferred to a future session with more image-read budget. |
| 2026-07-28 08:55 UTC | Automated review session (auto-discovery) | STEP 1C fast-lane recheck of the 91-row fixed-pending-recheck bucket found 64 Tier-1-clean (bulk-promoted, no LLM judgment spent), 9 still null_size (left for normal path). STEP 2's Tier 1 sweep over the full 935-row worklist found 80 null_size defects and zero other mechanical defect classes (no stub_leak/duplicate_brand/wrong_field_order/brand_casing/excess_content/canonical_field_mismatch/garbage_brand). STEP 2b (532 promo-language pack_count=1 hits) confirmed all are GWP 'Free Gift' patterns, correctly pack_count=1 — no misses. STEP 2c (22 shared-bucket size/multiplier hits) confirmed all false positives (unit-mismatch or already-correct internal-packaging breakdowns). STEP 3's GMV-prioritized Tier 2 sample (top 60 Tier-1-clean rows, ranked by Shopee-only GMV per this table's documented Tiktok-GMV-corruption caveat) found 8 D3 variant-collapse defects (Royal Canin/NutriEdge multi-breed/multi-flavor seller listings incorrectly left is_multi_variant=false) and 1 D5 bug (a 'GIFTx30' free-shipping promo threshold misparsed as pack_count=30 on a single 13kg bag). Also found via image-read while resolving null_size: product 27977009949 (SKU-151890) carries a real on-package brand 'LLPET' despite being mapped to BRD-UNBRANDED. SKU-150407 (Greenies Pill Pockets)'s product image shows Feline-labeled packaging despite dual-species sku_name text — a possible species mis-scope, left unresolved (no-delete constraint, genuinely ambiguous whether the mapped product is truly dual-species) but flagged for a future session. Re-confirmed the documented platform-mislabeling issue (product_taxonomy_map.platform='Shopee' on rows whose actual source ecommerce_platform is 'Tiktok') while fetching images — already tracked, not re-investigated. | Fixed 33 null_size entries: 13 resolved from text already present in canonical_name/sku_name (chew/piece counts, 'XL Size', stick counts), 20 resolved via image reads (Petchef family 200g, Natural Core/Hegen 1kg, Rosy Fresh family 50g, Brit Mono Protein/Hepatic 400g, Hill's i/d 354g, Wanpy 100g×8, Popoo Probiotic 120 chews), plus 2 pack_count corrections uncovered alongside (Petchef Value Pack ×10, Wanpy ×8) and 4 promotions to is_multi_size=TRUE (Amelisa Bundle 10/20, Greenies Teenie 11/22/43, Honestbite N33 2kg/2.5kg dual bag, Greenies Pill Pockets multi-weight value pack). Fixed 8 D3 variant-collapse entries via is_multi_variant=TRUE (Royal Canin ×3, NutriEdge ×2, Wilderness Legend, Orijen, Natural Core — all multi-breed/multi-flavor buyer-choice listings bulk-matched into one entry at initial extraction). Fixed the GIFTx30 misparse (pack_count 30→1, canonical_name corrected) for SKU-150057. Minted new brand_dict row BRD-MY-01288 (LLPET) and corrected SKU-151890's brand_id + canonical_name off BRD-UNBRANDED. All fixed rows: meta_agent='CLAUDE_CODE', _meta reset to {"is_reviewed": false}. Bulk-promoted 64 fast-lane rows + 51 Tier-2-judged-correct rows via single STRUCT _meta UPDATEs (two statements total, not one-per-row). No new taxonomy entries minted — no SKU block claimed. Self-check gates: G1=0, G2=0 (no --skip-coexistence), placeholder-leak=0, structured-fields-NULL%=1%, G5=0, duplicate product_id=0. Confidence distribution after session: 43 confident, 93 unconfident, 1041 unreviewed. Residual: 47 null_size entries remain genuinely unresolvable this session (3 delisted source rows, ~30 zero-GMV long-tail TikTok listings with no legible size signal even after image read, plus a handful of GMV-bearing items — Paw-cipes, MYDOG, Hey!Bobo ×3, Duck Jerky, Yogurt Cube, WagglyCove Flavored, Tarokun — whose packaging text was too small/illegible to read confidently; Tarokun already investigated in a prior session per QA History). |
| 2026-07-28 09:55 UTC | Automated review session (auto-discovery) | Pre-fix independent QA report showed 4 canonical_name-fields failures (a translated-vs-literal Malay descriptor mismatch on a Royal Canin entry, a duplicated-brand-token entry, and two entries silently missing their size suffix). STEP 1C fast-lane recheck of the 35-row fixed-pending-recheck bucket found 30 genuinely Tier-1-clean (bulk-promoted, no LLM judgment spent) and 5 still null_size. STEP 2's full Tier 1 sweep over the 888-row worklist found zero stub_leak/duplicate_brand/wrong_field_order/brand_casing/excess_content/garbage_brand hits and 40 null_size defects (mechanical fields all otherwise clean after the 4 initial fixes). STEP 2b's 506 promo-language pack_count=1 hits were overwhelmingly correct GWP except one duplicate-brand-in-product_line defect (Amelisa) surfaced incidentally. STEP 2c's shared-bucket sweep found 6 candidate rows, all false positives (unit-mismatch or already-correct) except one more duplicate-brand-in-product_line defect (Cassiel Pet, a second entry with the identical bug) that turned out to be a true duplicate of an already-existing taxonomy entry once compared side by side. A 30-row GMV-prioritized Tier 2 sample (Shopee-only GMV per this table's documented Tiktok-GMV-corruption caveat) surfaced a significant find: 4 taxonomy entries (STweet, Pro Mill's, Trendy Woof, Yamagold — 4 genuinely different real brands) had all been merged onto one fabricated brand_dict row 'DVS STweet', where 'DVS Approved' (a Malaysian Dept. of Veterinary Services certification stamp, not a brand) had been misread as brand text and concatenated with the first product's real brand, then reused for three unrelated products. Also found: 2 Chunk Bits entries with promo/shipping-threshold text ('GIFT +FREE SHIP...', 'FREE') contaminating product_line/canonical_name (one had pack_count misparsed 30→1, the same GIFTx30 bug class already fixed once before on a different SKU), 1 Rosy Fresh entry with the reseller's storefront name ('The Neko Pet Shop') leaking into canonical_name (a §11 violation), and 1 Notti entry with a dangling stray '&' left over from stripping 'Dog & Cat' text. | Fixed 30 taxonomy entries total: the original 4 canonical_name-fields failures; 13 null_size entries resolved via image reads (Viasana home-cooked and Fussy-Eater lines confirmed 200g across both lines via packaging zoom-crop, MoeMagic 200g, a Freeze-Dried Salmon Fillet 100g, Urbanwolf Ranch Raised Lamb 10kg, a Cassiel Pet dewormer tablet '1 Tablet' plus a stray-fragment product_line cleanup, and a count-based '50pcs' sausage box); 2 products promoted to is_multi_size=TRUE where the product image showed a genuine buyer-selectable size range (S/M/XL/XXL bones; 8/10/12/20/30cm rawhide bones) rather than a single missing value; 3 duplicate-brand-in-product_line cleanups (2 Cassiel Pet entries, 1 Amelisa Pet & Co entry), one of which turned out to be a true duplicate of an existing entry — rerouted the duplicate product's map row rather than deleting either taxonomy row; the 4-way DVS STweet brand mis-resolution, fixed by minting one new brand_dict row (BRD-MY-01289, STweet) and re-pointing the other 3 entries to their correct pre-existing brand_dict rows (Pro Mill's, Trendy Woof, Yamagold) with corrected canonical_name/product_line; 2 Chunk Bits promo-text-contamination cleanups (one including a pack_count 30→1 correction); 1 merchant-name-leak cleanup (Rosy Fresh); 1 cosmetic dangling-ampersand cleanup (Notti). All fixed rows: meta_agent='CLAUDE_CODE', _meta reset to unreviewed. Separately bulk-promoted 30 STEP-1C fast-lane rows and 23 Tier-2-judged-correct rows via two single STRUCT _meta UPDATE statements (not one-per-row). No SKU block claimed — no new taxonomy entries were minted, only existing ones corrected or rerouted. Self-check of every gate from the pre-fix report plus G1/G2/G4/duplicate-product-id: all 0 except structured-fields-NULL% (1%, unchanged, well under the 50% threshold) and G4 (1, confirmed via direct lookup to be the same pre-existing residual '24442421432' documented in every prior session, not new). Confidence distribution after session: 67 confident, 96 unconfident, 771 unreviewed (up from 43/93/1041 pre-session — the 30 fast-lane and Tier-2-correct promotions plus the fixed rows' resets account for the shift). Residual: 25 null_size entries remain (down from 40) — 2 confirmed delisted (SKU-149590, SKU-150617 and SKU-151895's product all return 0 rows from the source table), several matching the prior session's already-documented illegible-packaging class (Paw-cipes, MYDOG, Hey!Bobo x3, Duck Jerky, Yogurt Cube, WagglyCove, Tarokun, LLPET — re-confirmed this session, no new information to change that verdict), and the rest (Lemo, Pet Record cans, Yamato Mart rawhide, drdingo strips, Alps Natural dental-chew variety grid, Fatty Cat Mall collage banners, Dr. Dingo twists) genuinely have no legible weight anywhere in their product images even after crop/zoom — deferred to a future session with fresh image sourcing, since these are stock/marketing photos rather than packaging shots. |
| 2026-07-28 10:26 UTC | Automated review session (auto-discovery) | Automated review of the 867-row never-reviewed/unconfident worklist for makanananjing_my (custom source makanananjing.9_makanananjing_my_daily, Shopee+Tiktok). STEP 1C fast-lane recheck of the 30-row fixed-pending-recheck bucket bulk-promoted 24 Tier-1-clean rows (6 still null_size, left for normal path). STEP 2's Tier 1 sweep found zero stub_leak/duplicate_brand/wrong_field_order/brand_casing/excess_content/garbage_brand hits and 25 null_size defects: 3 confirmed delisted (SKU-149590, SKU-150617, SKU-151895 — zero rows in source table) and 22 reconfirmed genuinely unresolvable (spot-checked the highest-GMV instance, Alps Natural Dental Chews SKU-190407, via fresh image download+read — reproduced the exact 'no legible weight, marketing collage' verdict already documented by 3 prior sessions). STEP 2b/2c's mechanical sweeps were both largely false positives (single-unit '(N x 1 Pack)' notations, internal-packaging breakdowns, unit-mismatch between lb/kg) EXCEPT one systemic bug this session's manual review surfaced: '[FREE GIFT xN]'/'[GIFT xN]' promotional-threshold text in sku_name gets misparsed as pack_count=N — the same root cause as a GIFTx30 bug fixed once before in this category, but recurring across 14 more taxonomy entries (mostly Chunk Bits and Brit Care) not caught by any existing Tier 1 flag. Also found: a Tier 1 SQL gap where NULL product_line makes the canonical_field_mismatch check silently evaluate to NULL instead of TRUE/FALSE (missing the IS NOT NULL guard that sub_line/variant have), so it never mechanically flags such rows; manually reviewed all 11 worklist rows with product_line IS NULL and found 2 genuine defects (Pedigree Dentastix missing its real product line) plus 5 D3 variant-collapse cases (Royal Canin x3, Orijen, Vitalplus with life-stage/formula buyer-choice listings left is_multi_variant=false). A 70-row GMV-prioritized Tier 2 sample (Shopee-only GMV per this table's documented Tiktok-GMV-corruption caveat) surfaced 7 more defects: 2 Pet Cuisine entries with duplicated brand-name fragments in product_line, 2 Chunk Bits promo-text contaminations, 1 Orijen entry with garbled triplicated variant text plus a missed variant-collapse, 1 Bravery entry with the gift-misparse bug plus a missed variant-collapse, and 1 Chunk Bits entry with product_line left as the Chinese generic word '狗粮' ('dog food') — a Tier-C-style stub an English-only regex can't catch (found a second instance, Alps Natural, via a manual scan). Two self-caught process errors during this session, both corrected before finalizing: (a) 6 rows got double-touched by two separate bulk-promote statements in the same session, incorrectly reaching 'confident' from what was really one review — demoted back to 'unconfident'; (b) two independent product_line fixes (Brit Care 'Adult Senior' and 'Care Adult Senior') converged on byte-identical canonical_name/product_line/size, creating an undetected duplicate taxonomy entry — caught before shipping and resolved by rerouting the map row rather than deleting either taxonomy row. | Fixed 28 taxonomy entries total (all _meta reset to unreviewed for next-run re-confirmation, meta_agent='CLAUDE_CODE'): 2 Pedigree Dentastix product_line/canonical corrections; 7 is_multi_variant=TRUE corrections for life-stage/formula buyer-choice listings (Royal Canin x3, Orijen x2, Vitalplus, Bravery x2); 2 Pet Cuisine duplicate-brand-fragment cleanups; 12 gift-misparse pack_count corrections (all reset to pack_count=1, promo text stripped from product_line/canonical_name — Chunk Bits x8, Brit Care x4, Bravery already counted above); 2 Chinese-generic-stub cleanups ('狗粮' stripped — Chunk Bits, Alps Natural). One true duplicate resolved by rerouting product_taxonomy_map (SKU-150565's single product rerouted to SKU-150564; no taxonomy or map rows deleted, per STEP 7). Bulk-promoted 80 unique taxonomy_ids as reviewed-and-correct (no fix needed) via 3 STRUCT _meta UPDATE statements: 24 STEP 1C fast-lane + 63 Tier-2-judged-correct, net of 6 double-touched and 1 later fixed (24+63-6-1=80). No new taxonomy entries minted, no SKU block claimed. Self-check gates after all fixes: G1 dual-mapped=0, G2 HUMAN+LLM coexistence (no --skip-coexistence)=0, placeholder-leak=0, structured-fields-NULL%=1%, G5 provenance=0, duplicate product_id=0, G4 cross-master_table=1 (confirmed identical to the pre-existing documented residual 24442421432, not new). Confidence distribution after session: 75 confident, 159 unconfident, 32 pending-recheck, 668 unreviewed (of 934 total taxonomy entries for this category). |
| 2026-07-28 15:23 UTC | Automated review session (auto-discovery) | Tier 1 sweep over the 858-row worklist found 4 missing-product_line entries and 25 null_size entries (8 freshly image-verified via TikTok CDN — all genuinely unresolvable marketing-collage photos, no legible weight). STEP 2b/2c sweeps confirmed no genuine pack/size misses. A GMV-prioritized 60-row Tier 2 sample found 11 real D1-D3 defects (garbled Hill's trademark symbols + Malay-glue text, 5 missed is_multi_variant flags, duplicate-brand text, truncated 'MADE IN' text). Most significant finding: 16 taxonomy entries had a reseller storefront name ('CATQUEEN'/'POODEE') mistakenly resolved as the product brand (llm-extraction-rules.md §11 violation) — tracing real brands (Nature's Protection, Belif Smartcare, Nourish, Chunk Bits, IQ, Spotty, Hug, Iskhan, Molly, Michio, Cesar, Bravery) surfaced a further pre-existing bug where 'Belif Smartcare' and 'Molly' pet-food brands had been silently merged into unrelated GLOBAL/SG brand_dict entries (Korean skincare 'belif', Thai haircare 'molly'), plus 3 exact-duplicate products (same item listed by two different resellers, each resolved to its own storefront name). Also found brand_dict itself had 'Belcando dog food' baked in as a brand's canonical_name (11 taxonomy rows affected). | Promoted 16 Tier-1-clean fast-lane rows. Fixed 4 missing-product_line entries (VitalPlus x2, Chunk Bits, Trendy Woof) as genuine multi-variant listings. Fixed 11 Tier-2-discovered defects (Hill's x3, VitalPlus, Orijen, Bravery, Amelisa, Iskhan, Cesar, Wilderness Legend, PWF). Fixed brand_dict 'Belcando dog food' -> 'Belcando' and propagated to 11 taxonomy rows. Minted 5 correctly-scoped new brand_dict rows (BRD-MY-01294 Belif Smartcare, 01295 Molly, 01296 IQ, 01297 Hug, 01298 Spotty) and rebranded 16 CATQUEEN/POODEE-mislabeled entries plus 3 pre-existing belif-contaminated entries to their real brands; merged 4 exact-duplicate products via product_taxonomy_map reroutes (no rows deleted) to pre-existing correct entries (150284, 150274, 149902, 151904) plus one mis-matched Monte product rerouted to 151827. Caught and fixed 4 self-caused brand-casing/apostrophe regressions via immediate post-fix gate re-check. Bulk-promoted 84 taxonomy_ids to confident (STEP 5 Path 1) and 18 genuinely-unresolvable null_size rows to unconfident. No new SKU block claimed (no new taxonomy entries minted, only existing ones fixed/rerouted/rebranded). Self-check gates: G1=0, G2=0, placeholder-leak=0, structured-fields-NULL%=1%, G5=0, duplicate product_id=0, G4=1 (confirmed identical to the pre-existing documented residual 24442421432, not new). Confidence distribution: 234 confident, 777 unreviewed, 154 unconfident, 12 pending_recheck (up from 110/822/225/20 pre-session). |
| 2026-07-28 15:43 UTC | Automated review session (auto-discovery) | STEP 1C fast-lane recheck of the 12-row fixed-pending-recheck bucket found 0 rows clean enough for bulk promotion (unlike prior sessions) — 6 tripped null_size (already-documented illegible-packaging cases) and 6 tripped a genuine, previously-undetected Tier-1 SQL gap: blank/NULL product_line silently evaluates the canonical_field_mismatch check to NULL instead of TRUE, so these rows (Royal Canin x3, Orijen, Vitalplus, Bravery multi-formula listings, all missing their structured product_line despite is_multi_variant precedent existing elsewhere in this category) were never mechanically flagged before. Fixed the query to treat NULL product_line as a positive hit and re-ran the full 772-row Tier 1 sweep, surfacing 8 canonical_field_mismatch rows total and 25 null_size rows (matching the prior session's residual count exactly — no drift). Image-verified the Chunk Bits '(unresolved)' stub (SKU-150065) and the fresh 5th-top-up null_size candidates (LEMO, Pet Record, Viasana) via TikTok/Shopee CDN — all reconfirmed genuinely unresolvable marketing-collage photos with no legible weight; also reconfirmed the already-tracked platform-mislabeling bug (product_taxonomy_map.platform='Shopee' on TikTok-sourced rows) blocks direct image lookup for most of the fresh SKU-190xxx null_size entries, consistent with prior sessions' finding — not re-investigated, out of scope. STEP 2b (433 promo-language hits) and STEP 2c (16 shared-bucket hits) found zero new genuine misses — all were GWP free-gift patterns already correctly pack_count=1, or internal-packaging-breakdown/x1-pack/lb-kg-unit-mismatch false positives. STEP 3's 60-row GMV-prioritized Tier-2 sample (Shopee-only GMV per this table's documented TikTok-GMV-corruption caveat) surfaced: two true-duplicate reseller listings (JerHigh 70g treat merged into its clean twin; Royal Canin Poodle Adult Breed Health Nutrition 3kg merged into its clean twin), a Vitalplus 18kg true duplicate found during STEP 1C's investigation, promo-text contamination (Chunk Bits '**Pay 13kg Get 17kg**' FOC threshold, Iskhan '*FREE GIFT*' prefix), two Royal Canin entries with a translated-vs-literal Chinese descriptor leaking into canonical_name (French Bulldog Adult, Shih Tzu Adult — same defect class already fixed once on a different entry in a prior session, recurring on two more), a reseller-storefront-name leak ('The Neko Pet Shop' on a Chun Fu entry, a section-11 violation) plus embedded untranslated Chinese noise on two other entries, stray punctuation artifacts (dangling '&', stray '{ }' braces, garbled repeated text), and a genuine D5 bug on two entries (Iskhan, Probalance) where a sub-pack size (300g/400g) had been written into the size field as if it were the outer total (6kg/9.6kg) while pack_count correctly held the sub-pack count — inflating the true total by ~20x if taken at face value. Most significant find: 10 taxonomy entries were split across two brand_dict rows for the identical real-world brand 'Hug' — the older BRD-MY-01037 ('Hug Dog', pre-dating the fix) and BRD-MY-01297 ('Hug', correctly minted in the immediately-prior 2026-07-28 15:23 session when tracing CATQUEEN/POODEE reseller-name mis-resolutions) — every affected canonical_name/product_line also duplicated 'Hug' as a token (e.g. 'Hug Dog Hug Canned...') since the product_line itself had been extracted starting with the brand name again. | Fixed 29 taxonomy entries total (all _meta reset to {"is_reviewed": false} then re-verified same-session, meta_agent='CLAUDE_CODE'): 6 blank-product_line multi-variant entries backfilled from bracket/flavor-list text in sku_name (Royal Canin x3, Orijen, Bravery, plus Natural Signature found via the same query bug); 1 Chunk Bits '(unresolved)' consistency fix (product_line set to match canonical_name's already-correct '{Brand} (unresolved)' pattern, per the documented no-guess rule — confirmed via image the underlying listing is a genuine multi-line marketing collage, nothing to resolve); 1 promo-text cleanup (Chunk Bits FOC threshold); 1 reseller-storefront-name + embedded-Chinese cleanup (Chun Fu); 1 Chinese-noise-in-otherwise-complete-name cleanup (Brit Pate & Meat); 2 translated-descriptor Chinese-text strips (Royal Canin French Bulldog, Shih Tzu); 1 garbled-text cleanup (Royal Canin Recovery Canned, also confirmed correct dual-species Dog & Cat scope per this doc's precedent); 1 dangling-ampersand cleanup (Wanpy); 1 Chinese-noise-in-product_line cleanup (Notti, caught a self-introduced product_line/canonical_name mismatch on immediate recheck and corrected it before finalizing); 1 stray-brace cleanup (Rich.Co); 2 genuine D5 sub-pack/total-pack corrections (Iskhan 6KGx20→300gx20, Probalance 9.6kgx24→400gx24, both confirmed against sku_name's own '(unit x count)' parenthetical); 1 LEMO raw-title cleanup (structured into a proper name; size stays unresolved — no legible weight in any source signal). Merged the 'Hug Dog'/'Hug' brand duplication: reassigned all 10 taxonomy entries from BRD-MY-01037 to BRD-MY-01297 and stripped the resulting duplicate brand token from each product_line/canonical_name (no new brand_dict row minted — reused the correct existing one). Resolved 4 true-duplicate reseller listings by rerouting the single product_taxonomy_map row of the losing entry to the correct surviving entry (Vitalplus 18kg, JerHigh 70g treat, Royal Canin Poodle Adult 3kg, Hug Wet Food Pouch 100g) — zero taxonomy or map rows deleted, per STEP 7; the 4 losing stub entries (SKU-149986, SKU-150195, SKU-149708, SKU-150101) are left standing unused, _meta untouched. Same-session gate-verify re-ran Tier 1 on all 28 directly-edited entries: 26 came back fully clean immediately, 1 (Notti) needed one follow-up correction before it was clean, 1 (LEMO) stays flagged null_size as expected (genuinely unresolvable). Promoted via 2 bulk STRUCT _meta UPDATE statements (not one-per-row): Path 1 — 64 taxonomy_ids (37 never-reviewed rows that were both Tier-2-judged correct and Tier-1-clean, plus 27 rows fixed and confirmed clean this session) straight to confident; Path 2 — 9 re-reviewed rows whose prior stored verdict already agreed 'correct', likewise promoted to confident. No SKU block claimed — no new taxonomy entries were minted, only existing ones corrected, rerouted, or rebranded. Self-check gates: G1 dual-mapped=0, G2 HUMAN+LLM coexistence (no --skip-coexistence)=0, placeholder-leak=0, structured-fields-NULL%=0%, G5 provenance=0, duplicate product_id=0, G4 cross-master_table=1 (confirmed identical to the pre-existing documented residual 24442421432, not new — this session's own 4 rerouted product_ids introduced zero new cross-table collisions). Confidence distribution after session: 343 confident, 693 unreviewed, 136 unconfident, 5 pending-recheck (up from 234/777/154/12 pre-session; totals reconcile exactly at 1,177 map-row-fanout count both before and after). |
| 2026-07-28 17:27 UTC | Automated review session (auto-discovery) | Auto-discovery review of the 695-taxonomy-entry (1,177 map-row-fanout) never-reviewed/unconfident worklist for makanananjing_my (custom source makanananjing.9_makanananjing_my_daily, Shopee+Tiktok). STEP 1C fast-lane recheck of the 5-row fixed-pending-recheck bucket: all 5 still tripped null_size (already-documented illegible-packaging/delisted cases), none promoted. STEP 2's full Tier 1 sweep found zero stub_leak/duplicate_brand/wrong_field_order/brand_casing_mismatch/excess_content/canonical_field_mismatch/garbage_brand hits and 25 null_size defects — 3 previously confirmed delisted (SKU-149590/150617/151895) and the remaining 22 (including 8 freshly-fetched TikTok CDN images this session) reconfirmed genuinely unresolvable marketing-collage photos with no legible weight, matching 3+ prior sessions' verdicts on the same entries. STEP 2b (394 promo-language pack_count=1 hits) and STEP 2c (13 shared-bucket size/multiplier hits) found zero new genuine misses — all GIFTxN/FREE-threshold or 'x 1 Pack'/unit-mismatch false positives, consistent with established patterns. A 60-row GMV-prioritized Tier 2 sample (Shopee-only GMV per this table's documented TikTok-GMV-corruption caveat) surfaced a significant brand_dict defect: 31 of this category's Nature's Protection entries (BRD-SG-00100) resolved to a mis-cased/curly-quote brand_dict.canonical_name ('NATURE‘S PROTECTION') distinct from a second, correctly-cased brand_dict row for the same real brand (BRD-SG-02886, 'Nature's Protection') — invisible to the Tier 1 casing regex because it compares canonical_name against brand_dict's own (already-wrong) value. Also found within that same brand cluster: 2 embedded-Chinese-descriptor contaminations, 1 more Chinese-descriptor case, 2 stray-brace artifacts, 1 stray emoji, and 2 duplicate-brand-fragment ('Nature Protection' repeated) entries. Elsewhere in the sample: 6 D3 variant-collapse misses (is_multi_variant left false on genuine buyer-choice-of-flavor listings: Probalance x3, BOCO, Trendy Woof, IQ Dog), 1 misleading single-flavor claim on a true 3-flavor catch-all (BOCO), 1 FREE-SHIPPING promo-text contamination (Chunk Bits), 1 reseller-store-name leak per §11 ('NEWB PET STORE', Chunk Bits), 1 marketing-noise contamination ('New Arrive !!'/'Ship in 24h'/emoji, JerHigh — also extracted a genuine 'Wheelers' shape variant), 1 Chinese-generic-descriptor stub ('狗粮/狗干粮/狗饼', IQ Dog), and 1 duplicated-brand-token defect ('IQ IQ', IQ Dog). Verified two cat-mention candidates (SKU-190362, SKU-190353) as genuine dual-species listings per this doc's existing precedent, not cat-scope violations — left in scope. Confirmed the pre-existing G4 residual (24442421432, collision with makanankucing_my) is unchanged, not a new collision. | Fixed 42 taxonomy entries total (all _meta reset to unreviewed then re-verified same-session, meta_agent='CLAUDE_CODE'): brand_dict.canonical_name corrected for BRD-SG-00100 ('NATURE‘S PROTECTION' → 'Nature's Protection', scoped fix — verified only 31 of the brand's global taxonomy rows belong to this category before touching anything, avoiding a cross-category scope violation since the same brand_id is also used by shopee_sg_pet_food); bulk REGEXP_REPLACE corrected the brand prefix on all 31 in-scope Nature's Protection entries; 7 of those also got targeted noise cleanup (2 Chinese-descriptor strips, 1 more Chinese-descriptor strip, 2 stray-brace strips, 1 emoji+brace strip, 2 duplicate-brand-fragment strips). 6 is_multi_variant=TRUE corrections for buyer-choice-of-flavor listings (Probalance x3, BOCO, Trendy Woof, IQ Dog) plus 1 misleading-single-flavor-claim correction on the BOCO entry (dropped the arbitrary 'Salmon' pick from a genuine 3-flavor catch-all). 5 individual text corrections: FREE-SHIPPING strip (Chunk Bits), reseller-store-name strip (Chunk Bits 'NEWB PET STORE'), promo-noise strip + Wheelers variant extraction (JerHigh), Chinese-generic-stub strip (IQ Dog), duplicate-brand-token fix (IQ Dog 'IQ IQ'→single). Same-session gate-verify re-ran Tier 1 on all 42 directly-edited entries: all came back fully clean immediately. Bulk-promoted via 2 STRUCT _meta UPDATE statements (not one-per-row): Path 1 — 82 taxonomy_ids (40 never-reviewed rows both Tier-2-judged correct and Tier-1-clean, plus 42 rows fixed and confirmed clean this session) straight to confident; Path 2 — 8 re-reviewed rows whose prior stored verdict already agreed 'correct', likewise promoted to confident. No SKU block claimed — no new taxonomy entries minted, only existing ones corrected. Self-check gates: G1 dual-mapped=0, G2 HUMAN+LLM coexistence (no --skip-coexistence)=0, placeholder-leak=0, structured-fields-NULL%=0%, G5 provenance=0, duplicate product_id=0, G4 cross-master_table=1 (confirmed identical to the pre-existing documented residual 24442421432, not new). Confidence distribution after session (map-row-fanout count): 493 confident, 557 unreviewed, 122 unconfident, 5 pending-recheck (up from 343/693/136/5 pre-session; totals reconcile exactly at 1,177 both before and after). Residual: 25 null_size entries remain (unchanged count, same identities as pre-session) — all re-confirmed genuinely unresolvable this session via image or prior-session lookup; no further action possible without a new image source or product delisting resolution. |
| 2026-07-28 17:42 UTC | Automated review session (auto-discovery) | Automated review of the 613-row never-reviewed/unconfident worklist for makanananjing_my (custom source makanananjing.9_makanananjing_my_daily, Shopee+Tiktok). STEP 1C fast-lane recheck of the 5-row fixed-pending-recheck bucket: all 5 still tripped null_size (SKU-151889/151897/151890/151895/190382 - the already-documented illegible-packaging/delisted cases), none promoted. STEP 2's full Tier 1 sweep found zero stub_leak/duplicate_brand/wrong_field_order/brand_casing_mismatch/excess_content/canonical_field_mismatch/garbage_brand hits and 25 null_size defects (3 confirmed delisted via source-table lookup: SKU-149590/150617/151895 return zero rows; the remaining 22 match prior sessions' identities exactly, genuinely unresolvable marketing-collage photos - not re-investigated this session). STEP 2b (375 promo-language pack_count=1 hits across 99 taxonomy entries) found zero genuine misses - all internal-packaging-breakdown false positives. STEP 2c (13 shared-bucket size/multiplier hits) found zero genuine misses. A 45-row GMV-prioritized Tier 2 sample surfaced 24 genuine defects: (1) a Tier-1-regex gap - 'All Flavors'/'Assorted Flavors' in canonical_name is the identical banned generic-stub class as 'All variant'/'All size' but was never checked for; found 5 instances. (2) 9 D3 variant-collapse misses - genuine slash/comma-separated buyer-choice multi-formula or multi-breed listings left is_multi_variant=false. (3) 2 entries genuinely stating both multiple sizes AND multiple flavors as buyer-choice, needing both flags plus size cleared to NULL. (4) 8 text-quality defects: duplicate/repeated brand tokens, embedded CJK descriptive noise, and one promo/shipping-tier-code contamination. 4 additional sampled rows were reviewed but left genuinely ambiguous - not promoted, not fixed, deferred to a future session. Self-check gates: G1=0, G2=0, placeholder-leak=0, structured-fields-NULL%=0%, G5=0, duplicate product_id=0, G4=1 (confirmed identical to the pre-existing documented residual 24442421432, not new). | Fixed 24 taxonomy entries (all _meta reset then re-verified same-session, meta_agent='CLAUDE_CODE', no rows deleted): 5 'All Flavors' stub removals + is_multi_variant=TRUE; 9 is_multi_variant=TRUE flag corrections; 2 combined is_multi_size+is_multi_variant corrections with size cleared to NULL; 8 text-quality cleanups (duplicate brand tokens, CJK noise, one promo-code-vs-real-brand-signal correction). One self-caused size-formatting regression caught and fixed on immediate recheck. Bulk-promoted via 2 STRUCT _meta UPDATEs: 43 taxonomy_ids to confident (19 never-reviewed+clean, 24 fixed+clean), 2 re-reviewed rows agreeing with prior verdict also promoted. No new taxonomy entries minted, no SKU block claimed. Confidence distribution after session: 357 confident, 109 unconfident, 5 pending-recheck, 454 unreviewed (up from 312/114/5/494 pre-session). |
| 2026-07-28 18:06 UTC | Automated review session (auto-discovery) | Automated review of the 568-row never-reviewed/unconfident worklist for makanananjing_my (custom source makanananjing.9_makanananjing_my_daily, Shopee+Tiktok). STEP 1C fast-lane recheck of the 5-row fixed-pending-recheck bucket: all 5 still tripped null_size (SKU-151889/151890/151895/151897/190382, already-documented illegible-packaging cases), none promoted. STEP 2's full Tier 1 sweep (with the NULL-product_line canonical_field_mismatch guard applied) found zero stub_leak/duplicate_brand/wrong_field_order/brand_casing_mismatch/excess_content/canonical_field_mismatch/garbage_brand hits and 25 null_size defects — 3 reconfirmed delisted via live source-table lookup (SKU-149590/150617/151895, zero rows), the remaining 22 matching prior sessions' identities exactly (genuinely unresolvable marketing-collage/illegible-packaging photos). STEP 2b (346 promo-language pack_count=1 hits) and STEP 2c (7 shared-bucket size/multiplier hits) found zero genuine misses (GWP free-gift patterns and 'x 1 Pack' internal-packaging false positives, consistent with established patterns). A GMV-prioritized 80-row Tier 2 sample (Shopee-only GMV per this table's documented TikTok-GMV-corruption caveat) surfaced a text-quality defect class never previously swept mechanically across the full worklist: emoji (🐾❤️🐕‼️✨), pipe characters, promo-marker noise (*FREE GIFT*, NEW ARRIVE!!, a merchant-storefront-leak fragment 'STORE!!' on a BACK2NATURE entry), and embedded duplicate-of-English Chinese descriptive text (狗粮/皇家狗粮/狗罐头/etc.) contaminating 81 canonical_name/product_line pairs. A full-worklist regex sweep for these patterns (not just the 80-sample) found all 81 affected rows. Individual translation review of the CJK-heavy subset surfaced genuine content losses requiring reconstruction rather than blind stripping: 2 Chunk Bits entries missing 'Small Breed'/'Puppy' (小型犬/幼犬 signal), 1 ROSY FRESH entry missing 'Puppy' (幼犬粮), 1 PETSEE DOCILE entry with real unique content (Shiba Inu breed, freeze-dried egg yolk, duck meat, triple blend) that a blind strip would have destroyed, 2 more PETSEE entries with garbled duplicate-token English fragments alongside the CJK duplication (DR GLINT 'Pomeranian Pomeranian', DR KERES 'poodle poodle toy poodle'), and 1 garbled Bidy dual-species (dog+cat) snack listing. Also found: 1 duplicate-brand-token defect (IQ Dog IQ Canned...), 1 genuine D4/D5 bug on a Pedigree Dentastix entry where a dog-weight-suitability range ('10-25kg') had been misread as the product size instead of the real '344g/14pcs' stated in the same sku_name — plus a missing 'Dentastix' product-line name. Found 7 D3 variant-collapse misses: 4 Chunk Bits multi-formula-code catch-alls (22/23/24/25/26/27/30 buyer-choice codes), 1 Probalance '2 Flavours' listing, 1 Brit 'Adult M / Adult L / Sport' 3-way buyer-choice, plus 1 Zignature listing stating 3 distinct sizes (1.8/5.6/11.3kg) needing is_multi_size instead of a single (wrong) size. Found 2 true-duplicate reseller listings via this review: SKU-149999 (Petchef Duck, missing the DUCK flavor token in its own extraction) duplicating SKU-149997 (same flavor, correctly extracted); and a Pet Cuisine dog+cat dual-species multi-formula/multi-size catch-all (SKU-150087) duplicating an equivalent entry (SKU-150085) once both were corrected to the same multi-variant/multi-size structure. Self-check gates: G1 dual-mapped=0, G2 HUMAN+LLM coexistence (no --skip-coexistence)=0, duplicate product_id=0, placeholder-leak=0, structured-fields-NULL%=0%, G5 provenance=0, G4 cross-master_table=1 (confirmed identical to the pre-existing documented residual 24442421432, not new). | Fixed 85 taxonomy entries total (all _meta reset to {"is_reviewed": false} then re-verified same-session via immediate Tier 1 recheck, meta_agent='CLAUDE_CODE'): bulk REGEXP_REPLACE mechanical cleanup (emoji/pipe/promo-marker/dangling-ampersand/duplicate-CJK-descriptor strip) across 81 canonical_name+product_line pairs in one statement; 14 individual content-preserving rewrites for rows where blind stripping would have destroyed genuine signal (Small Breed/Puppy life-stage tokens, a real breed+ingredient descriptor set on PETSEE DOCILE, garbled duplicate-token cleanup on 2 more PETSEE entries, a Bidy dual-species rewrite, an IQ Dog duplicate-brand-token fix, a Wanpy/Bravery/Brit cleanup); 1 D4/D5 correction (Pedigree Dentastix: size 25kg→344g, product_line/canonical_name corrected to include 'Dentastix'); 7 is_multi_variant/is_multi_size flag corrections for buyer-choice listings (4 Chunk Bits formula-code catch-alls, Probalance 2-flavours, Brit 3-way life-stage/sport choice with variant text added, Zignature 3-size); 1 Pet Cuisine entry rebuilt as a proper multi-variant+multi-size catch-all. Immediate post-fix Tier 1 gate-verify on all 85 directly-edited entries found 2 initial regressions from an incomplete dangling-ampersand regex (end-of-string '&' wasn't matched by the mid-string pattern) affecting 3 rows (2 of the 85 plus discovery that this bug is pre-existing and out-of-scope in ~50 other categories' rows, left untouched since only this table's rows are in scope) — corrected with a second targeted regex pass, re-verified clean. Resolved 2 true-duplicate reseller listings by rerouting the single product_taxonomy_map row of the losing entry to the correct surviving entry (product 46156824871 SKU-149999→SKU-149997; product 8313575699 SKU-150087→SKU-150085) — zero taxonomy or map rows deleted, per STEP 7; the 2 losing stub entries are left standing unused, _meta untouched. Final gate-verify: 83 of 85 touched entries came back fully clean; the remaining 2 (SKU-150617 confirmed delisted, SKU-190426 already-documented illegible-packaging) keep _meta at {"is_reviewed": false}, unresolved, same as before this session. No new taxonomy entries minted, no SKU block claimed — every fix was a correction, rewrite, or reroute of an existing entry. Bulk-promoted via 2 STRUCT _meta UPDATE statements (not one-per-row): Path 1 — 134 taxonomy_ids (51 never-reviewed rows both Tier-2-judged correct and Tier-1-clean from the 80-row GMV sample, plus 83 rows fixed and confirmed clean this session) straight to confident; Path 2 — 12 re-reviewed rows (previously unconfident, judged correct again this session, prior verdict already agreed 'correct') likewise promoted to confident. Confidence distribution after session (taxonomy-entry level / map-row-fanout level): 501/717 confident, 83/89 unconfident, 7/7 pending-recheck, 332/364 unreviewed (fanout total reconciles at 1,177, matching prior sessions' established denominator). Residual: the 25 null_size entries (3 confirmed delisted, 22 genuinely unresolvable marketing-collage/illegible-packaging photos) are unchanged in count and identity from before this session — deferred to a future session with fresh image sourcing, as documented across 5+ prior sessions. |
| 2026-07-29 02:40 UTC | Automated review session (auto-discovery) | Automated review of the 422-row never-reviewed/unconfident worklist for makanananjing_my (custom source makanananjing.9_makanananjing_my_daily, Shopee+Tiktok). STEP 1C fast-lane recheck of the 7-row fixed-pending-recheck bucket: all 7 still tripped null_size (already-documented illegible-packaging/delisted cases), none promoted. STEP 2's full Tier 1 sweep found zero new mechanical defects (no stub_leak/duplicate_brand/wrong_field_order/brand_casing_mismatch/excess_content/canonical_field_mismatch/garbage_brand hits) and the same 25-row null_size residual documented across 8+ prior sessions (3 confirmed delisted, 22 genuinely unresolvable marketing-collage/illegible-packaging photos). STEP 2b (210 promo-language pack_count=1 hits) found one genuine miss after filtering false positives: SKU-150196 (Pedigree Dentastix), whose sku_name states two buyer-choice sizes ('210g, 344g') but the entry had captured only 210g as a fixed single value. STEP 2c (7 shared-bucket hits) found zero genuine misses (regex false positives on already-correctly-sized entries). A 180-row GMV-prioritized Tier 2 sample (Shopee-only GMV per this table's documented Tiktok-GMV-corruption caveat) surfaced 19 further defects: 2 Carnilove entries (a GWP-freebie-text leak with a genuinely ambiguous 12kg/6kg buyer-choice size mis-recorded as a fixed 12kg x2, and a garbled 'Can can' product_line fragment from 'Wet Can Food'), 1 Hill's entry with stray registered-trademark symbols baked into the text, 2 PURINA Supercoat entries with a trailing '2 x'/'3 x' fragment leaked into product_line — one of which also had the wrong pack_count (sku_name states '3 x 2.7kg' but the entry held pack_count=2), a family of 9 Chunk Bits multi-formula-code buyer-choice catchalls missing is_multi_variant=TRUE (2 of which also stated multiple sizes in their own sku_name and needed is_multi_size=TRUE too; one FREE-promo-text leak; one reseller-username leak 'Meow7'; one true-duplicate pair resolved by rerouting the losing product's map row), 2 Royal Canin multi-breed buyer-choice catchalls missing is_multi_variant=TRUE (one of which also stated two sizes and needed is_multi_size=TRUE), 1 Orijen entry with a stray unrelated brand token ('Blue Bay') contaminating product_line from a garbled multi-size/multi-formula seller title, and 1 Brit entry with a reseller-username leak ('Meowseven77' — the same signature pattern as the Chunk Bits 'Meow7' leak, suggesting one reseller stamps their username across multiple unrelated brands' listings) plus a missing is_multi_variant flag. All other sampled rows were reviewed and found structurally correct. Self-check hard gates: G1 dual-mapped=0, G2 HUMAN+LLM coexistence (no --skip-coexistence)=0, placeholder-leak=0, structured-fields-NULL%=0%, G5 provenance=0, duplicate product+taxon=0, G4 cross-master_table=1 (confirmed identical to the pre-existing documented residual 24442421432, not new). | Fixed 20 taxonomy entries total (all _meta reset to {"is_reviewed": false} then re-verified fully clean via immediate same-session Tier 1 recheck, meta_agent='CLAUDE_CODE'): the Dentastix multi-size split (size cleared, is_multi_size=TRUE); 2 Carnilove corrections (GWP-text strip + genuine multi-size flag; 'Can can' -> 'Wet Can Food' cleanup); 1 Hill's trademark-symbol strip; 2 PURINA Supercoat trailing-fragment cleanups (one including a pack_count 2->3 correction); 9 Chunk Bits is_multi_variant corrections (2 also is_multi_size, 1 also FREE-text strip, 1 also reseller-username strip); 2 Royal Canin is_multi_variant corrections (1 also is_multi_size); 1 Orijen cross-brand-contamination strip + is_multi_variant/is_multi_size correction; 1 Brit reseller-username-leak strip + is_multi_variant correction. Resolved 1 true-duplicate reseller listing (two Chunk Bits entries with byte-identical formula-code text, SKU-150053/SKU-150060) by rerouting the losing product's product_taxonomy_map row to the surviving entry — zero taxonomy or map rows deleted, per STEP 7; the losing stub entry is left standing unused, _meta untouched. No new taxonomy entries minted, no SKU block claimed — every fix was a correction to an existing entry. Bulk-promoted via 2 STRUCT _meta UPDATE statements (not one-per-row): Path 1 — 154 taxonomy_ids (134 never-reviewed rows both Tier-2-sampled-correct and Tier-1-clean, plus 20 rows fixed and confirmed clean this session) straight to confident; Path 2 — 24 re-reviewed previously-unconfident rows judged correct again this session, promoted per the stored-verdict-agreement rule. Confidence distribution after session (taxonomy-entry level): 679 confident, 53 unconfident, 7 pending-recheck, 183 unreviewed (up from 501/83/7/332 pre-session; denominator dropped from 923 to 922 because the losing duplicate's entry lost its only map row this session). Residual: the 25 null_size entries (3 confirmed delisted, 22 genuinely unresolvable marketing-collage/illegible-packaging photos) are unchanged in count and identity from prior sessions — deferred, as documented across 8+ prior sessions. |
| 2026-07-29 02:55 UTC | Automated review session (auto-discovery) | Auto-discovery review of the 243-row worklist (never-reviewed + previously-unconfident entries). Tier 1 SQL sweep found only null_size flags (25 rows) — no stub_leak/duplicate_brand/wrong_field_order/brand_casing_mismatch/excess_content/canonical_field_mismatch/garbage_brand hits. Of the 25 null_size rows: 3 reference product_ids no longer present in the source table at all (delisted, unverifiable); 12 are affected by the pre-existing platform-mislabeling issue already flagged in the 2026-07-28 QA History entry (product_taxonomy_map.platform='Shopee' but the source table's actual ecommerce_platform is 'Tiktok' for these, so joining on (product_id, platform) as instructed silently misses them); the remaining 10 were checked directly against product images — all show marketing/lifestyle hero shots with no legible net-weight text, and this custom source table has no product_specification/product_description fallback field. None of the 25 were resolvable via any available signal. STEP 2b (promo-language pack_count sweep, 18 unique hits) and STEP 2c (shared-bucket single-size sweep, 1 hit) were both 100% false positives (regex collisions on 'grain free'/'freeze-dried'/'allergy free', and a literal 'x 1 Pack'=1) — pack_count=1 was already correct in every case. A GMV-prioritized Tier 2 sample of the top 70 Tier-1-clean rows (Shopee-only ranking; TikTok GMV excluded per the known scale-corruption issue) found 9 genuine defects: 2 reseller-store-name leaks ('IPET.STORE'), 1 merchant-name leak ('QUEEN' from 'CATQUEEN.CO'), 1 image-confirmed size miss (Furry Wonder 4oz vs. actual 16oz net weight printed on pack), 1 GWP/real-size confusion (ProBalance FOC 100g bonus wrongly used as the product's own size instead of the real 1.5kg x7), 1 dropped specific product-line name (Pedigree 'Dentastix' collapsed to generic text), 2 stray-text extraction artifacts, and 1 genuine multi-flavor/multi-weight product incorrectly pinned to a single flavor's weight (ProBalance Pro Selection, 2.7-3.2kg depending on flavor). | Applied all 9 confirmed fixes via in-place DML UPDATE (canonical_name/product_line/size/is_multi_size corrections as appropriate), each with meta_agent='CLAUDE_CODE' and _meta reset to {"is_reviewed": false}. Immediate post-fix Tier 1 recheck confirmed all 9 fully clean, so they promoted to review_confidence='confident' this same session (Path 1b) alongside 53 never-reviewed Tier-1-clean rows that Tier 2 judged correct with no defect (Path 1a) and 8 previously-reviewed rows where this session's judgment agreed with the prior 'correct' verdict (Path 2) — 70 taxonomy entries promoted to confident in total. No new taxonomy entries were minted and no SKU block was claimed (no fix required creating/rerouting an entry). The 25 null_size rows were left unresolved (no size signal available from any of sku_name/image/spec/description) — flagged for a future session, not force-filled. Final confidence distribution for this master_table: 749 confident (up from ~679 pre-session), 128 unreviewed, 45 unconfident (unchanged, pre-existing, not touched this session). Self-check hard gates (no --skip-coexistence): G1 dual-mapped=0, G2 HUMAN+LLM coexistence=0, placeholder-leak=0, structured-fields NULL%=0%, G5 provenance=0, duplicate product_id=0 — all pass. |
| 2026-07-29 03:14 UTC | Automated review session (auto-discovery) | Ran the auto-discovery two-tier review on the 173-row incremental worklist (94 never-reviewed, 69 previously-unconfident, 7 fixed-pending-recheck at session start). STEP 1C fast-lane: all 7 fixed-pending-recheck rows still tripped null_size on recheck (not promotable, left as-is). Tier 1 SQL sweep (STEP 2) found exactly one defect class across the whole worklist — null_size on 25 of 173 rows (14.5%); zero hits on stub_leak, duplicate_brand, wrong_field_order, brand_casing_mismatch, excess_content, canonical_field_mismatch, or garbage_brand. STEP 2b (promo-language-vs-pack_count=1) matched 8 distinct products, all false positives — 'free' substring-matched inside 'grain free'/'freeze dried' marketing text, not real promo language. STEP 2c (shared-bucket single-size/multiplier) returned 0 hits. All 25 null_size rows were individually investigated: sku_name text held no size for any of them; images were fetched for the 22 products still present in the live source table (TikTok images required parsing the JSON url_list structure and WebP→PNG conversion) — none showed a legible weight/size on packaging, only marketing collages/lifestyle photography; the remaining 3 (SKU-149590, SKU-150617, SKU-151895) no longer exist in the source table at all (delisted since mapping), so neither signal was re-checkable. Since this custom source table has no product_specification/product_description columns, the full text→image→spec→description priority chain is genuinely exhausted for all 25 — none fixed, matching the category's existing SKU-156803 (Tarokun) precedent for accepted-unresolvable size gaps. Investigating these images also reconfirmed and extended the 2026-07-28 6th-top-up session's flagged platform-mislabeling issue: 12 of the 25 null_size products are TikTok listings per the source table's own ecommerce_platform column, but product_taxonomy_map.platform records them as 'Shopee', so the standard (product_id, platform) join silently fails for them — worked around this session via a product_id-only fallback lookup for sku_name/image retrieval only, not fixed (touching product_taxonomy_map.platform is a map-row edit outside this session's in-place product_taxonomy field-fix scope, and the full extent wasn't exhaustively audited). Tier 2 GMV-prioritized sample (top 30 Tier-1-clean rows by Shopee-only GMV, per this session's scope note that TikTok GMV is unreliable for ranking) found 7 real defects regex couldn't catch: SKU-149603 (expiry-date stamp 'Best Before: 5 Sep 2027' leaked into product_line/canonical_name), SKU-149610 (retailer bullet noise 'Cans *' leaked in), SKU-149735 (size '410gm' duplicated into product_line on top of the correct size field), SKU-149910 (canonical_name/size wrongly showed the FOC free-gift bag's '500g' instead of the paid product's real weight), SKU-149934 (single '7kg' size falsely applied to a 4-flavor listing whose image confirms Salmon=7kg but Beef/Lamb/Chicken=8kg), SKU-150118 (stray leftover '3' digit artifact from an unresolved '3-4kg' size range), and SKU-150608 (pack_count wrongly set to 18 — image confirms '18pcs'/'6pcs' in the title describes pieces-per-bag for a Petite-vs-Regular size variant, not a purchase multiplier). | Fixed all 7 Tier-2-discovered defects via direct UPDATE (product_line/canonical_name text cleanup on 3 Royal Canin entries; size+pack_count+canonical_name correction on SKU-149910 confirmed via image; is_multi_size=TRUE conversion with size=NULL on SKU-149934 confirmed via image, following the established multi-size-seller-listing precedent and avoiding the banned 'Multiple Sizes' phrasing; product_line cleanup on SKU-150118; pack_count correction on SKU-150608 confirmed via image), all with meta_agent='CLAUDE_CODE' and _meta reset. Immediate same-session Tier 1 recheck confirmed all 7 fully clean, qualifying for STEP 5 Path 1(b) promotion. Bulk-promoted 30 taxonomy entries to review_confidence='confident' in two statements: 28 via Path 1(a+b) (21 never-reviewed rows judged correct + Tier-1-clean, plus the 7 just-fixed rows) and 2 via Path 2 (SKU-150185, SKU-150220 — already carried a prior 'unconfident'/'correct' verdict from 2026-07-28; this session's agreeing second review promoted them per the two-consecutive-agreement rule). The 25 null_size rows and remaining 118 unsampled Tier-1-clean rows received no _meta write (no fabricated review). No new SKU block claimed — no new taxonomy entries were minted, every fix was an in-place edit to an existing entry, and no product_taxonomy_map rows were created, deleted, or rerouted. Self-check hard gates: G1 dual-mapped=0, G2 HUMAN+LLM coexistence (no --skip-coexistence)=0, placeholder-leak=0, structured-fields-NULL%=0%, G5 provenance=0, G4 full cross-master_table=1 (confirmed via direct lookup to be the same pre-existing `24442421432` residual documented in every prior session, not a new collision). Live worklist reduced 173 → 143 (94 never-reviewed + 42 unconfident + 7 fixed-pending-recheck, unchanged), exactly matching the 30 rows promoted this session. |
| 2026-07-29 03:31 UTC | Automated review session (auto-discovery) | Automated review of the 143-row never-reviewed/unconfident worklist for makanananjing_my. STEP 1C fast-lane recheck of the 7-row fixed-pending-recheck bucket: all 7 still tripped null_size on immediate reverification (6 confirmed via fresh image/source-table reads as genuinely unresolvable — LLPET, Duck Jerky, Yogurt Cube confirmed no legible weight in any marketing image; SKU-150617/SKU-151895 reconfirmed delisted (zero rows in source table); LEMO reconfirmed via all 7 of its product images), 1 (Rawhide, SKU-190426) turned out to have a real fix — its own product image contained a 2/4/6/8/10/12-inch size chart, a genuine multi-size listing never previously discovered because the platform-mislabeling bug (product_taxonomy_map.platform recorded 'Shopee' on actual-TikTok rows) blocked prior sessions' direct image lookups for this SKU-190xxx block; this session worked around it by re-querying the source table on the correct ecommerce_platform. STEP 2's Tier 1 sweep over the full worklist found 25 null_size flags and zero other mechanical defect classes. STEP 3's GMV-prioritized Tier 2 sample (top 60 Tier-1-clean rows by Shopee-only GMV) surfaced 12 real defects invisible to any Tier 1 regex, all confirmed directly from each product's own sku_name text (no image reads needed): a brand mis-resolution (SKU-190402 'drdingo' packaging text mapped to BRD-UNBRANDED instead of the existing Dr. Dingo brand), 4 D3 variant-collapse misses (Orijen 11.4kg 5-formula listing, Carnilove 6-flavor listing, an Unbranded Chicken/Duck/Beef rawhide stick, Dogsome '5 flavors' can food), 2 D4 multi-size misses hiding in sku_name (Pedigree '2.7KG/3KG', Happy Dog '11kg 8kg 1kg 4kg' with a leaked expiry-date fragment and triplicated brand text), and 4 garbled/promo-text-contamination cleanups (Brit duplicated 'cans gastro intestinal' SEO tail, Goodies 'NEW PROMOTION!' leak, Probalance stray empty '( Packs)' bracket, Mr.Vet 'LIMITED PROMO D1' code leak). | Fixed 13 taxonomy entries total (all meta_agent='CLAAUDE_CODE', _meta reset then re-verified clean same-session): SKU-190426 promoted to is_multi_size=TRUE with cleaned name; SKU-190402 brand-corrected from BRD-UNBRANDED to Dr. Dingo (BRD-MY-01138); SKU-190466 got a duplicate-brand-in-product_line cleanup found incidentally while fixing SKU-190402; 4 is_multi_variant=TRUE corrections (SKU-149831 Orijen, SKU-190445 Carnilove, SKU-190423 rawhide stick, SKU-190409 Dogsome); 2 is_multi_size=TRUE corrections with size unpinned from an arbitrary single value (SKU-150208 Pedigree, SKU-150318 Happy Dog); 4 text-quality cleanups (SKU-150582 Brit, SKU-150388 Goodies, SKU-190460 Probalance, SKU-190430 Mr.Vet). Inserted 18 qa_gate_exceptions rows for gate_name='null_size' (2 confirmed-delisted products, 16 confirmed-genuinely-unresolvable-packaging entries), each backed by this session's fresh confirmation plus 1-4 prior documented session confirmations — closes this recurring bucket so future sessions stop re-spending turns on the same set. Bulk-promoted via 3 STRUCT _meta UPDATE statements: Path 1 (55 taxonomy_ids: 13 fixed-and-clean + 42 never-reviewed-and-Tier-1-clean) straight to confident; Path 2 (18 taxonomy_ids: 7 previously-reviewed rows re-judged correct, 6 pending-recheck rows now exception-covered, 5 previously-unconfident SKU-190xxx entries re-judged correct this session, agreeing with their stored prior verdict) promoted per the two-agreeing-reviews rule. No new taxonomy entries minted, no SKU block claimed. Confidence distribution after session: 846 confident, 43 unreviewed, 33 unconfident (up from 779/101/42; totals reconcile at 922 both before and after). |
| 2026-07-29 03:54 UTC | Automated review session (auto-discovery) | Live worklist was 76 taxonomy entries (never-reviewed/unconfident). STEP 1C fast-lane found 0 fixed-pending-recheck rows. Tier 1 SQL sweep flagged only null_size (17 rows) — all confirmed genuinely unresolvable this session: 3 products (SKU-149590, SKU-150617, SKU-151895) are fully delisted from the source table (zero rows, no sku_name/image available at all); the other 14 were image-verified (downloaded and read each product photo) and none show a legible net-weight anywhere in text or on packaging — all are marketing hero/collage shots with no visible size. STEP 2b/2c promo-language and shared-bucket sweeps found nothing actionable (the one STEP 2b hit was 'freeze' matching the 'free' substring, not a real promo signal). A GMV-prioritized Tier 2 sample of 11 taxonomy entries got genuine judgment (image reads for 7 of them): confirmed 3 defects — SKU-190393 (Mr.Vet) had size wrongly extracted as '4kg' when the actual packaging (verified by image, NET printed on the bag) is 1.4kg, one of four buyer-visible options in a garbled product_line '(1 / 2 / 4 / )' that dropped decimals; SKU-190418 (Masti Chicken Breast) had pack_count=1 despite an explicit bracket-prefixed '[200 PCS]' bulk-box multiplier in sku_name and image (same signal class as the established '[แพ็คคู่]' bracket-format precedent — bracket-prefix in the title itself, not a buyer-selector); SKU-150479 (Wanpy Meat Treats) used the lower bound '380g' of a stated '380g-450g' size range in sku_name where the image was ambiguous (generic 7-flavor marketing collage with no single confirmable value) — per docs/llm-extraction-rules.md §2 the larger value should be used when the image doesn't disambiguate a range. Also found and fixed a cosmetic artifact on SKU-190437 (Probalance): canonical_name had a dangling empty '( Packs)' fragment left over from stripping the '9' multiplier. 8 sampled rows were confirmed correct as-is (including one, Royal Canin Wet Food Pouch 85g, that was worth double-checking given this category's documented history of RC wet-pouch entries turning out to be Feline — image confirmed genuinely canine breed-specific formulas). One entry, SKU-150708 (Dermoscent Essential 6 Spot On, canonical size '10KG'), is suspicious — a topical spot-on pipette supplement normally sold in small ml vials, not 10kg — but the mapped product (22457341802) no longer exists in the source table, so it could not be re-verified this session; left untouched and flagged for a future session with cached image access. Independently reconfirmed this category's known platform-mislabeling defect (previously flagged only for the sibling makanankucing_my category): several product_taxonomy_map rows carry platform='Shopee' while the same product_id's actual row in the source table has ecommerce_platform='Tiktok' (7 of the null_size rows and 3 of the Tier-2-sampled rows exhibited this) — confirms the issue is present in makanananjing_my's own map rows too, not just documented-but-unconfirmed. Self-check hard gates: G1=0, G2=0, placeholder-leak=0, structured-fields-NULL%=0%, G5=0, duplicate product_id=0, duplicate product+taxon=0, 'all variant/size' name=0, garbled brand text=0, G4 (full cross-master_table)=1 — confirmed via direct lookup to be the same pre-existing documented residual (24442421432) noted in every prior session's QA History entry, not a new collision. | Fixed 4 taxonomy entries in place (all _meta reset to unreviewed, immediately gate-verified clean, then promoted straight to confident this session): SKU-190393 size 4kg→1.4kg + product_line cleaned + variant='Lamb' set, SKU-190418 pack_count 1→200 + canonical_name updated to '...40g x200', SKU-150479 size 380g→450g, SKU-190437 canonical_name/product_line dangling '( Packs)' fragment removed. No new taxonomy entries minted, no SKU block claimed (no session needed one), no product_taxonomy_map rows rerouted or deleted. STEP 5 promotions: 9 taxonomy_ids promoted via Path 1 (4 freshly-fixed-and-reverified + 5 never-reviewed rows that were both Tier-2-judged correct and Tier-1-clean this session: SKU-190454, SKU-190404, SKU-190410, SKU-190388, SKU-190412), 1 promoted via Path 2 (SKU-190459, prior stored verdict 'correct' agreed with this session's fresh 'correct' judgment). Net: confidence distribution moved from 76 unconfident/unreviewed to 66 remaining (10 resolved to confident this session). Residual 17 null_size entries are genuinely exhausted (no further text/image signal available) and 66 total remain for future sessions' Tier 2 sampling. The Dermoscent size/type-plausibility concern and the platform-mislabeling finding are flagged for follow-up, not fixed here (no source data for the former; broader-scope data-quality fix out of this session's bounds for the latter). |
| 2026-07-29 04:10 UTC | Automated review session (auto-discovery) | Live worklist (never-reviewed/unconfident) was 66 taxonomy entries, far smaller than prior sessions' worklists, reflecting steady progress from prior review passes (856 confident going in). Tier 1 SQL sweep found only null_size defects (17 rows) — zero stub_leak/duplicate_brand/wrong_field_order/brand_casing_mismatch/excess_content/canonical_field_mismatch/garbage_brand. STEP 2b (promo-language pack_count=1 sweep) returned one hit, a false positive ("freeze dried" substring-matching the "free" pattern, not real promo language). STEP 2c (shared-bucket size/multiplier) returned zero hits. Tier 2 review of all 49 Tier-1-clean rows (full sample, not GMV-capped, since the worklist was small enough) found: (1) SKU-190432/SKU-190434 ("MEAT UP ... BUY 1 FREE 1") — a genuine missed pack-count multiplier, invisible to STEP 2b's strict (product_id, platform) join because of this category's already-documented platform-mislabeling bug (product_taxonomy_map.platform recorded 'Shopee' while the actual source row is 'Tiktok') — only surfaced via a broader product_id-only join, confirming the mislabeling bug has real, concrete downstream cost beyond the abstract concern noted in a prior session. Confirmed via image: the product photo shows two identical bags side by side. (2) SKU-190438 — wrong brand entirely: canonical_name/brand_id pointed to 'BARK' (BRD-SG-06896), but the product image's own packaging brand lockup is unambiguously 'Happi Doggy' with no 'BARK' text anywhere on the product; a matching brand_dict entry already existed (BRD-SG-02477), no new brand needed. Of the 17 null_size entries, only 1 (SKU-151850, Paw-cipes) had a legible weight on a zoomed crop of its listing image ('Net Weight: 100g / 3.5oz'); the other 16 remain genuinely unresolved this session — 3 have product_ids no longer present in the source table at all (SKU-149590, SKU-150617, SKU-151895 — delisted, no text/image available), and 13 (WagglyCove Chhurpi Bite, Hey!Bobo TwistChew/Yak Cheese x3, MYDOG, Unbranded Duck Jerky, LLPET, Unbranded Freeze Dried Yogurt Cube, Tarokun) have only marketing hero-collage images with no legible net-weight text even after cropping/zooming. Also flagged but not fixed: SKU-150708 ('Dermoscent Essential 6 Spot On 4 Pip 0 skin problem 10KG') has a garbled, non-sensical field fragment ('4 Pip 0 skin problem 10KG') for what is a topical skin/coat spot-on product — questionable both on data-quality grounds and possibly on category-scope grounds (similar to this doc's documented flea/tick-treatment exclusion) — its product_id no longer exists in the source table, so it could not be re-verified or safely corrected this session. | Fixed 4 taxonomy entries, all meta_agent='CLAUDE_CODE': SKU-151850 (size NULL → '100g', resolved from image), SKU-190432 and SKU-190434 (pack_count 1→2, canonical_name suffixed 'x2', confirmed via image showing two identical bags), SKU-190438 (brand_id BRD-SG-06896→BRD-SG-02477, canonical_name/product_line corrected from 'BARK Happi Doggy/Royal...' to 'Happi Doggy Dental Chew Zest Care...', off an existing brand_dict row — no new brand minted). All 4 re-checked clean against the full Tier 1 sweep immediately after fixing (same-session gate-verify) and promoted straight to confident. Bulk-promoted 35 entries to confident via Path 1 (the 4 fixed rows above + 31 never-reviewed rows that were Tier-1-clean and Tier-2-judged correct) and 14 entries via Path 2 (previously-unconfident rows re-confirmed correct this session, now agreeing twice). Left untouched (no promotion, no fix): 16 null_size entries (3 delisted-source, 13 illegible-image) and SKU-150708 (suspicious garbled fields, no source data to safely correct) — all remain in the review queue for a future session with either restored source data or a different image angle. Confidence distribution after session: 905 confident, 15 unconfident, 2 unreviewed (of 922 total taxonomy entries currently mapped to master_table='makanananjing_my'). Self-check QA gates: G1 dual-mapped=0, G2 HUMAN+LLM coexistence (no --skip-coexistence)=0, placeholder-leak=0, structured-fields-NULL%=0%, G5 provenance=0, duplicate product_id=0, G4 cross-master_table (full true-composite-key check)=2 — both confirmed identical to the pre-existing documented residuals (24442421432 makanananjing_my/makanankucing_my; 24279117321 shopee_sg_shampoo/shopee_sg_hair_conditioner_or_treatment), not new collisions. No qa_gate_exceptions needed — no rows in this session's flags matched the wrapper's named gate categories requiring that carve-out. |
| 2026-07-29 04:24 UTC | Automated review session (auto-discovery) | Reviewed all 17 never-confident product_taxonomy entries (STEP 1 worklist). Tier 1 SQL sweep flagged only null_size on 16/17 — all 16 already had a confirmed qa_gate_exceptions row from earlier sessions today, so no new exception inserts were needed. Manual Tier-2 reading of canonical_name/product_line text (this category has no image-based extraction history — bulk regex only) surfaced 3 genuine defects Tier 1's regex couldn't catch: SKU-149590 ('Royal Canin 2 BOX GET DISCOUNT Pouch x12') and SKU-150617 ('ROSY FRESH All Categories And Combination Pack Pek Gabungan') both had promotional/bundle-selector text mistaken for product_line — both source products are now delisted from `makanananjing.9_makanananjing_my_daily` (0 rows), so no fresh re-derivation was possible. SKU-190394 carried a leftover empty-bracket artifact ('【】') from brand-token stripping. Separately, STEP 1B's pre-run qa_report.sh had flagged '[FAIL] canonical_name fields: 1' — re-running that gate's exact query unscoped by _meta (my worklist-scoped Tier 1 sweep had silently excluded it because it was already marked confident) found SKU-151850: its `size` field held a hallucinated '100g' extracted from '100% Pure Meat' marketing text in sku_name, populated and marked confident only ~45 minutes before this session by an apparently very recent prior pass, contradicting that same day's own null_size qa_gate_exceptions entry for this exact taxonomy_id ('no legible weight'). Also verified SKU-151890 ('LLPET' brand) by fetching its actual product image after noticing neither sku_name nor merchant_name supported the brand token — confirmed 'LLPET'/'LL PET' is genuinely printed on the packaging, correctly distinguishing it from the reseller's own 'PetRadise' storefront watermark banner (§11 discipline). Noted but did not act on two other findings: (1) SKU-151897's Tier-1-adjacent STEP 2b hit was a false positive — 'free' matched inside 'freeze dried', not real promo language. (2) A platform-provenance defect: taxonomy_ids SKU-190382/190394/190407/190416 carry product_taxonomy_map.platform='Shopee' but their source rows in makanananjing.9_makanananjing_my_daily are actually ecommerce_platform='Tiktok' (product_ids are TikTok-format, e.g. 1735062526895359511) — this breaks any (product_id, platform)-keyed join against this source table for these 4 rows (confirmed by re-running STEP 2b/2c against them via a product_id-only join instead — both came back empty, so no defect was hidden by it, but the mismatch itself persists as unfixed provenance corruption; same root cause flagged and deferred, unresolved, in the 6th top-up session's QA History entry). SKU-150708 (Dermoscent Essential 6 Spot On, a topical spot-on skin/joint supplement) was left unpromoted deliberately: its source product is delisted (0 rows), so no fresh image/text check was possible, and its topical delivery format raises an unresolved scope question — it resembles the flea/tick spot-on treatments this category's docs explicitly exclude as 'pet medicine', despite being nominally a 'nutritional supplement'. Its product_line also carries likely regex-stripping noise ('0 skin problem', '4 Pip') from the original bulk extraction, same class of artifact fixed on SKU-190394, left as-is pending that scope call. | Fixed 4 taxonomy_id rows via bq DML: SKU-149590 and SKU-150617 rewritten to '{Brand} (unresolved)' product_line/canonical_name per llm-extraction-rules.md's unresolved-product-line rule; SKU-190394 had its empty-bracket artifact stripped from product_line/canonical_name; SKU-151850 had its false '100g' size reverted to NULL (re-aligning it with its own pre-existing null_size exception). All 4 were re-verified clean against the full Tier 1 sweep immediately after fixing (same-session gate-verify) and promoted to review_confidence='confident' (Path 1b). The remaining 13 worklist rows were Tier-2-judged correct against their source sku_name text/image and, agreeing with their prior 'correct' verdict from an earlier session, promoted to 'confident' via Path 2. SKU-150708 was left un-promoted (_meta untouched) pending the scope question above. All meta_agent set to CLAUDE_CODE on every row touched. Post-fix self-check of all 8 hard/entry gates from script/qa_report.sh (dual-mapped, HUMAN+LLM coexistence, placeholder-leak, structured-fields NULL%, duplicate product_id, duplicate product+taxon, 'all variant/size' name, canonical_name fields, garbled brand text) all returned 0. Live review worklist reduced from 17 to 1 (SKU-150708, intentionally held back). |
---

## Targeted QA Fix Brief

> Scope for a future `targeted_qa_fix.sh` / NULL-coverage session — not executed this session.

**Verdict:** D6 in-scope NULL coverage gap + a cross-category re-routing task + a D1/D2 precision pass.

- **Coverage gap** (~137 products, dog-scope estimate): products matching a dog-food text signal in
  the 95%-cumulative-GMV worklist but not resolved to a brand/line by this session's keyword
  dictionary. Candidates for direct image-based resolution.
- **Cross-category re-routing** (78+ products, see Cross-category discovery): genuinely dog-labeled
  products currently sitting under `master_table='makanankucing_my'` (out of scope for that category
  per its own doc). Re-route these `product_taxonomy_map` rows to `master_table='makanananjing_my'`
  once a session is scoped to safely touch `makanankucing_my`'s data. **Grown substantially again as of
  the 2026-07-23 third top-up session**: of 44 dog-scoped candidates identified from that session's live
  worklist (spanning Royal Canin vet-diet variants, Orijen, Hill's Prescription Diet, Farmina, Brit,
  Wanpy, PetCoCo, N&D, Nature's Protection, Greenies, and more), **43 were already mapped under
  `makanankucing_my`** — this is no longer a residual edge case, it is now the dominant explanation for
  why this category's live coverage gap barely moves session over session (1,085 → 1,071 → 1,070: two
  full top-up sessions' worth of classification work, net one product resolved here). The true scope of
  this re-routing task is almost certainly much larger than the ~78 products (35 + 43) directly observed
  across the two top-up sessions that happened to collide with it — a dedicated audit of
  `makanankucing_my`'s **full** map for dog-signal canonical_names/sku_names (not just the intersection
  each top-up session's worklist happened to touch) is now overdue and should be treated as higher
  priority than further `makanananjing_my` top-up passes, which will keep returning near-zero net new
  coverage until this is resolved.
- **Precision pass**: this was a bulk regex/text-matching, no-image-read pass (Full Rebuild scenario
  explicitly deprioritizes per-row precision). `product_line` for long-tail brands is a regex-stripped
  `sku_name` remainder and sometimes retains noise or embedded CJK text. A pass re-reading product
  images for the long tail would sharpen D1/D2.
- **`brand_dict` cleanup**: 18 of the 47 newly-minted `BRD-MY-*` brand rows from this session's first
  extraction pass ended up unused after the cat-scope correction (their only referencing taxonomy rows
  were deleted). They remain in `brand_dict` as unused rows — harmless but not cleaned up this
  session.

---

## Scripts

No committed pipeline scripts for this category — extraction was done directly via ad hoc SQL against
BigQuery (bulk regex-based scope/brand/size/pack classification), not a
`pipeline/05_product_taxonomy/llm_{table}/` script, per the session brief for this custom, non-NIQ
source table.

---

## Map Row Counts (as of this session)

| Source | Count | Notes |
|--------|-------|-------|
| LLM | 731 | This session, Full Rebuild first run |
| HUMAN | 0 | No prior keyword-seed pass for this category |
| NULL (unmapped, in-scope dog food) | ~137 | Brand/line not resolvable from text alone — see Targeted QA Fix Brief |
| NULL (out-of-scope, correctly excluded) | 1,237 | Cat food (belongs to `makanankucing_my`) + non-pet-food contamination + pet non-food/medicine |
