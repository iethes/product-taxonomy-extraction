# shopee_th_body_wash — Category Context

## Status

| Field | Value |
|-------|-------|
| LLM Pass 1 | ✅ Complete (v3 rebuild) |
| LLM Pass 2 | ✅ Complete (v3 rebuild) |
| GMV Coverage | 75.5% LLM (Apr 2026); top-up Jul 20 2026 closed 92.4% of the live 95%-GMV coverage gap (Jun 2026); top-up Jul 25-26 2026 (Session 67) closed the live Jun-2026 gap again from 3,005 to 534 distinct unmapped products (~93.5M→~14.1M THB, all 534 residual independently confirmed correctly out-of-scope); top-up Jul 26 2026 (Session 68) found 15 of the 534 were a genuine false-exclusion (2-in-1 wash+shampoo combos wrongly caught by the shampoo exclusion), routed them, closing to 519 (~13.1M THB); remaining 519 re-confirmed correctly out-of-scope; top-up Jul 26 2026 (Session 69) found 2 more of the 519 were a further false-exclusion (same wash+shampoo class, but phrased with the Thai phonetic transliteration "วอช" rather than English "wash" or Thai "อาบน้ำ", so neither the original nor Session 68's own broadened regex caught them), routed both, closing to 517 (~13.03M THB); remaining 517 re-confirmed correctly out-of-scope; top-up Jul 26 2026 (Session 70) re-ran the live worklist and got the identical 517-product/~13.1M THB residual Session 69 had already re-confirmed OOS — rather than trust that re-confirmation at face value (Sessions 68 and 69 had each just found the prior session's "confirmed OOS" verdict was incomplete), re-screened all 517 by keyword and read all non-obviously-OOS sku_names in full; found 13 more false-exclusions from the same wash+shampoo/bath+wash/cleansing-gel bundle class (Eucerin วอชแอนด์แชมพู, Cetaphil/Aveeno/Sebamed lotion+wash bundles, Mustela cleansing-gel bundles, Dove scrub+bodywash bundle), routed via 12 new mint entries (13th and 14th products share one entry), closing to 504 (~12.8M THB); remaining 504 re-confirmed correctly out-of-scope (standalone baby shampoo/conditioner, lice shampoo, hand wash/hand soap, solid bath bombs); top-up Jul 26 2026 (Session 71) re-screened all 504 independently, found 2 more false-exclusions (Hug shower-gel-with-scrub-texture wrongly caught by the scrub exclusion), routed both via 2 new mints, closing to 502 (~12.75M THB); remaining 502 re-confirmed correctly out-of-scope or genuinely indeterminate at product grain (1 multi-type buyer-choice listing) — first session in the Session 68–71 chain to find zero new wash+shampoo/bundle-class false-exclusions |
| Last run | Jul 20 2026 (top-up, Session 59); re-verified Jul 20 2026 (Session 60, zero new writes — residual 380 confirmed OOS); Jul 21 2026 (auto-discovery targeted QA fix, Session 61 — 95 entries fixed, no new SKUs minted); Jul 21 2026 (auto-discovery targeted QA fix, Session 62 — 132 entries fixed, no new SKUs minted); Jul 21 2026 (auto-discovery targeted QA fix, Session 63 — 23 entries fixed + 6 map rows rerouted, no new SKUs minted; flagged a 56-entry/~5.32M THB hand-wash-in-body-wash scope contamination for dedicated remediation); Jul 22 2026 (auto-discovery targeted QA fix, Session 64 — 85 entries fixed, no new SKUs minted; added SKU-001484/SKU-001442 as specific instances of the Session 63 hand-wash cluster, still awaiting dedicated remediation); Jul 22 2026 (auto-discovery targeted QA fix, Session 65 — 7 entries content-fixed + 21 map rows rerouted, no new SKUs minted; found and quantified a new 320-entry generic-product_line defect class category-wide, flagged for Full-Rebuild-scale remediation; added SKU-001488 as a new fully-contaminated instance of the Session 63 hand-wash cluster); Jul 22 2026 (auto-discovery targeted QA fix, Session 66 — 750 entries content-fixed + 55 map rows rerouted + 3 new SKUs minted, no scope-cluster deletes; category-wide fix of the newly-unconditional "Multiple Sizes"/"Multiple Variants" ban (706 entries), plus a null-size sweep and several GMV-prioritized D5 pack-count reroutes); Jul 25-26 2026 (top-up coverage, Session 67 — 1,802 new SKUs minted (SKU-165037–166838) + 2,214 map rows total across 2 processing rounds; found and fixed 18 pre-existing orphaned-map-row G1 collisions, see QA History); Jul 26 2026 (top-up coverage, Session 68 — re-verification of Session 67's residual found a genuine false-exclusion class (2-in-1 wash+shampoo combos), 15 products routed via reuse-before-mint (1 new SKU minted), see QA History); Jul 26 2026 (top-up coverage, Session 69 — re-verification of Session 68's residual found 2 more false-exclusions from the same class via Thai-transliteration signal, both routed (1 new SKU minted), see QA History); Jul 26 2026 (top-up coverage, Session 70 — re-verification of Session 69's residual found 13 more false-exclusions from the same wash+shampoo/bath+wash/cleansing-gel-bundle class, routed via 12 new SKUs, see QA History); Jul 26 2026 (top-up coverage, Session 71 — re-verification of Session 70's residual found 2 more false-exclusions (scrub-texture body wash), both routed (2 new SKUs minted), zero new wash+shampoo/bundle-class misses found, see QA History) |
| Current MAX taxonomy_id | SKU-168473 |

**Version history:** v1 (Session 17, Jun 21) had NULL size everywhere. v2 (Session 48, Jun 23) had API auth errors → text fallback → generic names. v3 (Session 58, Jun 24) is the authoritative version using text-based smart extraction.

---

## SKU Blocks Assigned

| Block | Usage |
|-------|-------|
| SKU-058000–058423 | Pass 1 OFFICIAL (424 entries, 56 brands) |
| SKU-057000–057569 | Pass 2 RESELLER (570 entries, 80+ brands) |
| SKU-036010–036015 | Bennett bar soap gap fill (6 entries, Session 41) |
| SKU-041230 | Shokubutsu x12 body moisturizer cross-ref |
| SKU-107746–109745 | Top-up coverage claim (Session 59, Jul 20 2026); 1,861 entries used (SKU-107746–109606), 139 slots unused |
| SKU-112406–112605 | Targeted QA fix claim (Session 66, Jul 22 2026); 3 entries used (SKU-112406–112408, Tea Tree 500ml x3 / Sebamed 1000ml x2 / Pharmapure 450ml x3), 197 slots unused |
| SKU-165037–167036 | Top-up coverage claim (Session 67, Jul 25-26 2026); 1,802 entries used (SKU-165037–166838: 1,589 round-1 + 213 round-2), 198 slots unused |
| SKU-167037–167871 | Top-up coverage claim (Session 68, Jul 26 2026); 1 entry used (SKU-167037, Cetaphil Baby Gentle Wash & Shampoo 230ml), 834 slots unused |
| SKU-168072–168271 | Top-up coverage claim (Session 69, Jul 26 2026); 1 entry used (SKU-168072, Dermollient Organic Baby Gentle Wash & Shampoo 250ml), 199 slots unused |
| SKU-168272–168471 | Top-up coverage claim (Session 70, Jul 26 2026); 12 entries used (SKU-168272–168283), 188 slots unused |
| SKU-168472–169264 | Top-up coverage claim (Session 71, Jul 26 2026); 2 entries used (SKU-168472–168473, Hug Hom-Nin Rice Scrub Shower Gel 500ml/1000ml refill), 791 slots unused |
| SKU-006000–006999 | DELETED (v1) |
| SKU-044000–044999 | DELETED (v2) |
| SKU-049000–049383 | DELETED (v2 dedup recovery) |

---

## Brand Scope (GMV threshold 95%, Apr 2026)

56 brands with official stores (Pass 1). 80+ brands in Pass 2. Key brands by GMV:

**Top-tier (Pass 1 official stores):**
- **Shokubutsu / Kodomo / Goodage / Kirei Kirei** — shared `Lion Shop Online` store (BRD-SG-00249)
- **Dove** — `Unilever Body Wash Official Store`
- **Lifebuoy** — Unilever store
- **Vaseline** — Unilever store
- **Safeguard / Protex** — P&G store
- **Lactacyd** — separate store
- **Shower Mate** — separate store
- **Enchanteur** — separate store
- **Biore** — `KAO Beauty & Personal Care`
- **Nivea** — `Nivea Body Official Store TH`
- **Citra** — Unilever store
- **Lux** — Unilever store
- **Bennett** — No official store (Pass 2 only → SKU-036010–036015)

---

## Official Store Allowlist (Pass 1)

| Brand | Merchant Name |
|-------|---------------|
| Shokubutsu, Kodomo, Goodage, Kirei Kirei | `Lion Shop Online` |
| Dove, Lifebuoy, Citra, Lux, Vaseline (body wash) | `Unilever Body Wash Official Store` |
| Safeguard | P&G official store |
| Biore | `KAO Beauty & Personal Care` |
| Nivea | `Nivea Body Official Store TH` |
| Lactacyd | standalone |
| Shower Mate | standalone |
| Enchanteur | standalone |

**Multi-brand stores (brand_from_image required):**
- `Lion Shop Online` — 4 brands: Shokubutsu / Kodomo / Goodage / Kirei Kirei

**Excluded retailers (multi-brand, not brand-owned):**
- Watsons, Boots, BigC, Lotuss, Tsuruha

**Brands with no official store (Pass 2 only):**
- Bennett, Parrot, and ~70 other reseller-only brands

---

## Scope — What's In vs Out

**In scope:**
- Body wash / shower gel (เจลอาบน้ำ, ครีมอาบน้ำ)
- 2-in-1 wash+soften (สบู่เหลว)
- Baby wash (สบู่อาบน้ำเด็ก)
- Kodomo Head-to-Toe Wash — INCLUDED despite containing แชมพู keyword

**Out of scope (leave NULL):**
- Hand wash (ล้างมือ, โฟมล้างมือ, สบู่ล้างมือ, hand wash)
- Feminine wash (เฟมินีน, feminine)
- Floor cleaner / dish soap
- Standalone shampoo/conditioner
- Body scrub (สครับ)

**Edge cases:**
- Kodomo Head-to-Toe: contains "แชมพู" keyword but IS body wash — the keyword exclusion must only fire when NO body wash keywords (อาบน้ำ, body wash, shower) AND NO "Head to Toe" phrase are present
- Multi-category NIQ: this table maps to 6 NIQ category_3 values (Body Wash, Baby Shampoo & Body Wash, Men's Body Wash, Hand Wash, Hand Cream) — universe refresh must use NIQ join, not a single category_3 filter

---

## Taxonomy Design Notes

**Product line extraction approach:**
- Every brand has its own `extract_{brand}()` function detecting product_line from sku_name text
- `Lion Shop Online` uses `lion_shop_route()` — routes by brand name keyword before product line
- Size extracted from Thai patterns: `(\d+(?:\.\d+)?)\s*(?:ml|มล\.?|g|กรัม|ก\.|oz)` — note: `ก.` is Thai gram abbreviation
- Pack-count: แพ็คคู่/แพ็ค 2/x2/Pack 2/ซื้อ1แถม1 = 2; ยกลัง = case qty; `N ฟรี M` same product = N+M

**Size extraction notes:**
- `ก.` is a short form of กรัม — regex must include `r'(\d+(?:\.\d+)?)\s*(?:g(?:r(?:am)?)?\.?|กรัม|ก\.)'`
- Without this, sizes like "105ก." in bar soap sku_names are missed

**Pack-count patterns common in this category:**
- `[N ฟรี M]` or `N แถม M` = buy N get M free → pack_count=N (M is GWP, NOT additional units)
- `แพ็คคู่` / `แพ็ค 2` / `x2` / `ซื้อ1แถม1` = pack_count=2
- Cross-product bundle: `Brand A Product xN + Brand B Product xN` → is_bundle=True

**Farsight DML special handling:**
- Multi-model products cause "UPDATE/MERGE must match at most one source row" error
- Fix: add `QUALIFY ROW_NUMBER() OVER (PARTITION BY product_id, category_3, month ORDER BY taxonomy_id) = 1` to src subquery

---

## QA History

| Date | Session | Finding | Resolution |
|------|---------|---------|------------|
| Jun 21 | 17 | All 233 entries had NULL size | Full rebuild required |
| Jun 23 | 48 (v2) | API auth error → text fallback → generic names | Full rebuild v3 required |
| Jun 23 | 53 | Dedup script deleted 362 valid entries | Recovery: rebuild from orphaned map rows |
| Jun 24 | 58 (v3) | Final rebuild with text-based smart extraction | ✅ Clean, 75.5% GMV coverage |
| Jul 20 | 59 (top-up) | Live re-check (month 2026-06) found 3,286 in-scope-candidate rows (1,994 distinct products) with no taxonomy_id, despite category being marked complete — categories accumulate new listings over time. Bulk-first reuse-before-mint: grouped by (brand, size, pack_count, normalized product line text) rather than reading images per product. Only 18 products matched existing Pass 1/2 taxonomy entries (this gap is almost entirely new long-tail reseller listings, not variants of already-covered products) — 1,861 new taxonomy entries were minted, one per distinct group. 343 candidate rows (238 distinct products, ~2.39M THB) were confirmed out-of-scope (hand wash, standalone shampoo/conditioner, lice shampoo, scrub soap, bath bombs/onsen bath-salt powder — a distinct product type from body wash despite containing "bath" — and a body-lotion listing miscategorized into this table) and correctly left NULL. One Thai-Unicode variant (น้ํา using NIKHAHIT instead of the standard SARA AM in น้ำ) initially misrouted a genuine body-wash bundle to the OOS bucket; caught on manual review of the 347 OOS candidates and corrected. Brand resolution: 818 products had no product_brand_map brand at all (221 with zero row, 597 FALLBACK/Undefined); ~140 recovered via merchant-official-store-name cross-validated against sku_name text (e.g. "enfant.official" → Enfant), remainder minted under BRD-UNDEFINED with brand text left out of canonical_name rather than guessed. 628 of 1,861 new entries have no extractable size from text (bar soap sold by piece count, multi-size seller listings, etc.) — written as is_multi_size=TRUE, size=NULL per existing precedent rather than left unmapped. Per-row canonical_name wording precision (exact product_line phrasing, e.g. a few entries retain the brand name duplicated in product_line) intentionally not polished this session — deferred to `script/targeted_qa_fix.sh`, scoped by GMV impact. All hard gates (G1 dual-map, G2 HUMAN+LLM coexistence, G4 cross-category range, G5 provenance, placeholder-leak) verified 0 post-run. | ✅ Live worklist gap closed from 3,286→380 rows (~31.28M→~2.39M THB, 92.4% GMV reduction); remaining 380 rows all independently verified as correctly-classified out-of-scope, zero unexpected leftovers |
| Jul 20 | 60 (top-up, independent re-verify) | Wrapper re-fired the top-up job citing "380 unmapped products" as if new — re-ran the live STEP 0 worklist query rather than trusting the number: still exactly 380 rows / 238 distinct products / ~2.33M THB, matching Session 59's already-committed residual almost exactly (238 distinct products identical). Applied the per-product category/type gate live rather than only trusting Session 59's summary text: keyword-classified 337 of 380 rows against documented OOS categories (223 hand-wash/feminine/scrub, 110 standalone-shampoo, 4 conditioner); image-verified the 3 highest-ambiguity remainders per the "keyword gate must never decide extraction alone" rule — (1) "Perfect Official" โปรคละ 3 ชิ้น 1000 เฉพาะในไลฟ์ (9 rows, ~347K THB, the single largest unmapped chunk): cover image shows a buyer's-choice-of-3-of-7 mixed set (baby lotion, eyebrow serum, sunscreen, shower gel, 4-in-1 shampoo) — genuinely indeterminate which 3 items ship, left NULL per existing buyer-choice-bundle precedent; (2) TOPJapan/DONDONKI "Horse Oil" listings (สบู่/ยาสระผม/ครีมนวด, Body Soap/Shampoo variant-selector, 4 rows ~9.9K THB): image confirms a 3-way product-type variant selector (shampoo/conditioner/body soap) with no option-name data available to determine which model_id maps to the body-soap variant — left NULL, same "can't determine what buyer receives" precedent as multi-size ambiguous listings; (3) "Perfect" PREMIUM SET (1 row, 7.5K THB): image confirms a fixed 4-item cross-category bundle (shampoo+hair oil+shower gel+baby lotion) where only 1 of 4 items is body wash — left NULL, no precedent supports single-category-izing a mostly-other-category fixed set. Zero rows resolved to a mintable in-scope product; no SKU block claimed (nothing to write). QA gates re-run without --skip-coexistence: G1=0, G2=0, placeholder-leak=0, G5=0, structured-fields-missing=0% — unchanged from Session 59, confirming no regression. | ✅ Independently re-confirmed Session 59's finding via live re-query + per-product image verification of every high-ambiguity candidate (not just keyword text) — all 380 rows / 238 distinct products correctly out-of-scope or genuinely indeterminate (buyer-choice bundles). No writes made, no SKU block claimed. Live gap is 0 mintable products, not a fresh 380-product backlog — treat future wrapper re-fires on this exact residual as expected until new listings actually change the worklist |
| Jul 21 | 61 (auto-discovery, `_meta`-tracked) | First run of the new auto-discovery `targeted_qa_fix.sh` loop (no `## Targeted QA Fix Brief` existed) — worklist = 2,997 distinct never-reviewed/unconfident taxonomy entries. Tier 1 SQL sweep flagged 71 stub-leak, 8 duplicate-brand, 439 wrong-field-order, 10 brand-casing-mismatch, 20 excess-content candidates; each was individually judged rather than auto-applied. **Biggest finding:** 71 SKU-001xxx catch-all entries (predating this category's own SKU-057xxx/107xxx blocks, globally shared brand+category buckets from the pre-Phase-5 HUMAN keyword-seed era) had literal `"(all variants)"` canonical-name stubs banned by `quality-standards.md` — each covers dozens to 474 distinct real products under one generic `Brand + "Body Wash"` name. Renamed to the established `{Brand} {Line} Multiple Variants` convention (matches the `th_softdrink` precedent) and set `is_multi_variant=TRUE`; the underlying genericness (`product_line` still just "Body Wash") is a Full-Rebuild-scale problem — splitting hundreds of products per bucket into real per-product entries needs per-product image reads, out of this session's scope, flagged for a future Full Rebuild pass targeting this SKU-001xxx block specifically. Other confirmed-and-fixed defects: 12 entries with an impossible `size="0ml"` — root cause is comma-thousands-separator sizes ("1,000 ml") being misread as "000ml"→"0ml"; corrected to `1000ml` from the underlying `sku_name` text. 2 entries (`Aveeno Aveeno Baby Body Care`) had a literal duplicated-brand naming bug (same pattern as `th_moisturizer_for_face`'s fix), collapsed to one `Aveeno`. 2 entries (`"360" LRL ...`) had a stray duplicate "ml" token before the real size, stripped. §11 signal-provenance violations found via targeted scan: 3 "MORITOMO Official NAIVE Body Wash ... Set" entries had the reseller's store name baked into `canonical_name`/`product_line` and were mis-branded `BRD-UNDEFINED` even though `Naive` exists in `brand_dict` (`BRD-GLOBAL-01100`) — corrected brand_id, stripped the merchant name, and recovered the missing 360ml free-refill size from `sku_name`. 1 entry ("Qin Feisi ... goodhomeofficial CR081 ...") had the same merchant-name leak, stripped. §11 short/ambiguous-size cross-validation found 4 more G2G-style digit-collision misreads: `Philosophy ... 1g` (real size stated as "480ml (16oz)" in `sku_name`, size was misread from unrelated promo text), `Pinks Angel ... 1g x2` (misread from "1แถม1" promo language, no real gram weight stated — set to NULL, genuinely unresolved), `Fyne ... 04L` (fabricated from a stray "R04 l" product code with zero real size signal in `sku_name` — set to NULL), and `Medix5.5 ... 5.5g x5` (both size and pack_count misread from the brand's own "5.5" digits — real size "444ml" stated plainly in `sku_name`, no multiplier language present so pack_count corrected to 1). 96 content-fixing UPDATEs total (95 distinct taxonomy_ids, one entry touched twice across two defect classes), each resetting `_meta` to unreviewed and setting `meta_agent='CLAUDE_CODE'`. 451 rows judged correct (Tier 1 false positives): 435 `wrong_field_order` hits were `BRD-UNDEFINED` products correctly omitting brand from `canonical_name` per existing precedent (not a defect); the remaining 16 were brand-casing "mismatches" traced to `brand_dict` itself using non-standard casing (`"Safe guard"`, `"Benice"` vs. the taxonomy's more-correct `"Safe Guard"`/`"BeNice"`/`"Kirei Kirei"`) — a `brand_dict` data-quality note, not a taxonomy fix, plus one bilingual Thai+English `Protex` name and 4 legitimate two-size bundle entries (`Babi Mild ... 800ml + Refill 350ml x3 Bundle`) that correctly trip the "multiple sizes in one name" regex. **Findings left unfixed, flagged for follow-up (not blockers — nothing prevented completing the session):** (1) 2 taxonomy entries (SKU-107983, SKU-108497) are wet-tissue/wet-wipe products (ทิชชู่เปียก) mapped into the body_wash taxonomy under a garbage `brand="S"` (mis-scanned from a `(S)` size code in `sku_name`) — a genuine scope violation, but left as-is rather than deleting the `product_taxonomy_map` rows per this session's "never delete a row" instruction; needs a human scope decision. (2) SKU-108074 (3M hand soap) and SKU-108753 (a health-coffee product with "Deproud Official Store" baked into the name) are similarly out-of-category entries left untouched for the same reason. (3) ~14 excess-content-flagged entries are raw Thai marketing copy dumped wholesale as `canonical_name` (values look directionally correct, just not distilled to `Brand + Line + Size` format) — likely a much broader pattern across reseller-sourced entries in this category than just these 14; deferred to a dedicated distillation pass rather than one-off fixes this session. Hard gates re-verified post-run without `--skip-coexistence` (this category has already shipped): G1=0, G2=0, G5=0, placeholder-leak=0, structured-fields-missing=0%. Confidence distribution left behind: 451 `unconfident` (first-ever review, per the promote-on-agreement rule), 2,546 `unreviewed` (2,451 never touched this session + 95 freshly fixed and reset for re-review next run), out of 2,997 in the worklist. | ✅ 95 distinct entries fixed (96 UPDATEs), 451 judged correct, hard gates clean. Biggest structural finding (71-entry SKU-001xxx catch-all block, `"(all variants)"` stub naming) has its naming-convention violation fixed but its underlying per-product genericness flagged as Full-Rebuild-scope work, not resolved here. 3 scope-violation entries (wet wipes ×2, hand soap, coffee) and ~14 verbose-canonical-name entries flagged for follow-up, left unmodified |
| Jul 21 | 63 (auto-discovery, `_meta`-tracked) | Third auto-discovery run — worklist = 2,550 distinct never-reviewed/unconfident entries (down from 2,997 as promotions to `confident` accumulate). Tier 1 SQL sweep (same 6 flags as Session 62) flagged 67 distinct entries: 0 stub-leak, 4 duplicate-brand, 3 wrong-field-order, 9 brand-casing-mismatch, 13 excess-content, 43 canonical-field-mismatch. 54 were false positives matching established precedent (brand_dict casing artifacts — NAIVE/Naive, Oxe Cure, BeNice, Medix5.5/Medix 5.5; BRD-UNDEFINED brand-omission convention; "360" brand-text/size-text collision; the SKU-001xxx `Multiple Variants` sub-line-not-printed convention) and were promoted per the agreement rule. **Biggest new finding, not fixed this session:** a systemic scope-contamination pattern — 56 entries (~5.32M THB Apr-2026 GMV) whose `canonical_name`/`product_line` are literally liquid **hand wash** (สบู่เหลวล้างมือ) products, explicitly excluded by this category's own scope rules, minted into the body_wash taxonomy anyway. The largest coherent piece is an 8-entry Kirei Kirei "Foaming Hand Wash Refill" product line (~4.6M THB) sitting alongside the brand's legitimate "Foaming Body Wash" line; the remainder is ~48 long-tail reseller entries (3M, BJC, Double A Care, K-Plus, Dshow, Watsons, Diversey, and others). Per this session's "never delete a row" constraint these can't be cleanly resolved (the map rows would need deletion or reroute-to-NULL, both out of scope) — flagged for a dedicated remediation session with authority to reroute/null them. Two smaller new instances of the same class: SKU-107803 (Veet Hair Removal Cream — not a wash product at all) and SKU-107904/SKU-107963 (fixed multi-item bundles where body wash is only one of several unrelated product types in the set, matching the Session 60 "mostly-other-category fixed set → leave NULL" precedent, except these already have taxonomy_ids). **Fixes applied:** 23 distinct taxonomy entries content-fixed — 7 raw-Thai-marketing-copy `canonical_name`/`product_line` dumps distilled to `Brand + Line + Size` (continuing the distillation pass Sessions 61/62 deferred; found via the `excess_content` Tier 1 flag), 4 new instances of the comma-thousands-separator size misparse bug from Session 61's changelog (`"2,800 มล."`→wrongly read as `"800ml"`, same pattern for 3 more: Suk Skin 500→1500ml, Be Fresh 800→3800ml, Kin Hotel 800→3800ml, Benice 800→2800ml), 3 `(all scents)/(all products)/(all scents and variants)` placeholder stubs renamed to the established `Multiple Variants` convention (LUX — which also had a bogus fixed `size="70g"` on a 263-product bucket, corrected to `size=NULL, is_multi_size=TRUE` per the Session 62 precedent; Lush; Bath & Body Works), 1 single-product `[ทุกกลิ่น]`("all scents" option-selector) entry distilled to its real product line (Grace and Glow, `is_multi_variant=TRUE`), 1 `(unresolved)` product-line write-up per `llm-extraction-rules.md` §3's explicit-unresolved convention combined with an image-confirmed size correction (KO Rich — image read confirmed real product line "Organic Tea Tree Oil Essence" and size "60g", neither stated in `sku_name` text), 1 more `(unresolved)` write-up with no image check needed (Old Spice, a 10-scent assortment with no single nameable line), 1 pack-count fix from an explicit case-multiplier stated in `sku_name` (Madame Louise "ยกลัง24ชิ้น" → pack_count 1→24), 1 pack-count fix from unambiguous "ซื้อ 2 แถม 1" language where every mapped product agreed (Tea Tree Shower Gel 400ml → pack_count 1→3). **6 `product_taxonomy_map` rows rerouted** (not re-minted) to existing correct entries where a specific product's `sku_name` clearly stated a different pack/size than the entry it was sitting under, and the correct entry already existed: 1 Babi Mild 350ml product with explicit "x12" text → rerouted from the plain 350ml entry to the existing 350ml×12 entry; 3 Sebamed Baby Bubble Bath products explicitly "1,000ml" → rerouted from the 200ml entry to the existing 1000ml entry; 2 Vaseline Gluta Glow 425ml products explicitly "425 มล.x2" → rerouted from the single-unit entry to the existing 425ml×2 entry. **Flagged, left unfixed (not blockers):** SKU-057386 (Asepso "450ml" entry conflates bar soap 70-80g products and 450/500ml liquid GWP-promo products under one entry — needs a proper split, out of this session's per-row-reroute budget); SKU-058270 (Tea Tree 500ml entry has 2 of 15 mapped products stating an explicit "ซื้อ 2 แถม 1" x3 multiplier, but no 500ml×3 entry exists yet to reroute them to — would need a new SKU mint); SKU-062018 (Enfant Foam Mousse entry mixes a genuine 2×-refill-pouch product config with a bottle+separate-refill-pouch bundle config under one `pack_count`, ambiguous which the field should reflect — left as-is). Previously-flagged unfixed findings re-confirmed still present, not re-litigated: SKU-062075/SKU-062024 (shampoo-only contamination, Session 62), SKU-107983/SKU-108497 (wet tissue) and SKU-108074 (3M hand soap, now understood as one instance of this session's larger hand-wash cluster finding). **Environment note:** hit a `Could not serialize access... due to concurrent update` BigQuery error on one `_meta` UPDATE, consistent with Session 62's observation of a concurrent session active on shared infrastructure during this run; the statement was simply retried and succeeded — no data loss, but confirms multiple sessions can race on this table's row-level locks. All hard gates re-verified post-run without `--skip-coexistence`: G1=0, G2=0, placeholder-leak=0, G5=0, structured-fields-missing=0%. Confidence distribution left behind: 494 `confident`, 241 `unconfident`, 2,260 `unreviewed`, across 2,995 distinct taxonomy entries mapped to this category. | ✅ 23 distinct entries content-fixed + 6 map rows rerouted, 288 judged correct (54 Tier 1 false positives + 234 Tier 2 GMV-sampled), hard gates clean. New large-scale finding (56-entry / ~5.32M THB hand-wash-in-body-wash scope contamination, centered on an 8-entry Kirei Kirei product line) flagged for a dedicated remediation session with delete/reroute authority — not resolved here per this session's constraints. Continued the raw-Thai-marketing-copy distillation pass from Sessions 61/62 (7 more entries resolved) and extended Session 61's comma-thousands-separator size bug fix to 4 new instances |
| Jul 22 | 64 (auto-discovery, `_meta`-tracked) | Fourth auto-discovery run — worklist = 2,501 distinct never-reviewed/unconfident entries (down from 2,550 in Session 63). Tier 1 SQL sweep flagged 43 distinct entries: 0 stub-leak, 2 duplicate-brand, 2 wrong-field-order, 6 brand-casing-mismatch, 3 excess-content, 32 canonical_field_mismatch. 37 were false positives matching established precedent (30 SKU-001xxx `Multiple Variants` sub-line-not-printed convention + 1 combo case; 5 `Benice`/`BeNice` and `Oxe cure`/`Oxe Cure` brand_dict casing artifacts; 1 `BRD-UNDEFINED` brand-omission convention, SKU-108747) and were promoted per the agreement rule. The remaining 6 Tier 1 flags are already-known, still-unresolved scope violations re-confirmed present, not re-litigated: SKU-107983/SKU-108497 (wet tissue, flagged since Session 61), SKU-108048/SKU-108074/SKU-108724 (BJC/3M/Double A Care hand-wash-in-body-wash, part of Session 63's 56-entry cluster), SKU-001475 (garbage `12/+＝` brand bucket, flagged since Session 62 for dedicated brand_dict cleanup). **New finding (via Tier 2 GMV-prioritized sample of the top 150 clean rows by Jun-2026 GMV, ~66% of clean-worklist GMV):** a distinct, previously-undiscovered mechanical defect — 77 entries across many brands had a stray Thai word "ขนาด" ("size") leaked into `product_line`/`canonical_name` immediately before the real size value (the same root-cause pattern Session 62 fixed once for `SKU-107747` Goongbe, but never swept category-wide). Fixed in one bulk `REPLACE`-based UPDATE that substitutes each row's own stored `product_line` substring for its size-word-stripped version inside `canonical_name`, avoiding any risk of touching an unrelated "ขนาด" occurrence elsewhere in the string. Separately, investigating the `Madame Louise` brand's raw-Thai-marketing-copy pattern surfaced a related but distinct cluster: 5 taxonomy entries (SKU-039016 Shokubutsu, SKU-039027 Smooto, SKU-039033 Kodomo, SKU-039055 Snake Brand, SKU-039061 Care — pre-Phase-5 `SKU-039xxx` globally-shared catch-all buckets, an older sibling series to the `SKU-001xxx` block) were literally named `"{Brand} Body Lotion / Moisturizer"` despite every one of their mapped products (confirmed via full `sku_name` audit, ~451K THB combined GMV) being genuine body wash/shower gel/soap — a pure naming-category mismatch, not an actual cross-category product problem. Renamed to the established `{Brand} Body Wash / Shower Gel Multiple Variants` convention (`is_multi_size`/`is_multi_variant` were already TRUE on all 5, only the text was wrong). 3 individual raw-Thai-marketing-copy distillations continuing Sessions 61–63's deferred pass: `SKU-107749` (`Madame Fin ​ Madame Fin มาดามฟิน ครีมอาบน้ำ 450ml` → duplicated-brand-in-product_line bug, same pattern as Session 61's Aveeno fix, collapsed to one `Madame Fin`), `SKU-107748` (`LUX ใหม่! เจลอาบน้ำ ลักส์ สปาร์คกลิ้ง ขนาด 700มล - Sparkling 700ML 700ml` → distilled to `LUX Sparkling Body Wash 700ml`, matching sibling LUX entries' naming convention), `SKU-107755` (`Madame Louise 2 ฟรี 2 snow lotus 2 + สบู่ 2 ชิ้น 120g x4` → distilled to `Madame Louise Snow Lotus Soap 120g x4`, "Snow Lotus" being Madame Louise's established product line per its other taxonomy entries). 85 distinct taxonomy_ids content-fixed total (77 + 5 + 3), each resetting `_meta` to unreviewed and setting `meta_agent='CLAUDE_CODE'`. **Investigated but left unfixed (not blockers, GMV impact too small or no authority to act):** `SKU-001484` (`3M Medical Body Wash / Skin Cleanser Multiple Variants`, ~295K THB) is confirmed 100% liquid hand soap across all 27 mapped products — a whole-entry instance of Session 63's hand-wash cluster, not previously itemized by SKU ID; `SKU-001442` (`Protex Antibacterial Body Wash Multiple Variants`, ~272K THB) is ~17.6% hand-wash-contaminated (47.9K of 272K THB across 116 mapped products) — a partial instance of the same cluster; both need the cluster's already-flagged dedicated reroute/split remediation, not a one-row fix. `SKU-062070` (`Johnson's Baby Bedtime Bath 1000ml x2`) has a minor size mismatch — 27.5K of 296K THB GMV is genuinely 750ml×2 product misrouted under the 1000ml×2 entry, but no 750ml×2 entry exists yet to reroute to and the GMV is too small to justify minting one this session. `SKU-107760` (`วิจิตรชื่น`) and `SKU-107762` (`V Care`) are undistilled raw-Thai product names without a clearly separable real product-line signal — left as-is pending a clearer read, not marked reviewed. Previously-flagged unfixed findings re-confirmed still present, not re-litigated: the Session 63 hand-wash cluster (Kirei Kirei's 8-entry "Foaming Hand Wash Refill" sub-line, ~4.32M THB across SKU-062025–062032, re-confirmed via this session's GMV sample), SKU-062075/SKU-062024 (shampoo-only contamination, Session 62), SKU-062018 (Enfant Foam Mousse bundle pack_count ambiguity, Session 63), SKU-107983/SKU-108497/SKU-108074 (wet tissue/hand soap, Session 61/63). 166 rows judged correct with no fix needed (37 Tier 1 false positives + 129 Tier 2 GMV-sampled, including two genuinely-unresolved-size re-confirmations — `SKU-062000` Dr.Pong and `SKU-058396` ASAKA — checked against `raw_niq_history` this session, which does not exist for this table, confirming text/image were already the only available signals per Session 62's original finding). Hard gates re-verified post-run without `--skip-coexistence`: G1=0, G2=0, placeholder-leak=0, G5=0, structured-fields-missing=0%. Confidence distribution left behind: 640 `confident`, 111 `unconfident`, 2,244 `unreviewed`, across 2,995 distinct taxonomy entries mapped to this category. | ✅ 85 distinct entries fixed (77 bulk + 5 mislabeled-bucket rename + 3 individual distillations), 166 judged correct, hard gates clean. New mechanical defect class found and fixed category-wide (77-entry stray-"ขนาด"-word leak). New naming-mismatch cluster found and fixed (5 pre-Phase-5 `SKU-039xxx` buckets mislabeled "Body Lotion/Moisturizer" for products that are actually body wash). Two new specific instances (`SKU-001484` fully, `SKU-001442` partially) added to Session 63's already-flagged hand-wash-in-body-wash cluster, still awaiting dedicated remediation; one new small size-misroute (`SKU-062070`) flagged, too low-GMV to fix via new SKU mint |
| Jul 22 | 65 (auto-discovery, `_meta`-tracked) | Fifth auto-discovery run — worklist = 2,355 distinct never-reviewed/unconfident entries (down from 2,501 in Session 64). Tier 1 SQL sweep flagged only 10 distinct entries (0 stub-leak, 2 duplicate-brand, 2 wrong-field-order, 0 brand-casing-mismatch, 3 excess-content, 4 canonical_field_mismatch) — the smallest Tier 1 yield yet, consistent with 4 prior sessions having cleaned up most mechanical defects. All 10 were already-known: 3 hand-wash-cluster instances re-confirmed present (SKU-108048 BJC, SKU-108074 3M, SKU-108724 Double A Care), 2 wet-tissue scope violations re-confirmed (SKU-107983, SKU-108497), SKU-001475 (garbage-brand bucket, Session 62), 3 SKU-001xxx `Multiple Variants` sub-line-not-printed false positives (LUX, Bath & Body Works, Lush — judged correct), and SKU-107749 (Madame Fin) traced to a literal zero-width-space (U+200B) trailing the brand's own `brand_dict.canonical_name` — a brand_dict artifact, not a taxonomy defect, judged correct. **New finding (via Tier 2 GMV-prioritized sample of the top 150 clean rows, ~70% of clean-worklist GMV):** `SKU-057228` (`Kodomo Body Wash 400ml`, pack_count=1) was ~95% (152.8K of 161.1K THB) a "ซื้อ1แถม1"/`400+400มล.` genuine 2-bottle promo mis-bucketed as a single unit under the wrong, generic `product_line="Body Wash"` — existing correct entries already existed (`SKU-058131` "Kodomo Head-to-Toe Wash 400ml x2", `SKU-058144` "...400ml" single) so 20 `product_taxonomy_map` rows were rerouted (8 to the x2 entry, 12 to the single) rather than minting new SKUs; the now-orphaned `SKU-057228` was left as-is per the never-delete rule. Similarly, 1 `SKU-057310` ("Snake Brand Cool Shower Gel 450ml") map row explicitly stating "450 มล.+450มล." (genuine same-size 2-pack, not the entry's dominant bottle+refill-pouch GWP pattern) was rerouted to the existing `SKU-107791` ("...450ml x2") entry. **Biggest new finding, a systemic defect class not previously characterized at scale:** 320 distinct taxonomy entries category-wide (13.6% of all entries mapped to this category, `is_multi_variant=FALSE`) have `product_line` set to a bare generic category word (`"Body Wash"`, `"Shower Gel"`, `"Shower Cream"`) — exactly the Tier-C "generic stub" failure pattern `quality-standards.md` §3 uses as its own worked example. Spot-checked 6 of these via image/text: 3 confirmed genuine misses with a real printed line recoverable (`SKU-058010` Dove — image showed "Deeply Nourishing" printed under the logo, matching the brand's existing `SKU-057165`/`SKU-057166` "Deeply Nourishing"/"Go Fresh Revive" sibling entries, fixed; `SKU-057512` Moist Diane — sku_name itself stated "PROTECT Body Wash", fixed to product_line="Protect"; `SKU-058231` Protex 600ml — image confirmed "Fresh" as one real line, but the entry's mapped products span 3 different lines, Fresh/Propolis/Blossom Care, under one bucket — too entangled to one-line-fix, left flagged), 1 confirmed a multi-variant reseller listing masquerading as a single product (`SKU-058022` LUX 400ml x2 — image showed a 9-way buyer-choice-of-scent selector, Rosy/Even Tone/Dewy/Magical Orchid/Hydrating/Bouncy/Peachy/Healthy/Renewal Glow, needs restructuring to a `Multiple Variants` bucket rather than a single line name, left flagged), and 2 (SIRIRAJ, Tarakaru) appeared to genuinely lack any distinguishable printed line in the available text. Given the volume (320 entries) and that a correct fix requires a per-product image read to recover the real line name, this cannot be resolved in one targeted session — flagged for a dedicated Full-Rebuild-scale remediation pass, same treatment as the SKU-001xxx/039xxx catch-all genericness flagged in Sessions 61/64. Also found via this same sample: `SKU-108240` (`Kaff & Co.` GWP freebie) had raw promotional Thai copy ("ของแถม เมื่อมียอดสั่งซื้อสุทธิครบ 1,299.- รับฟรี...") dumped wholesale into `product_line`/`canonical_name` instead of the real product name "Safflower Soap" (สบู่ดอกคำฝอย) — distilled, continuing Sessions 61–64's raw-copy cleanup pass. 4 entries (`SKU-058003` Dr.Pong BHA Acne Clear Body Cleanser, `SKU-058291` Lush Rose Jam, `SKU-058309` Lush Sleepy, `SKU-062001` Dr.Pong Natural Volcanic Sulphur Soap) had `size=NULL, is_multi_size=FALSE` with no size anywhere in any mapped `sku_name` — confirmed via image (140ml, 250g, 110g, 70g respectively, all legible on the pack/box) and filled in, per the text→image→spec→description priority chain in `llm-extraction-rules.md` §2. 7 distinct taxonomy_ids content-fixed total, each resetting `_meta` to unreviewed and setting `meta_agent='CLAUDE_CODE'`; 21 `product_taxonomy_map` rows rerouted across 2 source entries. **New instance added to the Session 63 hand-wash-in-body-wash cluster:** `SKU-001488` (`Bath & Body Works Shower Gel / Body Wash Multiple Variants`, ~89K THB) is confirmed 100% hand soap and hand sanitizer listings (`Hand Soap Refill`, `PocketBac Hand Sanitizer`) — zero genuine body wash products — a full-entry instance not previously itemized by SKU ID; needs the cluster's already-flagged dedicated reroute/split remediation. Previously-flagged unfixed findings re-confirmed still present, not re-litigated: the 56-entry/~5.32M THB hand-wash cluster including Kirei Kirei's 8-entry refill sub-line (Session 63), `SKU-001484`/`SKU-001442` (Session 64), `SKU-062075`/`SKU-062024` (shampoo-only contamination, Session 62), `SKU-062018` (bundle pack_count ambiguity, Session 63), `SKU-107983`/`SKU-108497`/`SKU-108074` (wet tissue/hand soap), `SKU-062070` (size misroute, Session 64), `SKU-107760`/`SKU-107762` (undistilled raw-Thai names, Session 64). 90 rows judged correct with no fix needed (3 Tier 1 false positives + 87 Tier 2 GMV-sampled, including a re-confirmation of Session 63's `SKU-062010` KO Rich fix — its huge single-sku_name 11.36M THB entry checked against its own prior image-derived size/product_line, still consistent, no regression). Hard gates re-verified post-run without `--skip-coexistence`: G1=0, G2=0, placeholder-leak=0, G5=0, structured-fields-missing=0%. Confidence distribution left behind: 692 `confident`, 90 `unconfident`, 2,213 `unreviewed`, across 2,995 distinct taxonomy entries mapped to this category. | ✅ 7 distinct entries content-fixed (2 product_line/canonical_name distillations, 1 GWP-copy distillation, 4 image-confirmed size fills) + 21 map rows rerouted across 2 entries, 90 judged correct, hard gates clean. Headline new finding: a 320-entry (13.6% of category) generic-product_line defect class quantified category-wide via a full-worklist scan and confirmed genuine (not an established convention) via targeted image spot-checks — flagged for dedicated Full-Rebuild-scale remediation, 2 highest-confidence instances fixed this session as a down payment. One new fully-contaminated instance (`SKU-001488`, Bath & Body Works) added to the Session 63 hand-wash cluster |
| Jul 22 | 66 (auto-discovery, `_meta`-tracked) | Sixth auto-discovery run — worklist = 2,303 distinct never-reviewed/unconfident entries (down from 2,355 in Session 65). **Headline finding, a category-wide rule-compliance sweep, not a newly-discovered defect:** `llm-extraction-rules.md`'s §2 rule banning "Multiple Sizes"/"Multiple Variants" in `canonical_name` unconditionally (even with `is_multi_size`/`is_multi_variant=TRUE`) was strengthened same-day (2026-07-22), superseding the `th_softdrink` precedent that had sanctioned this exact phrasing — the doc explicitly calls out that existing entries using it "need correcting, not exempting." A full-category scan (not scoped to just the 2,303-entry worklist, since 219 of the 706 hits were already marked `confident` from before the rule tightened) found 706 entries category-wide using this banned phrasing: the 71 SKU-001xxx/5 SKU-039xxx pre-Phase-5 catch-all buckets renamed to this convention in Sessions 61/64, plus 627 previously-uncharacterized SKU-107xxx entries from Session 59's top-up mint (raw `sku_name` text with a literal `" Multiple Sizes"`/`" Multiple Sizes xN"` suffix appended — the mechanical form Session 59's "628 of 1,861 new entries have no extractable size" note took, never flagged as a naming defect until today's rule change). Fixed all 706 in one bulk `REGEXP_REPLACE` UPDATE stripping the banned phrase while preserving any trailing `xN` multiplier (`is_multi_size`/`is_multi_variant` already carry that semantic, so no information is lost) — placeholder-leak gate re-confirms 0 remaining hits post-fix. This is a mechanical compliance fix, not a distillation pass: the underlying raw-Thai-marketing-copy-as-`product_line` quality issue on the SKU-107xxx block (same class flagged at smaller scale every session since 61) is untouched and remains flagged for the same future dedicated pass. **Second finding:** the `null_size` Tier 1 flag (wired in for the first time today, alongside the stub checks) surfaced 128 in-worklist entries with `size IS NULL AND is_multi_size IS NOT TRUE` — previously uncounted since no automated check existed. Cross-validated each against its own mapped `sku_name`s (text→image priority per `llm-extraction-rules.md` §2, image reads deferred given the volume): 5 had a size backfillable directly with no existing duplicate entry (`SKU-058105` Babi Mild Shower Milk 190ml x2, `SKU-107882` Fyne Acne Defence Body Wash 4L, `SKU-001485` Snake Brand Prickly Heat Body Wash 450ml, `SKU-057222` Protex Body Wash 180ml x3, `SKU-058223` Giffarine Shower Gel 500ml); 10 had a size consistently stated in every mapped `sku_name` that turned out to exactly duplicate an already-existing sized sibling entry (e.g. `SKU-057278` "Parrot Shower Cream" NULL-size duplicating `SKU-057274` "...400ml") — rerouted (39 `product_taxonomy_map` rows) rather than backfilled, leaving the NULL-size originals orphaned per the never-delete rule; 2 (`SKU-057534` Rueanmaihom, `SKU-058099` Babi Mild Shower Milk x3) genuinely split across two different sizes among their own mapped products with existing sized twins for both — rerouted per-product by regex-matching each product's own stated size (8 rows total); 2 SKU-001xxx buckets (`SKU-001500`, `SKU-001488`) were confirmed genuinely multi-size and set `is_multi_size=TRUE` instead. The remaining 104 of 128 have no size signal anywhere in any mapped `sku_name` (broader regex covering `มิลลิลิตร`/`ม.ล` spelled-out forms, not just `มล`/`ml`, still found nothing) — genuinely unresolved without an image read at a scale beyond this session's budget; flagged for a future session with image-read capacity. **Third finding, via a GMV-prioritized Tier 2 sample of the STEP 2b pack-count sweep's top 40 rows:** confirmed 4 genuine "ซื้อ 2 แถม 1" (buy-2-get-1, same product) Tea Tree Shower Gel 500ml products (~277K THB combined) sitting under the pack_count=1 `SKU-058270` entry — this is the exact gap Session 63 flagged ("no 500ml×3 entry exists yet to reroute them to") but at higher confirmed GMV than then known; similarly found 2 Sebamed Baby Bubble Bath 1000ml×2 products (~56K THB) and 1 Pharmapure Gentle Body Wash 450ml×3 product (~50K THB) with no existing multiplier-sized twin. Claimed a 200-slot block (`SKU-112406–112605`) and minted 3 new entries (`SKU-112406` Tea Tree Shower Gel 500ml x3, `SKU-112407` Sebamed Baby Bubble Bath 1000ml x2, `SKU-112408` Pharmapure Gentle Body Wash 450ml x3), rerouting all 7 source products. Also rerouted 1 Parrot Shower Cream product (`24760623889`, ~97K THB, "1แถม1" same-size-same-product) from the pack_count=1 `SKU-057272` entry to the already-existing `SKU-057283` "...500ml x2" twin (no mint needed), and fixed `SKU-107894` (Dermapon DMP, a dedicated single-product entry, "ขวด 450 มลฟรี ขวด450มล" = buy-1-get-1-same-size) directly: `pack_count` 1→2, canonical_name appended `x2`. **Fourth, via the same Tier 2 GMV sample against the full worklist (top 35 clean rows by Jun-2026 GMV):** 2 more raw-copy/duplicate-brand distillations continuing the Sessions 61–65 deferred pass — `SKU-037486` (`Aveeno Baby Body Care 354ml x2`, product_line was literally `"Aveeno Baby Body Care"`, redundantly repeating the brand per the same bug class as Session 61's Aveeno fix — corrected product_line to `"Baby Body Care"`) and `SKU-107771` (`Cow Brand คาว แบรนด์ บาวน์เซีย บอดี้ โซป...`, Thai transliteration of the brand name duplicated immediately after the English brand name — distilled to `"Cow Brand Bouncia Body Soap Premium Moist + Refill 460ml"`). One new instance added to the Session 63 hand-wash-in-body-wash cluster: `SKU-107774` (`K-Plus เค-พลัส สบู่เหลวล้างมือ...3800ml`, ~95K THB) is explicitly `สบู่เหลวล้างมือ` — liquid hand wash — one of the "long-tail reseller entries" Session 63's summary already counted but never itemized by SKU ID; left unfixed, no delete/reroute authority this session. All previously-flagged unfixed findings re-confirmed still present via this session's Tier 1/Tier 2 passes, not re-litigated: the 56-entry/~5.32M THB hand-wash cluster (Kirei Kirei's 8-entry refill sub-line, `SKU-001484`/`SKU-001442`/`SKU-001488`, `SKU-108048`/`SKU-108074`/`SKU-108724`), the 320-entry generic-product_line class (Session 65), `SKU-062075`/`SKU-062024` (shampoo-only contamination), `SKU-062018` (bundle pack_count ambiguity), `SKU-107983`/`SKU-108497` (wet tissue), `SKU-001475` (garbage brand bucket), `SKU-058231` (Protex 600ml entangled sub-lines), `SKU-062070` (size misroute). 750 distinct taxonomy_ids content-fixed total (706 bulk stub-strip + 5 size backfills + 2 multi-size flags + 3 new mints + 1 direct pack fix + 2 distillations, some overlapping the 706), each resetting `_meta` to unreviewed and setting `meta_agent='CLAUDE_CODE'`; 55 `product_taxonomy_map` rows rerouted (39 duplicate-twin + 8 split-by-size + 7 new-mint + 1 Parrot); 3 new SKUs minted. 34 rows judged correct with no fix needed (32 Tier 2 GMV-sampled + 2 Tier 1 false positives re-confirming Session 65's brand_dict-artifact findings for `SKU-107749` Madame Fin and `SKU-057512` Moist Diane). Hard gates re-verified post-run without `--skip-coexistence`: G1=0, G2=0, placeholder-leak=0 (confirms the 706-entry fix left zero residual banned phrasing), G5=0, structured-fields-missing=0%. Confidence distribution left behind: 501 `confident` (down from 692 — expected, since 219 previously-confident entries lost their status when the strengthened rule invalidated their old-convention naming and reset them for fresh review), 59 `unconfident`, 2,426 `unreviewed`, across 2,986 distinct taxonomy entries mapped to this category. | ✅ 706 entries brought into compliance with today's strengthened "no Multiple Sizes/Variants" rule (category-wide, not just the worklist) — the session's headline output. Plus: 17 null-size entries resolved (5 backfilled, 10+2 rerouted to existing twins, 2 flagged multi-size), 7 products moved via 3 new SKU mints + 1 reroute for confirmed D5 pack-count misses (~480K THB combined GMV), 1 direct pack-count fix, 2 more raw-copy distillations, 1 new hand-wash-cluster instance flagged. Hard gates clean; all already-known unresolved clusters re-confirmed present and not re-litigated |
| Jul 21 | 62 (auto-discovery, `_meta`-tracked) | Second auto-discovery run — worklist = 2,997 distinct never-reviewed/unconfident entries (same size as Session 61's, since promotion to `confident` requires two consecutive agreeing reviews and Session 61 was the first). Tier 1 SQL sweep (now including the new `canonical_field_mismatch` gate added to mirror `qa_report.sh`'s independent wrapper-side check) flagged 0 stub-leak, 6 duplicate-brand, 437 wrong-field-order, 13 brand-casing-mismatch, 21 excess-content, 72 canonical_field_mismatch candidates. **Biggest finding, a new defect class:** 69 entries (mostly SKU-062xxx block) had `size` stored as bare digits with no unit (`"400"` instead of `"400ml"`, `"160"` instead of `"160g"`) despite `canonical_name` showing the correct unit — violates `llm-extraction-rules.md` §2's explicit-units rule; fixed in one bulk UPDATE that regex-extracts the correct unit from each row's own `canonical_name` and appends it to `size`. Separately, 46 of the SKU-001xxx `Multiple Variants` catch-all entries (session 61's naming fix) had a single non-representative `size` value populated (e.g. `"500ml"`) despite genuinely spanning many distinct sizes per bucket (confirmed by sampling underlying `sku_name`s — one bucket alone spans 26 distinct sizes across 102 products); corrected to `size=NULL, is_multi_size=TRUE`, consistent with the multi-size convention used elsewhere in this pipeline — the sub_line-word-not-in-canonical-name half of the same flag (e.g. `sub_line="Antibacterial"` not printed in `"{Brand} Body Wash Multiple Variants"`) is by design for this bucket convention, not a defect, and was judged correct instead. 7 individual (non-bucket) entries had `size` populated but completely absent from `canonical_name` (e.g. `"Neutrogena Rainbath Shower Gel"` with `size="473ml"` never appended) — fixed by appending the size (and, for one x2 entry, inserting it before the `xN` suffix). 4 Sebamed entries were missing their `pH 5.5` sub-line segment (present in `product_line`, absent from `canonical_name`) — appended. `SKU-107747` (Goongbe) had a stray Thai word "ขนาด" ("size") leaked into `product_line`/`canonical_name` between the line name and the actual size — stripped. `SKU-062066` ("Eucerin Body Wash / Bath") was flagged `is_multi_size=TRUE` with `size=NULL`, but every one of its 12 mapped products is confirmed 400ml from `sku_name` (different formulas — Urea Repair, Omega Atopi, pH5 — not different sizes); corrected to `is_multi_variant=TRUE, is_multi_size=FALSE, size='400ml'`. Two brand-assignment defects found via the `duplicate_brand` flag tracing to the same root cause: `SKU-107763`/`SKU-108186` (`"12/+＝ เบนเนท สบู่ วิตามินซีแอนด์อี 130g x12"`) were genuine Bennett Vitamin C&E 130g×12 soap listings misassigned to a garbled global catch-all brand bucket (`BRD-SG-08876`, "12/+＝" — an OCR/promo-banner artifact shared as a garbage brand across at least 7 unrelated categories including pet food, toothpaste, and coffee) instead of the real Bennett brand (`BRD-SG-00038`) — an exact duplicate of the already-existing `SKU-036012` entry, so rerouted (not re-minted): 3 `product_taxonomy_map` rows repointed to `SKU-036012`, old entries left orphaned per the never-delete rule. `SKU-109047` ("สบู่พอลล่าแพลตตินัม" a real Paula-brand soap, no existing Paula taxonomy entry) and `SKU-108747` ("โปร 12.12 สบู่เหลว โพรโพลิส" — a promo-code string with no real brand identifiable, and `pack_count` wrongly left at 1 despite an explicit "×12" in `sku_name`) were also under the same garbage bucket — reassigned to `BRD-SG-13285` (Paula, `x12` pack_count corrected from an unstated size) and `BRD-UNDEFINED` (pack_count corrected to 12) respectively. The `BRD-SG-08876` bucket's other ~65 entries across other categories, and its remaining body_wash instance `SKU-001475` (a `Multiple Variants` catch-all with no single identifiable brand among its contents), are out of scope for this session — flagged for a dedicated cross-category brand_dict cleanup. 126 distinct taxonomy_ids content-fixed (several via more than one UPDATE statement touching different fields), each resetting `_meta` to unreviewed and setting `meta_agent='CLAUDE_CODE'`; 3 `product_taxonomy_map` rows rerouted. 496 rows judged correct with no fix needed (435 `wrong_field_order` `BRD-UNDEFINED` omissions and 1 `Medix5.5`/`"Medix 5.5"` spacing mismatch — both brand_dict-formatting artifacts, not taxonomy defects, re-confirming Session 61's exact precedent; 13 brand-casing mismatches, same brand_dict-casing-inconsistency pattern as Session 61 (`BeNice`/`Safe Guard`/`NAIVE`/`Oxe Cure`); 4 legitimate Babi Mild two-size bundle entries; 4 duplicate-brand false positives (`"360"` brand coinciding with `"360ml"` size text, plus the already-known-correct `Protex`/`Kirei Kirei` entries from Session 61 — both now promoted to `confident` on second agreeing review); the remaining SKU-001xxx sub-line-convention false positives and additional entries spot-checked in a GMV-prioritized Tier 2 sample of the top ~80 remaining rows by GMV). **Self-caught bookkeeping bug:** the STEP 5 "judged correct" bulk `_meta` update initially ran against a correct-list that overlapped 43 taxonomy_ids with rows already content-fixed this session (e.g. a SKU-001xxx entry whose `size` was nulled for the multi-size fix, but whose separately-true "sub_line not in canonical_name" aspect was also on the correct-judgment list) — this clobbered their post-fix `{"is_reviewed": false}` reset marker with a "reviewed, confident/unconfident" marker, which would have wrongly fast-tracked freshly-changed rows toward `confident` without a genuine fresh review. Caught by a post-hoc reconciliation of the fixed-set against the correct-set before finalizing counts; corrected with a follow-up UPDATE resetting all 43 back to the unreviewed marker. The 126/496 counts above and the confidence distribution below already reflect the corrected state. **New scope-contamination findings from the Tier 2 GMV sample, left unfixed (not blockers):** (1) `SKU-062075` ("Johnson's Baby Body Wash / Bath", ~552K THB GMV) — of 39 mapped products, only 3 mention wash/bath/shower at all; the other 36 are standalone kids'-shampoo listings (`แชมพูเด็ก`), explicitly out-of-scope per this category's own scope rules — a much larger-scale instance of the same "out-of-scope product silently bucketed" pattern Session 61 found at 1-2-row scale, here affecting ~36 products under one entry; needs a dedicated remediation pass to split the 3 genuine body-wash products out and determine disposition for the shampoo-only ones, not a one-row fix. (2) `SKU-062024` ("Enfant Baby Shampoo & Body Wash") shows the same pattern at smaller scale — at least 3 of its ~16 mapped products are standalone shampoo/conditioner-only listings with no wash component. (3) The `SKU-107983`/`SKU-108497` (wet-tissue) and `SKU-108074` (3M hand soap) scope violations flagged unfixed in Session 61 are still present (still no clean fix possible without deleting a map row). (4) Two entries (`SKU-062000` "Dr.Pong Natural Volcanic Sulphur Soap", ~6.1M THB GMV — a GWP freebie explicitly marked "งดจำหน่าย"/not-for-individual-sale in every mapped `sku_name`, hence no size stated anywhere; `SKU-058396` "ASAKA Body Wash", ~616K THB — no size in any mapped `sku_name` and the product image is a marketing lifestyle shot with no legible size text) were left with `size=NULL` after exhausting text and image signals per the extraction priority chain — genuinely unresolved, not a fix deferred. (5) The verbose-raw-Thai-marketing-copy `canonical_name` pattern flagged in Session 61 (~14 entries then) persists at similar scale, deferred to the same future distillation pass. **Environment note:** mid-session, shared `/tmp` filenames (`worklist.csv`, `tier1.csv`) were silently overwritten by what appears to be a concurrent session for a different category (sunscreen data observed) running on the same machine — caught via a content sanity-check before any decision was based on the corrupted files; all affected intermediate analysis was re-derived fresh from BigQuery into a session-unique temp directory. No BigQuery writes were affected (every write query was independently scoped to `master_table='shopee_th_body_wash'` at execution time), but this is worth fixing at the harness level (session-unique temp paths) to avoid relying on a human catching it next time. Hard gates re-verified post-run without `--skip-coexistence`: G1=0, G2=0, placeholder-leak=0, G5=0, structured-fields-missing=0%. Confidence distribution left behind: 445 `confident`, 52 `unconfident`, 2,498 `unreviewed`, across 2,995 distinct taxonomy entries mapped to this category. | ✅ 126 distinct entries fixed + 3 map rows rerouted, 496 judged correct, hard gates clean. New defect class found and fixed pipeline-wide-pattern: unitless `size` values (69 entries) and non-representative `size` on multi-size buckets (46 entries). Two brand-assignment fixes recovered 3 body-wash products from a cross-category garbage brand bucket into the correct existing Bennett entry, plus 2 more reassigned to their real/correct brand identity. New, larger-scale scope-contamination finding (`SKU-062075`, ~36 shampoo-only products under a body-wash entry) flagged for a dedicated remediation session, not resolved here. A self-introduced `_meta`-bookkeeping bug (fixed rows getting overwritten by the correct-judgment pass) was caught and corrected before session end |

---

| Jul 26 | 68 (top-up) | Wrapper cited a live pre-check of 835 unmapped model-grain rows (month 2026-06) — this is the exact residual Session 67 already closed to and independently confirmed as 100% correctly out-of-scope. Re-ran the live STEP 0 query rather than trusting either number: confirmed 835 rows / 534 distinct products / ~14.19M THB, matching Session 67's closing state exactly. Rather than accept that at face value (Session 60 precedent: re-verify, don't rubber-stamp), applied the category's documented OOS keyword gate at product grain with a broadened regex (adding bare English `\bwash\b`, `สบู่เหลว`, `ทำความสะอาดผิวกาย/หน้า` as in-scope signals, and `hand ?wash` to also catch the unspaced "Handwash" spelling) and found a genuine, previously-uncaught false exclusion: the category's own exclusion logic for "แชมพู/shampoo-tagged" rows only recognized the literal phrase `body wash` as a save signal, not bare English `wash` — so 2-in-1 "Baby Wash & Shampoo" combo products (Eucerin, Cetaphil, Aveeno, D-nee, Kodomo, DMP) were being swept into the standalone-shampoo exclusion bucket despite this exact product class already being extensively represented in existing taxonomy (confirmed via sibling entries, e.g. `SKU-107833` "Eucerin Baby Wash and Shampoo 400ml", `SKU-166039` DMP 3-in-1 200ml x2). Isolated 21 distinct products (29 model-grain rows, ~1.2M THB, 8.5% of the residual's GMV) matching this pattern and classified each individually rather than bulk-accepting the whole bucket: 6 were genuine cross-category/buyer-choice bundles (wash+lotion sets, 4-item Cetaphil sets, a literal slash-separated buyer-choice-of-4 listing) matching the Session 60 "mostly-other-category fixed set / can't determine what ships → leave NULL" precedent — correctly left NULL, not force-mapped (G4 would have been violated otherwise). The remaining 15 were genuine single-product 2-in-1s: checked each for a pre-existing (including orphaned) `product_taxonomy_map` row first per Session 67's discovered orphan-collision failure mode — none existed, clean to insert. 13 products bulk-matched by exact brand+line+size to 5 existing sibling entries (9→`SKU-107833` Eucerin 400ml, 1→`SKU-166039` DMP 200ml x2, 1→`SKU-166043` Kodomo Mild Original 800ml x2, 1→`SKU-166742` D-nee 750ml, 1→`SKU-165243` D-nee 350ml x2 — the last two at lower confidence given pre-existing pack_count inconsistencies/near-duplicate fragmentation already present on those sibling entries, not touched this session). The remaining 2 (Cetaphil "Baby Gentle Wash & Shampoo" 230ml, a distinct product line from the already-taxonomized "Baby Moisturising Bath & Wash" 230ml) had no existing sibling — claimed the instructed 835-slot block (`SKU-167037–167871`) and minted one new entry (`SKU-167037`), mapping both products to it; 834 slots unused. Re-scanned the three other OOS buckets (hand_wash 469 rows/288 products/~7.09M THB, scrub, feminine, bath_bomb/salt) at their top-GMV end for the same class of false exclusion before accepting them — all genuinely correct (Kirei Kirei/Dettol/3M/Common Ground hand-wash products, Dove/Benice/Perfect scrub, feminine wash, bath bomb/salt), no further misses found. Hard gates re-verified post-run without `--skip-coexistence`: G1(LLM-scoped)=0, G2=0, placeholder-leak=0, G5=0; a same-session integrity check (new map rows' `taxonomy_id` all `EXISTS` in `product_taxonomy`) also=0, guarding against the Session 67 orphan-reference failure mode. Final live re-verification: 819 rows / 519 distinct products / ~13.11M THB remain unmapped — every one accounted for by standalone-conditioner exclusions, the 6 already-classified bundles/variant-selectors, or the pre-existing hand-wash/scrub/feminine/bath-bomb buckets. Per-row canonical_name precision on the newly-touched entries not polished this session (coverage-first, consistent with Sessions 59/67), and the underlying pack_count-inconsistency/near-duplicate-fragmentation issue on the D-nee 350ml/750ml sibling entries used for reuse-matching is flagged but not fixed, deferred to `targeted_qa_fix.sh`. | ✅ Live gap re-verified as 835→819 rows / 534→519 distinct products (~14.19M→~13.11M THB) — 15 products routed (13 to 5 existing entries, 2 to 1 new mint `SKU-167037`), all via reuse-before-mint bulk text matching, zero image reads needed. Root-cause finding (broader than this one session's 15 rows): the category's `body wash` literal-phrase exclusion-override for shampoo-tagged listings misses bare English "wash," a blind spot that likely affects other categories using the same pattern — worth checking `llm-extraction-rules.md` §5 categories with an analogous keyword-override rule. Remaining 519 products independently re-verified as correctly out-of-scope or already-flagged-indeterminate, not a fresh backlog |
| Jul 26 | 69 (top-up) | Wrapper cited a live pre-check of 819 unmapped model-grain rows (month 2026-06) — the exact residual Session 68 already closed to and independently confirmed as correctly out-of-scope. Re-ran the live STEP 0 query rather than trusting the number: confirmed 819 rows / 519 distinct products / ~13.03M THB, matching Session 68's closing state (~13.11M THB) almost exactly. Independently re-classified the full worklist against the category's documented OOS keyword gate (hand wash, feminine, scrub, bath-bomb/salt, standalone shampoo/conditioner without a body-wash keyword) rather than accepting Session 68's summary at face value: 469 hand-wash rows (exact match to Session 68's own 469), 275 standalone-shampoo, 35 scrub, 19 bath-bomb, 9 feminine, and 5 unclassified distinct products that resolved to Session 68's already-documented "6 genuine cross-category/buyer-choice bundles" bucket (Cetaphil multi-item wash+lotion+shampoo sets, a slash-separated buyer-choice listing) — correctly left NULL, not re-litigated. Spot-checked the top 15 rows by GMV across the whole worklist (all in the hand-wash/standalone-shampoo/feminine buckets) — all correctly classified, no false exclusions at the high-GMV end. Rather than stop there, re-applied Session 68's own broadened in-scope-signal check specifically to the 275-row standalone-shampoo bucket — the exact bucket Session 68's last fix targeted — instead of assuming that bucket was now fully clean: found a further, distinct gap Session 68's fix didn't cover. Session 68 added bare English `\bwash\b` as an in-scope signal, but 2 products' sku_names use the Thai phonetic transliteration "วอช" for "wash" (not the Latin word, not `อาบน้ำ`), so neither the original exclusion logic nor Session 68's own broadened regex recognized them: `Dermollient Baby` "[Official Store] ออร์แกนิก เบบี้ เจนเทิล วอช แอนด์ แชมพู 250 ml" (Organic Baby Gentle Wash & Shampoo 250ml, product 46108553939, ~4.2K THB) and `Aveeno` via `Watsons Official Store` "อาวีโน่ เบบี้ วอช แอนด์ แชมพู 236 มล." (Baby Wash & Shampoo 236ml, product 1223259557, ~2.8K THB) — both genuine single-product 2-in-1s, same class as Session 68's 15. A third hit on the same broadened check (`Aveeno` "...เบบี้ สบู่เหลว เดลี่ วอช&แชมพู... Set Aveeno Baby Daily Care-x2", a fixed lotion+wash&shampoo bundle) was correctly left NULL, matching the Session 60/68 cross-category-bundle precedent. Checked both routable products for a pre-existing (including orphaned) `product_taxonomy_map` row first per the Session 67 orphan-collision precedent — none existed. Aveeno/Watsons bulk-matched by exact brand+line+size (236ml) to an existing sibling entry, `SKU-109362` "Aveeno Baby Wash & Shampoo...236ml" (one of two near-duplicate 236ml Aveeno entries already flagged as unresolved fragmentation — picked the cleaner-named one, fragmentation itself not touched, deferred to `targeted_qa_fix.sh` same as the D-nee case). Dermollient had no existing sibling (its one prior entry, `SKU-109036`, is a different line/size, "Baby Soft Cleanser 200g") — claimed a 200-slot block (`SKU-168072–168271`) and minted one new entry (`SKU-168072`, "Dermollient Organic Baby Gentle Wash & Shampoo 250ml"), mapping both products (199 slots unused). Hard gates re-verified post-run without `--skip-coexistence`: G1(LLM-scoped)=0, G2=0, placeholder-leak=0, G5=0; a same-session integrity check (both new map rows' `taxonomy_id` `EXISTS` in `product_taxonomy`) also=0; a category-wide orphaned-map-row scan (guarding against the Session 67 failure mode) also=0. Final live re-verification: 817 rows / 517 distinct products / ~13.03M THB remain unmapped, matching the expected 819→817 / 519→517 delta from the 2 products just routed — no unexplained residual change. Per-row canonical_name precision elsewhere in the category not polished this session (coverage-first, consistent with Sessions 59/67/68). | ✅ Live gap re-verified as 819→817 rows / 519→517 distinct products (~13.11M→~13.03M THB) — 2 products routed (1 to an existing sibling entry, 1 to 1 new mint `SKU-168072`), reuse-before-mint, zero image reads needed. Root-cause finding: Session 68's fix for the bare-English-"wash" blind spot didn't cover the Thai phonetic transliteration "วอช" of the same word — a second, narrower instance of the same class of gap, worth checking for in any other category using an analogous keyword-override exclusion rule. Remaining 517 products independently re-verified as correctly out-of-scope or already-flagged-indeterminate (including the newly-confirmed Aveeno lotion+wash&shampoo bundle), not a fresh backlog |

| Jul 26 | 71 (top-up) | Wrapper cited a live pre-check of 793 unmapped model-grain rows (month 2026-06) — the exact residual Session 70 had just closed to and independently re-confirmed as correctly out-of-scope. Re-ran the live STEP 0 query rather than trusting the number: confirmed 793 rows / 504 distinct products / ~12.76M THB, identical to Session 70's closing state. Given Session 70's own explicit caveat ("a future session should not assume 504 is final without its own independent re-screen") and the pattern of every prior re-verification (Sessions 68, 69, 70) each finding the previous session's "confirmed OOS" verdict incomplete, did not accept the residual at face value: bulk keyword-classified all 504 distinct products against the category's full documented OOS taxonomy (hand wash 288, standalone shampoo 144, feminine wash 6, standalone conditioner 7, standalone scrub 21, lice shampoo 30, bath bomb 5) plus an explicit check for the wash+shampoo/bath+wash/cleansing-gel-bundle class Sessions 68–70 established (0 hits this session — first session in the sequence to find zero new instances of that class). Spot-checked the top-20-GMV items in the two largest buckets (hand wash, standalone shampoo) and read every product in the five small buckets (feminine, conditioner, scrub, lice, bath bomb) in full — all 501 matched a documented exclusion category correctly, no misclassification found. **3 products (of the 504) did not cleanly match any exclusion keyword and were reviewed individually:** (1) two Hug Organic Store products, "Hom-Nin Rice Moisturizing Scrub Shower Gel" 500ml and 1000ml-refill (~2.9K + ~2.1K THB) — sku_name contains "สครับ" (scrub), which naive keyword classification would exclude, but the product is genuinely `เจลอาบน้ำ` (shower gel) with a scrub texture, confirmed via image ("WASH BODY AND RINSE WELL" directions, Hug's own "Shower Gel" branding) — the scrub exclusion rule applies to standalone scrub products, not body-wash-with-scrub-texture, a previously undocumented sub-case of the existing scrub exclusion. No existing Hug taxonomy entry covers this "Hom-Nin" (black rice) sub-line — the brand's existing entries (SKU-165711/165482) are a plain "ข้าว" (rice) line, a different named variant — so reuse-before-mint found no match; minted 2 new entries following the established Hug naming convention (SKU-168472 "Hug เจลอาบน้ำ สครับข้าวหอมนิล สูตรอ่อนโยน ส่วนผสมจากธรรมชาติ 95% 500ml", SKU-168473 "Hug Refill เจลอาบน้ำ สครับ ข้าวหอมนิล สูตรอ่อนโยน จากธรรมชาติ 95% 1000ml"), mapped both products, confidence 0.85 (Shopee Star Seller, not Mall — reseller tier). (2) TOPJapan "Kumano Horse Oil 600ml" (product 25356841658, 3 model_ids, ~8.3K THB total) — the STEP 0 query as given doesn't select `option_name`, so fetched it directly: the 3 models are a buyer-choice selector across 3 *different product types*, not variants of one product — model 235963131977 = "แชมพู" (Shampoo, OOS), 235963131978 = "ครีมนวด" (Conditioner, OOS), 235963131979 = "สบู่อาบน้ำ" (Bath Soap, in scope — matches an existing sibling entry almost exactly, SKU-108896 "Kumano Horse Oil Moisture Body Soap 600ml"). Could not map: `product_taxonomy_map` dedups on `product_id` alone (no model_id in the key), so mapping this product_id to the Body Soap sibling would also silently claim the Shampoo/Conditioner models' GMV under a body-wash entry — the same "can't cleanly split a multi-type buyer-choice listing at product grain" constraint already established in Sessions 60/69/70 (leave NULL rather than force a partial/incorrect claim); left NULL, a new instance of that precedent (same brand as a Session 60 finding, but a different product_id/listing). Claimed the 793-slot block per the prompt's literal STEP 2 SQL (`SKU-168472–169264`) rather than resizing to the known small yield, consistent with this category's established practice of following the prompt's literal claim size (see Sessions 68–70's own large-block/small-yield pattern); used 2 slots (SKU-168472–168473), 791 unused, block marked COMPLETE. Hard gates re-verified post-write without `--skip-coexistence`: G1(LLM-scoped)=0, G2=0, placeholder-leak=0, G5=0, structured-fields-missing=0%. Final live re-verification: 791 rows / 502 distinct products / ~12.75M THB remain unmapped — matches the expected 793→791 / 504→502 delta from the 2 routed products exactly, no unexplained residual. Per-row canonical_name precision elsewhere in the category not polished this session, consistent with the established coverage-first pattern. | ✅ Live gap closed from 793→791 rows / 504→502 distinct products (~12.76M→~12.75M THB) — 2 products routed via 2 new mint entries (SKU-168472–168473), reuse-before-mint attempted first but no safe match found for the Hom-Nin sub-line. First session in the Session 68–71 re-verification chain to find **zero** new instances of the wash+shampoo/bath+wash bundle false-exclusion class — that class appears exhausted for this residual, though per Session 70's own caveat a future session should still independently re-screen rather than assume finality. New minor false-exclusion sub-case documented: "สครับ" (scrub) keyword wrongly excludes genuine shower-gel-with-scrub-texture products (distinct from standalone scrub soap, which is correctly excluded). Remaining 502 products independently re-verified as correctly out-of-scope (as Session 70's 5 categories) or genuinely indeterminate at product grain (1 multi-type buyer-choice listing, Kumano) — not a fresh backlog |
| Jul 26 | 70 (top-up) | Wrapper cited a live pre-check of 817 unmapped model-grain rows (month 2026-06) — again the exact residual Session 69 had just closed to and independently re-confirmed as correctly out-of-scope. Re-ran the live STEP 0 query rather than trusting the number or the prior session's confirmation: got the identical 817 rows / 517 distinct products / ~13.11M THB. Given that Session 68's "confirmed OOS" verdict on 519 turned out to still hide 2 false-exclusions (found by Session 69), and Session 67's "confirmed OOS" verdict on 534 turned out to hide 15 (found by Session 68), treated Session 69's own re-confirmation as unverified rather than authoritative and re-screened from scratch: applied the documented OOS keyword gate (hand wash, feminine, scrub, bath-bomb) plus an in-scope override extended beyond Session 69's `วอช`-inclusive regex to also catch `wash & shampoo`/`wash and shampoo`/`gentle wash` variants — 302 products cleanly OOS by keyword, 215 not obviously so. Read all 215 sku_names in full rather than sampling, per the rule that a keyword gate alone may not decide scope. Found 13 genuine false-exclusions from the same wash+shampoo/bath+wash/cleansing-gel-bundle class Sessions 68–69 established, none caught by prior sessions' narrower single-product-focused checks because these are multi-item bundles (wash/bath component + a bundled lotion or shampoo of a different product type) rather than standalone 2-in-1s: `Eucerin` "เบบี้ วอชแอนด์แชมพู 400มล" (single product, ~93.4K THB, the largest single miss), 2× `Cetaphil Gentle Wash & Shampoo + Daily Lotion` bundle variants (230ml/Pump, ~41K + ~8.5K THB), `Aveeno Daily Moisture Lotion & Wash & Shampoo` set (~20.6K THB), `Sebamed Baby Bubble Bath + Shampoo` in two pack configurations (~29K + ~6.5K THB — "Bubble Bath" as a liquid wash product, distinguished from the solid/effervescent "Bath Bomb" type Session 67 excluded, of which 3 more instances were found and correctly left OOS this session), 2× `Cetaphil Moisturizing Bath&Wash + Shampoo[+ Lotion]` set variants (~17.4K + ~7.8K THB), 2× `Mustela Cleansing Gel` bundles with shampoo or lotion (~42K + ~15.9K THB) plus a `Mustela Cleansing Gel 500ml x3` standalone multipack (~12.6K THB, distinct from the existing `SKU-165168` x2 entry per the "never merge xN pack counts" rule), and `Dove Body Scrub + Body Wash` bundle sold identically by 2 different resellers (~5.6K + ~5.1K THB, bulk-matched to one entry). One further candidate (`tkpharmacy`'s slash-separated "Lotion / Bath & Wash / Shampoo / Gentle..." listing, ~1.6K THB) was a genuine buyer-choice selector across mixed product types (can't determine which item ships) — correctly left NULL, same precedent as Session 69's Aveeno bundle call. Checked all 13 candidate products for a pre-existing (including orphaned) `product_taxonomy_map` row first per the Session 67 orphan-collision precedent — none existed, clean to insert. Queried existing taxonomy for reuse candidates across all 6 brands involved (Eucerin, Cetaphil, Aveeno, Sebamed, Mustela, Dove) — none of the 13 bundle/pack compositions matched an existing entry closely enough to reuse safely (existing entries are standalone products or different bundle/pack compositions), so minted 12 new entries covering all 13 products (the 2 Dove-bundle products share one entry). Claimed a 200-slot block (`SKU-168272–168471`) sized to the actual post-triage count rather than the wrapper's literal 817-slot instruction, per the runbook's "claim after you know the count" guidance — used SKU-168272–168283, 188 slots unused. Hard gates re-verified post-run without `--skip-coexistence`: G1(LLM-scoped)=0, G2=0, placeholder-leak=0, G5=0. Per-row canonical_name precision beyond what bulk text-matching produced (e.g. exact bundle-set naming conventions, sub_line capture) not polished further this session, consistent with the established coverage-first pattern. | ✅ Live gap closed from 817→793 rows / 517→504 distinct products (~13.11M→~12.76M THB) — 13 products routed via 12 new mint entries (SKU-168272–168283), reuse-before-mint attempted first but no safe match found. Root-cause finding: the wash+shampoo/bath+wash false-exclusion class Sessions 68–69 identified is broader than either session's fix covered — it also includes multi-item bundles (wash/bath component + bundled lotion or shampoo), not just standalone 2-in-1 products; each prior session's "remaining N confirmed OOS" verdict undercounted because its own broadened regex was still single-product-shaped. Remaining 504 products independently re-verified as correctly out-of-scope (standalone baby shampoo/conditioner, lice shampoo, hand wash/hand soap, solid bath bombs/bath salts, 1 buyer-choice selector), not a fresh backlog — but given the pattern of each session finding the prior session's residual incomplete, a future session should not assume 504 is final without its own independent re-screen |

| Jul 25-26 | 67 (top-up) | Wrapper cited a live pre-check of 5,235 unmapped products (month 2026-06) — a large jump from Session 60's confirmed 380-row residual. Re-ran the live STEP 0 worklist query rather than trusting the number: confirmed 5,234 rows / 3,005 distinct products / ~93.5M THB non-GWP GMV, genuinely current, not stale. Applied the category's documented scope keyword gate (hand wash ล้างมือ/โฟมล้างมือ/สบู่ล้างมือ, feminine เฟมินีน, floor/dish cleaner, standalone shampoo/conditioner without a body-wash keyword, scrub สครับ, plus a newly-added bath-bomb/bath-salt exclusion บาธบอมบ์/Bath Bomb/Bath Spa Salt — Session 59 precedent, a distinct product type from body wash despite containing "bath") to the full worklist at product grain: 2,016 in-scope, 408 OOS (213 hand-wash, 165 standalone-shampoo, 18 scrub, 7 bath-bomb/salt, 5 feminine). Found and corrected 7 products misassigned to a known garbage global brand bucket (`BRD-SG-08876` "12/+＝", the OCR/promo-banner artifact flagged in Session 62) before matching: 2 genuine Bennett Vitamin C&E 130g×12 products reassigned to `BRD-SG-00038` (matched the existing `SKU-036012` entry), 3 "หมอแว่น"/Rawra-brand herbal soap products reassigned to `BRD-SG-00211` (Rawra, confirmed present in brand_dict), 1 unbranded hotel-soap listing to `BRD-UNBRANDED`, 1 unidentifiable promo-code listing to `BRD-UNDEFINED`. Built a bulk reuse-before-mint pipeline (not per-product image reads): BigQuery's own `magpie_reference.parse_size` UDF for size extraction (comma-thousands-safe), a custom pack-count regex covering the documented xN/แพ็คคู่/N-แถม-M/ยกลัง/buy-N-get-M patterns (with a same-day-caught false-positive fix — "10X"-style concentration markers like "Niacinamide 10X 425ml" were being misread as a 425-unit pack multiplier via naive `[x×X]\d+` matching; fixed with a negative lookbehind excluding digit-adjacent X, plus a 200-unit sanity cap on all pack patterns), and text-normalization-based grouping (brand_id + size + pack_count + a token-signature of the sku_name with brand/size/pack/promo-bracket noise stripped) matched against existing `shopee_th_body_wash` taxonomy entries. Matching used exact-key lookup with unambiguous single-candidate auto-accept (brand+size+pack alone is a strong signal when only one existing entry shares it — needed because many existing entries carry English product lines while sku_name text is Thai, defeating pure token-overlap scoring) and Jaccard token-overlap arbitration (≥0.25) when multiple candidates shared a key. Round 1 (2,016 in-scope products): 329 matched to existing taxonomy, 1,589 new entries minted (SKU-165037–166625) covering 1,687 products, 214 of the new entries flagged `is_multi_size=TRUE` (no extractable size from any signal, matching Session 59's established convention). Live re-verification after round 1 surfaced a second population: the wrapper's literal STEP 0 query is model-grain and orders by raw (non-GWP-zeroed) `gmv_monthly` for row rank while only zeroing GWP rows' contribution to the *cumulative sum* — so high-raw-GMV GWP/freebie rows can rank early enough to still clear the ≤95%-cumulative-pct filter despite contributing nothing to it, and more importantly this round-1 query had aggregated to *product* grain over only the then-still-null subset, which is a different (undercounting) denominator than "top-95% of the full category's GMV" — 663 legitimate products (1,057 distinct remaining minus 394 already correctly OOS) had never entered round 1's candidate pool at all. Ran a corrected round 2 directly off the live model-grain worklist's residual: applied the same scope gate (523 in-scope of 663, 140 OOS) and the same matching pipeline (308 matched to existing incl. round-1 mints, 213 new entries minted SKU-166626–166838 for 215 products; one round-2 SQL draft initially omitted the `month='2026-06'` filter, inflating GMV 40x — caught via a sanity total-GMV check before any write). **G1 dual-mapping discovery:** post-round-2 live re-verification still showed 18 products unresolved despite being freshly mapped; traced to a genuine pre-existing data bug, not a session error — these 18 already had a `product_taxonomy_map` row (dated Jul 20, Session 59) pointing to a `taxonomy_id` (11 distinct SKU-057xxx/058xxx IDs, e.g. SKU-058239, SKU-058126, SKU-058263) that no longer exists in `product_taxonomy` — an orphaned reference from some earlier untracked deletion that silently resolved to `canonical_name IS NULL` in every STEP-0-style query, making these 18 products read as "unmapped" all along even though a stale map row existed, and this session's fresh inserts turned that silent breakage into an active G1 dual-map (verified via `EXISTS` against `product_taxonomy` that all 11 old taxonomy_ids were genuinely gone, not a query bug). A category-wide orphan scan found exactly these 18 rows and no others. Deleted only the 11-taxonomy_id/18-row orphaned set (broken dangling references providing zero value, not legitimate existing work — narrowly targeted by `taxonomy_id NOT EXISTS IN product_taxonomy`, not a blanket delete) and kept the fresh valid rows. All hard gates re-verified after the fix, without `--skip-coexistence` (category already shipped): G1=0, G2=0, placeholder-leak=0, G5=0, structured-fields-missing=0%. Final live re-verification: 534 distinct products remain unmapped (835 model-grain rows, ~14.1M THB non-GWP GMV), and every one of the 534 is accounted for by this session's own scope classification (hand-wash/standalone-shampoo/scrub/bath-bomb/feminine) — zero unexplained residual. Per-row canonical_name precision (exact product_line distillation vs. raw sku_name text, sub_line/variant capture) intentionally not polished this session, consistent with the established coverage-first / `targeted_qa_fix.sh`-follow-up pattern from Session 59 — expect the new SKU-165xxx/166xxx block to need the same distillation-pass treatment Sessions 61–66 gave the SKU-107xxx block. | ✅ Live worklist gap closed from 3,005→534 distinct products (~93.5M→~14.1M THB, 84.9% GMV reduction) across 2 processing rounds; 1,802 new taxonomy entries minted (SKU-165037–166838) + 637 products routed to existing entries, 2,214 total map rows written. Found and fixed a genuine pre-existing G1 defect (18 orphaned map rows dangling to already-deleted taxonomy entries from a prior untracked cleanup) that this session's coverage work exposed. All hard gates (G1, G2, G5, placeholder-leak, structured-fields) clean post-fix. Remaining 534 products independently re-verified as correctly out-of-scope, not a fresh backlog |

## Scripts

| Script | Purpose |
|--------|---------|
| `pipeline/05_product_taxonomy/llm_th_body_wash/build_taxonomy_v3.py` | Pass 1 text-based extraction |
| `pipeline/05_product_taxonomy/llm_th_body_wash/build_p2_taxonomy_v3.py` | Pass 2 reseller routing |
| `/tmp/bw_p1_smart_text.py` | v3 Pass 1 (canonical version) |
| `/tmp/bw_p2_reseller.py` | v3 Pass 2 (canonical version) |
| `/tmp/bw_cleanup_and_refresh.py` | HUMAN cleanup + universe refresh |

---

## Map Row Counts (Jun 24 2026)

| Source | Count | Notes |
|--------|-------|-------|
| LLM/OFFICIAL | 856 | Pass 1, 56 brands × ~15 entries avg |
| LLM/RESELLER | 3,596 | Pass 2, 80+ brands |
| HUMAN | 3,167 | Long-tail out-of-scope products (retained) |
| Total universe rows | 61,512 | sincere; 61,531 farsight |
