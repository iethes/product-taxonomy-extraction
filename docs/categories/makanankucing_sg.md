# makanankucing_sg — Category Context

> Custom (non-NIQ) source: `sincere-hearth-273704.makanankucing.9_makanankucing_sg_daily`
> (**TikTok Shop Singapore**, daily grain summed to monthly — not Shopee, despite the naming
> convention shared with the `makanankucing_my`/`makanananjing_my` precedent categories, which
> *are* Shopee-sourced). Not a `master_clean_niq` table — different schema, no
> `model_id`/`month`/`merchant_badge`-driven official-store data in the form other categories rely
> on. Taxonomy state is keyed under `master_table = 'makanankucing_sg'`, not the source table name.
> Raw `ecommerce_platform` column value is `'Tiktok'`; normalized to the documented canonical
> `'TikTok Shop'` in `product_taxonomy_map.platform` (no live collision risk confirmed —
> `universe_taxonomy_overlay` had zero pre-existing rows for any `makanan*` table before this
> session, and its only other platform value in use is `'Shopee'`).

---

## Status

| Field | Value |
|-------|-------|
| LLM Pass | ✅ Complete (Full Rebuild, first run) — single pass, no official-store tier exists for TikTok Shop (`merchant_badge` is uniformly `'Tiktok Shop'` for all 871 SG products, confirmed live — no Mall-equivalent signal) |
| GMV Coverage | 239/246 (97.15%) of the top-95%-cumulative-GMV (GWP-zeroed) in-scope worklist, after the 2026-07-28 top-up session; 239/871 of all SG products in the source table |
| Last run | 2026-07-28 (top-up session) |
| Current MAX taxonomy_id (this category) | SKU-190845 |
| SKU block this category has claimed | SKU-188352–SKU-190351 (2,000 slots, `custom_full_rebuild`) + SKU-190843–SKU-191042 (200 slots, `custom_topup`) |

---

## SKU Blocks Assigned

| Block | Usage |
|-------|-------|
| SKU-188352–SKU-190351 | Claimed block (2,000 slots, `custom_full_rebuild`) |
| SKU-188352–SKU-188540 | Actually used (189 taxonomy entries) — 1,811 slots left unused in the block |
| SKU-190843–SKU-191042 | Claimed block (200 slots, `custom_topup`, 2026-07-28) |
| SKU-190843–SKU-190845 | Actually used (3 new taxonomy entries) — 197 slots left unused in the block |

---

## Brand Scope (GMV threshold 95%, in-scope worklist)

62 distinct brands mapped (58 reused from the existing global `brand_dict`, 4 newly minted as
`BRD-SG-*`). Top 15 by GMV:

1. **Aatas Cat** — `BRD-SG-00561` — 45.7K SGD
2. **Royal Canin** — `BRD-GLOBAL-00001` — 34.4K SGD
3. **Ciao** — `BRD-SG-00024` — 25.2K SGD
4. **Daily Delight** — `BRD-SG-00664` — 22.2K SGD
5. **Aristo-Cats** — `BRD-SG-00563` — 22.1K SGD
6. **Sumo Cat** — `BRD-SG-01278` — 10.5K SGD
7. **Uncle Tails** — `BRD-MY-01124` — 6.7K SGD
8. **Happi Skippi** — `BRD-SG-01937` — 6.1K SGD
9. **Fancy Feast** — `BRD-SG-00713` — 5.6K SGD
10. **Kit Cat** — `BRD-SG-00588` — 4.7K SGD
11. **Loveabowl** — `BRD-SG-01072` — 4.3K SGD
12. **NurturePro** — `BRD-MY-01029` — 4.1K SGD
13. **Catsmart** — `BRD-SG-14506` (new) — 3.1K SGD
14. **SAVA** — `BRD-SG-02059` — 2.6K SGD
15. **FAENBEI** — `BRD-SG-05598` — 2.2K SGD

Remaining 47 brands (each < 2.1K SGD in-scope GMV): Top Ration, Friskies, Absolute Bites, Ziwi Peak,
Vitachat (new), Bitacat (new), Sparkles (new), Purina ONE, IQ Cat, Taste Of The Wild, Wellness CORE,
Boneve, SmartHeart, Farmina, Food For The Good, Holuah!, Whiskas, D'Zahara, Reflex, BelliFull, Fussie
Cat, Sheba, Felix, Angel, Rich Choice, Zealandia, Absolute Holistic, Urbanwolf, Schesir, babyKET,
Snappy Tom, Instinct, Nutripe, Solid Gold, Furvit, Petag, Wanpy, Unicharm, Singapaw, Meow Meow, LDECO,
Vitakraft, Aixia, Platinum Choice, Amelisa, Prof. Bengal, Ishtar.

**New brands minted this session** (verified against the *global* `brand_dict` first — all four had
zero matches under any spelling/casing before minting; MAX+1 computed inside a single
`INSERT...SELECT` to avoid the read-then-write collision documented in `makanankucing_my.md`, and a
parallel session did in fact claim the immediately-preceding ID range in the few minutes between
research and insert, confirmed via `COUNT(*) = COUNT(DISTINCT brand_id)` post-insert):

| Brand | brand_id | Notes |
|-------|----------|-------|
| Catsmart | `BRD-SG-14506` | SG pet retailer's own private-label cat food line (also appears as `merchant_name` `catsmartsg`, but the sku_name itself states "Catsmart"/"CatSmart" as the product brand, not just the seller — a real product brand per §11, not a merchant-name leak) |
| Bitacat | `BRD-SG-14507` | Multivitamin/supplement cat candy brand |
| Sparkles | `BRD-SG-14508` | Halal wet cat food brand (SilverSky-distributed); deliberately NOT merged with the existing unrelated `BRD-GLOBAL-00184` "Sparkle" (singular, no confirmed pet-food association) |
| Vitachat | `BRD-SG-14509` | Soft jelly wet cat food brand |

**Brand consolidation applied during extraction** (spelling/casing variants merged to one existing
`brand_dict` entry, not re-minted): `Aristo Cat`/`Aristo-cats`/`ARISTO-CATS`→`ARISTO-CATS`
(`BRD-SG-00563` — the SG-scoped entry, not the separately-minted `BRD-MY-01076 Aristo Cats` from the
`makanankucing_my` session; same real-world brand has two IDs across markets, a known dedup debt, not
resolved this session); `Dzahara`→`D'Zahara` (`BRD-MY-01089`); `CIAO Churu`→`Ciao` (`BRD-SG-00024`);
`Uncle Tail`/`UNCLE TAIL`→`Uncle Tails` (`BRD-MY-01124`); `Furvite`→`Furvit` (`BRD-MY-01177`);
`NurturePRO`→`NurturePro` (`BRD-MY-01029`, the no-space spelling match — a separate `Nurture Pro`
(`BRD-SG-00801`) also exists in `brand_dict` and was not used); `Farmina N&D`/`N&D`→`Farmina`
(`BRD-SG-00565`, no separate `N&D` entry exists); `PetAg`→`Petag` (`BRD-GLOBAL-00554`); `Prof
Bengal`→`Prof. Bengal` (`BRD-SG-03451`); `Sava`/`Sava Essential(s)`→`SAVA` (`BRD-SG-02059`, distinct
from the unrelated `SAVAS` `BRD-SG-11980`); `Smartheart`→`SmartHeart` (`BRD-GLOBAL-00039` generic —
no products in this worklist referenced the separate `SmartHeart Gold` `BRD-SG-00018`);
`Instinct`→`BRD-GLOBAL-00938` (not the ambiguous `Instinct Pet Food` `BRD-SG-01397`);
`MeowMeow`→`Meow Meow` (`BRD-SG-00113` — deliberately not the unrelated `ME-O`/`Me O`/`Meow`
look-alikes); `Absolute Bites`→`BRD-SG-00740`; `Absolute Holistic`→`BRD-GLOBAL-00902` (two genuinely
distinct Absolute-prefixed brands, not merged with each other).

**Known unresolved duplicate (flagged, not fixed):** `Rich Choice` has two active, identically-named
`brand_dict` entries — `BRD-MY-01046` and `BRD-MY-01113`. This session used `BRD-MY-01046`
(lower/earlier ID) for the one in-scope Rich Choice product; a future session should investigate and
merge.

No official-store allowlist — TikTok Shop has no Mall-equivalent tier (`merchant_badge` is uniformly
`'Tiktok Shop'` for every listing; per `docs/brand-extraction.md` and `ARCHITECTURE.md`, TikTok Pass 1
is always skipped, go directly to Pass-2-style bulk routing). This category went straight to
bulk-first text-matching per `docs/headless-runbook.md`'s Full Rebuild coverage-first mandate.

---

## Scope — What's In vs Out

**In scope:** cat dry food, cat wet food (can/pouch/tray), cat treats (sticks/purees/freeze-dried),
cat supplements (multivitamin candy). The source table's `category_1/2/3` is uniformly
`Pet`/`Pet Food`/`Cat Food` for all 871 SG products — no dog food, litter, or other mislabeled
non-food items were found in this table (unlike the `makanankucing_my` precedent, which required an
explicit dog-food/litter/flea-treatment exclusion list).

**Out of scope (left unmapped, 7 products, ~9.3K SGD GMV in the in-scope worklist, as of the 2026-07-28
top-up session):** all 7 are genuinely brand-unidentifiable — confirmed via image, not just text, this
session (the original 12-product list below included 5 that a fresh image check resolved; see QA History
2026-07-28 top-up):
- Generic freeze-dried treat listings with no discernible packaging-printed brand, only reseller
  watermarks/composite marketing collages (`PetJoy.PJ`/`MomJoy MJ`/`PetJoy Trusted Local Pet Store`
  resellers — "Big bucket!!! Pet Food Freeze Dried Treats...", "Top Value!!! Freeze Dried Chicken
  500g...", "【SG ready stock】Freeze Dried Pet Treats..." ×2, "Pet Joy Shop Freeze Dried Pet Treats...")
  — image-verified 2026-07-28: three of these five share one identical composite marketing image showing
  small Korean-text foil pouches that don't match the 500g bulk-bin product actually being sold (classic
  cover/banner-image mismatch per §6's caveat) — same "genuinely unbranded commodity treat" pattern
  documented in `makanankucing_my.md`.
- `[New Buyer Only] Yappy Pets Welcome Box for Cat and Dog` — image-verified 2026-07-28: a genuine
  cross-brand bundle (Top Ration dry food + happi freeze-dried treats + nurturePRO pet wipes), not a
  single-brand product; `Yappy Pets` is the `merchant_name`, not a packaging brand (§11).
- `SG Fresh cat grass without soil organic cat grass...` — image-verified 2026-07-28: the image's "ALL
  LUCKY" banner matches this listing's own `merchant_name` (`All-Lucky`) exactly — a merchant-name leak,
  not a printed product brand (§11). Correctly excluded.

**Resolved this session (moved out of the unmapped list, 2026-07-28 top-up):** 5 of the original 12 were
image-verified to have a real packaging-printed brand distinct from the reseller/merchant name — see QA
History for the full finding. `NUTRILICK CAT CANDY SUPPLEMENT PROBIOTIC` (→ Furvit Nutri Lick) and the
two generic-looking cat-grass/cat-strip listings that turned out to be FAENBEI and Ishtar respectively are
no longer in the out-of-scope list. `【Combo Sets 】Cat food wet food mixed...` also resolved — its image
is 100% Uncle Tails-branded (box + cans + sticks), not a multi-brand assortment as originally assumed from
text alone.

**Edge cases:**
- Cross-border brand entities: several brands in this SG TikTok worklist (`D'Zahara`, `Uncle Tails`,
  `NurturePro`, `Furvit`, `Urbanwolf`, `Rich Choice`) only had existing `brand_dict` entries under
  `BRD-MY-*` scope from the `makanankucing_my`/`makanananjing_my` sessions. Reused directly rather than
  minting SG-scoped duplicates — same real-world brand sold cross-border on TikTok Shop SG.
- `Aristo-Cats` has two brand entities (`BRD-SG-00563` and `BRD-MY-01076`) for the same real brand —
  see "Known unresolved duplicate" note above (this one wasn't flagged in either precedent doc).

---

## Taxonomy Design Notes

**Extraction method:** bulk regex/text-matching on `sku_name` against a ~62-brand curated keyword
dictionary (longest-pattern-first, Latin tokens word-bounded to avoid the substring-collision class of
bug documented in `makanankucing_my.md`, e.g. a bare `"tom"` alias colliding with unrelated text) — no
merchant/official-store data to seed a Pass 1 the way NIQ tables allow. Size and pack-count extracted
via regex over `sku_name` (kg/g/gm/ml/L units; `x{N}`, `*{N}` and `{N} cans/pouches/sticks` multiplier
patterns; `{N}unit/{M}unit` and `{N}kg/{M}kg` alternation → `is_multi_size`). 5 of the highest-GMV
size-ambiguous listings (all >1K SGD, all Ciao Churu / FAENBEI treat-stick jars) were verified directly
against the product image (`curl` + `Read`, converting the source's native WebP to PNG via Pillow since
this environment has no `dwebp`/ImageMagick) — confirmed 14g×50 for four Ciao Churu jar listings and
15g×60 for the FAENBEI stick box.

**Regex gotchas found and fixed this session** (noted here in case a future session hand-rolls similar
regex against this or another TikTok-sourced table):
- **Size-vs-pack ambiguity around a bare `x`**: `"24 cans x 80g"` initially mis-parsed as `pack_count=80`
  or `pack_count=8` — the naive `[x×]\s*(\d+)` pattern greedily matched the *size* half of a
  `"{count} cans x {size}"` phrase, and Python's `\d+` backtracking against a negative-lookahead unit
  check silently truncated `"80"` down to `"8"` to satisfy the lookahead at a shorter match. Fixed by
  switching to a manual `re.finditer` scan that takes each *full* digit run following `x`/`×`/`*` and
  only rejects it if that exact (non-truncated) run is immediately followed by a unit word — a single
  regex with a lookahead was not reliable here regardless of anchoring, because `\b` after `\d+` doesn't
  fire between adjacent digits.
- **`x` with a space before the count** (`"x 50"`, not `"x50"`) was missed entirely by an earlier
  version of the pack regex that required the digits to immediately follow `x` with no gap — silently
  defaulted several genuine 50-stick jars to `pack_count=1` until fixed.
- **`*` as a multiplier separator** (`"70g*24"`, `"15g*60Pcs"`) is used interchangeably with `x` in this
  source's listings — not handled by the original `x`-only pattern.
- **No-space unit-then-multiplier runs** (`"85gx12pouch"`): the naive `\b` word-boundary check after a
  unit fails here since `g` and `x` are both word characters with no boundary between them — same class
  of gotcha `makanankucing_my.md` documented for `"80gx24pouch"`.

**Variant/flavor collapse risk (caught before finalizing, not after):** the first grouping pass keyed
taxonomy entries by `(brand_id, size, pack_count)` alone, which silently merged genuinely distinct named
product lines sharing the same size/pack into one entry — e.g. all 7 different Aatas Cat 80g×24 flavor
lines (Tantalizing Tuna, Creamy Chicken, Finest Soul Superfoods, Complete Care, Finest Daily Defence...)
collapsed into one `"Aatas Cat 80g x24"` stub, and similarly for Aristo-Cats' 6 "Premium Plus {X}
Series" lines and Royal Canin's many named care lines (Hair & Skin, Indoor27, Urinary S/O vs Urinary
Care, Fit 32, Hairball Care, Maincoone, British Shorthair). This is exactly the D3 variant-collapse
defect class documented elsewhere in this pipeline (e.g. th_softdrink's Pepsi Zero Sugar silently
falling back to Pepsi Cola) — caught by manually reading every multi-product group's underlying
`sku_name`s before generating DML, not by an automated check. Re-grouped with an explicit per-product
line override for all 35 raw groups that had more than one underlying product, splitting them into the
correct number of distinct taxonomy entries by their actual on-label line name. Genuinely-identical
listings (same brand/size/pack, no distinguishing line text — e.g. 3 reseller listings of "Sumo Cat
Premium Can Food 80g x24" with only cosmetic title differences) were correctly kept merged.

**product_line vs variant split:** where `sku_name` yields a real on-label formula/line name (e.g. Royal
Canin's `Hair & Skin Care`, `Indoor27`, `Fit 32`; Aristo-Cats' `Premium Plus {X} Series`; Kit Cat's
`Deboned Food Topper`, `Petite Pouch`), that text is written to `product_line`. Where the only
differentiating text is a flavor/ingredient descriptor (e.g. `Chicken & Tuna Broth`, `Tantalizing Tuna`,
`Soft Jelly Cup Tuna Flake`) with no named formula, that text is written to `variant` instead and
`product_line` left NULL, per the food-keyword heuristic used in the `makanankucing_my` precedent.
Structured-fields-NULL rate (both fields NULL, brand+size+pack only): 26% of non-multi-size entries —
under the 50% QA-gate fail threshold, and expected for single-line brands like Sumo Cat and generic
"Royal Canin 2kg dry food" reseller listings with no line stated at all.

**Multi-size / ambiguous-bundle handling:** buyer-choice size ranges (`"2kg/4kg/10kg"`,
`"1.5kg | 3.5kg | 7kg"`) and pack-count ranges (`"Bundle of 12/24"`) get `is_multi_size=TRUE, size=NULL`
with a real descriptive name — never the banned "Multiple Sizes"/"Multiple Variants" text (checked
against every entry's `canonical_name` via the placeholder-leak gate regex before writing: 0 hits).
Genuinely ambiguous multi-line bundles (e.g. `"Royal Canin Cat Dry Food (Hair & Skin, Digestive,
Hairball, Urinary)"`, `"Rich Choice HOLISTIC CHEEK BOOSTER/OVEN-BAKED LARGE BREED/GASTROINTESTINAL
CARE"` — buyer picks one of several named lines in one SKU) got a distinct `"{Brand} Assorted Care
Lines"`-style catch-all rather than being force-fit into any one of the named lines.

**Known coverage gap (flagged for a future top-up/`targeted_qa_fix.sh` pass, not fixed this session):**
12 products (~10.1K SGD in-scope GMV) — brand not identifiable from `sku_name` text alone, all
low-signal generic freeze-dried-treat/merch/combo listings. Left `taxonomy_id IS NULL`. See Scope
section above for the full breakdown.

---

## QA History

| Date | Pass | Finding | Resolution |
|------|------|---------|------------|
| 2026-07-28 | Pre-flight | STEP 1 re-verification: `SELECT source, COUNT(*) ... WHERE master_table='makanankucing_sg'` returned 0 rows — genuine first run, matching the wrapper's pre-check | Proceeded per Full Rebuild scenario |
| 2026-07-28 | Pre-flight | Source table's real `ecommerce_platform`/`country` columns (needed for the `product_taxonomy_map` composite key) are absent from the prompt's STEP 0 query — live schema check found the source is 100% TikTok Shop (871 SG products + 1 stray MY product from an earlier month, excluded via explicit `country='SG'` filter), not Shopee like the `makanankucing_my`/`makanananjing_my` naming-convention precedent | Normalized raw `'Tiktok'` → documented canonical `'TikTok Shop'`; confirmed zero collision risk against `universe_taxonomy_overlay` (which has no rows for any `makanan*` table and only uses `'Shopee'` elsewhere) before deciding, per an advisor consult |
| 2026-07-28 | Full Rebuild | Regex bugs in initial pack-count extraction (space-before-`x`, `*`-as-multiplier, size-vs-pack ambiguity around `"N cans x Sg"` phrasing, backtracking truncation of a lookahead-guarded `\d+`) — see Taxonomy Design Notes | Fixed all four before generating any DML; re-ran extraction from scratch each time and re-verified against known test strings |
| 2026-07-28 | Full Rebuild | Variant/flavor collapse: naive `(brand_id, size, pack_count)` grouping merged 35 groups of genuinely distinct named product lines into single stub entries (see Taxonomy Design Notes) — caught by manually reading every multi-product group's underlying `sku_name`s before writing, not by an automated check | Re-grouped with explicit per-product line overrides; re-verified 0 placeholder-leak hits and spot-checked the top 40 entries by GMV after the fix |
| 2026-07-28 | Full Rebuild | 5 highest-GMV size-ambiguous listings (Ciao Churu Festive Pack ×2, Ciao Churu 50-stick jars ×2, FAENBEI 60pcs box) image-verified via `curl`+Pillow-converted-PNG+`Read` | Confirmed 14g×50 (Ciao Churu) and 15g×60 (FAENBEI); applied as overrides |
| 2026-07-28 | Full Rebuild | Bulk global `brand_dict` dedup check (single UNNEST-joined query against all ~62 candidate brand names, normalized) before minting anything — found 58 of 62 candidates already existed (including several only under `BRD-MY-*` scope from the `makanankucing_my`/`makanananjing_my` sessions); only 4 genuinely new (Catsmart, Bitacat, Sparkles, Vitachat) | Reused 58 existing `brand_id`s; minted the 4 new ones in a single `INSERT...SELECT` computing `MAX+1` inside the statement (not read-then-write) — a parallel session claimed the immediately-preceding ID range in the few minutes between planning and insert, confirmed via `COUNT(*) = COUNT(DISTINCT brand_id)` post-insert, no collision |
| 2026-07-28 | Full Rebuild | Claimed SKU block `SKU-188352`–`SKU-190351` (2,000 slots) — re-verified live `MAX(block_end)` immediately before claiming (moved from 186351 to 188351 between the first check and the claim, confirming a parallel session was active) | Used `SKU-188352`–`SKU-188540` (189 of 2,000 slots), 1,811 unused |
| 2026-07-28 | Full Rebuild | Self-check QA gates (G1 dual-mapped [LLM-scoped], G2 HUMAN+LLM coexistence, G5 provenance, placeholder-leak, structured-fields-NULL%) run per `docs/headless-runbook.md`'s QA-gate-as-code, scoped to `master_table='makanankucing_sg'` | All passed: 0 dual-mapped, 0 HUMAN+LLM coexistence (no HUMAN rows exist for this category), 0 provenance gaps, 0 placeholder-leak canonical names, 26% NULL product_line among non-multi-size entries (well under the 50% fail threshold) |
| 2026-07-28 | Full Rebuild | GMV coverage: 234/246 (95.88% of in-scope GMV) of the top-95%-cumulative-GMV worklist mapped; 234/871 of all SG products in the source table | 12 remaining in-scope products (~10.1K SGD) are genuinely brand-unidentifiable — flagged for a future top-up pass |
| 2026-07-28 | Top-up | Wrapper's live pre-check reported 13 still-NULL in-scope products; re-ran STEP 0's worklist query live per this session's instructions rather than trusting that number | Live worklist = 12 products (~10.1K SGD), not 13 — matched exactly the 12 already documented in Scope/Targeted QA Fix Brief, confirming the pre-check number was stale, not the doc |
| 2026-07-28 | Top-up | The prior full-rebuild session's "genuinely brand-unidentifiable" conclusion for all 12 was based on `sku_name` text reasoning only — no product image had actually been checked for any of them. Downloaded + Pillow-converted all 12 product images (`curl` + WebP→PNG, same pattern as the original session) and read each one | 5 of 12 resolved via image: a real packaging-printed brand was visible and confirmed distinct from the reseller watermark/merchant name (cross-checked against live `merchant_name` per product to rule out a §11 merchant-name leak before accepting each read) |
| 2026-07-28 | Top-up | Resolved: `NUTRILICK CAT CANDY SUPPLEMENT PROBIOTIC` (1729615000726374387) image = Furvit's "Cat Candy's Nutri Lick" boxes (Beef/Salmon/Chicken), merchant is unrelated "Komi Pets" | Mapped to existing `SKU-188486` (Furvit Candy Nutri Lick 5g x40) — brand+line match; exact pack format not independently reconfirmed for this specific listing, accepted per this session's coverage-first priority |
| 2026-07-28 | Top-up | Resolved: `SG 60Pcs Cat Treats Cat Wet Food Treats Cat Strip...` (1730284236200511461) image clearly shows "FAENBEI" branded stick pouches, "60 PIECES PER BOX / every single 15g pack" — exact match to the category's existing FAENBEI 15g×60 precedent; merchant is unrelated "All-Lucky" | Mapped to existing `SKU-188373` (FAENBEI Chicken Egg Yolk/Salmon Antarctic Krill/Tuna Mussels 15g x60) |
| 2026-07-28 | Top-up | Resolved: `SG Ready Stock Cat Strips Premium... 16g*30 Per Stick...` (1733369514871588475) image shows "Ishtar"-branded 4-flavor stick pouches (Tuna Roe, Cod Fish, Beef, Salmon Fish); merchant is unrelated "PetJoy.PJ" — an existing single-stick Ishtar entry (`SKU-188539`, pack_count=1) didn't match this 30-count multi-flavor box | Minted new entry `SKU-190843` "Ishtar Cat Snack Strips Tuna Cod Fish Beef Salmon 16g x30" (`is_multi_variant=TRUE`, brand reused: `BRD-SG-05334`) |
| 2026-07-28 | Top-up | Resolved: `【Combo Sets 】Cat food wet food mixed...` (1730978220752536698) image is 100% "UNCLE TAILS PET'S MEAT RIGHTS"-branded across every item (30-pouch box + 9 cans + 6 snack sticks) — not a multi-brand buyer-choice assortment as the original text-only read assumed; merchant is unrelated "Moon Shadow Shop". Neither existing Uncle Tails entry (single-format snack sticks) matched this multi-item combo box | Minted new entry `SKU-190844` "Uncle Tails Complete Feeding Combo Pouch Soup Can Snack Stick Set" (`is_multi_size=TRUE`, brand reused: `BRD-MY-01124`) |
| 2026-07-28 | Top-up | Resolved: `Cat Grass Freeze Dried Granules Snack Cat Food...` (1733321874134959739) image shows a `PetJoy.PJ` reseller watermark overlay AND a separate genuine container label (with ingredients/manufacturer/batch number/barcode) reading "PET&SNACKS" — distinct text from both the watermark and the `merchant_name`, so not a §11 leak. Checked global `brand_dict` first: no existing "Pet & Snacks"/"PET&SNACKS" entry under any spelling | Minted new brand `BRD-SG-14511` "Pet & Snacks" (computed `MAX+1` inside the `INSERT` statement per the collision-avoidance pattern from the original session; verified no `brand_id` duplicate post-insert) + new taxonomy entry `SKU-190845` "Pet & Snacks Freeze-Dried Cat Grass Nuggets 500g" |
| 2026-07-28 | Top-up | Remaining 7 (~9.3K SGD) re-confirmed unresolvable after image check: 5 freeze-dried-treat listings show only reseller watermarks (PetJoy/MomJoy) or a composite marketing image whose pictured products (small Korean-text pouches) don't match the actual 500g-bulk item sold (cover-image mismatch, §6); Yappy Pets box confirmed genuine cross-brand bundle (Top Ration + happi + nurturePRO); the remaining cat-grass listing's "ALL LUCKY" image banner matches its own `merchant_name` exactly (`All-Lucky`) — a merchant-name leak, not a product brand | Left `taxonomy_id IS NULL`; Scope section and Targeted QA Fix Brief updated to reflect the narrowed list |
| 2026-07-28 | Top-up | Self-check QA gates (G1 dual-mapped [LLM-scoped], G2 HUMAN+LLM coexistence — run *without* `--skip-coexistence` per this session's instructions, G4 cross-category, G5 provenance, placeholder-leak) run scoped to `master_table='makanankucing_sg'` | All passed: 0 dual-mapped, 0 HUMAN+LLM coexistence, 0 provenance gaps, 0 placeholder-leak, all 3 new `taxonomy_id`s confirmed inside the claimed `SKU-190843`–`SKU-191042` block |
| 2026-07-28 | Top-up | Claimed SKU block `SKU-190843`–`SKU-191042` (200 slots, `custom_topup`) via the atomic DECLARE/BEGIN TRANSACTION pattern | Used `SKU-190843`–`SKU-190845` (3 of 200 slots), 197 unused |

| 2026-07-28 07:26 UTC | Automated review session (auto-discovery) | STEP 1 live re-check matched the brief (239 LLM / 0 HUMAN, no discrepancy). The Brief's only actionable item was the 'Below-661-GMV long tail products not yet reviewed for line-name noise' precision-cleanup pass (3 named examples: SKU-188486, SKU-188480, SKU-188532); the other two Brief items (7-product coverage gap, Rich Choice/Aristo-Cats brand_dict duplicates) were explicitly marked not-fixable by this session and were left untouched. A systematic sweep (duplicate-consecutive-words, stray single-letter tokens, non-ASCII/invisible characters) across every taxonomy entry mapped to this category, cross-checked against each product's raw sku_name to rule out false positives (confirmed 'Meow Meow'/'Miaw Miaw'/'w Real Meat' are genuine source text, not artifacts), found exactly 6 entries with real canonical_name/product_line corruption -- matching STEP 1B's pre-fix gate count of 6 for 'canonical_name fields' exactly, which corroborated the scope. Defects found: SKU-188486 'Furvit Candy Nutri Lick 5g s 5g x40' (stray '5g s' fragment from 'Furvite...5gx40s'); SKU-188480 'Sheba Melty Moochie Puree Puree Treats' (duplicated 'Puree'); SKU-188532 'Amelisa Rawly Stick Pet Puree & Dog 16g 16g Premium Treats 16g x22' (duplicated '16g 16g' size token); SKU-188419 'Kit Cat Deboned Canned Can Food topper can 80g x24' (redundant Canned/Can/can tokens, inconsistent with the sibling 70g entry's clean 'Deboned Food Topper' naming); SKU-188512 'Urbanwolf...NOW️️ GRAIN FREE...' (two orphaned U+FE0F variation-selector characters with no base emoji, carried verbatim from the raw sku_name); SKU-188538 'Prof. Bengal...Hairball Reductio igestive Support...' (extraction dropped 'n & D' from the raw title's 'Reduction & Digestive Support', producing corrupted text). | Updated canonical_name and product_line (text-only, no other columns) on all 6 entries via bq DML UPDATE: SKU-188419 -> 'Kit Cat Deboned Food Topper 80g x24' / 'Deboned Food Topper'; SKU-188480 -> 'Sheba Melty Moochie Puree Treats' / 'Melty Moochie Puree Treats'; SKU-188486 -> 'Furvit Candy Nutri Lick 5g x40' / 'Candy Nutri Lick'; SKU-188512 -> 'Urbanwolf ADVENTURES SUPERBOOST SERIES NOW GRAIN FREE FORMULATED & MADE IN CANADA 2kg' / 'ADVENTURES SUPERBOOST SERIES NOW GRAIN FREE FORMULATED & MADE IN CANADA' (stripped the U+FE0F chars); SKU-188532 -> 'Amelisa Rawly Stick Pet Puree & Dog Premium Treats 16g x22' / 'Rawly Stick Pet Puree & Dog Premium Treats'; SKU-188538 -> canonical_name/product_line with 'Reductio igestive Support' corrected to 'Reduction & Digestive Support'. Pre-checked each replacement canonical_name against the full product_taxonomy table to confirm no collision with an existing entry before writing. Claimed a 200-slot SKU block (SKU-192942-SKU-193141, scenario=custom_targeted_qa_fix) per STEP 2 as instructed, but this was an in-place fix with zero new rows -- the block is unused and stays ACTIVE/reusable. Post-fix QA gates all passed scoped to master_table='makanankucing_sg': G1 dual-mapped (LLM-scoped)=0, G2 HUMAN+LLM coexistence=0, placeholder-leak=0, G5 provenance=0. Did not touch: the 7-product coverage gap (Brief states these were already image-checked and are genuinely unresolvable), the Rich Choice brand_dict duplicate (BRD-MY-01046/BRD-MY-01113), or the Aristo-Cats cross-market brand duplicate (BRD-SG-00563/BRD-MY-01076) -- all three are explicitly out of scope for a taxonomy-extraction session per the Brief. Did not run universe refresh (per STEP 5) or edit this category file/git (per STEP 6). |
| 2026-07-28 08:04 UTC | Automated review session (auto-discovery) | STEP 1B's pre-fix gate count of 6 for 'canonical_name fields' had already been resolved by a prior identical auto-discovery session (QA History entry timestamped 2026-07-28 07:26 UTC) before this session's own Tier 1 sweep ran -- verified live against BigQuery before doing any further work, since the doc entry could have been aspirational rather than applied (it wasn't: all 6 SKUs already carried the corrected text). This session's own fresh Tier 1 sweep over the full 192-row worklist found 17 new/different flagged rows: 9 were a single brand_dict casing defect (BRD-SG-00563 'ARISTO-CATS' vs the established 'Aristo-Cats' casing used everywhere in this category's own docs and taxonomy text, confirmed against the product's own packaging image, which reads 'Aristo-Cats'), 1 more was the same casing class for BRD-SG-00701 'snappy tom'/'Snappy Tom', 6 were canonical_name/size defects (missing pack-count suffix despite a correct stored pack_count, a truncated word fragment, or a genuinely ambiguous multi-line product wrongly holding a single pack_count with no suffix), and 1 was a brand mismatch between canonical_name text ('Amelisa') and the assigned brand_id's fuller registered name ('Amelisa Pet & Co') -- image-confirmed the fuller name is correct (packaging shows an 'AP&C' logo). Tier 2 GMV-prioritized sampling of the remaining un-flagged rows (covering roughly the top 50 by GMV plus several outlier-shaped rows further down) found: a duplicate taxonomy entry for the same real Aristo-Cats product line under two different sku_name phrasings; a maximally generic 'Royal Canin' stub duplicating the category's existing 'Royal Canin Assorted Care Lines' catch-all; a BelliFull Cat Strips listing whose sku_name's '20 for $5.90' bulk-pricing phrase was never converted to pack_count (image confirmed 15g/packet, so pack_count was corrected from 1 to 20); a Reflex entry whose canonical_name had dropped the real product line 'Plus' in favor of an unrelated 'MADE IN TURKEY' marketing fragment from a second reseller listing of the same product; and three entries (LDECO, babyKET, Amelisa) where the word 'Cat' had been silently dropped from canonical_name/product_line during original extraction even though each product's own sku_name explicitly describes it as a cat-and-dog product. STEP 2b (promo-language sweep) and STEP 2c (single-size/multiplier shared-bucket sweep) found no genuine defects this pass -- STEP 2b's hits were all regex false positives from 'free' appearing inside 'Freeze Dried'/'Grain-Free'/'Sugar-free'/'Lactose Free', or correctly-classified GWP mentions ('Buy 3 Free Cat Bowl', 'Free Treats'). | Fixed via bq DML, all with meta_agent='CLAUDE_CODE': (1) brand_dict.canonical_name corrected for BRD-SG-00563 and BRD-SG-00701; (2) canonical_name/size/product_line corrected in place on SKU-188396, 188402, 188492, 188430, 188480, 188532, 188411, 188521, 188465, 188532 (10 entries total, _meta reset to unreviewed on each); (3) product_taxonomy_map rerouted for two products: the Aristo-Cats duplicate into SKU-188395, and the bare Royal Canin stub into SKU-188504; (4) BelliFull Cat Strips (SKU-188445) pack_count corrected 1->20 with size set to 15g, confirmed via product image. 11 additional rows given genuine Tier 2 judgment and found already correct were bulk-promoted to review_confidence (SKU-188379, 188384, 188406, 188409, 188414, 188424, 188427, 188459, 188473, 188497, 188512) -- all land on 'unconfident' since this is each one's first-ever recorded review, per the two-agreeing-reviews-to-confident rule. Left open for a future session, not fixed here: SKU-188404's possible pack_count understatement (Sampling Bundle listing, image not yet checked); the pre-existing Rich Choice and Aristo-Cats cross-market brand_dict duplicates (need a human merge decision); the pre-existing 7-product coverage gap. Post-fix hard gates re-checked scoped to master_table='makanankucing_sg', all passing: G1 dual-mapped=0, G2 HUMAN+LLM coexistence=0, placeholder-leak=0, structured-fields-NULL%=24% (well under the 50% threshold), provenance=0, canonical_name-fields=0 (down from 6 pre-session), garbled-brand-text=0, duplicate product_id=0, duplicate product+taxon=0. Current state: 11 unconfident, 179 unreviewed (of 190 taxonomy entries now mapped to this category, down from 192 -- two entries, SKU-188422 and SKU-188412, are now orphaned stubs with zero mapped products after the reroutes and no longer join into this category's worklist). |
| 2026-07-28 08:47 UTC | Automated review session (auto-discovery) | STEP 1C fast-lane recheck on the 10 'fixed pending recheck' rows from the prior session found 9 clean and 1 regression: SKU-188411 'Reflex Plus 1.5kg' still carried a stale variant='MADE IN TURKEY' (a country-of-origin marketing fragment, not a genuine variant) left over from the prior session's canonical_name-only fix. Full Tier 1 sweep (STEP 2) over the whole 192-row worklist found no other flags. STEP 2b (promo-language) and STEP 2c (single-size/multiplier shared-bucket) found no genuine defects — STEP 2b's hits were all 'free' matching inside 'Freeze Dried'/'Grain-Free'/'Sugar-free' substrings or correctly-classified GWP mentions, consistent with the prior session's identical finding. Tier 2 GMV-prioritized sampling (39 rows, cross-checked against real sku_name text, roughly the top 40 by GMV among never-reviewed entries) found: (1) SKU-188357 'Royal Canin Intense Beauty Adult Gravy Wet Pouch 85g x12' was a D3 variant-collapse defect — 6 products with genuinely different Royal Canin lines (generic, Kitten Wet Range, Urinary S/O, Renal Care with Fish, Gastrointestinal, Intense Beauty) were all mapped to one entry named only for the Intense Beauty line, silently mislabeling clinical/specialty-diet products (Urinary S/O, Renal Care, Gastrointestinal) as a cosmetic line; (2) SKU-188376 'Fancy Feast 85g' (pack_count=1) was image-verified to be a Purina-official-store assorted-flavor listing with an ambiguous 24/48-unit bundle range, missing both the multi-variant flag and the pack-count ambiguity; (3) SKU-188381 had a text-corruption defect ('Treat and Dogs' — 'for Cats' silently dropped during original extraction); (4) SKU-188389 'Wellness CORE Tiny Tasters' incorrectly used is_multi_size=TRUE with size=NULL when the product's own sku_name states one fixed 50g size and the actual ambiguity ('12 Recipes') is a variant/flavor choice, not a size choice. Two additional ambiguous cases were found but left unfixed pending image verification: SKU-188354 'Ciao Churu 14g x50' bundles listings that read as a single 'Chicken Mix' flavor alongside listings that read as a buyer-choice selector across 4 flavor mixes — text alone can't confidently resolve whether this needs a split; SKU-188382 'Uncle Tails Staple Treat Sticks...50g' has an ambiguous per-stick vs per-box weight reading in its sku_name. | Bulk-promoted 9 of 10 fixed-pending-recheck rows to _meta review_confidence via STEP 1C (all landed 'unconfident' since their prior _meta carried no last_verdict to agree with, per the IF() logic re-reading each row's own prior state — expected, not a bug). Fixed SKU-188411 by clearing the stale variant field. Fixed the Royal Canin collapse by claiming SKU block SKU-193648-SKU-193847 (custom_targeted_qa_fix, 200 slots, 5 used) and minting SKU-193648 (generic Wet Food Pouch), SKU-193649 (Wet Range Kitten), SKU-193650 (Urinary S/O), SKU-193651 (Renal Care with Fish), SKU-193652 (Gastrointestinal), then rerouting the 5 corresponding product_taxonomy_map rows off SKU-188357, which now correctly holds only the genuine Intense Beauty product. Fixed SKU-188376 to is_multi_variant=TRUE, is_multi_size=TRUE (reused for the ambiguous 24/48 pack-count range per the th_softdrink NULL-pack_count-under-is_multi_size precedent), pack_count=NULL, canonical_name='Fancy Feast Assorted Flavors Wet Food 85g'. Fixed SKU-188381's canonical_name/product_line text corruption. Fixed SKU-188389 to size='50g', is_multi_size=FALSE, is_multi_variant=TRUE, canonical_name='Wellness CORE Tiny Tasters 50g' (caught a second gate re-check failure after the first fix omitted the size suffix from canonical_name, corrected before finishing). Bulk-marked 35 additional rows genuinely Tier-2-judged-and-found-correct via one UPDATE statement. Did not touch: SKU-188354 and SKU-188382 (flagged above, need image verification, not fixed this session); the pre-existing 7-product coverage gap, Rich Choice brand_dict duplicate, or Aristo-Cats cross-market brand duplicate (all explicitly out of scope per the category's Targeted QA Fix Brief, unchanged from prior sessions). Post-fix hard gates re-checked scoped to master_table='makanankucing_sg', all passing: G1 dual-mapped(LLM-scoped)=0, G2 HUMAN+LLM coexistence=0, placeholder-leak=0, canonical_name-fields=0, garbled-brand-text=0, structured-fields-NULL%=23% (under 50% threshold), provenance=0, duplicate product_id=0, duplicate product+taxon=0. Current _meta state: 55 unconfident, 131 unreviewed, 9 fixed_pending_recheck (of 195 taxonomy entries now mapped to this category, up from 190 due to the 5 new Royal Canin entries). |
| 2026-07-28 09:00 UTC | Automated review session (auto-discovery) | STEP1C fast-lane recheck: all 9 'fixed pending recheck' rows from the prior session came back Tier-1-clean, bulk-promoted. STEP2 Tier1 SQL sweep over the full 195-row worklist found zero mechanical defects (stub_leak, duplicate_brand, wrong_field_order, brand_casing_mismatch, excess_content, canonical_field_mismatch, null_size, garbage_brand all FALSE on every row) — this category has been thoroughly cleaned by prior sessions. STEP2b/STEP2c initially returned false zeros because the instructions' default join `s.ecommerce_platform = m.platform` silently matches nothing for this category: the source table stores the raw value 'Tiktok' while product_taxonomy_map stores the documented normalized 'TikTok Shop' (per this category's own doc header) — there is only one platform in this table so the fix was to join on (product_id, country) instead. Re-run with the corrected join: STEP2b's ~29 hits were all confirmed regex false positives ('free' matching inside 'Freeze Dried'/'Grain-Free'/'Sugar-free'/'Lactose Free' substrings) or correctly-classified GWP mentions ('Buy 3 Free Cat Bowl', 'FREE TREATS'), matching the prior two sessions' identical finding on this exact regex. STEP2c found 0 hits with the corrected join. Tier2 GMV-prioritized sample (54 rows spanning the top ~36K down to ~700 SGD/month GMV) re-confirmed correct; two previously-flagged-ambiguous, unresolved items with real GMV were resolved this session via image verification (curl+Pillow WebP-to-PNG+Read, same pattern as prior sessions): (1) SKU-188354 'Ciao Churu 14g x50' (~12.2K SGD/month, 7 products) was a genuine D3 variant-collapse — of the 7 mapped products, 1 ('CIAO Churu Chicken Mix (14g x 50)') states a single named flavor, the other 6 are buyer-choice assortments across multiple named Churu mixes ('Festive Pack', 'Chicken Mix/Tuna Mix/Seafood Mix/Tuna with Collagen & Fiber', etc.) or list additional non-flavor editions (Functional, Hairball) beyond simple flavor choice; (2) SKU-188382 'Uncle Tails Staple Treat Sticks Beef Chicken Tuna 50g' (~1.5K SGD/month, 2 products) — image for the OPTOPRIME listing ('[Buy 3 Free Cat Bowl]...50gr/pcs boxed...') shows retail boxes explicitly labelled 'WT 50g x 30', confirming this listing sells a 30-stick box, not a single 50g stick as the shared entry's pack_count=1 implied; the second product on this taxonomy_id (Moon Shadow Shop listing) has no box/count visible in any of its 4 images and was left unresolved per the aggregation-first rule (never write one product's confirmed count onto a bucket without independently confirming the other member). | STEP1C: bulk-promoted 9 rows via the standard _meta UPDATE (all landed 'unconfident' as first-ever recorded review). Claimed SKU block SKU-194120-SKU-194319 (200 slots, custom_targeted_qa_fix) and used 2: minted SKU-194120 'Ciao Churu Chicken Mix 14g x50' (variant='Chicken Mix') and rerouted product 1729779303209207720 to it; updated SKU-188354 to canonical_name='Ciao Churu Assorted Mixes 14g x50', is_multi_variant=TRUE, _meta reset to unreviewed, to correctly represent the remaining 6 buyer-choice-assortment products. Minted SKU-194121 'Uncle Tails Staple Treat Sticks Beef Chicken Tuna 50g x30' (pack_count=30, is_multi_variant=TRUE) and rerouted product 1734872705042515780 (OPTOPRIME) to it; left product 1730296553064858746 (Moon Shadow Shop) on SKU-188382 unchanged, unresolved, flagged below. Bulk-promoted 54 additional rows genuinely Tier-2-judged-and-found-correct via one UPDATE statement (per the two-agreeing-reviews rule; several reached 'confident' this pass since their prior session's judgment already recorded last_verdict='correct'). 198 slots of the claimed block remain unused/ACTIVE. Did not touch: the 7-product coverage gap, the Rich Choice brand_dict duplicate (BRD-MY-01046/BRD-MY-01113), or the Aristo-Cats cross-market brand duplicate (BRD-SG-00563/BRD-MY-01076) — all explicitly out of scope per this category's Targeted QA Fix Brief and unresolvable within a taxonomy-extraction session. Left open for a future session: product 1730296553064858746's true pack count under SKU-188382 (no box/count visible in any of its 4 images — genuinely unresolved, not guessed). Post-fix hard gates re-checked scoped to master_table='makanankucing_sg', all passing: G1 dual-mapped(LLM-scoped)=0, G2 HUMAN+LLM coexistence=0, placeholder-leak=0, garbled-brand-text=0, structured-fields-NULL%=22% (well under the 50% threshold), G5 provenance=0, duplicate product_id=0. Current _meta state: 119 unreviewed, 40 confident, 38 unconfident (of 197 taxonomy entries now mapped to this category, up from 195 due to the 2 new entries). Did not run universe refresh (STEP 8) or edit this category file/git (STEP 9). |
| 2026-07-28 09:21 UTC | Automated review session (auto-discovery) | STEP 1C fast-lane: 3 'fixed pending recheck' rows (SKU-194121, SKU-188354, SKU-194120) all Tier-1-clean, bulk-promoted. STEP 2 Tier 1 SQL sweep over the full 157-row worklist found 0 mechanical flags — this category has been thoroughly cleaned by 5 prior sessions. STEP 2b (promo-language) found 29 hits, all confirmed false positives ('free' inside 'Grain Free'/'Freeze Dried'/'Sugar-free'/'Lactose Free', or genuine GWP mentions). STEP 2c (shared-bucket single-size/multiplier) found 0 hits with the corrected (product_id, country) join — this category's product_taxonomy_map stores 'TikTok Shop' while the source table's ecommerce_platform stores raw 'Tiktok', so a literal (product_id, platform) join per the session brief's SCOPE note silently matches nothing for this single-platform category, per a prior session's documented finding. Tier 2 GMV-prioritized sample covered the top 100 of 157 worklist rows (spanning ~11.8K down to ~400 SGD/month GMV), cross-checked against real sku_name text and, for 5 genuinely ambiguous cases, product images (curl+Pillow WebP-to-PNG+Read). Found: (1) SKU-188398 Holuah! was a false single-size stub — image confirmed a genuine 14-flavor assortment with size varying 50-70g by flavor, not a fixed 70g; (2) SKU-188404 Sparkles and 5 other entries (SKU-188447, 188456, 188460, 188508, 188522) shared a systemic defect — sku_name states an ambiguous bundle-count range ('Bundle of X/Y', 'Xs/Ys/Zs') but pack_count was left at 1 instead of the established NULL+is_multi_size=TRUE ambiguous-quantity treatment (same class already fixed on SKU-188376/188415 by prior sessions); (3) SKU-188415 Fancy Feast Inspirations had this same defect, found independently via GMV sampling before the systemic sweep confirmed it; (4) SKU-188432 dropped Ciao's real product line 'Churu' in favor of generic tagline text ('Meal Topper Meow'), inconsistent with how every other Ciao Churu entry in this category names the line; (5) SKU-188444 Friskies Party Mix had size unresolved (is_multi_size=TRUE) despite sku_name stating one explicit, unambiguous weight (567g) for its single mapped product; (6) SKU-188449 Farmina used the generic category/format word 'KIBBLE' as product_line, violating the no-generic-category-word rule, when the sku_name genuinely has no real line/formula name at all; (7) SKU-188473 BelliFull was wrongly flagged is_multi_size=TRUE/size=NULL/pack_count=1 — image confirmed a fixed '50g x 2 in 1 box' with flavor-pair choice, not an ambiguous size or count; (8) SKU-188480 Sheba merged two genuinely distinct named lines (Melty=20 sticks, Moochie Puree=25 sticks per the sku_name) under one pack_count=20, silently misrepresenting the Moochie Puree option's real count — image confirmed these are two separate packaged products. Minor, lower-priority gaps identified but left unfixed (not touching their _meta): SKU-188382's true pack format for its one remaining Uncle Tails product (no box/count visible in the 2 images checked, consistent with a prior session's exhaustive 4-image check reaching the same unresolved conclusion); SKU-188410 Boneve and SKU-188464/188465's un-flagged variant text (a stated flavor/multi-flavor descriptor not mirrored into the variant field) — cosmetic Tier-B gaps, not blocking. | Fixed 13 taxonomy entries via bq DML UPDATE, all with meta_agent='CLAUDE_CODE' and _meta reset to unreviewed: SKU-188398 (size→NULL, is_multi_size/is_multi_variant→TRUE, canonical_name dropped the false '70g'), SKU-188404/188447/188456/188460/188508/188522/188415 (pack_count→NULL, is_multi_size→TRUE per the established ambiguous-bundle precedent; SKU-188508 also got is_multi_variant=TRUE for its stated '5 Flavours'), SKU-188432 (canonical_name/product_line corrected to include the real 'Churu' line name), SKU-188444 (size='567g' set from its sole product's unambiguous sku_name, canonical_name updated), SKU-188449 (product_line→NULL, canonical_name stripped of the generic 'Kibble' word), SKU-188473 (size='50g', pack_count=2, is_multi_size→FALSE, is_multi_variant→TRUE, canonical_name corrected per image), SKU-188480 (pack_count→NULL, is_multi_variant→TRUE, canonical_name dropped the misleading 'x20' suffix). Checked every replacement canonical_name against the full product_taxonomy table for collisions before writing (none found). Bulk-promoted 3 fixed-pending-recheck rows (STEP 1C) plus 83 rows genuinely Tier-2-judged and found correct (STEP 5) via single bulk UPDATE statements. Post-fix hard gates re-checked scoped to master_table='makanankucing_sg', all passing: G1 dual-mapped(LLM-scoped)=0, G2 HUMAN+LLM coexistence=0, placeholder-leak=0, structured-fields-NULL%=22% (well under the 50% threshold), duplicate product_id=0, provenance=0, garbled-brand-text=0. No new taxonomy entries minted this session (all fixes were in-place field corrections on existing SKUs), so no SKU block was claimed. Current _meta state: 66 confident, 68 unconfident, 50 unreviewed, 13 fixed_pending_recheck (of 197 taxonomy entries mapped to this category, unchanged count — no reroutes or new entries this session). Did not touch: the pre-existing 7-product coverage gap, Rich Choice brand_dict duplicate (BRD-MY-01046/BRD-MY-01113), Aristo-Cats cross-market brand duplicate (BRD-SG-00563/BRD-MY-01076) — all explicitly out of scope per this category's Targeted QA Fix Brief. Left open for a future session: SKU-188382's unresolved pack format, SKU-188410/188464/188465's minor un-flagged variant-text gaps (~57 rows below GMV rank 100 in the worklist not yet Tier-2-sampled this session — remain eligible next run, no _meta write since no genuine Tier 2 judgment was spent on them). Did not run universe refresh (STEP 8) or edit this category file/git (STEP 9). |
| 2026-07-28 09:38 UTC | Automated review session (auto-discovery) | STEP 1C fast-lane: 13 'fixed_pending_recheck' rows all came back Tier-1-clean, promoted to unconfident (first agreeing review, per the 13 true prior _meta values of {"is_reviewed": false} with no last_verdict to agree with). STEP 2 Tier-1 SQL sweep over the full 131-row worklist found zero mechanical flags (stub_leak, duplicate_brand, wrong_field_order, brand_casing_mismatch, excess_content, canonical_field_mismatch, null_size, garbage_brand all false/null on every row) -- this category has been thoroughly cleaned by 6 prior sessions. STEP 2b (promo-language, corrected to join on (product_id, country='SG') per this category's documented platform-value mismatch between source 'Tiktok' and product_taxonomy_map's 'TikTok Shop') found 24 unique hits, all confirmed false positives -- substring 'free' matching inside 'Freeze Dried'/'Grain-Free'/'Sugar-free', or genuine GWP 'FREE TREATS' mentions -- matching every prior session's identical finding on this exact regex. STEP 2c found one hit (SKU-188398, a '50-70g' range) which is an already-resolved genuine multi-size case from a prior session's image verification, not a new defect. Tier 2 review covered the entire 131-row non-confident worklist at the text level (canonical_name checked against real sku_name samples for D1-D5 structural issues) with targeted field-level follow-up (is_multi_size/size/pack_count verification against sku_name-stated buyer-choice ranges) on 23 rows where the pattern was ambiguous enough to need it (9 Royal-Canin/D'Zahara/Absolute-Holistic/Instinct-style buyer-choice-size-range entries, 5 buyer-choice-size/flavor entries, and re-verification of 9 previously-flagged bundle-range entries already fixed by a prior session) -- all 23 confirmed correctly structured. No new D1-D5 defects found beyond what six prior sessions already resolved; this category is now largely mature. Self-correction mid-session (caught by an independent advisor review before finalizing): an initial STEP 5 bulk-promotion mistakenly treated the whole 130-row non-confident worklist (minus one known-unresolved row) as 'genuinely Tier-2-judged' when only 27 rows (13 STEP1C + 14 field-verified) had actually received real judgment; the other 103 had only been eyeballed at the canonical_name/sample-sku_name level while scanning for candidates worth deeper investigation, which does not meet the bar for a _meta write. This also caused a rule violation: the 13 STEP1C rows, freshly promoted to unconfident, were re-read by the same buggy STEP5 pass and bumped straight to confident, skipping the required second independent review. Recovered the true pre-session _meta for all 130 affected rows via BigQuery time travel (FOR SYSTEM_TIME AS OF the timestamp immediately before this session's first write) and corrected: the 103 shallow-scanned rows restored verbatim to their true original _meta (untouched by this session, as they should be); the 13 STEP1C rows confirmed already correct at unconfident; 9 of the 14 field-verified rows corrected from unconfident to confident (their true prior state was already unconfident with last_verdict='correct', so this session's fresh agreement is the legitimate second consecutive agreeing review); the remaining 5 field-verified rows correctly left at unconfident (true prior state was unreviewed, so this is their first review). | Final _meta state: 75 confident (66 pre-existing + 9 newly confirmed via second agreeing review: SKU-188428, 188431, 188436, 188438, 188451, 188463, 188466, 188472, 188420), 77 unconfident (13 from STEP1C fast-lane + 5 newly first-reviewed: SKU-188496, 188499, 188505, 188514, 188528 + 59 restored to their true pre-session state), 45 unreviewed (all restored to their true pre-session state, including SKU-188382 which was never touched). No canonical_name/size/pack_count/product_line content was changed this session -- all writes were _meta bookkeeping only, later corrected to reflect only genuinely-performed review work. Post-fix hard gates re-checked scoped to master_table='makanankucing_sg', all passing: G1 dual-mapped (LLM-scoped)=0, G2 HUMAN+LLM coexistence=0, placeholder-leak=0, duplicate product_id=0. Did not touch (left open for a future session): SKU-188382's unresolved pack format (no box/count visible across multiple sessions' image checks -- genuinely unresolved, not guessed); a possible D3 gap on SKU-188491 (Aatas Cat Gold, sku_name states '2 Flavours Available' but is_multi_variant=FALSE -- flagged but not investigated, since this session could not confirm whether it's a genuine gap or this category's established accepted pattern for simple flavor-selector listings without a named formula); the pre-existing 7-product coverage gap, Rich Choice brand_dict duplicate (BRD-MY-01046/BRD-MY-01113), and Aristo-Cats cross-market brand duplicate (BRD-SG-00563/BRD-MY-01076) -- all explicitly out of scope per this category's Targeted QA Fix Brief. Did not run universe refresh (STEP 8) or edit this category file/git (STEP 9). |
| 2026-07-28 09:48 UTC | Automated review session (auto-discovery) | STEP1C: 0 fixed-pending-recheck rows existed (clean carryover from prior session). STEP2 Tier-1 SQL sweep over the full 122-entry worklist found 0 mechanical flags -- category remains thoroughly clean. STEP2b (promo-language, joined on (product_id, country='SG') per this category's documented platform-value mismatch between source 'Tiktok' and product_taxonomy_map's 'TikTok Shop') found only confirmed regex false positives ('free' matching inside 'Freeze Dried'/'Grain Free'/'Sugar-free') or genuine GWP mentions, matching every prior session's identical finding. STEP2c found 1 hit (SKU-188398, a '50-70g' range) that is an already-resolved genuine multi-size case, not a new defect. A full-worklist Tier 2 text-level review (every entry's canonical_name/product_line/variant/size/pack_count cross-checked against its actual sku_name, not just a sample) found: (1) SKU-188494 'Friskies Party Mix Treats 60g' -- sku_name explicitly states '(Pack of 5)' but pack_count was left at 1 with no 'x5' suffix in canonical_name; missed by every automated sweep since '(Pack of 5)' doesn't match any Tier-1/2b/2c regex pattern. (2) SKU-188532 'Amelisa Pet & Co Rawly Stick...16g x22' -- sku_name states an ambiguous dual pack option '16gx22 / 16gx25' but canonical_name/pack_count had locked in only the 22-count, missed by 2 prior sessions that fixed this entry's text/brand but not its count ambiguity. Also resolved a previously-open flag from the 08:47 UTC session: SKU-188491 Aatas Cat Gold's '2 Flavours Available' with is_multi_variant=FALSE was confirmed correct, not a D3 gap -- 5 other entries in this same worklist (Unicharm, Wanpy, Wellness CORE, Schesir, Aatas Cat Creme) show the identical established pattern of leaving is_multi_variant=FALSE for unnamed flavor-count callouts, reserving the flag for genuinely *named* buyer-choice lines. Left open, not fixed: SKU-188511 Royal Canin Kitten's sku_name also mentions 'MOTHER & BABY CAT READY STOCK!', which may indicate an uncaptured 4th buyer-choice line, but the raw text is ambiguous enough (could be boilerplate) that a blind edit risked an incorrect claim -- needs an image check a future session should do; low GMV (~176 SGD) so not prioritized further here. SKU-188382's pack format remains unresolved, consistent with 3 prior sessions' identical conclusion (no box/count visible across multiple image checks). Did not touch: the pre-existing 7-product coverage gap, Rich Choice brand_dict duplicate (BRD-MY-01046/BRD-MY-01113), or Aristo-Cats cross-market brand duplicate (BRD-SG-00563/BRD-MY-01076) -- all explicitly out of scope per this category's Targeted QA Fix Brief. | Fixed 2 taxonomy entries via bq DML UPDATE, both with meta_agent='CLAUDE_CODE' and _meta reset to unreviewed: SKU-188494 (pack_count 1->5, canonical_name -> 'Friskies Party Mix Treats 60g x5'); SKU-188532 (pack_count 22->NULL, is_multi_size FALSE->TRUE, canonical_name -> 'Amelisa Pet & Co Rawly Stick Pet Puree for Cat & Dog Premium Treats 16g', dropping the falsely-precise 'x22' suffix per the established ambiguous-bundle-count convention already used elsewhere in this category). Checked both replacement canonical_names against the full product_taxonomy table for collisions before writing (none found). Bulk-promoted 118 taxonomy entries genuinely Tier-2-judged and found correct this session via one STEP5 _meta UPDATE statement. No new taxonomy entries minted (both fixes were in-place field corrections), so no SKU block was claimed. Post-fix hard gates re-checked scoped to master_table='makanankucing_sg', all passing: dual-mapped(LLM-scoped)=0, HUMAN+LLM coexistence=0, duplicate product_id=0, placeholder-leak=0, provenance gaps=0. Final _meta state: 151 confident, 42 unconfident, 4 unreviewed (of 197 taxonomy entries mapped to this category, unchanged count -- no reroutes or new entries this session). Did not run universe refresh (STEP 8) or edit this category file/git (STEP 9). |
| 2026-07-28 10:10 UTC | Automated review session (auto-discovery) | STEP1C fast-lane: 2 fixed-pending-recheck rows (SKU-188494, SKU-188532) both Tier-1-clean, bulk-promoted. STEP2 Tier-1 SQL sweep over the full 44-row worklist found 0 mechanical flags -- category remains thoroughly clean after 8 prior sessions. STEP2b (promo-language, joined on (product_id, country='SG') per this category's documented platform-value mismatch between source 'Tiktok' and product_taxonomy_map's 'TikTok Shop') found 7 hits, all confirmed regex false positives ('free' matching inside 'Freeze Dried'/'Grain Free'/'Sugar-free'/'Lactose Free' substrings). STEP2c found 0 hits. A full-worklist Tier-2 text-level review (every entry's canonical_name/product_line cross-checked against its own sku_name; all 44 remaining entries are single-product, non-catch-all taxonomy_ids) found 4 genuine defects: (1) SKU-188516 Singapaw -- extraction silently dropped the word 'Cats' from 'for Dogs and [Cats] Premium Rich Milk...', a text-corruption defect matching the same class multiple prior sessions found elsewhere in this category. (2) SKU-188534 Ciao -- extraction dropped the real product line word 'Churu' (sku_name explicitly says 'Ciao Churu Jumbo Mix...'), the identical defect class already fixed on sibling entries SKU-188432/188354/194120 by prior sessions. (3) SKU-188506 Aristo-Cats -- image-verified (curl+Pillow WebP-to-PNG+Read): canonical_name/product_line 'big bag Aristo' was a garbled fragment, not the real on-label line; packaging actually reads 'Aristo-Cats YI·HU' across 3+ visible flavor variants (Fish Ahoy, Mackerel & Chicken, Tuna & Chicken), and the multi-variant nature was never flagged. (4) SKU-188511 Royal Canin Kitten -- image-verified: confirms a genuine 4th buyer-choice line 'Mother & Babycat' visible on packaging (a prior session, 2026-07-28 09:48 UTC, had flagged this exact ambiguity as needing an image check but left it unresolved due to low prioritized GMV; this session's image check resolved it as a real gap, not boilerplate marketing text). 40 of the 44 worklist rows were genuinely Tier-2-judged and found already correct. | Fixed 4 taxonomy entries via bq DML UPDATE, all with meta_agent='CLAUDE_CODE' and _meta reset to unreviewed (pending next session's recheck): SKU-188516 (canonical_name/product_line corrected to restore 'Cats'), SKU-188534 (canonical_name/product_line corrected to restore 'Churu'), SKU-188506 (canonical_name -> 'Aristo-Cats YI·HU Assorted Flavours 7.5kg', product_line -> 'YI·HU Assorted Flavours', is_multi_variant -> TRUE), SKU-188511 (canonical_name -> 'Royal Canin Kitten (British Shorthair/Persian/Maincoone/Mother & Babycat) 2kg', product_line updated to match). Checked all 4 replacement canonical_names against the full product_taxonomy table for collisions before writing (none found). Bulk-promoted 2 STEP1C fast-lane rows plus 40 genuinely Tier-2-judged-correct rows to _meta review_confidence via two bulk UPDATE statements (per the two-agreeing-reviews-to-confident rule). No new taxonomy entries minted (all fixes were in-place field corrections), so no SKU block was claimed. Did not touch: the pre-existing 7-product coverage gap, Rich Choice brand_dict duplicate (BRD-MY-01046/BRD-MY-01113), or Aristo-Cats cross-market brand duplicate (BRD-SG-00563/BRD-MY-01076) -- all explicitly out of scope per this category's Targeted QA Fix Brief. Post-fix hard gates re-checked scoped to master_table='makanankucing_sg', all passing: G1 dual-mapped(LLM-scoped)=0, G2 HUMAN+LLM coexistence=0, placeholder-leak=0, structured-fields-NULL%=22% (well under the 50% threshold), duplicate product_id=0, duplicate product+taxon=0, garbled-brand-text=0. Current _meta state (per mapped product row): 234 confident, 1 unconfident, 4 fixed_pending_recheck (the 4 just-fixed rows). Did not run universe refresh (STEP 8) or edit this category file/git (STEP 9). |
| 2026-07-28 10:14 UTC | Automated review session (auto-discovery) | STEP1C fast-lane promoted 2 fixed-pending-recheck rows (SKU-188532, SKU-188494), both Tier-1-clean. STEP2 Tier-1 SQL sweep over the full 44-row worklist found 0 mechanical flags. STEP2b/2c (joined on (product_id, country='SG') per this category's documented source-vs-map platform-value mismatch) found only confirmed regex false positives ('free' inside 'Freeze Dried'/'Grain-Free'/'Sugar-free') and 0 shared-bucket hits, matching every prior session. A full-worklist Tier 2 text review plus targeted image verification (curl+Pillow WebP-to-PNG+Read) resolved a defect 4 prior sessions had left unresolved: SKU-188382 'Uncle Tails Staple Treat Sticks...50g' (pack_count=1) — its box image explicitly reads 'Cat Pet Food Pouch 30in1...WT 50g x 30', confirming this is the same 30-stick product as the existing SKU-194121 minted by an earlier session for an identical listing. Also image-verified SKU-188538 'Prof. Bengal Bye Bye Floof' — packaging reads '20 Sachets, Net Wt: 10g X 20 Sachets (200g)', showing the entry's is_multi_size=TRUE/size=NULL was wrong (it's a fixed single-SKU size never actually ambiguous, just unstated in sku_name text). Also image-verified SKU-188511 'Royal Canin Kitten (British Shorthair/Persian/Maincoone)' — the listing's 'MOTHER & BABY CAT' text (previously left ambiguous across 2 sessions) is a genuine 4th distinct buyer-choice product line visible in the image, not boilerplate. A concurrent parallel session was found actively reviewing this same category mid-run (detected via updated_at timestamps 04:49-10:09 UTC on rows I had just read) — it independently fixed SKU-188511 (same finding, already applied before I wrote), SKU-188516 (restored a dropped 'Cats' word), SKU-188506 (added a real packaging-printed 'YI·HU' sub-line name, which I independently image-verified as genuine rather than fabricated), and SKU-188534 (added 'Churu' to the line name), and bulk-promoted nearly all other worklist rows to confident via its own STEP5 pass. | Rerouted SKU-188382's sole product (1730296553064858746) to the existing SKU-194121 via product_taxonomy_map UPDATE (no new entry minted, meta_agent='CLAUDE_CODE') — SKU-188382 is now an orphaned stub with 0 mapped products. Fixed SKU-188538 via bq DML: size='10g', pack_count=20, is_multi_size=FALSE, canonical_name updated to end '10g x20', _meta reset. Added is_multi_variant=TRUE to SKU-188511 for consistency with the established multi-line-bundle pattern (the concurrent session's fix had left this flag unset), _meta reset. Re-verified live state immediately before every write throughout to avoid clobbering the concurrent session's work — checked canonical_name collisions before each write, none found. Bulk-promoted 5 fixed-pending-recheck rows (SKU-188506, 188511, 188516, 188534, 188538) to _meta via one STEP5 UPDATE after independently re-confirming each was clean (Tier-1 sweep + text/image cross-check) — all landed 'unconfident' as the first-ever agreeing review on their current content. Post-fix hard gates all pass scoped to master_table='makanankucing_sg': G1 dual-mapped(LLM-scoped)=0, G2 HUMAN+LLM coexistence=0, placeholder-leak=0, duplicate product_id=0, G5 provenance=0, structured-fields-NULL%=22% (under 50% threshold). Final _meta state: 191 confident, 5 unconfident, 0 unreviewed, 0 fixed_pending_recheck (of 196 taxonomy entries now mapped to this category, down from 197 due to SKU-188382 becoming an orphaned stub after its product was rerouted). Did not touch: the pre-existing 7-product coverage gap, Rich Choice brand_dict duplicate (BRD-MY-01046/BRD-MY-01113), or Aristo-Cats cross-market brand duplicate (BRD-SG-00563/BRD-MY-01076) — all explicitly out of scope per this category's Targeted QA Fix Brief. No new taxonomy entries minted this session (the one fix that needed a target reused an existing entry), so no SKU block was claimed. Did not run universe refresh or edit this category file/git. |
---

## Targeted QA Fix Brief

> Scope for a future `targeted_qa_fix.sh` / top-up session — not executed this session.

**Verdict:** D6 in-scope NULL coverage gap (small) + two flagged data-quality items needing human
judgment, not extraction work.

- **Coverage gap** (7 products, ~9.3K SGD GMV, updated 2026-07-28 after a top-up session's image check
  resolved 5 of the original 12): the remaining 7 are genuinely unresolvable — reseller
  watermark/composite-marketing-image only (no packaging-printed brand visible), a confirmed cross-brand
  bundle, or a confirmed merchant-name leak. See Scope section for the full list. Not candidates for
  further image-based resolution — this session already checked every one of them against its image.
- **`Rich Choice` brand_dict duplicate**: `BRD-MY-01046` and `BRD-MY-01113` are both active,
  identically-named entries. Needs a merge decision (which ID is canonical, re-point any dependent
  rows) — out of scope for a taxonomy-extraction session to resolve unilaterally.
- **`Aristo-Cats` cross-market brand duplicate**: `BRD-SG-00563` (used by this category) and
  `BRD-MY-01076` (used by `makanankucing_my`) are almost certainly the same real-world brand. Same
  merge-decision caveat as above.
- **Below-661-GMV long tail products not yet reviewed for line-name noise**: a handful of low-GMV
  entries (e.g. `Furvit Cat Candy Nutri Lick 5g s 5g x40`, `Amelisa Rawly Stick... 16g 16g...`, `Sheba
  Melty Moochie Puree Puree Treats`) retain minor regex-stripping artifacts (duplicate tokens, a stray
  trailing `s`) — cosmetic, don't trip the placeholder-leak gate, but a precision pass could clean them
  up.

---

## Scripts

No committed pipeline scripts for this category — extraction was done directly (multimodal reading +
ad hoc SQL/Python, not a `pipeline/05_product_taxonomy/llm_{table}/` script) per this session's brief
for this custom, non-NIQ source table.

---

## Map Row Counts (as of the 2026-07-28 top-up session)

| Source | Count | Notes |
|--------|-------|-------|
| LLM | 239 | 234 from Full Rebuild (229 bulk text-matched, 5 image-verified) + 5 from the 2026-07-28 top-up session (all image-verified) |
| HUMAN | 0 | No prior keyword-seed pass for this category |
| NULL (unmapped, in-scope, live-worklist gap) | 7 (~9.3K SGD) | Genuinely unresolvable after both text and image checks — see Scope section |
| NULL (out-of-scope / below 95% GMV threshold) | 625 | Long-tail resellers below the GMV cutoff — may legitimately remain unresolved per `docs/quality-standards.md` §2 |
